# M5.E5 — Progress (v0.1.10 carry-over bug squash)

Dispatch is **sequential** (one fresh executor per task, atomic commit between) for shared-tree safety; waves are checkpoint boundaries. Suite baseline at Epic open: 1529.

## Wave 1 — gate-touching fixes
- [x] **T1 (B24)** — dangling-delta resolved-target re-key — commit `501ad9d` (suite 1529 → 1533; AC1.1–AC1.6 met; multiset test verified to fail against a `Set` swap).
- [x] **T2 (B26)** — Epic-close STATE fallback — commit `d62e5f2` (suite 1533 → 1549; AC2.1/2.2/2.3/2.5/2.6/2.7 met). **AC2.4 (Layer 3) descoped + ratified** — `detectDirtyExecute` runs at EXECUTE, the fallback exists at SHIP (empty intersection = no-op); B26 harm fully closed by Layers 1+2. Enhancement captured in `ISSUES-INBOX.md`.

### Checkpoint: after Wave 1
- [ ] Full suite green (T1+T2 together); validate + lint clean → **verifying now**.

## Wave 2 — clean fixes
- [ ] **T3 (B25)** — FR5 read-enclosure interleaving test + `_afterRead` seam (all 6 wrappers) — pending.
- [ ] **T4 (B6)** — `isStateStale` file-identity tightening (`BOOKKEEPING_PATHS`) — pending.

### Checkpoint: complete
- [ ] All AC1.*–AC4.* met (AC2.4 struck); suite green; validate + lint clean; double-run deterministic; BUGS.md reconciled.

## Findings surfaced during EXECUTE
- **B27** (`needs-triage`, P3) — archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts migrate (fail-safe but blocks stranger repos; R7 tension). Surfaced in T1; catalogued in `BUGS.md`. Out of scope — awaits a design call.
- **B26 Layer-3 descope → enhancement** — resume-time retro nudge at SHIP; captured in `ISSUES-INBOX.md`.

## Last Updated
2026-07-21 (EXECUTE — Wave 1 complete, verifying checkpoint)
