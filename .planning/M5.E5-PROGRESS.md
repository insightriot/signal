# M5.E5 — Progress (v0.1.10 carry-over bug squash)

Dispatch was **sequential** (one fresh executor per task, atomic commit between) for shared-tree safety; waves were checkpoint boundaries. Suite: 1529 (open) → **1561** (+32 net new tests).

## Wave 1 — gate-touching fixes
- [x] **T1 (B24)** — dangling-delta resolved-target re-key — `501ad9d` (1529 → 1533; AC1.1–AC1.6; multiset test verified to fail against a `Set`).
- [x] **T2 (B26)** — Epic-close STATE fallback (Layers 1+2) — `d62e5f2` (1533 → 1549; AC2.1/2.2/2.3/2.5/2.6/2.7). **AC2.4 / Layer 3 descoped + ratified** (no-op: Layer 3 at EXECUTE, fallback at SHIP); harm closed by Layers 1+2; enhancement → `ISSUES-INBOX.md`.

### Checkpoint: after Wave 1 — ✅ PASSED
- [x] Suite 1549 green; validate + lint clean. Committed `d3a676c`, pushed.

## Wave 2 — clean fixes
- [x] **T3 (B25)** — read-enclosure interleaving test + `_afterRead` seam (all 6 wrappers) — `8cbb8b7` (1549 → 1557; AC3.1–AC3.6; discriminator proven fail-on-broken / pass-on-real). `planning-index.js` (7th wrapper) intentionally not seamed — out of `RMW_PATHS`, covered indirectly (justified divergence).
- [x] **T4 (B6)** — `isStateStale` file-identity tightening (`BOOKKEEPING_PATHS`) — `35f6a04` (1557 → 1561; AC4.1–AC4.8; AC4.2/4.3 RED→GREEN). `STATE_AFFECTING_PATHS` byte-unchanged.

### Checkpoint: complete — ✅ PASSED
- [x] All AC met (AC2.4 struck); suite **1561** green on a **deterministic double-run**; validate + lint 0 errors; `BUGS.md` reconciled (B24/B25/B26 → fixed, B6 refinement noted, B27 catalogued).

## Findings surfaced during EXECUTE
- **B27** (`needs-triage`, P3) — archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts migrate (fail-safe but blocks stranger repos; R7 tension). In `BUGS.md`. Out of scope — awaits a design call.
- **B26 Layer-3 → enhancement** — resume-time retro nudge at SHIP; in `ISSUES-INBOX.md`.

## EXECUTE complete — next: VERIFY
All 4 tasks landed, sequential + RED-first, no false-greens. Two executors correctly STOPPED/flagged rather than fake past a wall (T2's Layer-3 descope; T3's scope-divergence + coverage-precision).

## Last Updated
2026-07-21 (EXECUTE complete)
