# M5.E8 — Verification

**Epic:** M5.E8 — the measurement foundation · **Tier:** FULL (`nyquist_enforcement: strict`)
**Date:** 2026-07-28 · **Commit:** `5649cbf` · **Suite:** 1652 → **1730 tests**, validator green, eslint clean

---

## Verdict: **PASS**, with one criterion outstanding by design and one honest limit restated

28 acceptance criteria mapped in `M5.E8-VALIDATION.md`. **27 met. 1 (AC5.2, second half) lands at
SHIP** — it requires the ceiling in the Epic's *retrospective*, which does not exist until then.

---

## FR1 — the harness (AC1.1–AC1.4)

| AC | Verdict | Evidence |
|---|---|---|
| AC1.1 | **PASS** | `captureTrace` records planning-file digests, STATE fields and commits; `diffTraces` reports the delta. Exercised live in 20+ real runs. |
| AC1.2 | **PASS** | `tests/adherence-suite-guard.test.js` — no test imports or spawns `tools/adherence-run.js`; the mechanics module contains no `-p`/`--print`. |
| AC1.3 | **PASS** | `assertIsolatedFixture` refuses the invoking project, anything inside it, anything containing it, and anything outside the OS temp dir. Four refusal cases tested. |
| AC1.4 | **PASS — verified end-to-end** | With `claude` genuinely off `PATH`, the entry point aborts *"This is not a measurement of zero — no measurement was taken"* and exits **1**, **before any fixture is built**. Not only unit-tested. |

## FR2 — the control run (AC2.1–AC2.4)

| AC | Verdict | Evidence |
|---|---|---|
| AC2.1 | **PASS** | Two arms run against freshly-built, identical fixtures. Live: 3 treatment + 3 control. |
| AC2.2 | **PASS** | `resolveVerdict` returns `inert` when the trace is present in both arms; unit-tested, and the `inert` path was **live-exercised** by the broken control arm before it was fixed. |
| AC2.3 | **PASS** | `summarizeArm` refuses fewer than two runs; `planCost` refuses `runsPerArm < 2`. Spread reported per arm (`unanimous` / `SPLIT`). |
| AC2.4 | **PASS — checked against the tree** | `git status commands/` is **clean** after 20+ mutated runs; the real command files are byte-identical. Mutation applies only to a temp copy. |

## FR3 — the canary set (AC3.1–AC3.3)

| AC | Verdict | Evidence |
|---|---|---|
| AC3.1 | **PASS** | `references/adherence-canaries.json` — auditable, with prose `$comment` fields explaining the rule and the choice. |
| AC3.2 | **PASS** | `B41-phase-entry` is the registry's first and only entry, and is the instruction that was measured. |
| AC3.3 | **PASS, and extended** | Every canary declares `trace` + `declaredAt` before any run, asserted by test. **Extended beyond the AC:** the *verdict threshold* is fixed in the same file for the same reason (D-M5E8-5) — AC3.3's logic applied to the thing AC3.3 did not cover. |

## FR4 — the record (AC4.1–AC4.3)

| AC | Verdict | Evidence |
|---|---|---|
| AC4.1 | **PASS** | Each record carries date, commit, CLI version + model, per-arm hits/runs, unanimity, failed runs, and the seam precondition. |
| AC4.2 | **PASS — structural, not procedural** | `appendRunRecord` is a pure concatenation: **no code path reads an existing record**, so none can rewrite one. Ten-append test; ceiling-block-survives test. |
| AC4.3 | **PASS** | A run is repeatable from the record (canary id, command, runs per arm, surface, commit). `--combine` **refuses** to pair arms differing on commit, CLI version, model, or run count. |

## FR5 — the ceiling (AC5.1–AC5.3)

| AC | Verdict | Evidence |
|---|---|---|
| AC5.1 | **PASS** | `tools/lib/directive-classifier.js` — split rule written out in full in the header, pinned against a hand-labelled fixture. **407 directives, 91 measurable (22.4%), 316 not.** |
| AC5.2 | **PARTIAL — by design** | Published in `.planning/ADHERENCE-LOG.md`. ⏳ **The retrospective half lands at SHIP**, because the retro does not exist yet. Carried forward explicitly rather than silently. |
| AC5.3 | **PASS** | The log states the remainder is *unmeasured, not passing*, and a **wording test** pins the sentence. |

## FR6 — the ship anchor (AC6.1–AC6.3)

| AC | Verdict | Evidence |
|---|---|---|
| AC6.1 | **PASS** | `commands/ship.md` § 1, adjacent to the `docs/map` line (adjacency asserted). |
| AC6.2 | **PASS** | Checklist, not trigger (D-M5E8-3 / `B39`). |
| AC6.3 | **PASS — labelled** | Test asserts presence only, and says so in its own header: it cannot prove a maintainer acts on it. M5.E9's precedent. |

## Non-functional

| NFR | Verdict | Evidence |
|---|---|---|
| NFR1 | **PASS** | 1730 tests run offline and free. `tools/audit-network-calls.js` exits 0. No test spawns `claude` (the three `.claude` grep hits are directory paths). |
| NFR2 | **PASS** | Cost preview precedes every spend path, including `--dry-run`; interactive confirm unless `--yes`. |
| NFR3 | **PASS** | Failed/timed-out runs are counted and force `indeterminate`. The CLI-absent path aborts rather than reporting a number. |
| NFR4 | **PASS** | `inert` and `indeterminate` are first-class recorded results. The `inert` gloss states in the log that it is a finding, never retried until it passes. |

**FULL-tier ops checklist** — health probes, graceful shutdown, request logging, security headers, rate
limiting: **N/A**, local CLI harness, no server, no listener. Recorded rather than skipped, per the spec.

---

## The live result

```
canary       B41-phase-entry
as-written   3/3  unanimous
deleted      0/3  unanimous
failed runs  0
VERDICT      OBEYED
```

**M5.E9's phase-entry fix works.** It shipped in v0.1.12 unverified — labelled as such in its own
test header — and is the first instruction this harness measured.

---

## What VERIFY is NOT asserting, restated because a green suite is the thing this Epic exists to stop being misread

1. **The suite does not verify the harness's answers about real agents** (`VALIDATION` F2). Every
   suite-level test of the control arm uses fixed inputs. A green run proves arithmetic. The
   `OBEYED` verdict above rests on **live runs**, recorded in `ADHERENCE-LOG.md` — not on the suite.
2. **The ship anchor is presence, not obedience** (AC6.3).
3. **77.6% of Signal's own instructions remain unmeasured** — *not passing*.
4. **One canary is not a survey.** `OBEYED` is a fact about `B41`'s phase-entry rule at commit
   `f3ca9b2` on `claude 2.1.220 / claude-opus-5`. It is **not** evidence that Signal's instructions
   are followed generally, and the log must never be read that way.

---

## Findings raised during EXECUTE, carried into REVIEW

- **`B48`** (`confirmed`, **P2**) — `execute.md`'s phase-entry instruction is unconditional, and an
  agent **explicitly refused** it rather than write a false record into the phase log. Affects all
  four commands M5.E9 changed. **Live in shipped v0.1.12.**
- **Two instrument defects caught before they produced a published verdict**, both disguised as
  legitimate findings: an unreachable fixture (`ABSENT`), and a control arm that deleted one line
  from a section restating the instruction three more times (near-miss `INERT`). Both are now
  guarded by tests. **REVIEW should ask what else wears that costume.**
- **`package.json` says `0.1.11`; the plugin manifest says `0.1.12`.** Out of this Epic's scope —
  flagged for cataloging, not fixed here.
