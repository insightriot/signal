# M6.E1 — PROGRESS

Plan: `M6.E1-PLAN.md` · Branch: `epic/m6-e1-plugin-payload`

## Wave 1 — S1: prove the geometry (nothing moves)

- [x] **S1.t1** — `boundPluginRoot()` proven at the post-move depth — commit `c634803`
- [x] **S1.t2** — `roster.js` `ROOT` proven, incl. reading the roster from the derived root — commit `c634803`
- [x] **S1.t3** — the payload-boundary detector — commit `(this slice)`
- [x] **S1.t4** — the detector's population and blind spots published (`AC1.2a`) — commit `(this slice)`

**Suite:** 2602 → **2615** (+13).

### Verdict: the geometry holds

Both module-path expressions resolve to the **plugin root** at the post-move depth, and both are
proven **depth-sensitive** — a wrong-depth fixture yields a different answer, so the assertions
cannot pass for a function that ignores depth. `roster.js` reads `commands/`, `agents/` and `skills/`
from the root it derived, pinned with a **decoy** at the old location so a regression to the repo root
cannot pass silently.

**S2 is therefore unblocked on its stated gate.**

### The finding: the detector shipped as a false green, and only mutation-testing found it

`AC1.2a` asked for the detector's population and blind spots to be published. Writing that section is
what prompted actually testing the detector, and the detector was **broken in the exact way the AC
exists to catch**.

The first `escapes()` was the obvious one — `relative(PAYLOAD_ROOT, abs).startsWith('..')`. It reads
correctly. It is **structurally incapable of firing before S2**: today the payload root *is* the
repository root, so `../../tests/fixtures/nope.js` resolves to a path *inside* it. A deliberately
escaping import planted in `tools/lib/` **passed clean**.

**It would have reported "no escapes" for the whole pre-move period and then been trusted after the
move** — a check whose green means *could not look*, guarding this Epic's central invariant. `B39`'s
class, inside the check written to enforce the boundary.

**The fix is a different rule, not a wider one:** the payload boundary is the **set of payload
directories**, not the root that happens to contain them. Post-move the two coincide; pre-move they
do not, and pre-move is the case that has to work for the check to have value before S2 lands.

**Verified three ways** rather than declared: clean tree passes → escaping probe fails with the file
and specifier named → probe removed, green returns.

### Two smaller things the slice turned up

1. **My own path arithmetic was wrong first.** The hooks-geometry test asserted `dirname` twice where
   the payload root is three up; it failed on first run and caught its author. Kept as a comment
   rather than silently corrected.
2. **The citation population is 31, not the 12 I measured at PLAN.** The PLAN-time grep covered
   `commands/` and `agents/` only; the detector walks all six payload directories, and `skills/` and
   `references/` cite more. **Roughly nine of the 31 are false positives by design** — a skill telling
   a *user* to keep ADRs in `docs/decisions/`, an example naming `tests/path/to/test.ts`. Rule 3
   matches text shaped like a path and cannot tell Signal's repo from the user's repo from an
   example, which is why it is a **ratchet over a recorded set** ("nothing new appeared" — sound under
   a false-positive rate) rather than a prohibition ("every match is a defect" — not sound).

## Wave 2 — S2: move the payload · **COMPLETE**

- [x] **S2.t1** — `git mv` the payload into `plugin/` — commit `3b1e5da`
- [x] **S2.t2** — repoint the test imports (133 files) — this slice
- [x] **S2.t3** — build / lint / vitest config — this slice
- [x] **S2.t4** — `validate-plugin.js` ROOT (`AC1.5`) — this slice
- [x] **S2.t5** — detector clean against the moved tree — this slice

**Suite: 2616 passing.** `npm run build`, `lint`, `validate` all green. Payload **2,004 KB** of an
11,032 KB repo.

### What the move cost, against what the plan predicted

The plan said *"repoint the 136 test imports"*. That was the smallest part. The failure count went
**137 → 0** across roughly a dozen passes, and the work split into five kinds the plan did not name:

1. **The nine maintainer scripts broke.** They stayed at `tools/` and import `./lib/` — which moved
   out from under them. 27 imports across 8 scripts.
2. **Two shipped libraries hardcoded moved paths.** `doc-hygiene`'s `readPluginVersion` and
   `VERSION_SOURCES`, and `checkRosterCounts`, which counted the roster from the same `baseDir` it
   read the count *sites* from — one directory before, two now.
3. **`README.md` carried two dead links** into `references/`. Real user-visible breakage on GitHub,
   not a test artifact.
4. **Test path-construction had six distinct forms**, not one: quoted specifiers, `join(ROOT, 'x')`,
   `join(ROOT, 'x/y.md')`, `join(__dirname, '..', 'x')`, `join(import.meta.dirname, ...)`, and
   `new URL('../../' + rel)`. Each needed its own pass, and each pass revealed the next.
5. **`state/` was missing from the payload list entirely** — see S2.t1.

### The distinction the move forced into the open

**There are two roots now, and code that conflated them was correct only by coincidence.**
`tests/helpers/roots.js` names it once — `PLUGIN_ROOT` vs `REPO_ROOT`, plus `resolveSignalPath()` for
artifacts that mix both in a single list. The adherence apparatus list is the clearest case:
`tools/lib/adherence-log.js` ships and `tools/adherence-run.js` does not, **two lines apart**.

`checkRosterCounts` is the one worth remembering: given the repo root it counted **0 commands, 0
agents, 0 skills** and reported every documentation site as a mismatch. It failed **loudly**, which
is the only reason it cost minutes. The same bug in a check that fails quietly is `B39`.

### Three assertions changed on purpose, not to get to green

Each was testing a fact the move legitimately changed, and each carries its reasoning in place:

- **`boundPluginRoot() === REPO`** → `=== join(REPO, 'plugin')`. This is `B52`'s mechanism, and the
  plugin root is the value it actually wants; equality with the repo root was a coincidence of layout.
- **`validate-plugin.js`'s `REQUIRED_FILES`** mixed payload with repository artifacts.
  `marketplace.json` and two launch docs have **never** been in a user's install; pointing `ROOT` at
  the plugin is what made the mixture visible. Split into two lists.
- **The plugin copy no longer contains `tools/adherence-run.js`** — and that is `AC2.2` arriving. The
  original comment argued the script had to be copied *because `ship.md` orders it*. The honest fix
  is the other direction: `ship.md`'s line is now marked a **Signal-repository step**. The
  instruction and the tree agree again by amending the instruction, not by shipping a script to
  satisfy it.

## Wave 3 — S3: the dependency · **COMPLETE**

- [x] **S3.t1** — decided: **carry `yaml`, don't declare it** (`D-M6E1-7`, supersedes `D-M6E1-3`) — this slice
- [x] **S3.t2** — `plugin/node_modules/yaml/` committed (233 files); no plugin-root manifest — this slice
- [x] **S3.t3** — `AC3.3` — vendored version pinned to the root lockfile — this slice
- [x] **S3.t4** — `AC3.4` dissolved; replaced by `AC3.6` (fallback trigger) + `AC3.7` (faithful copy) — this slice

Suite **2616 → 2626**. Lint clean.

### The finding: `RESEARCH` §4's failure-mode sentence was inverted, and it was the deciding number

The plan's default was **A** (the manifest), and `RESEARCH` recommended it while describing a failed
install as costing *"the 18 commands that never touch `profile.js`/`state.js`"* — i.e. a minority
tail, with `AC3.4` asking only that it be **made legible, not prevented**.

Computed as a **transitive closure** over `plugin/tools/lib` rather than as a grep for two filenames:

| | |
|---|---|
| `tools/lib` modules reaching `yaml` | **28 of 51** |
| Commands that break without it | **17 of 20** |
| Survivors | `doctor`, `escalate`, `update` |

`archive`, `sweep`, `index` and `migrate-memory` are in the broken set and reach `yaml` through
`archive-command.js`, `sweep.js`, `planning-index.js` and `migrate-memory.js` — **never** through
`profile.js`/`state.js`, which is precisely why a grep for those two names missed them.

So `AC3.4`'s subject was never a degraded mode. It was **"the plugin does not work, silently, at
import, on a restricted network"** — because a failed dependency install *"never blocks the plugin."*
At that magnitude, prevention was both available and free at the boundary: the ~1.2 MB is bytes the
manifest would have installed into the same cache anyway.

**The AC did its job.** `AC3.4` demanded the outcome be *characterised* — and characterising it
honestly is what destroyed the option it was written to accompany.

### Two smaller things the slice turned up

1. **`.gitignore`'s `dist/` would have committed a dependency containing nothing that runs.**
   Unanchored, it matched `plugin/node_modules/yaml/dist/` — the 796 KB that *is* the package.
   `git add -An` reported `LICENSE`, `README.md` and `bin.mjs` and silently dropped the rest. Caught
   by checking the dry-run's file count against the tarball's own `233`, not by reading the pattern.
   Fixed by anchoring the repo's build output as `/dist/`; both directions are now pinned by test.

2. **`RESEARCH` rejected option C on a false premise.** Its stated ground was that deep-importing a
   CJS package past its `exports` map makes *"named-export interop the fragile part."* Verified
   directly: a relative deep import of `dist/index.js` yields both named exports, because an
   `exports` map governs **bare specifiers only** and vendoring imports by relative path. C was
   genuinely viable; it was declined on **citation fidelity** (the import line should keep reading
   `from 'yaml'` so command files citing these modules still describe what runs), and `D-M6E1-7`
   records that as the reason rather than inheriting the wrong one.

### The letter collision, resolved

`RESEARCH` §4's **B** = commit `node_modules/yaml` (no code change). `AC3.5`'s vendoring =
`plugin/vendor/` + rewritten imports = `RESEARCH`'s **C**. `PLAN` S3.t1 said *"Decide A-vs-B"* and
cited `AC3.5` for B's trigger, pointing at both at once — the `M5.E17` class. Outcomes are **named,
not lettered**, from here on.

## Wave 4 — S4: the catalogs · **COMPLETE**

- [x] **S4.t1** — root `marketplace.json` → `"source": "./plugin"`; the `B58` guard's string-form and no-sha halves kept — this slice
- [x] **S4.t2** — `docs/map/install/marketplace.json` authored, `git-subdir`, no `ref`, no `sha` — this slice
- [x] **S4.t3** — `AC4.3` — the invariant pinned, and the *difference* pinned with it — this slice

Suite **2626 → 2632**. `validate-plugin` clean.

### The schema was read, not inferred

`git-subdir`'s shape was taken from a **real entry** in this machine's plugin-catalog cache rather
than reconstructed from prose:

```json
{"source":"git-subdir","url":"…/claude-plugins.git","path":"plugins/api-security-testing","ref":"v1.5.5","sha":"2ed4978…"}
```

Two things that only reading it settles. **`source` is an object here and a string in the root
catalog** — the two catalogs differ in *type*, not just in value, which is why `AC4.3` pins an
invariant rather than equality. And the sampled entry carries `ref` **and** `sha`; ours carries
neither, deliberately, so `D-M5E17-4`'s *"users track `main`"* survives the move.

### The `B39` shape, found inside the new guard

The first draft of the published-catalog tests passed **two assertions against a file that did not
exist yet**. `publishedRaw` was `null`, `JSON.stringify(null)` is `"null"`, and `"null"` contains no
40-char sha and no `"ref":` — so *"carries no ref and no sha"* and *"neither carries a pin"* both
reported green on **nothing at all**.

Caught because the RED run was read line-by-line rather than by its failure count: 5 failed, but two
of the three that should also have failed were green. Both now assert the catalog exists first, with
the reason stated in the message — *"no published catalog to check — this is a FAIL, not a pass."*
**A guard whose absent input reads as compliance is the defect this repo has shipped twice**, and
this one was written during the Epic that keeps citing it.

### `AC4.4` is owed, not met

`AC4.4` needs the file served from `https://signal.insightriot.com/install/marketplace.json` and a
real `claude plugin marketplace add` against it. The custom domain serves the **production**
deployment, which tracks `main`, so the URL cannot return this file until the Epic merges.
**Recorded as owed to `S5`** rather than counted here — an AC marked covered by the slice that
merely made it possible is the coverage-claim defect `M5.E10` shipped seven checks about.

## Wave 5 — S5: prove the new install path end to end · **PARTIAL — two tasks blocked until merge**

- [x] **S5.t2** — real install into an isolated config, from a clone — this slice
- [x] **S5.t3** — delivered tree measured, executed, and pinned by test — this slice
- [ ] **S5.t1** — ⛔ blocked: the custom domain serves production, which tracks `main` (carries `AC4.4`)
- [~] **S5.t4** — ⚠ mechanism proven, GitHub fetch not testable pre-merge (`AC5.1`)

Suite **2632 → 2636**. Lint and `validate-plugin` clean.

### The numbers, from a real install rather than a projection

| | Before (`source: "."`) | After (`source: "./plugin"`) |
|---|---|---|
| Tracked files delivered | 985 | **382** |
| Delivered size | 12.4 MB | **3.2 MB** |
| `node_modules` installed into the cache | 47 MB | **none — the install never fires** |
| `.planning/` files delivered | 270 | **0** |
| `tests/` files delivered | 218 | **0** |

**`B99`'s two costs, both discharged at the delivery boundary.** Size: ~59 MB → 3.2 MB. Content: the
270 `.planning/` files — measurements against real client projects, bug diagnoses, business
reasoning — **reach no user by this path at all.** The separate question of whether `.planning/`
should be public *in the repository* is untouched and still deferred, exactly as `MILESTONE-6.md`
scoped it.

### The install was isolated before it was run

A local install writes into `~/.claude` and would have put a second `sig` into the operator's live
config — `B52`'s exact territory, during the Epic that keeps citing it. So isolation was **verified
before anything was installed**, not assumed: `CLAUDE_CONFIG_DIR` pointed at a scratch directory
reported *"No plugins installed"* against a live config holding several, and wrote its `.claude.json`
into the scratch directory. Only then did the install run.

**And it installed from a `git clone`, not from the working directory.** `B99`'s own correction is
why: its first version claimed *"57 MB delivered to users"* from a local-path install that had copied
untracked `node_modules`. Cloning delivers tracked files only, which is what a real fetch does — so
the measurement answers the question that was asked.

### The delivered tree was executed, not just counted

Counting files proves what shipped; it does not prove it runs. The two modules that import `yaml` —
the whole subject of `S3` — were imported **from the cache** and called:

- `readState` → `EXECUTE / M6.E1 / schema 1`
- `readEffectiveProfile` → `FULL / strict`
- `boundPluginRoot()` → the cache root (`B52`'s mechanism, correct at the new depth)
- `listCommands()` → 20

Real YAML frontmatter parsed by a vendored dependency at the installed depth, with **no install
having run**. That is `S3`'s claim, confirmed from the far side of a fetch.

*A wrong call of my own, recorded because the first output looked like a defect and was not:*
`readState` and `readEffectiveProfile` are **async**, and the first probe did not await them, so both
returned `undefined` fields. The tree was correct; the probe was wrong. Checking the export
signatures before reporting a break is the difference between finding a bug and inventing one.

### The measurement is now a guard, not a one-off

`tests/plugin-boundary.test.js` gains a `S5` block deriving the shipped set from `git ls-files
plugin/` — tracked files only, since the working directory carries untracked output that would make
the check read clean for the wrong reason. It refuses any `.planning/`, `tests/`, `analysis/`,
`examples/`, `docs/` path, any dev-tooling package, and any plugin-root manifest or lockfile.

**Proven to have teeth twice**, because a predicate test alone is not proof: the predicate rejects
five planted paths, *and* a real tracked file at `plugin/tests/probe.test.js` was planted, turned the
check **red**, and was removed — green again, no residue. A guard that has never failed is a guard
nobody has tested.

### What merge owes

`S5.t1`/`AC4.4` (the URL serves the catalog; `marketplace add <url>` installs from it) and `S5.t4`'s
GitHub half (`AC5.1`) are **structurally unrunnable from a branch** — both depend on `main`. Recorded
as owed at merge rather than marked covered, because an AC ticked by the slice that merely made it
possible is the coverage-claim defect `M5.E10` shipped seven checks about.

## Wave 6 — S6: migration, the detector and the page · **COMPLETE**

- [x] **S6.t1** — `detectP6LegacyMarketplaceSource` (`AC5.2`) — this slice
- [x] **S6.t2** — `AC5.3` absent-safe both ways — this slice
- [x] **S6.t3** — install page: URL form primary, existing-user section (`AC6.1`, `AC6.3`) — this slice
- [x] **S6.t4** — `AC6.2` — no delete-your-cache advice; the marketplace clone covered — this slice

Suite **2636 → 2655**. Lint and `validate-plugin` clean.

### `runAllDetectors` was dropping any check that could not run

The detector loop filters on `d.detected`. A `cannotDetermine` result has `detected: false`, so it
would have been **silently discarded** — and the command would have printed a clean bill of health
from a check that never looked. `B39`, arriving inside the fix for the defect class that keeps citing
it.

`runAllDetectors` now returns `checked_all` and `undetermined` alongside `healthy`, and `doctor.md`
renders a could-not-check block **before any verdict**, unconditionally. `healthy` now means *no
defects among the checks that ran*; `checked_all` is what stops it reading as *everything was
checked*. The healthy line itself changes text when a check was skipped, rather than staying
identical and hoping the reader scrolls.

### Two `AC`s that look contradictory, and are not

`AC5.2` says an **absent** file reports *could not check*. `AC5.3` says a machine with **no Signal
marketplace** must not error. Read quickly these conflict; they describe different states:

| State | Answer |
|---|---|
| No `known_marketplaces.json` at all | **could not check** — the detector never looked |
| File present, no Signal entry | **clean** — it looked and found nothing |
| File present, Signal entry, `github` source | **detected** |
| File unreadable or malformed | **could not check** |

Both ACs are satisfied by keeping the first two rows *distinct*. Collapsing them is precisely how
*"I could not tell"* comes to render as *"you are fine."*

### Matching on the repo, not the key — and a case that would have silenced the whole check

The marketplace **key** is chosen by whoever runs `marketplace add`; the **repo** is not. The
detector matches on the repo.

It matches **case-insensitively**, and that is not defensive coding. The live entry on a real machine
reads `Insightriot/signal`; every document in this repository writes `insightriot/signal`. A
case-sensitive match would have compiled, passed review, shipped, and **never fired once** — a
detector that is correct in every respect except the one that matters. Pinned by a test over three
capitalisations.

### The detector is pinned as *reached*, not merely built

`UNREACHED-MECHANISM-ANALYSIS.md`'s class is a mechanism that exists, is correct, and is invoked by
nothing. A test asserts `detectP6LegacyMarketplaceSource` appears in the array `runAllDetectors`
executes, and that `doctor.md` renders both the finding and the switch command. **Mutation-verified:**
deleting the detector from that list turns the test red; reverting the page's primary snippet to the
old form turns `AC6.3` red. Both restored, green.

### `AC6.2` is the one that would have shipped bad advice

The obvious existing-user section tells people to clear their plugin cache. `B99` measured that **11
of 13 cached versions sit inside the documented ~14-day orphan sweep and clear themselves** — so
`rm -rf` inside a user's Claude config would be advice for a problem that solves itself. The page
says so explicitly, and points the one instruction that *is* needed at the **marketplace clone**,
which does not self-clean. The test asserts the absence, because a missing warning leaves no trace.

### The ceiling moved, and not in the good direction

Adding to `doctor.md` broke `adherence-ceiling.test.js`, which is the check working. Regenerated:
**483 → 488 directive lines**, and **trace-measurable stayed at 104** — so all five lines I added
landed in *no observable trace*, 379 → 384. **The coverage fraction fell, 21.5% → 21.3%, because of
this slice.** Recorded plainly rather than as a rounding note: the migration nudge is prose a person
has to follow, and Signal cannot yet observe whether it did.

### `NFR3` is moot, not met

*"If the dependency install fails … a missing `yaml` must surface as a clear error"* — there is no
dependency install any more (`D-M6E1-7`), so its **precondition** was removed rather than its
requirement satisfied. Marked moot in `REQUIREMENTS.md` rather than deleted, because an NFR that
quietly disappears is indistinguishable from one that was skipped.

## Post-merge — the two owed tasks, discharged (2026-08-18)

PR **#152** merged as `b849788`. `S5.t1`/`AC4.4` and `S5.t4`/`AC5.1` were then executed for real in
fresh isolated configs — both **PASS**. Detail and the full table live in `M6.E1-VERIFICATION.md`
§ Post-merge discharge; the headline is that the marketplace fetch went **19 MB → 4 KB** on the new
path, while the old path still installs an identical plugin from a **16 MB** clone. Nobody is
stranded, and the nudge now has a measured number behind it rather than an argument.

**The base moved mid-ship** (#151 landed on `main` while the Epic was in SHIP), and `main` was
**merged into** the branch rather than the branch rebased onto it — `ADHERENCE-LOG.md` pins
`76d8960`, one of this Epic's own commits. All five pinned SHAs verified reachable from `main` after
the merge.
