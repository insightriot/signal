# M4.5.E9 — Validation (8 dimensions + Nyquist mapping)

**Epic:** Retro Foundations
**Phase:** PLAN — final gate before EXECUTE
**Tier:** FULL (`plan_validation_dims: all`, `nyquist_enforcement: strict`)
**Source:** M4.5.E9-PLAN.md (19 tasks, ~58 tests, 2 slices)
**Date:** 2026-05-25

---

## Part 1 — 8-dimension plan validation

| # | Dimension | Verdict | Evidence |
|---|---|---|---|
| 1 | Goal alignment | **PASS** | See § 1.1 |
| 2 | Completeness | **PASS** | See § 1.2 |
| 3 | Dependency correctness | **PASS** | See § 1.3 |
| 4 | Testability | **PASS** | See § 1.4 |
| 5 | Scope discipline | **PASS-WITH-NOTE** | See § 1.5 |
| 6 | Context feasibility | **PASS** | See § 1.6 |
| 7 | Risk coverage | **PASS** | See § 1.7 |
| 8 | Vertical slicing | **PASS** | See § 1.8 |

**Overall: PASS** — plan is cleared to enter EXECUTE pending user approval and successful Nyquist mapping (Part 2 below).

---

### 1.1 Goal alignment — PASS

**Phase goal:** *Make per-Epic retrospectives a structurally enforced artifact that fails closed under context pressure, and provide an indexed traversal layer so captured learning is findable.*

| Task | Traces to | Verdict |
|---|---|---|
| S1.t1 (DECISIONS promote) | Process-quality precondition for D-E9-8 + AMEND fixes living in canonical place | PASS |
| S1.t2 (templates) | Enables FR2 — tier-aware artifact shape | PASS |
| S1.t3 (parsers) | Foundation for FR3 validation + FR1 path derivation | PASS |
| S1.t4 (validateRetroContent) | FR3 directly | PASS |
| S1.t5 (path/epic-close helpers) | FR1 directly + AMEND A3, A4 | PASS |
| S1.t6 (commands/ship.md) | FR1 directly (command-internal layer of D-E9-8) | PASS |
| S1.t7 (hooks) | D-E9-8 hook layers; addresses original failure mode | PASS |
| S1.t8 (backfill enum) | FR4 enumeration | PASS |
| S1.t9 (stub gen) | FR4 generation | PASS |
| S1.t10 (dry-run gate) | Risk mitigation for S1.t6 sequencing | PASS |
| S1.t11 (thresholds) | FR3 precision | PASS |
| S1.t12 (E9 own retro) | Phase goal directly — first dogfooded artifact + integration test | PASS |
| S2.t1 (retro-index enum) | FR5 foundation | PASS |
| S2.t2 (hybrid render) | FR5 maintenance pattern (item #3) | PASS |
| S2.t3 (post-commit regen) | FR5 lifecycle | PASS |
| S2.t4 (initial RETROSPECTIVES.md) | FR5 deliverable | PASS |
| S2.t5 (FR5 integration tests) | FR5 verification | PASS |
| S2.t6 (manual meta-retro) | FR6 (downgraded per A6) | PASS |
| S2.t7 (resume integration) | Open Q #3 → "indexed traversal layer" goal | PASS |

**Every task serves the phase goal directly or via an explicit support relationship.** No orphan tasks.

### 1.2 Completeness — PASS

All 6 FRs from REQUIREMENTS.md are mapped to at least one task:

| FR | Task(s) | Coverage |
|---|---|---|
| FR1 (SHIP hard-block) | S1.t5, S1.t6, S1.t7 | Complete (3 layers per D-E9-8) |
| FR2 (tier-aware template) | S1.t2 | Complete (4 templates) |
| FR3 (minimum-content sanity check) | S1.t3, S1.t4, S1.t11 | Complete |
| FR4 (stub backfill) | S1.t8, S1.t9, S1.t10 | Complete |
| FR5 (RETROSPECTIVES.md index) | S2.t1, S2.t2, S2.t3, S2.t4, S2.t5 | Complete |
| FR6 (milestone meta-retro) | S2.t6 (downgraded per A6) | Complete (manual variant) |

All 19 + 3 ACs from REQUIREMENTS (AC1-19, plus AC1H/2H/3H added per D-E9-8) are mapped to at least one task. Full AC→task→test mapping in Part 2 below.

**No FR or AC is unmapped.** The 7 originally-deferred-to-PLAN items + 2 added post-DISCUSS items (#8, #9) all have explicit resolution:
- Items 1-3, 5-7: resolved via task specs (S1.t8 / S1.t9 / S2.t2 / S1.t11 / S1.t2 / S1.t1).
- Item 4: resolved via S1.t5 `isEpicCloseShip` helper.
- Item 8: resolved via D-E9-8 layered enforcement (S1.t6 + S1.t7).
- Item 9: resolved via skip-in-E9 + FUTURE-IDEAS M5.E1 capture.

### 1.3 Dependency correctness — PASS

Dependency graph (PLAN § "Dependency graph") accurately reflects implementation order:

- **S1.t1 has no deps** — pure docs; can ship first.
- **S1.t2 → S1.t3 (loadTemplate reads template file)** — accurate.
- **S1.t3 → S1.t4 (validator uses parsers + getRequiredSections)** — accurate.
- **S1.t3 → S1.t5 (deriveRetroPath shared)** — accurate.
- **S1.t4 + S1.t5 → S1.t6 (command-internal check composes these)** — accurate.
- **S1.t3 + S1.t4 + S1.t5 → S1.t7 (Node CLI surface wraps these)** — accurate.
- **S1.t8 → S1.t9 (stub gen needs Epic enumeration)** — accurate.
- **S1.t8 + S1.t9 → S1.t10 (dry-run gates the live run)** — accurate.
- **S1.t10 MUST precede S1.t6 shipping** (sequencing rule, not graph dep — captured in PLAN § "Critical sequencing rules" #1).
- **All S1 → S1.t12** — integration test, runs last.
- **S2.t1 → S2.t2 → S2.t3 / S2.t4 / S2.t5** — accurate, sequential.
- **S2.t6 + S2.t7 parallelizable with each other and with S2.t3-5** — accurate.

**No false dependencies; no missing dependencies surfaced during review.** One soft dep worth noting: **S1.t7 hooks task should NOT block S1.t6 ship.** PLAN already explicitly notes (Risk mitigation): "if S1.t7 hooks-API smoke test reveals a blocker, ship command-internal-only and document gap." This is correct — the layered enforcement degrades gracefully.

### 1.4 Testability — PASS

Every task has either (a) explicit ACs with test counts, or (b) explicit "manual gate" with git-artifact verification.

| Task | Tests | Verification mode |
|---|---|---|
| S1.t1 | 0 | Doc-only; verifiable via git diff |
| S1.t2 | 0 (validated indirectly via S1.t4) | File-exists + AC4/AC5 |
| S1.t3 | ~6 unit | Per-helper |
| S1.t4 | ~10 unit (parametrized × 4 tiers) | Direct AC6-9 |
| S1.t5 | ~6 unit | Per-helper + shelved edge case |
| S1.t6 | ~6 fixture integration | Direct AC1-3 + AC1-extended |
| S1.t7 | ~5 unit (CLI) + 2 manual smoke | AC1H-3H + hook smoke |
| S1.t8 | ~5 unit | Enumeration + commit scan + partial flag |
| S1.t9 | ~6 unit + 1 integration | AC10-13 |
| S1.t10 | 0 (manual gate + git artifact) | User approval + RESEARCH addendum |
| S1.t11 | ~4 parametrized | Per-tier thresholds |
| S1.t12 | 0 (manual + AC verification via validator) | Self-validates via S1.t4 |
| S2.t1 | ~4 unit | Enumeration + stub detection |
| S2.t2 | ~5 unit | Render + merge + sort |
| S2.t3 | ~2 fixture integration | Post-commit regen flow |
| S2.t4 | 0 | One-shot + git artifact |
| S2.t5 | ~4 integration | Direct AC14-17 |
| S2.t6 | ~3 fixture | AC18-19 (reframed) |
| S2.t7 | ~2 unit | Resume briefing line + empty-state |

**Test count: ~58 automated + 3 manual gates.** Aligns with phase-researcher's estimate (~58 tests). All ACs traceable to at least one test slot per Nyquist strict.

### 1.5 Scope discipline — PASS-WITH-NOTE

**One scope-by-discovery surfaced** in S1.t6 part 2: bringing `commands/ship.md` into `transitionPhase` + `markFresh` parity with verify.md and review.md. RESEARCH § 1.1 documented that SHIP currently does NOT call `tools/lib/state.js` programmatically — the "update STATE.md" step is prose. Without fixing this, FR1's hard-block has no programmatic phase-completion signal to gate against.

**Verdict:** explicitly named in S1.t6 spec, ~30 lines of work, structurally required for the Epic to function. Not gold-plating — it's a dependency the original DISCUSS didn't see because the codebase pattern wasn't surfaced until research. This is the right form of "scope-by-discovery": surfaced, named, justified, contained to one task.

**No other scope creep identified.** Skipped items (auto-draft, wiki restructure, Dreaming-style automation, migration tooling) all explicitly listed in PLAN § "Non-goals" with M5.E1 forward reference.

### 1.6 Context feasibility — PASS

Every task is implementable in a single agent context (~50K-150K tokens). Largest tasks:
- **S1.t6** — multi-part change to commands/ship.md (3 parts: pre-check, state-write fix, current_epic handling) + ~6 fixture tests. Estimated context: ~40K tokens (reads existing ship.md, verify.md for pattern, profile.js, state.js, plus its own work). **Feasible.**
- **S1.t9** — stub generation orchestrator + 6 unit + 1 integration test. Estimated context: ~35K tokens. **Feasible.**
- **S2.t2** — hybrid render with merge logic + 5 unit tests across edge cases. Estimated context: ~30K tokens. **Feasible.**

No task requires loading more than ~3-5 source files plus its own implementation + tests. Context-rot risk low.

`context_rot_reread: true` (per FULL tier PROFILE.md) means each EXECUTE task re-reads its plan section before starting — preserves task-level coherence across the full EXECUTE phase.

### 1.7 Risk coverage — PASS

| Risk | Mitigation in plan |
|---|---|
| FR1 enforcement immediately blocks SHIP because no past Epic has retro | S1.t10 dry-run gate + critical sequencing rule (S1.t10 before S1.t6) |
| Shelved-slice edge case breaks isEpicCloseShip (E1.S3-S5 never resolves) | S1.t5 explicit test for shelved status; encoded as first-class slice state |
| Hooks API smoke test fails / blocker | PLAN explicitly notes graceful degradation (ship command-internal-only; S1.t7 becomes follow-on) |
| current_epic empty/stale (right now CONTEXT.md says "Active Epic: none") | S1.t6 part 3 — A2 fix encoded as explicit error path with documented guidance |
| Epic-ID shape change in M5 breaks hardcoded globs | S1.t3 deriveRetroPath + S2.t1 path-agnostic glob — A3 fix |
| Git-log path filter unreliable for commit-range scan | S1.t8 switched to commit-message convention scan; A4 fix |
| MILESTONE.md final-Epic detection unparseable | S2.t6 downgraded to manual invocation; A6 fix |
| AC mismatch between command-internal and hook mechanisms | New AC1H/2H/3H added per A8 fix; explicit per-mechanism testing |
| First dogfooded retro (S1.t12) surfaces UX pain | S1.t12 IS the integration test — if painful, S1 not done; explicit success criterion |
| `--dangerously-skip-permissions` bypasses hooks | Documented as known limitation; command-internal layer still fires |
| Stop hook hang (superpowers #390) | Explicitly NOT included in D-E9-8 |
| Cross-runtime (Cursor) enforcement weakness | Documented as known limitation in PLAN; A9 deferred per Open Q #5 |

**All identified risks have explicit mitigations.** Two known limitations explicitly carried forward without "fix in E9" — both have explicit user-visible documentation in PLAN.

### 1.8 Vertical slicing — PASS

S1 ships a complete user-facing capability: SHIP refuses to close without a retro, with tier-appropriate template, with backfill for past Epics in place, AND the first dogfooded retro written for E9 itself. **End-to-end value:** the user invokes `/sig:ship` on the next Epic, gets blocked if no retro, writes one, ships. Complete vertical slice.

S2 ships a complete addition: the index makes retros traversable, the meta-retro mechanism is available, `/sig:resume` surfaces retro status. **End-to-end value:** the user runs `/sig:resume` and sees "Retros: 1/6 complete" along with the rest of the briefing; opens RETROSPECTIVES.md and navigates to any retro by Epic ID. Complete vertical slice.

Neither slice ships a "horizontal layer" (e.g., "all the parsers but no callers") that would create integration debt. Each slice is shippable in isolation.

---

## Part 2 — Nyquist test-coverage mapping (strict)

`nyquist_enforcement: strict` per FULL tier. **Every AC must have a corresponding test slot with explicit test type and red-before-green ordering.**

### Coverage matrix (AC → task → test type → red-before-green note)

| AC | Source | Task | Test type | Test count | Red-before-green |
|---|---|---|---|---|---|
| AC1 | FR1 | S1.t6 | Fixture integration | 1 | Yes — test reads fixture with no retro, asserts SHIP halts with documented message |
| AC1-extended | A2 fix | S1.t6 | Fixture integration | 2 | Yes — fixture with empty current_epic + fixture with stale current_epic |
| AC1H | D-E9-8 hook layer | S1.t7 | Unit (CLI) | 1 | Yes — CLI given proposed STATE.md content sans retro, asserts exit 2 + stderr format |
| AC2 | FR1 | S1.t6 | Fixture integration | 1 | Yes — test invokes with `--force` flag, asserts halt persists |
| AC2H | D-E9-8 hook layer | S1.t7 | Manual smoke | 1 (smoke) | Manual — hook fires on actual `Edit` invocation, can't be bypassed by user flags |
| AC3 | FR1 | S1.t6 | Fixture integration | 1 | Yes — fixture with passing retro + complete state, asserts SHIP proceeds, transitionPhase/markFresh fire |
| AC3H | D-E9-8 hook layer | S1.t7 | Unit (CLI) | 1 | Yes — fixture with dirty-EXECUTE state, assert SessionStart-resume injects warning |
| AC4 | FR2 | S1.t2 | Unit (via S1.t4) | (covered) | Templates validated as side effect of AC7 tests |
| AC5 | FR2 | S1.t2 | Unit | 1 | No (file-exists is post-condition, no red-state useful) |
| AC6 | FR3 | S1.t4 | Unit | 1 | Yes — empty input, expect rejection |
| AC7 | FR3 | S1.t4 | Unit parametrized × 4 tiers | 4 | Yes — per-tier "missing one required heading" fixtures |
| AC8 | FR3 | S1.t4 | Unit | 1 | Yes — headings present but empty body |
| AC9 | FR3 | S1.t4 | Unit parametrized × 4 tiers | 4 | Yes — per-tier well-formed-passes fixtures |
| AC10 | FR4 | S1.t9 | Integration | 1 | Yes — fixture M4.5 state, run backfill, assert per-Epic stub files |
| AC11 | FR4 | S1.t9 | Unit | 1 | Yes — generated stub parsed for links, assert each resolves |
| AC12 | FR4 | S1.t9 | Integration | 1 | Yes — run backfill twice, assert no diff on second run |
| AC13 | FR4 | S1.t9 | Integration | 1 | Yes — edit a stub (add content beyond template), re-run, assert no overwrite |
| AC14 | FR5 | S2.t5 | Integration | 1 | Yes — pre-run state has no RETROSPECTIVES.md, run S2, assert exists |
| AC15 | FR5 | S2.t5 | Integration | 1 | Yes — fixture with N retros, assert all listed in index |
| AC16 | FR5 | S2.t5 | Integration | 1 | Yes — parse links, follow each, assert all resolve |
| AC17 | FR5 | S2.t5 | Integration | 1 | Yes — fixture mixing stub+complete, assert status flags match |
| AC18 (reframed) | FR6 / A6 | S2.t6 | Fixture | 1 | Yes — fixture milestone, invoke `--milestone-meta`, assert file generated |
| AC19 | FR6 | S2.t6 | Fixture | 1 | Yes — invoke with no milestone in state, assert handles gracefully |

**Plus standalone unit tests for helpers** (not directly mapped to AC numbers but required for strict Nyquist coverage of `tools/lib/retrospective.js` and `tools/lib/retro-index.js` surface area):

| Helper | Task | Tests |
|---|---|---|
| `parseSections` | S1.t3 | 2 unit (basic + edge case) |
| `getRequiredSections` | S1.t3 | 1 unit (per-tier table) |
| `deriveRetroPath` | S1.t3 | 2 unit (M4.5.E9 + M5.E1 shape) |
| `loadTemplate` | S1.t3 | 1 unit |
| `expectedRetroPath` | S1.t5 | 2 unit (populated + null current_epic) |
| `isEpicCloseShip` | S1.t5 | 4 unit (Epic-close, mid-Epic, shelved-correctly, error) |
| `enumerateRetros` (retro-index) | S2.t1 | 2 unit (basic + path-agnostic glob) |
| `isStubRetro` | S2.t1 | 2 unit (stub detection + complete detection) |
| `renderIndex` (hybrid) | S2.t2 | 5 unit (fresh, merge, new-entry, sort, multi-line hook) |
| Backfill enumeration | S1.t8 | 2 unit (M4.5 set correctness, partial flagging) |
| Commit-range scan | S1.t8 | 3 unit (convention match, fallback path filter, missing-Epic warning) |
| Threshold measurement | S1.t11 | 4 parametrized (per-tier minimum-fill pass + empty-fail) |
| Resume retro line | S2.t7 | 2 unit (count rendering, empty-state) |

**Tests per slice:** S1 ≈ 40 automated + 3 manual (smoke + dry-run gate + own-retro). S2 ≈ 18 automated. **Grand total: ~58 automated + 3 manual gates.**

### Coverage gaps (none found)

Every AC has at least one test slot. Every helper has at least one unit test. Per FULL `nyquist_enforcement: strict`, this is the minimum bar — every AC must have a corresponding test, and the test must run red before turning green.

**Strict Nyquist's "failed before fixed" rule:** Each EXECUTE task writes the test FIRST, runs it, captures the red state (either via test runner output OR via explicit attestation in the test file's commit message), then writes implementation, runs green. This is enforced at VERIFY phase — VERIFY rejects ACs where there's no evidence of red-before-green ordering.

### One escalation pre-emptively flagged

**S1.t10 (dry-run gate)** is a manual gate, not an automated test. Per `nyquist_enforcement: strict`, ACs need test slots. **However:** S1.t10 has no AC of its own — it's a process gate, not a feature. The git-artifact verification (commit + RESEARCH addendum capture) is the documentary evidence. This is consistent with M4.5.E6's D15 gate which was also process-not-feature.

### Test infrastructure verified

Per RESEARCH § 1.5: vitest + temp-dir fixture-clone pattern is the existing convention. All new tests use the same shape:
```js
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
beforeEach(async () => { tempDir = await mkdtemp(join(tmpdir(), 'signal-retro-test-')); });
afterEach(async () => { await rm(tempDir, {recursive: true}); });
```

Per-tier tests use `it.each([['SKETCH', ...], ['FEATURE', ...], ...])` parametrization.

---

## Validation gate result

| Gate | Status |
|---|---|
| 8-dimension validation | **PASS** (all 8 dimensions; one pass-with-note flagged in §1.5) |
| Nyquist strict mapping | **PASS** (all ACs mapped to test slots with red-before-green ordering) |
| Environment check | **PASS** (no drift; Node 22+ matches; no new runtime deps) |
| Anti-rationalization (PLAN command's table) | **PASS** (all 4 rows addressed: file-based plan ✓, ACs per task ✓, test strategy known ✓, vertical slicing verified) |
| Exit criteria | **PENDING** user approval — final gate before EXECUTE |

**Recommendation:** Plan is cleared to enter EXECUTE pending explicit user approval. The user should specifically validate: (1) the D-E9-8 layered enforcement scope (3 mechanisms), (2) the S1.t10 dry-run gate must precede S1.t6 ship rule, (3) the scope-by-discovery noted in §1.5 (state-write fix bundled into S1.t6).
