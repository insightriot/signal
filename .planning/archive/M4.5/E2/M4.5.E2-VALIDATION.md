# M4.5.E2 — `/sig:add` — Plan Validation

**Inputs:** `M4.5.E2-PLAN.md`, `M4.5.E2-RESEARCH.md`.
**Tier:** FULL → all 8 plan-validation dimensions + strict Nyquist test-coverage mapping.
**Validated:** 2026-05-14.

---

## Part 1 — 8-Dimension Plan Validation

### Dim 1 — Goal alignment

**Phase goal:** Ship `/sig:add` — a not-tier-gated capture command that lets any user (especially newcomers) append a stamped entry to one of 4 `.planning/` destinations via a zero-question hot path or a single-question cold path, hardened against file corruption, secret leakage, and vocabulary drift.

| Slice | Serves goal? | Justification |
|---|---|---|
| S1 — Hardened hot path | ✓ | Delivers the hot path (`/sig:add "idea"`) + atomic write + scrub. This *is* the MVP form of the goal. |
| S2 — Force-route flags | ✓ | Delivers the "one of 4 destinations" surface (FUTURE-IDEAS + OPEN-QUESTIONS + current MILESTONE + other MILESTONE). |
| S3 — Cold-path interview | ✓ | Delivers the "single-question cold path" surface explicitly stated in goal. |
| S4 — Stranger-safety | ✓ | Delivers the "hardened against secret leakage" and stranger-friendliness implied by "especially newcomers." |
| S5 — Close the loop | ✓ | Delivers the missing back-half (planning-phase reads FUTURE-IDEAS) — without it, captures rot, which violates the implicit goal of "captures should reach action." |

**Verdict:** PASS. Every slice traces directly to a clause in the phase goal.

---

### Dim 2 — Completeness

Cross-reference: FUTURE-IDEAS § `/sig:add` spec (lines 471-532) lists 25 locked decisions in RESEARCH § 1. Check each lands in a slice:

| Decision # | Lands in | Coverage |
|---|---|---|
| 1 (default FUTURE-IDEAS) | S1 | ✓ |
| 2 (hot path 0 questions) | S1 | ✓ |
| 3 (flag-based force-routing) | S2 | ✓ |
| 4 (naked invocation 1 question) | S3 | ✓ |
| 5 (verbatim capture) | S1 (entry shape spec; "body verbatim — no LLM rewrite") | ✓ |
| 6 (no preview before write) | S1 (no preview); S4 gate_strictness: strict overrides (acceptable; documented escape) | ✓ |
| 7 (atomic write) | S1 | ✓ |
| 8 (lock file with TTL) | S1 | ✓ |
| 9 (per-destination templates) | S1 (FUTURE-IDEAS), S2 (OPEN-QUESTIONS + MILESTONE × 2) | ✓ |
| 10 (footer rewrite) | S1 (FUTURE-IDEAS footer); S2 should extend to OPEN-QUESTIONS + MILESTONE — **GAP: need explicit acceptance criterion in S2 for footer rewrite per destination.** |
| 11 (not tier-gated) | S1 (no preamble) | ✓ |
| 12 (honor gate_strictness) | S4 | ✓ |
| 13 (never write DECISIONS/STATE) | S2 (hard-refuse) | ✓ |
| 14 (sensitive-data scrub) | S1 | ✓ |
| 15 (first-run onboarding) | S4 | ✓ |
| 16 (plain English, vocab on confirmation) | S3 (heuristic phrasing); S1 success line | ✓ |
| 17 (3+other, not strict-enum) | S3 (heuristic prompts use 3+other) | ✓ |
| 18 (one destination per call) | S2 (single destination per invocation; flags mutually exclusive — **GAP: need explicit check that combined flags like `--question --milestone` fail-fast**) |
| 19 (new helper milestones.js) | S2 | ✓ |
| 20 (STATE.md source of truth for current milestone) | S2 (`currentMilestone` reads STATE) | ✓ |
| 21 (brownfield without .planning/ → /sig:init) | S1 (basic), S4 (enriched) | ✓ |
| 22 (no new ESM deps) | Cross-slice constraint | ✓ |
| 23 (no partial-draft persistence) | Out-of-scope section | ✓ |
| 24 (no numeric entry IDs) | S1 (entry shape uses Title-Cased heading, not IDs) | ✓ |
| 25 (single-key override for heuristics) | S3 | ✓ |

**Gaps identified — close in plan revisions:**
1. **GAP-1:** S2 acceptance criteria need explicit "footer rewrite per destination" criterion (currently buried in scope). Add as criterion #8: "OPEN-QUESTIONS.md and MILESTONE-*.md footer markers (`---`, `*Created ...*`) preserved/rewritten correctly per fixture test."
2. **GAP-2:** S2 needs explicit "mutually-exclusive flags" criterion. Add as criterion #9: "Invoking with multiple destination flags (e.g., `--question --milestone`) exits non-zero with a clear error before any write."

Plus one cross-cutting completeness check:
- **Documentation completeness:** README and CLAUDE.md updates land in S1 only — but S2-S4 add flags and features that need README mentions too. **GAP-3:** Each slice should explicitly include "README workflow table updated if user-visible behavior changes" in its scope.

**Verdict:** PASS WITH MINOR REVISIONS (3 gaps; all addressable by adding acceptance criteria lines, no slice restructuring needed).

---

### Dim 3 — Dependency correctness

Plan declares linear dependencies: S1 → S2 → S3 → S4 → S5.

| Edge | Justified? | Notes |
|---|---|---|
| S2 → S1 | ✓ | S2 reuses Slice 1's `atomicWrite`, `acquireLock`, `scrubSensitive`. Cannot skip. |
| S3 → S2 | ✓ | S3's heuristic prompts can route to OPEN-QUESTIONS / MILESTONE — those destinations exist only after S2. Cannot skip. |
| S4 → S3 | Partial | S4's gate_strictness preview applies to all paths (S1-S3). Onboarding warning is independent. **Refinement:** S4 *could* land partially after S1 (onboarding warning + brownfield-detection independent of S2/S3). But for atomic-commit hygiene, keeping S4 monolithic after S3 is fine. |
| S5 → S4 | Weak | S5 edits `commands/plan.md` to read FUTURE-IDEAS — only needs S1 (FUTURE-IDEAS being writable). Independent of S2-S4. **Refinement:** S5 could theoretically ship between S1 and S2 (would let users feel the loop sooner). Keeping it last preserves "build the writer fully, then update the reader" mental order. Acceptable but flag as optional reorder. |

**Are dependencies minimal?** Yes — no slice depends on a downstream slice. The only soft point is S5's "depends on S4" which is weaker than declared; revisit at execution time if user wants S5 sooner.

**Verdict:** PASS. Linear dependency graph is correct; S5's order is a soft preference, not a hard dependency.

---

### Dim 4 — Testability

Every acceptance criterion must be verifiable by a test or manual check.

| Slice | Total criteria | Auto-testable | Manual-only | Acceptable? |
|---|---|---|---|---|
| S1 | 8 | 7 | 1 (#7 — validator pass — auto via `npm run validate` but counts as integration check) | ✓ |
| S2 | 7 (+ 2 from gaps) = 9 | 9 | 0 | ✓ |
| S3 | 6 | 6 | 0 | ✓ |
| S4 | 6 | 6 | 0 | ✓ |
| S5 | 4 | 4 | 0 | ✓ |

**Verdict:** PASS. Every criterion has a defined test path. Nyquist mapping (Part 2 below) makes the unit/integration/E2E split explicit.

---

### Dim 5 — Scope discipline

The plan explicitly defers 7 items to "Out-of-scope" — strong scope-discipline signal. Cross-check against scope-creep temptations:

| Temptation | Held in plan? |
|---|---|
| Adding `--new-milestone` scaffold path | Deferred ✓ |
| Adding `--quick` flag | Deferred ✓ |
| Adding edit/delete commands | Deferred ✓ |
| Adding user-extensible heuristics | Deferred ✓ |
| Adding partial-draft persistence | Deferred ✓ |
| Including more file destinations (CONTEXT, PROJECT, etc.) | Implicitly held — only 4 destinations |
| Bigger entry templates / structured fields | Implicitly held — verbatim capture rule |
| Adding LLM auto-classification | Held (anti-rationalization #6 in plan) |

**Gold-plating risk areas:**
- S1 includes a lock file. Justified (silent corruption mode); plan's anti-rationalization defends it explicitly. **Not gold-plating.**
- S1 includes `EXDEV` cross-FS fallback. Edge case; symlinked `.planning/` is rare. **Borderline.** Minimal code (one catch + copy+unlink fallback); keep for robustness.
- S4 includes vocab-lint test. Justified (top-5 risk #2 — vocabulary drift poisoning corpus). **Not gold-plating.**

**Verdict:** PASS. Plan held discipline; no creep detected; all in-scope features have load-bearing justification.

---

### Dim 6 — Context feasibility

Can each slice fit in a single agent context (≈100K-150K tokens working budget)?

| Slice | Estimated context surface | Feasible? |
|---|---|---|
| S1 | new file `commands/add.md` (~300 lines), new helper `tools/lib/add.js` (~250 lines), new test `tests/add.test.js` (~200 lines), 1 fixture (~30 lines), 3 file edits (validator, README, CLAUDE.md) — total ~30K tokens including reading deps | ✓ |
| S2 | Extends `add.js` (+150 lines), new `tools/lib/milestones.js` (~80 lines), extends tests (+150 lines), 3 new fixtures (~100 lines) — ~25K tokens | ✓ |
| S3 | Extends `add.js` (+100 lines), extends tests (+120 lines), AskUserQuestion mock helper (~50 lines) — ~20K tokens | ✓ |
| S4 | Extends `add.js` (+80 lines), extends `validate-plugin.js` (+40 lines), extends tests (+80 lines) — ~15K tokens | ✓ |
| S5 | Edits `commands/plan.md` (~50 lines added), new fixture for FUTURE-IDEAS review (~40 lines), test (~80 lines) — ~12K tokens | ✓ |

All slices fit comfortably in a single execute-agent context. Context rot risk (FULL tier sets `context_rot_reread: true`) suggests re-reading PROFILE.md mid-slice if context budget approaches 70% — handled at EXECUTE time by sig:executors:executor agent.

**Verdict:** PASS. No slice approaches context-window pressure.

---

### Dim 7 — Risk coverage

Cross-reference plan slices against top-5 risks from RESEARCH:

| Risk | Slice(s) covering | Coverage quality |
|---|---|---|
| #1 Footer/format corruption | S1 (FUTURE-IDEAS template + fixture); S2 (OPEN-QUESTIONS + MILESTONE templates) | Strong — per-destination templates + fixture tests |
| #2 Vocabulary drift | S4 (vocab-lint validator test) | Adequate — banned-term test for `tranche`; could extend to a full vocabulary file if drift continues |
| #3 Misrouting + invisible recovery | S1 (success line with path + line); S3 (plain-English routing); S5 (planning-phase promotion) | Strong — recovery surface end-to-end |
| #4 Atomic-write discipline | S1 | Strong — temp+rename + lock + `EXDEV` fallback |
| #5 Sensitive-data leak | S1 (regex scrub + prompt); S4 (onboarding warning) | Strong — defense in depth |

**Lower-tier risks reviewed:**
- Markdown special chars in input → S1 entry shape mitigates (verbatim wrap inside known-shape heading + body). Adequate.
- Multi-paragraph paste → handled implicitly (verbatim capture preserves paragraphs). Documented as "one capture = one entry" in plan.
- Very long input → soft-cap mention in research; plan doesn't include hard handler. **Minor gap:** add to S1 scope: "Body > 4000 chars warns with confirm prompt (no hard cap)." [Optional; not blocking.]

**Verdict:** PASS. All top-5 risks covered with at least one slice each (most with multiple). One minor gap (length cap) — add or accept.

---

### Dim 8 — Vertical slicing

Is each slice a *full slice* (user-visible value end-to-end), or a *horizontal layer* (e.g., "all the helpers, then all the templates, then all the tests")?

| Slice | Vertical? | Justification |
|---|---|---|
| S1 — Hardened hot path | ✓ | Ships a working command + tests + manifest. User can run `/sig:add "idea"` and see value. Not a horizontal "just write helpers" slice. |
| S2 — Force-route flags | ✓ | Adds user-visible features (`--question`, `--milestone N`). Each flag is independently testable + user-feel-able. |
| S3 — Cold-path interview | ✓ | User-visible: naked `/sig:add` now prompts; heuristic hints appear. End-to-end. |
| S4 — Stranger-safety | ✓ | User-visible: first-run warning shows; brownfield error improves; gate_strictness honored. End-to-end. |
| S5 — Close the loop | ✓ | User-visible: `/sig:plan` now surfaces FUTURE-IDEAS candidates. Different command, but same user-flow. |

**Verdict:** PASS. No horizontal slicing detected. Each slice ships independently as v0.1.x patch and delivers user-feel-able value.

---

### 8-Dimension Summary

| Dim | Verdict | Notes |
|---|---|---|
| 1 Goal alignment | PASS | Every slice traces to phase goal. |
| 2 Completeness | PASS w/ revisions | 3 minor gaps; close by adding acceptance criteria (no restructuring). |
| 3 Dependency correctness | PASS | Linear; S5's order is soft preference. |
| 4 Testability | PASS | Every criterion has a test path. |
| 5 Scope discipline | PASS | 7 explicit deferrals; no creep. |
| 6 Context feasibility | PASS | All slices ≤30K tokens. |
| 7 Risk coverage | PASS | Top-5 covered; one minor optional gap (length cap). |
| 8 Vertical slicing | PASS | Each slice ships user-feel-able value. |

**Overall: PASS.** Plan is ready for EXECUTE pending 3 minor revisions (close GAP-1, GAP-2, GAP-3 in PLAN.md).

---

## Part 2 — Nyquist Test-Coverage Mapping (strict)

Per FULL tier `nyquist_enforcement: strict`, every acceptance criterion maps to a specific test of a specific type. Strict mode also requires *proof-of-fail-before-pass* for each (enforced in VERIFY phase, not PLAN — but flagged here for visibility).

### Test type taxonomy

- **U** = Unit (pure function, no I/O, fast).
- **I** = Integration (file I/O, multi-module, real temp dirs).
- **E** = E2E (full command invocation, smoke-style).
- **V** = Validator (`npm run validate` or extended validator check).
- **M** = Manual (interactive prompt requiring human, used sparingly).

### Slice 1 — 8 criteria + 2 implicit (length cap if added)

| # | Acceptance criterion | Test | Type | Proof-of-fail-first? |
|---|---|---|---|---|
| 1 | `/sig:add "test"` writes well-formed entry above footer; exit 0 | `add.test.js#captureToFutureIdeas writes entry` | I | ✓ (test starts with assertion that file doesn't yet contain entry) |
| 2 | No `.planning/` → exit non-zero with init suggestion | `add.test.js#missing-planning-dir` | I | ✓ (test sets up empty temp dir; asserts error) |
| 3 | Concurrent runs: second invocation fails with lock error | `add.test.js#concurrent-lock` | I | ✓ (test acquires lock manually; asserts second acquire rejects) |
| 4 | Sensitive-data hit prompts user; abort → no write; keep → write | `add.test.js#scrub-sensitive-prompts` (2 sub-cases) | I + U | ✓ (`scrubSensitive` unit returns hits; integration mocks prompt response) |
| 5 | Atomic write: `rename` fail → destination unchanged | `add.test.js#rename-fails-mid-write` | I | ✓ (mock `fs.rename` to throw; assert file unchanged) |
| 6 | Footer date rewritten on every successful write | `add.test.js#footer-rewrite` | I | ✓ (fixture has 2020-01-01 footer; assert rewritten to today) |
| 7 | `npm run validate` passes | `validate-plugin.test.js` extended | V | N/A (validator green is the proof) |
| 8 | `npm install --dry-run` clean | Manual + CI smoke | M | N/A (env check) |

### Slice 2 — 7 + 2 gap criteria = 9

| # | Criterion | Test | Type | Proof-of-fail-first? |
|---|---|---|---|---|
| 1 | `--question` routes to OPEN-QUESTIONS | `add.test.js#flag-question` | I | ✓ |
| 2 | `--milestone` routes to current (per STATE.md) | `add.test.js#flag-milestone-current` | I | ✓ |
| 3 | `--milestone N` routes to specific | `add.test.js#flag-milestone-N` | I | ✓ |
| 4 | `--milestone 99` (nonexistent) fails with deferral message | `add.test.js#flag-milestone-missing` | I | ✓ |
| 5 | `--file /etc/passwd` → hard-refuse outside-`.planning/` | `add.test.js#flag-file-outside-planning` | I | ✓ |
| 6 | `--file .planning/DECISIONS.md` → hard-refuse | `add.test.js#flag-file-decisions` | I | ✓ |
| 7 | `listMilestones` decimal-sorted | `milestones.test.js#listMilestones-decimal-sort` | U | ✓ |
| 8 (GAP-1) | OPEN-QUESTIONS + MILESTONE footers preserved/rewritten | `add.test.js#footer-rewrite-per-destination` | I | ✓ |
| 9 (GAP-2) | Multiple destination flags → exit non-zero before write | `add.test.js#mutually-exclusive-flags` | I | ✓ |

### Slice 3 — 6 criteria

| # | Criterion | Test | Type | Proof-of-fail-first? |
|---|---|---|---|---|
| 1 | Naked `/sig:add` prompts; response routes per heuristics | `add.test.js#naked-invocation` (mocked AskUserQuestion) | I | ✓ |
| 2 | Naked + empty response → exit 0, no write | `add.test.js#naked-empty-aborts` | I | ✓ |
| 3 | Input ending `?` → OPEN-QUESTIONS recommended | `add.test.js#heuristic-question-mark` | I | ✓ |
| 4 | `fix|bug|TODO` prefix + current milestone → milestone recommended | `add.test.js#heuristic-fix-prefix` | I | ✓ |
| 5 | Plain input → silent default FUTURE-IDEAS | `add.test.js#heuristic-no-match-default` | I | ✓ |
| 6 | `suggestDestination` table-driven 12-case test | `add.test.js#suggestDestination-table` | U | ✓ |

### Slice 4 — 6 criteria

| # | Criterion | Test | Type | Proof-of-fail-first? |
|---|---|---|---|---|
| 1 | First `/sig:add` shows onboarding; second does not | `add.test.js#onboarding-warning-once` | I | ✓ |
| 2 | `.add-onboarded` flag git-tracked-by-default | Manual + `.gitignore` audit | M | ✓ (audit confirms no exclusion rule) |
| 3 | Brownfield detection error mentions `/sig:init` | `add.test.js#brownfield-detection-message` | I | ✓ |
| 4 | `gate_strictness: strict` shows entry preview | `add.test.js#gate-strictness-strict-preview` | I | ✓ |
| 5 | `gate_strictness: off` still confirms destination | `add.test.js#gate-strictness-off-confirms-dest` | I | ✓ |
| 6 | Vocab-lint catches planted `tranche` term | `validate-plugin.test.js#vocab-lint-catches-banned-term` | V + U | ✓ (planted file fails; clean file passes) |

### Slice 5 — 4 criteria

| # | Criterion | Test | Type | Proof-of-fail-first? |
|---|---|---|---|---|
| 1 | `/sig:plan` reads FUTURE-IDEAS, surfaces dated entries from window | `plan.test.js#plan-reads-future-ideas` | I | ✓ |
| 2 | Empty FUTURE-IDEAS → silent skip with note | `plan.test.js#plan-empty-future-ideas` | I | ✓ |
| 3 | Include/defer/cull flow per entry; cull requires confirmation | `plan.test.js#plan-cull-confirmation` | I | ✓ |
| 4 | Included items appear as candidate tasks in resulting plan | `plan.test.js#plan-includes-promoted-entries` | I | ✓ |

### Aggregate test count

| Type | Count |
|---|---|
| Unit (U) | 4 |
| Integration (I) | 28 |
| E2E (E) | 0 (smoke is manual per Epic DoD) |
| Validator (V) | 2 |
| Manual (M) | 3 |
| **Total** | **37 acceptance criteria → 37 tests** |

Tests / acceptance criteria coverage: **100% — Nyquist satisfied.**

**New fixture directories:** `tests/fixtures/add/future-ideas-minimal/`, `tests/fixtures/add/open-questions-minimal/`, `tests/fixtures/add/milestone-current/`, `tests/fixtures/add/milestone-other/`, `tests/fixtures/add/brownfield/`, `tests/fixtures/add/onboarded/`. Six new fixtures; each ~30-50 lines.

**New test files:** `tests/add.test.js` (covers slices 1-4, ~25 test cases), `tests/milestones.test.js` (S2 helper, ~6 test cases), `tests/plan.test.js` extension (S5, ~4 test cases). Existing `tests/validate-plugin.test.js` extended for V tests (or created if absent — codebase research showed `tools/validate-plugin.js` is the validator but didn't confirm a test file; check during S1 execution).

---

## Part 3 — Environment check

Pre-EXECUTE verification (run as final PLAN step):

- [ ] **Node 22+** — to verify at execute-time: `node --version` ≥ v22.0.0. Plan assumes; verify on dev machine.
- [ ] **`npm install --dry-run` clean** — to verify at execute-time. Plan adds no new deps; should resolve cleanly with existing `package.json`.
- [ ] **No native modules** — confirmed by plan (pure-JS file ops). No Xcode CLT / build-essential required.
- [ ] **POSIX `fs.rename` atomic on Mac** — confirmed (M4.t8 already exercises this pattern in `walkthrough.js`).
- [ ] **`yaml` package present** — not directly used by `/sig:add` but check existing `tools/lib/profile.js` loads it; no version bump needed.
- [ ] **No registry yank** — `package.json` has no version pins introduced by this plan; existing deps unchanged.

**Status:** All checks deferrable to execute-time Slice 1 (when `tools/lib/add.js` first uses Node APIs). Plan-time check is documented; no blocker found.

---

## Part 4 — Required PLAN.md revisions before approval

Three gaps surfaced during validation. Revise `M4.5.E2-PLAN.md` to close them before user approval:

1. **GAP-1** — Add S2 acceptance criterion: "OPEN-QUESTIONS.md and MILESTONE-*.md footer markers (`---`, `*Created ...*`) preserved/rewritten correctly per fixture test."
2. **GAP-2** — Add S2 acceptance criterion: "Multiple destination flags (e.g., `--question --milestone`) exit non-zero with clear error before any write."
3. **GAP-3** — Add cross-slice constraint: "Each slice updates README workflow table if its user-visible behavior changes (S2 flags, S3 cold-path UX, S4 onboarding warning, S5 plan.md changes)."

Plus one optional addition:
4. **OPTIONAL** — Add S1 acceptance criterion: "Body > 4000 chars warns with confirm prompt (no hard cap)." [Top-5 risk #3 lower-tier coverage; minor gap. User decides whether to include.]

---

## Final verdict

**8-Dimension Plan Validation:** PASS with 3 minor revisions (no restructuring; add 3 acceptance criteria + 1 optional).
**Nyquist Test-Coverage Mapping:** 100% — every acceptance criterion maps to a specific test of a specific type. Strict mode satisfied.
**Environment Check:** Pre-clean; verify at execute-time.

**Plan is ready for user approval** pending GAP-1/2/3 closure (small edits to PLAN.md).

---

*Validated 2026-05-14 from M4.5.E2-PLAN.md + M4.5.E2-RESEARCH.md. Tier: FULL; gate strictness: strict; Nyquist enforcement: strict.*

---

# 2026-05-30 RE-VALIDATION — Slices 2–5

Validates the 2026-05-30 RE-PLAN (`M4.5.E2-PLAN.md` § "2026-05-30 RE-PLAN") against the 8 dimensions + Nyquist, FULL tier (`plan_validation_dims: all`, `nyquist_enforcement: strict`).

## 8-Dimension Validation

| # | Dimension | Verdict | Notes |
|---|---|---|---|
| 1 | **Goal alignment** | ✅ PASS | Every slice serves "frictionless capture + flag routing + advisory drain." S2 routing+spine; S3 naked capture; S4 safety; S5 drain. No orphan task. |
| 2 | **Completeness** | ✅ PASS | FR1→S2.t4; FR2→S2.t2/t5; FR3→S2.t6; FR4→S2.t3; FR5→S3.t1/t2; FR6→S4.t1/t2; FR7→S5.t1–t4; FR8→each slice's docs/CHANGELOG/validate task. Q1→S4.t1; Q2→S5.t1. All FR1–FR8 + both open questions covered. |
| 3 | **Dependency correctness** | ✅ PASS | S2→S3→S4→S5 linear, minimal. S2.t1 (spine) is the real prerequisite for t4/t5/t6. S5 depends on S1's populated FUTURE-IDEAS. No false/missing edges. |
| 4 | **Testability** | ✅ PASS | Every task has explicit ACs + named test type. Prompts unit-tested via injected callbacks (S1 pattern). Hard gates (R1/R2/R5-delete) have concrete snapshot/refusal tests. |
| 5 | **Scope discipline** | ✅ PASS | Heuristics CUT; 5th destination deferred; no per-slice tags; `--file` undocumented. S2.t1 spine refactor closes R7 + DRY (justified, not gold-plating). Nothing added beyond REQUIREMENTS. |
| 6 | **Context feasibility** | ✅ PASS | Each task = one small module/function + tests (≤~5 files). `milestones.js`, `drain.js` small. No whole-codebase task. |
| 7 | **Risk coverage** | ✅ PASS | R1→S5.t1/t2 + S5.t3 preview gate; R2→S2.t6; R3→S2.t4/t5; R4→S2.t3; R5→S5.t1/t2 (byte-range + delete/merge sub-gate); R6→S3.t1; R7→S2.t1. All 7 mapped; 2 hard gates + 1 sub-gate land in tasks. |
| 8 | **Vertical slicing** | ✅ PASS | Each slice ships a usable increment (S2 flags; S3 naked capture; S4 safer capture; S5 loop closed). S2.t1 spine is an internal refactor *within* S2, shipped with t3–t7 as one user-visible unit — not a horizontal layer. |

**Overall: PASS across all 8 dimensions.**

Flagged deviations for the gate: (a) **Q2** = drain surfaces all un-dispositioned entries, no date window — supersedes FR7.1's placeholder. (b) **Q1** = no destination-confirm at any level; `gate_strictness` modulates the first-run onboarding note instead — narrows AC-FR6.3 (asserts note behavior, not confirm counts).

## Nyquist Test-Coverage Mapping (strict)

| FR / AC | Test type | Artifact |
|---|---|---|
| FR1 (OPEN-QUESTIONS write + R3) | integration + full-file equality | `tests/add.test.js`, fixture `open-questions-minimal/` |
| FR2.1–2.5 (milestone resolve + holding section + errors) | unit + integration | `tests/milestones.test.js`, `tests/add.test.js`, fixtures `milestone-minimal/` + STATE |
| FR3.1–3.3 (`--file` refuse, R2 hard gate) | integration | `tests/add.test.js` |
| FR4 (multi-flag guard, R4) | unit + integration | `tests/add.test.js` |
| FR5.1–5.4 (naked interview, R6, no heuristics) | integration + unit export-surface | `tests/add.test.js` |
| FR6.1–6.3 / Q1 (onboarding note × gate_strictness matrix, brownfield error) | integration via PROFILE fixtures | `tests/add.test.js` |
| FR6.4 (vocab lint catches `tranche`) | unit (`findJargonHits`) + validator subprocess | `tests/add.test.js` / vocab-lint test |
| FR7.1–7.4 / Q2 (drain candidates + dispositions, R1) | unit (parser, disposition snapshot) + integration (plan drain) | `tests/drain.test.js` |
| FR7.5 (anti-rationalization row) | contains/snapshot | `tests/cross-file-consistency.test.js` or inline |
| FR8.1–8.3 (validator/README/CHANGELOG/no-deps) | validator subprocess + install dry-run | vocab-lint/validator test |
| R1 (drain byte-identical neighbors) | full-file snapshot, multi-entry fence fixture | `tests/drain.test.js` |
| R5 (no cross-contaminate; date-in-heading insert; delete confirm) | unit snapshots | `tests/drain.test.js` |
| R7 (scrub on all destinations) | integration (secret per dest) | `tests/add.test.js` |

**Nyquist verdict: PASS — every AC has ≥1 mapped test; R1/R2 hard gates + R5 delete/merge sub-gate each have a dedicated proof test.**

*Re-validation completed 2026-05-30. 8 dimensions PASS. Nyquist strict mapping complete. Two flagged deviations (Q1 narrows AC-FR6.3; Q2 supersedes FR7.1). Ready for EXECUTE gate pending user approval.*
