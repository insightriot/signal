# M4.5.E11 — Verification (VERIFY phase)

**Phase:** VERIFY. **Effective tier:** FULL (`nyquist_enforcement: strict`, `gate_strictness: strict`) — `readEffectiveProfile(baseDir, {currentEpic: M4.5.E11})` → project PROFILE (no Epic PROFILE; E11 runs at project FULL). **Verdict: ✅ PASS** — every AC verified with evidence; full suite + validator green; strict-Nyquist satisfied (RED-first for extractable helpers, prose-attestation for markdown ACs).

## 1. Full test suite + build

- **Suite:** `npx vitest run` → **999 passed / 999** (49 files). Baseline entering the Epic was 894; S1 → 948; S2+S3 → 997; S4 → 999 (+105 across the Epic; +51 this VERIFY-scoped span S2–S4).
- **Build/lint:** `node tools/validate-plugin.js` → **Plugin validation passed.** No JS build step beyond the validator; no eslint config in-repo (validator is the structural gate). No new warnings.

## 2. Nyquist compliance (strict) — every AC mapped

Strict evidence forms (either sufficient): **(a)** separate red→green git commits; **(b)** attestation that the test was written *before* the implementation, naming file + commit. Signal's atomic-commit-per-slice workflow uses **(b)** — the RED run was observed before each helper existed (visible in the EXECUTE transcript), then implemented to GREEN in the same task commit. Fixtures/integration ACs are golden/against-disk. Markdown-prose ACs are prose-review + validator + the explicit attestation below (the 2026-04-26 evidence form).

### Slice 1 (executed + closed in the prior session; re-verified green here)

| AC | Evidence | Status |
|---|---|---|
| S1.t1 `EPIC_ID_STRICT_RE` accepts real IDs / rejects `v0.1.6`/`''`/`../x`; writer IDs round-trip `currentMilestone` | `epic-native.test.js` describes "S1.t1 …" (15 accept/reject + depth-2 round-trip) | ✅ |
| S1.t2 `setCurrentEpic` valid→round-trips; invalid→throws pre-disk; roll resets coupled fields; idempotent | describe "S1.t2 setCurrentEpic" (5 cases) | ✅ |
| S1.t3 `detectMode` fail-open matrix | describe "S1.t3 detectMode" | ✅ |
| S1.t4 hook fail-open (exit 0) + warn-not-block | `hook-check-state-write-spawn.test.js` / `hook-warn-dirty-execute.test.js`; commit `92c0c8d` | ✅ |
| S1.t5 `isEpicDone` + done-Epic guard | describe "S1.t5 isEpicDone"; commit `4b10bd0` | ✅ |
| S1.t6 linear byte-identity | linear golden fixture + full suite green, no fixture edits; commit `8d36e38` | ✅ |
| S1.t7 `deriveNextEpicId` + `--epic` wiring | describe "S1.t7 deriveNextEpicId"; commit `77dcc5e` | ✅ |

### Slice 2 — Epic-scoped artifact naming (FR2)

| AC | Evidence | Status |
|---|---|---|
| S2.t1 Epic→`{EpicID}-{artifact}.md` (all 8); linear→`1-*` except REQUIREMENTS→`REQUIREMENTS.md`; write→read round-trip; DISCUSS-REQUIREMENTS asymmetry; RETROSPECTIVE = `deriveRetroPath` | describe "S2.t1 artifactName" — **exact-equality** pins (not just round-trip) for the REQUIREMENTS asymmetry, `deriveRetroPath` agreement pin, fail-open on non-strict `currentEpic`, epic+linear round-trip. RED observed (26 failing) before impl; commit `35efecd` (b) | ✅ |
| S2.t2 Epic-mode PLAN writes `{EpicID}-PLAN.md`; linear writes `1-PLAN.md` | **Attestation:** `artifactName(...)` threaded into the write points of discuss/plan/execute/verify/review (`grep -l` confirms 5/6); **ship.md correctly excluded** — it reads via `resolveArtifactPath` and names the retro via `deriveRetroPath` (retro single-owner carve-out). Per-command "Artifact naming (M4.5.E11)" note governs all `{phase}-*.md` refs. Logic unit-tested in S2.t1. Validator green. Commit `4678361` | ✅ |
| S2.t3 Epic-mode resolve finds `{EpicID}-PLAN.md`, no "not found" | describe "S2.t3 Epic-mode golden fixture" — `tests/fixtures/epic-native/epic/` (M4.5.E99), resolve NOT null + write→read symmetry against a real fixture. Commit `1f43d85` | ✅ |

### Slice 3 — Per-Epic calibration (FR3)

| AC | Evidence | Status |
|---|---|---|
| S3.t1 no-`currentEpic`→project (byte-identical golden); Epic file→Epic; no Epic file→project fallback; malformed Epic PROFILE→`ProfileSchemaError`; fail-open on non-strict `currentEpic` | describe "S3.t1 readEffectiveProfile" (10 cases incl. R7 `toEqual(readProfile)` golden + neither-exists not-found). RED observed (10 failing) before impl; commit `82eabf3` (b) | ✅ |
| S3.t2 opening an Epic can set its tier; escalate on active Epic writes the Epic PROFILE | **Attestation:** discuss.md `--epic` open flow offers per-Epic calibrate → `{EpicID}-PROFILE.md`; calibrate.md §5 + escalate.md §5 target `.planning/{current_epic}-PROFILE.md` when a strict Epic is active (R10), project PROFILE untouched (`grep -l` confirms all 3). Helper (`readEffectiveProfile`) unit-tested in S3.t1. Commit `c29f388` | ✅ |
| S3.t3 status/resume show `Tier: SKETCH (Epic … override; project default FULL)` — never silent | describe "S3.t3 formatTierLine" (differing→provenance, same-tier/linear/non-strict→bare) + "renderResumeBriefing surfaces the override". RED observed (9 failing) before impl. **Surface attestation:** `formatTierLine`/`readEffectiveProfile` wired into resume.md + status.md (`grep -l` confirms both). Commits `07e9ac9`, `e1d3644` | ✅ |
| S3.t4 SKETCH Epic in FULL project runs its phases at SKETCH; project PROFILE unchanged | **Attestation + integration:** all 6 phase commands' §0 gate-read reads `readEffectiveProfile(baseDir, {currentEpic})` (`grep -l` confirms 6/6); not-found halt copy byte-unchanged; fail-open on non-strict `current_epic`. Runtime chain verified: `execute.md` passes the effective `profile` into `dispatchTaskWithState(baseDir, task, profile)` which keys SKETCH-disable/FULL-halt off `profile.tier` (`execute.js:60,66`); **no rigor path reads the project profile directly** (`grep -rn "readProfile(" tools/lib/` clean — only `readEffectiveProfile`'s own fallback). S4.t2 e2e proves Epic-SKETCH shadows project-FULL against disk. Commit `cbff2fd` | ✅ |

### Slice 4 — Bootstrap dogfood + docs

| AC | Evidence | Status |
|---|---|---|
| S4.t1 E11's flow runs with auto-managed `current_epic` + Epic-named artifacts | **Attestation** (`M4.5.E11-PROGRESS.md` § S4.t1): each by-hand E11-bootstrap step → its automating helper, with evidence; honest caveat that E11's Epic pre-dates the write-half, so the S4.t2 fixture is the mechanical proof. Commit `2b56c59` | ✅ |
| S4.t2 end-to-end mode+name+profile chain against disk | describe "S4.t2 end-to-end Epic-native chain" — opens `M4.5.E77` via `setCurrentEpic` (zero hand-edited STATE) → `detectMode` → `readEffectiveProfile` shadow → `formatTierLine` → 7-artifact write→read round-trip; + roll-to-next-Epic re-points the chain. Commit `116b85a` | ✅ |
| S4.t3 docs (Epic mode + two-tier retro); MILESTONE-4.5 § E11; no linear migration | `references/epic-native-flow.md` + `MILESTONE-4.5.md` § E11; both state "no linear-project migration" explicitly. Commit `e5becf9` | ✅ |
| S4.t4 validator + full suite + `markFresh` | Validator green; 999 tests; `markFresh` advanced `last_updated_commit` → `3a1ac75`. Commits `3a1ac75`, `6ae07f7` | ✅ |

**No unmapped AC.** Every VALIDATION.md Nyquist row has a corresponding test or explicit attestation above.

## 3. Non-functional requirements (re-checked)

- **Additive / linear byte-identical:** linear golden fixture (`tests/fixtures/epic-native/linear/`) resolves `1-PLAN.md`, full suite green with no existing-fixture edits. ✅
- **No new runtime dependencies:** no `package.json` deps added across S1–S4. ✅
- **No `schema_version` bump:** per-Epic tier rides in a separate `{EpicID}-PROFILE.md`, not a STATE field. ✅
- **Fail-open:** every Epic-mode read (`detectMode`, `readEffectiveProfile`, `artifactName`, `formatTierLine`) degrades to linear on a null/non-strict/garbage `current_epic` — never throws on the STATE value. Unit-covered. ✅
- **Platform-agnostic:** no macOS/`~/.claude`-scoped paths in the flow. ✅

## 4. Anti-Rationalization (strict exit)

| Temptation | Check |
|---|---|
| "Tests pass, so it works" | The FR3 runtime chain was verified beyond tests — grepped every `readProfile(` call site to confirm no rigor path bypasses `readEffectiveProfile` (the thing a green suite couldn't see). |
| "That edge case won't happen" | Fail-open on non-strict `current_epic` is in the ACs → verified for `artifactName`, `readEffectiveProfile`, `formatTierLine`, `detectMode`. |
| "Close enough" | The REQUIREMENTS-asymmetry AC is pinned by exact equality (not a round-trip that would pass a wrong `1-REQUIREMENTS.md`); RETROSPECTIVE pinned to `deriveRetroPath`. |

## 5. Known limits / carry-forward

- **Prose ACs (S2.t2 / S3.t2 / S3.t3-surface / S3.t4) are attestation-verified, not unit-tested** — by design (markdown is LLM-instruction prose; the underlying helpers are unit-tested). Their behavioral proof is this attestation + the S4.t2 e2e + REVIEW's prose read. This is the plan's intended coverage, not a gap.
- **Two-tier retro split (B2)** surfaced at the plan gate remains an open design choice for SHIP: `/sig:ship` §0.5 keeps E9's hard retro gate; downgrade-to-warn is a one-line change if preferred. Carry to SHIP.
- **Staleness floor:** post-`markFresh`, `/sig:resume` shows the inherent 1-commit self-reference (the markFresh commit). Expected, not drift.

## Last Updated

2026-07-15 — VERIFY complete. Verdict PASS. Next: REVIEW.
