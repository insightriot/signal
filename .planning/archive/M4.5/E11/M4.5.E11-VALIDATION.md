# M4.5.E11 — Validation (8-dimension + Nyquist)

**Phase:** PLAN. **Tier:** FULL (`plan_validation_dims: all`, `nyquist_enforcement: strict`). Validated by the `plan-checker` agent (adversarial, claims verified against `main`) + Nyquist mapping below.

## 8-Dimension validation

Initial verdict: **PASS-WITH-CONCERNS** (2 blocking, 3 non-blocking). All closed in-phase; re-stated post-fix:

| Dim | Initial | Post-fix | Resolution |
|---|---|---|---|
| 1. Goal alignment | CONCERN | **PASS** | S1 had no task for "a command opens an Epic + auto-writes `current_epic`" → added **S1.t7** (`--epic` wiring + `deriveNextEpicId`). |
| 2. Completeness | CONCERN | **PASS** | `--epic` parsing + Epic-ID assignment were homeless → S1.t7. D-E11-5 was cosmetic (touched only the rarely-firing hook) → **B2 two-tier split** documented (Architecture decisions + S4.t3). |
| 3. Dependency correctness | CONCERN | **PASS** | S2.t2 & S3.t4 edit the same 6 command files → **serialization note (C2)** added; pure-helper tasks stay parallel. |
| 4. Testability | PASS | **PASS** | Helper-extraction mapping is honest; every non-unit AC is explicitly attestation-tagged (verify.md house pattern). No AC secretly untestable. |
| 5. Scope discipline | PASS | **PASS** | No creep into the parked `features[]`/subdir layer. Orphan `clearCurrentEpic` dropped (C3, YAGNI). |
| 6. Context feasibility | PASS | **PASS** | All tasks S/M; largest is mechanical 6-file prose editing. |
| 7. Risk coverage | CONCERN | **PASS** | R1/R2/R4/R7/R10 each have a task; **R3 corrected** — the real block (`shipFR1Check`, command layer) is now explicitly addressed by the two-tier split (hook warns; command gate retained by design), not left over-claimed. |
| 8. Vertical slicing | CONCERN | **PASS** | S1 was a helper library missing its command surface → S1.t7 wires it, making S1 a full vertical slice. |

**Overall: PASS** (post-fix). One design choice is **surfaced for the plan gate, not silently resolved**: the two-tier retro split (B2) keeps `/sig:ship`'s own §0.5 retro gate hard — override to warn is a one-line change if Brett prefers.

**Verified-solid (plan-checker, not rubber-stamped):** every named helper (`setCurrentEpic`, `detectMode`, `artifactName`, `readEffectiveProfile`, `isEpicDone`, `deriveNextEpicId`) is net-new (grep-confirmed) → RED-first extraction is real. R2 reachable (`check-state-write.js:103` unguarded → `deriveRetroPath` throws). R4 safe by construction (pattern-0 existence-gated, ahead of numeric; linear passes `null`). R10 real (`calibrate.md`/`escalate.md` hard-code `.planning/PROFILE.md`).

## Nyquist test-coverage map (strict)

Principle: every AC whose logic lives in code gets a **RED-first** unit/integration test on the extracted pure helper; markdown-prose ACs (the 6-command write-points + gate-reads) are covered by prose-review + validator + explicit `M4.5.E11-VERIFICATION.md` attestation (the 2026-04-26 evidence form). No AC is unmapped.

| Task | AC | Test type |
|---|---|---|
| S1.t1 | validator accepts `M4.5.E11`/`M5.E1`, rejects `v0.1.6`/`''`/`../x`; writer IDs round-trip through `currentMilestone` | **unit** |
| S1.t2 | `setCurrentEpic` valid→round-trips; invalid→throws pre-disk; coupled `current_wave`/`current_tasks` reset on roll; other frontmatter preserved; re-set = no-op | **unit** (mirror `setCurrentTask`) |
| S1.t3 | `detectMode`: null/absent/`''`/whitespace→linear; `M4.5.E11`→epic | **unit** |
| S1.t4 | malformed `current_epic` SHIP write→exit 0; hook-path Epic-SHIP w/o retro→exit 0 + stderr warn (never exit 2) | **integration** (hook spawn) |
| S1.t5 | done Epic + no `--epic`→error; done Epic + `--epic new`→rolls; live Epic + no flag→proceeds | **unit** (`isEpicDone`) + **attestation** (command rule) |
| S1.t6 | linear behavior byte-identical to pre-E11 | **golden** + **regression** (894 suite green, no fixture edits) |
| S1.t7 | `--epic <label>`→auto-assigned strict ID in `current_epic` (no hand-typing); explicit strict-ID arg used verbatim; `deriveNextEpicId`→`M4.5.E12` after `M4.5.E11` | **unit** (`deriveNextEpicId`) + **attestation** (flag parse) |
| S2.t1 | `artifactName`: strict `currentEpic`→`{EpicID}-{artifact}.md`; null→`1-{artifact}.md`; write→read round-trip all 8 kinds; DISCUSS-REQUIREMENTS asymmetry handled | **unit** + **round-trip** |
| S2.t2 | Epic-mode PLAN writes `{EpicID}-PLAN.md`; linear writes `1-PLAN.md` | **prose-review** + **validator** + **attestation** (logic in t1) |
| S2.t3 | Epic-mode `/sig:resume` finds `{EpicID}-PLAN.md`, no "not found" | **integration** (fixture) |
| S3.t1 | `readEffectiveProfile`: no-`currentEpic`→project (byte-identical golden); Epic file exists→Epic; no Epic file→project fallback; malformed Epic PROFILE→`ProfileSchemaError` | **unit** + **golden** |
| S3.t2 | opening an Epic can set its tier; escalate on active Epic writes the Epic PROFILE | **attestation** (+ helper unit) |
| S3.t3 | status/resume show `Tier: SKETCH (Epic … override; project default FULL)` — shadowing never silent | **unit** (render) |
| S3.t4 | SKETCH Epic in FULL project runs its phases at SKETCH; project `PROFILE.md` unchanged | **prose** + **attestation** + **integration** (tier-gating) |
| S4.t1 | E11's own EXECUTE→SHIP runs with zero hand-edited STATE, auto-Epic-named artifacts | **attestation** (dogfood evidence in VERIFICATION) |
| S4.t2 | end-to-end mode+name+profile chain against disk | **integration** (fixture) |
| S4.t4 | validator green + full suite green + `markFresh` | **regression** |

## Last Updated

2026-07-15 — 8-dim PASS (post-fix); Nyquist map complete, no unmapped AC. Next: commit PLAN artifacts → `markFresh` → plan-approval gate.
