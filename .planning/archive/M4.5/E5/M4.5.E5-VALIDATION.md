# M4.5.E5 — Plan Validation (PLAN output)

**Phase:** PLAN (output of `/sig:plan`, 2026-06-03). **Tier:** FULL — all 8 dimensions + strict Nyquist (`plan_validation_dims: all`, `nyquist_enforcement: strict`).
**Validates:** `M4.5.E5-PLAN.md` (4 slices, 9 tasks) against `M4.5.E5-REQUIREMENTS.md` (8 FRs, 9 ACs).

## 8-Dimension Validation

### 1. Goal alignment — PASS
Phase goal: produce every Claude-draftable launch asset for a quiet peer release. Every task maps:
- t1–t4 → the launch narrative (post + demo) + its guard/validator wiring.
- t5–t6 → the peer validation kit (brief + friction-log).
- t7 → the internal launch kit (rubric + release-notes + handoff + channels).
- t8 → CHANGELOG (release wiring).
- t9 → verification sweep.
No task serves anything outside the goal. The human outward-actions are explicitly **out of scope** (captured as the t7 handoff checklist), consistent with D-E5-6.

### 2. Completeness — PASS (FR→task coverage)
| FR | Covered by | 
|---|---|
| FR1 launch post | S1.t2 (+ guard S1.t1) |
| FR2 demo script | S1.t3 |
| FR3 tester brief + friction-log | S2.t6 (+ guard S2.t5) |
| FR4 who-to-ask guidance | S2.t6 (folded into the brief, per requirements "decide in PLAN") |
| FR5 version rubric | S3.t7 §a (inline, R1) |
| FR6 release-notes draft | S3.t7 §b |
| FR7 handoff checklist | S3.t7 §c |
| FR8 CHANGELOG + validator/test wiring | S1.t4 (validator) + S3.t8 (CHANGELOG) + S1.t1/t3/t5 (test) |
All 8 FRs covered. R1/R2 settled in the plan's architecture-decisions section. Bonus: dangling `CHANGELOG.md:3` ref fix folded into S3.t8.

### 3. Dependency correctness — PASS
Graph is acyclic and minimal (see PLAN § Dependency graph). t1 (guard scaffold) gates the doc tasks that turn it green; t8 (CHANGELOG) depends on the docs it names; t9 (sweep) depends on all. S2 and S3 are genuinely independent (no false ordering imposed beyond the linear FULL/strict execution).

### 4. Testability — PASS (docs-Epic posture)
Every AC has a verification (see Nyquist map below). Automated where mechanically checkable (existence, link-resolution, word-count, structural-marker presence, validator subprocess); manual-acknowledged where it requires human judgment (prose accuracy, usability) — the same split E4 used (its 17 automated / 7 manual). No AC is unverifiable.

### 5. Scope discipline — PASS
No gold-plating. Guards against the two creep vectors are explicit: **no `docs/versioning.md`** (R1 — would unshelf E1), and **no README link to the draft post** (deferred to publish-time handoff). No new examples (D-E5-10). Internal kit is one consolidated file, not four. The demo is a *script*, not a recording (Claude can't record).

### 6. Context feasibility — PASS
Every task touches 1 file (t1/t2/t3/t4/t6/t7/t8) or is verification-only (t9). t3 touches the doc + appends to the guard (2 files). t7 is the largest (one ~M internal doc, four sections) but well within a single focused session. No task exceeds ~5 files. All S/XS except t7 (M) and t2 (S, high-care).

### 7. Risk coverage — PASS
All five PLAN risks have mitigations (PLAN § Risks). The headline risk (over-claiming) is mitigated at three layers: baked-into-acceptance guardrails on t2, the manual accuracy read, and the locked "ports two / analyzed seven" framing from research. The green-checkpoint risk is mitigated by the same-slice add-doc-and-its-assertions discipline.

### 8. Vertical slicing — PASS
Each slice is a complete, independently shippable asset path (doc + its guard + its wiring), not a horizontal layer. S1 = launch narrative; S2 = validation kit; S3 = launch ops; S4 = verify. Any slice could ship alone and leave the repo coherent + green. Highest-risk asset (post) is in S1 (fail-fast).

**Verdict: 8/8 PASS.** Plan is executable without further clarification.

---

## Nyquist Test-Coverage Mapping (strict)

Strict Nyquist requires every acceptance criterion to map to a verification. For a docs Epic the verification types are: **automated** (guard test `tests/e5-launch-assets.test.js` + validator subprocess) and **manual-acknowledged** (human judgment on prose accuracy/usability — recorded with evidence at VERIFY). Proof-of-fail-before-pass (red→green) is captured for the automated guards via the test-first tasks (t1, t5).

| AC | Verification | Type | Red→green evidence |
|---|---|---|---|
| **AC1** — launch post exists, ~600–800 words, leads landscape→wedge, heritage-respectful, no fabricated validation, required links | word-count band (550–850) + required-link presence = automated; "leads with…/heritage-respectful/no over-claiming" = manual accuracy read | automated + manual | t1 writes the assertion RED (file absent) → t2 GREEN |
| **AC2** — demo script: reproducible command sequence + narration beats, assumptions stated | assumptions-block + command-sequence presence = automated; "a reader could record from this" = manual | automated + manual | t3 adds assertion; RED until demo-script written, GREEN same task |
| **AC3** — tester brief usable async, friction-log template present, nothing sensitive, Mac caveat | template-marker + Mac-caveat presence + link-resolution = automated; "usable async / nothing sensitive" = manual | automated + manual | t5 RED → t6 GREEN |
| **AC4** — version rubric committed, gives written basis for the call | file/section existence = automated (existence) ; rubric soundness = manual | existence-automated + manual | n/a (existence check) |
| **AC5** — release-notes draft exists, derived from CHANGELOG, links the post | existence + link-to-post resolution = automated; "derived from CHANGELOG" = manual | existence-automated + manual | covered by sweep (t9) |
| **AC6** — CHANGELOG has E5 entry under `[Unreleased]`, coherent | manual diff (structural); no-dead-link via sweep | manual + automated(links) | n/a |
| **AC7** — handoff checklist committed | section existence | existence-automated | n/a |
| **AC8** — every link in new/changed docs resolves | relative-link resolution over all E5 docs | **automated** | t1 establishes the link-resolver RED; greens as docs land; full sweep t9 |
| **AC9** — validator green, no new runtime deps, suite green | `node tools/validate-plugin.js` subprocess smoke test + `npx vitest run` + `package.json` unchanged | **automated** | t4 registers launch-post.md; validator green; suite green |

**Coverage summary:** 9/9 ACs mapped. Automated mechanical coverage: AC1 (partial), AC2 (partial), AC3 (partial), AC8 (full), AC9 (full), + existence checks for AC4/AC5/AC7. Manual-acknowledged (human judgment, evidenced at VERIFY): the prose-quality / accuracy / usability halves of AC1–AC6. This mirrors the E4 precedent where doc-quality NFRs are partly manual — appropriate for a docs Epic; no test-theater manufactured for judgment-based criteria.

**Strict-Nyquist note:** the two genuinely test-first tasks (t1 launch-post guard, t5 tester-brief guard) provide explicit red→green for the mechanical assertions (link-integrity, word-count, structural markers). The manual halves are attested in VERIFY with the artifact as evidence, per the two-form Nyquist convention (`references/tier-definitions.md` / DECISIONS M3 Task 5).
