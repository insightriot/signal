# M4.5.E6 — Resume Reliability — Plan Validation + Nyquist Test Coverage

**Phase:** PLAN. **Tier:** FULL — `plan_validation_dims: all` (8 dimensions); `nyquist_enforcement: strict` (per-test attestation of test-first written-before-implementation).
**Validates:** `M4.5.E6-PLAN.md`.
**Created:** 2026-05-17.

---

## Part 1 — Plan Validation (8 Dimensions)

### Dim 1 — Goal Alignment

**Question:** Does every task serve the phase goal?

**Phase goal:** "Ship a STATE.md schema + auto-update protocol that makes `/sig:resume` an unambiguous, validated picture of where the user left off — even after a full context-clear mid-EXECUTE — so session-to-session handover stops being prohibitively expensive."

| Task | Serves goal? | How |
|---|---|---|
| S1.t1–t2 | Yes | Shared modules enable atomic + lock-safe STATE.md writes (foundation for everything else) |
| S1.t3–t4 | Yes | Frontmatter parser + migration unblock the schema-versioned design (D1, D4, D14) |
| S1.t5 | Yes | `readState` three-way detection is the contract every consumer reads through |
| S1.t6 | Yes | `setCurrentTask`/`clearCurrentTask` ARE the auto-protocol (D3 + D10) |
| S1.t7–t8 | Yes | Orphan + staleness detection are the safety nets that make resume trustable |
| S1.t9–t10 | Yes | Decision + blocker tracking are part of "complete picture for handover" |
| S1.t11–t12 | Yes | Existing readers/writers must use new schema or contract breaks |
| S2 (all tasks) | Yes | `/sig:checkpoint` is the manual peace-of-mind layer (user-chosen; complementary to auto-protocol) |
| S3 (all tasks) | Yes | dispatchTaskWithState IS the auto-protocol's hook into EXECUTE |
| S4 (all tasks) | Yes | renderResumeBriefing + staleness/orphan UI is what the user actually sees after context-clear |
| S5.t1 | Yes | Integration test proves AC#7 (auto-verification of the end-to-end handover) |
| S5.t2–t7 | Yes | Validator + docs are how the work becomes durable + discoverable |
| S5.t8 | Yes | AC#8 dogfood is the load-bearing "did we actually fix the problem" test |

**Verdict:** PASS. No off-goal tasks.

### Dim 2 — Completeness

**Question:** Are all requirements (AC + locked decisions) covered?

**Acceptance criteria (8) — all mapped** (see PLAN.md "Acceptance Criteria — final mapping" section). Spot-check:
- AC#1 (wave-boundary reporting) → S1 + S3 ✓
- AC#2 (post-context-clear briefing) → S1 + S3 + S4 + S5.t1 ✓
- AC#3 (/sig:checkpoint idempotent + always callable) → S2 ✓
- AC#4 (schema backwards-compat + auto-upgrade) → S1.t4 + S1.t5 + D15 gate ✓
- AC#5 (staleness warning) → S1.t8 + S4.t2 ✓
- AC#6 (tier-aware) → S5 + S3 ✓
- AC#7 (existing tests green + new tests cover all) → S1-S5 tests + S5.t1 ✓
- AC#8 (dogfood) → S5.t8 ✓

**Locked decisions (16) — all reflected in tasks:**
- D1 amended (yaml package) → S1.t3 explicitly uses existing `yaml` lib ✓
- D2 (two-mode checkpoint) → S2.t1 + S2.t5 ✓
- D3 (per-task cadence) → S3.t1 ✓
- D4 (auto-extend migration) → S1.t4 ✓
- D5 (markStale/markFresh in verify/review) → S4.t4 + S4.t5 ✓
- D6 (state-affecting paths scope) → S1.t8 ✓
- D7 (unit + fixture tests) → entire test suite ✓
- D8 (show-diff-confirm under strict) → S2.t5 ✓
- D9 (tier-aware failure handling) → S3.t1 ✓
- D10 (current_tasks[] array) → S1.t6 ✓
- D11 (hash-based staleness) → S1.t8 ✓
- D12 (orphan detection) → S1.t7 + S2.t7 + S4.t3 ✓
- D14 (strict three-way schema detection) → S1.t5 ✓
- D15 (dry-run gate) → S1.t4.gate ✓
- D16 (--context appends to DECISIONS.md too) → S2.t4 ✓

**Verdict:** PASS. Every AC and every D-decision has at least one task implementing it.

### Dim 3 — Dependency Correctness

**Question:** Are dependencies accurate and minimal?

Slice-level: linear S1 → S2 → S3 → S4 → S5 (per "Cross-slice Dependencies" diagram in PLAN.md).

Task-level dependencies within S1:
- S1.t1 + S1.t2 (extracted modules) — no internal dep; can be done in either order
- S1.t3 (frontmatter parser) — uses neither t1 nor t2 directly; foundational
- S1.t4 (upgradeStateFile) — uses t1 (atomic-write) + t3 (frontmatter parser)
- S1.t5 (readState rewrite) — uses t3 (frontmatter parser) + t4 (upgradeStateFile sentinel)
- S1.t6 (set/clear/getCurrentTasks) — uses t1 (atomic-write) + t2 (file-lock) + t5 (readState)
- S1.t7 (detectOrphans) — uses t5 (readState) + git CLI
- S1.t8 (isStateStale) — uses t5 (readState) + git CLI
- S1.t9 (blockers) — uses t6's lock + atomic-write pattern
- S1.t10 (appendDecision/markFresh) — uses t6's pattern
- S1.t11 (initState/transitionPhase rewrite) — uses t3 (frontmatter)
- S1.t12 (status.md routing) — uses t5 (readState)

S2-S4 internal dependencies similar; documented per task in PLAN.md.

**Minimality check:** No "this task waits for X but doesn't need it" cases. Dependencies are functional (real consumer/producer), not procedural.

**Verdict:** PASS.

### Dim 4 — Testability

**Question:** Can every task be verified?

| Task | Verifiable? | Mechanism |
|---|---|---|
| S1.t1, S1.t2 (extracted modules) | ✓ | New test files + existing add.test.js stay green |
| S1.t3 (frontmatter parser) | ✓ | Pure function, 6 test cases |
| S1.t4 (upgradeStateFile + D15 gate) | ✓ | 5 helper tests + dry-run gate with human verdict |
| S1.t5 (readState three-way) | ✓ | 5 test cases covering D14 |
| S1.t6 (current_tasks helpers) | ✓ | ≥8 tests + lock-contention test |
| S1.t7 (detectOrphans) | ✓ | Mocked git execFileSync, ≥4 cases |
| S1.t8 (isStateStale) | ✓ | Mocked git rev-list, ≥5 cases |
| S1.t9 (blockers) | ✓ | ≥5 cases |
| S1.t10 (appendDecision/markFresh) | ✓ | ≥3 cases |
| S1.t11 (initState/transitionPhase rewrite) | ✓ | Existing test suite updated for new shape |
| S1.t12 (status.md routing) | ✓ | Manual smoke (markdown change; no code) |
| S2.t1-t4 (checkpoint helpers) | ✓ | ≥4 tests per helper |
| S2.t5 (checkpoint.md command) | ⚠ Markdown | Verified via S5.t1 integration test |
| S2.t6 (manual smoke) | ⚠ Manual | Committed evidence |
| S2.t7 (orphan prompt) | ✓ | ≥3 tests |
| S3.t1 (dispatchTaskWithState) | ✓ | ≥6 tests with mocked agent |
| S3.t2 (orphan recovery) | ✓ | ≥3 tests |
| S3.t3-t4 (execute.md, executor.md doc) | ⚠ Markdown | Validator green + manual smoke |
| S3.t5 (manual smoke) | ⚠ Manual | Committed evidence |
| S4.t1 (renderResumeBriefing) | ✓ | ≥6 tests + snapshot |
| S4.t2-t3 (staleness/orphan UI) | ✓ | ≥3 tests each |
| S4.t4-t6 (verify/review/resume.md patches) | ⚠ Markdown | Validator green + manual smoke |
| S4.t7 (manual smoke) | ⚠ Manual | Committed evidence |
| S5.t1 (integration test) | ✓ | The test IS the verification |
| S5.t2 (validator update) | ✓ | Validator green |
| S5.t3-t7 (docs) | ⚠ Docs | Manual review |
| S5.t8 (dogfood) | ⚠ Manual | Captured in M4.5.E6-VERIFICATION.md |

**Verdict:** PASS with footnote — markdown/manual verification is honest (some tasks are intrinsically untestable via unit tests). S5.t1 integration test is the load-bearing automated end-to-end verification.

### Dim 5 — Scope Discipline

**Question:** No gold-plating or scope creep?

PLAN.md explicitly carves out 6 scope cuts (RESEARCH-surfaced ideas that didn't make E6):
- `/sig:checkpoint --clear-task` — DEFERRED
- `previous_tasks[]` for --undo — FUTURE-IDEAS
- Heartbeat/touchCurrentTask — DEFERRED
- Cross-project state separation — OUT OF SCOPE
- CALIBRATE-phase checkpoint behavior — RESOLVED IN-SCOPE (one-line behavior; trivial)
- Aggressive narrative-parsing migration — REJECTED (D15)

Each cut has rationale. No "while we're at it" expansions.

**Spot-check for hidden gold-plating:**
- S1.t1 + S1.t2 extracted modules — is this a refactor that doesn't belong in E6? No — both are required because state.js needs the same atomic-write + lock patterns add.js uses, and duplicating the code would be the scope-creep. Extraction is the minimum.
- S4.t1 `renderResumeBriefing` extraction — is this beyond E6's scope? No — required for S5.t1 integration test (AC#7); the alternative is manual-only verification, which violates AC#7's "automated tests cover the end-to-end scenario."
- S5.t6 `references/state-schema.md` — required reference doc, not gold-plating (mirrors existing `profile-schema.md` precedent).

**Verdict:** PASS.

### Dim 6 — Context Feasibility

**Question:** Can each task fit in a single agent context?

| Task | Estimated context size | Fits? |
|---|---|---|
| S1.t1 | ~5K tokens (lift-and-shift + 4 tests) | ✓ |
| S1.t2 | ~5K tokens | ✓ |
| S1.t3 | ~6K tokens (frontmatter parser + 6 tests) | ✓ |
| S1.t4 | ~12K tokens (upgradeStateFile + 5 tests + dry-run gate execution) | ✓ |
| S1.t5 | ~10K tokens (readState rewrite + 5 D14 cases + existing tests) | ✓ |
| S1.t6 | ~12K tokens (3 helpers + 8 tests + lock-contention + legacy-upgrade) | ✓ |
| S1.t7 | ~8K tokens (detectOrphans + 4 tests with mocked git) | ✓ |
| S1.t8 | ~9K tokens (isStateStale + 5 tests with mocked git) | ✓ |
| S1.t9 | ~6K tokens (blockers + 5 tests) | ✓ |
| S1.t10 | ~5K tokens (2 helpers + 3 tests) | ✓ |
| S1.t11 | ~10K tokens (initState/transitionPhase rewrite + test updates) | ✓ |
| S1.t12 | ~3K tokens (markdown only) | ✓ |
| S2.t1-t4 | ~6-10K tokens each | ✓ |
| S2.t5 | ~12K tokens (significant markdown spec; references multiple precedents) | ✓ |
| S2.t6 | ~3K tokens (manual smoke notes) | ✓ |
| S2.t7 | ~6K tokens | ✓ |
| S3.t1 | ~12K tokens (wrapper + 6 mocked-agent tests + D9 branches) | ✓ |
| S3.t2 | ~6K tokens | ✓ |
| S3.t3-t4 | ~5K tokens each (markdown) | ✓ |
| S3.t5 | ~3K tokens (manual smoke) | ✓ |
| S4.t1 | ~15K tokens (extraction + 6 tests; biggest single task in S4) | ✓ |
| S4.t2-t3 | ~6K tokens each | ✓ |
| S4.t4-t6 | ~5K tokens each (markdown patches) | ✓ |
| S4.t7 | ~3K tokens (manual smoke) | ✓ |
| S5.t1 | ~12K tokens (integration test + fixture setup) | ✓ |
| S5.t2 | ~2K tokens | ✓ |
| S5.t3-t5 | ~5K tokens each (docs) | ✓ |
| S5.t6 | ~10K tokens (reference doc) | ✓ |
| S5.t7 | ~5K tokens (migration doc) | ✓ |
| S5.t8 | ~5K tokens (dogfood capture) | ✓ |

**Verdict:** PASS. No task exceeds 20K tokens. All comfortably within 200K context budget.

### Dim 7 — Risk Coverage

**Question:** Are identified risks mitigated?

Risks from RESEARCH (HIGH/MEDIUM-HIGH only):

| Risk | Mitigation in plan |
|---|---|
| Wave-parallel single-writer corruption (A1.1) | D10 → `current_tasks[]` array; D6 lock; S1.t6 lock-contention test |
| Wall-clock vs git committer-date skew (A2.1) | D11 → hash-based staleness; S1.t8 uses git rev-list, not timestamp |
| Orphan in_progress permanence (A5.1) | D12 → orphan detection; S1.t7 + S2.t7 + S4.t3 |
| Schema-detection auto-upgrade clobbering user data (A4.1) | D14 → strict three-way; S1.t5 cases 2 + 4 explicit |
| Signal-on-Signal migration confusion (A6.1, A10.5) | D15 → dry-run gate; S1.t4.gate explicit |
| `--context` discoverability fail (A8.1) | D2 mitigations (banner, README, staleness check) + D16 audit-trail; S2.t5 + S4.t2 + S5.t3 |
| DECISIONS.md fragmentation (A9.1) | D16 → `--context` appends to both; S2.t4 |
| Atomic-write contract for STATE.md (A10.1) | S1.t1 extracted module; S1.t6 uses it |
| `markStale()` naming confusion (A10.10) | Renamed to `markFresh()` in PLAN; documented |

**Verdict:** PASS. Every HIGH-risk assumption has at least one task implementing the mitigation.

### Dim 8 — Vertical Slicing

**Question:** Is each slice a full slice, not a horizontal layer?

| Slice | Independently usable? | Why |
|---|---|---|
| S1 | Yes (sort of) | After S1 ships, manual STATE.md updates work + the schema is in place. Users can hand-edit STATE.md in the new format. But no `/sig:checkpoint` and no auto-protocol yet — the user can't actually exercise the new capability without S2 or S3. |
| S2 | Yes | After S2 ships, `/sig:checkpoint` works manually. Users gain the manual peace-of-mind path even before auto-protocol exists. |
| S3 | Yes | After S3 ships, EXECUTE runs the auto-protocol. Combined with S1, the full design works end-to-end (minus staleness warnings and resume-time UI). |
| S4 | Yes | After S4 ships, `/sig:resume` shows staleness + orphan detection. The full UX is delivered. |
| S5 | Yes | After S5 ships, the tier-aware behavior is enforced + docs exist + AC#8 dogfood completes. |

**S1 caveat noted in PLAN.md** — S1 is foundational; on its own, users gain the schema but not yet the capability. This is the most-horizontal slice. Mitigation: S1's value is internal (other slices depend on it); shipping as v0.1.x is a packaging decision but doesn't add user-visible behavior. Pair with S2 for v0.1.2 ship per PLAN's bundling note.

**Verdict:** PASS with annotation — S1 is best understood as a foundation slice; pair-shipping with S2 in v0.1.2 is the recommended packaging.

---

## Part 2 — Nyquist Test Coverage Mapping

Per `nyquist_enforcement: strict`: every test must have either (a) per-test red→green git evidence — a commit where the test was added and CI showed red, followed by the implementation commit that turned it green; OR (b) explicit attestation in this VALIDATION.md that the test was written before the implementation, naming the file and the implementation commit it predates.

(b) is the lighter-weight default the plan uses. Each task's "test first" step in PLAN.md serves as the attestation contract.

### Test count by slice

| Slice | Test file | Test count (est.) |
|---|---|---|
| S1 | `tests/atomic-write.test.js` | 4 |
| S1 | `tests/file-lock.test.js` | 5 |
| S1 | `tests/state-schema.test.js` (parseFrontmatter, stringifyFrontmatter) | 9 |
| S1 | `tests/state.test.js` (extended for new schema + helpers) | 30 (8 setCurrentTask + 4 detectOrphans + 5 isStateStale + 5 blockers + 3 markFresh + 5 readState three-way) |
| S2 | `tests/checkpoint.test.js` | 14 (4 parseArgs + 4 detectChanges + 3 renderDiff + 3 orphan-prompt) + extras |
| S3 | `tests/dispatch-task-with-state.test.js` | 9 (6 wrapper + 3 orphan recovery) |
| S4 | `tests/resume-briefing.test.js` | 12 (6 renderResumeBriefing + 3 staleness + 3 orphan) |
| S5 | `tests/state-end-to-end.test.js` | 1 (the integration test) + 4 fixture coverage |
| **Total new** | | **~88** |

(Slight upward revision from PLAN's 68 estimate after fully enumerating; both are within the M4.5.E2.S1's "+40 tests" precedent ratio for an Epic of this size.)

### Nyquist mapping (AC → tests)

| AC | Test file(s) | Test(s) | Form (a/b) | Attestation |
|---|---|---|---|---|
| #1 wave-boundary reporting | tests/state.test.js, tests/dispatch-task-with-state.test.js | setCurrentTask/clearCurrentTask + dispatchTaskWithState | (b) | PLAN.md S1.t6 + S3.t1 "test first" steps |
| #2 post-context-clear briefing | tests/state-end-to-end.test.js | "renders accurate briefing after simulated context-clear" | (b) | PLAN.md S5.t1 |
| #3 /sig:checkpoint idempotent | tests/checkpoint.test.js | parseArgs + integration | (b) | PLAN.md S2.t1, S2.t5, S2.t6, S2.t7 |
| #4 schema backwards-compat | tests/state-schema.test.js, tests/state.test.js | upgradeStateFile + readState three-way + D15 dry-run | (b) | PLAN.md S1.t4 + S1.t5; D15 gate is human-verified |
| #5 staleness warning | tests/state.test.js, tests/resume-briefing.test.js | isStateStale + staleness banner | (b) | PLAN.md S1.t8 + S4.t2 |
| #6 tier-aware | tests/dispatch-task-with-state.test.js | D9 branches (FULL/strict halt; FEATURE/light warn; SKETCH skip) | (b) | PLAN.md S3.t1 D9 test cases |
| #7 existing 225 green + new tests | All existing test files + all new | All | (a) at run-time | Existing tests CI-green is the evidence |
| #8 dogfood | (manual; captured in M4.5.E6-VERIFICATION.md) | N/A | Manual | PLAN.md S5.t8; evidence in VERIFICATION.md |

### Nyquist coverage gaps (intentional)

- **commands/*.md markdown-spec changes** are not directly unit-tested. Coverage is via (a) validator green and (b) integration tests that exercise the command end-to-end. This matches Signal's existing precedent — markdown specs are spec, not code.
- **Manual smoke tasks** (S2.t6, S3.t5, S4.t7, S5.t8) are not automated. Evidence captured in commit messages and VERIFICATION.md.

### Pre-strict-mode permanent gap (none)

E6 is greenfield for state mutation. No pre-existing code shipped under non-strict mode that would create a permanent Nyquist gap per the `references/tier-definitions.md` "Recoverable vs. permanent backfills" rule. Every helper and behavior in E6 was test-first from the start.

---

## Validation Summary

| Dimension | Verdict |
|---|---|
| 1 — Goal alignment | PASS |
| 2 — Completeness | PASS |
| 3 — Dependency correctness | PASS |
| 4 — Testability | PASS (with documented markdown/manual exceptions) |
| 5 — Scope discipline | PASS |
| 6 — Context feasibility | PASS |
| 7 — Risk coverage | PASS |
| 8 — Vertical slicing | PASS (S1 noted as foundation; pair-ship recommendation) |

**Nyquist:** All AC mapped to tests; strict-mode coverage achievable via (b) attestation per task's "test first" step.

**Verdict:** Plan is PASS across all 8 dimensions. Ready for user approval + EXECUTE.

---

*VALIDATION authored 2026-05-17 against M4.5.E6-PLAN.md. Plan-validation strict; Nyquist strict. No issues blocking transition to EXECUTE.*
