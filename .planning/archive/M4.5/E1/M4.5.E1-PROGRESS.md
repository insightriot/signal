# M4.5.E1 — `/sig:add` install hardening — Execution Progress

**Phase:** EXECUTE. **Tier:** FULL — strict gate at each slice boundary.
**Started:** 2026-05-15.

---

## Slice 1 — Source-block fix + v0.1.1 ship  [**shipped + R1 verified 2026-05-15**]

**Goal:** Marketplace install survives multi-identity SSH configs and similar non-default machine setups; close today's stranger-install break (failure mode A1).

**Files written / modified:**

- ✓ `.claude-plugin/marketplace.json` — `source.source`: `"github"` → `"url"`; added explicit HTTPS URL, `ref: "v0.1.1"`, `sha: "ae3b1ab9c64d8a3fcb9ac75fc033b585bed6db2f"` (pinned in commit B to v0.1.1 release commit).
- ✓ `.claude-plugin/plugin.json` — `version`: `"0.1.0"` → `"0.1.1"`.
- ✓ `README.md` — Install section: replaced stale `/plugin install signal` with the correct 3-line block (`marketplace add` → `install sig@signal` → `reload-plugins`); added Troubleshooting subsection naming `CLAUDE_CODE_PLUGIN_PREFER_HTTPS=1` (Claude Code 2.1.141+) + local-clone fallback.
- ✓ `CHANGELOG.md` (new) — seeded with `[0.1.1]` + `[0.1.0]` entries; format loosely follows Keep a Changelog + SemVer; `[BREAKING]` tag convention noted in header.
- ✓ `tools/validate-plugin.js` — semver-format check on `plugin.json.version` (`/^\d+\.\d+\.\d+$/`).
- ✓ `tests/install-contract.test.js` (new, 12 tests) — guards marketplace.json source-block shape (source, url, sha hex, ref-matches-version), plugin.json version contract (semver + ≥0.1.1), and CHANGELOG presence with both version headings.
- ✓ `tests/readme-content.test.js` (new, 4 tests) — README install section smoke tests (marketplace add line, install sig@signal line, `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` present, stale bare-slug `/plugin install signal` absent).

**Acceptance criteria (all met):**

| # | Criterion | Status |
|---|---|---|
| 1 | Install succeeds on R1 machine (Brett's business box; multi-identity SSH) | ✓ **Verified 2026-05-15.** `/plugin marketplace update signal` on the business machine fetched the v0.1.1 marketplace.json (new HTTPS source-block) and successfully re-cloned the plugin source via HTTPS. Plugin updated from v0.1.0 → v0.1.1 with zero git/SSH workaround commands. Had the old `source: "github"` SSH path been used, the same "Permission denied (publickey)" from this morning would have recurred. Verified by absence of error + version transition. |
| 2 | Validator enforces semver-format on plugin.json.version | ✓ (test: `plugin.json — version field contract > version is semver-shaped`) |
| 3 | `marketplace.json.plugins[0].source.source === "url"` | ✓ (test: `marketplace.json — source block contract > source.source === "url"`) |
| 4 | `source.sha` is 40-char hex | ✓ (test: `source.sha is 40-char hex string`) |
| 5 | `source.ref` matches `v<plugin.json.version>` | ✓ (test: `source.ref matches v<plugin.json.version>`) |
| 6 | README has `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` reference | ✓ (test: `contains CLAUDE_CODE_PLUGIN_PREFER_HTTPS troubleshooting reference`) |
| 7 | CHANGELOG.md exists with `[0.1.1]` + `[0.1.0]` headings | ✓ (tests: `CHANGELOG.md — release history > contains ## [0.1.1] heading` + `contains ## [0.1.0] heading`) |
| 8 | `npm test` green (209 baseline + 16 new = 225) | ✓ (`Tests 225 passed (225)`) |
| 9 | Git tag `v0.1.1` exists locally + pushed | ✓ (pushed to origin 2026-05-15) |

**Test summary:** **225 / 225 passing** (209 prior + 12 install-contract + 4 readme-content). `npm run validate` green.

**SHA-pin chicken-and-egg resolution:**
- Commit A (`ae3b1ab`): all S1 changes; `marketplace.json.source.sha` = `"0000000000000000000000000000000000000000"` (placeholder — passes regex test, clearly not real).
- Tag `v0.1.1` at commit A.
- Commit B (`e602726`): update `marketplace.json.source.sha` to commit A's actual SHA.
- Result: tag v0.1.1 = commit A; HEAD of main = commit B. Marketplace.json fetched from HEAD pins to v0.1.1 by SHA. Plugin code identical between A and B (only sha field moves).

**Anti-rationalization honored (per M4.5.E1-PLAN.md):**
- TDD red-green respected — 16 tests written first, ran red (12 failed); implementation made them green.
- README touch kept minimum-viable (install section + troubleshooting only); full README rewrite deferred to E3.
- CHANGELOG seeded with both 0.1.0 and 0.1.1 — substrate every versioning option needs.
- No `--no-verify`, no force-push, no skipped tests.
- Validator gained one focused check (semver format); the broader hardening lives in S5 as planned.

---

## Slice 2 — F2 verification + agent restructure (conditional)  [**Phase A shipped 2026-05-19; Phase B does NOT fire (outcome a)**]

**Outcome: (a) — all 25 Signal agents auto-register; no fallback path needed; no agent restructure needed.**

Empirical verification on the maintainer business machine (R1 row of `docs/install-verification.md`) confirmed:

- Marketplace install of v0.1.2 registers all 25 nested agents under naming convention `sig:<subdirectory>:<name>` (e.g., `sig:scanners:stack-scanner`, `sig:specialists:security-auditor`).
- `/sig:init` Step 2 spawns all 4 scanners in parallel via the Task tool with named `subagent_type` — no "Agent type not found" errors, no fallback to `general-purpose`.
- Scanner output parity holds on a fresh codebase (`expressjs/express` shallow clone) against the dev-mode reference in `.dogfood/M4-INIT-DOGFOOD/.planning/scan/`.

**Phase B is permanently shelved.** The conditional 26-agent flat-restructure (outcome c) was the worst-case branch the PLAN protected against; that case empirically does not exist.

**Files written / modified for this slice:**

- ✓ `docs/install-verification.md` (new) — R1 row, install-adventure chronology, F2 verification section
- ✓ `.planning/DECISIONS.md` — appended 2026-05-19 entry "F2 resolved: outcome (a)"
- ✓ `commands/init.md` line 170 — speculative `signal-` prefix paragraph replaced with empirically-confirmed `sig:<subdirectory>:<name>` description (acceptance criterion #3)

**Acceptance criteria (per `M4.5.E1-PLAN.md` Slice 2):**

| # | Criterion | Status |
|---|---|---|
| 1 | Phase A run documented in `docs/install-verification.md` with `/agents` output, `/sig:init` log, scan-file comparison, dated + machine-signature | ✓ R1 section in `docs/install-verification.md` |
| 2 | Outcome label (a/b/c) recorded in `DECISIONS.md` | ✓ 2026-05-19 entry: outcome (a) |
| 3 | `commands/init.md:170` stale `signal-` prefix is fixed to match actual outcome | ✓ Paragraph rewritten to describe the `sig:<subdirectory>:<name>` convention empirically observed |
| 4 | If outcome (b)/(c): validator `REQUIRED_AGENTS` expanded to 26 | n/a — outcome (a) |
| 5 | If outcome (c): all 26 agent files moved to flat layout | n/a — outcome (a) |

**Side-discoveries routed elsewhere:**

- **Synthesizer character-eating bug** in LANDSCAPE.md + PROJECT.md output (6+ instances in one run) — routed to **M4.5.E7**.
- **3 install-path UX papercuts** (stale `gitCommitSha` short-circuit, no uninstall verb in `/plugin` UI, disable state survives uninstall+reinstall) — workarounds documented in R1; troubleshooting docs routed to **M4.5.E7**.
- **CLAUDE.md says 26 agents; on-disk count is 25 Signal agents (32 reported by `/reload-plugins` including built-ins/aliases).** Docs/reality drift; reconciliation routed to **M4.5.E1.S5** validator hardening.

**Bonus positive findings (recorded in `docs/install-verification.md` § R1 "Bonus findings beyond F2"):**

- Shallow-clone detection works (new real-world condition M3 dogfood didn't exercise)
- Non-standard file casing handled (`Readme.md`, `History.md`)
- M4.t8 assumption-surfacing walkthrough fires correctly on real brownfield (first dogfood)
- Vision / Problem Statement inference quality reads naturally

## Slice 3 — Fresh-machine verification matrix  [**R1 shipped 2026-05-19; R2/R3/R5 pending**]

R1 row written into `docs/install-verification.md` as part of S2 Phase A (the two slices necessarily share the same run). Remaining rows require additional fresh machines:

- **R2 — Mac happy-path** (default SSH config; pending) — isolates whether the R1 install adventure was caused by multi-identity SSH or would reproduce on a default machine
- **R3 — Linux** (pending) — cross-platform coverage; required for E1 exit gate
- **R5 — IdentitiesOnly hardened** (pending) — stress-test the v0.1.1 SSH workaround

`docs/install-verification.md` now exists as the matrix doc; R2/R3/R5 append as rows when run.

## Slice 4 — Versioning + deprecation policy  [**pending**]

Independent of S1 once shipped. Writes `docs/versioning.md`.

## Slice 5 — Validator hardening (scope-beyond-exit)  [**pending**]

Depends on S2 outcome for final `REQUIRED_AGENTS` list — **outcome (a) means the 25-agent nested layout is canonical**; validator's REQUIRED_AGENTS list should be expanded to enforce all 25. Should also reconcile the 26-vs-25 documentation drift in CLAUDE.md.

---

*Updated 2026-05-19 after S2 Phase A verification on the R1 machine. Outcome (a) locked; side-discoveries routed to M4.5.E7.*
