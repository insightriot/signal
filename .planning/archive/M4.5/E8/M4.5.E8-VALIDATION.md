# M4.5.E8 — Plan Validation

**Epic:** `/sig:doctor` install-state diagnostician + ownership reframe
**Tier:** FULL — `plan_validation_dims: all`, `nyquist_enforcement: strict`
**Inputs:** `M4.5.E8-REQUIREMENTS.md`, `M4.5.E8-RESEARCH.md`, `M4.5.E8-PLAN.md`
**Validated:** 2026-05-28

---

## Overall verdict: **PASS**

All 8 dimensions PASS. Nyquist strict mapping covers every AC with explicit test type. Three conflicts with REQUIREMENTS were reconciled at PLAN, not deferred; six new D-candidates added at S1.t1.

---

## 8-Dimension Validation

### 1. Goal alignment — **PASS**

**Question:** Does every task serve the phase goal?

PLAN goal: "Ship `/sig:doctor` that detects 5 P-states, generates user-reviewed remediation script, surfaces version staleness, and reframes the troubleshooting doc with honest ownership."

Task-by-task alignment audit:

| Task class | Serves phase goal? |
|---|---|
| S1.t2-t9 (detectors, aggregate, IO orchestrator, stub) | Yes — implements FR1, FR2, FR7 |
| S1.t10-t12 (command file, validator, count bump) | Yes — wires the command surface required for users to invoke it (FR10) |
| S1.t13-t14 (upstream cross-link + new P3 filing) | Yes — half of FR8 + FR9; the "ownership reframe" half of the goal |
| S2.t1-t13 (script gen) | Yes — implements FR3, FR4, FR5 |
| S3.t1-t10 (version check, cache, status integration) | Yes — implements FR6 |
| S3.t11 (doc reframe) | Yes — second half of FR8 |
| S3.t12 (CHANGELOG) | Yes — close-the-loop hygiene |
| S3.t13 (dogfood) | Yes — AC #13 |
| S3.t14 (retrospective) | Yes — E9 FR1 enforcement (Epic SHIP requires retro) |
| S3.t15 (state close) | Yes — workflow hygiene |

**No orphan tasks.** No task serves a side-goal or speculative future work. S1.t1 (DECISIONS write) is meta-but-essential — locks the 6 new D-candidates that PLAN surfaced.

### 2. Completeness — **PASS**

**Question:** Are all requirements covered?

| Requirement | Covered by |
|---|---|
| FR1 (detect 5 P-states) | S1.t2-t6 |
| FR2 (read-only default, exit 0/1/2) | S1.t8 (D-E8-12) + S1.t10 |
| FR3 (`--fix` surgical script) | S2.t2, S2.t10 |
| FR4 (`--reinstall` full script) | S2.t3, S2.t10 |
| FR5 (per-step `[y/N]` prompts) | S2.t1-t2 (RED→GREEN), S2.t12 |
| FR6 (`/sig:status` version-check) | S3.t1-t10 |
| FR7 (non-macOS stub) | S1.t9 |
| FR8 (doc reframe) | S1.t13 (cross-links) + S3.t11 (full reframe) |
| FR9 (upstream filings) | S1.t13 (cross-link 2 existing) + S1.t14 (file new P3) |
| FR10 (validator + CLAUDE.md wiring) | S1.t11 + S1.t12 |
| AC #1 (healthy) | S1.t6 fixture `healthy/` |
| AC #2 (per-P-state) | S1.t6 fixtures `p1-...` through `combined-p1-p3/` |
| AC #3 (`--fix` script gen + post-exec healthy) | S2.t1 lint + S2.t9 + S3.t13 dogfood |
| AC #4 (`--fix` no-op on healthy) | S2.t9 |
| AC #5 (`--reinstall` identical body) | S2.t1, S2.t3 |
| AC #6 (interactive prompts) | S2.t1 |
| AC #7 (version-check accuracy) | S3.t9 — 4-row matrix |
| AC #8 (cache behavior) | S3.t3, S3.t5 |
| AC #9 (doc reframe) | S3.t11 |
| AC #10 (non-macOS stub) | S1.t9 |
| AC #11 (suite green) | All slice gates |
| AC #12 (validator) | S1.t11 |
| AC #13 (dogfood) | S3.t13 |

**Zero gaps.** Every functional requirement maps to one or more tasks; every acceptance criterion maps to a specific test or manual gate.

### 3. Dependency correctness — **PASS**

**Question:** Are dependencies accurate and minimal?

Dependency graph (in PLAN § "Dependencies") follows the natural data flow:

- Detectors before aggregate before IO orchestrator before command-file wiring.
- Cross-link doc (S1.t13) runs **parallel** with S1.t2-t12 (no shared file surface; doc edit doesn't depend on detector code). New P3 issue (t14) is sequenced after t13 only to capture the issue number before doc cross-link finalizes.
- S2 begins after S1 ships (strict-gate handoff per FULL `gate_strictness: strict`).
- S3 begins after S2 ships.
- Within slices, RED tasks precede GREEN tasks per TDD discipline.

**No spurious blocks.** S2 doesn't need S1's doc edits to be code-complete (S1.t13 is the only mid-S1 task that could in principle block S2 — and it doesn't, S1.t13 is parallel).

**No missing blocks.** S2.t13 (writeDoctorScript) depends on `atomic-write.js` being present (it already is — built in M4.5.E6). S3.t4 depends on the same. No reach into work not yet done.

### 4. Testability — **PASS**

**Question:** Can every task be verified?

Per-task test mapping in PLAN § "S1/S2/S3 Task Breakdown" tables. RED→GREEN pairs explicit. Per FULL `nyquist_enforcement: strict`, every AC has an automated test EXCEPT AC #13 (dogfood, explicitly manual + captured in `docs/install-verification.md` § R6).

Three tasks have no test delta:
- S1.t1 (DECISIONS write) — pure planning artifact, not testable as code.
- S1.t13-t14 (doc cross-link + GitHub issue filing) — doc + external; not unit-testable. Verified by S1.t12 cross-file-consistency suite (catches the upstream issue references) + GitHub issue link presence (grep test could be added in S3.t11 as part of the reframe lint).
- S3.t12 (CHANGELOG) — append-only; verified by manual review and grep test in S3.t14 retrospective.

**Verification-by-other-means is documented:** the dogfood AC #13 is captured in `docs/install-verification.md` § R6 (the same place R1-R5 live). Verifiable retrospectively even though not unit-tested.

### 5. Scope discipline — **PASS**

**Question:** Any gold-plating or scope creep?

Checked against REQUIREMENTS § "Anti-rationalizations locked":

- **`--dry-run` flag** — NOT added. Default behavior IS the dry-run mode. ✓
- **Marketplace-vs-local auto-detect** — NOT added. Marketplace-only per design. ✓
- **Generalize for other plugins** — NOT added. Signal-only. ✓
- **Tutorial-narrated script comments** — NOT added (minimal comments per D-E8-?). ✓
- **Auto-execute `--fix`** — NOT added (D-E8-1 holds). ✓
- **Linux/WSL support in S1** — NOT added (D-E8-2 holds; polite stub). ✓
- **Skip upstream filings** — NOT skipped, but **reframed**: PLAN promotes cross-links + 1 new filing into S1 per "[[feedback_document-in-the-moment]]" memory + research finding that 2 issues already exist. This is a scope **shift**, not creep — the work was always in scope; only the timing changed.

**Scope additions over REQUIREMENTS:** 6 new D-candidates (D-E8-7 through D-E8-12). All emerged from research as required-to-implement, not optional. None are speculative.

**Scope subtractions:** none. All FRs + ACs covered.

### 6. Context feasibility — **PASS**

**Question:** Can each task fit in a single agent context?

Per FULL `context_rot_reread: true`, each EXECUTE-phase agent will re-read PROFILE.md + STATE.md + relevant artifacts at task boundaries.

Task-size audit:
- **Largest tasks** (M complexity): S1.t6 (6 fixture trees + integration tests), S1.t10 (commands/doctor.md), S2.t2 (buildFixScript), S3.t11 (doc reframe).
- Each fits comfortably in a single 200K-context agent session. Fixture trees are small synthetic JSON files (estimated <500 LOC across all 6). `commands/doctor.md` is ~250 lines based on `commands/status.md` precedent.
- S3.t11 (doc reframe) is the largest pure-prose task: existing `docs/install-troubleshooting.md` is ~280 lines; reframe with ownership statement + 5 per-section tags + length reduction = ~200-250 lines net. Single context.

**No task crosses the context-feasibility threshold.** No agent will need to re-read more than: PROFILE.md (~40 lines) + STATE.md (~30 lines of YAML + maybe 20 lines of narrative) + the immediate task's artifact + 1-2 reference files. Total budget under 10K tokens per task.

### 7. Risk coverage — **PASS**

**Question:** Are identified risks mitigated?

Top 3 risks from RESEARCH § 7 → mitigation in PLAN:

| Risk | Mitigation in PLAN |
|---|---|
| **R1 — Test isolation** (highest blast radius) | D-E8-10 locked. Every task that touches `homeDir` uses parameter injection. S2.t12 is a dedicated meta-test asserting generated script never contains literal `~/.claude/`. |
| **R2 — Signal-scoped detectors** | D-E8-11 locked. Every detector in S1.t3 filters by `sig@`/`signal@` key or `signal/sig`/`signal/signal` path prefix before emitting. S1.t2 RED tests include explicit non-Signal-plugin-in-state fixtures asserting `detected: false`. |
| **R3 — GitHub API as advisory** | S3.t1 RED tests cover offline, 404, empty array, malformed JSON, timeout — all collapse to `null` → no banner. S3.t9 (`readStalenessWarning`) returns null on null upstream; status.md Step 2.0 renders nothing on null. AC #7 tests NEVER hit live network. |

Lower-priority risks (RESEARCH § 8):

| Risk | Mitigation |
|---|---|
| Script destructive-action safety | S2.t7 case-mismatched abort; D-E8-10 absolute-path-only literals; S2.t12 meta-test. |
| State-machine race (concurrent `/plugin install`) | S1.t7-t8 RED test for malformed JSON → graceful exit 2 with "(state file mid-write; retry)" message. |
| Version-compare correctness | S3.t6 test table covers v-prefix, pre-release suffix, double-digit minor, installed-ahead-of-latest case. |
| AC #13 dogfood substrate | Mac Studio has live P2 + P4 (RESEARCH bonus finding). S3.t13 backs up `~/.claude/` first; runs `--fix` (surgical), not `--reinstall`. |
| Upstream filing collision | RESEARCH already verified — 2 exist, file only 1 new. D-E8-9. |

**All identified risks have explicit mitigations traceable to specific tasks.** None deferred to "we'll handle it later."

### 8. Vertical slicing — **PASS**

**Question:** Is each task a full slice, not a horizontal layer?

Per PLAN § "Vertical-Slicing Verification":

- **S1 alone** ships a diagnostic-only `/sig:doctor` that prints findings. Useful for users without `--fix`. Doc cross-links land. P3 issue filed. **Independently shippable.**
- **S2 alone (after S1)** adds script generation. Strangers can run `--fix` on detected states. **Independently shippable.**
- **S3 alone (after S2)** adds version-check + finalizes doc reframe + CHANGELOG + Epic close. **Independently shippable.**

Within slices, tasks are **TDD-vertical** (each RED→GREEN pair ships one function or function family end-to-end), not horizontal (e.g., "write all the tests then write all the code").

**No horizontal anti-patterns spotted.** Each task lands a working, tested behavior.

---

## Nyquist Test-Coverage Mapping (strict)

Per FULL `nyquist_enforcement: strict`: every AC must have a corresponding test type AND red→green evidence captured at commit time.

| AC | Test type | Specific test(s) | RED→GREEN evidence captured at |
|---|---|---|---|
| **AC #1** healthy install | Fixture + integration | `tests/doctor.test.js#healthy-fixture-passes` (S1.t6) | S1.t6 commit body |
| **AC #2** 5 P-states | Per-state fixture + integration (6 tests) | `tests/doctor.test.js#detects-{P1,P2,P3,P4,P5,combined}` (S1.t6) | S1.t2 RED commit + S1.t3 GREEN commit |
| **AC #3** `--fix` script gen | Fixture + script-content lint | `tests/doctor-script-gen.test.js#fix-writes-script`, `#fix-script-content-matches-findings` (S2.t1-t2, S2.t9) | S2.t1 RED + S2.t2 GREEN commits |
| **AC #4** `--fix` no-op on healthy | Fixture | `tests/doctor-script-gen.test.js#fix-no-script-on-healthy` (S2.t9) | S2.t9 commit |
| **AC #5** `--reinstall` always full | Two fixtures + content compare | `tests/doctor-script-gen.test.js#reinstall-identical-regardless-of-state` (S2.t3) | S2.t3 commit |
| **AC #6** interactive prompts | Script-content lint | `tests/doctor-script-gen.test.js#per-mutating-step-wrapped-in-y-N` (S2.t1) | S2.t1 commit |
| **AC #7** version warning matrix | Mocked fetch + matrix (4 tests) | `tests/status-version-check.test.js#matrix-row-{1,2,3,4}` (S3.t9) | S3.t1-t9 RED→GREEN sequence |
| **AC #8** 24h cache | Mocked clock + fetch counter (2 tests) | `tests/status-version-check.test.js#cache-{hits-on-first,skips-within-ttl,refreshes-after-ttl}` (S3.t5) | S3.t3 RED + S3.t5 GREEN |
| **AC #9** doc reframe | Grep + section count (2 tests) | `tests/doctor.test.js#install-troubleshooting-ownership-statement`, `#per-section-ownership-tags` (S3.t11) | S3.t11 commit |
| **AC #10** non-macOS stub | Mocked platform (2 tests) | `tests/doctor.test.js#stub-on-linux`, `#stub-on-win32` (S1.t9) | S1.t9 commit |
| **AC #11** suite green | CI | full `npm test` at each slice gate | S1.t15, S2.t13, S3.t15 commit bodies |
| **AC #12** validator | install-contract test | `tests/install-contract.test.js#doctor-required` (S1.t11) | S1.t11 commit |
| **AC #13** dogfood | Manual | `docs/install-verification.md` § R6 capture | S3.t13 commit body documents real before/after |

**Coverage:** 13/13 ACs mapped. 12 automated, 1 manual (AC #13). Strict-Nyquist evidence captured per-task per VERIFY-phase convention (commit body says "S1.t2 RED: tests fail; S1.t3 GREEN: tests pass").

**Permanent gap warning:** None expected. Per `escalate.md` Nyquist warning, AC #13's manual nature is the one place strict-Nyquist relaxes — `docs/install-verification.md` § R6 captures the proof-of-failure-before-fix in human-readable prose. Acceptable per "Recoverable vs. permanent backfills" subsection in `references/tier-definitions.md`.

---

## Conflicts with REQUIREMENTS (reconciled at PLAN, not relitigated)

Per PLAN § "Conflicts with REQUIREMENTS":

1. **FR6 endpoint** — REQUIREMENTS says `/releases/latest`, `tag_name`. RESEARCH says `/tags`, `name`, strip leading `v`. PLAN substitutes throughout S3.
2. **FR9 timing** — REQUIREMENTS says "S3 optional". RESEARCH found 2 issues already filed. PLAN promotes cross-link work into S1; files new P3 alongside S1 ship.
3. **`installed_plugins.json` shape** — REQUIREMENTS treats as object; reality is `[{...}]` per-scope array. Detector signatures handle the array.

**No DISCUSS-relitigation.** PLAN does not revisit D-E8-1 through D-E8-6 (those were DISCUSS-locked); PLAN adds D-E8-7 through D-E8-12 as new locks at S1.t1.

---

## Test Count Projection

| Slice | New tests | Cumulative |
|---|---|---|
| Pre-Epic | — | 384 |
| S1 ship | +13 | 397 |
| S2 ship | +14 | 411 |
| S3 ship | +10 (lower than RESEARCH § 5 because AC #11 already counted; AC #13 manual) | 421 |
| Forecast | **~38 new** | **~422** |

Matches REQUIREMENTS § Implications (~40-50). PLAN at the lower end of forecast — research surfaced opportunities to fold redundant tests (e.g., AC #4 is one test, not three; matrix row tests parameterize into 4 not 16).

---

## Environment Check Hooks (Step 6 placeholder)

Will run as task #6 after this VALIDATION commit, before EXECUTE begins:

- [ ] Confirm Node 22+ on dev runtime (`node --version`)
- [ ] Confirm no manifest changes — `package.json` unchanged; no new runtime deps
- [ ] Confirm GitHub API reachable unauth (`curl -sS -o /dev/null -w '%{http_code}\\n' https://api.github.com/repos/InsightRiot/signal/tags`) → expect 200
- [ ] Confirm `claude --version` returns ≥ 2.1.150 (for script execution path; not strictly required at dev time)
- [ ] Confirm `which jq` (expected empty — informs `node -e` script-gen strategy)
- [ ] Confirm `tools/lib/atomic-write.js` + `tools/lib/file-lock.js` present (M4.5.E6 deliverables; reused by E8)
- [ ] Confirm Mac Studio's `~/.claude/plugins/cache/signal/` contains the multi-version dogfood substrate (`0.1.0/`, `0.1.1/`, `0.1.2/` under `sig/` + `0.1.0/` under `signal/`)

Any drift discovered → addendum to RESEARCH.md; plan adjusted if necessary; otherwise EXECUTE proceeds.

---

## Verdict

**PASS across all 8 dimensions + Nyquist strict mapping complete. PLAN ready for EXECUTE pending user approval (strict gate) and environment check.**
