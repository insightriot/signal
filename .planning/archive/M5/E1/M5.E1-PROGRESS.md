# M5.E1 — Doc-runtime & memory hygiene — Progress

**Tier:** FULL / strict / TDD. **Plan:** `M5.E1-PLAN.md`. **Execution:** sequential, one slice at a time (S2/S3/S4 are DAG-parallel but land in overlapping files — `state.js` — so run in series to avoid git-index races on Signal's own repo).

Baseline before EXECUTE: 999 tests (after B7 release-hygiene fix `f54c0cc` greened clean main; the M4.5.E11 ship had left `plugin.json` at 0.1.6).

## Wave 1 — foundation
- [x] **S1** — Canonical doc-model (FR1) + STATE body skeleton (FR2c) — commit `a8f5f9f` — 999→1008 tests

## Wave 2 — mechanics (after S1) — COMPLETE
- [x] **S2** — Migration body-relocate (FR2a) + tier-aware size warning (FR2d) — commits `bdb35d0` + `d7beb0e` — delegated to executor, verified (circular import dodged via status.js wrapper; sync `readStateSize` preserved). 1008→1020.
- [x] **S3** — Evict-on-close + faithfulness gate (FR2b) — commits `3a4970a` (S3a gate) + `4675772` (S3b orchestration + ship/checkpoint wiring) — built in main context. 1020→1046. 26 tests incl. R1 lossy-card catcher.
- [x] **S4** — FUTURE-IDEAS physical eviction (FR3) — commits `7fd68d5` (classification) + `ba30267` (eviction) — delegated; executor stalled mid-stream AFTER committing, work verified independently (1046→1065, crash-injection + dedup + dangling-fence all correct).

## Wave 3 — proof (after S2/S3/S4) — COMPLETE
- [x] **S5** — Dogfood on Signal's own `.planning/` — done in main context, snapshot-first (clean `5fd9bec` baseline), auto-state-protocol suspended, no phase transition.
  - **t1** vector finding pinned in `references/doc-runtime-model.md` §3 (CMMC = vector-1 frontmatter-prose → E3; Signal own = vector-2, 64 KB body).
  - **t2** STATE.md body relocated → `.planning/STATE-HISTORY.md` (frontmatter byte-identical): **64,496 → 1,040 bytes**; body replaced with the S1 live-above-the-fold skeleton + pointer. Programmatic `atomicWrite` (bypasses the PreToolUse hook by design).
  - **t3** FUTURE-IDEAS terminal eviction: **6** promoted-to-M4.5.E10 entries (blockquote-stamped, shipped v0.1.5) → `.planning/archive/FUTURE-IDEAS-LEDGER.md`; inbox **187,488 → 169,491 bytes**, 41 → 35 entries, 0 terminal remaining. Second run = byte-identical no-op (dedup/crash-safety confirmed on live files). 0 deferred (none to keep); 35 undisposed untouched.
  - **t4** `/sig:resume`+`/sig:status` orientation facts **identical pre/post** (phase PLAN, epic M5.E1, tier FULL, next `/sig:execute`, last-completed S4 — all from frontmatter, unaffected). Link-health green (STATE→STATE-HISTORY resolves; no anchor links into the evicted FUTURE-IDEAS headings). Full suite 1065 unaffected (fixture-based).
  - **Finding (confirmed):** Signal's 64 KB STATE.md body was a single pre-schema legacy blob → wholesale body relocation, not per-Epic `evictEpicNarrative`. Orientation holds by construction (frontmatter + PLAN artifact drive it, never body prose).
  - **Deferred to SHIP (normal close-time doc refresh):** INDEX.md (add STATE-HISTORY.md + ledger rows, refresh corpus counts) + CONTEXT.md narrative (M5.E1 EXECUTE progress). Not an EXECUTE-phase task.

## EXECUTE outcome
All 5 slices shipped. **999 → 1065 tests** (+66), validator green. Next: VERIFY.

### Carry-forwards into VERIFY / REVIEW (named honestly — not a spotless close)
1. **FR2b `evictEpicNarrative` (the S3 flagship) is proven on FIXTURES ONLY — armed but never live-fired.** The S5 dogfood exercised **FR3** (`evictTerminalToLedger`) on real data, but **never invoked `evictEpicNarrative`** (grep-confirmed absent from S5): Signal's body had no `## <EpicId>` section to scope to. It is wired live into `ship.md §5.5` but its first real firing is a *future* Epic's SHIP that has a body section. It will **no-op at M5.E1's own SHIP** (the skeleton body has no `## M5.E1` heading), so it can't self-dogfood at close either. Claim precisely: *"FR3 dogfooded on real data; FR2b evict-on-close proven on fixtures, armed but not live-fired."* REVIEW should scrutinize the wired `ship.md §5.5` path.
2. **S5's plan-mapped test `tests/dogfood-orientation.test.js` was NOT written.** S5's two orientation ACs were verified **once, manually** via ephemeral node scripts (frontmatter byte-identity + orientation-unchanged + link-health). Under `nyquist_enforcement: strict`, VERIFY checks every AC maps to a *test*. **Decision for VERIFY (recommend option b):** record in VERIFICATION.md that S5's ACs are a **verified-once historical event**, not a standing invariant, and a live-`.planning/`-coupled standing test was **deliberately declined** to keep the suite fixture-based (the underlying mechanisms — `evictTerminalToLedger`, frontmatter-preserving writes — ARE fixture-tested in `drain.test.js` / `evict-on-close.test.js` / `state.test.js`). Option (a) — a live-coupled anti-re-bloat guard — was considered and rejected: it false-fails on normal future growth (a stamped-but-not-yet-drained terminal entry; a growing In-flight section) and re-introduces the fixture-coupling correctly avoided elsewhere.

### Finding for M5.E3 (FR6 migrate command) — no action now
Relocating an **already-migrated** inlined STATE.md body (vector-2-existing) has **no shipped mechanism**: FR2a fires only on legacy *no-frontmatter* files, and FR2b's `evictEpicNarrative` is Epic-section-scoped. S5 relocated Signal's own body with a **one-off hand script** (`scratchpad/s5-relocate.mjs`, not reusable production code). E3/FR6 must build this and must **not** assume it already exists. (`references/doc-runtime-model.md` §3 vector-2 row corrected to say so.)

## Notes
- Faithfulness-gate blind spot (carry to VERIFY/REVIEW): the deterministic backstop catches ID/date/status-token **loss**, not **paraphrase distortion**. Green S3 tests ≠ "faithfulness proven" — that's proven only on live content (S5) + REVIEW.
- Pre-existing failing test on entry (B7, plugin.json version drift) fixed as standalone hygiene commit `f54c0cc` before S1 per Brett's call.
