# M5.E1 — Doc-runtime & memory hygiene — Plan

**Phase:** PLAN complete → EXECUTE next. **Tier:** FULL / `gate_strictness: strict` / `nyquist_enforcement: strict` / `plan_validation_dims: all`.
**Spec:** `M5.E1-REQUIREMENTS.md` (FR1–FR7). **Research:** `M5.E1-RESEARCH.md`. **Decisions:** DECISIONS 2026-07-16 (D-M5E1-1…6).

## Phase goal
Ship the doc-runtime **model + eviction mechanics** (STATE.md working-set + FUTURE-IDEAS inbox) so closed content leaves the live files into unit-homed archives, faithfully, and prove it by dogfooding on Signal's own `.planning/`. (All-docs hygiene FR4 + backlog lifecycle FR5 → M5.E2; migrate command FR6 + layout stamp FR7 → M5.E3.)

## Scope this Epic
FR1 (doc-model) · FR2a (new-migration body relocate) · FR2b (evict-on-close + faithfulness gate) · FR2c (body skeleton) · FR2d (tier-aware size warning) · FR3 (FUTURE-IDEAS physical eviction) · dogfood. **Explicitly deferred:** the acute vector-1 (frontmatter-prose) remediation → E3 migrate command (CMMC file is its fixture); FR4/FR5 → E2; FR6/FR7 → E3.

## Slices (vertical; each independently shippable)

### S1 — Canonical doc-model (FR1) + STATE body skeleton (FR2c) — [M]
The foundation every other slice references. Mostly docs + one code stub.
- **t1** Write `references/doc-runtime-model.md`: the two axes (load-frequency × growth-policy); the **unit-homed single-home** eviction-destination rule; the 3-vector bloat taxonomy; RETROSPECTIVE-as-SUMMARY-card decision; the distill→verify-against-source→evict ordered gate; Curator borrow/leave; tiered-index shape (build deferred to E2); the derived-vs-hand-curated-INDEX conflict flagged for E2.
- **t2** Add the normative "live-above-the-fold" body skeleton to `references/state-schema.md` (resume pointer → in-flight → blockers → pending ops → one-line pointers to closed work); mark the current free-prose guidance superseded.
- **t3** Update `initState` body stub (`state.js` ~233) to emit the skeleton headings.
- **Deps:** none. **AC:** (1) model doc covers the two axes, the destination rule, the 3-vector taxonomy, and the ordered gate; (2) `state-schema.md` documents the skeleton; (3) `initState` output matches the skeleton headings; (4) INDEX.md hand-curation stance unchanged (derived-index decision explicitly deferred to E2).
- **Test:** unit test asserting `initState` body contains the skeleton section headings; doc presence/section-lint check.

### S2 — Migration body-relocate (FR2a) + tier-aware size warning (FR2d) — [M]
Two low-risk, independent `state.js` changes.
- **t1** FR2a: in `upgradeStateFile`, write the legacy `raw` to `.planning/STATE-HISTORY.md`; set body to a one-line pointer + a **rewritten** migration notice (the old "everything below is history" line becomes false). Preserve the `already-frontmatter` idempotency guard (170-172) — re-run is a no-op. New migrations only (not retroactive).
- **t2** FR2d: resolve PROFILE tier → size threshold (SKETCH < FEATURE < FULL) in an **async command-layer wrapper**; pass it to the existing `detectStateSize(bytes, threshold)`. Keep `readStateSize`/`readStateSizeBanner` **synchronous** (do not break sync callers/tests). Flat fallback when no PROFILE (fail-open).
- **t3** Rewrite the stale `detectStateSize` message (~1036) that says "Automated eviction is planned for M5…" (self-stale once this ships).
- **Deps:** S1 (destination rule for STATE-HISTORY.md). **AC:** (1) legacy no-frontmatter migration → STATE.md = pointer, STATE-HISTORY.md = notice + verbatim body, **frontmatter byte-identical**; (2) re-run = no-op; (3) tier thresholds applied per PROFILE, flat fallback when absent; (4) stale message replaced.
- **Test:** extend `tests/state-schema.test.js` (relocation + idempotency + frontmatter round-trip, no field lost); extend `tests/state-size.test.js` (per-tier threshold cases + fallback).

### S3 — Evict-on-close + faithfulness gate (FR2b) — [L] (judgment/risk slice)
The heart. A new body-eviction helper wired into ship/checkpoint.
- **t1** Helper: given `current_epic` at Epic-close, extract *that Epic's* closed narrative from the STATE body (scope to the closing unit — never heuristic GC of arbitrary prose).
- **t2** Distill: produce the RETROSPECTIVE-shaped card — **lift-verbatim-don't-rewrite** when a faithful paragraph exists, IDs verbatim, cite artifacts; **lift open carry-overs/blockers UP** into the live in-flight/blockers section (never bury them in archive).
- **t3** Verify-against-source gate: read the card against the archived original; **deterministic coverage backstop** — assert every `M*.E*`/`D-*`/`FR*`/`AC*` ID, ISO date, and `open/deferred/shelved/carry-over` status token survives into the card or an explicit "intentionally dropped" list. Gate FAILS on a lossy card.
- **t4** Move (mechanical): zero-loss-accounted relocation of the original narrative → `.planning/archive/<milestone>/<epic>/`; replace the live body narrative with a one-line pointer. Closed-vs-live confirm before the move. Programmatic `atomicWrite` (body-only; no write-guard interaction).
- **t5** Wire attach points: `commands/ship.md` §5 + `commands/checkpoint.md` step-5.
- **Deps:** S1 (skeleton + gate spec). **AC:** (1) a closed Epic's narrative → pointer in STATE + card in archive; (2) an **in-flight** Epic block is byte-identical (untouched); (3) a **lossy** card FAILS the coverage gate; a golden faithful card PASSES; (4) a closing block's live carry-over appears in the live section post-eviction; (5) original survives byte-identical in archive (zero-loss word-accounting holds).
- **Test:** net-new suite; fixtures: closed+in-flight body (only closed touched), lossy-summary (gate fail), golden faithful (gate pass), carry-over-lift. Patterns from `tests/dispatch-task-with-state.test.js` / `tests/retro-index.test.js`.

### S4 — FUTURE-IDEAS physical eviction (FR3) — [L] (data-integrity slice)
Make shipped/promoted entries physically leave the inbox.
- **t1** Add a move-to-ledger branch to `drain.js` `transformBlock` (emit block for ledger + return `''` for source).
- **t2** Two-file atomic helper: **ledger-append FIRST** (keyed by heading+date hash → dedupe-safe), **THEN** remove from inbox; a mid-crash re-run completes without dupe/loss.
- **t3** Terminal-only eviction predicate — shipped/promoted/merged/deleted — **explicitly NOT Deferred** (deferred = parked/live, stays in inbox).
- **t4** Inherit the dangling-fence posture: refuse/scope-above the marker when `danglingFence`; never mutate a `recovered` entry. R1 diff-preview gate wraps every eviction write.
- **Deps:** S1. **AC:** (1) a shipped/promoted entry physically leaves the inbox → ledger; a second drain doesn't re-surface it; (2) a **Deferred** entry survives in the inbox; (3) dangling-fence → scoped no-op + warning, file uncorrupted; (4) crash-injection between ledger+inbox → no dupe/loss on re-run; (5) non-target blocks byte-identical.
- **Test:** extend `tests/drain.test.js` — predicate, dangling-fence no-op, crash-injection (renameFn stub, per the existing atomic-fail test), byte-identical neighbors (snapshot), ledger dedup.

### S5 — Dogfood on Signal's own `.planning/` — [M]
Prove the model on the real repo; the CMMC file stays the E3 fixture.
- **t1** Record the pinned vector finding (CMMC = frontmatter-prose, vector 1 → E3; Signal own = vector 2, 64 KB body) in the model doc / a note — no code.
- **t2** Apply S3's move-helper to Signal's own inlined legacy body (vector 2-existing) → `STATE-HISTORY.md` + pointer; evict any already-closed-Epic narrative from the body.
- **t3** Evict shipped FUTURE-IDEAS entries via S4 to the ledger (batch, diff-previewed).
- **t4** Verify: `/sig:resume` + `/sig:status` render the **same** orientation facts pre/post; link-health green (no dangling pointers). Discrete committed step, snapshot-first, **no phase transition mid-flight** (per the dogfood-on-self risk).
- **Deps:** S2, S3, S4. **AC:** (1) Signal's STATE.md body shrinks (64 KB → lean skeleton + pointers); (2) `STATE-HISTORY.md` holds the relocated body verbatim; (3) `/sig:resume`/`/sig:status` orientation unchanged; (4) link-health green; (5) shipped FUTURE-IDEAS entries in the ledger, inbox lean.
- **Test:** post-dogfood assertion that resume/status orientation facts are unchanged; link-health check.

## Dependency graph
```
S1 ──┬─→ S2 ─┐
     ├─→ S3 ─┼─→ S5
     └─→ S4 ─┘
```
S2/S3/S4 parallelizable after S1. S5 (dogfood) last — needs the mechanisms.

## Environment
Node 22+ (dev 22.13.0), vitest; **no new deps**, no manifest change. `npm install` unaffected.

## Anti-scope (guardrails)
- No new index/graph *generation* (tiered-index build = E2); no `SUMMARY.md` family (reuse retro); no OKF/`llms.txt`; no migrate command (E3); no `docs_layout_version` stamp/hook (E3); no vector-1 de-prose transform (E3, CMMC fixture). Do NOT rebuild v0.1.6 prevention (write-guard, size-banner, drain don't-resurface REs).

## Last Updated
2026-07-16 — 5-slice plan; 3-vector finding routes acute remediation to E3.
