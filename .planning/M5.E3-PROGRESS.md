# M5.E3 — EXECUTE Progress

**Epic:** M5.E3 (final doc-runtime layer). **Tier:** FULL/strict. **Plan:** `M5.E3-PLAN.md` (7 slices / 5 waves).

Slice-level dispatch (executor per slice, sequential), auto-state-protocol wrapped, full suite green after each slice. One hard human gate at S6b.t3 (live dogfood diff).

## Wave 1
- [x] S1 (FR1) — ISSUES-INBOX rename + smart capture + `--bug` — **done** (`1de9ca6`→`6e94ee8`, 5 commits)
  - t1 resolver `1de9ca6` · t2 title-thread `0ad1633` · t3 `--bug` `e72271c` · t4 rename/docs/fixture `a948c24` · t5 lazy-create `6e94ee8`
  - New: `tools/lib/inbox-path.js`, `tests/inbox-path.test.js`, `tests/fixtures/add/issues-inbox-minimal/`. Suite 1300→1337.
  - Carry-forward → S6a: `drain.js` `LEDGER_HEADER` const still reads "FUTURE-IDEAS" (fresh-ledger branch only; fold into the FR6 file+content rename).

## Wave 2
- [x] S2 (FR3) — auto `/sig:index` + D-ID→home map — **done** (`bf1db45`→`be91278`, 6 commits)
  - t1 roster.js `bf1db45` · t2 enumeratePlanningDocs `c30399e` · t3 parseExistingAnnotations `25559c8` · t4 render/regen+fixpoint `68bdf36` · t5 D-ID map+resolveDecisionId `6429306` · t6 /sig:index `be91278`
  - New: roster.js, planning-index.js, commands/index.md (+2 test files). **Roster now 17 commands** (added /sig:index). Suite 1337→1365.
- [x] S4 (FR2) — living `BACKLOG.md` + drain classify/promote/evict — **done** (`dc722cb`→`d62d140`, 7 commits)
  - t1 backlog.js `dc722cb` · t2 promoteToBugs `823af9d` · t3 classify+plan.md `e3e7b2d` · t4 crash-safe `571e067` · t5 ship sweep `7111b7c` · t6 snapshot-seed `23e81fd` · +coverage `d62d140`
  - New: backlog.js (+test). plan.md Step 1b classify (work/bug + roadmap/hygiene tag), ship.md §6.5 sweep. Real BACKLOG/inbox/BUGS untouched (S6b owns dogfood). Suite 1365→1389.
  - **→ S6b:** create-with-snapshot-then-promote runtime path is structurally sound but untested — eyeball during the live BACKLOG restructure. Snapshot pointer seeded (`> Seeded from BACKLOG-REVIEW-…`); S6b folds content + decides pointer fate.

**Wave 2 complete** — S2 + S4 green, pushed.

### Carry-forwards from S2
- **→ S3 (FR4):** count-reconcile target is **17 commands** (roster.js authoritative), not the plan's literal "16". README/CLAUDE.md ("15"/"16 slash commands") are stale by S2's action — S3.t1 fixes.
- **→ S5 (FR5):** `resolveDecisionId` resolves by prefix **range-membership**, not exact entry — a gap-ID in a range over-resolves. S5's anchor gate must verify REAL evicted IDs resolve (which they do), not rely on range hits alone.
- **→ S6b:** live-INDEX regen is a **manual judgment pass** — curated Gotchas are in old table format the list-parser can't read (→ `_(note pending)_`); HOT/WARM/COLD legend mismatches the generator's two-tier sectioning. Port gotchas + reconcile legend + human-eyeball. Mechanism proven on fixtures (AC3.2); legacy-format port is manual.
- **REVIEW watch-item:** validate-plugin now glob-sources commands → an *expected-but-absent* command file is no longer flagged "Missing command" (glob lists only what's on disk). Intentional per glob-as-authority; slight net reduction in validator strictness. Flag for REVIEW; not a product defect.

## Wave 3
- [x] S3 (FR4) — all-docs hygiene runtime — **done** (`23e87bf`→`b51cd04`, 6 commits)
  - t1 reconcile 15→17 `23e87bf` · t2 links `28cff3f` · t3 roster `a4e52f6` · t4 version `6e1d61b` · t5 [FILL IN] `8fbdbb7` · t6 meta+wiring `b51cd04`
  - New: doc-hygiene.js (+test). Guard 0 HARD/0 SOFT on live Signal · HARD: dead-link/roster/version/stub · SOFT: anchors. Inbox-agnostic + offline (meta-tested). Suite 1389→1409.
  - Reconciled: CLAUDE.md:70 (15→17 +names), docs/map:478 (15→17), README:72 de-numanized. Left CLAUDE.md:32 (historical). Narrow `#`-comment allowlist excludes the line-32 narrative; tester-brief.md carved out of [FILL IN].
  - **→ S6b/SHIP:** README's Command-reference **prose list** still enumerates 15 commands (omits `/sig:index` + `/sig:migrate-memory`) — a release doc pass (prose authoring, out of S3 count-reconcile scope). Must be filled before the combined release ships.
- [x] S5 (FR5) — append-log eviction (evict-with-anchors) — **done** (`d912aa5`→`4687906`, 6 commits)
  - t1 parser+cutoff `d912aa5` · t2 verbatim relocate+pointer `46578e1` · t3 anchor gate fail-closed `595f705` · t4 standalone e2e `9ccb312` · t5 compat/full-read `4a403b0` · seam extract `4687906`
  - migrate-memory.js + e2e test + fixture. Real DECISIONS.md byte-unchanged (S6b owns dogfood). Suite 1409→1433. Byte-identity vs original raws (not conserves-tautology); anchor gate covers the S2 prefix-split→live-tie over-resolution (fail-closed→detect-only).

**Wave 3 complete** — S3 + S5 green, pushed.

### Critical S6a handoffs from S5
- **Compose via the discrete seam, NOT `runAppendLogEvict`:** `senseAppendLogEvict(text, {boundaryDate, milestoneOf, dateStr})` (pure) → `applyAppendLogEvict(baseDir, plan, {snap, rollback, resolveId})` under applyMigrate's ONE shared snapshotter/lock. `runAppendLogEvict` owns its own snapshotter+regen — can't compose.
- **AC5.3 dangling half is S6a's:** verbatim relocate re-roots relative `](*.md)` links inside moved blocks (block → `.planning/archive/M1/DECISIONS.md` mis-roots `](archive/M1/…)` → `…/archive/M1/archive/M1/…`). S6a's ONE dangling gate must rewrite-or-flag; extend the snapshot to cover DECISIONS.md + each `archive/M{n}/DECISIONS.md`. This is the BYTE-vs-link-integrity tension → why FR5 NFR mandates the human-approved dry-run diff (S6b). Real repo: only ~2 file-level hard links (669 refs are index-resolvable D-IDs).
- **No INDEX regen inside the seam** — the anchor gate reads the disk-fresh D-ID map; the tail `index-regen` is the ONE INDEX refresh (Issue 4).
- **Boundary map is M5-only** (`MILESTONE_OPEN_DATES` = {M5: 2026-07-15}); pre-M5 sections are `unroutable` → fail-safe detect-only until **S6b** supplies the M1–M4.5 open-date map (the messy-windows judgment).

## Wave 4
- [ ] S6a (FR6) — v3 migrate mechanics — pending

## Wave 5
- [ ] S6b (FR6) — rollout + dogfood + retire Curator — pending

## Test-count log
- Baseline (pre-EXECUTE): 81 files / 1300 tests green
- After S1: 82 files / 1337 tests green (+37) · validate green
- After S2: 84 files / 1365 tests green (+28) · validate green · roster 17/26/21
- After S4: 85 files / 1389 tests green (+24) · validate green [Wave 2 done]
- After S3: 86 files / 1409 tests green (+20) · validate green · hygiene guard live
- After S5: 87 files / 1433 tests green (+24) · validate green [Wave 3 done]

## Last Updated
2026-07-19 — EXECUTE opened; baseline capture.
