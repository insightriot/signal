# M4.5.E11 — Review Report (REVIEW phase)

**Effective tier:** FULL — `review_depth: full`, `security_audit: full`, `performance_pass: true`, `simplification_pass: true`, `gate_strictness: strict`. Full four-lens panel: two **independent specialist agents** (code-quality + security) reviewed the Epic's code diff (`da0c400..HEAD`, `tools/` + `hooks/`) adversarially; performance + simplification taken by the lead. Mirrors the E10 two-agent pattern.

**Verdict: ✅ PASS-WITH-FIXES** — 0 Critical, 1 Important (fixed in-phase), 1 Suggestion (rejected with reason), 1 Nit (deferred, author's discretion). 999/999 tests green after the fix; validator green.

## Critical Issues
None.

## Important Issues

### I-1 — Stale header comment in `hooks/check-state-write.js:3-11` — ✅ FIXED IN-PHASE
The file header described **pre-E11** behavior the code no longer has: it claimed a missing Epic-close retro "exits 2 (block)" and that "this layer is what makes D-E9-3's 'no bypass' hold." Post-E11 (S1.t4, D-E11-5) the retro-absence path **warns at exit 0**; the hard retro contract moved to `/sig:ship` §0.5's `shipFR1Check`. Under the repo's docs-always-accurate rule this is a defect, not cosmetics — a maintainer of a security-relevant hook would reason about a block-guarantee the code doesn't provide.

- **Fix:** rewrote the header (lines 4-14) to state the two-tier posture explicitly — (1) malformed frontmatter → hard block (exit 2); (2) missing Epic retro → warn (exit 0), throw-safe; the hard gate lives in `ship.md` §0.5. Comment-only; 999 tests unchanged, hook spawn tests (28) green.
- **Fix-in-phase justification:** ≤12 lines, doc-only, zero design/behavior impact, tests stay green — squarely within the PASS-WITH-FIXES rule (<50 LOC, no architectural impact).

## Suggestions

### S-1 — Read/write Epic-ID regex asymmetry — ❌ REJECTED (would break back-compat)
Both specialists noted that `resolveArtifactPath` pattern-0 (read) uses the permissive `EPIC_ID_RE = /^[A-Za-z0-9._-]+$/` while `artifactName` (write) uses strict `EPIC_ID_STRICT_RE`. The security auditor offered narrowing pattern-0 to strict as optional tidiness (explicitly *not* required — both paths are path-confined, non-exploitable).

**Rejected, surfaced not silently applied:** the permissive read is **load-bearing back-compat**, not incidental. This repo contains legacy non-strict Epic-prefixed artifacts (`v0.1.6-PLAN.md`, `v0.1.6-REQUIREMENTS.md`, … — the v0.1.6 patch tracked `current_epic: v0.1.6`). `v0.1.6` fails the strict regex but passes the permissive one; narrowing pattern-0 to strict would make `/sig:resume` unable to resolve those artifacts (the exact papercut E10's read-half exists to fix). The strict-write / permissive-read asymmetry is intentional: the writer emits only strict names going forward, while the reader stays permissive to resolve pre-E11 hand-managed artifacts. Left unchanged by design; the existing comment on `EPIC_ID_STRICT_RE` already documents the intent.

## Nits

### N-1 — `warn(retro.reason)` reuses a block-authored message (`check-state-write.js:114`) — deferred
`retro.reason` reads slightly imperatively under the `warning:` prefix. Harmless (the prefix disambiguates); author's discretion. Not worth a churn commit.

## Security Findings (`security_audit: full` — OWASP + threat-model audit)
**0 Critical, 0 Important.** Threat model: a hostile/malformed `.planning/STATE.md` where `current_epic` (user-editable YAML) flows into file paths and git arguments. All four surfaces **safe by construction**, each empirically verified:

1. **Path traversal via `current_epic` — SAFE.** Every E11-new sink (`artifactName`, `readEffectiveProfile` probe, `isEpicDone`, `setCurrentEpic`) validates against `EPIC_ID_STRICT_RE` **before** building a path or touching disk; non-strict values fail open to linear/project defaults, never reaching interpolation. The strict regex admits only `M`/digits/`.`/`E` — `../`, absolute paths, separators, and newline/null-byte injection all rejected (tested). `resolveArtifactPath` pattern-0 (permissive, pre-existing) is path-confined by `startsWith(planningRoot + sep)`.
2. **Git-arg / option injection — SAFE.** No E11-new code feeds `current_epic` into git. The named git helpers are pre-existing (E10), unchanged in-range; `execFileSync` args-array + `COMMIT_TOKEN_RE` leading-alnum anchor still guard `last_updated_commit`.
3. **Fail-open — SAFE, and hardened by E11.** The hook's new try/catch around `checkProposedStateWrite` (`:112-117`) **fixes a latent crash**: pre-E11, a malformed-but-present `current_epic` on an Epic-close SHIP write crashed the PreToolUse hook (via an uncaught `deriveRetroPath` throw) in a stranger's session. The integrity block (`checkStateFrontmatterShape` → exit 2) is unchanged.
4. **ReDoS — SAFE.** All regexes linear; sub-ms on 100k-char adversarial inputs. `(\.\d+)*` is `.`-delimited, no catastrophic backtracking.

Positive: `EPIC_ID_STRICT_RE` centralized as single source of truth (`deriveRetroPath` now imports it — closes the read/retro regex schism); consistent guard-before-build discipline across every new sink.

## Performance Findings (`performance_pass: true`)
**No anti-patterns.** The Epic adds pure functions + one bounded probe: `readEffectiveProfile` does a single `existsSync` + conditional single-file `readFile` on a command's first action. No N+1, no unbounded fetches, no added loops in the new helpers (loops in status.js/profile.js are pre-existing, bounded by `PHASES.length`=7 / fixed schema keys). `artifactName` / `formatTierLine` are O(1) pure.

## Simplification Opportunities (`simplification_pass: true`)
**Clean.** No dead code; every new import (`EPIC_ID_STRICT_RE` ×3, `formatTierLine`) is used. The `readProfile` → `readProfileFromPath` refactor removes duplication (one validator, two entry points). `LINEAR_UNPREFIXED` as a one-member Set is intent-documenting (extensible carve-out list) — acceptable, not over-abstraction.

## Invariants attested (code-reviewer, verified against tests)
1. `artifactName` ↔ `resolveArtifactPath` round-trip — HOLDS (strict ⊂ permissive; all 8 kinds).
2. RETROSPECTIVE single-owner — HOLDS (Epic-mode byte-identical to `deriveRetroPath`; linear excluded, ship-owned).
3. Carve-outs REQUIREMENTS / CONTEXT / RETROSPECTIVE — all correct.
4. `readEffectiveProfile` fail-open-on-STATE vs throw-on-malformed-PROFILE-content — HOLDS exactly.
5. `readProfile`→`readProfileFromPath` refactor — behavior-preserving (byte-identical halt copy; golden-equality test).
6. Hook fail-safety — HOLDS (both predicates crash-proof; shape check still hard-blocks).
7. Strict-write / permissive-read regex placement — correct everywhere.

## Verdict
- [ ] PASS
- [x] **PASS-WITH-FIXES** — I-1 fixed in-phase (hook header comment); ready for SHIP.
- [ ] FAIL

**Carry to SHIP:** the two-tier retro split (B2) — `/sig:ship` §0.5 keeps E9's hard retro gate; downgrade-to-warn is a one-line change if preferred. Surfaced at the plan gate and again here for a conscious SHIP-time decision.

## Last Updated
2026-07-15 — REVIEW complete. Verdict PASS-WITH-FIXES (I-1 fixed). Next: SHIP.
