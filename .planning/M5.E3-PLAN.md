# M5.E3 — PLAN

**Phase goal:** Ship the final doc-runtime layer — the ISSUES-INBOX/BACKLOG/BUGS lifecycle, the auto-`/sig:index`, the all-docs hygiene guard, closed-milestone `DECISIONS.md` eviction, and the v2→v3 rollout — so the combined E1+E2+E3 release delivers a complete, self-maintaining memory/doc system.

**Tier:** FULL/strict — `tdd_required: true`, `nyquist_enforcement: strict`, `plan_validation_dims: all`, `gate_strictness: strict`. **7 slices / 5 waves.** Research: `M5.E3-RESEARCH.md`. Requirements: `M5.E3-REQUIREMENTS.md`.

## Cross-cutting decisions (locked from research — apply in every slice)
- **Back-compat inbox resolver (R1):** `resolveInboxPath(baseDir)` = `ISSUES-INBOX.md` if present else `FUTURE-IDEAS.md` — the code rename is non-breaking, migrate-timing-independent.
- **v3-conformance predicate gates the stamp (R2):** v3-conformant ⟺ (V1/V2/V3 clean) AND `FUTURE-IDEAS.md` absent AND `BACKLOG.md` present AND FR5-evict done. Bump `CURRENT_LAYOUT_VERSION`/`LAYOUT_VERSION` **only** after conformance is v3-aware.
- **FR5 = verbatim relocate behind a dated pointer, no distilled card**; selector = date cutoff (`< 2026-07-15`, strict); history → per-milestone `archive/M{n}/DECISIONS.md`.
- **Minimal rename** (path const + strings + resolver; internal symbol names kept). **`--bug` = simple entry** (no B-ID/table-row). **Exclude `archive/`** from the rename rewrite+residual-scan.
- **Single roster authority:** `tools/lib/roster.js` (filesystem-glob), imported by `validate-plugin.js` + the FR4 guard.
- **The bar (advisor):** **each slice lands green on Signal's own repo at its own wave.** The FR4 guard (S3, W3) must pass on *pre-dogfood* Signal (still `FUTURE-IDEAS.md` until S6b/W5) — so the guard is **inbox-name-agnostic** (checks counts/versions/links, never the inbox filename). **Intended side-effect (plan-checker Rec 2):** after S6a bumps the version constant (W4) but before S6b migrates the repo (W5), Signal's *own* `.planning/` is a v2-stamped project under `CURRENT=3` → it emits the fail-open drift banner on its own `/sig:resume`/`/sig:status`. This is **intended** (Signal becomes the canonical un-migrated-v2 project S6b's dogfood then fixes), non-blocking — don't mistake it for a regression.

## Dependency graph / waves
```
W1: S1 (FR1)
W2: S2 (FR3)  ‖  S4 (FR2)          [both depend on S1]
W3: S3 (FR4)  ‖  S5 (FR5)          [S3 needs S2's roster.js; S5 needs S2's D-ID map]
W4: S6a (FR6 migrate mechanics)     [depends S1, S4, S5]
W5: S6b (FR6 rollout + dogfood)     [depends S6a]
```

---

## S1 — FR1: ISSUES-INBOX rename + smart capture  (Wave 1 · complexity M)
**Goal:** `/sig:add` captures verbatim body + agent-authored title to `ISSUES-INBOX.md` (back-compat to `FUTURE-IDEAS.md`), with a new `--bug` fast-path. Non-breaking.
**Depends:** none. **Files:** `tools/lib/add.js`, `tools/lib/drain.js`, `commands/add.md`, tests + fixture dir.

**Tasks (TDD — RED first where behavioral):**
- t1 `resolveInboxPath(baseDir)` (new shared helper) + wire into `add.js` (`FUTURE_IDEAS` const site) and `drain.js` (`inboxRel` default). **RED:** a project with only `FUTURE-IDEAS.md` resolves to it; with `ISSUES-INBOX.md` present, resolves to the new name; with neither, resolves to the new name (for lazy-create).
- t2 Thread optional `title` through `captureToDestination` → the `buildEntry` closures (`heading = title?.trim() || deriveHeading(body)`). **RED:** given a `title`, the entry heading is the title; body byte-identical; omitting `title` → `deriveHeading` fallback.
- t3 `--bug` fast-path: `parseInput` recognizer + `resolveDestination` arm + `captureToBugs`/`buildBugsEntry` (simple entry: heading + `**Status:** needs-triage` + verbatim body + `---`; routes like `--question`, bypasses `--file` denylist). **RED:** `--bug "x"` appends to `.planning/BUGS.md`; multi-flag conflict guard rejects `--bug --question`.
- t4 Rename path const + user-facing strings + ledger default (`FUTURE-IDEAS-LEDGER.md`→`ISSUES-INBOX-LEDGER.md` default, resolver-guarded) + missing-file error text. Update `commands/add.md` prose + the fixture dir. **RED:** existing add/drain tests pass against the resolver (rename is behavior-preserving).
- t5 Lazy-create the inbox: `captureToDestination` creates `ISSUES-INBOX.md` on first capture instead of throwing `missingFileError` (born-on-v3 half; AC6.3). **RED:** `/sig:add` in a repo with no inbox creates `ISSUES-INBOX.md` + writes the entry (no throw).

**Acceptance:** AC1.1–AC1.5 + the AC6.3 lazy-create half. `resolveInboxPath` proven both-directions. Full suite green on Signal (still `FUTURE-IDEAS.md` present → resolver picks it).

---

## S2 — FR3: auto-generated `/sig:index` + D-ID→home map  (Wave 2 · complexity L)
**Goal:** `INDEX.md` regenerates deterministically, preserving curated annotations by ID, and publishes a `D-ID → home` map (FR5's lifeline).
**Depends:** S1 (parallel-safe). **Files:** new `tools/lib/planning-index.js`, new `tools/lib/roster.js`, new `commands/index.md`, tests.

**Tasks (TDD):**
- t1 `roster.js` — filesystem-glob canonical counts (`commands/*.md`, `agents/**/*.md`, `skills/**/SKILL.md`). **RED:** returns 16/26/21 (verify actuals) from disk. Refactor `validate-plugin.js` to import it (no behavior change).
- t2 `enumeratePlanningDocs(baseDir)` — walk `.planning/` + `archive/`, classify tier (COLD = under `archive/` or `*-RETROSPECTIVE.md`), reuse `enumerateRetros`/`classifyDocGrowthPolicy`. **RED:** enumerates every tracked doc with mechanical facts.
- t3 `parseExistingAnnotations(content)` — generalize `retro-index.js:parseExistingHooks` to INDEX's two keyspaces (file-path for tier tables, Epic-ID for the ledger) + tier legend by heading. **RED:** round-trips curated notes by key.
- t4 `renderPlanningIndex(docs, annotations)` — mechanical rows + curated-by-ID re-attach + placeholder for absent; **stable-key sort (path, never mtime)**. `regeneratePlanningIndex` = render-then-compare-before-write. **RED:** no-op run → `{written:false}`; **parse↔render fixpoint** test (`render(parse(render(x))) === render(x)`).
- t5 `D-ID → home` map: scan live `DECISIONS.md` (+ per-milestone `archive/M{n}/DECISIONS.md` when present) for `D-…` prefixes → compact `{sourceFile → ID-prefix ranges}`, plus a `resolveDecisionId(id)` consumer for FR5. **RED:** every `D-…` in DECISIONS resolves to its home; an archived block's IDs resolve to the archive file. **Mechanical-fresh (self-heals on eviction).**
- t6 `commands/index.md` (`/sig:index`) thin wrapper over `regeneratePlanningIndex`.

**Acceptance:** AC3.1–AC3.4 + the AC3.3 D-ID map with a `resolveDecisionId` API FR5 consumes. Idempotent (no-op + fixpoint proven). Regenerating Signal's own INDEX preserves its curated Gotchas + tier legend.

---

## S4 — FR2: living `BACKLOG.md` + drain classify/promote/evict  (Wave 2 · complexity L)
**Goal:** the `/sig:plan` drain classifies (work→BACKLOG / bug→BUGS), promotes into a live `BACKLOG.md` (with retitle), and evicts from the inbox; a light `/sig:ship` sweep clears terminal entries.
**Depends:** S1 (uses the resolver + `title` threading). **Files:** new `tools/lib/backlog.js`, `tools/lib/drain.js`, `commands/plan.md`, `commands/ship.md`, tests.

**Tasks (TDD):**
- t1 `backlog.js` — idempotent create-if-missing `BACKLOG.md` (skeleton: intro + `*Last updated:*` footer) + `promoteToBacklog(baseDir, {block, tag, title})` appending a `**Tag:** roadmap|hygiene` entry via `insertAboveFooter`; sha1-dedupe marker. **RED:** promote appends a tagged entry; a duplicate promote (same key) is a no-op.
- t2 `promoteToBugs` — simple entry into `BUGS.md` (reuse S1's `buildBugsEntry`). **RED:** bug-classified promote lands in BUGS.
- t3 Drain **classify** step: at promote time, `strict-enum [work, bug]` → destination; retitle via the `title` param. Wire into `commands/plan.md`'s drain (after the existing disposition). **RED:** a work entry → BACKLOG (retitled), a bug entry → BUGS; both then stamp `→ Promoted` (terminal).
- t4 Crash-safe ordering: **write destination first, then stamp+evict** via the existing `evictTerminalToLedger`. **RED:** a crash between destination-write and evict leaves no duplicate on re-run (dedupe holds); promoted entry gone from inbox, present in destination + ledger (double-home intended).
- t5 Light `/sig:ship` sweep: `evictTerminalToLedger(dryRun)`→confirm→real at Epic-close in `commands/ship.md` (near §6 regen; **not** `/sig:execute`). **RED:** ship sweep evicts a terminal inbox entry; execute never invokes it.
- t6 BACKLOG-from-snapshot skeleton note: `backlog.js` seeds from `BACKLOG-REVIEW-2026-07-04.md` if present, else empty skeleton (the *content* restructure is a dogfood task in S6b; here just the create-if-missing branch). **RED:** create-if-missing with/without a snapshot present.

**Acceptance:** AC2.1, AC2.3–AC2.6 (AC2.2 snapshot-content migration completes in S6b). Convergence (promoted entry doesn't resurface) via the `DISPOSED_RE` triad + eviction. Green on Signal.

---

## S3 — FR4: all-docs hygiene runtime  (Wave 3 · complexity L)
**Goal:** reconcile every current doc, then land a deterministic+offline test-suite guard that turns structural drift red.
**Depends:** S2 (`roster.js`). **Files:** new `tools/lib/doc-hygiene.js`, new `tests/docs-hygiene.test.js`, doc fixes across README/CLAUDE.md/docs/map, tests.

**Tasks (TDD — reconcile FIRST, then guard):**
- t1 **Reconcile (mechanical, driven by `roster.js`):** diff every prose count claim (README, CLAUDE.md, `docs/map/index.html`) against `roster.js`; fix all mismatches — **15→16 commands** + re-verify **26 agents / 21 skills / test-count**. This RESOLVES the deferred count-bump (see MILESTONE-5 § Release-prep). No guard yet → nothing to be red against.
- t2 Internal link-health check (HARD): generalize `scanDanglingLinks`/`walkMarkdown` past `.planning/` to `docs/`,`README`,`CLAUDE.md`,`analysis/`; file-existence HARD, anchor/slug SOFT. **Walk ignore-set (Issue 3 — the walk does NOT honor `.gitignore`):** exclude `.claude/` (the `worktrees/dogfood-status/` duplicate plugin tree — a 2nd `plugin.json`+`commands/`+`agents/` that would double-count the roster / false-fail version-consistency), `node_modules/`, `examples/`, and `archive/` (for the rename scan). **RED:** a seeded dead internal link fails; a seeded external URL is ignored (AC4.3); the duplicate-tree `plugin.json` is not counted.
- t3 Roster/count-drift check (HARD) over README/CLAUDE.md/docs-map vs `roster.js`. **RED:** a seeded wrong count fails; correct counts pass (post-t1).
- t4 Version-consistency check (HARD, B7 class): `plugin.json` === `marketplace.json` ref === **latest real CHANGELOG version heading (skip `[Unreleased]`)**. **RED:** a seeded version mismatch fails; **the live batched-unreleased state (all 0.1.7, doc-runtime in `[Unreleased]`) passes** — the guard must be green now and post-release.
- t5 `[FILL IN]` check (HARD, narrow allowlist): line-anchored `isStubRetro`, applied only to README + top-level `docs/*.md` minus templates; stub retros/meta-retros/milestone docs excluded. **RED:** a seeded stub in README fails; a stub retro passes. (If a clean allowlist can't be drawn → downgrade to SOFT, log it.)
- t6 AC4.3 meta-test: assert `doc-hygiene.js` source contains no `fetch`/`http`/`https`/`curl` tokens (reuse the `audit-network-calls` shape). Wire hard→fail / soft→warn in `tests/docs-hygiene.test.js`; guard is **inbox-name-agnostic**. **AC4.4 read-only assertion:** hash the doc tree before/after running the guard, assert unchanged (the guard never writes prose). **Index-idempotency (Issue 2):** deliberately covered by S2.t4 (test the generator where it's built), *not* re-asserted in this standing hygiene guard — noted so it's not read as a dropped check.

**Acceptance:** AC4.1–AC4.5. **The whole suite is GREEN on pre-dogfood Signal** (post-reconcile) — including the still-present `FUTURE-IDEAS.md`.

---

## S5 — FR5: append-log eviction (evict-with-anchors)  (Wave 3 · complexity L — the risky piece)
**Goal:** a standalone, end-to-end-testable evict step that relocates closed-milestone `DECISIONS.md` date-sections verbatim to `archive/M{n}/DECISIONS.md`, anchors preserved via S2's D-ID map.
**Depends:** S2 (the D-ID map + `resolveDecisionId`). **Files:** `tools/lib/migrate-memory.js` (new `senseAppendLogEvict` planner + apply step), tests + a fixture `DECISIONS.md`.

**Tasks (TDD):**
- t1 Date-section parser + date-cutoff classifier: parse `## YYYY-MM-DD` sections; boundary = current-milestone-open date (derive from `current_epic`→milestone; `2026-07-15` for M5); evict strictly `<`; undatable → stays live. **RED (fixture DECISIONS.md):** sections before the boundary selected, on/after kept, undatable kept.
- t2 Verbatim relocate to `archive/M{n}/DECISIONS.md` via `relocateFaithful` (BYTE mode) + `conserves(BYTE)`; leave a dated pointer with a byte-stable idempotency marker; **no `verifyCardCoverage` card**. **RED:** evicted block byte-identical in archive; live file has the pointer; re-run = no-op (AC5.5).
- t3 Anchor-resolvability gate (net-new — the "with-anchors"): after evict, assert every evicted `D-…` resolves via S2's `resolveDecisionId` to its archive home; **fail-closed to detect-only if <100%** (R4). **RED:** evicted D-IDs resolve post-regen; a broken map → the evict refuses (detect-only).
- t4 **Standalone end-to-end test (advisor — S5 must not be dead code until W4):** a `runAppendLogEvict(baseDir)`-style entry that parses→cuts→relocates→regens-index→anchor-checks on a fixture repo, independently of `applyMigrate`. **RED:** full path green on the fixture; idempotent re-run.
- t5 Compatibility: confirm `checkpoint.js` dated-today appends (≥ boundary) are never touched; give the section-parser the FULL file read (not the B15-truncated copy). **RED:** a checkpoint-authored current-date section stays live.

**Acceptance:** AC5.1–AC5.5. **End-to-end runnable + tested standalone** (not pending W4 wiring). Faithfulness = BYTE identity + anchor-resolvability (no human-eyeball card gate — verbatim).

---

## S6a — FR6: v3 migrate mechanics  (Wave 4 · complexity L — highest SHIP-blocker risk)
**Goal:** teach `/sig:migrate-memory` the v2→v3 transition and gate the stamp on v3-conformance — the risky compose surface, tested on fixtures, fully reversible.
**Depends:** S1, S4, S5. **Files:** `tools/lib/migrate-memory.js`, `tools/lib/layout-stamp.js`, `tools/lib/archive-tree.js`, `commands/migrate-memory.md`, tests.

**Tasks (TDD):**
- t1 `senseV3Rename(baseDir)` — emit the fixed rename moveMap (`FUTURE-IDEAS.md`→`ISSUES-INBOX.md` + ledger), merged into `archiveSense.moveMap`; **exclude `archive/`** from the rename rewrite+residual-scan (R7). **RED (fixture):** rename move planned; referrers rewritten; archived flat-paths untouched; dangling-gate green.
- t2 Compose FR5's append-log evict + BACKLOG-create + rename into `applyMigrate`'s chain (V1→V3→V2→append-log-evict→BACKLOG→rename→**index-regen**→stamp) under the ONE coarse lock + ONE snapshot (extended to cover DECISIONS.md + each `archive/M{n}/DECISIONS.md` + the inbox) + ONE dangling gate. The FR5 **anchor-resolvability gate runs against the in-evict-step index regen** (per S5.t4), *not* the tail regen — the tail `index-regen` is a final post-rename refresh (Issue 4: the gate must read a fresh map, not the pre-evict one). **RED:** a mid-apply throw rolls back byte-identical (the E2 C1 lesson — rollback wraps the whole mechanical phase).
- t3 **v3-conformance predicate (R2, highest risk — plan-checker Issue 1):** introduce a **NEW** `isV3Conformant(baseDir, stateText)` = (V1/V2/V3 STATE-vectors clean) AND no `FUTURE-IDEAS.md` on disk AND `BACKLOG.md` present AND the FR5 evict-marker present. **Do NOT widen `senseState`'s signature** — it's pure-text (`conformant = vectors.length === 0`, `:1199`) and shared by the fail-open banner hook + `status.js`; adding `baseDir` there widens the blast radius to the banner path. **Relocate the stamp** (currently `applyMigrate:1977`, *before* the mechanical file-moves) to the **tail of the mechanical phase**, gated on `isV3Conformant`. **RED (real filesystem, both directions):** a STATE-vector-clean project that still has `FUTURE-IDEAS.md` on disk → NOT stamped v3, banner stays live; a fully file-migrated project → stamped v3 + silent. (A STATE-text-only predicate passes a text-only test while shipping the bug — the fixture must plant real files.)
- t4 Bump BOTH `CURRENT_LAYOUT_VERSION` + `LAYOUT_VERSION` 2→3 **last** (after t3). **RED:** the mirror-sync assertion tests pass; a v2-stamped project banners (`stamp 2 < 3`).
- t5 Dry-run rendering for the v3 plan (rename + BACKLOG + append-log evict) in `renderDryRun`; `commands/migrate-memory.md` doc update. **RED:** dry-run shows the full v2→v3 plan, applies only on confirm, re-run = no-op.

**Acceptance:** AC6.1, AC6.2 + the FR5 compose. **Fixtures only** (Signal's own repo is NOT migrated here). Rollback proven byte-identical. This slice gets the adversarial 3-specialist REVIEW focus.

---

## S6b — FR6: rollout + dogfood + retire Curator  (Wave 5 · complexity M)
**Goal:** migrate Signal's own `.planning/` to v3 (the faithfulness proof), finish born-on-v3 scaffolding, retire Curator, land the memory/DECISIONS updates.
**Depends:** S6a. **Files:** `commands/new-project.md`, `commands/init.md`, `commands/ship.md`, Signal's `.planning/` (dogfood), the `curator-dormant-on-signal-planning` memory, `DECISIONS.md`.

**Tasks:**
- t1 Born-on-v3 scaffolding: `new-project.md`/`init.md` stamp `docs_layout_version: 3` at init (via `spliceDocsLayoutVersion`); confirm the lazy-create (S1.t5) + create-if-missing (S4.t1) cover the on-demand files. **RED:** a fresh project self-reports v3, banner silent, `/sig:add` creates `ISSUES-INBOX.md`.
- t2 Retire `ship.md §8` Curator block; confirm native FR3 index-regen (add the `regeneratePlanningIndex` SHIP call in the vacated slot) + FR4 hygiene + FR2 sweep replace it; no Signal command invokes `curator`. **RED:** SHIP reconciles INDEX natively; grep confirms no `curator` invocation.
- t3 **Dogfood (AC6.5):** run the v3 migrate on Signal's own `.planning/` — rename `FUTURE-IDEAS.md`→`ISSUES-INBOX.md` (+ ledger), create `BACKLOG.md` + **restructure `BACKLOG-REVIEW-2026-07-04.md` content into it** (the AC2.2 judgment pass) + archive the snapshot with a pointer, append-log evict M1–M4.5 `DECISIONS.md` sections → `archive/M{n}/DECISIONS.md`, stamp v3. Verify 0 content dropped, dangling-gate green, D-IDs resolve, reversible.
- t4 Update the `curator-dormant-on-signal-planning` memory (INDEX now auto-generated) + a `DECISIONS.md` entry recording FR3-lands-reverses-the-stance (per D-M5E3-8).
- t5 MILESTONE-5 § Release-prep: mark the count-reconciliation resolved (in FR4/S3), and the doc-runtime E1+E2+E3 ready for the combined release cut.

**Acceptance:** AC6.3, AC6.4, AC6.5. Signal's own repo is v3, INDEX auto-generated, `DECISIONS.md` shrunk with anchors intact, Curator retired. The combined E1+E2+E3 release is ready to cut.

---

## Test strategy (Nyquist — full mapping in `M5.E3-VALIDATION.md`)
Every AC → a RED-first test. Load-bearing gates proven RED-first: the resolver (both directions), the parse↔render fixpoint, the version-check-skips-`[Unreleased]`, the date-cutoff selector, the anchor-resolvability fail-closed, the v3-conformance-gates-stamp, and the `applyMigrate` rollback (byte-identical). Standalone FR5 e2e test (not W4-wired). Deterministic + offline throughout (AC4.3 meta-test).

## Environment check
Node 22+, no new runtime deps (all reuse `tools/lib`), vitest (existing). FR4 checks are fs-read + string-parse (no native, no network). `roster.js` glob confirmed cross-platform. `npm install` unchanged. No dry-run drift expected.

## Last Updated
2026-07-18 — PLAN written. 7 slices / 5 waves; S6 split (mechanics S6a / dogfood S6b) per advisor; FR5 standalone-e2e + FR4 version-skips-`[Unreleased]` ACs added; "green on Signal at each wave" bar.
