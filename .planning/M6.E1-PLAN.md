# M6.E1 — PLAN

**Phase goal:** make an install carry the plugin (~2 MB) instead of the repository (58 MB), without
breaking existing installs and without pinning users off `main`.

**Tier:** FULL · `plan_validation_dims: all` · `nyquist_enforcement: strict` · `gate_strictness: strict`
**Reads:** `M6.E1-REQUIREMENTS.md`, `M6.E1-RESEARCH.md`, `MILESTONE-6.md`, `B99`, `D-M6E1-1 … 6`.

---

## What this Epic does for the first time — and it is scheduled in wave 1

**Two shipped path expressions have never run at a tree depth other than the one they were written
at.** `tools/lib/plugin-binding.js:56` and `tools/lib/roster.js:22` both derive the **plugin root from
their own module path**, and both are correct today only because the plugin root and the repo root are
the same directory. This Epic makes them different for the first time in Signal's history.

**`plugin-binding.js` is `B52`'s mechanism** — *which copy of Signal is actually running*. Its
failure mode is silent and documented: one earlier sighting *"silently discarded M5.E8's six-phase
ledger."* A move that quietly breaks it produces a Signal that reports the wrong version of itself,
which is the condition under which every other verdict in this Epic becomes untrustworthy.

**Neither expression is new code.** That is the point (`M5.E17` FR1): the risk lives in machinery that
already exists, was built for a purpose, and has never been run under the shape about to be created.

**Therefore S1 is wave 1** — prove the two expressions under the new depth **before a single file
moves**, using a throwaway fixture tree. If the geometry is wrong, it is wrong against a fixture
rather than against 147 relocated files and a rewritten test suite.

**Second first, scheduled in S5 where it belongs:** Signal has never been fetched through a
URL-hosted marketplace, and `git-subdir` has never been used for it. That path is exercised for real
in S5, against the live domain, before the install page tells anyone to use it.

---

## Slices

Each slice is independently shippable and leaves the suite green. **The repo is usable at every
boundary** — no slice ends with a half-moved tree.

### S1 — Prove the geometry (wave 1) · **S**

*Nothing moves. This slice only establishes that the move is safe.*

| Task | What |
|---|---|
| `S1.t1` | Build a fixture tree mirroring the post-move depth (`fixture/plugin/tools/lib/…`) and assert `plugin-binding.js`'s root derivation resolves to `fixture/plugin`, not `fixture`. |
| `S1.t2` | Same for `roster.js`'s `ROOT`; assert `listCommands`/`listAgents`/`listSkills` read from the plugin root. |
| `S1.t3` | Write the **outward-reference detector** (`AC1.2`) and run it against today's tree, where it must find the four `hooks/ → ../tools/lib` imports and classify them **inward-safe** (both endpoints inside the payload). |
| `S1.t4` | **`AC1.2a`:** publish the detector's population — what it scans, what it matches, and the reference forms it provably does **not** reach. A detector that reports "no matches" without stating its reach is `B81`. |

**Exit:** the two expressions are proven at the new depth, and the detector exists and has declared
its own blind spots. **Nothing has moved.**

### S2 — Move the payload · **L**

| Task | What |
|---|---|
| `S2.t1` | `git mv` the six directories + `.claude-plugin/plugin.json` into `plugin/`, in one commit, so the rename is reviewable as a rename. |
| `S2.t2` | Repoint the 136 test files' imports (mechanical, scripted, one commit). |
| `S2.t3` | Repoint the 4 build/config entry points: `build`, `lint`, `validate` scripts and `vitest.config.js` `include`. |
| `S2.t4` | Fix `validate-plugin.js`'s `ROOT` — **the one expression that breaks rather than retargets** (`RESEARCH` §5): the script stays at the repo root while everything it validates moves. |
| `S2.t5` | Run the S1 detector against the moved tree. Zero outward references, or the slice is not done. |

**Exit:** suite green at 2602+, `npm run build/lint/validate` all pass, detector clean.

### S3 — The dependency · **M**

> **AMENDED 2026-08-17 at `S3.t1`.** The decision went the other way from the plan's default, so t2–t4
> changed with it. Original tasks struck through, not deleted. **Note the letter collision the
> decision had to resolve first:** `RESEARCH` §4's **B** is *commit `plugin/node_modules/yaml/`*,
> while `AC3.5`'s vendoring is *`plugin/vendor/` with rewritten imports* — `RESEARCH`'s **C**. This
> row said *"Decide A-vs-B"* and cited `AC3.5` for B's trigger, so it pointed at both. The outcome is
> named rather than lettered from here on.

| Task | What | Status |
|---|---|---|
| `S3.t1` | Decide on the evidence in `RESEARCH` §4 and record it. **The default (A, manifest) was rejected** — `RESEARCH`'s failed-install figure was inverted (17 of 20 commands, not a tail), and correcting it removed A's case. `D-M6E1-7` supersedes `D-M6E1-3`; `FR3` rewritten in `REQUIREMENTS`. | ✅ |
| `S3.t2` | Commit `plugin/node_modules/yaml/` — the published tarball, 233 files, verified byte-identical to the installed copy. **No `plugin/package.json`, no lockfile**, so the install never fires. `.gitignore` exception added. | ✅ |
| `S3.t3` | `AC3.3` — pin the **vendored** copy's version to the version the root lockfile resolves. | ✅ |
| `S3.t4` | `AC3.4` **dissolved** rather than satisfied — nothing installs, so no install can fail. Replaced by `AC3.6` (a test fires the fallback trigger when `yaml` gains a dependency) and `AC3.7` (the copy is faithful and provable). | ✅ |

- ~~`S3.t2` — Add `plugin/package.json` (one dependency, `yaml`) + lockfile.~~
- ~~`S3.t3` — pin the two manifests' `yaml` versions to each other.~~
- ~~`S3.t4` — characterise the failed-install path and make it legible.~~

**Exit (amended):** ~~a fresh cache install carries ≤ 3 MB of `node_modules`~~ — there is no install to
measure. **`plugin/node_modules` contains exactly one package, `yaml`, and no `vitest`/`eslint`/`esbuild`;
the plugin root carries no manifest and no lockfile; the bare specifier resolves to the vendored copy
from both import sites.** All pinned by `tests/plugin-dependency.test.js` (10 tests).

### S4 — The catalogs · **M**

| Task | What | Status |
|---|---|---|
| `S4.t1` | Root `marketplace.json` → `"source": "./plugin"`; `install-contract.test.js`'s assertion moved, **keeping** its string-form and no-sha checks (`B58`). | ✅ |
| `S4.t2` | `docs/map/install/marketplace.json` authored with a `git-subdir` source, no `ref`, no `sha`. Schema taken from a **real** entry in the local plugin-catalog cache, not inferred: `source` is an **object** (`{source, url, path}`), unlike the root catalog's string. | ✅ |
| `S4.t3` | `AC4.3` — the **invariant, not equality**: same catalog `name`, same plugin `name`, neither carrying `ref`/`sha` — plus an assertion that the two sources are **deliberately not byte-equal**, so a later "tidy-up" that copies one over the other fails here instead of in the field. | ✅ |

**Exit:** both catalogs valid; the invariant test fails if anyone later adds a pin. **Met** — 6 new
assertions in `install-contract.test.js`, suite 2626 → 2632, `validate-plugin` clean.

> **`AC4.4` is NOT verified by this slice, and cannot be from a branch.** It requires
> `https://signal.insightriot.com/install/marketplace.json` to return the published catalog and
> `claude plugin marketplace add` against that URL to install a working plugin. The custom domain
> serves the **production** deployment, which tracks `main`, so the file does not exist at that URL
> until this Epic merges. **`AC4.4` belongs to `S5`** (prove the new install path end to end) and is
> recorded as owed here rather than quietly counted as covered by `S4`.

### S5 — Prove the new install path end to end · **M**

| Task | What |
|---|---|
| `S5.t1` | Deploy the published catalog; confirm `signal.insightriot.com/install/marketplace.json` serves it. |
| `S5.t2` | **First execution:** `claude plugin marketplace add <url>` + install, on this machine, into a throwaway cache dir. Measure the delivered tree. |
| `S5.t3` | Verify the delivered tree contains **no** `.planning/`, **no** `tests/`, and that `/sig:status` runs from it. |
| `S5.t4` | `AC5.1` — confirm the **old** path still installs a working plugin. Nobody is stranded. |

**Exit:** measured before/after numbers, from a real install, not a projection.

### S6 — Migration: the detector and the page · **M**

| Task | What |
|---|---|
| `S6.t1` | `/sig:doctor` check (`AC5.2`): detect a `github`-sourced `signal` marketplace, print the one-line switch. Read-only, fail-open — unreadable reports *could not check*, never *clean* (`B39`). |
| `S6.t2` | `AC5.3` — absent-safe both ways: no nag for a user already on the URL, no error with no Signal marketplace at all. |
| `S6.t3` | Install page: existing-user section + swap the primary snippet to the URL form (`AC6.1`, `AC6.3`). |
| `S6.t4` | `AC6.2` — the section must **not** tell users to delete version directories (they self-clear); it **must** cover the marketplace clone, which does not. |

**Exit:** a user on the old path is told, by Signal, at the moment they run `/sig:doctor`.

---

## Dependencies

```
S1 ──▶ S2 ──▶ S3 ──▶ S4 ──▶ S5 ──▶ S6
```

Strictly linear, and deliberately so. S2 is the only large slice and everything downstream depends on
the tree being settled; parallelising S3/S4 against a moving tree would buy hours and cost the ability
to bisect. **S1 gates S2 absolutely** — if the geometry does not hold, the Epic's approach changes
rather than its schedule.

## Complexity

| Slice | Size | Risk |
|---|---|---|
| S1 | S | low — writes tests, moves nothing |
| S2 | **L** | **highest** — 147 files, 136 test rewrites |
| S3 | M | medium — the `AC3.4` failure path is genuinely unpleasant |
| S4 | M | low |
| S5 | M | medium — first contact with an unexercised install path |
| S6 | M | low |

## Environment

Node **22.13.0**; plugin targets Node 22+. No drift, no native modules, **no new dependency
introduced** — `yaml@2.8.3` is already resolved and merely re-declared. `npm ci --ignore-scripts`
needs no compiler toolchain.

## Rollback

Every slice is a revert. The **old install path keeps working throughout** (`AC5.1`), so a stop at any
boundary leaves users unaffected — including a stop after S2, when the repo is reorganised but the
catalogs still point the old way.
