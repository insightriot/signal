# M5.E3 — Plan Validation (8 dimensions) + Nyquist coverage

**Tier:** FULL → `plan_validation_dims: all`, `nyquist_enforcement: strict`. Validates `M5.E3-PLAN.md` (7 slices / 5 waves) before EXECUTE.

## 8-Dimension validation

### 1. Goal alignment — PASS
Phase goal = "complete the self-maintaining memory/doc system for the combined release." Every slice serves it: S1 the capture inbox, S4 the backlog lifecycle, S2 the traversal index, S3 the hygiene guard, S5 the append-log eviction, S6a/b the rollout. No slice is orthogonal to the goal.

### 2. Completeness — PASS
All 6 FRs + 28 ACs mapped to slices: FR1→S1 (+ AC6.3 lazy-create half), FR2→S4 (AC2.2 content-restructure completes in S6b), FR3→S2, FR4→S3, FR5→S5, FR6→S6a+S6b. The two advisor-added requirements (FR5 standalone-e2e; FR4 version-check-skips-`[Unreleased]`) are in S5.t4 + S3.t4. Reconcile-before-guard (the deferred 15→16 count-bump) resolves in S3.t1.

### 3. Dependency correctness — PASS (accurate + minimal)
`S1 → {S2, S4}`; `S2 → {S3 (roster.js), S5 (D-ID map)}`; `{S1, S4, S5} → S6a → S6b`. Verified minimal: S4 depends only on S1 (its BACKLOG create-if-missing is shared-but-independent of FR6, so it does not wait on S6); S3 depends on S2 only for `roster.js` (could be earlier if roster.js were split out, but bundling it in S2 keeps FR3's roster need co-located — acceptable). FR5→FR3 is a genuine hard dependency (anchor resolves only via the index), correctly placing S5 after S2. No false or circular deps.

### 4. Testability — PASS
Every AC has a RED-first test (see Nyquist map). Load-bearing gates are proof-of-fail-first: resolver both-directions, parse↔render fixpoint, version-skips-`[Unreleased]`, date-cutoff, anchor-resolvability fail-closed, v3-conformance-gates-stamp, `applyMigrate` byte-identical rollback. Faithfulness (FR5) is mechanical (BYTE + anchor), so testable without a human-eyeball gate.

### 5. Scope discipline — PASS (explicit non-goals recorded)
Deliberately NOT built (anti-gold-plating): full symbol rename (minimal path/string rename only), `--bug` B-ID allocation / table-row insertion (simple entry), distilled SUMMARY cards for FR5 (verbatim relocate), external-URL link checks (AC4.3 ban), GFM anchor/slug hard-resolution (soft only), full knowledge-graph (deferred with M5.E1), re-deriving curated INDEX notes each run (preserve-by-ID). The FR4 reconcile is scoped to counts/versions/links — not a prose rewrite.

### 6. Context feasibility — PASS with 1 watch-item
S1/S4/S2/S3/S5 each fit one executor context. **Watch: S6a is the densest** — it composes the rename moveMap + FR5 evict + BACKLOG + conformance predicate + version bumps + rollback into `applyMigrate` under one lock. It is cohesive (one file, one compose chain) so it stays single-context, but it is the slice most likely to want a mid-execute sub-split (mirrors M5.E1's S3→S3a/b and M5.E2's S2→S5a/b). Flagged for the executor; not a plan defect.

### 7. Risk coverage — PASS
The 9-risk register (R1–R9) each maps to a slice mitigation: R1 resolver (S1.t1), R2 conformance predicate (S6a.t3), R3 date-cutoff+dry-run (S5.t1), R4 in-transaction index-regen + fail-closed (S5.t3/S6a.t2), R5 reconcile-first (S3.t1), R6 fixpoint test (S2.t4), R7 exclude-archive (S6a.t1), R8 write-first+dedupe (S4.t4), R9 full-read parser (S5.t5). The E2 SHIP-blocker class (rollback around the mechanical phase) is an explicit S6a.t2 AC.

### 8. Vertical slicing — PASS (advisor's key catch addressed)
Each slice ships something usable at its wave, not a horizontal layer:
- S1 → `/sig:add` works with the new names + `--bug` (usable immediately).
- S2 → `/sig:index` regenerates INDEX + the D-ID map (usable).
- S4 → the drain promotes into BACKLOG (usable).
- S3 → the hygiene guard runs green on Signal (usable + gating).
- **S5 → standalone-runnable evict with its own e2e test (S5.t4)** — NOT pure functions waiting for W4 wiring (the horizontal-layer trap the advisor flagged).
- S6a → the migrate dry-runs the full v2→v3 on fixtures. S6b → Signal is migrated + Curator retired.
**The bar:** each slice lands green on Signal's own repo at its wave; S3's guard is inbox-name-agnostic so it passes on pre-dogfood Signal (still `FUTURE-IDEAS.md` until S6b).

**Overall: PASS — cleared for EXECUTE** (pending user approval).

### Independent plan-checker gate (2026-07-18) — WARN, cleared for EXECUTE
A fresh-context plan-checker validated all 8 dims: no FAIL-level defect, every FR/AC covered, no false/circular deps, S5/FR5 confirmed vertical (not dead code). Four call-outs raised — **all folded into `M5.E3-PLAN.md`**:
- **Issue 1 (R2/S6a.t3):** the v3-conformance stamp gate must be a **new `isV3Conformant(baseDir, stateText)`** (filesystem-aware) — *not* an extension of the pure-text `senseState` (shared by the banner path) — with the stamp relocated to the tail of the mechanical phase and a **real-filesystem** RED test both directions. (Prevents the E2-class self-silencing-banner bug.)
- **Issue 2 (S3):** index-idempotency is covered by S2.t4, deliberately not re-asserted in the standing hygiene guard — noted so it's not read as a dropped check.
- **Issue 3 (S3.t2):** the doc-walk ignore-set must exclude `.claude/` (the `worktrees/dogfood-status/` duplicate plugin tree), `node_modules/`, `examples/` — the walk doesn't honor `.gitignore`.
- **Issue 4 (S6a.t2):** the anchor-resolvability gate reads the **in-evict-step** index regen, not the tail refresh.
- **Rec 2 (the bar):** recorded the intended side-effect — Signal's own repo self-banners between W4 (version bump) and W5 (dogfood); it's the canonical un-migrated-v2 project S6b fixes.
All grounding premises verified true (16/26/21 vs 15-in-docs; 45 `DECISIONS.md` date-sections; the reuse spines exist).

---

## Nyquist test-coverage map (AC → test type → slice)

| AC | Requirement | Test type | Slice |
|---|---|---|---|
| AC1.1 | untyped `/sig:add` → `ISSUES-INBOX.md` | unit | S1.t4 |
| AC1.2 | body byte-identical; only title agent-authored | unit | S1.t2 |
| AC1.3 | no-agent-title → deterministic `deriveHeading` fallback | unit | S1.t2 |
| AC1.4 | `--bug` → `BUGS.md`; `--question` unchanged | unit | S1.t3 |
| AC1.5 | pre-existing `FUTURE-IDEAS.md` resolves (back-compat) | unit (resolver) | S1.t1 |
| AC2.1 | `BACKLOG.md` sequenced; roadmap/hygiene = tag | unit | S4.t1 |
| AC2.2 | snapshot migrated in + archived w/ pointer | integration (dogfood) | S6b.t3 |
| AC2.3 | drain classifies feature→BACKLOG, bug→BUGS | integration | S4.t3 |
| AC2.4 | promoted entry evicts + doesn't resurface | integration | S4.t3/t4 |
| AC2.5 | retitle-on-promote | unit | S4.t3 |
| AC2.6 | drain at plan + light ship sweep; not execute | integration | S4.t5 |
| AC3.1 | generator covers every doc + archive | unit | S2.t2 |
| AC3.2 | curated annotation survives regen by ID | unit | S2.t3+t4 |
| AC3.3 | index resolves every live+archived D-ID → home | unit | S2.t5 |
| AC3.4 | regen idempotent (no-op = no diff) + fixpoint | unit | S2.t4 |
| AC4.1 | roster/count drift = hard fail | integration | S3.t3 |
| AC4.2 | dead internal link / stub = fail; prose hint = soft | integration | S3.t2/t5 |
| AC4.3 | no network in hygiene path (meta-test) | unit (meta) | S3.t6 |
| AC4.4 | guard read-only — hash doc-tree before/after, assert unchanged | unit | S3.t6 |
| AC4.5 | map-drift (docs/map counts) folded in | integration | S3.t3 |
| — | version-check skips `[Unreleased]`; green now + post-release | unit | S3.t4 |
| AC5.1 | closed-milestone block → history behind pointer; nothing deleted | integration | S5.t2 |
| AC5.2 | every evicted `D-…` resolves via index | integration | S5.t3 |
| AC5.3 | dangling-gate green; current-milestone stays live | integration | S5.t1/t2 |
| AC5.4 | append (`checkpoint`) still targets live file | unit | S5.t5 |
| AC5.5 | idempotent re-run = no-op | unit | S5.t2 |
| — | FR5 standalone end-to-end runnable (not W4-dead-code) | e2e (fixture) | S5.t4 |
| AC6.1 | v2 project banners; v3 silent | unit | S6a.t3/t4 |
| AC6.2 | dry-run full v2→v3 plan; apply-on-confirm; re-run no-op | integration | S6a.t5 |
| — | `applyMigrate` mid-throw rolls back byte-identical | integration | S6a.t2 |
| AC6.3 | fresh project born-on-v3 (new names, silent) | integration | S6b.t1 |
| AC6.4 | ship.md §8 Curator removed; native regen replaces | integration | S6b.t2 |
| AC6.5 | Signal dogfood: rename+BACKLOG+evict, 0 dropped, reversible | integration (dogfood) | S6b.t3 |

**Coverage verdict:** every AC + the 3 advisor/E2-lesson gates has a mapped RED-first test. No AC is unmapped. Strict Nyquist (proof-of-fail-before-pass) is enforced at VERIFY per PROFILE.

## Last Updated
2026-07-18 — 8-dim PASS (1 watch: S6a density) + full Nyquist map. Independent plan-checker: WARN → 4 call-outs folded into PLAN, cleared for EXECUTE. Pending user approval.
