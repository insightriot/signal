# M5.E5 — Implementation Plan (v0.1.10 carry-over bug squash)

**Tier:** FULL / strict · **Phase:** PLAN → EXECUTE
**Phase goal:** Clear the four M5.E4 carry-overs — B24 (migrate dangling-gate over-eager abort), B26 (retro gate blind on the self-hosted flow), B25 (FR5 read-enclosure has no behavioral test), B6 (stale-nudge over-suppresses phase-artifact commits) — each RED-first, with no production regression, shipping as v0.1.10.

## Architecture decisions (locked in DISCUSS — see `CONTEXT.md` D1–D4)

- **B24:** key `computeDanglingDelta` on the **resolved repo-root-relative absolute target** (the invariant `computeLinkEdits` preserves) with multiset/count semantics — fixed at the shared scanner layer so all migrate paths are repaired once. Not the catalogue's "decision-ID/link-text" (the reroot mutates link text).
- **B26:** add `isEpicCloseByState(state, profile)` (tier-aware, derived from `PHASES` − `phases_skipped`), gate it on **milestone-row-absence**, and apply the combined predicate to **all 3 enforcement layers** (Layer 2 re-keyed off `phase: SHIP` + tier-complete phases instead of the never-written `- SHIP`).
- **B25:** additive `_afterRead` opts-seam (default no-op, mirrors `renameFn`), proven by a deliberately-broken read-outside-lock twin (RED). **Full close — all 6 RMW wrappers.**
- **B6:** new `BOOKKEEPING_PATHS` (`STATE.md` + `CONTEXT.md`) drives Walk 2's exclude; `STATE_AFFECTING_PATHS` untouched.
- **Interface note (api-and-interface-design):** every new surface is *additive/optional* — `_afterRead` defaults undefined, `isEpicCloseByState` is a new helper, `BOOKKEEPING_PATHS` a new const. No existing signature changes except `regenerateIndex(baseDir)` → `(baseDir, opts = {})` (backward-compatible optional param). **Deprecation skill: N/A** — nothing is removed except the inverted B23(a) test assertion.

## Task list — 4 vertical slices, 2 waves

Each task is one bug = one independently-shippable slice. All four are **file-disjoint** (source and tests), so parallel-safe. Waves sequence *risk*, not dependency: the two harder, gate-touching fixes go first (fail-fast), with a checkpoint before the two cleaner ones.

### Wave 1 (parallel) — the gate-touching fixes

#### Task T1 — B24: dangling-delta resolved-target re-key  ·  scope **L**
**Description:** Make `applyMigrate` stop attributing a pre-existing dangle to the migrate when a closed DECISIONS block is relocated + re-rooted; close the dry-run/apply divergence; preserve the gate against genuinely introduced dangles.
**RED-first:**
1. **Invert** `tests/migrate-dangling-baseline.test.js:162-219` — assert apply *succeeds*, block relocates, live DECISIONS shortens (currently asserts abort). → RED against current code.
2. New **non-injected gate-strength** test: a genuinely migrate-introduced dangle (resolved target absent from baseline — e.g. migrate moves a real target out from under a live link) still aborts + rolls back byte-identical.
3. New **dry-run/apply parity** test on the B24 fixture: any link `renderDryRun` calls "pre-existing" does not abort apply.
**Impl:** add a resolved-abs-target field in `scanDanglingLinks` (`migrate-memory.js:~1492`); re-key `computeDanglingDelta` (:1503-1507) on it with count/multiset semantics; update the `computeDanglingDelta` unit test (carry the new field).
**Acceptance:** AC1.1–AC1.6 (`M5.E5-REQUIREMENTS.md`).
**Files:** `tools/lib/migrate-memory.js`, `tests/migrate-dangling-baseline.test.js`, the `computeDanglingDelta` unit test, +1 new test file. `archive-tree.js` read-only (confirm the invariant).
**Dependencies:** None. **Watch (execution-critical):** the delta key MUST be a per-abs-target **count/multiset**, NOT a `Set`. A `Set` silently reintroduces exactly the masking bug AC1.4 exists to catch (one pre-existing + one introduced dangle to the same missing target collapse to one key → the introduced one slips). Largest slice — splittable if it overflows one context.

#### Task T2 — B26: STATE-based Epic-close fallback across 3 layers  ·  scope **M-L**
**Description:** Fire the FR1 retro gate on a completed self-hosted Epic when the milestone table lacks the Epic's row, across all three D-E9-8 enforcement layers.
**RED-first:**
1. New `ship-fr1.test.js` case: `current_epic` set + `phase: SHIP` + `completed_phases` through REVIEW + **no milestone row** + retro missing → `{halt:true, code:'NO_RETRO_FILE'}` (the M5.E4 scenario). → RED.
2. New **no-false-fire** case: a maintained row reading `pending`/`in flight` still `{skipped:true}` even with full `completed_phases`.
3. Layer 3 (`detectDirtyExecute`) + Layer 2 (`checkProposedStateWrite`) tests for the same self-hosted shape.
**Impl:** add `isEpicCloseByState(state, profile)` (tier-aware via `PHASES` − `phases_skipped`, `(date)`-suffix-tolerant phase match); combine as `rowStatus close OR (findEpicStatusRow === null AND isEpicCloseByState)` in `shipFR1Check` + `detectDirtyExecute`; re-key Layer 2 off `phase: SHIP` + tier-complete phases. Re-verify `detectDirtyExecute`'s existing "milestone-says-close but STATE-says-EXECUTE" trigger (:600-603) is undisturbed.
**Acceptance:** AC2.1–AC2.7.
**Files:** `tools/lib/retrospective.js`, `tests/ship-fr1.test.js`, +hook test(s) for Layers 2/3.
**Dependencies:** None. **Watch (execution-critical):** `detectDirtyExecute` has a *second, pre-existing* trigger at `retrospective.js:600-603` — it warns when the milestone table marks the Epic **closed** but STATE is still mid-**EXECUTE** (a "you shipped the retro but there's dirty execute state" signal). AC2.7 = the new STATE-fallback must NOT alter or swallow that existing trigger; read :600-603 before editing the shared predicate. Also: false-positive Epic-close on linear-multi-slice is held off by the row-absence gate (not a pure OR) + the AC2.2 negative test.

### Checkpoint: after Wave 1
- [ ] Full suite green; the two inverted/new gate tests pass; no regression in migrate or ship-fr1 suites.
- [ ] `npm run validate` + `npm run lint` clean.

### Wave 2 (parallel) — the clean fixes

#### Task T3 — B25: FR5 read-enclosure behavioral test + `_afterRead` seam (all 6)  ·  scope **M**
**Description:** Add a test-only `_afterRead` seam to each of the 6 RMW Cores and prove read-enclosure with an interleaving test that fails on a read-outside-lock twin.
**RED-first:** define a broken `reads-outside-lock` twin from exported primitives; the interleaving harness yields `lostUpdate === true` (FAIL) on the twin and `false` (PASS) on the real wrapper. → the twin is the RED discriminator.
**Impl:** `const { _afterRead } = opts; if (_afterRead) await _afterRead();` after the version-establishing read in each Core; bump `regenerateIndex` to `(baseDir, opts = {})`; parametrize the harness via `RMW_PATHS`. Reconcile the `M5.E4-REVIEW.md:29` "7 wrappers" wording.
**Acceptance:** AC3.1–AC3.6.
**Files:** `tools/lib/drain.js`, `tools/lib/checkpoint.js`, `tools/lib/retro-index.js`, `tools/lib/planning-index.js`, `tests/rmw-lock.test.js`.
**Dependencies:** None. **Watch (execution-critical):** inside the harness, writer B is invoked from within writer A's `_afterRead` hook — B's `withStateLock` acquire will **throw** (A holds the lock). That throw MUST be `.catch()`-ed at the call site in `_afterRead`; if it propagates, it unwinds into writer A's own Core and aborts A's write → the test **false-passes** (looks green, proves nothing). A fresh agent will misread this as flakiness — it isn't. Also: seam must be inert in prod (grep `commands/*.md`); no re-entrancy regression for `promoteDrainEntry` (AC3.5, still routes the lock-free Core).

#### Task T4 — B6: isStateStale file-identity tightening  ·  scope **S-M**
**Description:** Treat a committed-but-unrolled phase artifact as work (→ nudge) while still suppressing the genuine STATE-only "+1".
**RED-first:** new `is-state-stale.test.js` case "single `*-PLAN.md`-only commit → `stale:true`" → RED against current code. Plus "two STATE-only commits (split refresh) → `stale:false`" (the case the reviewer's count candidate got wrong).
**Impl:** add `BOOKKEEPING_PATHS` (`STATE.md` + `CONTEXT.md`); build Walk 2's exclude from it, not from `STATE_AFFECTING_PATHS` (left unchanged).
**Acceptance:** AC4.1–AC4.8.
**Files:** `tools/lib/state.js`, `tests/is-state-stale.test.js`.
**Dependencies:** None. **Watch:** do NOT edit `STATE_AFFECTING_PATHS` (invisibility bug); `:136` pathspec test stays green.

### Checkpoint: complete
- [ ] All AC1.*–AC4.* met; full suite green (1529 → +N new tests); validator + lint clean; double-run deterministic.
- [ ] `BUGS.md` updated (B24/B25/B26 → fixed; B24 named limitation recorded; B6 refinement noted); ready for VERIFY.

## Risks and mitigations
| Risk | Impact | Mitigation |
|---|---|---|
| B24 re-key weakens the gate (introduced dangle slips) | High | AC1.3 non-injected gate-strength test + AC1.4 multiset test — both RED-first. |
| B24 abs-target resolution edge cases (relative/posix) | Med | Reuse the resolution logic `computeLinkEdits` already relies on; assert via the inverted fixture. |
| B26 false-positive Epic-close (linear multi-slice) | Med | Row-absence gate (not pure OR) + explicit negative test (AC2.2). |
| B26 shared-predicate change disturbs Layer 3's other trigger | Med | AC2.7 — re-verify `retrospective.js:600-603` before touching. |
| B25 seam leaks into production behavior | Med | Default `undefined`; grep `commands/*.md`; byte-identical assertion (AC3.4). |
| B6 edits the wrong list → phase artifacts go invisible | Med | New `BOOKKEEPING_PATHS`; `STATE_AFFECTING_PATHS` untouched; `:136` test guards it. |
| 4 parallel executors strain the test suite / git | Low | 2 waves of 2 (not one wave of 4); all tasks file-disjoint; atomic commits per task. |

## Environment
Node 22+ assumed (dev machine current). No native deps, no new dependencies, no external services — see `M5.E5-RESEARCH.md` § Environment check. No Slice-1 build budget needed.

## Open questions
None — all four decisions ratified in DISCUSS. Proceed to EXECUTE on approval.

## Last Updated
2026-07-21 (PLAN)
