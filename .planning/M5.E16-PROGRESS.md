# M5.E16 — Execution progress

**Epic:** M5.E16 — STATE-vs-world drift detection · **Tier:** FEATURE
**Plan:** [`M5.E16-PLAN.md`](M5.E16-PLAN.md) · **Target:** v0.1.16

---

## Wave 1 — S1: the oracle harness

- [x] **S1.t1** — check registry; an under-declared check cannot be defined — `tools/lib/state-drift.js`
- [x] **S1.t2** — `cannot-evaluate` branch, proven against the real `affiliate-mojo`
- [x] **S1.t3** — four render states, with checked-clean and could-not-check pinned apart
- [x] **S1.t4** — `tools/state-drift-probe.mjs`, and a test that it actually runs
- [x] **S1.t5** — every declared heal category must have an implementation

**Tests:** 1836 → **1856** (+20). Validator green.

### What wave 1 was for, and what it found

Wave 1 exists because of M5.E17's first-use rule: *name the thing this Epic will run for the first
time, and schedule it first.* Here that was `readProfileFromPath` and the artifact resolvers used as
**drift oracles** over foreign `.planning/` trees — machinery built to *resolve* being asked, for the
first time, whether what a file claims is true.

Running it at PLAN already produced `B59` (Signal's own Epic profile was unloadable, so the Epic
declaring FEATURE ran DISCUSS at FULL) and check **(h)** (not in the requirements; found only by
measuring). Wave 1 finished the job:

**The `cannot-evaluate` branch is not defensive programming — it is the deliverable.** `readState`
**throws** on `affiliate-mojo` (*"has frontmatter but no schema_version key"*). Before this slice,
that was a crash or a silent skip; a silent skip would have reported the project **clean** when
nothing looked at it. `tools/lib/state-drift.js` now reports it, with the reason, under a heading
that says in words *"these checks could not look. This is NOT the same as clean."*

**The probe reproduces the plan's numbers exactly**, so they are re-derivable rather than asserted:

```
readable STATE.md ........ 12/13
canonical phase .......... 7/12
Epic mode ................  4/12
...of those, strict ID ...  2/4
non-empty blockers[] .....  0/12
```

That last row is why check **(f)** was dropped: **no real project has a blocker**, so FR2.1's
"validate against a real non-Signal project" cannot be satisfied for it even in principle.

### Two design points that came out of building it, not planning it

1. **`healMechanism` is required for categories 1 and 2, enforced by the constructor.** Categories 1
   and 2 make a *promise* — "this clears on its own", "Signal fixed it". A promise whose mechanism
   cannot be named is unfalsifiable, and an unfalsifiable reassurance is worse than a finding. This
   is the generalised form of the defect caught in the plan itself, where **(d)** declared category 2
   while nothing healed it.
2. **The category-1 heading is tier-independent by construction.** *"Clears the next time Signal
   writes STATE here"* is true whether the next write is a phase-close `markFresh`
   (FEATURE/SPIKE/FULL) or a manual `/sig:checkpoint` (SKETCH, where four command files skip
   `markFresh`). The obvious wording — *"heals on next phase command"* — would be **false at
   SKETCH**, which is `B42`'s shape: a rule that silently mis-serves one tier.

**The registry ships empty at S1, and that emptiness is asserted, not assumed.** A test pins that the
category-2 bucket holds nothing, so the day a "Signal runs it" check is registered without
`/sig:sweep --heal` existing to run it, the suite fails rather than the user receiving a promise
nothing keeps.

---

## Wave 2 — S2 (checks c, d, g) · S3 (checks h, a, b)

- [x] **S2.t1** — (c) Epic worked, no retrospective
- [x] **S2.t2** — (d) `last_updated_commit` not an ancestor of `HEAD` — category 1, self-heal *tested*
- [x] **S2.t3** — (g) every `PROFILE.md` parses — reports **both** bad fields
- [x] **S3.t1** — (h) `current_epic` set but not a strict Epic ID
- [x] **S3.t2** — (a) phase vs. later-phase artifact — **fixture-only evidence**
- [x] **S3.t3** — (b) body never mentions `current_epic` — pinned to real history

**Tests:** 1856 → **1891** (+35). Validator and lint green.

### The FR2 precision gate, run against all 13 real projects

```
check                       findings  clean   n/a    cannot-eval
baseline-commit-off-history        1      7      4              1
body-omits-current-epic            0      2      8              3
epic-id-not-strict                 2      2      8              1
epic-without-retro                 1     11      0              1
phase-behind-artifacts             0      2      8              3
profile-parses                     0     12      0              1
```

**4 findings, all four true positives, zero false positives.** Each was confirmed
independently before the check existed: `agent-tools-sync` (`current_epic: "M1"`) and
`traction-engine` (`"PHASE12"`) resolve as linear while believing they run Epics;
`cm-mentor-coach`'s baseline commit is reachable from no ref; this repo's `M5.E17` shipped without
a retrospective. **AC2.1 met** — sweep is clean on healthy non-Signal projects.

**The design's central claim, confirmed by the run:** the 2 projects where **(h)** fires are exactly
the 2 showing `cannot-evaluate` for **(a)** and **(b)**. Their blindness is *reported*, not silent —
which is the difference between this and `B39`.

### `profile.js` needed a change to make (g) honest

Check (g) must report **every** bad field, and validation short-circuits — `B59`'s file had two, and
fixing the one the error message named would have left it still unloadable. The validators now
report through a `fail` sink defaulting to throw, so **one implementation** serves both modes and
`readProfileFromPath` throws `issues[0]` by construction. Two implementations of one rule is a defect
this project has shipped before (M5.E13 REVIEW). `ensureObject`/`ensureArray` were orphaned by the
change and removed.

### A defect in this slice's own test, caught before commit

The (b) history regression first returned `null` when `git show` failed, so a shallow clone would
make it **pass by never running** — in the Epic about detectors that look like coverage and are not.
It now **throws**, per `B58`'s principle that a guard must never pass by being unable to run. CI
clones with `fetch-depth: 0` deliberately, so an unreachable commit there is a real regression.
Verified the four commits are reachable and the check's verdicts are red/red/red/green.

## Wave 3 — S4: reachability

- [x] **S4.t1** — findings render in their own sweep group, not folded into structural/advisory
- [x] **S4.t2** — two sweeps over unchanged input are byte-identical (AC1.3); still read-only
- [x] **S4.t3** — `/sig:resume` gains one line, category 3 only (D-M5E16-2)
- [x] **S4.t4** — FR4's four-project baseline re-measured (**AC4.3**)

**Tests:** 1891 → **1904** (+13). `commands/sweep.md` and `commands/resume.md` updated to match
the code.

### S4.t4 — the AC4.3 re-measurement, and why the headline number is misleading

Re-ran the four projects FR4 originally measured (traction-engine, nextpass, conversor,
cm-mentor-coach):

| | Original (2026-08-01) | Now |
|---|---|---|
| Structural findings | **16** | **5** |
| …that should never have reached a person | 15 | — |
| STATE findings that **reach a person** | — | **1** |
| STATE findings that clear themselves | — | 1 |
| Checks that **could not look** | *(not reported)* | **2** |

**The 16 → 5 drop is not this Epic's, and saying otherwise would be the exact claim-integrity
defect the project has been cataloguing.** Eleven of the original sixteen were `/sig:sweep` walking
`.planning/.migrate/snapshot/`, a frozen backup — that is `B57`, found and fixed on 2026-08-01,
**before M5.E16 wrote a line of code**. 16 − 11 = 5, exactly. M5.E16 contributed **zero** to the
reduction.

**What M5.E16 actually contributed is additive:**

- **+1 finding that reaches a person** — `traction-engine`'s `current_epic: "PHASE12"`, a real
  defect that was previously invisible to everything.
- **+1 finding reported as reassurance** rather than as a chore — `cm-mentor-coach`'s off-history
  baseline, which clears itself on the next STATE write.
- **+2 explicit "could not look"** where there was previously silence.

**So by AC4.3's own metric — how many findings reach a person — this Epic made the number go UP,
from 0 STATE findings to 1.** That is recorded because AC4.3 says to record it *"whether it
improved or not"*, and because the honest reading is that the metric is the wrong way round for a
detector: the job was to surface true drift, not to reduce a count. A version of this Epic that
optimised AC4.3's headline would have shipped fewer checks.

### The adherence ceiling fell again, and the reason is the usual one

Documenting the new group in `sweep.md` and the drift line in `resume.md` added **8 directive
lines and 0 named library calls**, so the published coverage share went **21.2% → 20.8%**
(87/411 → 87/419). Caught by `adherence-ceiling.js --check`, which is the guard working. Recorded
in `ADHERENCE-LOG.md` above the table, where the same thing is now documented **four times**: every
time this project makes an instruction clearer, the published number goes down.

## S5 — `INDEX.md` at every phase transition (FR5)

- [x] **S5.t1** — regeneration fires on phase transition (AC5.1)
- [x] **S5.t2** — an unchanged doc set still writes nothing (AC5.2)
- [x] **S5.t3** — no hook gains write capability (AC5.3)

**Tests:** 1904 → **1911** (+7).

**Hooked to `transitionPhase`, not to the command files.** `regeneratePlanningIndex` was called from
`/sig:ship` §8 and `/sig:index` and nowhere else, so the docs map was accurate at exactly the moment
an Epic *finished* and drifted through the whole span of work — the span when someone is actually
re-orienting from it. Putting the call in ten command files would have been **`B60`'s shape**: an
instruction stated in some files and silent in others. In `transitionPhase` it cannot be forgotten.

Three constraints, each load-bearing:
1. **The lock-free Core.** `regeneratePlanningIndex` self-locks on the same `.state.lock`
   `transitionPhase` already holds — calling it would deadlock. Same discipline
   `/sig:migrate-memory` follows with `evictEpicNarrative`.
2. **Dynamic import.** `planning-index.js` imports `withStateLock` from `state.js`; a static import
   back would be a cycle.
3. **Never throws.** The phase record is load-bearing; the docs map is not. A docs problem must not
   become a state-corruption problem — asserted by a test that makes `INDEX.md` a *directory* so
   every write to that path fails, and checks the transition still records.

**AC5.3 passed before it was written**, and it is a lock rather than a fix: FR5 rejected session-start
regeneration because no Signal hook writes anything, and a hook that edits files before the user has
asked for anything crosses a line the design holds deliberately. The test asserts that line — no
write call and no index import in any hook — so it now takes a failing suite to cross it.

**The `B48` guard fired on this slice's own fixture** — the first test project had a PLAN but no
REQUIREMENTS, and `transitionPhase` correctly refused to record DISCUSS as complete. The fixture was
wrong, not the guard.

## S6 — `/sig:update` (FR6)

- [x] **S6.t1** — installed vs. available; structured `plugin.json` preferred, list-parse as fallback
- [x] **S6.t2** — the CHANGELOG entries in between
- [x] **S6.t3** — restart stated on every successful update, wording pinned
- [x] **S6.t4** — roster 18 → 19 across every declaration site
- [x] **S6.t5** — offline: one honest line, no update, no guess

**Tests:** 1911 → **1929** (+18). It did **not** need to split to M5.E18.

**Parsing is the fragile part, and it is treated as fragile.** `claude plugin list` has **no
`--json` flag** — confirmed by running it, not by reading docs — so the command prefers the
structured `plugin.json` and treats the text format as a fallback that may break. When it does not
match, `findPluginVersion` returns `null` and the report says *"could not determine"*. **It never
guesses**, because a plausible wrong version number is exactly what `B58` was for two releases:
*"sig is already at the latest version (0.1.13)"* was truthful and wrong at the same time.

`compareVersions` compares **numerically**. String comparison puts `0.1.9` after `0.1.13`, and
Signal has shipped 0.1.9 through 0.1.16 — the precise range where the naive version is wrong.

**The restart notice is a statement, never an action** (`B52`). Brett asked for the timing to be
his. The constraint is not theoretical: **the session that built this command ran bound to v0.1.13
while the repo was at v0.1.15**, with the v0.1.15 payload already sitting in the cache — two
releases of command text on disk and unreachable for the life of that session.

### The `docs/map` trigger fired, one drain after being re-parked

At this Epic's PLAN drain, the *"`docs/map` refresh Stage 2"* watchlist row was re-parked with a
condition rewritten to be observable: *"a ship changes the roster **and** `docs/map/index.html` is
unchanged in that release"* — noting it would become live because FR6 moves the roster 18 → 19.
It did. `docs/map/index.html` was updated with the count **and** the new command's entry, so the
row stays un-fired for the right reason rather than by never applying.

---

## Roll-up

| | |
|---|---|
| Slices | **6 of 6** — S6 did not need to split |
| Tests | 1836 → **1929** (+93) |
| Checks shipped | 6 — `(a)` `(b)` `(c)` `(d)` `(g)` `(h)` |
| Checks dropped | 2 — `(e)` duplicate, `(f)` unvalidatable |
| Live corpus | 13 real projects · **4 findings, all true positives, 0 false positives** |
| Commands | 18 → **19** |

## VERIFY — PASS (2026-08-02)

17/17 ACs, 4/4 NFRs. Full report: [`M5.E16-VERIFICATION.md`](M5.E16-VERIFICATION.md).

- **NFR3 measured** (the plan's only deferred measurement): **+20.6 ms** for all six checks against a
  **200 ms** budget — ~10% of it.
- **AC1.3 measured on 6 real projects**, not just a fixture — including `affiliate-mojo`, whose
  error path is the one most likely to leak a timestamp into output. All byte-identical.
- **AC5.1 verified by the act of verifying**: entering VERIFY regenerated `INDEX.md` (217 docs).
- **Nyquist `strict` evidence was MEASURED, not attested** — each slice's tests re-run against its
  own parent commit in a detached worktree. That is what surfaced **Finding 1**.

**Finding 1 — 10 of 93 new tests passed against their own pre-implementation baseline.** An
attestation would have said "written first", been true, and hidden this. Classified: 3 legitimate
regression guards, 2 posture locks (already disclosed), and **5 absence assertions that are vacuous
at baseline** — structural, since you cannot write a red-first test for the absence of a thing that
does not exist. Each of the 5 has a positive sibling that *was* measured red. **The honest number is
83 of 93 red-first**, and it is published rather than rounded to 93/93.

**Finding 2 — check `(a)` still has fixture-only evidence.** Zero hits across 13 projects; the live
corpus cannot tell "precise" from "inert". Kept, recorded as a known limit.
