---
epic: M6.E7
tier: FEATURE
phase: EXECUTE
started: 2026-09-05
---

# `M6.E7` — Execution Progress

**Branch:** `feat/m6.e7-roadmap-advisor`. **Attention:** `checkpointed` — waves run without
per-wave confirmation; the confirm lands at the phase boundary.

Waves are the plan's three slices, in the plan's order. **S1 goes first because it is the property
everything else claims** — the citation verifier. S2 and S3 are sequenced behind it rather than
parallelised: they land in one working tree, and interleaved commits across a shared `.planning/`
is the race this repo already has a rule against.

## Wave 1 — S1, the citation verifier (`plugin/tools/lib/citations.js`)

- [ ] t1.1/t1.1b — `extractCitations`, reading only the evidence marker
- [ ] t1.2 — `verifyCitations`: path exists, line in range, range ends ordered
- [ ] t1.3 — root-confinement before any disk touch; truncation reported, not under-reported
- [ ] t1.4 — tests, including the quoted-token-outside-the-marker case

## Wave 2 — S2, corpus reading (`plugin/tools/lib/advise-corpus.js`)

- [ ] t2.1 — backlog rows at `maxDepth: 4`
- [ ] t2.2 — `walkBugEntries` gains `line` (additive; do not fork the walker)
- [ ] t2.3 — retros, honouring `isStub`
- [ ] t2.4 — state/closure via `resolveClosures`; no `StateSchemaError` escapes
- [ ] t2.5 — one milestone-row reader, exported and used here (call sites NOT re-pointed)
- [ ] t2.6 — each source unreadable in turn → its own `cannotCheck`
- [ ] t2.7 — the depth test, both directions

## Wave 3 — S3, the advisor, the artifact, and the gate

- [ ] t3.1 — ranking, `N = 5`, stable line-number tiebreak
- [ ] t3.2/t3.2b — declined section; proposes-never-selects as a rendering contract
- [ ] t3.3 — `renderArtifact`, pure and file-facing
- [ ] t3.4 — producer attribution (`via /sig:advise`)
- [ ] t3.5 — the run-boundary gate, asserting a **count**
- [ ] t3.6 — `writeArtifact`, idempotent by byte-compare
- [ ] t3.7 — artifact name `BACKLOG-REVIEW-YYYY-MM-DD.md`
- [ ] t3.8/t3.8b — the 23rd command's count sites; the command contract
- [ ] t3.9 — the `prose`-independence scan, with a reach declaration
