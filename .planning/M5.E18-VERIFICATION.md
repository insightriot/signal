# M5.E18 — VERIFICATION (pass 2, after REVIEW returned FAIL)

**Epic:** M5.E18 · **Tier:** FULL (inherited; no Epic PROFILE — read, not assumed) · **Date:** 2026-08-04
**Rigor:** `nyquist_enforcement: strict` · `gate_strictness: strict`

**Verdict: PASS-WITH-A-DOCUMENTED-GAP.**

The Epic now does end-to-end what it was opened to do. **One acceptance criterion was found unmet
during this pass and fixed in-loop** (loop 2, option A). The strict-Nyquist gap from pass 1 is
unchanged and remains permanent.

*Supersedes pass 1 (commit `3d79975`), which verified code that REVIEW then changed.*

---

## Why a second pass was owed

REVIEW returned **FAIL** — its in-phase fix was 112 LOC against a ≤ 50 cap. Between pass 1 and now:
`renderArchivePlan` was **deleted**, AC4.2/AC4.4's golden tests moved onto the wired functions, the
mover's default closed-set became a **union**, and a move breakdown was added to the migrate preview.
Pass 1 signed off on code that no longer exists.

## 1. The regression that mattered most — AC6.1

Wave 6 changed which units the mover treats as closed. Pass 1's own report, and a comment I wrote in
`archive-tree.js`, both said doing that "would silently change which **Epics** archive" — which
`AC6.1` forbids.

**Verified against all 12 real projects, not argued: 0 previously-planned moves lost.** The union is
strictly additive — `M5.E17` (retro, no VERIFICATION) keeps archiving, `v0.1.6` (verdict, no retro)
starts. The concern was real and the implementation answers it.

## 2. AC4.5's second half was unmet — found, fixed, re-verified

`AC4.5` requires that a bound be reported by count and reason, **and that its absence be stated**.
The absence statement lived in a `renderDropped` helper that only the deleted standalone report ever
called. Measured on `eval-project-C`:

```
dry-run mentions a dropped/skipped bound at all? NO — AC4.5 second half unreachable
```

**Same class as the two gaps already found in this Epic** — correct library code, no command path.
That is three now, and the pattern has not self-corrected: pass 1 caught one, Brett's question
caught the second, this pass caught the third.

**Fixed** by folding the statement into `explainArchiveOutcome` (the wired function) and deleting
`renderDropped` — which existed only because a test imported it, itself a smell. Scoped so it fires
only when units were derived: a project with nothing to bound gets no line.

```
    2 rename(s), not a unit archive
  ↳ Nothing was dropped: every derived unit was considered.
```

## 3. Reachability — clean

Every new export now reaches a user-facing command: `deriveUnits`, `currentUnit`, `parseVerdict`,
`rankTerminalArtifacts`, `resolveClosures`, `explainArchiveOutcome`, `renderMoveBreakdown`,
`retroStatusFromContent`. `deriveUnitArchiveDir` / `isSafeUnitName` are consumed inside
`archive-tree.js` by `planArchiveMoves`, which is reachable. **No orphans.**

## 4. AC6.3 — idempotency under the new default

The changed closed-set is a live risk to NFR2, so it was re-run end-to-end rather than assumed:

| pass | moves |
|---|---|
| 1 | 3 |
| 2 (after apply) | **0** |

Both shapes archived correctly — `GATE-D` (non-Epic) → `.planning/archive/GATE-D/`, `M9.E1` →
`.planning/archive/M9/…` — and **the retrospective stayed in root**, as the design intends.

## 5. What the Epic now delivers, measured

| | Epic open | now |
|---|---|---|
| files `/sig:migrate-memory` archives | 67 — all of them this repo's own | **114** |
| projects that archive at all | **1 of 12** | **6 of 12** |

`eval-project-C` 0 → 26 · `operator-os-lean-loop` 0 → 6 · `eval-project-D` 0 → 3 · `eval-project-A`
0 → 3 · `consensus` 0 → 2 · `signal` 67 → 74.

## 6. Full suite · build

**129 files · 2167 tests · 0 failures.** (1994 at Epic open.) `npm run lint` clean. Node
**v22.13.0** (≥ 22). No build step beyond lint.

## 7. Nyquist — the gap is unchanged and still permanent

| Slice | RED-first ACs | Status |
|---|---|---|
| S1, S2/S3, S5, S6, S7 | 14 | ✅ documented red-first |
| **S4** | **AC2.2′, AC2.4, AC2.10, AC2.11** | ❌ **permanent** — module preceded its tests |

Unchanged from pass 1: red-first is a claim about ordering and no action now can make the tests have
been written earlier. Mutation testing of four load-bearing clauses stands as a **substitute, not an
equivalent** — it proves the assertions discriminate, not that the criteria predate the answer.

**Wave 6's own accounting, stated rather than rounded:**

- **Union closure — genuine RED-first** (behavioural: `expected [] to include GATE-B-PLAN.md`).
- **Stub veto — mutation-tested.** Removing `&& !stubbed.has(u.unit)` turns the `B64` guard red. It
  passed before the change too (the retro-only rule already excluded stubs), so it is a *survival*
  guard, and mutation is what proves it binds.
- **`renderMoveBreakdown` (3 tests) and the AC4.5 fix (2 tests) are POST-HOC.** Both were written
  after defects found by running the code, not before. They pin real field-found bugs; they are not
  red-first and are not counted as such.

---

## 8. Known limits carried to REVIEW

1. **`senseArchiveTree`'s `closedEpicIds` (retro-derived) and `closedUnits` (what actually drove the
   moves) are two fields with two meanings.** Deliberate — collapsing them would be the drift this
   Epic keeps finding — but a caller reading `closedEpicIds` as "what archived" would now be wrong.
   Documented at the return site.

2. **Three implementations of the path-confinement rule** (`path-confine.js:45`,
   `archive-tree.js:399`, `migrate-memory.js:230`). Pre-existing, all currently agreeing, filed as a
   bug. REVIEW deferred it with reasons; nothing here changes that.

3. **The count line in the migrate dry-run folds renames into "archive-tree moves"** — pre-existing
   behaviour that the new breakdown made *visible* (28 = 26 archives + 2 renames) rather than
   introduced. The breakdown names the renames; the count line still doesn't separate them.

## 9. Exit criteria

- [x] All acceptance criteria verified with evidence — 40/40, after one in-loop fix
- [x] Full test suite passes — 2167/2167
- [ ] **Nyquist compliance passes — 14 of 18 red-first; S4's four are a permanent gap**
- [x] Build succeeds cleanly — lint clean, Node 22.13.0
- [ ] User approves verification results
