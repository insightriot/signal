# M4.5.E2 — `/sig:add` — Research Synthesis

**Phase:** PLAN. **Tier:** FULL (4-agent research, all 8 plan-validation dims, strict Nyquist, strict gate).
**Scope:** `/sig:add` capture-and-route command (M4.5.E2, per `MILESTONE-4.5.md` § Epic 2).
**Spec source:** `FUTURE-IDEAS.md` § "`/sig:add` — capture-and-route new work to the right altitude in `.planning/`" (logged 2026-05-13).
**Research agents:** domain (productivity/UX prior art), codebase (Signal internals), risk (failure modes), prior-art (Claude Code plugin ecosystem + CLI patterns).
**Synthesized:** 2026-05-14.

---

## 1. Locked decisions (research-confirmed; carry into plan)

The 4-agent research produced strong convergence on the load-bearing design. The following are locked — the plan will encode them without re-litigation:

| # | Decision | Source agent(s) | Rationale |
|---|---|---|---|
| 1 | **Default routing destination: `FUTURE-IDEAS.md`** | Domain + Prior-art + Risk | GTD canon; Things 3 / Obsidian Inbox / Reflect / GSD `--note` all converge. Safe asymmetry: misrouting *into* FUTURE-IDEAS is recoverable via `/sig:plan` promotion; misrouting *out of* it (e.g., into wrong milestone) costs more. |
| 2 | **Hot path = 0 questions** (revises original 1-3 spec) | Prior-art (GSD `--note`, Things 3, Bear) | `/sig:add "idea text"` writes immediately to FUTURE-IDEAS.md and prints one-line confirm. Original "1-3 routing questions" applies only to the cold path (no args, or `--new-milestone` without title). |
| 3 | **Flag-based force-routing for power path** | Prior-art (GSD `capture --note/--backlog/--seed`) | `--question` → OPEN-QUESTIONS.md; `--milestone` → current; `--milestone N` → specific; `--new-milestone "title"` → scaffold. Zero questions when flagged. |
| 4 | **Naked invocation `/sig:add` (no args) → single question** | Prior-art (GSD `plant-seed.md`) | "What's the idea?" then default-route. Mirrors `plant-seed.md` "only prompt for the idea when `$ARGUMENTS` is empty." |
| 5 | **Verbatim capture — non-negotiable** | Prior-art (GSD `note.md`) | "Never modify the note text — capture verbatim, including typos." Resist any LLM rewrite. The user's exact phrasing is the signal. |
| 6 | **No "are you sure?" preview before write** | Prior-art (Linear "C" lesson; GSD pattern) | Capture latency dies on every confirmation step. Diff-style one-liner *after* write is sufficient. |
| 7 | **Atomic write discipline** | Risk (#1 of top-5) | Read-build-write-temp-rename via `fs.rename` (POSIX atomic). NEVER `fs.appendFile` directly. Markdown files are the entire product database. |
| 8 | **Lock file `.planning/.add.lock` with 30s TTL** | Risk | Concurrent runs unlikely (solo dev) but corruption mode is silent. Released on `process.on('exit')`. |
| 9 | **Per-destination insertion templates** | Risk (top-5 #1) | Every target file has a non-trivial tail (`---`, dated footer, both). Per-file template + fixture-tested for each of 5 destinations. Naive append shatters silently. |
| 10 | **Footer-aware insertion + rewrite** | Risk + Codebase | `FUTURE-IDEAS.md` has `*Last updated: YYYY-MM-DD*` footer; `MILESTONE-4.5.md` has `*Created ...*` footer; `OPEN-QUESTIONS.md` and `MILESTONE-*.md` end with `---`. Insert *before* footer, rewrite footer's date. |
| 11 | **NOT tier-gated** | Risk + Codebase | Capture is orthogonal to rigor flow. SKETCH user still needs to log ideas; tier-block would silently lose work. Precedent: `/sig:status`, `/sig:resume`, `/sig:escalate` (meta-commands, no PROFILE.md preamble). |
| 12 | **Honor `gate_strictness`** | Risk | `strict` → confirm destination + show entry preview; `light` → confirm destination only; `off` → still confirm destination (capture is cheap; never auto-accept). |
| 13 | **Never write to `DECISIONS.md` or `STATE.md`** | Risk + FUTURE-IDEAS spec | Hard refuse — DECISIONS is post-deliberation, STATE is regenerated. Capture is upstream of both. No override flag exists. |
| 14 | **Sensitive-data regex pre-scrub** | Risk (top-5 #5) | Detect AWS `AKIA…`, GitHub `ghp_…`, generic `Bearer …`, 40-char hex. User-facing prompt on hit, never auto-redact silently. `.planning/` is git-tracked by convention — public leak is irreversible. |
| 15 | **First-run onboarding warning + `.planning/.add-onboarded` flag** | Risk + Domain | One-time: "`.planning/` is typically committed. Treat entries as public-ish." Persist flag to suppress on subsequent runs. |
| 16 | **Plain-English routing questions; vocabulary appears only on confirmation** | Domain (top finding #5) | "Actionable now or future-state?" not "Is this a Task or an Epic?" Neither Jira nor Linear teaches vocabulary at capture; teach on confirmation: "Added to FUTURE-IDEAS.md — that's where 'someday' ideas land before being promoted." |
| 17 | **Use `3+other` pattern, NOT `strict-enum`, for destination disambiguation** | Domain + Codebase | 5 destinations flat is the vocabulary-wall `/sig:add` exists to eliminate. Build the question dynamically: after first answer narrows to ≤3 options, surface those with "other" as third leg. |
| 18 | **One destination per `/sig:add` call** | Risk + FUTURE-IDEAS spec | No multi-write. If both OPEN-QUESTIONS and FUTURE-IDEAS need entries, that's 2 captures. |
| 19 | **New helper: `tools/lib/milestones.js`** | Codebase | No milestone-discovery helper exists today. New module: `listMilestones(baseDir)` (glob `.planning/MILESTONE-*.md`, numeric sort incl. decimals like 4.5) + `currentMilestone(baseDir)` (reads STATE.md as source of truth, not file scan). |
| 20 | **STATE.md is source of truth for "current milestone"** | Risk + Codebase | Not file scan, not "highest-numbered open." If STATE has no active milestone, the "current milestone" route is removed from the menu entirely. |
| 21 | **Brownfield-without-`.planning/` → fail-loud → `/sig:init`** | Risk + Domain | Don't scaffold standalone. Capture before calibration breaks PROFILE.md gating contract. Bounds Epic scope cleanly. |
| 22 | **No new ESM dependencies** | Codebase | `yaml` is the only non-node dep in `tools/lib/`. `/sig:add` stays zero-new-dep — uses `node:fs/promises`, `node:path`, `new Date().toISOString().split('T')[0]`. |
| 23 | **No partial-draft persistence on Ctrl+C** | Risk | Complexity not worth it for v1; user re-runs. Document. Lock file released on `process.on('exit')`. |
| 24 | **No numeric entry IDs (FUTURE-001, IDEA-042, etc.)** | Prior-art (GSD seeds counter-example) | FUTURE-IDEAS entries are prose; numbering invites ID-management bookkeeping and "is this gap a deletion?" anxiety. Date + slug heading is enough. |
| 25 | **Single-key override for heuristic routing hints** | Prior-art | If input ends in `?` → suggest OPEN-QUESTIONS *but* show "press `q` for OPEN-QUESTIONS, Enter to default FUTURE-IDEAS". One keypress, not a full prompt. |

---

## 2. Domain research — productivity/UX prior art

**Canonical pattern:** capture-to-inbox first, route later. Validated by Things 3 Quick Entry, Obsidian Inbox plugin (tmfelwu, Zachatoo variants), Reflect, Notion Quick Capture, GTD literature (David Allen, facilethings.com, super-productivity.com).

**Key load-bearing findings:**
- **GTD principle: mixing capture with routing is the textbook anti-pattern.** Every routing question violates it. Signal's design (FUTURE-IDEAS default + planning-phase promotion) is the canonical answer; locked in research.
- **AskUserQuestion adaptive shape** (Jeremy Watt, Claude Code docs): if a question's answer is inferable, skip it. Don't ask "Is this actionable now?" if input contains "TODO" or "fix bug" — infer.
- **POSIX `O_APPEND` makes byte-races moot** for single-user CLI (nullprogram.com source). Focus instead on positional failures (YAML, code fences) — atomic-write discipline still required for crash-recovery.
- **Vocabulary teaching is not done at capture time** in any mainstream productivity tool. Linear/Jira teach by post-capture review (Triage, Inbox processing). Signal must absorb the vocabulary burden via plain-English routing questions, surface vocabulary only on confirmation.
- **Missing back-half (cross-Epic insight):** `/sig:plan` must explicitly reference reviewing FUTURE-IDEAS.md and promoting/culling entries. Without this loop being documented, FUTURE-IDEAS.md becomes a write-only graveyard. Linear's daily Triage ritual is the model. **This adds a sub-task to M4.5.E2's exit criteria** — see § 6 below.

---

## 3. Codebase research — Signal internals

### Command file template

`/sig:add` is a **meta command** (same camp as `/sig:status`, `/sig:resume`, `/sig:escalate`, `/sig:calibrate`). No tier-gating preamble, no skill-loading block, no agent spawning. Per FUTURE-IDEAS spec line 481 ("Probably not phase-gated").

Live at `/Users/bvantil/dev-biz/signal/commands/add.md` (flat, post-M4.t19 rename). Frontmatter shape (modeled on `commands/resume.md`):

```yaml
---
name: sig:add
description: "Capture-and-route new work — appends a stamped entry to FUTURE-IDEAS / OPEN-QUESTIONS / a MILESTONE-*.md after a 0-1 question routing interview. Not phase-gated."
args: "[\"idea text\"]"
---
```

Sections: role paragraph → authoritative references → Workflow (numbered steps) → Anti-Rationalization table (lift dragons from FUTURE-IDEAS.md:524-530 verbatim) → `## Gate:` checklist.

### Validator update surface (every file that mentions commands)

1. `tools/validate-plugin.js:27-40` — append `'commands/add.md'` to `REQUIRED_COMMANDS`.
2. `.claude-plugin/plugin.json` — **no edit** (commands auto-discovered; validator line 100-103 documents this).
3. `.claude-plugin/marketplace.json` — review `plugins[0].description` (lists phases, not command names; likely no edit).
4. `README.md:90-99` workflow table + `:134-145` per-command bullet list — both need `/sig:add` row.
5. `docs/map/index.html` — **optional skip** (tier/calibration decision-tree viewer; `/sig:add` is not in the phase flow).
6. `CLAUDE.md:78` — bump "9 slash commands" count to current (after adding `/sig:add` we'll be at 13).
7. `references/question-patterns.md:560-570` — add `/sig:add` to "Used in" notes for the `3+other` pattern.

### Reusable helpers in `tools/lib/`

| Helper | Module | Use in `/sig:add` |
|---|---|---|
| `readProfile(baseDir)` | `profile.js` | Optional — read PROFILE.md gracefully (capture should work pre-calibration; only used to honor `gate_strictness`). |
| `readState(baseDir)` | `state.js` | Source of truth for "current milestone" + "is user mid-phase" (for trigger-context). |
| `readOpenQuestions(baseDir)` | `status.js` | Pattern for "read-if-exists, return null else." |
| `extractSection(content, heading)` | `landscape.js` | Pull `## H2` block by name from milestone files (e.g., "Epics" section detection). |
| `extractField(content, label)` | `landscape.js` | Parse `**Label:** value` lines. |
| `appendNote(content, note)` | `walkthrough.js` | **Closest pattern, not directly reusable.** Targets `## Notes` section; `/sig:add` needs end-of-file pattern with footer-rewrite. Mirror its safety discipline (trailing-blank collapse, idempotency). |

**No date/timestamp helper exists.** Inline `new Date().toISOString().split('T')[0]` matches existing pattern (state.js:28, state.js:93).

### Test patterns

- **Framework:** `vitest`. `describe / it / expect / beforeEach / afterEach`.
- **Temp-dir pattern** for write-tests: `mkdtemp(join(tmpdir(), 'signal-test-'))` → `rm({recursive: true})`. Canonical in `tests/state.test.js:9-17`.
- **Fixture-tree pattern** for read/input-driven: real files under `tests/fixtures/init/{slug}/`. Canonical in `tests/init-fixtures.test.js:11`.
- **No fs mocking** — always real temp dirs or real fixtures.
- **Assertions:** `.toBe / .toEqual / .toMatch / .toMatchInlineSnapshot`. Inline snapshots preferred.

For `/sig:add`: hybrid pattern — temp-dir writes (state.test.js shape) + fixture trees for each of 5 destinations (`tests/fixtures/add/{slug}/`).

### AskUserQuestion idioms

- **`calibrate.md:30-66`** — Scenario A: 3 branched-on options + recommendation derived from project state.
- **`init.md:88-126`** — partial-`.planning/` ambiguity question; "destructive action requires confirmation" pattern (relevant for new-MILESTONE scaffold path).
- **`discuss.md:71-84`** — adaptive-flow idiom. Key rule (line 72): *"One `AskUserQuestion` call per gray area — never bundle multiple gray areas into a single response."*
- **`init.md:370-442`** — walks markers one at a time, choosing different question pattern per marker type. Closest precedent for `/sig:add`'s adaptive shape (where each answer determines the *shape* of the next question).

### Codebase anti-patterns / dragons

1. Don't bundle gray-areas into a markdown wall (`question-patterns.md:183`).
2. Don't accept free-text where enum is required (`calibrate.md:106` — treat "Other" as request to restate).
3. Don't auto-create or auto-route without confirmation (`calibrate.md:63`, `init.md:122`).
4. Don't skip `.gitignore` check (every write-to-`.planning/` command runs it).
5. Don't write to DECISIONS.md or STATE.md (FUTURE-IDEAS.md:511 locks).
6. Don't write to multiple destinations in one call (FUTURE-IDEAS.md:526).
7. Don't add `--quick` skip-interview flag in v1 (FUTURE-IDEAS.md:527).
8. Don't auto-trigger from `/sig:discuss` (FUTURE-IDEAS.md:529).
9. Don't introduce new ESM deps (zero-new-dep constraint).

---

## 4. Risk research — top-5 design-against list

Full risk table in agent output; top-5 ranked here for plan prioritization:

| Rank | Risk | Mitigation (lands in plan) |
|---|---|---|
| 1 | **Footer/format corruption on append.** Every target has non-trivial tail (`---`, dated footer). Naive append shatters silently. | Per-destination insertion template; fixture test per of 5 files; footer rewrite on every write. |
| 2 | **Vocabulary drift poisoning corpus.** `/sig:add` becomes the dominant writer; if templates encode deprecated terms ("Tranche"), spreads at scale. | Single source of truth (`references/vocabulary.md` or M4.t18 lock in PROJECT.md § Vocabulary); validator extended to flag banned terms in command files; test asserting no deprecated vocab in command file. |
| 3 | **Misrouting + invisible recovery for newcomers.** Stranger captures real-now task → lands in FUTURE-IDEAS → assumes queued → weeks pass → forgotten. | Success line must always print path + line number; routing interview plain-English (no vocab); planning-phase promotion documented as recovery mechanism (not buried). |
| 4 | **Atomic-write discipline.** Single truncated write during kernel-update/Ctrl+C corrupts asset that took weeks to compose. | Write-to-temp + `fs.rename` (POSIX atomic); lock file with TTL; no `appendFile` shortcut. Non-negotiable infrastructure. |
| 5 | **Sensitive-data leak via committed `.planning/`.** Stranger pastes stack trace with tokens, ships to public repo on next commit. Irreversible. | Regex pre-scrub (AWS `AKIA…`, GitHub `ghp_…`, Bearer, 40-char hex); user-facing prompt on hit; first-run onboarding warning + `.planning/.add-onboarded` flag. |

---

## 5. Prior-art comparison — capture-and-route ecosystem

| Reference | Q's before save | Routing model | Adopt | Reject |
|---|---|---|---|---|
| **GSD `gsd:capture --note`** | **0** | Subcommand flags; default = structured todo | **Zero-Q verbatim hot path**, frontmatter w/ `promoted: false`, flag pattern for force-route | File-per-note (overkill) |
| **GSD `--seed`** | 0-1 | Default + optional `--enrich SEED-N` second pass | "One-shot now, enrich later" philosophy; separate enrichment | Numeric SEED IDs |
| **GSD `--backlog`** | 0 | Force-route into ROADMAP.md `## Backlog` w/ 999.x | **Append-to-section in single file** (matches Signal) | 999.x sentinel numbering |
| **GSD default `capture` → add-todo** | Many (criteria, priority, theme) | Force-routing | — | **High-friction default = the trap to avoid** |
| **`gh issue create --interactive`** | 2 (title, body) | Force at capture | Title + body 2-field shape (too heavy alone) | "Fill all fields up front" |
| **`gh issue create --title ... --body ...`** | 0 | Args-as-routing | **Fully-arg'd path** = no prompts when user knows | — |
| **`npm init -y`** | 0 with -y; 8+ without | Defaults-with-overrides | `-y` accept-defaults flag for power users | Question-by-question chain |
| **`cookiecutter`** | Many | Force-routing, linear | — | **Linear unbranching interview** = anti-pattern |
| **Things 3 Quick Entry** | 0 (Inbox) | Default-to-inbox + inline tag overrides | **Default-to-inbox + inline routing syntax** | — |
| **Bear "+"** | 0 | Default-to-inbox + `#tag` inline | **Inline flag routing for power users** | — |
| **Linear "C" quick-add** | 1 (title) | Default + force at modal | — | Team selection friction (killed adoption v1) |
| **Reflect / Obsidian Daily Inbox** | 0 | Single-file append | **Single-file-append model = Signal's FUTURE-IDEAS** | — |
| **GTD canon (Allen)** | 0 | Two-step: collect → process batch | **The doctrine**: `/sig:add` = collect; `/sig:plan` = process | — |
| **Notion Quick Capture** | 0-2 (DB picker) | Hybrid default | — | DB picker friction when dumping |

### Anti-patterns from prior art (lock in command's anti-rationalization table)

1. **Don't ask for priority/criteria/category at capture.** GSD's `add-todo` is the heaviest path; it turns a 2-second thought into 30-second form. Inbox-style structuring happens at `/sig:plan` time.
2. **Don't preview before write.** Cookiecutter/`gh --web` do this for irreversible scaffolds; capture-append is not in that class. Show diff *after* — print path + line.
3. **Don't auto-classify with LLM call on default path.** Notion's AI auto-categorize is famously distrusted. Use heuristic hints (e.g., `?` → suggest OPEN-QUESTIONS) with single-key override, not a model call.
4. **Don't force routing at capture for the unflagged case.** Linear "C" learned this; force-routing kills adoption.
5. **Don't number entries** (FUTURE-001, IDEA-042). GSD seeds do this; appropriate for seeds (referenced by ID with trigger conditions). FUTURE-IDEAS entries are prose — numbering invites bookkeeping.

---

## 6. Open sub-questions for plan to resolve

Three sub-questions surfaced during research that the plan must decide:

### Q1. New-milestone scaffold path — keep or defer to v0.2?

FUTURE-IDEAS routing table assigns ~2% to "new MILESTONE-N.md scaffold." It's the highest-blast-radius destination (creates new top-level planning file). Two options:
- **(a) Keep in v1**, gated behind two-stage confirmation (filename + skeleton preview → explicit "yes scaffold"). Refuse if N collides; refuse decimal numbers without explicit flag (anchors precedent that 4.5 was a one-off).
- **(b) Defer to v0.2**, route the 2% case to FUTURE-IDEAS.md with a note, let user hand-author milestone files (M4 + M5 + M4.5 all hand-authored — no breaking precedent).

**Recommendation: (b) — defer.** Hand-authoring milestone files is the existing precedent; capturing "new milestone idea" to FUTURE-IDEAS preserves the safe-asymmetry principle. Saves Epic scope.

### Q2. Cross-Epic missing back-half — does `/sig:plan` get a "review FUTURE-IDEAS.md" update in this Epic?

Domain agent flagged that without an explicit promotion loop, FUTURE-IDEAS.md becomes a write-only graveyard. Two options:
- **(a) In-scope:** add a sub-task to M4.5.E2 — update `commands/plan.md` to include a "review FUTURE-IDEAS.md and surface candidates for promotion" step.
- **(b) Out-of-scope:** log as a separate Epic (`M4.5.E2.1` or new entry).

**Recommendation: (a) in-scope, but as a small sub-task.** ~20 line edit to `commands/plan.md`. Without it, `/sig:add` ships into a system that doesn't read from where it writes. Atomic capture-AND-process is the GTD principle; ship both halves together.

### Q3. Heuristic routing hints — which patterns trip a single-key override?

Domain + prior-art both endorse heuristic suggestion with single-key override. Initial heuristic set proposed:
- Input ends with `?` → suggest OPEN-QUESTIONS (press `q` to confirm, Enter to default FUTURE-IDEAS)
- Input matches `/^(fix|bug|TODO|broken)\b/i` → suggest current MILESTONE (if STATE.md has one; press `m`)
- Otherwise → silent default to FUTURE-IDEAS

**Open:** should the heuristic set be hardcoded in v1 (recommended) or extensible via `.planning/.add-rules.json`? Plan recommendation: hardcoded; user-extensible later if real demand surfaces.

---

## 7. Environment / runtime assumptions (for environment-check step in plan)

- **Runtime:** Node 22+ (per `package.json` engines). `fs.rename` POSIX-atomic on Mac/Linux; Windows note — semantics differ but Signal targets Mac primary, Linux secondary, Windows-WSL only.
- **Native modules:** None. `/sig:add` is pure JS (`node:fs/promises`, `node:path`, `Date`).
- **External services:** None.
- **Existing deps:** `yaml` is the only non-node `tools/lib/` dep — `/sig:add` doesn't need it (no YAML frontmatter handling; markdown-only).
- **`npm install --dry-run` check:** required as part of plan's environment-check step.
- **Cross-FS rename caveat:** if `.planning/` is symlinked across filesystems, `fs.rename` returns `EXDEV`. Fall back to copy-then-unlink with documented caveat.

---

## 8. File-path index (for planner)

**Spec sources (read before drafting plan):**
- `.planning/FUTURE-IDEAS.md:471-532` — `/sig:add` design spec (locked routing table + anti-rationalizations)
- `.planning/MILESTONE-4.5.md:37-46` — M4.5.E2 Epic scope

**Codebase templates to model:**
- `commands/resume.md` — closest meta-command shape
- `commands/status.md` — next-closest; meta-command + adaptive output
- `commands/calibrate.md:30-66` — adaptive-recommendation question shape
- `commands/init.md:88-126`, `:370-442` — 3+other + adaptive walkthrough
- `commands/discuss.md:71-84` — one-question-per-call idiom

**Authoritative references:**
- `references/question-patterns.md` — `3+other` / `strict-enum` / `open-ended` framework
- `references/anti-rationalization.md` — anti-rationalization patterns

**Validator / manifest update targets:**
- `tools/validate-plugin.js:27-40` — `REQUIRED_COMMANDS`
- `README.md:90-99`, `:134-145` — workflow table + per-command list
- `CLAUDE.md:78` — command count

**Helpers to reuse:**
- `tools/lib/profile.js` (readProfile)
- `tools/lib/state.js` (readState)
- `tools/lib/status.js` (readOpenQuestions, read-if-exists pattern)
- `tools/lib/landscape.js` (extractSection, extractField)
- `tools/lib/walkthrough.js` (appendNote — pattern, not reuse)

**Helpers to build:**
- `tools/lib/milestones.js` — NEW. `listMilestones(baseDir)`, `currentMilestone(baseDir)`.
- `tools/lib/add.js` — NEW. Core capture-and-route logic: routing decision, sensitive-data scrub, atomic write, per-destination templates, footer rewrite.

**Test patterns:**
- `tests/state.test.js:9-17` — temp-dir write-test pattern
- `tests/init-fixtures.test.js:11` — fixture-tree pattern
- `tests/fixtures/init/` — fixture directory layout precedent

---

## 9. Token budget note

Research surface: 4 agents × ~30-90K tokens each. Synthesis here ~450 lines. Plan draft (next) and validation+nyquist (after) will run ~500-700 lines combined. Within Opus 1M context budget comfortably.

---

*Synthesized 2026-05-14 from 4 parallel research agents (domain, codebase, risk, prior-art). All claims traced to the source agent's findings; no novel design decisions introduced in synthesis. Plan draft is the next artifact.*

---

# 2026-05-30 PLAN Research Addendum — Slices 2–5 re-plan

Re-ran 4 parallel research agents (codebase substrate, plan/validator/CHANGELOG, test patterns, risk+Q-resolution) for the S2–S5 re-plan against `M4.5.E2-REQUIREMENTS.md` (2026-05-30 DISCUSS), plus direct lock-contract verification and a live env check. The 2026-05-14 synthesis above still holds for substrate-level decisions.

## 1. Substrate is reusable as-is (one false alarm corrected)

- `tools/lib/atomic-write.js` → `atomicWrite(targetPath, content, {renameFn?})` — temp-in-dir → `rename`, `EXDEV` copy+unlink fallback. Path-agnostic; reuse verbatim for every destination.
- `tools/lib/file-lock.js` → `acquireLock(lockPath, {ttlMs, label})` **returns `{path, released}`** (a handle); `releaseLock(lockPath)` standalone, idempotent. `add.js#acquireLock(baseDir)` wraps it (30s TTL) and `captureToFutureIdeas` correctly calls `lock.released()`. **Verified against `file-lock.js:70-73` — the "lock-wrapper bug" an early pass flagged was a misread. No bug.**
- `add.js` builders (`buildFutureIdeasEntry`, `insertAboveFooter`, `rewriteFooter`) are FUTURE-IDEAS-specific and key on a `*Last updated:*` footer. The reusable spine is: parse → scrub → length → lock → read → (build+insert per destination) → `atomicWrite` → release. **Generalize** to `captureToDestination(baseDir, {relPath, buildEntry, insert, …})` so scrub+length+lock always run for every destination (closes R7); `captureToFutureIdeas` delegates (keeps S1's 40 tests green).

## 2. Destination file shapes (empirical) — three different anchors

- **`OPEN-QUESTIONS.md`** — flat `## ` blocks separated by `---`; **no `*Last updated:*` footer**; ends on a trailing `---`. Sub-fields (Status/Watch-for/Resolve-by) observed but not enforced. → FR1 helper appends `## {title}` + `**Status:** Logged {date} via /sig:add.` + body + `---` at EOF. Do **not** reuse `insertAboveFooter` (no footer to anchor on).
- **`MILESTONE-4.5.md`** — 333 lines; ends with a `---` (L331) then a `*Created …*` italic footer (L333) — a *different* footer shape `rewriteFooter`'s `*Last updated:*` regex won't match. No `## Captured via /sig:add` section exists. → FR2 helper find-or-creates that section after `## Notes`, above the L331 `---`/footer; append entries under it. Bespoke insertion.
- **`CHANGELOG.md`** — `## [0.1.3] — Unreleased` block (bundles E7+E3+E9+E8) with multiple `### Added — … (M4.5.E{n})` subsections. → each slice appends a `### {Added|Changed} — … (M4.5.E2.S{n})` bullet; the L9 heading's Epic list gains `+ M4.5.E2`; no new version header, no per-slice tag (Decision 8).
- **No milestone-resolution helper exists.** `state.js#readState` returns `current_epic`; there is **no** epic→milestone mapper and **no** `tools/lib/milestones.js`. → S2 builds it: `currentMilestone(baseDir)` parses `current_epic` via `/^M(\d+(?:\.\d+)?)\.E\d+/` (delimiter is `.E`, **not** the first dot — `M4.5.E2` → milestone `4.5`) → `MILESTONE-{ms}.md`; null `current_epic` → null. `listMilestones(baseDir)` globs + decimal-sorts.

## 3. Test approach (confirmed reuse path)

- Keep `AskUserQuestion` in the `.md` command layer; `tools/lib` functions take **injected async prompt callbacks** (`async () => 'keep'|'abort'|'confirm'`) — S1's exact pattern (`tests/add.test.js`, 40 tests, no `vi.mock`). Abort assertion = read-before === read-after.
- Temp project via `mkdtemp`/`rm` (before/afterEach); fixtures copied via a file-local `setupFixture`. Assertions are `toContain`/`not.toContain`/regex/`indexOf` ordering — **no snapshots** in add.test.js today (S5's byte-identical-neighbor proof will be the first to need a full-file equality assertion).
- STATE/PROFILE fixtures: ready-made `tests/fixtures/state/schema-v1/.planning/STATE.md` carries `current_epic: M4.5.E6`; `readState().current_epic` is the field. PROFILE inline-frontmatter blocks with `rigor_overrides.gate_strictness` appear 3× in `status.test.js` — reuse for S4. Epic-ID parse precedent: `checkpoint.test.js:101-104`.
- Vocab lint (FR6.4): `tests/helpers/template-lint.js#findJargonHits` already exists (E3) and is the precedent (`cross-file-consistency.test.js:80-88`). **There is no `tests/validate-plugin.test.js`** and `validate-plugin.js` `process.exit`s, so test it via a subprocess (`spawnSync`, assert exit code) like `audit-network-calls.test.js`, OR add the lint as a `cross-file-consistency`-style vitest using `findJargonHits`. PLAN picks: extend `validate-plugin.js` with `checkBannedVocabulary()` (requirement-literal) **and** unit-test `findJargonHits` with a planted `tranche`.
- **plan.md step insert (S5):** to avoid renumbering Steps 4–6 (the rigor-overrides table at `plan.md:19-31` references "Step 4/5/2" by number), insert the drain as **`### 1b.`** between Step 1 and Step 2 rather than before Step 4.

## 4. Open questions resolved

### Q1 — `gate_strictness` → ZERO destination-confirm prompts at any level

A flag *is* the destination statement; naked-invocation's destination is the constant FUTURE-IDEAS — neither is a *decision* to ratify, so a confirm would ratify a constant (the dead-weight the anti-rationalization kills). Instead `gate_strictness` modulates the **first-run onboarding note** (FR6.1), the only genuinely-new decision point, fired at most once per repo via `.planning/.add-onboarded`:

| `gate_strictness` | First-run onboarding note | Steady-state (flagged / naked / quoted) |
|---|---|---|
| `strict` | blocking `strict-enum [continue, abort]`, once | no prompt (beyond scrub/length) |
| `light` / **PROFILE absent** | one-line FYI, auto-continue, once | no prompt |
| `off` | silent (write marker, no note) | no prompt |

PROFILE-absent defaults to `light` (a no-profile stranger must not hit a blocking abort on first capture, but should see the git-permanence note once). **This narrows AC-FR6.3** (it asserts onboarding-note behavior, not confirm-prompt counts) — flagged for the gate.

### Q2 — drain surfaces ALL un-dispositioned entries (pure (c); date-window REJECTED)

Disposition-state is the only gate. Considered Agent 4's hybrid (date-window dated entries + fail-open undated) and **rejected the date-window**: the backlog the 2026-05-24 drain decision exists to clear is itself old-and-dated, so a recency window hides exactly the entries that most need draining. Parse rule per top-level `## ` block: candidate iff **not** (heading `/^##\s*(✓\s*)?(SHIPPED|PROMOTED|DEFERRED|MERGED|DELETED)\b/i` OR Status line `/\b(Promoted|Deferred|Merged|Deleted)\b/`). First post-S5 drain surfaces the whole ~20-entry backlog = the intended one-time triage; mitigate the wall with **compact rendering** (heading + 1 line) and a **"defer all remaining" batch** action — not by hiding entries. **Supersedes REQUIREMENTS FR7.1's "in-window" placeholder.**

## 5. Risk register (feeds ACs) — 2 hard gates

| # | Risk | Hard gate? | Mitigation → AC |
|---|---|---|---|
| R1 | Drain mutates/corrupts FUTURE-IDEAS (first mutate-existing op; code fences, nested `##`, a mid-file orphaned `*Last updated:*` footer) | **YES** — preview-diff before drain write (planning-time mutation ≠ capture hot path → OK vs Decision 4) | one shared byte-range `parseEntries` for surface+write; full-file `atomicWrite`. AC: dispositioning entry #i leaves all other blocks byte-identical (full-file snapshot) |
| R2 | `--file` path escape outside `.planning/` | **YES** — refuse before lock | `path.resolve` + `startsWith(resolve(baseDir,'.planning')+sep)`; basename denylist DECISIONS/STATE. AC: `../`, absolute, `.planning/../../x`, DECISIONS, STATE all refuse zero-write/zero-lock |
| R3 | Wrong-anchor insert clobbers hand-authored MILESTONE/OPEN-QUESTIONS | No | per-destination insertion; before/after full-file snapshot identical except insertion region; 2nd `--milestone` reuses the section |
| R4 | Multi-flag silently picks one destination | No (error) | `resolveDestination` errors if >1 dest flag, before lock. AC: all pairs → non-zero, zero writes/locks |
| R5 | Drain edits the WRONG entry (heading-prefix collision; date-in-heading entry has no Status line) | No (subsumed by R1 byte-range design) | `applyDisposition(content, entryIndex, …)` operates on `parseEntries` block range, never global text replace; **appends** to Status line (preserves provenance) or **inserts** one; `delete`/`merge` removal = per-entry `strict-enum [confirm, keep]` regardless of gate_strictness (hard sub-gate). AC: shared 6-word-prefix entries don't cross-contaminate; date-in-heading gets a Status line inserted |
| R6 | Naked empty answer leaves lock/partial state | No | lock acquired only inside capture, which runs only on non-empty body. AC: naked + empty → exit 0, no write, no `.add.lock` |
| R7 | Scrub/length bypassed on a new destination | No | single capture spine runs scrub+length before every write. AC: secret in `--question`/`--milestone`/`--file` body triggers the same scrub prompt |

## 6. Environment check (live, 2026-05-30)

- `node --version` = **v22.13.0** (the `engines` floor — corrects a v25.8.1 assumption carried over from the 2026-05-14 plan's env section; stable `fs` APIs, no concern).
- `npm install --dry-run` resolves cleanly, 0 vulnerabilities; only runtime dep is `yaml@^2.8.3`. **No new runtime deps** (`milestones.js`/`drain.js` are pure Node). Honors Decision 8 / FR8.3.
- `npm run validate` = "Plugin validation passed."
- `npm test` = **608 passed (36 test files)** — the real baseline (CLAUDE.md's "561+" was stale). S2–S5 must keep all 608 green.
- No native modules / network / external services; `tools/audit-network-calls.js` (E3) stays green.

## 7. Observation (out of S2–S5 scope, flagged per working-norms)

The live `FUTURE-IDEAS.md` has its `*Last updated:*` footer **orphaned mid-file** (~L1044, with ~430 lines of entries below it) — evidence S1's footer-anchored `insertAboveFooter` has drifted, or entries were hand-added below the footer. Not a S2–S5 task, but: (a) S5's `parseEntries` must tolerate a mid-file footer (treat as non-entry text), and (b) worth a separate FUTURE-IDEAS hygiene pass (log via `/sig:add` after this Epic, or fix opportunistically during S5's first real drain).

*Addendum synthesized 2026-05-30 from 4 parallel agents + direct lock-contract verification + live env check (Node 22.13.0, 608 tests green). Tier: FULL.*
