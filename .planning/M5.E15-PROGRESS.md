# M5.E15 — Progress

**Epic:** M5.E15 — *the control arm, made real (`B55`)* · **Tier:** FULL · **Branch:** `m5.e15-control-arm`
**Plan:** [`M5.E15-PLAN.md`](M5.E15-PLAN.md) · **Started:** 2026-08-05

Baseline at EXECUTE entry: **2168 tests / 129 files**, green.

---

## Wave 1 — S1 ‖ S2 ‖ S3 ‖ S4 — **COMPLETE**

- [x] **S1** — Directive sites declared; field migrated; `applyDeletion` goes live — `7468c32`
- [x] **S2** — `appendNotice` gains `DIAGNOSED` (`B80`) and its first real call — `d3997d2`
- [x] **S3** — The independent leak walk, fail-closed — `6948a4c`
- [x] **S4** — The canary registry leaves the plugin copy — `6948a4c`

**2168 → 2210 tests** (+42), 129 → 132 files. Lint clean.

### Both first-executions happened, and both held

The two paths that had never run for their real purpose were deliberately placed in
wave 1 rather than near the live measurement. Both now execute:

- **`applyDeletion`** — was the false branch of `canary.deleteSection ? … : …`, a ternary
  whose condition was always true. `commands/ship.md` is its first real entry.
- **`appendNotice`** — had no production caller. The unisolated stamp on M5.E8's `OBEYED`
  record is the first annotation in `ADHERENCE-LOG.md` written by code rather than by hand.

### What the wave found that the plan did not predict

**1. `buildCaveats` could not be tested where it lived, and the plan's RED-first
requirement could not be met in place.** S1.t4 said to migrate `adherence-run.js:666-668`.
But `buildCaveats` sat in a CLI script that exports nothing, and
`tests/adherence-suite-guard.test.js` forbids *any* test file from importing that script —
a deliberate, load-bearing guard, because importing the runner is how a test suite ends up
spawning the paid agent CLI.

Exporting it (the obvious route) turned that guard red. Weakening the guard was rejected
outright. The function was extracted to `tools/lib/adherence-caveats.js` — it is pure string
work over declared data, no agent, no filesystem, no network — and tested there.

This mattered more than a placement detail: `buildCaveats` branched on the very field this
Epic replaces. Left in place, the rename would have made the branch permanently false and
the *"a whole section was removed"* caveat would have silently stopped rendering, **with no
test going red**. That is the `B39` shape reached through a field rename rather than a
missing detector.

**2. S1.t7's scrub had to cover code written during S1 itself.** The task named
`adherence-verdict.js:117` as the apparatus's verbatim copy of the measured instruction.
Writing the AC1.5 doc comment introduced **two new** mentions of the same token in the same
file. Both were scrubbed. The lesson is small and worth recording: a leak-by-comment is
introduced by ordinary careful documentation, which is why S3's walk exists rather than a
one-time cleanup.

**3. AC1.5 needed a mechanical criterion the requirements did not supply.** "A section
anchor that is not its own discrete section" was specified by example, not by rule. The
criterion implemented: *the deleted span must not order any other backticked call.*
Verified against the live corpus before being written into a test — the four `## Phase entry`
sections order only the measured call; `ship.md`'s `### 5. Update State` also orders
`completePhase`, which is precisely why `D-M5E15-8` declares that site by line.

### Open item for REVIEW

**AC2.3's allowlist includes `tools/lib/adherence-verdict.js`, which S1.t7 made unable to
match.** The requirement was written before the scrub. The entry is implemented as
specified and retained defensively, with the reason stated at the entry — but the two
criteria are in mild tension: S1.t7 removed the token *because* it was a leak, while AC2.3
allowlists the file so a future mention there would be classified descriptive. Flagged for
REVIEW rather than resolved unilaterally, since either reading is defensible and the
requirement is explicit.

---

## Wave 2 — S5 ‖ S6 — not started

- [ ] **S5** — Wire the walk into the run (depends S1 + S3)
- [ ] **S6** — The leak check has a caller, by a named assertion (depends S3)

## Wave 3 — S7 — not started

- [ ] **S7** — The live re-run, and the verdict is published (depends S1–S6)

**S7 is the only non-offline step in the Epic** and needs an explicit go-ahead: it spends
real money on ≥3 `claude` CLI runs per arm, and `D-M5E15-6` commits to publishing whatever
verdict comes back. `t0` confirms CLI reachability before any run starts.
