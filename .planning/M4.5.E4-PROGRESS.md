# M4.5.E4 — EXECUTE Progress

**Phase:** EXECUTE (output of `/sig:execute`-style inline execution, FULL/strict). **Tier:** FULL.
**Plan:** `M4.5.E4-PLAN.md` (3 vertical slices, 10 tasks). Atomic commit per task (`M4.5.E4.S{n}.t{k}`).

## Slice 1 — Worked example (`examples/url-shortener/` + currency guard)

| Task | Status | Commit | Notes |
|---|---|---|---|
| t1 — Promote dogfood → `examples/url-shortener/` | ✅ done | `dc80415` | 35 files; excluded `node_modules/` + nested `.git/`. |
| t2 — Make runnable, zero native deps | ✅ done | `7d10161` | See R1-residual below. 39/39 tests green. |
| t3 — Refresh artifacts + reconcile AC count | ✅ done | (this commit) | STATE.md → schema_version 1; AC counts reconciled. |
| t4 — Annotated-tour README | ⏳ next | — | FR2. |
| t5 — Currency guard test | ⏳ | — | FR4/AC4; `tests/example-currency.test.js`. |

## Slice 2 — `docs/vs.md` · Slice 3 — README links + CHANGELOG: not started.

---

## Drift-audit / curation note (AC3, FR3)

What changed when promoting the 2026-04-26 dogfood into the tracked `examples/url-shortener/`, and why. The example's own `.planning/` is kept as a faithful record of a real FULL-tier run; this note (Signal-level) records the curation so the refresh is auditable.

**Convention drift fixed:**
- **STATE.md** was the only real drift — legacy flat-markdown with `## Current Phase: DONE (v1.0 shipped)` (not a valid `phase` enum). Hand-authored fresh `schema_version: 1` frontmatter: `phase: SHIP` (terminal; no `DONE` enum), `current_epic: null` (single-feature linear run, no Epic structure), 7 completed phases as the array shape, null timestamps/commit (the example has no Signal-commit identity). Verified: `readState('examples/url-shortener')._schema === 1`, `readProfile(...)` returns tier FULL.
- **Vocabulary (Tranche→Milestone/Epic, M4.t18)** — **no-op**. Zero "tranche" hits anywhere in the example; it never used Signal's M/E/S/t work-unit IDs (it used its own F#/N# requirement IDs + GSD-style Slice/Wave language, all current). AC3's vocab clause is satisfied-by-absence.
- **PROFILE.md / config.json / PROJECT.md / CONTEXT.md** — already current; unchanged.

**R1-residual (storage), t2:** R1's chosen fix (swap `better-sqlite3` → Node built-in `node:sqlite`) hit a real wall in EXECUTE that PLAN research didn't surface: `node:sqlite` is so new it isn't in vite/vitest's built-in-module list, so the test runner can't resolve it (`Failed to load url sqlite`) and 3 suites failed to collect. Rather than add a bundler-externalize hack to a *showcase*, switched to the research's documented zero-dependency second option — a **plain JSON-file store** (`src/storage.js`), preserving the `{put,get,close}` contract + persistence + INSERT-OR-IGNORE semantics, and still throwing on a directory path (N3c). Net: no runtime deps at all, runs on any Node ≥22.5 / any runner. `vitest ^2.1.0 → ^3.1.0` to match root + clear the moderate npm-audit advisories.

**Accuracy reconciliations (AC3, "reconcile to true count" per user):**
- **AC count.** REQUIREMENTS said "16 testable"; VALIDATION/VERIFICATION/SHIP said "18 automated + 6 manual = 24" — but VERIFICATION's own line-by-line table actually shows **17 automated + 7 manual = 24** (REQUIREMENTS omitted N1a, which is tested; the 18/6 summaries miscounted the table, and the manual list dropped N3a). Reconciled all artifacts (REQUIREMENTS, 1-PLAN, 1-VALIDATION ×2, 1-VERIFICATION ×3, 1-SHIP) to the table-derived truth: **24 total = 17 automated (F1, F2×2, F3, F4, F5a–g, F6, N1a, N1d, N3b, N3c) + 7 manual-acknowledged (N1b, N1c, N2a–d, N3a)**. The line-by-line VERIFICATION table was already correct and was left untouched — only the summary tallies were fixed.
- **Test count.** 38-vs-39 across artifacts is **not** an error — VERIFY ran 38 tests; REVIEW (I-1/I-2 fixes) added one → 39 at ship (visible in the quoted git log). Left phase-accurate; fixed only the example README's final-state count (38 → 39, verified by `npm test`).

**Known caveat (out of E4 scope):** `npm audit` in the example reports 1 critical — GHSA-5xrq (Vitest **UI server** arbitrary file read). Not triggered by the example's headless `vitest run`; the fix is vitest 4, which would push the example ahead of Signal's own root (`vitest ^3.1.0`, same advisory). Matching root is the correct posture; chasing it to v4 is a root-level decision, not E4's.

## Last Updated

2026-06-02 (Slice 1 t1–t3 done; t4 next.)
