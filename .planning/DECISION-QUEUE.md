# Decision queue

Decisions the loop reached, did **not** guess at, and parked for a person.

An entry here means the run continued past a question rather than blocking on it.
Nothing in this file has been acted on. Answer an entry by replacing the
placeholder on its **Answer** line; the loop reads answers on its next pass.

(That placeholder is deliberately not quoted here: it is the exact token the
parser keys on, and a header containing it would make the file's own preamble
look like an unanswered entry to anything scanning loosely.)

⚠ **A long queue is a signal, not a backlog.** If entries accumulate faster than
they are answered, the attention setting is wrong for this project — that is the
measurement this file exists to produce.

---

## Q-M6E6-1 — M6.E6's outcome oracle was reported NOT MET because no released copy carried `routeDecision`. v0.1.37 is now installed and does carry it, and this /sig:drive run is executing it. Does meeting the oracle now re-open VERIFY's verdict, or does the recorded 'NOT MET' stand as the honest account of what was true at verification time?

**Phase:** VERIFY → REVIEW (during /sig:drive) · **Queued:** 2026-09-04 · **Altitude:** product · **Reversibility:** painful

**Recommendation:** Amend rather than overturn: leave the VERIFY verdict's 'NOT MET' sentence standing as the true statement about 2026-09-04 pre-release, and append a dated addendum recording that the oracle was subsequently met on v0.1.37, with this queue entry as the evidence. Rewriting the verdict would erase the finding that produced B115 and the release-gap lesson; appending keeps both facts. The Epic-close retrospective is the place that reconciles them.

**Why it is here:** Queued because it is a product-altitude call, which is yours to make, and undoing it is painful.

**Why it was queued rather than asked:** REVIEW runs identically either way, so the loop can honestly continue without the answer. But the call decides what M6.E6 claims to have achieved — and this repository has a named defect class (CLAIM-INTEGRITY-ANALYSIS.md) for completeness claims written from the shape of the work rather than the artifact. Not a call to make silently mid-run.

**Answer:** _(unanswered)_
