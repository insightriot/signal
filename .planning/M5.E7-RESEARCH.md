# M5.E7 — Research: the v2 direction audit (BR-8)

**Phase:** PLAN · **Tier:** FULL (Epic-scoped, ceremony off — `M5.E7-PROFILE.md`)
**Method:** 4 parallel research agents (`research_parallelism: 4`), reconnaissance-scoped —
size the work and validate the method, **not** perform the harvest (that is EXECUTE/FR1–FR2).
**Date:** 2026-07-25

> **Evidence discipline.** Each agent was instructed to separate what it verified from what it
> inferred, and to say "could not verify" rather than fill a template. They did. Where a number
> below is extrapolated from a sample, it says so. Treat unmarked claims as directly measured.

---

## 0. Corrections to the record — read first

Research falsified five claims. Three were in Signal's own analysis docs; **two were mine, in
artifacts already committed at DISCUSS.** Recording both classes together because the
correction discipline is the same.

### 0a. My errors (committed in `D-M5E7-3` + `M5.E7-REQUIREMENTS.md`)

| # | Claim as committed | Reality | Fix |
|---|---|---|---|
| **C1** | "B24 → B27/B28 → **B34/B35** is one recurring signal about the migrate dangling-gate's fail-safe posture" (`AC1.2` exemplar) | **B34/B35 are not in that chain.** B34 is *B29-parity* — the same prototype-pollution shape B29 hardened for `_afterRead`, left unguarded on the `renameFn` sibling. B35 is `sweep.js` `checkIndexFreshness` crash-safety from the M5.E6 REVIEW panel. Neither touches `computeDanglingDelta`. | Chains are **B24 → B27 → B28** (migrate gate) and **B29 → B34** (sibling sweep). `AC1.2`'s exemplar replaced. |
| **C2** | "**16** of 18 retros carry `## What to feed back into Signal`" — used as the harvest base | 16 carry the *heading*; the 4 stubs hold only the `[FILL IN]` placeholder under it. **Harvestable = 12.** The heading count was right; using it as the demand base was not. | Volume planned against **12 files / 39 measured items / ~1,510 words**. |
| **C6** | "That frame is **~15 months** old" (`D-M5E7-1`) and "~15 months stale by M5" (`REQUIREMENTS` FR2) | **False by ~4.5×.** `REPO-ANALYSIS.md` is dated **2026-04-21**; first repo commit **2026-04-13**; today **2026-07-25** → **3 months 4 days**. Inherited uncritically from `…-v2-SEED.md:9` and propagated into a *locked decision*, the spec, and the research briefs. **Load-bearing:** it supplied one of D-M5E7-1's two supports and implied a port left un-built that long had been implicitly rejected. Three months — while shipping v1, closing an 11-Epic milestone, cutting 11 releases, and building the doc-runtime — is ordinary sequencing under finite capacity. | **D-M5E7-6.** Staleness carries **zero** weight in any abandon decision. D-M5E7-1's reframe stands on its surviving support: calibrate + the doc-runtime came from Brett's own pain and appear on no source repo's checklist — an argument unaffected by dates. |
| **C7** | `D-M5E7-1`: "**eight** commands no source repo ever called for" | Then lists **nine**. Stale from before `/sig:sweep` shipped — in a repo whose own suite hard-fails on roster-count drift. | Corrected inline + D-M5E7-6. |

> **Why C6 is the most instructive error in this set.** It is what motivated reasoning looks like
> from the inside: a supporting detail nobody checked, because it pointed where the argument
> already wanted to go. It was caught only because an agent was briefed to be adversarial and
> rejected the premise in its own brief. That is the argument for **FR7**.

### 0b. Signal's analysis docs assert things now demonstrably untrue

| # | `analysis/` claims | Verified reality (2026-07-25) |
|---|---|---|
| **C3** | `<HARD-GATE>` is a portable Layer-3 mechanism — "code literally can't proceed"; its philosophy "permeates Layer 3" | **4 grep hits repo-wide; exactly 1 in a live skill** (`brainstorming`, gating design approval). One other hit is the maintainer's planning doc: *"we'll workshop the wording/mechanism together"* — the mechanism is **unsettled upstream**. The real blocking machinery is `subagent-driven-development`'s **five-round breaker** with `BLOCKED` propagation (`SKILL.md:96`, `:67`, `:98`, `:368`). **Signal has been planning to port a syntax instead of the mechanism.** |
| **C4** | pm-skills has "zero engineering integration. No code review, no testing, no security, no deployment" | **False since June 2026.** New 9th plugin `pm-ai-shipping` v2.0.0 ships `/security-audit-static`, `/performance-audit-static`, `/derive-tests`, `/ship-check`, and `intended-vs-implemented` (PRD-vs-code drift). Creates **overlap** with the gstack `/cso` port. |
| **C5** | The Compound phase comes with `learnings-researcher` + `session-historian` as a unit | `session-historian` **is** under `ce-compound`. `learnings-researcher` **is not** — it lives under `ce-ideate`, `ce-optimize`, `ce-plan`, and as a `ce-code-review` persona. Porting "the Compound phase and its two agents" would build **something the source does not have.** |
| **C8** *(added 2026-07-26, S4.t10 — surfaced by S2.t4 after this section was first written)* | `SIGNAL-INTEGRATION-RUNDOWN.md:186`: *"Anti-rationalization tables pattern → **Adopt as Signal convention** → **Every phase gate uses this format**"* | **The generalization is measurably wrong for one class of gate, and Signal took the generalization.** Verified at source 2026-07-25 (superpowers v6.2.0, `skills/writing-skills/SKILL.md` § "Match the Form to the Failure"). Prohibition + rationalization table is **right for discipline failures** (*"skips/violates a rule under pressure — knows better, does it anyway"*) — most of Signal's tables, which are fine. It is the **wrong form for shaping failures** (*"complies, but output has the wrong shape"*), where the right form is a **positive recipe** stating what the output IS. Evidence is empirical, not stylistic: *"in head-to-head wording tests … the prohibition arm produced clearly more of the unwanted content than the recipe arm (fully separated distributions), and trended worse than even the no-guidance control."* Signal carries ≥1 shaping rule in prohibition form (`commands/resume.md`). Two further source rules Signal has nowhere: **no nuance clauses** and **exemption clauses don't scope**. Tracked as **`B38`** (`confirmed`, P2); **the fix is reclassification, not removal.** **Widest-reaching of the five** — it touches every `commands/*.md` table plus `references/anti-rationalization.md`. |

**This is itself a finding.** Signal's `analysis/` docs are load-bearing inputs that have gone
stale without any mechanism noticing — the same failure class as the `.planning/` drift the
doc-runtime was built to fix, in the one directory the doc-runtime does not govern.
→ becomes **FR6** (see §5).

---

## 1. Demand side — Signal's own record

### 1.1 The retro corpus (agent: retro-recon)

**Inventory:** 18 `*-RETROSPECTIVE.md` files. 16 carry the `## What to feed back into Signal`
heading; **12 have content**; 4 are stubs (M4.5.E1, E3, E6, E7). M4.5.E4 and M4.5.E8 use
different templates and lack the section entirely.

**Measured yield:** **39 distinct items** across the 12 populated sections (mean 3.25, range 2–5).

**Specificity — the corpus is unusually disciplined:**

| Bucket | Share |
|---|---|
| Direct "add X to Y" | ~60% |
| Promote-to-default (change a default, i.e. a product change) | ~25% |
| Ledger pointer (a B-ID) | ~15% |
| **Vague reflection ("be more careful")** | **~0 of 39** |

The retro template's own phrasing — *"what Signal itself — commands, skills, agents, defaults,
references — could change"* — forces a named target. **The extraction method is validated.**

**Extraction is scriptable at the section boundary, judgment below it.** `awk` pulled all 12
sections in one pass; item segmentation and dedup need judgment. M4.5.E4/E8 need per-file handling.

**Six themes clear a 3+ raise bar.** Ordered by strength as a *gap* claim, not raw count:

| Theme | Retros raising it | Ledger coverage |
|---|---|---|
| **F. EXECUTE delegation / dispatch guidance** | M5.E1, M5.E4, M5.E5 | **ZERO** — `delegat\|executor cost` returns 0 hits across all four ledgers |
| B. Green tests ≠ proven; false-green; dogfood real data | M5.E1–E4, M4.5.E10 | Partial (bug-level only) |
| A. Adversarial/independent REVIEW panel earns its cost | M4.5.E10, M5.E1–E3 | Cultural, untracked |
| D. Reusable pattern has no durable home | 6 retros | Partial |
| C. Dry-run + human-approved gate for user-state writes | M4.5.E9, M5.E2, M5.E3 | Tracked |
| E. Ledger/bookkeeping reconcile discipline | M4.5.E11, M5.E4, M5.E6 | Partial |

**Theme F is the cleanest gap claim in the corpus:** three independent raises across three
consecutive Epics, zero tracking anywhere.

**The promotion loop lags 1–2 Epic cycles — this is the key scoping fact.**

- M4.5-era retro items: **~58% already tracked or shipped**
- M5.E4/E5/E6 items: **~0% tracked**, excepting bug-shaped ones (logged by construction)

The confound is age, not quality. **Bug-shaped items get logged immediately; process- and
guidance-shaped items are the ones that fall through.** Value is concentrated in the three most
recent retros. *(15-item dedup sample, ±15pp.)*

**Volume estimate:**

| Scope | Raw | After theme dedup | Net-new after ledger dedup | Confidence |
|---|---|---|---|---|
| FB section only (12 files) | 39 | ~28–30 | **~15–17** | High on raw (measured), med-high on net-new |
| + `We'd do differently` + E4/E8 analogues | ~83 | ~50 | ~26–30 | Medium — extrapolated from 4 of 12 |

**Scoping call (taken, plumbing altitude):** FB-section-first. `## What we'd do differently` is
a comparable-density second channel with ~35–40% overlap; it runs as a **second pass only if the
first underdelivers**. NFR3 mandates depth-limiting and the FB section has the higher yield per
item because the template demands a named target.

**Stub cost — reported, not reconstructed (AC1.5 upheld, independently endorsed by the agent):**

| Epic | Subject | Commits | Signal lost |
|---|---|---|---|
| **M4.5.E6** | STATE.md schema + `/sig:checkpoint` (v0.1.2); 5 slices + a REVIEW loop-back | **43** | Biggest hole — Signal's core state contract was designed here, *and* there was a rework event, exactly what retros exist to explain |
| **M4.5.E3** | Public-facing docs; audience reframe (Privacy / Compatibility / Open-Source Origins) | 18 | **Most relevant to a direction audit** — this is Signal's positioning Epic, and the signal isn't there |
| M4.5.E7 | Synthesizer prose fix + install-UX docs | 13 | Moderate; residue captured at `ISSUES-INBOX.md:170` |
| M4.5.E1 | Marketplace install-path fix (v0.1.1) | 4 | Least — rationale survives in `BACKLOG.md:182` + D-E3-12 |

### 1.2 The work ledgers (agent: ledger-recon)

**Inventory** (entry counts verified by running the live drain parser, not grep):

| File | Size | Entries | Open |
|---|---|---|---|
| `BUGS.md` | 48 KB | 36 (B1–B36, no gaps) | **5** (`needs-triage`, B32–B36) |
| `BACKLOG.md` | 14 KB | 30 items / 6 sprints + 8 parked | 29 |
| `ISSUES-INBOX.md` | **174 KB** | 39 | **39 — zero dispositioned** |
| `OPEN-QUESTIONS.md` | 2.5 KB | 2 | 2 |

The inbox is **3.6× the other three combined.** Its header is also stale — still titled "Future
Ideas," still describing `MILESTONE-4.md` as the rundown-v2 home.

**Carry-over chains — documented edges (Signal's own rows cross-reference) vs. inferred:**

**Chain 1 — the FR1 retro gate silently no-ops. `B26 → B30 → B36`. *Documented. OPEN. P2.***
Root theme: *the retro hard-block's Epic-close detection keeps carrying a state/ordering
precondition the real self-hosted flow never satisfies, so the gate skips instead of blocking.*
- B26 (M5.E4): `shipFR1Check` returns `{skipped:true}` for a fully-completed Epic
- B30 (M5.E5): B26's fix can't fire — Step 0.5 runs *before* the SHIP transition its fallback needs
- B36 (M5.E6, open): both fallbacks are row-absence-gated; at Step 0.5 the row is never yet
  "shipped," so Layer-1 enforcement is **structurally inert** for a row-maintained project

**Three Epics. And each fix was invalidated by dogfooding that same fix's own SHIP.**

**Chain 2 — the migrate dangling gate fails in *both* directions. *Documented. Closed.***
Over-abort `B24 → B27 → B28`; under-detect `B15 → B23(b)` (1 MB `FILE_SCAN_CEILING` truncation
silently blinds it). Tighter theme than "fail-safe posture": *the same gate fails both ways, and
the suite covered neither.*

**Chain 3 — a fix lands where it was found; identical siblings go unswept. `B29 → B34`
*(documented)*; `B14`, `B22` *(inferred)*.** Argues for a **sibling-sweep step in REVIEW** — one
mechanism, not four more tickets.

**Chain 4 — concurrency retrofitted onto the doc-runtime, each retrofit exposing the next gap.**
`B25 → B29 → B31 → B33`.

**Chain 5 — generated artifacts drift from or destroy truth where the guard doesn't reach.**
`B18 → B19 → B32` *(inferred)*.

**Not chains — co-occurring clusters** with zero cross-reference edges: B6/B7/B9, B2/B3/B4.
Do not present alongside the five above.

**Theme clustering pilot (39 inbox entries → 7 themes, 2 resist):** T1 operating-commands (8 raw
→ **6 real**), T2 memory/doc-runtime (7, mostly already shipped as M5.E1–E3), T3 v2-port re-audit
(5, includes **#19 = this Epic**), T4 phase-mechanics (5), T5 calibration depth (3), T6 drift
guards (6), T7 dev/QA infra (3).

**Dedupe order is load-bearing — and I had not specified it.** The 2026-07-04 backlog review
sharpened 10 inbox entries into BACKLOG sprints via a `> **Update …**` blockquote **but removed
none from the inbox** (the drain's `BLOCKQUOTE_DISPOSED_RE` deliberately doesn't match `Update`).
So **BACKLOG is the deduped view; the inbox is the raw log.** Cluster the inbox without
subtracting BACKLOG first and you re-derive Sprints 2–7 and mistake it for a finding —
**roughly 2× inflation on every cluster.**

Required order: **subtract BACKLOG → subtract shipped commands (`/sig:add`, `/sig:doctor`) →
collapse intra-file duplicates (#13 ≡ #27).**

**Two independent agents converged on "BACKLOG already knew":**
- `BACKLOG.md:86` — *"Cross-Epic pattern detection — detect recurring patterns across
  RETROSPECTIVES.md over time"* — **this Epic's own premise**
- `BACKLOG.md:82` — *"Retro replay into the next Epic's DISCUSS/PLAN"*
- Sprint 5 "Cockpit & interaction surface" — already sequences the entire T1 command cluster

**`OPEN-QUESTIONS.md` contributes zero unique signal** — both open items already sit in BACKLOG.

**Metadata reality:** inbox has reliable per-entry `Logged YYYY-MM-DD` (2026-04-22 → 2026-07-21) —
the best metadata in the corpus. BUGS.md mostly dated + Epic-stamped, not uniform. **BACKLOG has
no per-entry dates at all.** OPEN-QUESTIONS has one file-level stamp. **Frequency is nowhere
recorded** — inferable only, via four proxies (the 10 `Update 2026-07-04` second-touch markers;
duplicate entries; cross-file recurrence; BUGS cross-reference edges).
→ **`AC1.3` must distinguish recorded recency from inferred frequency.** As written it is
partly unsatisfiable.

### 1.3 The convergent finding — both halves of the retro gate are broken

Two agents, different corpora, same mechanism:

- **It doesn't fire** — `B26 → B30 → B36`, three Epics, open, P2 (ledger-recon)
- **When it fires, it validates nothing** — `M4.5.E9-RETROSPECTIVE.md:47` records that **all five
  backfilled stubs passed `validateRetroContent('FULL')` on the first try**; the auto-populated
  Links section plus `[FILL IN]` markers push scaffolding past the 687-byte FULL floor
  (retro-recon)

**A byte-count floor cannot distinguish a filled retro from scaffolding — which is why four stubs
exist and pass.** The mechanism M5.E7 depends on for input quality is broken at both ends.

*Held as a hypothesis for EXECUTE to test, not a conclusion. It is the shape a confirmation-biased
audit would most want to find, so it gets adversarial treatment (see FR7).*

---

## 2. Supply side — the live repos (agent: supply-recon)

Verified via authenticated `gh` API + a shallow clone of superpowers, 2026-07-25.

| Repo | Last push | Verdict | Surface | Wanted capabilities |
|---|---|---|---|---|
| **gstack** | 2026-07-15 (PRs #2200s) | **ACTIVE** | ~57 skills (its own description says 23 — stale against its own tree) | **4/4 intact, same names.** `/cso` still exactly 15 phases (0–14) |
| **pm-skills** | 2026-07-03 (v2.1.0) | **MAINTAINED-BUT-QUIET** — release-punctuated, single maintainer | 69 skills / 9 plugins / 42 commands | **All intact.** Growth purely additive, zero renames |
| **superpowers** | 2026-07-24 (v6.2.0) | **ACTIVE** | 14 skills, 180 files | **2/3 confirmed; `<HARD-GATE>` far weaker than believed (C3)** |
| **compound-engineering** | 2026-07-24 (PRs #1250s) | **ACTIVE** — most active | 32 `ce-*` skills, 27 agents + 17 personas | **Restructured; `learnings-researcher` relocated (C5); style agents deliberately removed** |

**No port can be abandoned on dormancy grounds.** Everything is alive. `AC2.3` is moot —
abandonment must now be argued on **fit or overlap**, a materially harder case. *The reframe
built in an easy exit and the evidence removed it.*

**Harder TDD held completely.** superpowers `test-driven-development/SKILL.md`: *"Write code
before the test? Delete it. Start over."* (:37), *"Delete means delete"* (:43, :221), with the
anti-rationalization table intact at ~:204–244.

**Three new capabilities worth wanting that did not exist in April:**

1. **Cross-model adversarial review** (compound) — `cross-model-eval.md`,
   `scripts/cross-model-adversarial-review.sh`, `peer-job-runner.py`, machine-checkable
   `findings-schema.json`. Findings validated by a peer running a **different model**.
   **Directly answers a problem in Signal's own ledger** — the v0.1.9 false-green security bypass,
   which Signal answered with a *same-model* 3-specialist panel in v0.1.10.
2. **`/cso` lazy-loading** (gstack) — `sections/manifest.json` holds scope-dependent phases 2–11
   on demand; always-run phases stay in the skeleton; scope flags `--infra/--code/--skills/…`.
   **Signal's stated #1 risk is skill token cost; gstack independently solved exactly that.**
   The manifest pattern is worth stealing *independent of `/cso`'s content*.
3. **`ce-retune`** (compound, landed 2026-07-24) — measurement-first corpus retuning for model
   upgrades. Signal has no answer to model-upgrade drift.

**Three sources independently built memory/compounding layers since April** — gstack `gbrain`
(cross-project memory + MCP), compound `ce-compound`/`ce-compound-refresh`, pm-skills `retro`.
Signal planned to source Compound from compound-engineering alone. **It is now a three-way choice.**

**Design lesson, not just a removal:** compound's persona catalog explicitly narrows away the
stack-philosophy agents Signal wanted — *"do not spawn extra stack reviewers for philosophy or
convention-only passes."* The source tried Signal's plan and pulled back from it.

**Deep-verify sizing, ordered by decision leverage rather than effort:**

| Order | Repo | Effort | Why this position |
|---|---|---|---|
| **1** | superpowers | **S** | Cheapest verify, biggest belief correction. **Its answer changes what Layer 3 is architected as**, so it must precede any Layer-3 design |
| 2 | pm-skills | S | Purely additive, zero renames. Only judgment work is `pm-ai-shipping` overlap |
| 3 | gstack | M | 4/4 confirmed by name → confirmatory. Cost is per-artifact size (`/cso` spans skeleton + sections + manifest) |
| 4 | compound-engineering | **L** | Must **re-map an architecture**, not confirm one. Every one of Signal's four wanted bullets needs re-derivation |

**Pattern sources:** planning-with-files is v3.8.2, very active, default branch `master`; its
"deterministic completion gate" is **now a script, not a thesis** (`gate-stop`, `attest-plan`,
`check-complete` in `.sh` + `.ps1`). oh-my-claudecode is v4.15.7, pushed today, and has **pivoted
to "Teams-first multi-agent orchestration"** — but `deep-interview`, `ralph`, and `visual-verdict`
all survive, so what Signal borrows is unaffected.

**Not verified (declared, not guessed):** whether `/office-hours` still uses six forcing questions;
whether `/plan-ceo-review` still has four scope modes; whether `systematic-debugging` is still
specifically 4-phase; whether planning-with-files still claims the 96.7% benchmark.

---

## 3. Risk & prior art (agent: risk-recon)

Delivered after two requests. All four tasks complete.

### 3.1 The April recommendations — what actually happened

**One recommendation was explicitly cut with a recorded reason. Two were silently dropped. Five
ports have sat un-started with no cut decision for the project's entire life.**

| Recommendation | Fate | Recorded reason? |
|---|---|---|
| GSD engine · Agent Skills substrate · `/sig:calibrate` + PROFILE + tier-gating · the name "Signal" | **Built** | n/a |
| Compounding substrate **per-org** ("the differentiator") | **Reversed to per-repo** | ✅ DECISIONS 2026-07-15 |
| Meta-plugin architecture over monolith | **Silently dropped** — Signal is a monolith | ❌ No decision anywhere in the repo |
| `/new-feature` command · "audit the 21 skills, ~12 survive" · "don't defer upstream phases" | **Not done** | ❌ None; the last was itself deferred |
| pm-skills · gstack · superpowers · compound-engineering · planning-with-files ports | **Never started** | ❌ **No cut decision exists for any of the five** |

**Why gstack was never ported — direct answer: there is no recorded reason.** Every mention
outside `analysis/` is an attribution line, a roster line marking it deferred, or a "queued behind
X" pointer. **It was never evaluated and rejected; it was queued, re-queued, and displaced.** This
is exactly the failure mode D-M5E7-4's `build/abandon/continue` verb set exists to close, and the
strongest argument that that decision is right.

### 3.2 The displacement chain — 0 for 6, in one week

The seed (2026-07-13) recorded a six-Epic v2-port queue. The 2026-07-16 override took E1 for the
doc-runtime **three days later**, and every one of the six was displaced:

| Planned | Actually shipped |
|---|---|
| E1 pm-skills upstream + `/office-hours` + `deep-interview` | E1 doc-runtime model + eviction |
| E2 compound loop (`/retro`, `/learn`, `learnings-researcher`) | E2 `/sig:migrate-memory` |
| E3 gstack `/cso` security | E3 all-docs hygiene + living BACKLOG |
| E4 superpowers TDD + `<HARD-GATE>` | E4 12-bug close-out + concurrency lock |
| E5 planning-with-files grafts | E5 carry-over bug squash |
| E6 (unassigned) | E6 `/sig:sweep` + doc-runtime close-out |

**Eleven releases have shipped containing zero lines from the April port queue.** Every
displacement was Brett's call, driven by felt pain (`nextpass/.planning/STATE.md` write-wedged at
529 KB; the 455 KB CMMC dogfood). Every replacement was work named in **none** of the analyses.

### 3.3 The crucial nuance — right about capabilities, wrong about sources and sequence

**The April audit was not uniformly wrong,** and the distinction reshapes how much authority this
one carries. Several capabilities it recommended **did** prove necessary — Signal just built each
natively, under a different name, from no source repo:

| April recommended | What Signal grew instead |
|---|---|
| compound-eng's ~50-agent review panel | The **3-specialist adversarial panel** — caught the `evict.js` false-green in v0.1.9; 12-case mutation matrix, 0 false-greens in v0.1.10 |
| superpowers' `<HARD-GATE>` | `phase-gate-enforcer` + the exit-2 STATE write-guard + the FR1 retro gate — which **hard-blocked its own SHIP** in v0.1.10 |
| planning-with-files' disk-as-cognitive-scaffold | The entire doc-runtime |
| gstack `/retro` + `/learn` | `RETROSPECTIVES.md` + the SHIP retro gate + 18 Epic retros |

→ **D-M5E7-7.** Capability-level conclusions deserve weight; source-and-sequence conclusions
deserve very little. **Measured prior on that class of claim, on this project: 0 for 6.**

### 3.4 On pm-skills as "the #1 gap"

Three months is too short to call the gap fake, and nothing that displaced it was frivolous. What
the record *does* show: **the gap was never once felt.** Across 18 retros, 36 catalogued bugs, and
a 1,507-line inbox, nothing surfaced as "I couldn't figure out what to build." Every displacement
was *downstream* pain — doc bloat, state corruption, resume drift, lost updates.

**Explicit caveat (inference, not evidence):** Signal is built by someone who already knows what
to build, from a spec written up front. That is close to a worst case for detecting demand for an
ideation layer. Absence of signal here is **weak** evidence about the gap for other users — but it
is the only evidence available, and it points one way. → **AC7.3**.

### 3.5 Shelf-ware failure modes — two instantiated in Signal's own record

Seven modes identified. Two are not hypothetical:

- **`M4.5.E5-LAUNCH-KIT.md` §3 — every box unchecked, yet v0.1.4 demonstrably shipped**
  (`CHANGELOG.md:196`). The work partly happened and the tracking artifact was abandoned
  mid-flight, so it now *looks* like nothing happened — **worse than no checklist.**
- **`archive/BACKLOG-REVIEW-2026-07-04.md:33` — A3, "Tester-friction intake protocol."**
  Identified 2026-07-04 as the specific missing mechanism behind M4.5's exit criterion, reasoning
  spelled out. Never built. The criterion it protected — "≥3 non-Signal users **with feedback
  merged**" — was then declared **met on recollection** (DECISIONS 2026-07-15). No artifact
  containing tester feedback exists anywhere. **Signal's own quality gate was satisfied by
  assertion.** This is the highest-value prior art in the report.

**And one that touches this Epic directly:** Signal ships a `[FILL IN]` stub detector
(`doc-hygiene.js:352`) and `/sig:sweep` — yet four retros still carry literal unfilled markers,
because `FILL_IN_NAME_EXCLUDE` (`doc-hygiene.js:317`) **deliberately exempts `-RETROSPECTIVE.md`**.
Defensible in isolation; uncomfortable now that M5.E7 has designated retros its primary evidence
base. **The one corpus Signal excused from its own hygiene enforcement is the one it now depends
on.** → a `## What to feed back into Signal` item for this Epic's own retro, not a mid-Epic change.

### 3.6 Verdict

Not likely to produce shelf-ware — **but not for the reasons the plan gave.** What genuinely
lowers the risk is **D-M5E7-4 making `abandon` first-class**: every prior audit could only say
"build (later)," which is why five ports sat un-cut. D-M5E7-2's double rejection of SPIKE is also
called out as unusually careful, and is what guarantees this Epic's findings land durably.

Three inherited defects, all now addressed: the false 15-month fact (→ D-M5E7-6), an unenforceable
input bound (→ P5, numeric, aimed at the inbox), and an evidence base whose richness was asserted
rather than measured (→ P6, C2).

---

## 4. Environment check (PLAN Step 6)

No runtime dependencies. The deliverable is markdown: no package manifest changes, no native
modules, no version pins. Node 22+ is already the suite runtime and nothing here touches it.

**The one real environment dependency — network reach to the source repos — is now DISCHARGED.**
supply-recon executed the live check successfully via authenticated `gh` API, so FR2 is proven
feasible rather than assumed. A shallow superpowers clone is left at the session scratchpad for
the deep verify to reuse.

---

## 5. What this changes about the plan

| # | Change | Driver |
|---|---|---|
| 1 | `AC1.2` exemplar corrected; chains are B24→B27→B28 and B29→B34 | C1 |
| 2 | Harvest base is **12 files / 39 items**, not 16 | C2 |
| 3 | **New AC: dedupe order** — BACKLOG → shipped commands → intra-file. Wrong order inflates ~2× | §1.2 |
| 4 | **BACKLOG is promoted from one-of-four inputs to the deduped authority.** The audit must *reconcile against* it, not re-derive it | Two agents converged |
| 5 | `AC1.3` split: recency is **recorded** (inbox, mostly BUGS); frequency is **inferred only**, via 4 named proxies; BACKLOG has no dates at all | §1.2 |
| 6 | **The audit must read `BUGS.md`** — the strongest signal (B26→B30→B36) exists nowhere else | §1.1/1.2 |
| 7 | Harvest weights **recent retros**; a uniform 12-file sweep mostly rediscovers BACKLOG | 58% vs ~0% split |
| 8 | `AC2.3` (dormancy → abandon) is **moot**; EXECUTE must not re-litigate it. Abandonment argues fit/overlap | §2 |
| 9 | Deep-verify **order fixed**: superpowers → pm-skills → gstack → compound | §2 |
| 10 | **New FR6 — "corrections to the record."** `analysis/` docs assert falsified claims (C3/C4/C5); leaving them means the next reader re-derives the same wrong plan | §0b |
| 11 | **New FR7 — adversarial check on the audit's own conclusion.** The memory-loop thesis is what a confirmation-biased audit would most want to find | §1.3 |
| 12 | Roadmap commits **hard near-term, loose far-term**, and every item carries a **trigger condition** not just a position — position is what got overridden six times | §3.2, D-M5E7-7 |
| 13 | **Staleness is struck as an abandon rationale** (C6). Abandonment must argue fit or overlap | D-M5E7-6 |
| 14 | **Inherit April's capability findings freely; discount its source/sequence claims** — measured prior 0-for-6 | §3.3, D-M5E7-7 |
| 15 | **New: 8 structural anti-shelf-ware properties (P1–P8)**, P1/P2/P3/P6 non-negotiable | §3.5 |
| 16 | **New AC5.6 — `build` items also land in `BACKLOG.md`** with trigger + first slice; `analysis/` is enforced by nothing | D-M5E7-8b |
| 17 | **P5 budget retargeted:** the retro corpus is ~1,510 words (~1 hour); the unbounded surface is `ISSUES-INBOX.md` at 1,507 lines | §3.5 |

---

## 6. Known gaps in this research

1. ~~risk-recon undelivered~~ — **delivered after two requests; §3 is complete.** Its own declared
   inferences: that `/new-feature` was "superseded by tiers + `--epic`" is its reading, not a
   recorded rationale; §3.4's worst-case-for-detecting-ideation-demand argument is analysis, not a
   documented finding.
2. **`## What assumptions broke` / `## What surprised us` yields unmeasured** — retro-recon read
   neither and excluded them from the volume table rather than guessing.
3. **Frequency data does not exist** anywhere in the corpus; only inferable.
4. **Four supply details declared unverified** (§2) — deliberately not guessed.
5. **No written user feedback exists to mine.** `M4.5.E5-LAUNCH-KIT.md` §3 is entirely unchecked,
   including "capture returned friction logs." The "4 testers, positive reception" claim is
   recollection, not artifact. `MILESTONE-5.md`'s "sequencing should follow real user pain points
   from v1 usage" **cannot be followed literally.**

---

## 7. Process finding — agents completed without delivering

Three of four agents went idle **without their reports reaching the caller**; each required an
explicit re-request. One never delivered at all.

This is a live defect class in Signal's own PLAN phase, which spawns research agents exactly this
way and synthesizes them into `{Epic}-RESEARCH.md`. **If findings can be silently lost, a plan
built on zero research is indistinguishable from one built on four agents' work, and no gate
catches it.** At `research_parallelism: 4` on a FULL-tier Epic, that is the difference between a
researched plan and a confident guess.

Needs triage before it earns a B-number — it may be harness behavior rather than a Signal defect,
and Signal's discipline is *never graduate a finding to a confirmed bug unverified*. Candidate
mitigation regardless: `/sig:plan` Step 2 should assert each spawned agent's findings actually
landed before writing RESEARCH.md.

---

## Last Updated

2026-07-25 — PLAN research complete (3 of 4 agents). Next: `M5.E7-PLAN.md`.
