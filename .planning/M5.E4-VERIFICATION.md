# M5.E4 — VERIFICATION (v0.1.9 bug/hygiene close-out)

**Verdict: ✅ PASS.** All 7 FRs' acceptance criteria verified against landed tests; full suite green (1528, double-run deterministic); build + validator + lint clean. One newly-discovered bug (B24) cataloged + deferred to v0.1.10 (user-approved 2026-07-21).

**Tier:** FULL/strict — `nyquist_enforcement: strict`, `gate_strictness: strict`. No browser/frontend surface (backend + doc-runtime only), so VERIFY is code/test-based.

## 1. Acceptance-criteria verification (AC-by-AC)

| FR / AC | Landed test | RED-first evidence | Verdict |
|---|---|---|---|
| **FR1 AC1.1** B19 skip fires + notes preserved | `migrate-index-guard.test.js` "LEGACY curated INDEX … intact" | RED-first: legend-bearing fixture failed on `main`, GREEN after (`dd5c1c5`) | ✅ |
| **FR1 AC1.2** flagged in dry-run **AND apply summary** | same file: `res.warnings.some(…)` (:71,:85) + `renderDryRun` LEFT-INTACT (:112) | RED-first (`dd5c1c5`) | ✅ (advisor-flagged: apply-summary path explicitly tested) |
| **FR1 AC1.3** new-format still regenerates | `migrate-index-guard.test.js` "regenerates when absent" + marker path | landed | ✅ |
| **FR1 AC1.4** RED proven against `main` | the defanged fixture was replaced with the real legacy INDEX | attested — proven red on `main` pre-fix | ✅ |
| **FR2 B14** dir-symlink escape rejected (evict/add/resume) | `path-confine.test.js` + `evict-on-close`/`add`/`resolve-artifact` escape tests | RED-first per site (`b5ed3f0`) | ✅ |
| **FR2 B18** `regeneratePlanningIndex('.')` round-trip | `planning-index.test.js` "…not slice-chopped" | regression guard (fix shipped `8f1dda8`/M5.E3; guard added `f810acb`) | ✅ |
| **FR2 B15** blocking gate scans full file (dangle >1 MB caught) | `migrate-dangling-baseline.test.js` "B15 … PAST the 1 MB cap" | RED-verified: restored truncation → RED (`cb4cbf9`) | ✅ |
| **FR2 B22** internal-link check bounded to repo root | `docs-hygiene.test.js` "B22 … escaping ../ link" | RED-first (`cd7504d`) | ✅ |
| **FR3 B16** rolled-back apply deletes the pre-apply tag | `migrate-apply-failure.test.js` "deletes pre-migrate-memory-<stamp>" | RED-first: tag remained pre-fix (`9d36aa4`) | ✅ |
| **FR3 B21** D-ID map built once | `migrate-append-log-evict.test.js` "B21 … ONCE" | RED-verified: once-build off → 0 namespace calls (`0505cab`) | ✅ |
| **FR4 B6** AC4.1/AC4.2 + local 3-case | `is-stale-vs-origin`/`is-state-stale` (+5 tests) | RED-first git fixtures (`8fbdecf`) | ✅ |
| **FR5 AC5.1** migrate no re-entrant deadlock | `migrate-lock.test.js` (applyMigrate completes under held lock) | RED-first: sub-step-A showed re-entrant `StateWriteError`, GREEN after core routing (`016a123`) | ✅ |
| **FR5 AC5.2** concurrent writer fails fast (6 RMW paths) | `rmw-lock.test.js` (8 tests, all 6 paths + the dual-role split) | RED-first before wrapping (`5ad1c24`); **read-enclosure confirmed by inspection** (every wrapper = `withStateLock(…, () => Core(…))`, read inside Core) | ✅ |
| **FR5 AC5.3 / NFR2** linear path byte-identical | existing migrate/checkpoint/drain/retro suites stay green (0 regressions) | — (invariance) | ✅ |
| **FR6 B17** deterministic double-run | `vitest.config.js testTimeout` — double-run green ×2 this phase | config (no unit test; verified by double-run) | ✅ |
| **FR6 B5/B20** `npm run lint` runs | `eslint tools/` exits 0 | tooling (verified by exit 0) | ✅ |
| **FR6 B23(a)** organic re-root-to-missing | `migrate-dangling-baseline.test.js` (real-gate abort + rollback) | literal case proven UNREACHABLE + adjacent real coverage (`8fec7bf`) | ✅ |
| **FR6 B23(b)** >1 MB truncation fixture | shared with B15 (above) | RED-verified | ✅ |
| **FR6 B23(c)** cached-empty after abort | `migrate-apply-failure.test.js` "B23(c)" | coverage guard (green; guards "do NOT reorder") | ✅ |
| **FR6 B23(d)** no re-promote of `→ Promoted` | `drain.test.js` "B23(d)" | coverage guard (green) | ✅ |
| **FR6 B23 nits** blockKey rename + fail-loud dedupe | `backlog.test.js` (24, green) | refactor (behavior-preserving; fail-loud added) | ✅ |
| **FR7 B8** command-level `block:true→false` flip | `migrate-all-vectors.test.js` (added assertion) | close-on-verify (auto-remediation shipped v0.1.8; command-path assertion added `651504d`) | ✅ |
| **FR7 AC7** ledger reconciled | `BUGS.md` footer 0 open · 1 confirmed · 2 dismissed · 21 fixed (24) | doc check (counts verified via node) | ✅ |

## 2. Full test suite

`npm test` → **96 files, 1528 passed / 0 failed.** Run **twice** this phase — deterministically green both times (B17's `testTimeout` fix confirmed: no flake). Baseline was 1492 (v0.1.8) → **+36**.

## 3. Nyquist compliance (strict)

Every AC maps to a landed test (§1). Strict evidence via **form (b)** — attestation that the test was written before the implementation, naming the file + the commit it predates — is recorded per row above. Honest classification:

- **True RED-first** (test failed on unfixed code, then GREEN): FR1/B19, FR2/B14, FR2/B15, FR2/B22, FR3/B16, FR3/B21 (reverse-verified), FR4/B6, FR5 (both sub-steps). These are the substantive fixes.
- **Coverage guards** (green on current code by design — they lock in existing-correct behavior or a fix that shipped earlier): FR2/B18 (fix was `8f1dda8`/M5.E3), FR6/B23(a,c,d), FR7/B8 (auto-remediation shipped v0.1.8). Documented as such, not claimed as RED-first.
- **Config/tooling** (no unit-test shape): FR6/B17 (verified by deterministic double-run), FR6/B5-B20 (verified by `lint` exit 0), FR6/B23-nits (behavior-preserving refactor).

**Permanent-gap note** (per `references/tier-definitions.md`): B18's and B8's *fixes* shipped before this VERIFY (M5.E3 `8f1dda8`; v0.1.8 vector-1 de-prose), so per-fix red→green evidence for those is structurally non-recoverable — M5.E4 adds regression/close-on-verify guards (green now, would be RED on the pre-fix code). This is an honest limit, not a gap in the M5.E4 work itself.

## 4. Build verification

- `npm run build` (esbuild → node22 esm): ✅ clean (`Done in 62ms`).
- `npm run validate` (plugin validator): ✅ passed.
- `npm run lint` (`eslint tools/`): ✅ exit 0, 0 errors (newly functional via B5/B20).
- No new warnings introduced.

## 5. Newly-discovered finding (out of scope, deferred)

**B24** (`confirmed`, P2) — a pre-existing broken `](*.md)` link inside a strictly-closed DECISIONS section blocks `applyMigrate` (fail-safe abort + byte-identical rollback), an FR6.3 tension + dry-run/apply divergence. Discovered during T4.1 (`8fec7bf`). **Fix deferred to v0.1.10** (user-approved 2026-07-21): the correct fix reworks the load-bearing dangling-delta gate all migrate paths depend on and needs its own RED-first + full re-validation — inappropriate to rush into a release whose purpose was stabilizing that gate. Cataloged in `BUGS.md`; inert on Signal (0 inline `](*.md)` in DECISIONS.md).

## Exit criteria

- [x] All acceptance criteria verified with evidence (§1)
- [x] Full test suite passes (1528, double-run)
- [x] Nyquist compliance check passes (strict; §3, with honest classification + permanent-gap note)
- [x] Build succeeds cleanly (§4)
- [ ] User approves verification results

## Last Updated
2026-07-21 (VERIFY — PASS; awaiting approval to REVIEW)
