# M5.E18 — EXECUTE progress

**Epic:** M5.E18 · **Tier:** FULL (`tdd_required: true`, `gate_strictness: strict`,
`context_rot_reread: true`) · **Opened:** 2026-08-04

Plan: [`M5.E18-PLAN.md`](M5.E18-PLAN.md) · Validation: [`M5.E18-VALIDATION.md`](M5.E18-VALIDATION.md)
· Research: [`M5.E18-RESEARCH.md`](M5.E18-RESEARCH.md)

**Executed inline rather than via dispatched executor agents.** Both research subagents used at PLAN
did real work and never reported it (one's output was recovered from its scratchpad, the other's
assignment was re-derived by hand). With S1's measured expected values already in context, inline
execution was the lower-risk path. Recorded as a mechanics decision, not a scope change.

---

## Wave 1 — S1: units derived from filenames (FR1)

- [x] **t1** — `tools/lib/work-units.js` + 23 tests — commit `4c651e1`
- [x] **t2** — repoint `state-drift.js`, delete the local regex — commit `22ec348`
- [x] **t3** — corpus harness + this file — commit *(this one)*

**Status: S1 COMPLETE.** 1994 → **2020** tests green, lint clean.

### The RED baseline was measured, not asserted

A module-not-found error proves nothing, so the **naive rule — right-anchored match only, exactly as
the requirements originally specified** — was implemented first and the corrected ACs run against it.

**8 failed / 15 passed.** The 8: five `AC1.2′` split-pair tests, two `AC1.6` phase-name tests, and one
bug in my own fixture (`CONTEXT.md` appeared in two inventories, so the corpus had a duplicate).
**Both PLAN-time corrections are load-bearing by measurement, not by argument.**

### First use — all 12 real trees, with a full before/after audit

The instruction that put S1 in wave 1 is *"name what the Epic RUNS for the first time."* This is it.

| Project | naive → final | every change accounted for |
|---|---|---|
| `nextpass` | 13 → **9** | 4 folds — `PLAN-GATE-A`→`GATE-A`, `PLAN-SC1`→`SC1`, `PLAN-SLICE-SSO`→`SLICE-SSO`, `PLAN-SLICE-VOICE1`→`VOICE1` |
| `agent-builder` | 3 → **0** | 3 phase-name exclusions (`PLAN`, `REVIEW`, `VERIFY`) |
| `conversor` | 2 → **0** | 2 phase-name exclusions (`EXECUTE`, `PLAN`) |
| `cm-mentor-coach` | 5 → **4** | 1 fold (`PLAN-SLICE-SSO`→`SLICE-SSO`) |
| `traction-engine` | 8 → **8** | unchanged — exactly `AC1.4′`'s corrected set |
| `signal` | 12 → **12** | unchanged — includes `v0.1.6`, a non-Epic unit in Signal's own tree its own archive path cannot reach |
| other 6 | unchanged | — |

**9 units removed across 4 projects: 5 exclusions + 4 folds. Zero unexplained, zero unexpected
additions.** The audit enumerated a reason for every single one rather than reporting a net count.

### A simplification against the plan, taken in the open

PLAN specified **"strip a leading phase marker, then fold."** Measured against all four real split
pairs, **the strip is redundant** — the fold alone merges all four, including the one the strip
*cannot*: `PLAN-SLICE-VOICE1` strips to `SLICE-VOICE1`, but its sibling is `VOICE1`; the fold matches
it directly because `PLAN-SLICE-VOICE1` ends with `-VOICE1`.

The fold is also **strictly more conservative** — it only renames onto a name the corpus already
evidences, so a `PLAN-ORPHAN` with no `ORPHAN` sibling is left exactly as derived. `AC1.2″` pins that.
**Strip dropped.** One pass instead of two, and the weaker rule was the one removed.

### Two bugs `AC1.7′` caught that "byte-identical" would have missed

`AC1.7` originally required check `(c)`'s findings be **byte-identical** after the extraction. PLAN
corrected it to `AC1.7′` — *differences enumerated and ratified* — because the suffix lists differ.
That correction is what surfaced both of these:

**1. The vocabulary is the caller's question, not a narrower default.** The first attempt shared the
rule *and* the vocabulary. Six of check `(c)`'s own tests went red: the local 4-suffix list was never
a subset-for-convenience. `SCAFFOLD_SUFFIXES` answers *"what archives with a closed unit"*; the 4
answer *"what evidences a unit was **executed**."* Including `REQUIREMENTS` fires on any unit that was
scoped and parked — *"manufacturing a chore for every idea anyone ever wrote down"*, stated verbatim
in the docblock I had skimmed past. Now `WORKED_SUFFIXES`, exported beside the rule with that
reasoning; `deriveUnits` takes `{suffixes}`.

**2. I diagnosed the first one from the test names and was wrong.** After the vocabulary fix the same
six stayed red. The actual error read `expected 'M5.E13,M5.E13-PLAN.md' to match /M5.E17/` —
`deriveUnits` returns a **Map**, and the consuming loop still spread it as the old **Set**, so each
"unit" was a `[key, files]` entry stringified into the finding text. Both bugs were real; the Map bug
was masking which was which. *Reading the failure beat reasoning about the failure — the same lesson
this Epic's own research produced twice.*

**`AC1.7′` measurement — all 12 real projects, status *and* findings compared before/after: ZERO
differences.** 9 `not-applicable`, 2 `clean`, 1 `cannot-evaluate` (`affiliate-mojo`, whose `readState`
throws — the live `NFR3` path `AC2.11` exists for).

---

## Waves 2–5 — not started

| Wave | Slices | Depends on |
|---|---|---|
| 2 | S2 (raw current-unit guard) ‖ S3 (verdict reading) | S1 ✅ |
| 3 | S4 (three-outcome closure) ‖ S5 (stub retro + `B72`) | S2, S3 / S1 |
| 4 | S6 (archive destination, `NFR4`) | S4 |
| 5 | S7 (four-status reporting, FR6) | S4, S5 |

**Gate:** `gate_strictness: strict` — wave 2 does not open without confirmation.
