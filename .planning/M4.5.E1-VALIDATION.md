# M4.5.E1 — Nyquist Test-Coverage Mapping

**Phase:** PLAN — validation output of `/sig:plan`.
**Tier:** FULL — `nyquist_enforcement: strict`. Every acceptance criterion gets a named test method. Manual rows are allowed only where the artifact itself is the test (verification docs) and only when the artifact is explicitly named and committed.
**Reads:** `M4.5.E1-PLAN.md` (sibling, source of acceptance criteria).

---

## 1 — Coverage map (every criterion → at least one test)

### Slice 1 — Source-block fix + v0.1.1 ship

| # | Acceptance criterion | Test type | Test file / artifact | Asserts |
|---|---|---|---|---|
| 1 | Install succeeds on R1 machine (Brett's business box) | **Manual E2E** | `docs/install-verification.md` § R1 | exit codes, stdout transcript, timestamp, commit SHA |
| 2 | `npm run validate` enforces semver-format on `plugin.json.version` | **Unit** | `tests/install-contract.test.js` — `describe('plugin.json version', …)`, 2 cases (`0.1.1` passes; `0.1.x` fails) | regex `/^\d+\.\d+\.\d+$/` |
| 3 | `marketplace.json.plugins[0].source.source === "url"` | **Unit (fixture)** | `tests/install-contract.test.js` — `describe('marketplace.json source block', …)`, positive case | structural equality on parsed JSON |
| 4 | `source.sha` is 40-char hex | **Unit (fixture)** | same as #3, additional `it` | regex `/^[a-f0-9]{40}$/` |
| 5 | `source.ref` matches `v<plugin.json.version>` | **Unit (fixture)** | same as #3, additional `it` | string equality after `v` prefix |
| 6 | README contains `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` | **Unit (grep smoke)** | `tests/readme-content.test.js` — 1 case | substring match on `README.md` |
| 7 | `CHANGELOG.md` exists with `## [0.1.1]` + `## [0.1.0]` headings | **Unit (fixture)** | `tests/install-contract.test.js` — `describe('CHANGELOG.md', …)`, 2 cases | regex on file content |
| 8 | `npm test` green (209 baseline + new tests) | **CI / regression** | full vitest run | exit code 0; assertion count ≥ 209 + new |
| 9 | Git tag `v0.1.1` exists + pushed | **Manual** | `docs/install-verification.md` § Release log; cross-check via `git ls-remote --tags origin` | tag presence on remote |

**S1 unit-test additions (count):** 8 new tests across 2 new files (`install-contract.test.js` + `readme-content.test.js`).

---

### Slice 2 — F2 verification + agent restructure (conditional)

| # | Acceptance criterion | Test type | Test file / artifact | Asserts |
|---|---|---|---|---|
| 1 | Phase A run documented in `docs/install-verification.md` | **Manual artifact** | `docs/install-verification.md` § F2 verification | `/agents` raw output captured; `/sig:init` log captured; scan-output diff vs dogfood reference |
| 2 | Outcome label (a/b/c) recorded in `DECISIONS.md` | **Unit (grep smoke)** | `tests/install-contract.test.js` — 1 case | `DECISIONS.md` contains `F2 resolved: outcome` substring |
| 3 | `commands/init.md:170` stale `signal-` prefix fixed | **Unit (negative grep)** | `tests/install-contract.test.js` — 1 case | `commands/init.md` does NOT contain `signal-` as agent prefix |
| 4 | (Outcome b/c) `REQUIRED_AGENTS` expanded to 26 | **Unit** | `tests/install-contract.test.js` — 1 case | `validate-plugin.js` parsed, REQUIRED_AGENTS array length === 26 |
| 5 | (Outcome c only) all 26 agents in flat layout | **Unit (fixture-shape)** | `tests/install-contract.test.js` — 1 case (conditional execution gated on outcome) | every `agents/**/*.md` path matches new convention |

**S2 unit-test additions:** 3-4 new tests (some conditional on Phase A outcome). Plus manual artifact.

---

### Slice 3 — Fresh-machine install verification matrix

| # | Acceptance criterion | Test type | Test file / artifact | Asserts |
|---|---|---|---|---|
| 1 | `docs/install-verification.md` exists with 8-row matrix | **Unit (grep)** | `tests/install-contract.test.js` — 1 case | file exists; `R1` through `R8` headings present |
| 2 | R1, R2, R3, R5 each have Result entry | **Manual artifact** | `docs/install-verification.md` § per-row results | each row has `**Result:** PASS \| FAIL: <reason>` |
| 3 | R1 specifically has `Result: PASS` with timestamp + commit SHA | **Manual artifact** | `docs/install-verification.md` § R1 | PASS marker present; timestamp parseable; SHA matches `git rev-parse HEAD` of v0.1.1 |
| 4 | New failure modes logged in `FUTURE-IDEAS.md` | **Unit (grep, conditional)** | `tests/install-contract.test.js` — 1 case | if `docs/install-verification.md` mentions any `R<N>-derived` finding, `FUTURE-IDEAS.md` has matching heading |

**S3 unit-test additions:** 2 new tests. Verification rows themselves are manual (this is fine and intentional — install-on-fresh-machine cannot be automated within Signal's own runtime).

---

### Slice 4 — Versioning + deprecation policy

| # | Acceptance criterion | Test type | Test file / artifact | Asserts |
|---|---|---|---|---|
| 1 | `docs/versioning.md` exists with required sections | **Unit (grep)** | `tests/install-contract.test.js` — 1 case | file exists; contains headings for "Pre-1.0 vs post-1.0", "CHANGELOG format", "Release checklist", "Revisit at E5" |
| 2 | `DECISIONS.md` has "Versioning + deprecation policy locked" entry | **Unit (grep)** | `tests/install-contract.test.js` — 1 case | substring match |
| 3 | S1's v0.1.1 ship followed the checklist | **Manual cross-check** | `docs/install-verification.md` § Release log + `docs/versioning.md` § Release checklist | each checklist step is checked off for v0.1.1 |

**S4 unit-test additions:** 2 new tests.

---

### Slice 5 — Validator hardening (scope-beyond-exit)

| # | Acceptance criterion | Test type | Test file / artifact | Asserts |
|---|---|---|---|---|
| 1 | New validator checks pass against current repo | **Integration** | `npm run validate` | exit code 0 |
| 2 | Each new check has ≥1 negative fixture test | **Unit (fixture)** | `tests/install-contract.test.js` — multiple `describe` blocks per check | each new check: 1 positive + 1 negative case |
| 3 | 22-vs-26 agent count reconciled in DECISIONS.md | **Unit (grep)** | `tests/install-contract.test.js` — 1 case | `DECISIONS.md` count assertion updated; matches `find agents -name '*.md' | wc -l` |

**S5 unit-test additions:** 10-12 new tests (one positive + one negative per new validator check, ~5-6 checks).

---

## 2 — Mitigation map (failure-mode → slice)

Cross-checks completeness (8-dim #2). Top failure modes from research → which slice mitigates.

| Failure mode | Severity | Slice |
|---|---|---|
| A1 — Multi-identity SSH (TODAY) | High | S1 (source-block fix) |
| A2 — `IdentitiesOnly yes` hardening | High | S1 (HTTPS bypass) + S3 (R5 row verifies) |
| A4 — Corporate proxy blocks SSH:22 | High | S1 (HTTPS over 443) |
| C1 — Claude Code too old | High | S3 (R7 row diagnoses) + S1 README mention of min version |
| C3 — Node < 22 | High | S5 (validator catches at install-author side); R6 row diagnoses |
| D4 — `plugin.json.version` not bumped → silent staleness | High | S4 (release checklist) + S5 (semver validator) |
| D6 — Agents subdir non-recursion | Medium-High | S2 (verify + restructure) |
| E1 — Agents not in `/agents` UI | Medium | S2 (resolution) |
| E2 — `/sig:*` not visible until `/reload-plugins` | Medium | S1 README install block (includes reload step) |
| F1 — Auto-update off for 3rd-party marketplaces | High | S1 README mention + S4 release-checklist (per-release notification advice) |
| G1 — Sticky cache on uninstall | Medium | S3 troubleshooting subsection in install-verification.md |
| F4 — `.planning/` schema drift across versions | High (latent) | **Deferred → FUTURE-IDEAS** (out of E1 scope) |
| H1 — Stale `/plugin install signal` (pre-rename) in README | High where it hits | S1 (README touch removes it) |

**Coverage:** 12 of top 13 risks mitigated within E1. F4 explicitly deferred — schema versioning is its own epic.

---

## 3 — Test inventory summary

| Source | Existing | New (S1) | New (S2) | New (S3) | New (S4) | New (S5) | Total post-E1 |
|---|---|---|---|---|---|---|---|
| Unit tests | 209 | +8 | +3-4 | +2 | +2 | +10-12 | **~234-237** |
| Manual artifacts | 0 | docs/install-verification.md § Release log | docs/install-verification.md § F2 | docs/install-verification.md § R1–R5 | docs/install-verification.md § Release log (cross-check) | n/a | 1 doc, 4 sections |

**Net: ~25-28 new unit tests + 1 verification doc with 4 sections.**

---

## 4 — Nyquist gate

Every acceptance criterion in `M4.5.E1-PLAN.md` has a row in § 1 above, with named test type, test file, and assertion. Manual rows are explicitly named (artifact = test) and the artifact is committed.

**No criterion is untested.** Plan passes strict Nyquist enforcement.

---

*Created 2026-05-15. Validation output for M4.5.E1 at FULL tier per PROFILE.md. Used by `/sig:execute` to gate per-slice completion.*
