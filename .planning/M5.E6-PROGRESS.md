# M5.E6 — EXECUTE Progress

**Tier:** FULL / strict · **Dispatch:** one executor per slice, sequential (git-index-race-safe).
**Baseline:** 96 files / 1561 tests green (commit `927318d`).

Serialization contract (from PLAN): NO bug-fix task edits `BUGS.md` (→ T25 only); `rmw-lock.test.js` owned by one executor per wave; roster sites (`CLAUDE.md`, `docs/map`) only in T8.

## Wave 1 — foundation + independent bug clears
- [x] Slice 1 (T1–T4) — doc-hygiene param + portable checks — T1 `0b1ebc4` · T2 `ab2ac98` · T3 `6fd514f` · T4 `a92d106`. Verified: 98 files / 1578 tests green; `docs-hygiene.test.js` byte-identical (AD2 held). [⚠ produced under a resolved concurrent-session incident — see note]
- [x] Slice 5 (T10–T13) — FR4 migrate gate flag-not-abort — T10 `cf352f9` · T11 `5ec8a81` · T12 `61f4b67` · T13 `c1a9577`. Verified: predicate is tight AND; migrate tests 27 green (exemptions work + abort still bites); BUGS untouched. 1587 suite green.
- [x] Slice 6 (T14–T16) — FR5 retro-gate fresh REVIEW→SHIP — T14 `8518669` · T15 `0b1ef86` · T16 `340ca5a`. Verified: diff scope = retrospective.js + ship-fr1.test.js only; ship.md/isEpicCloseByState byte-unchanged; predicate tight 3-AND; ship-fr1 22 green; BUGS untouched. 1593 suite green. (T14/T15 are intentional transient-red per-task commits — optional squash deferred to SHIP history-cleanup.)
- [x] Slice 7 + T21 (T18–T20, T21) — FR6 `_afterRead` guard (B29) + FR7 bookkeeping — T18 `58b61a4` · T19 `78c433e` · T20 `6edcb08` · T21 `64b55cb`. Verified: 6 sites use own-property `hasOwn(opts,…)`; full suite 1601 green (no pollution leak); B25 assertions unchanged; BACKLOG entry closed; BUGS/planning-index untouched.
  - **AC7.7 decision (orch):** met via a standalone lock-symmetry assertion (regeneratePlanningIndex wrapper rejects under held lock / resolves on release), NOT literal `RMW_PATHS` membership — that array is coupled to the `_afterRead` it.each and regeneratePlanningIndexCore has no such seam; forcing membership would need a dead test-only seam in planning-index.js (violates surgical-edits). Intent (7th RMW path is lock-protected) is proven. → carry into VERIFY + record in DECISIONS at close.
- [x] Slice 4 (T9) — FR3 docs/map ship-checklist line — `27357c5` (orchestrator-authored, doc-only, staged path-only). Used real tab names "Signal, explained" + "Functionality map". Validator green.

**✅ WAVE 1 COMPLETE** — all 5 slices landed; suite 1561→1601 (+40); guard byte-identical; FR4/FR5/FR6 bugs closed RED-proof; FR3 + FR7-bookkeeping done. Commit hygiene held throughout (STATE/PROGRESS never staged).

## Wave 2 — orchestrator + B31
- [x] Slice 2 (T5–T7) — runSweep + renderSweepReport + command-frontmatter + offline meta — T5 `11b6dd5` · T6 `bca2ab9`+`ed6fb05` · T7 `b82e46c`. Verified: 28 sweep tests green; diff scope = sweep.js+test only; **dogfooded read-only on real Signal repo** (found expected INDEX-stale + 39-inbox true-positives). Two accepted deviations: gate keys on `.claude-plugin/plugin.json` (not root); audit-network-calls.js is dir-based so no per-file add needed (coverage proven).
- [x] Slice 8 B31 (T22–T24) — add-vs-drain cross-lock fix — T22 `f70f008` · T23 `bef3ba3` · T24 `fe8a95a`. Verified: withStateLock called once (captureToDestination); pure helpers lock-free (re-entrancy safe); full suite 1623 green with no hang (AC7.4 no-deadlock); add.test.js untouched (byte-identical); BUGS untouched. Note: pre-existing stale LOCK_TTL_MS comment in add.js flagged for follow-up (not fixed — pre-existing, surgical-edits).

**✅ WAVE 2 COMPLETE** — Slice 2 + Slice 8. Sweep dogfooded read-only on Signal + stranger fixture; B31 interleaving green. Suite 1623.

## Wave 3 — command + roster + BUGS consolidation
- [x] Slice 3 (T8) — commands/sweep.md + roster 17→18 — `78e0c28` (one atomic commit). Verified: 18 command files; roster guard + roster.test green at 18; validator passes; README/BUGS/v0.1.8-snapshot untouched; sweep.md dogfood-clean. Flagged pre-existing map-array drift → B32.
- [x] T25 — flip B27–B31 → fixed + catalog B32/B33 (needs-triage) — `eea284f` (BUGS.md-only atomic commit). Aggregate: `29 fixed (33 total)`. Confirmed via git show + Read.

**✅ WAVE 3 COMPLETE.** Roster 17→18 reconciled; all bug statuses consolidated.

## Epic-complete checkpoint
- [x] Full suite green — **1623 passed / 99 files** (from 1561 baseline, +62)
- [x] Hygiene guard byte-identical — `docs-hygiene.test.js` never touched across the Epic
- [x] `/sig:sweep` dogfooded read-only on Signal's `.planning/` — expected true-positives only (INDEX-stale + 39-inbox); no false-positives
- [x] All 7 FRs' ACs met (FR1/2 sweep · FR3 map line · FR4 migrate gate · FR5 retro-gate · FR6 `_afterRead` · FR7 close-out+B31)
- [ ] `INDEX.md` regen — **deferred to SHIP** (auto `/sig:index` runs there; regenerating now would just re-stale as VERIFY/REVIEW/RETRO add more `.planning/` files)

## Carry-outs for VERIFY / follow-up
- **AC7.7** met via standalone lock-symmetry assertion (not literal `RMW_PATHS` membership) — record in DECISIONS at close.
- **B32** (docs/map COMMANDS array drift) + **B33** (stale add.js LOCK_TTL_MS comment) — cataloged `needs-triage`, out of M5.E6 scope.
- Transient-red per-task commits in Slices 6 & 7 (T14/T15, T18) — optional squash at SHIP's clean-history step.

## Dispatch mechanics (for the retro)
25 commits / 24 tasks (T6 split). Sequential one-executor-per-slice dispatch (git-index-race-safe). **Incident:** a stray orphaned executor (`exec-s1`) from a prior session latched onto a leftover task board and picked up Slice 1 — verified-not-duplicated, caused no corruption; froze both, confirmed with Brett no live parallel session, retired the stray. Every executor held commit hygiene (STATE/PROGRESS never staged) and RED-first with independent orchestrator verification per slice.
