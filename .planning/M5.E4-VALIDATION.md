# M5.E4 — Plan validation (8 dimensions) + Nyquist test-coverage mapping

## 8-dimension validation

| # | Dimension | Verdict | Notes |
|---|---|---|---|
| 1 | **Goal alignment** | ✅ | Every task closes a confirmed `BUGS.md` defect or the FR5 refactor — all serve "no known-defect debt into the re-audit." No task without a bug/FR anchor. |
| 2 | **Completeness** | ✅ | All 7 FRs mapped: FR1→T2.1 · FR2→T1.2/T1.3/T1.4/T2.4 · FR3→T2.2/T2.3 · FR4→T1.1 · FR5→T3.1/T3.2 · FR6→T1.5/T1.6/T4.x · FR7→T5.1/T5.2. Every open bug (B5,B6,B8,B14,B15,B16,B17,B18,B19,B21,B22,B23; B20 dismissed) has a task. |
| 3 | **Dependency correctness** | ✅ | Wave 1 disjoint (parallel-safe). Wave 2 **sequential** (shared files). FR5 after Wave 2 (wraps corrected code). B23(d) after FR5 (tests wrapped `promoteDrainEntry`). FR7 last (needs commit refs). Minimal + accurate. |
| 4 | **Testability** | ✅ | Every AC has a RED-first test (mapping below). No "verify manually" without a test companion. |
| 5 | **Scope discipline** | ✅ | No gold-plating: B14 does **not** dedup the 2 existing helper copies; eslint bounded to "lint runs" (4 fixes, confirmed no backlog); `/sig:sweep`/CLAUDE.md-de-bloat/`OBSERVATIONS.md` explicitly deferred (D-M5E4-1). B18/B8 target residuals only, not rebuild. |
| 6 | **Context feasibility** | ✅ | Each task ≤ ~5 files. **"Wave 2 serial" = sequential fresh-context executors with a commit between each** (the collision-avoidance mechanism), NOT one context holding 4 tasks — each task re-reads the shared file from the committed prior state. FR5's T3.2 is the largest (6 modules) but is mechanical wrap-with-`withStateLock` per fn. |
| 7 | **Risk coverage** | ✅ | The 3 High risks (file-collision, FR5 deadlock, B19 false-green) each have a structural mitigation (serial wave / lock-free core + T3.1 gate / RED-against-`main`). See PLAN risk table. |
| 8 | **Vertical slicing** | ✅ | Each task is a full slice — defect fix **+ its test + green suite** — not a horizontal "all code then all tests" layer. Each is independently shippable/revertable. |

**Validation verdict: PASS (8/8).** No dimension blocks EXECUTE.

## Nyquist test-coverage mapping (AC → test type)

`nyquist_enforcement: strict` — every acceptance criterion maps to a concrete test. Proof-of-fail (RED against `main`) is enforced in VERIFY.

| AC | Test | Type | File |
|---|---|---|---|
| **AC4.1** neither banner on clean `HEAD==origin` +1 | assert `isStaleVsOrigin` + `isStateStale` both non-stale on a bookkeeping-commit fixture | unit (git-fixture) | `state.js` tests |
| **AC4.2** genuine push fires origin | local-behind-origin fixture → banner fires | unit (git-fixture) | " |
| B6 local 3-case | pure+1 silent / real-work fires / mixed fires | unit (git-fixture) | " |
| **B14** evict+add symlink escape rejected | directory-symlink fixture → throws | integration | `tests/path-confine*.test.js` |
| B14 resume read-side + fs-seam intact | escaping resolve refused; injectable-fs tests still green | unit | `resume.js` tests |
| **AC2.x B18** `regeneratePlanningIndex('.')` | write round-trip, correct keys, no corruption | integration | `tests/planning-index.test.js` |
| **B22** escape refused | `](../../../x.md#a)` → hard finding, no outside read | unit | `doc-hygiene` tests |
| **B15**-doc-hygiene truncation flagged | >cap doc → truncation flag | unit | " |
| **B15**-scanner dangle >1MB caught | >1MB fixture, dangle past cap → gate catches | integration | `tests/migrate-*` (shared B23b fixture) |
| **B17** deterministic double-run | `npm test` ×2 green | suite | vitest |
| **B5/B20** lint runs | `npm run lint` exit 0 | tooling | eslint |
| **AC1.1 B19** skip fires + notes preserved | real `62bfa5b~1` INDEX survives `applyMigrate` (delta = only legit referrer-rewrite) | integration | `tests/migrate-index-guard.test.js` |
| **AC1.2 B19** flagged both places | dry-run + apply summary emit "LEFT INTACT" | integration | " |
| **AC1.3 B19** new-format still regen | marker-present INDEX regenerates | integration | " |
| **AC1.4 B19** RED against `main` | fixture fails on current `main` pre-fix | integration (proof-of-fail) | " |
| **B21** map built once | spy: `buildDecisionIdMap` called 1× for N IDs; resolution identical | unit | `tests/migrate-append-log-evict.test.js` |
| **B16** no stray tag | forced-rollback → `git tag -l` empty; success → retained | integration | `tests/migrate-apply-failure.test.js` |
| **AC5.1** migrate no deadlock | `applyMigrate` (with INDEX) completes under held lock | integration | `tests/migrate-lock.test.js` |
| **AC5.2** concurrent writer fails fast | 2nd writer under held lock → lock error, each RMW path | integration | `tests/rmw-lock.test.js` |
| **AC5.3 / NFR2** linear byte-identical | single-session path unchanged | integration | existing suites stay green |
| **B23(a)** re-root→missing dangle | organic (non-injected) dangle → abort+rollback | integration | `tests/migrate-*` |
| **B23(c)** cached empty on abort | `git diff --cached` == '' after abort | integration | " |
| **B23(d)** no re-promote | `→ Promoted` entry not re-selected on 2nd drain | unit | `tests/drain*.test.js` |
| **B8/FR7** command-level flip | `block:true`→`false` across `applyMigrate` vector-1 | integration | `tests/migrate-all-vectors.test.js` |
| **AC7** ledger reconciled | (manual) `BUGS.md` 0 open confirmed; footer counts match | doc check | `.planning/BUGS.md` |

**Coverage gap check:** every AC has ≥1 test; no acceptance criterion is verify-by-inspection except AC7 (a doc-ledger reconciliation, inherently a doc check). NFR5 (suite grows) is satisfied structurally — every task adds tests.

## Last Updated
2026-07-20 (PLAN validation — PASS 8/8; awaiting user approval)
