# M5.E5 — Review Report (v0.1.10 carry-over bug squash)

## Verdict: ✅ PASS

0 Critical, 0 Important. All four fixes are correct, RED-first, and **adversarially verified**. No production code changed in REVIEW — the only in-phase edits were two doc-accuracy corrections to my own VERIFICATION.md that the panel caught, plus cataloguing two deferred findings. Ready for SHIP.

## Method — 3-specialist adversarial panel (independent of the executors)
- **Code-quality** (5-axis), **Security** (OWASP Top 10 + ASVS L2), **Test-integrity** (false-green hunt). Two panels ran **empirical probes**, not static reading: security fired adversarial fixtures (`..`-escaping, anchored, deep-nested, mixed blocks) through the real `applyMigrate`; test-integrity built a **12-row mutation matrix in a throwaway worktree**, breaking the shipped code 12 ways and confirming the right test goes red each time. This is the pass that caught false-greens in M5.E4 (`evict.js`) and M5.E2 (rollback) — this time nothing slipped.

## Critical Issues (must fix before SHIP)
**NONE.** The test-integrity panel confirmed **zero false-greens**: Set-swap → only AC1.4 red; gate-gutted → AC1.3 via the *real unmocked* gate; pure-OR → AC2.2; `commits.length===1` → AC4.4; drop-CONTEXT → AC4.7. Both load-bearing discriminators (AC1.4 vs a `Set`; AC3.1 vs a real read-outside-lock) fail on the wrong implementation. Nothing is tautological.

## Important Issues (should fix)
**NONE.**

## Security Findings (OWASP + ASVS L2)
- **0 Critical / High / Medium. 1 Low (deferred → B29).** Both load-bearing safety controls are **strengthened, not weakened**: B24 gate strength empirically verified (pre-existing dangles survive the reroot with their key invariant; genuinely introduced dangles still abort); B26 is **monotonic toward more enforcement** (row-absence-gated OR — can only require a retro in more cases, never bypass); B6's `git log` option-injection guard (`COMMIT_TOKEN_RE`) covers the new second call. Checklist: **A03 Injection PASS**, **A01 Path-Traversal PASS** (write/move confinement is realpath-based and untouched; the dangling scan is stat-only/read-only), **A04 Insecure-Design PASS**, **A08 Data-Integrity PASS** (with the B29 hardening note). A02/A05/A07/A10 + web headers N/A (offline local CLI).
- **Defense-in-depth confirmed real:** the dangling gate (integrity) and the realpath write-confinement (containment) are independent layers, so a delta-logic bug cannot become a filesystem escape.

## Performance Findings
None actionable. `applyMigrate` is a one-time offline operation; `scanDanglingLinks` is a linear `matchAll` (no catastrophic backtracking); no N+1 / unbounded-fetch patterns. The one micro-nit (N2, a redundant `findEpicStatusRow` parse in the B26 path) is negligible.

## Simplification Opportunities (minor, deferred)
- **N2** — `shipFR1Check` computes `rowStatus` then `isEpicCloseShip` re-parses the same row. Trivial dedupe; negligible cost.
- **S2** — Layer 2's `parseCompletedPhasesFromFrontmatter` (raw-text) and Layer 1's `readState` (real YAML) can disagree on an inline flow-sequence `completed_phases: [...]`; only reachable via a non-canonical hand-edit, Layer 1 still catches. Unify the parsers someday.

## Suggestions (optional, deferred with rationale)
- **S1 — Layer 2 hook omits `profile`** (`check-state-write.js:117`). On SKETCH, Layer 2's *non-blocking WARN* under-fires (falls back to the full pre-SHIP set), and that hook-wiring path is untested. **Non-blocking:** it can only *under*-fire (never wrongly block), Layer 1 (`shipFR1Check`) is the hard gate and *is* tier-correct (receives `profile` from `ship.md:35`), and this is a **net improvement** over the prior structurally-dead `- SHIP` predicate. Deferred: either load the effective profile in the hook or add an intent comment stating the FULL-default is deliberate (Layer 1 owns tier-correctness). Recorded here for the next maintenance pass.
- **N1** — `findEpicStatusRow` treats a literal zero-width `| **E4** ||` status cell as row-absent → could force a retro on a maintained-but-blank row. Implausible (markdown tables pad with spaces). Note only.
- **N3** — AC1.6's named limitation is documented in-code (`migrate-memory.js:1519-1524`) + CONTEXT/VERIFICATION/CHANGELOG rather than the literal "in BUGS.md" of the FR wording (B24 is now cleared from BUGS.md). Substance met; location differs.

## New findings catalogued (deferred — see BUGS.md)
- **B28** (`needs-triage`) — an absolute-path `](/abs/foo.md)` pre-existing dangle in an evicted closed block **false-aborts** the migrate (the two path computations diverge on absolute targets). **Confirmed empirically.** Fail-SAFE (blocks, never escapes), not a regression (false-aborted pre-B24 too), near-zero occurrence. The proper fix ripples into `computeLinkEdits` (reroot machinery) → out of M5.E5's surgical scope. Same disposition as B27.
- **B29** (`needs-triage`) — the `_afterRead` seam reads inherited props (prototype-pollution gadget). **Unreachable today** (0 prod callers; JSON can't carry a function); worst case is a fail-safe `TypeError`. Deferred hardening per the auditor: own-property + `typeof` guard, or a `Symbol`.

## In-phase corrections applied (doc-accuracy only)
1. **VERIFICATION.md AC1.3 citation** L126 → **L313** — L126 was the *injected-scanner* test the AC explicitly warns against; the genuine non-injected proof (real unmocked `scanDanglingLinks`, the FR6-rename-orphan fixture) is at L313. Given M5.E4's false-green was precisely about injected/planted conditions, the citation had to point at the real gate test.
2. **VERIFICATION.md AC3.6 scoping note** — stated plainly that only `applyDispositionToFile` carries a true lost-update discriminator; the other 5 `it.each` paths are lock-enclosure proofs. Prevents "behavioral interleaving, all 6" from being read as six lost-update tests.

## Verdict
- [x] **PASS — ready for SHIP.** 0 Critical, 0 Important. Suite **1561** green (VERIFY + two REVIEW panels); validate + lint clean. Two doc-accuracy corrections applied; B28/B29 + S1/S2/nits deferred with rationale.

## Last Updated
2026-07-21 (REVIEW — PASS)
