# M4.5.E3 — Progress

**Epic:** Public-facing documentation rewrite (audience: self + peers)
**Tier:** FULL (`gate_strictness: strict`, `tdd_required: true`, `nyquist_enforcement: strict`)
**Started + completed:** 2026-05-24 (EXECUTE single-session)

---

## Slice 1 — Privacy posture (3 tasks → 3 commits)

- [x] **S1.t1** — TDD red: `tests/audit-network-calls.test.js` — commit `f7ef937`
  - 3 RED tests landed: existence + executable check, exit-0 on current repo, exit-1 + violation path on seeded fixture.
  - Seeded fixture co-landed at `tests/fixtures/audit-network-calls-seeded/with-fetch.js` so the GREEN test (S1.t2) has something concrete to detect.
  - Stderr/stdout robustness — test reads both streams for violation path (S1.t1 acceptance said stderr, S1.t2 said `console.log` → stdout; both contracts honored).
  - Suite: 384 → 387 (3 RED, as designed).

- [x] **S1.t2** — TDD green: `tools/audit-network-calls.js` — commit `bc36985`
  - Implementation matches 6-pattern set + 4-dir default scope + tests-fixture-override CLI contract from S1.t1.
  - **PLAN DEVIATION:** dropped `got` from the bare-word pattern `\b(axios|got|node-fetch)\b`. Pattern matched English "got" in error messages (13 false-positive hits across `tools/lib/{profile,state}.js` and `tools/validate-plugin.js`). `axios` and `node-fetch` aren't English words; stayed in. `require`/`import` patterns still catch the `got` module being loaded. In-script comment explains the omission.
  - CLI contract: no args = default 4-dir scope w/ excludes; one positional arg = override directory scan (file-type filter still applies; default-excludes do not).
  - Suite: 387 (3 RED → 3 GREEN, no count change).

- [x] **S1.t3** — SHIP — README Privacy section + CHANGELOG — commit `8af4f4b`
  - README `## Privacy & telemetry` inserted between the `.planning/` git-tracking section and `## Command reference`. Names the no-network claim, hands reader the audit command, scopes honestly (source only, not transitive deps), names the bar for future telemetry.
  - CHANGELOG `[0.1.3]` got a new `### Added — Privacy & telemetry posture (M4.5.E3 Slice 1)` block.
  - Suite 387 green; validator green. **Slice 1 closed.**

---

## Slice 2 — Compatibility + Open Source Origins + slim SECURITY + Epic close (7 tasks → 7 commits)

- [x] **S2.t1** — `references/facts.md` canonical source — commit `bd7dc5c`
  - 5 sections (Runtime / Dependencies / Test surface / License + repo). Header explains the contract: update HERE first; the consistency tests catch drift downstream.
  - Test-count field set to 387 as placeholder; updated to 397 at S2.t7.

- [x] **S2.t2** — TDD red: cross-file-consistency tests + jargon-lint helper — commit `4e4f352`
  - `tests/cross-file-consistency.test.js` with 10 test blocks (9 named assertions + 1 facts.md parse preamble).
  - `findJargonHits(content, regex)` added to `tests/helpers/template-lint.js` for the jargon-lint test.
  - At commit time: 6 PASS (Node / Claude Code versions / vacuous test-count / vacuous dep-count / Privacy section heading / parse preamble), 1 SKIP (SECURITY.md not yet created), 3 RED (Compat section heading, docs/map link, Open Source Origins heading + 9 URLs).
  - Suite: 387 → 397 (plan predicted 396 ± 1; +1 above for the parse preamble).

- [x] **S2.t3** — README Requirements & compatibility section + docs/map link — commit `446c40f`
  - Nested `### Requirements & compatibility` 4-row table under `## Install`, replacing the inline `**Requirements:**` prose line.
  - One-line "Visual companion: Signal map" pointer to `docs/map/index.html` under `## Your first project`.
  - Turned S2.t2 tests 7 + 8 GREEN.
  - **Latent test-1 break introduced** — initial table structure split "Node.js" and "22+" across two cells, killing the literal-string match. Fixed forward in S2.t4's commit (bundled there with explicit callout).

- [x] **S2.t4** — README rewrite Credits & Heritage → Open Source Origins — commit `1c306b7`
  - Gratitude-framed intro; warmer subsection labels (Directly ported / Inspiration for v2 / Patterns borrowed / Bridge references / Signal's own contribution). All 9 source-repo URLs + 1-line acknowledgments preserved verbatim. LICENSES.md cross-link retained.
  - Bundled S2.t3 followup — restructured compat table cells so "Node.js 22+" and "Claude Code 2.1.141+" appear as single contiguous strings.
  - Turned S2.t2 test 9 GREEN.

- [x] **S2.t5** — `SECURITY.md` (standard shape, zero Signal jargon) — commit `97af99c`
  - New file at repo root following GitHub `# Security Policy` convention. Four sections (Supported Versions / Reporting / Disclosure / Scope). Zero Signal workflow vocabulary. README footer `## Security` line added.
  - Turned S2.t2 test 5 (jargon-lint) GREEN. **All 397 tests now pass; 0 skipped.**

- [x] **S2.t6** — MILESTONE-4.5.md annotations + FUTURE-IDEAS deferred entry — commit `4b81b62`
  - MILESTONE-4.5.md: status snapshot table updated (E1.S3-S5 ⏸ shelved, E3 ✓ shipped); § E1 and § E3 prose appended with closing notes.
  - FUTURE-IDEAS.md: new "E3 contribution scaffolding — deferred" entry with verbatim re-promotion triggers from D-E3-11.
  - **PLAN DEVIATION:** plan acceptance cited "DECISIONS.md D-E3-12" but D-E3-10/11/12 currently live in `M4.5.E3-REQUIREMENTS.md`, not DECISIONS.md. Annotations cite REQUIREMENTS.md for accuracy. Promoting these decisions to DECISIONS.md per past Epic patterns (M4.5.E6 did this) flagged as a small follow-up.

- [x] **S2.t7** — SHIP — CHANGELOG + Epic close — commit `527ce73`
  - CHANGELOG `[0.1.3]` H2 heading expanded: M4.5.E7 + M4.5.E3.
  - 5 new blocks under `[0.1.3]` (Added facts.md / Added SECURITY.md / Added cross-file-consistency tests / Changed README rollup / Test suite 387 → 397).
  - `references/facts.md` test-count finalized at 397.
  - README `npm test` example line updated from "380+ tests should pass" to "397 tests should pass" (drift cleanup flagged in S2.t3, resolved here).
  - **Slice 2 closed. M4.5.E3 SHIPPED.**

---

## Test trajectory

| Phase | Suite | Notes |
|---|---|---|
| Pre-E3 | 384 | Post-M4.5.E7 baseline |
| Post-S1.t1 | 387 | +3 audit-script wrapper tests (RED) |
| Post-S1.t2 | 387 | Same count; 3 RED → 3 GREEN |
| Post-S1.t3 | 387 | No new tests |
| Post-S2.t1 | 387 | No new tests |
| Post-S2.t2 | 397 | +10 consistency suite (9 named + 1 preamble; 6 PASS, 1 SKIP, 3 RED) |
| Post-S2.t3 | 397 | 397 (2 RED → 2 GREEN; 1 PASS → 1 RED transient; 1 still RED; 1 SKIP) |
| Post-S2.t4 | 397 | 396 PASS, 1 SKIP (all-but-SECURITY-jargon GREEN) |
| Post-S2.t5 | 397 | **397 GREEN, 0 SKIPPED** |
| Post-S2.t6 | 397 | No new tests |
| Post-S2.t7 | 397 | Final |

Plan-time forecast was 396 ± 1; landed at 397, within tolerance.

---

## Anti-rationalization (gate_strictness: strict exit check)

| Temptation | Honest answer |
|---|---|
| "Tests too slow, skip for now" | N/A — full suite ran <1s after every commit. |
| "Works but refactor later" | N/A — no "TODO clean up" left in committed code. |
| "Commit everything at end" | N/A — 10 atomic commits, one per task. |
| "Plan says X but I think Y better" | **2 deviations, both flagged in their commits:** (1) dropped `got` from bare-word audit pattern (English-word false positives — `axios` and `node-fetch` aren't English so stayed); (2) cited `M4.5.E3-REQUIREMENTS.md § D-E3-12` instead of `DECISIONS.md D-E3-12` in MILESTONE annotations (decisions live in REQUIREMENTS.md until/unless promoted). Both surfaced explicitly, not silently substituted. |

## Exit criteria

- [x] All 10 plan tasks completed with atomic commits (commits `f7ef937` → `527ce73`)
- [x] All tests pass (397/397 green, 0 skipped)
- [x] Validator + audit script clean (`Plugin validation passed.` / `audit exit=0`)
- [x] No silently skipped tasks
- [x] Progress file written (this file)
- [x] Anti-rationalization check run (above)

---

## Recommended follow-ups (out of E3 scope; surfaced for triage)

1. **Promote E3 decisions to DECISIONS.md.** D-E3-1-amend, D-E3-10, D-E3-11, D-E3-12, D-E3-NEW-13, D-E3-NEW-14 currently live only in `M4.5.E3-REQUIREMENTS.md`. Past Epic pattern (M4.5.E6) promotes load-bearing decisions to canonical DECISIONS.md. ~30 minutes of writing.

2. **`update STATE.md frontmatter` for E3 SHIP.** Per the EXECUTE-only auto-update protocol (STATE.md schema_version 1), Epic-close should bump `last_completed_task`, `last_decision_at`, and `last_updated_commit`. Not in S2.t7 acceptance — leaving for either a follow-on `/sig:checkpoint` or the next phase command.

3. **VERIFY phase next.** Per workflow, EXECUTE → VERIFY. The VALIDATION.md and PLAN.md acceptance criteria are the inputs; this PROGRESS file is the inputs-side context for VERIFY.
