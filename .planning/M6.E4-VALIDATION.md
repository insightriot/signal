---
epic: M6.E4
tier: FEATURE
phase: PLAN
created: 2026-08-23
---

# M6.E4 — Validation

## Plan validation — `core` dimensions only

`plan_validation_dims: core` at this Epic's FEATURE profile → **3 dimensions**, not 8: goal
alignment, completeness, testability. The other five are not run, and are **not** silently claimed
as passed.

### 1. Goal alignment — **FAILED, then fixed**

The first-draft goal — *"a plan carries what the builder needs, and its own numbers hold up"* —
covered S1 and S3 and **did not cover S2 at all**. It had dropped the **reads** half of the Epic's
own defining phrase, *"what PLAN reads and writes"*.

**The slice was not the problem; the sentence was.** `commands/plan.md` Step 1b is PLAN reading the
inbox, and it is a required step. Goal widened; correction left visible in the PLAN.

**Now:** S1 → *numbers hold up*. S2 → *reads a clean inbox*. S3 → *carries what the builder needs*.
3 of 3 aligned.

### 2. Completeness — **PASS with one gap found and closed**

> ⚠ **Ids are enumerated, never written as an en-dash range.** The first draft of this table wrote
> `FR1.1` (`AC1.1a`–`AC1.1d`) and `checkValidationConsistency` returned **`cannot-evaluate`** —
> *"the completeness dimension and the Nyquist map share no requirement id."* An id that exists only
> inside a range is invisible to the coverage tools, which is **`M5.E10`'s exact documented
> finding**, reproduced here in the artifact whose job is completeness. Expanded so the check can
> actually run.

**Grouping, stated in prose because the checker reads table rows only** (deliberately — a sentence
*about* a requirement is indistinguishable from a row *covering* it): `FR1.1`/`FR1.2` → S1;
`FR2.1`/`FR2.2` → S2; `FR3.1`/`FR3.2` → S3; the four NFRs span all slices.

| Criteria | Slice |
|---|---|
| `AC1.1a`, `AC1.1b`, `AC1.1c`, `AC1.1d` | S1 |
| `AC1.2a`, `AC1.2b`, `AC1.2c` | S1 |
| `AC2.1a`, `AC2.1b`, `AC2.1c` | S2 |
| `AC2.2a`, `AC2.2b`, `AC2.2c`, `AC2.2d`, `AC2.2e` | S2 |
| `AC3.1a`, `AC3.1b`, `AC3.1c` | S3 |
| `AC3.2a`, `AC3.2b`, `AC3.2c`, `AC3.2d` | S3 |
| `NFR1-t1` fail-open | all |
| `NFR2-t1` malformed markdown | S1 |
| `NFR3-t1` names its input | S1 |
| `NFR4-t1` no new dependencies | all |

> ⚠ **Second correction, from running the checker rather than reading it.** The table above was
> first keyed `FR1.1 | AC1.1a…` — FR **and** AC ids in the same row. `checkValidationConsistency`
> then reported **`inconsistent`**, listing all six FRs as *assigned-not-mapped* and `AC1`/`AC2`/`AC3`
> as *mapped-not-assigned*: it takes the completeness table's ids as the assignment namespace, and
> the FR ids dominated. **Tested rather than assumed** — an AC-only copy returns **`consistent`**,
> which is what proves this was the document's shape and not a tool defect. The two compared
> sections now share one namespace; the FR grouping moved to the prose above, which the checker
> ignores by design.

**Gap found at PLAN:** `NFR2` and `NFR3` had **no test in any slice's strategy** — both were written
in REQUIREMENTS and neither was mapped. Added as `NFR2-t1` / `NFR3-t1`.

> ### ⚠ Corrected at VERIFY — this section then published a denominator that was itself two short
>
> After that fix this table read **"24 criteria, 24 mapped"**. `REQUIREMENTS.md` declares
> **`NFR1`–`NFR4`**, and only `NFR2` and `NFR3` ever got rows: `NFR1` (fail-open) and `NFR4` (no new
> dependencies) were **silently absent from the total**. So the artifact whose job is completeness
> announced full coverage against a denominator missing two of its own requirements.
>
> **The true total is 26.** Both omissions were verified directly at VERIFY — `NFR1` with 21 hostile
> calls across three functions (0 throws), `NFR4` against an empty `package.json` diff — and both now
> carry rows above. Left visible rather than repaired in place, because a repaired table reads
> exactly like one that was never wrong.

### 3. Testability — **PASS, with one criterion honestly marked unenforceable**

Every criterion has a test **except `AC1.2c`**, which states that nothing fails if the plan-checker
prompt ignores the worklist. That is `B75`'s shape and is deliberate.

**It is verified as *documented*, not as *enforced*** — the test asserts the disclosure exists at
the point of use. Claiming a test for the behaviour itself would be the exact false-coverage claim
`analysis/CLAIM-INTEGRITY-ANALYSIS.md` is about.

---

## Nyquist test-coverage mapping

`nyquist_enforcement: strict` — every row must be **proven to fail before it passes** (enforced at
VERIFY, not here).

| ID | Criterion | Test type | Test |
|---|---|---|---|
| `AC1.1a` | Detector returns the `M4.5.E9` S1.t11 task | unit | Copied fixture (never a live archive read) → detector returns it |
| `AC1.1b` | Numberless plan → empty + `clean` | unit | Fixture with no numeric tokens → `[]`, status `clean` |
| `AC1.1c` | `cannot-check` ≠ `clean` | unit | Absent/unreadable plan → `cannot-check`; asserted **distinct** from `clean` |
| `AC1.1d` | Reach published | unit | `REACH['plan-internal-consistency']` exists; prose via `describeReach` |
| `AC1.2a` | `plan-checker.md` names the detector | cross-doc | Grep the agent file for the detector + per-task obligation |
| `AC1.2b` | Folds into `testability`, count stays 8 | cross-doc | Assert 8 dimensions; assert obligation sits under `testability` |
| `AC1.2c` | Residual disclosed | cross-doc | Assert the not-enforced disclosure is present at the point of use |
| `AC2.1a` | `standing: true` on marked entry | unit | Marked fixture → `standing === true` |
| `AC2.1b` | Header-scoped + fence-aware | unit | Marker in a fence / deep in body → **not** marked |
| `AC2.1c` | `parseTriggerWatchlist` untouched | unit | Its existing tests pass unchanged; no new call sites |
| `AC2.2a` | Excluded from candidates | unit | Marked entry absent from `listDrainCandidates` |
| `AC2.2b` | Retrievable as its own category | unit | Standing entries returned as a distinct group |
| `AC2.2c` | **This repo: 0 live + 1 standing** | integration | Run against the real inbox; assert the counts |
| `AC2.2d` | No-marker project byte-identical | regression | Unmarked corpus fixture → candidate list identical to today |
| `AC2.2e` | No `--standing` flag | cross-doc | Assert `add.md` gained no such flag |
| `AC3.1a` | RESEARCH in executor inputs | cross-doc | `executor.md` `## Inputs` names `{phase}-RESEARCH.md` |
| `AC3.1b` | Exemplar followed, not re-derived | integration | Task naming an exemplar → executor references it |
| `AC3.1c` | No bulk injection | cross-doc | Assert dispatch passes a reference, not the document |
| `AC3.2a` | **Files likely touched** in `plan.md` | cross-doc | `plan.md` contents list ∩ planning skill fields — **agree** |
| `AC3.2b` | **Out of scope** field exists | cross-doc | Named in the same list |
| `AC3.2c` | Folds into `completeness` | cross-doc | Assert placement under `completeness` |
| `AC3.2d` | Advisory, not fatal | unit | Plan lacking fields → reported, **not** failed |
| `NFR2-t1` | Malformed markdown never throws | unit | Dangling fence / broken table → returns, does not throw |
| `NFR3-t1` | `cannot-check` names its input | unit | Assert the message identifies **which** file |
| `NFR1-t1` | Every check fails open | unit | Hostile inputs across all new parsers → no throw |
| `NFR4-t1` | No new dependencies | integration | `git diff main..HEAD -- package.json` is empty |

**26 criteria, 26 mapped.** (Was published as 24 of 24; see the correction above.)

## Dogfood scheduled at VERIFY

Run S1's detector against **`M6.E4-PLAN.md` itself**. This plan states `28.6%`, `34/119`, `74
packages` and `v22.13.0` alongside acceptance criteria, so it is exactly the shape the detector
flags. An Epic that ships a plan-consistency detector without ever pointing it at its own plan would
be this Epic's own defect class — *shipped but never run*, the predictor `M5.E17` FR1 exists to
catch.
