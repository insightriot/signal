# M5.E6 — Requirements (doc-runtime close-out — the maintenance-command half)

**Tier:** FULL / strict · **Source:** `BACKLOG.md` Sprint-3 residual (`/sig:sweep`, CLAUDE.md de-bloat, `docs/map` line, concurrency-lock) + `BUGS.md` B27/B28/B29/B30. Decisions locked in DISCUSS: **D-M5E6-1 … D-M5E6-4** (`DECISIONS.md`, 2026-07-22).

**Why now:** finish the doc-runtime so its self-maintenance is 100% locked *before* the v2-port re-audit (BR-8). E1–E3 shipped the *structure* (doc-model, eviction, migrate, index, hygiene guard); this Epic ships the *maintenance-command* half + clears the four M5.E5 carry-over bugs so nothing dangles into the re-audit.

Every FR is RED-first (strict Nyquist): a failing test lands before the fix/feature. ACs below are the Nyquist map for PLAN.

**Sequencing intent:** FR1–FR3 (the sweep command + its checks) are the anchor; FR4–FR6 are the bug clears (independent, parallelizable); **FR7 is sequenced LAST** — re-scoped at PLAN (D-M5E6-5) from "build the lock" (already shipped by M5.E4 FR5 + M5.E5 B25) to close-out bookkeeping + the **B31** cross-lock fix (a bounded `/sig:add` RMW refactor). Last because it touches the load-bearing `/sig:add` command; NOT droppable (B31 is a real, Brett-approved fix, not the vanished original build).

---

## FR1 — `/sig:sweep`: on-demand doc-hygiene sweep over the invoking project (D-M5E6-1)

A new meta command (same class as `/sig:status` / `/sig:migrate-memory` — no tier-gating preamble, no skills, no agents) that scans **the invoking project** (`process.cwd()`, never hard-coded Signal paths) for doc rot and prints a human-readable report. **Read-only** — it detects and reports; it never mutates (`--fix`/auto-remediation is Not-Doing, deferred). The name is the one reserved by the BR-1 `/sig:audit` split; `/sig:audit` (Sprint 5) keeps the readiness scorecard.

**Scan scope (the crux — differs from the existing test-suite guard):** `.planning/` (its own docs — STATE, CONTEXT, BACKLOG, BUGS, ISSUES-INBOX, DECISIONS, INDEX, retros, milestone files), **exempting `archive/`** (mirroring migrate's R7 "historical, not links to repair" exemption), plus repo docs (`docs/`, `README`, `analysis/`) and `CLAUDE.md`. The existing `doc-hygiene.js` `WALK_IGNORE` deliberately skips `.planning/`; FR1 parameterizes scan scope so sweep can include it while the test-suite guard is unchanged.

**Portable-vs-Signal-only check split:** portable checks (meaningful in any Signal-managed repo, including a stranger's) run everywhere; Signal-repo-only checks (need a command roster / `plugin.json`) **auto-skip** when `plugin.json` is absent — no false positive on a stranger repo, no no-op noise.

**Acceptance criteria**
- **AC1.1** `/sig:sweep` run in an arbitrary project dir scans *that* project's `.planning/` + docs (asserted against a fixture repo with a distinct baseDir), never Signal's own paths. No hard-coded Signal path in the code path.
- **AC1.2** `.planning/` docs are scanned; `.planning/archive/**` is exempt (a broken link inside `archive/` is NOT reported).
- **AC1.3** With no `plugin.json` present, the Signal-only checks (roster, version, command-frontmatter) are skipped — the report shows only portable findings and states they were skipped, not failed.
- **AC1.4** With `plugin.json` present (Signal itself), all checks run.
- **AC1.5 (read-only)** A sweep run mutates zero files — asserted by a before/after tree hash over the fixture.
- **AC1.6** The report groups findings by severity: **structural** (dead link, index drift, roster/version drift — the things the test-suite guard hard-fails on) vs. **advisory** (bloat, stale-inbox nudges). Exit is report-only; no process-exit contract in v1 (deferred with `--fix`).
- **AC1.7** The command markdown (`commands/sweep.md`) exists and is registered; roster count bumps 17 → 18 and the FR4 hygiene guard + README reference reconcile (release-cut task, tracked).

## FR2 — Sweep check set (D-M5E6-1)

The checks FR1 runs, reusing `doc-hygiene.js` internals where they exist and adding the new ones.

**Portable (any repo):**
- Dead internal links — reuse `checkInternalLinks`. It matches **only `](*.md)` inline links** (line 117) — prose `D-ID` mentions and the ~669 index-resolvable prose refs (M5.E3 catalogue) are out of scope *by construction*, so there is **no prose false-positive flood**. **But scope-widening to `.planning/` is a *validated* change, not assumed-free:** PLAN must confirm against Signal's real `.planning/` that `D-ID` **anchor** links (`](DECISIONS.md#d-…)`) and post-eviction `](archive/…)` links resolve-or-are-exempt, not mis-flagged. This may split the portable set into "ports cleanly" (`[FILL IN]`) vs. "needs `.planning/`-aware validation" (dead-links).
- `[FILL IN]` stubs — reuse `checkFillInStubs`, scope widened (clean port — no anchor/D-ID semantics).
- **`.planning/INDEX.md` freshness (new)** — the **pure compose-and-diff** of AC2.1 (`enumeratePlanningDocs` → `renderPlanningIndex` → diff on-disk), **never** the writing Core; drift is a structural finding.
- **Stale-inbox nudge (new, advisory)** — count undrained `ISSUES-INBOX.md` entries; report as advisory (a "consider draining" nudge), never a hard failure. Deterministic count, no judgment pass.
- **CLAUDE.md bloat (new, advisory)** — size threshold (advisory finding); no auto-edit.

**Signal-only (needs `plugin.json` / roster):**
- Roster counts — reuse `checkRosterCounts`.
- Version consistency — reuse `checkVersionConsistency`.
- **Command-frontmatter freshness (new)** — each `commands/*.md` frontmatter present + consistent with the roster (no stale/missing description).

**Acceptance criteria**
- **AC2.1** Index-freshness uses the **pure compose path** — `enumeratePlanningDocs` → `parseExistingAnnotations(on-disk)` → `renderPlanningIndex(docs, annotations)`, diffed against on-disk `INDEX.md`. It **MUST NOT** call `regeneratePlanningIndexCore` (which `atomicWrite`s on drift — `planning-index.js:323`) or `regeneratePlanningIndex` (its FR7 self-locking wrapper): a read-only sweep cannot invoke a writer (would violate AC1.5). Drifted index → structural finding; fresh → pass; **zero writes** either way.
- **AC2.2** Stale-inbox: N undrained inbox entries → advisory finding reporting N; 0 entries → no finding. Never structural.
- **AC2.3** CLAUDE.md bloat: a CLAUDE.md over the threshold → advisory; under → none. Threshold recorded as a named constant.
- **AC2.4** Command-frontmatter freshness runs only under the Signal-only gate; a command md with a missing/empty frontmatter description is flagged.
- **AC2.5** Every portable check runs on a fixture stranger repo (no `plugin.json`) and produces correct findings; the Signal-only checks are absent from that run.
- **AC2.6** Determinism + offline: two sweeps of the same fixture yield byte-identical reports; no network access (mirrors the existing guard's contract).
- **AC2.7 (`.planning/` dead-link surface validated)** The dead-link check run over Signal's own `.planning/` produces **zero *false* positives** — every flagged link is a genuine dead link; `D-ID` anchor refs and index-resolvable prose refs are not mis-flagged. (Real dead links, if any, are reported as intended — this AC is about no false flood, and forces the FR2 PLAN validation.)

## FR3 — `docs/map` refresh checklist line in `ship.md` (BACKLOG Sprint-3, scope-widened 2026-07-21)

One checklist line in `commands/ship.md` prompting, at each meaningful release, an explicit evaluate-and-confirm of whether the public `docs/map` needs updating — covering **both** screens: the structure/functionality view (`index.html` data) **and** the "Signal, explained" tab (mirrors `docs/signal-explained.md`). "No change needed" is a valid, expected outcome; the point is the *evaluation* happens every cycle.

**Acceptance criteria**
- **AC3.1** `commands/ship.md` carries a checklist line naming both the map data and the explainer doc/tab.
- **AC3.2** The line frames "no change needed" as a valid outcome (not a forced edit).
- **AC3.3** No mechanical enforcement in v1 (it's a human checklist line, not a test) — explicitly scoped as Stage 1; Stages 2/3 remain parked in BACKLOG.

## FR4 — Migrate dangling-gate: flag-not-abort for historical link shapes (B27 + B28, D-M5E6-4)

`applyMigrate`'s dangling-link gate must **flag** (not abort) a *pre-existing* dangling link of these two shapes, matching R7's existing archive-**prose** exemption:
- **B27** — an archive-doc **inline** `](*.md)` link whose target was renamed by an FR6 rename (e.g. `](../../FUTURE-IDEAS.md)` → `ISSUES-INBOX.md`).
- **B28** — an **absolute-path** `](/abs/foo.md)` `.md` link that is a pre-existing dangle inside an evicted closed block (the `scanDanglingLinks` `resolve()` vs `computeLinkEdits` `posix.join()` abs-key divergence).

The gate must still **abort** on a genuinely migrate-*introduced* dangle. The exemption is scoped to archive/closed-block + absolute-path shapes, never a blanket relaxation.

**Acceptance criteria**
- **AC4.1 (B27)** A stranger-repo fixture whose archive doc inline-links an FR6-renamed target **migrates successfully** (flag emitted, no abort/rollback). RED-first: fails against `main`.
- **AC4.2 (B28)** A fixture with a pre-existing absolute-path `.md` dangle inside an evicted closed block **migrates successfully** (flag, no false-abort). RED-first.
- **AC4.3 (gate not weakened)** A genuinely migrate-introduced dangle (relative OR absolute) still aborts + rolls back byte-identical — a non-injected discriminating test.
- **AC4.4** The B27 flag surfaces under a **distinct** `renderDryRun` line — e.g. `Archive links to FR6-renamed targets (N) — R7-historical, flagged not aborted` — **NOT** the "Pre-existing dangling links" section (the B27 link resolves *before* the rename, so it is not pre-existing-dangling; calling it that violates the real-names norm). `applyMigrate` captures `enforceNoDangling`'s returned `{flags}` (currently dropped at `:2458`) into `result.warnings`. One shared projection helper feeds both dry-run and apply so they stay consistent. (B28's link IS a genuine pre-existing dangle → it stays in the existing "Pre-existing dangling links" section; post-fix apply simply agrees and doesn't abort.)
- **AC4.5** B27/B28 move `needs-triage` → `fixed` in `BUGS.md`; the R7-consistency rationale is recorded.

## FR5 — Retro-gate fires on a fresh REVIEW→SHIP flow (B30, P2)

The FR1 retro pre-check (`/sig:ship` Step 0.5) must enforce the retro requirement on a fresh REVIEW→SHIP flow, where today it skips because it runs at `phase: REVIEW` (before the SHIP transition), and B26's STATE-based fallback only fires once `phase === SHIP`.

**Fix approach → PLAN decides between:** (a) `/sig:ship` transitions to SHIP *before* its own FR1 pre-check; (c) re-run the pre-check *post-transition*. **PLAN must not pick blind:** approach (a) means a subsequent halt leaves the project already-transitioned to SHIP (recovery implication); (c) re-run-post-transition avoids that and is the cleaner default. (Approach (b) — broadening `isEpicCloseByState` to "about-to-close" — risks false-fires and is disfavored.)

**Acceptance criteria**
- **AC5.1** On a fresh flow (`phase: REVIEW`, `completed_phases` through the tier's last pre-SHIP phase, retro missing), `/sig:ship` **halts** with `NO_RETRO_FILE` rather than shipping. RED-first: reproduces the M5.E5-SHIP dogfood miss.
- **AC5.2** Once a valid retro exists, the same flow proceeds.
- **AC5.3** No regression to B26's Layer-1/Layer-2 behavior (all `tests/ship-fr1.test.js` cases pass).
- **AC5.4** The chosen approach's recovery implication (already-transitioned-on-halt, if (a)) is documented or avoided (if (c)).
- **AC5.5** B30 moves `needs-triage` → `fixed`.

## FR6 — `_afterRead` seam prototype-pollution guard (B29, defense-in-depth)

The 6 RMW Cores that read `opts._afterRead` must gate on an own-property + `typeof` check (`Object.hasOwn(opts, '_afterRead') && typeof opts._afterRead === 'function'`), or key the seam on a `Symbol`, so an inherited `Object.prototype._afterRead` can never reach the awaited-under-lock path. Currently unreachable (0 prod callers, JSON/CLI can't carry a function) — this is hardening, not a live fix.

**Acceptance criteria**
- **AC6.1** With a polluted `Object.prototype._afterRead = fn`, the 6 Cores do NOT invoke it (a RED test that trips against the current destructure and passes after the guard).
- **AC6.2** The legitimate test seam (own-property function) still works — the B25 interleaving test is unaffected.
- **AC6.3** Prod path byte-identical (default `undefined`, no caller).
- **AC6.4** B29 moves `needs-triage` → `fixed`.

## FR7 — Concurrency close-out: AC7.1 bookkeeping + B31 cross-lock fix (D-M5E6-3, re-scoped D-M5E6-5) — *sequenced last*

**Scope corrected at PLAN (D-M5E6-5).** The original "build the lock" premise was **stale** — M5.E4 FR5 + M5.E5 B25 already locked every named RMW path (`captureCheckpointContext`, `promoteDrainEntry`, `evictTerminalToLedger`, `applyDispositionToFile`, `regenerateIndex`, `generateMilestoneMetaRetro`, `regeneratePlanningIndex`) in `withStateLock`, verified path-by-path with mutex-sufficiency RED-proven in `rmw-lock.test.js` (`M5.E6-RESEARCH.md` §FR7). The old AC7.1–7.4 are satisfied in shipped code. FR7 is therefore **(a) close-out bookkeeping** plus **(b) the one genuine remaining gap — B31 — surfaced by that verification.**

**(a) Close-out:** mark the `BACKLOG.md` "Concurrency-lock the doc-runtime RMW paths" entry done (credit M5.E4 FR5 + M5.E5 B25); optional coverage symmetry — add `regeneratePlanningIndex` to `RMW_PATHS` in `rmw-lock.test.js`.

**(b) B31 — add-vs-drain cross-lock inbox contention (folded in, Brett-approved 2026-07-24).** `/sig:add` writes `.planning/` docs (inbox / OPEN-QUESTIONS / BUGS) under `.add.lock` while drain/ship write the SAME files under `.state.lock` → different mutexes → concurrent lost-update (inside D-M5E6-3's threat model; the existing per-path locks don't cover it). **Fix:** route add's doc-**write** step through `.state.lock` (the coarse mutex drain uses) as a short non-interactive RMW Core — re-read the file *inside* the lock → pure-insert via the existing `insertFutureIdeasEntry`/`insertIntoHoldingSection`/`insertAtEnd` helpers → `atomicWrite`. The sensitive-data scrub prompt + entry-building stay **outside** `.state.lock` (guarded by `.add.lock` as today, for prompt-dedup + 30s TTL) so a slow interactive prompt never holds the coarse lock. No re-entrancy (the Core calls `atomicWrite` directly, not a self-locking fn); lock ordering is add-only (`.add.lock` → `.state.lock`, no cycle — drain takes only `.state.lock`).

**Acceptance criteria**
- **AC7.1** BACKLOG "concurrency-lock" entry marked done, crediting M5.E4 FR5 + M5.E5 B25.
- **AC7.2 (B31 RED)** A concurrent-writer interleaving test — an `/sig:add` inbox-write vs a drain disposition on the same inbox — shows a **lost update against `main`** (add under `.add.lock`, drain under `.state.lock`) and **no lost update after the fix**. RED-first (twin-discriminated, per the B25 harness shape).
- **AC7.3 (B31 fix)** add's inbox / OPEN-QUESTIONS / BUGS write step runs under `.state.lock` as a re-read-inside-lock RMW; mutually exclusive with drain's disposition writes on the same files.
- **AC7.4 (no re-entrancy / no deadlock)** add's `.state.lock` write Core calls no self-locking function; a test proves add completes under the lock (would hang on a naive nested self-lock).
- **AC7.5 (prompt not under coarse lock)** the sensitive-data scrub decision precedes `.state.lock` acquisition; the lock section is non-interactive (`.add.lock` retained for the prompt phase only). Asserted structurally.
- **AC7.6** Existing `tests/add.test.js` + `tests/rmw-lock.test.js` stay green; single-session add behavior byte-identical. B31 moves `needs-triage → fixed` in `BUGS.md`.
- **AC7.7 (coverage symmetry, optional)** `regeneratePlanningIndex` added to `RMW_PATHS`.

---

## Non-Functional Requirements (FULL-tier check)

The FULL NFR prompt (health/liveness probe, graceful shutdown, structured request logging, security headers, rate limiting) is **N/A — this Epic ships CLI + hook code with no network surface**: `/sig:sweep` is a local read-only command, the bug fixes touch local file/state machinery, and the concurrency lock is a local filesystem primitive. Nothing listens on a socket. Stated explicitly (not skipped) to clear the gate.

Applicable NFRs:
- **NFR1 (determinism + offline)** — `/sig:sweep` and all its checks are deterministic and offline (mirrors the existing `docs-hygiene.test.js` guard contract). AC2.6.
- **NFR2 (no new deps)** — reuse `doc-hygiene.js`, `planning-index.js`, `file-lock.js`; no new runtime dependency.
- **NFR3 (cross-platform)** — sweep and the fixes are platform-agnostic (NOT macOS-gated like `/sig:doctor`); path handling via `posix`/`toPosix` as the existing guard does.
- **NFR4 (test-seam inertness)** — the FR6 guard and any FR7 seam default inert in prod (mirrors `_afterRead`/`renameFn`).

## Not Doing (explicit — anti-creep)

- **`/sig:sweep --code`** — source-code rot sweep. Undefined scope; its own bet, later.
- **Inbox-curation ("Dreaming") pass** — the judgment-based inbox reorganization. Not a deterministic check; belongs with the compound work.
- **`/sig:sweep --fix` / auto-remediation** — sweep is detect-and-report in v1; migrate remains the fixer.
- **OBSERVATIONS.md passive capture** — deferred to the Sprint-4 compound Epic (D-M5E6-2): fuzzy Stop-hook mechanism, composes with retro/compound loop.
- **The v2-port re-audit (BR-8)** — follows this Epic; gates the speculative feature ports.
- **`docs/map` refresh Stages 2/3** — only the Stage-1 checklist line here (FR3).

## Last Updated

2026-07-22 — M5.E6 DISCUSS complete. Decisions D-M5E6-1…4 in `DECISIONS.md`. Awaiting PLAN.
