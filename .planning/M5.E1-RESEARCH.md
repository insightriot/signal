# M5.E1 — Doc-runtime & memory hygiene — Research

**Phase:** PLAN. **Tier:** FULL / `research_parallelism: 4`. **Date:** 2026-07-16.
**Method:** 4 parallel agents — doc-model prior-art, eviction code-seams, Curator design-mining, risk register — cross-validated. Primary sources read (not summarized from memory): Curator repo (`~/dev-biz/curator/`, `~/.claude/agents/{auditor,distiller}.md`, `~/.claude/skills/curator/SKILL.md`), Cline Memory Bank, planning-with-files, compound-engineering, AGENTS.md hierarchy, agent-memory literature; and Signal's own `tools/lib/{state,drain,retrospective,status}.js`, `references/state-schema.md`, `commands/{ship,checkpoint,status,plan}.md`, `hooks/`, `.planning/{INDEX.md,STATE.md,archive/}`, `examples/Example-cmmc-STATE.md`.

---

## 1. The canonical doc-organization model (FR1) — provisional-canonical

**The core insight: two orthogonal axes classify every `.planning/` file.** HOT/WARM/COLD alone can't explain why FUTURE-IDEAS evicts but DECISIONS doesn't — the second axis does.

- **Load-frequency (already in INDEX.md):** HOT (load by default) · WARM (on-demand) · COLD (archived). = academic heat-driven eviction; Signal already has it.
- **Growth-policy (NEW — the planner-facing axis):**
  - **working-set** → evicts on close (STATE.md body, active-Epic scaffolding) — FR2b
  - **inbox** → evicts on ship/promote (FUTURE-IDEAS.md) — FR3
  - **append-log** → grows by design, bounded by TOC+grep, never loaded whole, never evicted (DECISIONS.md)
  - **spine** → small, curated, permanent (INDEX.md, RETROSPECTIVES.md)

**Eviction destination — one rule (closes the FR2a/FR2b ambiguity): unit-homed single-home.** Closed content joins *its own unit's* archive folder:
- Closed Epic scaffolding + that Epic's evicted STATE narrative → `.planning/archive/<milestone>/<epic>/` (pattern already exists; **no SUMMARY cards there yet — that's the gap FR2b fills**).
- Closed milestone doc → `.planning/archive/milestones/`.
- Shipped/promoted FUTURE-IDEAS entries → an archive ledger (FR3).
- Dated snapshots (BACKLOG-REVIEW) → archive + pointer (FR5, deferred E2).
- **`STATE-HISTORY.md` is NOT a general destination** — it is *only* the one-time bucket for pre-existing un-unitized legacy *body* prose (FR2a's new-migration case). Evict-on-close goes to the Epic archive folder, not here.

**SUMMARY card = the RETROSPECTIVE (PLAN ratifies).** Don't mint a parallel `SUMMARY.md` family — Signal already has the retro spine + `RETROSPECTIVES.md` + the INDEX one-liner; a third artifact is a new copy to keep faithful (single-home violation). Consequence PLAN accepts: the retro's shape leads with outcome + decisions-with-IDs + what's-open (the "card"), then reflection.

**Read-first index — tiered (keeps the index itself bounded).** A flat root INDEX whose COLD table lists every closed Epic recreates the unbounded-growth problem this Epic kills. So: root INDEX = live working-set + one pointer per milestone; each `archive/<milestone>/` carries its own sub-index. Full build is **FR4 (E2)**; E1 records the shape and preserves INDEX.md's current **hand-curated** stance.

> **Flagged for E2, not E1: derived-vs-hand-curated INDEX.** Curator's Rule 3 = index is generated, never hand-edited. Signal's `INDEX.md` is hand-curated by locked decision (`curator-dormant-on-signal-planning`). These conflict. It bites at FR4 (index-freshness-in-validator), which is E2 — so E1 keeps hand-curation and defers the call. Do not paper over it in E2.

**Faithfulness = an ordered gate, reconciled across sources.** The move is **loss-proof by construction** (byte-identical, zero-loss word-accounting, original survives in archive) — so the only residual risk is *legibility degradation* if the card misrepresents. Curator has **no pre-archive summary verifier** (the summary is written after the move) — so don't build a phantom one. Instead:
- **distill** (judgment): lift-verbatim-don't-rewrite, cite evidence, single-home, IDs verbatim;
- **verify-against-source** (the gate): read the card against the *already-archived* original, confirm no material claim dropped/distorted (+ a deterministic coverage backstop: every `M*.E*`/`D-*`/`FR*`/`AC*` ID, ISO date, and status token survives into the card or an explicit dropped-list);
- **then evict** the live narrative. Plus an Auditor-style **closed-vs-live confirm** before any archive move.

---

## 2. The three bloat vectors (the pivotal finding — FR2a alone doesn't fix the acute case)

| Vector | Where | Evidence | Prevented by | **Remediated by** |
|---|---|---|---|---|
| **1. Frontmatter-list prose** | multi-paragraph prose crammed into `completed_phases`/`blockers` YAML entries | **The acute 455 KB `examples/Example-cmmc-STATE.md`** — a ~550-word SEC1 narrative inside one `completed_phases` item (frontmatter runs lines 1–75, prose-wrapped) | ✅ v0.1.6 write-guard (`checkStateFrontmatterShape`) blocks *new* ones | ❌ **not yet** — a one-time "de-prose" transform → **E3 migrate command** (CMMC file is its fixture) |
| **2. Inlined legacy body** | `upgradeStateFile` inlines the *entire* legacy file into the new body (state.js:188) and preserves it verbatim forever | **Signal's own STATE.md** — 64 KB body under the migration notice (line 15+) | — | **FR2a** for *new* migrations; existing ones via S3's move-helper in the **E1 dogfood** |
| **3. Ongoing body accretion** | closed-Epic narrative piles into the STATE body, never leaves | Signal's body will keep growing per Epic | — | **FR2b** evict-on-close (E1) |

**Consequence for the plan:** FR2a as originally worded ("fixes the acute 455 KB case at its root") is **wrong** — the acute case is vector 1 (frontmatter prose), which FR2a (a body mechanism) never touches. E1 covers vectors 2-new (FR2a) + 3 (FR2b) + shrinks Signal's own vector-2-existing in the dogfood. The **acute vector-1 remediation is routed to E3's migrate command**, where the CMMC file is the real-world test fixture. v0.1.6 already stopped vector 1 from recurring.

---

## 3. Seam map (FR1/FR2/FR3 — E1 scope)

- **FR2a** — `tools/lib/state.js:188` (`body = \`${notice}\n\n${raw}\``). Change: write `raw` → `.planning/STATE-HISTORY.md`, body = pointer + `MIGRATION_NOTICE_TEMPLATE` (rewritten — line-15's "everything below is history" becomes false). Preserve idempotency guard (170-172). Reuse `stringifyFrontmatter`, `atomicWrite`. **Not retroactive.**
- **FR2b** — new body-eviction helper (nothing today edits the body; `writeStateFrontmatter` preserves it verbatim, 422-427). Attach: `commands/ship.md` §5 + `commands/checkpoint.md` step-5. Scope to `current_epic`. **Write-amplification landmine:** every state mutator (~8 sites, all `withStateLock`) re-serializes the whole body — bloat is a *runtime* cost, not just read. Programmatic `atomicWrite` bypasses the PreToolUse hook; `checkStateFrontmatterShape` never inspects the body → no guard interaction.
- **FR2c** — `references/state-schema.md` (line 5 explicitly says "write whatever notes"; no skeleton today). Default body in `initState` (state.js:233-234) — update to match. Enabling structure for FR2b → sequence with/before it.
- **FR2d** — `tools/lib/state.js:1007-1081`: `detectStateSize(bytes, threshold)` **already takes a threshold param**; `readStateSize` hardcodes 150 KB. Thread PROFILE tier→threshold via `readProfile` (async) in a command-layer wrapper. **Sync→async landmine:** keep `readStateSize`/`readStateSizeBanner` synchronous (`statSync`; `tests/state-size.test.js` depends on it). **Stale-message landmine:** `detectStateSize`'s message (~1036) says "Automated eviction is planned for M5…" — self-referentially stale the moment this ships; rewrite it (docs-accurate norm).
- **FR3** — `tools/lib/drain.js`: `transformBlock` (309-330) stamps promote/defer **in place** (why FUTURE-IDEAS only grows, now 187 KB); delete/merge return `''` with **no ledger**. Net-new: (a) a move-to-ledger branch, (b) a **two-file atomic** helper (`applyDispositionToFile` is single-file today). `applyDispositions` already applies descending-offset (removal-safe). **Don't rebuild** the 3 disposed-detection REs (v0.1.6) — reuse them to recognize already-stamped entries.
- **DO NOT REBUILD (v0.1.6 prevention half):** `checkStateFrontmatterShape` write-guard; the read-time size-banner trio; the drain don't-resurface REs.

---

## 4. Curator: borrow the behavioral spine, no dependency

**Borrow (reimplement Signal-native):** clerk/librarian split (Node mover extending `tools/archive-migrate.mjs` + a judgment distill step at ship); read-first INDEX as derived projection; `archive/<unit>/` + one-page card; Distiller faithfulness discipline (= the FR2b distill spec); Auditor closed-vs-live gate + drift sweep (= FR4 + the pre-move gate); safety rules (never move live-zone, dry-run default, byte-identical `git mv`, zero-loss word-accounting, unit-boundary-only = FR6 NFRs); the two-speed cadence (deterministic-on-commit / judgment-at-boundary).

**Leave (solves problems Signal doesn't have):** the Python CLI + `.curator.yml` + taxonomy discovery (Signal's taxonomy is locked); OKF frontmatter / `llms.txt` (would collide a *second* frontmatter axis onto `schema_version`/`docs_layout_version`, and no external consumer); full reference graph / `graphify`; `log.md` (DECISIONS/RETROSPECTIVES cover it); the per-commit engine hook (FR4's validator-wiring is the native equivalent).

---

## 5. Risk register — top items shaping the plan

- **R1 Distillation faithfulness (High)** → its own gate step in S3 (distill→verify-against-source→evict + coverage backstop + carry-over lift). Not buried in eviction.
- **R2 FR2a targets the wrong population (High)** → resolved by the 3-vector finding above; acute vector-1 → E3; FR2a scoped to new migrations only.
- **R3 Evict-on-close carry-over burial (High)** → scope to `current_epic` (not heuristic GC); distill lifts open carry-overs/blockers UP to the live section.
- **R4 FUTURE-IDEAS byte-range eviction (Med-High)** → ledger-first hash-keyed atomicity; terminal-only predicate (not Deferred); dangling-fence recover+warn+exclude.
- **R5 Dogfood-on-self (Med)** → discrete committed step, snapshot-first, no phase transition mid-flight; Signal's 64 KB file is *under* the 150 KB banner so the CMMC file is the pathological fixture; verify `/sig:resume` orients identically post-dogfood.
- **R6 `docs_layout_version` axis (Med, FR7/E3)** → zero JS consumers today (clean); FR7's SessionStart read must use `parseFrontmatter` (fail-open), never `readState` (fail-closed on schema mismatch).
- **Awareness:** `STATE-HISTORY.md` is unguarded (no size banner, no write-guard) — acceptable for an archive, but nothing watches the file STATE shrinks *into*.

---

## 6. Environment check
Node 22+ (dev on 22.13.0), vitest, no new runtime/native deps (all internal JS + markdown). `npm install` unaffected — no manifest change anticipated. No version-drift risk for this Epic.

## Last Updated
2026-07-16 — 4-agent research synthesized; 3-vector bloat finding pinned from the real CMMC file.
