---
epic: M6.E7
tier: FEATURE
phase: VERIFY
verified: 2026-09-06
verification_pass: 2
---

# `M6.E7` — Verification

## Verdict

**PASS with documented limits.** *(Pass 2. Pass 1's verdict is superseded and kept below where it
was corrected, not deleted.)*

⚠ **WHY THERE IS A PASS 2.** Pass 1 certified **3280** tests. REVIEW then found nine Important
issues, and closing them changed the code by **62 non-comment source lines and 288 test lines** —
which put the in-phase fix over the 50-LOC cap and made REVIEW **FAIL** by its own table. Brett chose
the loop back. **So pass 1's verdict was not wrong when written; it was certifying a tree that no
longer exists.** That is the whole content of this loop: the work was already done and green, and
what was owed was the certification.

Every acceptance criterion is verified against the artifact rather than the plan, the suite is green,
and the command has been **run end to end on this repository** — which is the check the Epic's
sibling work (`/sig:drive`) is still missing and the reason it is the gate on further loop work.

Three limits decide the *"with limits"* half rather than a clean PASS, and none of them is
discovered here for the first time — all three are carried forward from EXECUTE, where they were
recorded as they happened:

1. **`S3` was written test-after against `tdd_required: true`.** Strict Nyquist asks for a
   red→green record or an attestation that the test predates the implementation. For
   `tests/advise.test.js` **I cannot honestly attest that**, so it is measured instead — see
   *Nyquist* below.
2. **`M6.E7-VALIDATION.md` does not exist**, so `checkValidationConsistency` is `cannot-evaluate`,
   not clean.
3. **The calibration check scores 0 of 3 on the headline number**, and the headline number is the
   wrong reading. Detail below.

## Gate results

| Gate | Result |
|---|---|
| Test suite | **3300 passed / 0 failed** (pass 1: 3280; Epic baseline: 2979) |
| Build | `npm run build` — 49 bundles, clean |
| Lint | `npm run lint` — clean |
| Plugin validation | `npm run validate` — passed |
| Branch posture | `ok` — `feat/m6.e7-roadmap-advisor`, not the default branch |
| End-to-end run | **re-executed on this repo** at pass 2: 51 live rows → 5 recommended, 46 declined, 52 citations resolved, exactly 1 file written |

## Requirement coverage

| Requirement | Verified by |
|---|---|
| **FR1** read the corpus it advises on | `readCorpus` reads five sources; `advise-corpus.test.js` "a clean corpus checks all five". Reuses `parseBacklogRows` + `walkBugEntries` rather than re-parsing — `walkBugEntries` was extended **additively** for `line`, and `deriveBugCounts`'s existing tests still pass |
| **FR2** top N with *why this and not that* | `advise.test.js` "AC1.1 — every recommendation has a reason and a citation, and the top one says what it outranked"; asserts `Ranked above *…*` is present |
| **FR3** every claim carries a resolving citation | `citations.test.js` (17 tests) + the run-boundary count gate. **51 citations resolved on the live run** |
| **FR4** a declined section with reasons | `advise.test.js` "every declined row carries a reason naming the input that demoted it" — asserts each line names one of the five inputs |
| **FR5** read-only | `advise.test.js` "writes its artifact and nothing else" — byte-compares every pre-existing `.planning/` file before and after |
| **FR6** proposes, never selects | `advise.test.js` t3.2b — status line present, forbidden verbs absent from both the artifact and `RENDER_LABELS` |
| **FR7** an unreadable source renders as `cannot-check` | `advise-corpus.test.js` t2.6 (double assertion: cannot-check **and** not clean, slot is `null`) **and** `advise.test.js` "an unreadable BUGS.md reaches the ARTIFACT, not just the return value" |
| **NFR1** offline + deterministic | `advise.test.js` "renders byte-identical output twice"; stable line-number tiebreak proven by ranking a reversed input |
| **NFR2** no `prose` dependency | `advise.test.js` AC1.6, **with a reach declaration** |
| **NFR3** no new dial | `RECOMMENDATION_LIMIT` is a module constant; no `PROFILE.md` field added. Verified by absence — see limits |
| **AC1.1** ≥1 recommendation and ≥1 declined | Live run: 5 and 45. Strengthened form asserted, not the tautological one |
| **AC1.2** every citation resolves, whole output | `verifyCitations` unit tests + the run-boundary **count** |
| **AC1.3** a bad citation fails the run loudly | **Two tests**: unit (bad path, out-of-range line) **and** run boundary (`status: skipped`, `artifactsIn(base)` empty) |
| **AC1.4** `BUGS.md` unreadable → `cannot-check` in the output | **Two tests**, S2 return shape and S3 artifact body |
| **AC1.5** writes nothing but its artifact | `advise.test.js`, byte-compare + `INDEX.md` absence |
| **AC1.6** no `prose` import or shell-out | Static scan over the four files this Epic adds or changes |

**Coverage: 16 of 16 — and the tool says 15 of 15. Both numbers are published, because the gap is
a defect I found by running the check, not a rounding difference.**

The denominator is the requirement set declared in
[`M6.E7-REQUIREMENTS.md`](M6.E7-REQUIREMENTS.md) § *Functional requirements* (FR1–FR7), §
*Non-functional requirements* (NFR1–NFR3) and § *Acceptance criteria* (AC1.1–AC1.6) — **three groups,
7 + 3 + 6 = 16**, counted from the artifact's own headings rather than recalled.

`diffRequirementCoverage` run against this file returns `outcome: covered`, `missing: []`,
`deferred: []`, `unattributableGroups: []`, **`denominator: 15`** — and its 15 omits **`FR1`**.

⚠ **That omission is `B116`, filed today, P2.** `groupOf` normalizes `AC{N}` into the `FR-{N}`
keyspace, which is *correct* for `M5.E10`'s convention — there `AC-16.1`…`AC-16.11` genuinely are
`FR-16`'s children, and counting the heading too would inflate the denominator. It is **wrong** for
the convention this Epic and nearly every other one uses, where `FR1…FR7` are independent
requirements and `AC1.1…AC1.6` are the unit's own criteria. So `AC1.1` marks `FR-1` as having
children and `FR1` is dropped as a heading. **Measured across this repo: 21 of 22 `*-REQUIREMENTS.md`
artifacts lose at least one id; `M5.E16` loses all six of its FRs.**

**The consequence is a false clean, and it lands on this very report's exit criterion.** `missing`
is `denominator.filter(not verified)`, so a requirement absent from the denominator **cannot** be
reported missing. A `missing: []` is therefore not evidence that FR1 was verified — the table above
is, and FR1's row cites `advise-corpus.test.js` and the additive `walkBugEntries` change by name.
**Not fixed here:** different module, different Epic, and the fix must choose between the two
conventions or detect which one an artifact uses. Fix lane, its own PR.

- **`missing`: none** — over the tool's 15, and none over the hand-derived 16 either, which is the
  stronger statement and the one the table supports.
- **Deferred by the REQUIREMENTS artifact: none.** No requirement is struck out of this unit.
- **Could not evaluate:** **one, and it is not empty.** `checkValidationConsistency` has no
  `M6.E7-VALIDATION.md` to read — see below.

## Nyquist compliance

`nyquist_enforcement: strict`. Every mapped test exists and runs. The evidence form differs by
slice, and the difference is the finding:

| Test file | Evidence form | Status |
|---|---|---|
| `tests/citations.test.js` (17) | **(a) red→green** — written first, run, failed on `Failed to load url ../plugin/tools/lib/citations.js`, then implemented | strict satisfied |
| `tests/advise-corpus.test.js` (24) | **(a) red→green** — same pattern; the input-5 additions failed on `declaresNotLiveWork is not a function` before the predicate existed | strict satisfied |
| `tests/advise.test.js` (22) | **neither (a) nor (b)** — the module was written first | **measured instead**, below |

⚠ **I will not attest that `advise.test.js` predates its implementation, because it does not.** Form
(b) is an attestation and an untrue one is worse than an absent one. What is offered instead is a
**measurement**, which is the thing strict Nyquist actually wants to know — did these tests ever
fail?

**An 11-mutation sweep over `advise.js`, run against `tests/advise.test.js` alone: 11 killed, 0
survived.** The mutations: the count gate removed; `quoteSafe` neutered; ranking input 5 unwired;
`RECOMMENDATION_LIMIT` 5→50; the declined pool losing its dropped rows; the line-number tiebreak
reversed; `cannotCheck` not rendered; `writeArtifact` always writing; the producer line dropped; the
blocked-by input ignored; unresolved citations ignored.

⚠ **This proves the cases I thought of, not coverage** — the plan says so in its own risk section,
and `tests/environment.test.js` records the humbling precedent where *"ALL SIX PASSED THE FIRST
SHIPPED VERSION."* A 100% kill rate on a hand-chosen mutation set is evidence the file is
load-bearing; it is not evidence the file is complete.

**The sweep also earned its keep once, prospectively rather than in hindsight.** Ranking input 5
shipped with five passing predicate tests and **no test that it was wired**: deleting
`&& !s.notLive.notLive` from `rankRows` left the **entire 3279-test suite green**. That is `B39`'s
shape — a value computed and then dropped — and the plan's own Nyquist table already named the
identical gap one input over. Fixed in `7360163`; the same mutation now kills three tests.

## What this could not establish

**1. That a cited line SAYS what the claim says it says.** The verifier resolves a path and checks a
line is in range. The semantic half is not built, and the artifact declares this in its own Citation
rule section rather than leaving a reader to assume otherwise. Same limit `M5.E10`'s seven checks and
`B75`'s ask-record both publish.

**2. Plan-validation consistency — `cannot-evaluate`, not clean.** `M6.E7-VALIDATION.md` **never
existed**; confirmed with `git log --diff-filter=A`, not assumed from its absence today. Validation
ran in-context at PLAN and its outcome was folded into `M6.E7-PLAN.md` (commit `85b1802`, 114
insertions to the plan and no new file). So `checkValidationConsistency` has nothing to read. The
FAIL was real and acted on — the citation gate would have refused to write on every run — but **this
phase cannot re-derive that from an artifact**, and reporting it as checked would be the exact
substitution this Epic exists to stop.

**3. The calibration check: 0 of 3 on the headline, and the headline is the wrong reading.** The
outcome oracle asks how many of the frozen
[`BACKLOG-REVIEW-2026-08-09.md`](BACKLOG-REVIEW-2026-08-09.md)'s **top three moves** appear in the
advisor's output. **None appears in the recommended five.** Scored per move, against the live corpus:

| Frozen review's move | Live today? | Where the advisor put it |
|---|---|---|
| (1) re-disposition `E10`–`E12`, restate closing M5 | partly — `M5.E11` at `BACKLOG.md:1326`, `M5.E12` at `:1669` | **declined #3 and #25.** `M5.E11`'s own heading reads *"OPENED AS `M6.E7` 2026-09-05"* — **this Epic is that re-disposition**, so half the move is discharged by the work being verified |
| (2) give the capture inbox a drain outside `/sig:plan` | **no** — zero live rows match `inbox` or `drain` | **nothing to recommend.** The row left the queue; this is not a ranking miss |
| (3) open the autonomy floor with `B76` | yes — `BACKLOG.md:728` | **declined #6 — one place below the cut**, and visible at all *only* because of the `maxDepth: 4` fix, since it is a depth-4 row |

So the honest reading is **not "the ranking found none of them."** Of the three, one is discharged by
this Epic, one is no longer a live row, and **the third was considered and declined at #6, a single
rank below the recommendation cut** — and would have been *invisible* under the default read depth.
**That distinction is only visible because the declined pool is complete rather than curated**, which
is `B39`'s rule doing exactly the work it was written for, on its own author.

⚠ **The requirements pre-committed to this being informative rather than fatal, and that
pre-commitment is load-bearing here** — it is why this section reports the number and then the
decomposition, instead of the decomposition being read as an excuse constructed after the number
arrived. The frozen review's best content came from **cross-document contradiction finding**, which
FR1–FR7 do not attempt and this build does not do. The ranking half is, as predicted, the less
valuable half.

**4. The primary outcome oracle is not yet observable, by construction.** It asks whether *the next*
backlog review is generated rather than hand-written **and the person acts on it**. One generated
artifact now exists with the command named as its producer, which is the necessary half. The
sufficient half — Brett acting on it instead of writing the next pass by hand — cannot be observed
from inside the Epic that produced it, and will not be until the next review is due.

**5. The coverage check's own denominator was wrong here, and I only know that because the two
numbers disagreed.** Had the hand-derived table not existed, `missing: []` over a 15-item denominator
would have read as a clean pass, and `FR1` — *"read the corpus it advises on"*, the Epic's first
requirement — would have gone uncounted in silence. The disagreement was the detector. That is worth
stating as a limit rather than a win: **this verification has no mechanism that would have caught it
if I had trusted the tool alone**, which is the same shape as everything else this Epic found.

**6. `NFR3` is verified by absence, which is the weaker form.** No `PROFILE.md` field was added, and
nothing tests that a future one is refused. A later Epic adding a ranking dial would not turn this
suite red.

**7. Ranking input 2's reach limit is unresolved and is now documented rather than fixed.**
`TRIGGER_MET_RE` reads a row's heading **and its whole body**, so it can still promote a row on a
trigger belonging to something else. The two measured false positives are caught **upstream** by
input 5, a heading-level signal — but a row about triggers that does *not* declare itself parked or
a record would still slip through. Stated in the module's reach declaration; not measured for
residual rate.

**8. One behaviour reported but not exercised end to end — and the second one I named here was
wrong.** `writeArtifact` refuses when `.planning/` is absent (unit-tested, not run end to end).

⚠ **CORRECTED AT REVIEW, 2026-09-05.** This limit originally also claimed that
`assertRealInsidePlanning` **throws** on a symlinked `.planning/`, so `runAdvise` would crash where
its contract says it returns a reason. **Running it says otherwise:** a symlinked `.planning/` returns
`{status: 'skipped'}` with the reason *"citation check failed … resolves outside the repo (through a
symlink)"*. The **citation gate refuses first** — every citation points into `.planning/`, so a
symlinked planning root makes them resolve outside the repo and the run stops before `writeArtifact`
is reached. The throw appears to be unreachable through `runAdvise` and is defence in depth, not a
live crash.

**This is the fourth time in this Epic that measuring contradicted something I had already written
down**, and the first where the wrong claim reached a committed artifact. Corrected here rather than
edited away, and counted in `M6.E7-REVIEW.md` as a finding against this report.

**9. A side effect this phase caused and did not plan.** `transitionPhase(VERIFY)` regenerated
`INDEX.md` at phase **entry** — the bug filed last session — and in doing so **auto-resolved the
orphan that `t3.7` designated a one-time human step at SHIP**. The command itself still wrote nothing
but its artifact (AC1.5 holds, and its test proves it). But the `INDEX.md` line for
`BACKLOG-REVIEW-2026-09-06.md` now exists because an unrelated bug fired, not because anyone decided
it should. The inbound `BACKLOG.md` link is still owed at SHIP.


---

# Pass 2 — 2026-09-06

Re-run after the REVIEW loop. Everything in the pass-1 body above was re-checked against the changed
tree rather than carried forward; the differences are here.

## What changed since pass 1

Nine Important issues closed (six at REVIEW, three late ones the reviewer had filed as suggestions),
plus the reviewer's remaining nits taken during the loop's EXECUTE pass. **62 non-comment source
lines, 288 test lines, 20 new tests: 3280 → 3300.**

**Two of those were defects pass 1 could not have caught, and one refuted pass 1 directly:**

- Pass 1's limit 8 claimed `assertRealInsidePlanning` was unreachable through `runAdvise`. **It is
  reachable** — a symlinked `.planning/` with a backlog of no live rows, and separately a read-only
  `.planning/` needing no symlink at all. Corrected in place above.
- A caller could inject a citation through `projectName`, and a live row could silently vanish
  through a missing `.filter(Boolean)`. Both are in the mechanism this Epic exists to provide.

## Gate results — pass 2

| Gate | Result |
|---|---|
| Test suite | **3300 passed / 0 failed**, 191 files |
| Build | `npm run build` clean |
| Lint | clean |
| Plugin validation | passed |
| End-to-end run | 51 live rows → 5 recommended, 46 declined, **52 citations resolved**, 1 file written |
| Loop counts | VERIFY 1 of 3, REVIEW 1 of 3 — neither near the ceiling |

## Requirement coverage — pass 2

Unchanged at **16 of 16**, `missing` empty. No requirement was added, removed or deferred by the
loop; every verifier named in the pass-1 table still exists and still passes. `B116`'s denominator
defect still applies (the tool derives 15, omitting `FR1`) and is still filed, not fixed.

## Nyquist — pass 2

The evidence forms shift, and in the right direction. **Every one of the 20 tests added since pass 1
was written test-first or is a regression test for a defect that was demonstrated before the fix** —
which is form (a), red→green, for all of them. Each REVIEW finding was reproduced first: the
self-contradicting count in an artifact this repo had already written; cases B and C reproduced
before accepting the reviewer's claim; the `projectName` injection measured yielding
`nope/missing.md:1`; the retros false-clean observed before the guard.

`tests/advise.test.js`'s **original** 21 remain test-after, with the 11-mutation sweep standing as
their measurement. That limit is unchanged and not re-litigated here.

## What this could not establish — pass 2

Everything in pass 1's list still stands except limit 8, corrected above. Added:

**A. The artifact this Epic ships is the second one it produced, and the first was deleted.**
`BACKLOG-REVIEW-2026-09-05.md` was generated by code carrying nine defects — including the
self-contradicting count — so it was removed rather than shipped as a record, and every reference
re-pointed to `BACKLOG-REVIEW-2026-09-06.md`. **Nothing verifies that a stale dated artifact gets
cleaned up in general**; this one was caught because I regenerated and noticed two.

**B. The command now recommends its own REVIEW finding, and that is not independent evidence.**
Position 3 of this run is *"`/sig:advise` ranks on the backlog alone, while reading five sources"* —
the row filed from REVIEW finding 1 a day earlier. It ranks there because it was filed with a met
trigger, which is the ranking working. It is **not** the advisor detecting its own limitation; it is
the advisor reading a row a person wrote about it.

**C. A second REVIEW pass by the same reviewer is not a fresh-context review.** The pass-2 review
reads a diff it has already seen most of. What it can establish is that the nine fixes are correct
and complete; what it cannot establish is that a stranger would find nothing new.

**D. The 50-LOC cap was measured one way.** `git diff -U0` with comments and blanks excluded,
counting insertions plus deletions as the rule states. A modified line therefore counts as two. Under
a different but equally defensible count — insertions only — the figure is smaller and the REVIEW
verdict might have been PASS-WITH-FIXES. **The stricter reading was taken deliberately**, and it is
recorded here so the choice is visible rather than implied.
