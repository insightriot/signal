---
tier: FEATURE
schema_version: 1

calibration:
  scope: feature
  stakes: major
  novelty: rare
  reversibility: moderate
  horizon: months

phases_skipped: []

rigor_overrides:
  tdd_required: true
  security_audit: basic
  performance_pass: true
  simplification_pass: true
  nyquist_enforcement: strict
  plan_validation_dims: core
  research_parallelism: 2
  gate_strictness: light
  attention: checkpointed
  context_rot_reread: true
  review_depth: quality-only

metadata:
  created_at: 2026-09-14T00:00:00.000Z
  created_by: sig:resume (per-Epic override, D-M6E8-1 as corrected by D-BR0914-1)
  escalation_history: []
---

# `M6.E8` — per-unit calibration

**FEATURE, inside a FULL project.** The fifth use of the per-unit dial, after `M6.E4`, `M6.E5`,
`M6.E6` and `M6.E7`.

**Written at PLAN entry, not at DISCUSS, and the reason is the finding.** `D-M6E8-1` chose FEATURE
on 2026-09-07 and then declined to write this file: *"the Epic inherits, which is the documented
default."* That sentence was written from the shape of the sibling Epics, not from the artifact —
the project `PROFILE.md` is **FULL**, and `M6.E6` and `M6.E7` were FEATURE only because each had a
file like this one. `readEffectiveProfile` returned FULL for `M6.E8` at `/sig:resume` on
2026-09-14, so the decision's stated tier and the tier the phase commands would actually read had
diverged. See `D-BR0914-1`.

**Derived from the ordered gates, not assigned** (`references/tier-definitions.md`, *"first match
wins"*). No FULL escalator fires — `stakes` is `major` not `catastrophic`, `reversibility` is
`moderate` not `irreversible`, `horizon` is `months` not `years`. SPIKE fails on `stakes: major`.
SKETCH fails on `scope: feature`. FEATURE is the default and the answer.

**Why `stakes: major`.** Same reason as `M6.E7`: the advisor's output is read to decide what gets
built next. Not `catastrophic`: `runAdvise` writes one artifact and `writeArtifact` refuses to write
anything else, so a person is always between the claim and the consequence.

**Why `reversibility: moderate`.** A pure-logic change to a read-only command; the one deletion
(`FR5`, the retrospective read) is a `git revert` away.

## The overrides, and why they are not tier inflation

**`nyquist_enforcement: strict`** — FEATURE's default is `basic`. Carried over from the project
profile, as `D-M6E8-1` records: the inputs this Epic adds (`FR1`–`FR4`) are ranking predicates whose
vocabulary is *measured and declared before it is chosen*; a sampled guarantee over a predicate is
not one.

**`attention: checkpointed`** — the project's dial (`D-BR0908-1`), set explicitly rather than
derived from `gate_strictness: light`, because `attention` missing from a profile is how four
consecutive Epics ended up writing per-Epic profiles to escape a stop nobody wanted.

**Escalation trigger, stated in advance:** if any input added here is allowed to *select* rather than
rank — or to write to `BACKLOG.md` or `BUGS.md` — `reversibility` and `stakes` both move and this
recalibrates.
