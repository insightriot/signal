---
epic: M6.E8
tier: FEATURE
phase: REVIEW
created: 2026-09-14
pass: 1
---

# `M6.E8` — Review Report (pass 1 of up to 3)

`review_depth: quality-only` ([`M6.E8-PROFILE.md`](M6.E8-PROFILE.md)), so Step 1 and Step 4.5 ran
and Steps 2–4 were skipped by the master switch. **Step 4.5 ran in full: all three fresh-context
agents dispatched in parallel, none given `CONTEXT.md`, `DECISIONS.md`, the plan, or any statement
of intent.** No `cannot-dispatch`.

## Verdict

- [ ] PASS
- [ ] PASS-WITH-FIXES
- [x] **FAIL — the in-phase fix budget was exceeded. Loop back to EXECUTE (path A).**

**Zero Critical from all three reviewers and from the author's own pass.** Eight Important issues
were found, every one reproduced before it was accepted, and all eight are fixed and green
(`e1528fe`, suite 3424 → **3427**, lint clean). The verdict is FAIL on one condition only:

| PASS-WITH-FIXES condition | Result |
|---|---|
| Important issues found AND closed in-phase | met |
| **Total fix ≤ 50 LOC of non-test source** | **NOT met — 59** |
| Tests still green | met (3427) |
| New coverage written for the defect | met — every fix carries a test that fails without it |
| No design impact | met — one regex, five constants, three files, no contract changed |

Of the 59 lines, **12 are functional** (one regex line, five `*_MEASURED` constants) and **47 are
comments recording what each reviewer found.** That distinction was put to the maintainer as the
gate question rather than resolved here, because *"the diff was mostly comments"* is exactly the
shape of argument the conjunctive conditions exist to refuse. **Brett chose path A**: treat the
fixes as EXECUTE work, which is what the rule intends when a fix outgrows REVIEW's budget, and
re-run REVIEW on the new diff with the three reviewers dispatched again — so the fixes are read by
someone other than their author. **Loop count at this decision: 0 completed REVIEW passes, ceiling
3, so this is pass 1 and the ceiling is not in reach.**

⚠ **The rule has a real tension and it is recorded rather than bent.** A cap counted in raw lines
penalises writing findings down in the source, which is this repository's own standing norm
(*"vocabulary measured before it was chosen"*, *"kept with a written reason"*). Filing that against
the rule is a separate decision from this Epic and was explicitly not taken here.

## Critical Issues

**None.** All three reviewers reported zero independently. No path loses a live row silently, no new
I/O or interpolation surface is unsafe, and the citation gate and stale-read guard are untouched.

## Important Issues — all eight fixed in `e1528fe`

| # | Finding | Found by | Reproduced? |
|---|---|---|---|
| I1 | `BUG_DISCHARGE_RE` matched **present tense only**. `Fixes B75` hit; `Fixed B75`, `Closed B12`, `Resolved B3`, `Discharged B9`, `B75 fixed` all missed. The predicate claimed to recognise *"a heading that says it discharges a bug"* and recognised about half the forms a maintainer writes, while `BUG_DISCHARGE_MEASURED` would have gone on reporting **0** with such a row on the file. | author's Step 1, independently by `code-reviewer` (#3) | yes — probed 18 forms; widened version adds **0** live matches and **0** false positives on the three real trap headings |
| I2 | **The live-measurement pins pass vacuously when the source they measure is unreadable.** With `BUGS.md` replaced by a directory the discharge check still reports `outcome: 'clean'` while its own `sources` field reads `{units: true, bugs: false}`, the confirmed-bug set is empty, and two zero-expecting pins go green having checked nothing. **This is the exact trap `sources` was added to close for the artifact, reproduced one level down in the tests that measure it.** | `security-auditor` | yes — reproduced in a scratch copy |
| I3 | **Five of six `*_MEASURED` constants recorded `rows: 48`** while the real populations are **45** (`live`) and **52** (`all`), and the docblock stated the test asserted `hits` only. A number beside the pattern that nothing verified — inside the constants written to satisfy `NFR6`. | `code-reviewer` | yes — both populations recomputed through `scoreRepo` |
| I4 | **AC1.2's body-not-read test could not fail.** The fixture body (*"`B1` measured that ceiling"*) carries no discharge verb beside the id, so a body-reading implementation — the exact `M6.E7` regression it names — returns `null` too and the test stays green. | `test-engineer` | yes; replacement body proved discriminating before commit (`null` heading-only, `B1` when read) |
| I5 | **AC3.2 was asserted at a layer that never held the risk.** Hand-built rows plus a hand-built `stale[]` matching nothing; `rankRows` drops only on `leadingId`/`line` and did so before this Epic, so the test passes on pre-change code. The prohibited loose predicate would live in the parser. | `test-engineer` | yes — the three real headings run through `parseBacklogRows` lead with `null`, `M5.E20`, `null` |
| I6 | **A comment claimed `KEPT` inherits `HELD_OPEN_RE`'s "do not drop" meaning. Inside the advisor that regex DROPS rows.** `KEPT OPEN, absorbed into M5.E11` returns `notLive: true` **and** `kept: true`, and input 5 wins. Two spellings of one intent, opposite outcomes. | `security-auditor` (#2), `code-reviewer` (#1) | yes — both states printed |
| I7 | `advise-corpus.js` still opened with **"Five sources"** and described missing *"the fifth"* as the failure to avoid, after the change that made it four. Stale on its own terms. | `code-reviewer` (#4) | yes |
| I8 | Three of six remedy messages printed **counts without headings**, so a red told the reader a number moved and not which row moved it; and AC2.1′'s gate-row assertion was wrapped in `if (gate)`, so it would **vanish silently** the moment the row was struck. | `test-engineer` (I3 tail, S1) | yes |

## Suggestions — recorded, deliberately not fixed

1. **`KEPT` does not shield a row from the discharge input** (`security-auditor`). Verified: a row leading with a closed unit and carrying `KEPT, absorbed into` is dropped with the discharge reason. Arguably correct — `KEPT` says *the work moved, keep the row*, not *this closed unit is open* — but undecided and now stated in the source.
2. **`KEPT OPEN` vs `KEPT` precedence** (I6's other half). Reconciling them means deciding what a maintainer's `KEPT OPEN` should mean to a ranking. **A design call, not this Epic's.** Zero live rows hit it.
3. **`STATE/closure` is resolved twice per run** (`security-auditor`). `readCorpus` runs `resolveClosures`, and `backlogDischargeStatus` runs it again; `corpus.sources.closure` feeds no ranking input. The corpus copy does serve the *Read* / *could-not-read* line, which is the same job `FR6` kept the milestone read for — so it is not read-and-unused — but the double resolution is real and the two reads can disagree under a concurrent edit.
4. **A swallowed exception becomes an artifact claim** (`security-auditor`). `runAdvise` discards the error from a throwing discharge check, and the renderer then prints *"the discharge input did not open it on this run"* — written from absence of data rather than a recorded reason.
5. **`AC2.2` is over-pinned** (`test-engineer` S2). It asserts `BLOCKED_RE` is byte-identical; the AC's property is *"adds, never subtracts"*. A legitimate future widening fails a test whose name claims nothing about widening.
6. **`FOLD_MEASURED` counts predicate hits, not drops** (`test-engineer` S3). Removing `s.moved.moved` from `isDropped` leaves the pin green; the unit test catches it, but the pin's name over-claims.
7. **Untested paths, with the inputs that reach them** (`test-engineer` S4): a blocked row that also discharges a bug; a declined row with `dischargesBug` set (reaches *"Demoted by the age input"*, which is then incomplete); `runAdvise`'s `catch { discharge = null }`; the renderer's *"not recorded"* branch, unreachable via `runAdvise`.
8. **Nits** (`test-engineer`, `security-auditor`): a duplicated `HELD_OPEN_RE` source pin; a test named for bodies that passes a heading; shape-only assertions that cannot fail in an implementation-relevant way; a closure-evidence filename is not newline-escaped (`quoteSafe` strips the marker, not newlines); `Fixes B07` does not match the catalog's `B7`.

## Documented, not fixed — and it will fire

**`TRIGGER_MET_MEASURED` breaks at this Epic's own SHIP, by construction** (`test-engineer` I3).
It is pinned at **3**, and one of the three is this Epic's own backlog row. `readCorpus` filters
struck rows, so discharging that row at SHIP — which `NFR1` itself calls a one-time human SHIP edit
— drops the count to 2 and turns the file red **unless the constant is bumped in the same commit.**
Recorded in the test header *before* it happens. **Bumping it is the correct response, not a
workaround:** the vocabulary's reach genuinely changed by one row, and a human genuinely looked at
that row. This is `B120`'s family, found by a reviewer rather than by a red build.

## Security Findings

`review_depth: quality-only` skips Step 2, but the dispatched `security-auditor` covered it anyway.
**ReDoS: clean** — every new pattern measured at 200,000-character adversarial inputs in fourteen
shapes, all under 2 ms; independently probed by the author at 50k in nine shapes, none over 50 ms.
**Path handling: clean** — the diff adds no path construction; the write path still passes
`assertRealInsidePlanning`. **Injection: clean for the diff** — every new interpolation is a regex
capture, a frozen constant, or passes `quoteSafe`; the one newline-capable input is the Nit above.
**Supply chain: clean** — no manifest or lockfile change, two internal imports removed and none
added.

**Tool-ceiling disclosure, as the step requires it be stated plainly.** All three agents ran on the
fallback path, where the specialist file's `tools:` frontmatter is **inert** — a general-purpose
sub-agent carries the full tool set, so *"you are read-only"* was **a prose ceiling in the prompt,
not enforcement.** `security-auditor` enumerated the write and network tools it could reach and
confirmed it used none; `test-engineer` was given the explicit report-only constraint the step
mandates and wrote nothing. The working tree was unchanged by all three, verified by `git status`.

## Performance Findings

Step 3 skipped by `review_depth`. Incidentally covered: the ReDoS measurements above, and
Suggestion 3 (a duplicated `resolveClosures` walk per run) is the only cost finding.

## Simplification Opportunities

Step 4 skipped by `review_depth`. Suggestion 3 and Nit *"duplicated source pin"* are the candidates
if it is run later.

## The chosen path

**A — loop back to EXECUTE.** The fixes are committed and green at `e1528fe`; that commit is the
EXECUTE work. REVIEW then re-runs on the new diff, **with all three fresh-context agents dispatched
again**, so the eight fixes are read by someone other than their author — which is the whole
premise of Step 4.5 and would be quietly skipped if this pass were allowed to stand as its own
review.
