# M5.E2 — VERIFICATION

**Phase:** VERIFY. **Tier:** FULL / `nyquist_enforcement: strict`, `gate_strictness: strict`. **Plan:** `M5.E2-PLAN.md`. **Coverage map:** `M5.E2-VALIDATION.md`. **Date:** 2026-07-18.

## Verdict: **PASS-WITH-GAPS** → gap closed at the VERIFY gate

The engineering goal — a safe, auto-sensing `/sig:migrate-memory` handling all three bloat vectors + archive-tree + FR7 stamp/hook/banner, relocate-never-delete, dry-run-default, git-reversible — is **fully achieved and independently verified**. Suite/build/validator green; both load-bearing gates proven to have teeth; safety invariants hold in code. The one gap was **by design**: the §5 semantic-faithfulness proof VALIDATION.md names (a human eyeballing *real relocated prose*) had never actually run — S4.t1's dogfood was byte-identical archive moves only (Signal's STATE was already clean), and S4.t2's word-conservation was machine-proven with the human diff deferred. **Closed at this gate** by a read-only real-prose card review on the nextpass dry-run (below).

## Baseline (independently re-run)
- `npm test` → **1264 passed / 1264 (77 files)**, exit 0.
- `npm run build` → clean (esbuild, v0.1.7), exit 0.
- `npm run validate` → "Plugin validation passed", exit 0.
- `npm run lint` → broken repo-wide (BUGS.md **B5**, pre-existing — no `eslint.config.js`); **not** a VERIFY failure; `npm run validate` is the effective gate.

## AC coverage — 22/22 automated ACs carry a real proof-of-fail; 2 S4 ACs manual
Independent verifier opened every named test file and confirmed the negative/proof-of-fail assertion exists (not filename-trusted). All PASS. The two S4 (dogfood/nextpass) ACs are **manual + PARTIAL** — see the faithfulness note. Full table in the verifier record; highlights:
- **Load-bearing mutation gates proven to bite (strict Nyquist teeth):**
  - **B8 prose-drop** — mutating `conserves()` WORD-branch (`migrate-memory.js:188`) to always-pass landed RED on exactly `migrate-faithful-gate.test.js:59/:86` + `migrate-conservation.test.js` (drop/multiplicity/B8-catastrophe); BYTE-mode proofs stayed green. Reverted; tree clean.
  - **Dangling-link** — mutating `enforceNoDangling` (`migrate-memory.js:1480`) to advisory-only landed RED on exactly `migrate-linkverify.test.js` "MUTATION A" (`rejects.toThrow(/dangling/i)` + rollback asserts); the 6 flag/no-abort tests stayed green. Reverted; tree clean.
- **Combined all-vectors integration fixture** (`tests/migrate-all-vectors.test.js`, commit `b62583b`) — the closed VERIFY carry-forward: V1+V2+V3+archive all fire in ONE apply → conformant+stamped; 2nd apply byte-identical (`after === before` — the real composition proof); all-or-nothing rollback restores every touched file. **No bug revealed** — this was missing coverage, not broken behavior.

## Safety invariants — all confirmed in code (node-verified)
- **§9** — engine never imports/calls self-locking `evictEpicNarrative` under the coarse lock (import block pulls only the lock-free spine; 0 non-comment call sites); positively tested by `migrate-lock.test.js` + `migrate-vector3.test.js`.
- **§10** — 0 `INDEX.md` write sites; classification routes INDEX→'spine'/flag-only (byte-unchanged); asserted in `migrate-archive-tree` + `migrate-linkverify`.
- **Append-log protection** — two layers (sense: `classifyDocGrowthPolicy`→'append-log'; move: `SCAFFOLD_SUFFIXES` excludes them + `{epicId}-{suffix}` non-match). Matches the logged M5.E3 forward-constraint (move-layer protection is incidental).
- **Relocate-never-delete** — BYTE archive moves byte-identical; in-body relocations word-conserved; `relocateFaithful` never shortens the source unless the gate passes; abort paths prove zero partial writes.

## §5 faithfulness — human eyeball on REAL relocated prose (gap closure)
The nextpass dry-run (read-only) *is* a real vector-1 de-prose of **19 `completed_phases` prose entries** + a 538 KB body relocate. Verified directly on the 546 KB `nextpass/.planning/STATE.md`:
- `conserves(raw, deprosed, WORD)` → `{pass: true, missing: []}` — **0 words dropped** across all 19.
- Frontmatter **212 → 40 lines**; the full prose relocates to the STATE body under an FR2c skeleton.
- **Card sample (REVIEW/COACH1 entry):** the complete 7-line narrative moves frontmatter → body **byte-for-byte identical** (rendered before/after; verbatim). This is the human-reviewable §5 card the prior dogfood lacked.

**Live nextpass apply remains a post-release user step** (Brett runs it via the shipped plugin — also validates the real `/plugin update` → command path). The faithfulness *mechanism* is now human-reviewable on real prose; the live apply is deployment, not proof.

## Carry-forwards → REVIEW (do not re-discover)
1. **`readLayoutBanner` hot-path efficiency** (`status.js:404`) — fail-open ✅ but calls `senseProject` unconditionally on every `/sig:status` + `/sig:resume` (full corpus walk + uncapped read, heaviest on bloated projects). Short-circuit on a cheap capped stamp read; pairs with promoting the hook's stamp helpers to `tools/lib/layout-stamp.js`.
2. **B11 — `vector-2-defer` flag over-fire** (P3) — one spurious flag per historical closed Epic on any already-evicted corpus (also fires on a re-run's dry-run for a migrated Epic). Advisory noise; doesn't touch idempotency/noop. Fix: only emit when un-sectioned closed-Epic narrative is actually present in the body.
3. **`applyArchiveTree` fresh re-sense is load-bearing** — a V2-created `STATE-HISTORY.md` that references a moved scaffold becomes a rewritten referrer only because archive-tree re-senses from disk (the pre-write snapshot loop never sees it). `migrate-all-vectors.test.js` #3 guards this — do NOT "optimize" it to reuse the caller's pre-sense.
4. **S3.t1 "malformed → silent" is structural** (no `---` fence → silent), not YAML-validity — a fenced-but-corrupt STATE banners by design (forced by the nextpass in-frontmatter-bloat case). Documented + regression-locked.
5. **B5** — `npm run lint` broken (no eslint flat config); separate chore.

## Exit criteria
- [x] All acceptance criteria verified with evidence (22/22 automated + 2 manual S4, PARTIAL by design).
- [x] Full test suite passes (1264).
- [x] Nyquist compliance — strict proof-of-fail teeth proven by targeted mutation on both load-bearing gates; per-task RED-first attested in the executor commit trail (`1b79b96`..`b62583b`).
- [x] Build succeeds cleanly.
- [ ] **User approves verification results** (+ the §5 real-prose card) — the VERIFY gate.

## Last Updated
2026-07-18 — VERIFY: PASS-WITH-GAPS. Combined all-vectors fixture added (`b62583b`); independent verifier confirmed 22/22 automated ACs + gate teeth + safety invariants; §5 human-prose-eyeball gap closed via the nextpass real-prose card. Next: VERIFY gate confirmation → REVIEW.
