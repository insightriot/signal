# M4.5.E5 — Ship (SHIP output — STAGED)

**Phase:** SHIP (FULL/strict). **Status: STAGED to the version-decision line.** Every buildable SHIP step is done; the version stamp, tag, and GitHub Release are the human handoff (D-E5-6) and are **user-gated** (D-E5-4) — not performed here.

## Pre-ship checklist

- [x] All EXECUTE tasks shipped with atomic commits (t1–t9).
- [x] VERIFY **PASS** — 9/9 ACs (`M4.5.E5-VERIFICATION.md`).
- [x] REVIEW **PASS** — 0 Critical / 0 Important (`M4.5.E5-REVIEW.md`).
- [x] `RETROSPECTIVE.md` written + passes the FULL-tier retro gate (`M4.5.E5-RETROSPECTIVE.md`).
- [x] Full suite green (**777**) + `npm run validate` green.
- [x] No new runtime deps (`package.json` unchanged).
- [x] Git history clean — one commit per task + per-slice bookkeeping, all pushed to `origin/main`.
- [x] CHANGELOG `[Unreleased]` carries the E5 entry (unstamped, by design).

## Remaining — human handoff (NOT done here)

The release itself is Brett's call. Full detail in `M4.5.E5-LAUNCH-KIT.md` §3; in brief:

1. **Voice pass** on `docs/launch-post.md` + `docs/demo-script.md` + `docs/tester-brief.md` (mechanical accuracy already clean).
2. **Pick the version** (rubric → recommended **v0.1.4**).
3. **Stamp the release:** CHANGELOG `[Unreleased]` → `[0.1.4]`; bump `package.json` + `.claude-plugin/plugin.json` 0.1.3 → 0.1.4; `references/facts.md` test count 762 → 777; tag `v0.1.4`; re-pin `marketplace.json` `ref`/`sha`.
4. **Create the GitHub Release** (paste LAUNCH-KIT §2; substitute the public post URL).
5. **Outward (optional / whenever):** README post link, recruit peers, record the demo, capture friction logs → v0.1.(N+1) backlog.

## Close-out

When the release is cut, **mark SHIP complete** (the retro gate is already satisfied) to close **M4.5.E5 → M4.5 → M5**. Until then, `STATE.md` `phase: SHIP` with SHIP not in `completed_phases` accurately reflects "staged, awaiting the human release."
