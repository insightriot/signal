# M4.5.E7 — Progress

**Epic:** Synthesizer prose-quality + install-UX hardening
**Tier:** FULL (`gate_strictness: strict` — per-wave confirmation)
**Started:** 2026-05-22 (EXECUTE phase entered after `/sig:resume` + user "yes")

---

## Slice 1 — Synthesizer fix (10 tasks)

- [x] **S1.t1** — Re-reproduce on fresh Express clone — commit _pending_
  - Express v5.2.1 cloned to `/tmp/express-repro-e7` (`--depth=1`).
  - 4 scanner agents spawned in parallel via `sig:scanners:*` subagent_type — all completed (78s / 98s / 72s / 129s, 21k–35k tokens each).
  - LANDSCAPE.md + baseline PROJECT.md synthesized per `commands/init.md` Steps 3 + 4.
  - **Empirical finding: 0 of 6 R1 patterns reproduced** under exact-string match. Re-repro produced clean output.
  - Escalated per S1.t1 AC. Brett chose **Option A — Synthetic actual/ from R1 snippets**.
  - Fixture written at `tests/fixtures/synthesizer-bug-r1/`:
    - `scan/{stack,structure,activity,quality}.md` — verbatim re-repro scanner outputs.
    - `actual/LANDSCAPE.md` + `actual/PROJECT.md` — clean re-repro structurally adapted to make all 6 R1 patterns injectable, then patterns injected at documented locations.
    - `README.md` — provenance + per-pattern bug→clean diff table.
  - RESEARCH.md addendum at `.planning/M4.5.E7-RESEARCH.md` § S1.t1 documents the empirical finding + implications for test strategy.
- [x] **S1.t2** — Classify patterns by determinism (Layer B vs C) — commit _pending_
  - Classification table at `tests/fixtures/synthesizer-bug-r1/CLASSIFICATION.md`.
  - Result: 4 Layer B (#1 heading, #2 heading, #3 table-cell, #4 command-flag) + 2 Layer C (#5 sentence/code-fence boundary, #6 mid-word truncation). 67% Layer B — above the 50% threshold; no escalation.
  - Sibling-coverage set identified: 8 LANDSCAPE.md h2 + 7 PROJECT.md h2 = 15 implicit heading-boundary assertions in S1.t4's round-trip test.
- [x] **S1.t3** — Hand-correct `expected/` fixture (D15-style review) — commit _pending_
  - `expected/LANDSCAPE.md` + `expected/PROJECT.md` produced by copying `actual/` and reversing exactly the 6 injected patterns. Diff verified by user (Brett) under D15 protocol — 6 lines total, one per pattern, no other edits.
- [x] **S1.t4** — Layer B tests: heading-literal + round-trip (RED) — commit _pending_
  - `tests/synthesizer-regression.test.js` — 9 new tests: 2 heading-literal (patterns 1 + 2), 2 round-trip via extractSection (LANDSCAPE + PROJECT), 4 sibling heading-boundary (length, well-formedness, RED-state anchors), 1 embedSection-existence (RED until S1.t5).
  - Suite count: 366 → 375 (374 green + 1 deliberate RED per S1.t4 AC).
  - 366 baseline tests stay GREEN; only the embedSection test is RED, as planned.
- [x] **S1.t5** — Build `embedSection` helper in `tools/lib/landscape.js` — commit _pending_
  - `embedSection(content, heading)` exported from `tools/lib/landscape.js`. Returns section body verbatim (trims trailing whitespace only); returns null on missing heading or null/empty content.
  - 3 new unit tests in `tests/landscape.test.js`: verbatim table preservation, missing-heading null, fenced-code preservation.
  - Suite: 375 → 378 (RED → GREEN on S1.t4's embedSection-exists test + 3 new GREEN unit tests).
- [x] **S1.t6** — Update `commands/init.md` Step 3 to reference `embedSection` — commit _pending_
  - L209 Project structure template: "Embed ... verbatim" → "Write the literal output of `embedSection(scans.structure, 'Source Tree (depth-3)')`. Do not paraphrase, retype, or summarize."
  - L257 Synthesis rules: "Embed scanner data; don't re-summarize" → "Embed scanner data via `embedSection`; don't re-summarize."
  - L17 Authoritative references: landscape.js helper list adds `embedSection`.
  - 1 new Layer B integration test: init.md template references `embedSection(scans.structure, 'Source Tree (depth-3)')` literally + appears ≥ 2 times.
  - Suite: 378 → 379.
- [x] **S1.t7** — Layer C tests: prompt-template lint (RED) — commit _pending_
  - `tests/helpers/template-lint.js` (new, ~95 LOC, no deps) — `loadTemplate`, `findLongLines`, `findSentenceBeforeFence`, `findShortHeadings`, `findDoubleBraces`.
  - 5 new Layer C property tests in `tests/synthesizer-regression.test.js`: line-length ≤ 500 chars, no sentence-then-fence on same line, h2 headings ≥ 8 chars (whitelist Vision/Scope/Notes), no double-braces, sibling-template coverage (discuss.md + calibrate.md).
  - 4 GREEN + 1 deliberate RED. RED: line-length lint fails on `commands/init.md` L170 (851 chars) and L404 (562 chars) — S1.t8 will GREEN.
  - Suite: 379 → 384 (383 GREEN + 1 RED).
- [ ] **S1.t8** — Patch `commands/init.md` template anti-patterns — pending
- [ ] **S1.t9** — Manual verification: rerun `/sig:init` on fresh Express clone — pending
- [ ] **S1.t10** — CHANGELOG entry + validator green + commit S1 — pending

## Slice 2 — Install troubleshooting docs (8 tasks)

- [ ] **S2.t1** — `docs/install-troubleshooting.md` skeleton + Quick Triage + Canonical Clean Reinstall — pending
- [ ] **S2.t2** — Symptom 1 — P1 (`/plugin install` already at latest) — pending
- [ ] **S2.t3** — Symptom 2 — P2 (`/plugin` UI no Uninstall verb) — pending
- [ ] **S2.t4** — Symptom 3 — P3 (Disabled state survives reinstall) — pending
- [ ] **S2.t5** — Symptom 4 — Pre-rename `signal@signal` cache orphan — pending
- [ ] **S2.t6** — Symptom 5 — SSH multi-identity cross-link — pending
- [ ] **S2.t7** — Reference table + See Also — pending
- [ ] **S2.t8** — README link + CHANGELOG + FUTURE-IDEAS + Epic close + commit S2 — pending

---

## Notes

- Strict gate is per-task, not per-slice — every task ends with explicit Brett confirmation before the next begins.
- The fixture at `tests/fixtures/synthesizer-bug-r1/` is the hermetic input for S1.t2 → S1.t8. `/tmp/express-repro-e7/` is keep-around evidence for the duration of S1; safe to delete after S1.t10 ships.

## Last Updated

2026-05-22 (S1.t1 complete; fixture + RESEARCH.md addendum + this PROGRESS.md ready for atomic commit.)
