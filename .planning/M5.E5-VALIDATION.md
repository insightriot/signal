# M5.E5 — Plan Validation + Nyquist Map

**Tier:** FULL / strict → all 8 validation dimensions + strict Nyquist mapping.

## 8-Dimension Validation

| # | Dimension | Verdict | Notes |
|---|---|---|---|
| 1 | **Goal alignment** | ✅ | All four tasks map 1:1 to the phase goal's four carry-overs; nothing extra. |
| 2 | **Completeness** | ✅ | FR1→T1, FR2→T2, FR3→T3, FR4→T4. Every FR + every AC (AC1.1–AC4.8) has an owning task. B24's named limitation + the "7 wrappers" doc nit are captured as task line-items. |
| 3 | **Dependency correctness** | ✅ | Zero inter-task dependencies — all four are file-disjoint in both source and tests. Waves sequence *risk* (gate-touching first), not dependency; the Wave-1 checkpoint is a fail-fast gate, not a blocker. |
| 4 | **Testability** | ✅ | Every task is RED-first with a named failing test that reproduces the defect (T1 inverts an existing wrong-behavior test; T2 adds the M5.E4 scenario; T3 uses a broken-twin discriminator; T4 adds the phase-artifact-only case). |
| 5 | **Scope discipline** | ✅ | Bugs-only. ISSUES-INBOX drain deliberately skipped (promoting inbox ideas = scope creep on a locked bug Epic); Sprint-3 hygiene commands deferred; B24 masking corner accepted-and-documented, not gold-plated away. No fix touches surface beyond its bug. |
| 6 | **Context feasibility** | ✅ | T2/T3 ≤5 files, T4 = 2 files, all fit one agent context. **T1 is L (~5 files)** — the one to watch; kept whole because RED-first couples the inverted fixture + gate-strength test to the re-key impl. Splittable at EXECUTE if the executor finds it too large. |
| 7 | **Risk coverage** | ✅ | Every identified risk (gate-weakening, resolution edge cases, false-positive Epic-close, shared-predicate disturbance, seam leakage, wrong-list edit) has a mitigation, and the load-bearing ones are encoded as acceptance criteria (AC1.3/1.4, AC2.2/2.7, AC3.4, AC4.6). |
| 8 | **Vertical slicing** | ✅ | Each task is a complete bug slice (failing test → fix → green → catalog update), not a horizontal layer. No "do all the impl, then all the tests" split. |

**Overall: PASS.** No blocking gaps; ready for EXECUTE on approval.

## Nyquist Test-Coverage Map

Library-level fixes → **unit** + **integration** (against real git/fs fixtures). No E2E/UI surface. Every AC has ≥1 owning test; strictness (proof-of-fail-before-pass) is enforced RED-first in EXECUTE and re-checked in VERIFY.

### T1 / FR1 — B24
| AC | Test | Type |
|---|---|---|
| AC1.1 migrate succeeds on pre-existing closed-block dangle | invert `migrate-dangling-baseline.test.js:162-219` | integration (real evict + fs) |
| AC1.2 dry-run/apply agree | new parity test on the B24 fixture | integration |
| AC1.3 introduced dangle still aborts (non-injected) | new gate-strength test (real moved-out-target) | integration |
| AC1.4 multiset — 2nd dangle to same missing target aborts | new unit/integration on `computeDanglingDelta` + apply | unit + integration |
| AC1.5 `computeDanglingDelta` returns only NEW dangle w/ new field | update existing unit test | unit |
| AC1.6 named limitation documented | `BUGS.md` + decision note (doc assertion) | doc |

### T2 / FR2 — B26
| AC | Test | Type |
|---|---|---|
| AC2.1 fires on M5.E4 self-hosted scenario | new `ship-fr1.test.js` case | unit (pure predicate) |
| AC2.2 no false-fire on maintained pending row + full phases | new negative case | unit |
| AC2.3 tier-aware (SKETCH/SPIKE/FEATURE/FULL) | parametrized cases over tiers | unit |
| AC2.4 ~~Layer 3 covered~~ **STRUCK — descoped** (no-op: Layer 3 runs at EXECUTE, fallback exists at SHIP) | — | — |
| AC2.5 Layer 2 re-keyed off phase:SHIP | new `checkProposedStateWrite` test | unit/integration |
| AC2.6 existing ship-fr1 cases still pass | existing suite | unit |
| AC2.7 other `detectDirtyExecute` trigger undisturbed | regression assertion at :600-603 | unit |

### T3 / FR3 — B25
| AC | Test | Type |
|---|---|---|
| AC3.1 discriminates (broken twin FAILs, real PASSes) | new interleaving harness + twin | integration (real lock + fs) |
| AC3.2 behavioral (on-disk lost-update check) | same harness assertion | integration |
| AC3.3 throw-lock semantics (arriving writer fails fast) | same harness | integration |
| AC3.4 seam inert in prod | `commands/*.md` grep + byte-identical assertion | unit + static |
| AC3.5 no re-entrancy regression (`promoteDrainEntry`) | targeted case | integration |
| AC3.6 full close — all 6 in `RMW_PATHS` | parametrized `it.each` | integration |

### T4 / FR4 — B6
| AC | Test | Type |
|---|---|---|
| AC4.1 STATE-only "+1" → not stale | existing/updated case | integration (real git) |
| AC4.2 single PLAN/PROGRESS-only → stale | new case (the RED) | integration |
| AC4.3 two PROGRESS-only → stale, count 2 | new case | integration |
| AC4.4 split STATE refresh (2 commits) → not stale | new case | integration |
| AC4.5 Case 2 + Case 3 still stale | existing cases | integration |
| AC4.6 `:136` pathspec test green | existing case | unit |
| AC4.7 CONTEXT.md classified bookkeeping | new case | integration |
| AC4.8 fail-open + hash short-circuit unchanged | existing cases | integration |

## Last Updated
2026-07-21 (PLAN)
