# M5.E1 — Doc-runtime & memory hygiene — Verification

**Phase:** VERIFY gate. **Tier:** FULL (`nyquist_enforcement: strict`, `gate_strictness: strict`).
**Verifies:** the EXECUTE deliverables against `M5.E1-PLAN.md` ACs + `M5.E1-VALIDATION.md` Part B test map.
**Suite:** 1065 passing / 52 files (999 baseline + 66 new). **Validator:** green. **Date:** 2026-07-16.

## Part A — Acceptance-criteria verification (per slice)

### S1 — doc-model (FR1) + STATE skeleton (FR2c) — commit `a8f5f9f`
| AC | Verdict | Evidence |
|---|---|---|
| Model doc covers the two axes, destination rule, 3-vector taxonomy, ordered gate | ✅ PASS | `references/doc-runtime-model.md` §§1–5; `tests/doc-runtime-model.test.js` (8 section-presence cases) |
| `state-schema.md` documents the body skeleton | ✅ PASS | `references/state-schema.md` § Body skeleton (live-above-the-fold) |
| `initState` output matches the skeleton headings | ✅ PASS | `tests/state.test.js` "emits the live-above-the-fold body skeleton headings (FR2c)" |
| INDEX hand-curation stance unchanged (derived-index decision deferred → E2) | ✅ PASS | No INDEX write in E1; model doc §6 flags the derived-vs-hand-curated conflict for E2; validator green |

### S2 — migration relocate (FR2a) + tier-aware size (FR2d) — commits `bdb35d0`, `d7beb0e`
| AC | Verdict | Evidence |
|---|---|---|
| Legacy migration → STATE.md pointer + STATE-HISTORY.md; frontmatter byte-identical | ✅ PASS | `tests/state-schema.test.js` (relocation verbatim, lean pointer body, frontmatter round-trip) |
| Re-run migration = no-op (idempotency) | ✅ PASS | `tests/state-schema.test.js` "is idempotent — re-calling … is a no-op" (`already-frontmatter`, `after2===after1`) |
| Tier→threshold applied per PROFILE; flat fallback when absent | ✅ PASS | `tests/state-size.test.js` (SKETCH 75 / FEATURE·SPIKE 150 / FULL 300 KB; unknown→flat 150; no/malformed PROFILE→fallback) |
| Stale `detectStateSize` message replaced | ✅ PASS | message now points at `/sig:checkpoint`\|`/sig:ship` eviction (M5.E1 FR2b); string-asserted in `state-size.test.js` |

### S3 — evict-on-close + faithfulness gate (FR2b) — commits `3a4970a` (S3a), `4675772` (S3b)
| AC | Verdict | Evidence |
|---|---|---|
| Closed Epic narrative → pointer in STATE + archived narrative | ✅ PASS (fixture) | `tests/evict-on-close.test.js` "evicts a closed Epic: narrative → archive, pointer in STATE (AC1)" |
| In-flight/other Epic block byte-identical (untouched) | ✅ PASS (fixture) | `evict-on-close.test.js` "leaves the other Epic block byte-identical (AC2)" |
| A **lossy** card FAILS the gate; a golden card PASSES | ✅ PASS | `tests/evict.test.js` "FAILS a lossy card and names exactly what was dropped" / "PASSES a golden card" — the R1 risk-catcher |
| Carry-over lifted to a live section | ✅ PASS (fixture) | `evict-on-close.test.js` "lifts the open carry-over into a live section (AC4)" (asserts position above the pointer) |
| Original byte-identical in archive (zero-loss) | ✅ PASS (fixture) | `evict-on-close.test.js` "archives the original byte-identical (AC5, zero-loss)" |

> **Known limit (see Part E-1):** S3's ACs are verified on **fixtures**. The wired live path (`ship.md §5.5`) was **never live-fired** in EXECUTE — carry into REVIEW.

### S4 — FUTURE-IDEAS physical eviction (FR3) — commits `7fd68d5`, `ba30267`
| AC | Verdict | Evidence |
|---|---|---|
| Shipped/promoted entry leaves inbox → ledger; 2nd drain doesn't re-surface | ✅ PASS (fixture **+ live**) | `tests/drain.test.js` AC1 + dedupe; **live dogfood**: 6 evicted, 2nd run byte-identical no-op (S5) |
| Deferred entry survives in inbox | ✅ PASS | `drain.test.js` "AC2: a DEFERRED entry survives … never leaks to the ledger" |
| Dangling-fence → scoped no-op + warning, file uncorrupted | ✅ PASS | `drain.test.js` dangling-fence case (`danglingFence:true`, byte-identical) |
| Crash between ledger+inbox → no dupe/loss on re-run | ✅ PASS | `drain.test.js` "AC4: crash between ledger-write and inbox-write → re-run: no dupe, no loss" (renameFn throws only on inbox write) |
| Non-target blocks byte-identical | ✅ PASS | `drain.test.js` "AC5: non-target (surviving) blocks are byte-identical after eviction" |

### S5 — dogfood on Signal's own `.planning/` — commit `1f4ba14`
| AC | Verdict | Evidence |
|---|---|---|
| Signal STATE.md body shrinks (64 KB → lean skeleton + pointers) | ✅ PASS (verified-once) | STATE.md 64,496 → 1,040 bytes; frontmatter byte-identical (measured in S5) |
| STATE-HISTORY.md holds the relocated body verbatim | ✅ PASS (verified-once) | `.planning/STATE-HISTORY.md` = legacy body verbatim (64,367 bytes) |
| `/sig:resume` + `/sig:status` orientation unchanged pre/post | ✅ PASS (verified-once) | pre/post orientation identical (phase PLAN, epic M5.E1, tier FULL, next `/sig:execute`, last-completed S4) — measured in S5 |
| Link-health green | ✅ PASS | STATE→STATE-HISTORY resolves; no anchor links into the evicted FUTURE-IDEAS headings |
| Shipped FUTURE-IDEAS entries in the ledger, inbox lean | ✅ PASS (**live**) | 6 → ledger; inbox 187,488 → 169,491 bytes, 41 → 35 entries, 0 terminal remaining |

> **Nyquist note (see Part C + E-2):** S5's two orientation ACs map to `tests/dogfood-orientation.test.js` in VALIDATION Part B. That file was **not written**; the ACs were verified **once, manually**. Resolved below as a conscious decision, not a silent skip.

## Part B — Full test suite
`npm test` → **1065 passed / 52 files**. No pre-existing test regressed (999 baseline preserved; +66 new). Clean run, no flakes.

## Part C — Nyquist compliance (strict)

**Mapped-test existence (VALIDATION Part B):** every mapped AC has ≥1 implemented test **except** the S5 pair — resolved below. Per-file case counts: doc-runtime-model 8 · state 16 · state-schema 26 · state-size 18 · evict 18 · evict-on-close 8 · drain 68.

**Proof-of-fail (strict "failed before fixed"):** the audit trail is **slice-level, not per-test red→green git commits** — EXECUTE's atomic-commit-per-slice bundled each slice's tests + implementation in one commit, so strict-evidence **form (a)** (a test-only commit showing CI red, then a green impl commit) was **not** separately maintained. Strict-evidence **form (b)** attestation is provided at slice granularity: the delegated slices (S2, S4) were executed under explicit TDD briefs (test-first); S1/S3 tests were authored within the slice alongside implementation. **In lieu of a per-test red→green trail, the load-bearing negative tests are proof-of-fail *by construction*** — each asserts a *distinguishing* outcome that a wrong implementation cannot satisfy:
- lossy-card → `pass:false` naming the dropped `D-M5E1-3`/`AC3`/date/token; golden → `pass:true` (a vacuous always-pass gate fails this pair);
- crash-injection → data survives in both files + re-run has no dupe/loss (an inbox-first ordering loses data → fails);
- migration frontmatter byte-identity (a dropped field → fails); Deferred-survives (a mis-classified predicate → fails); non-target byte-identical splice.

This is honest to the strict bar: coverage + failure-discriminating tests are present and green; a per-test git red→green trail is **not** claimed where it does not exist. M5.E1 was calibrated FULL/strict from DISCUSS, so this is **not** the "shipped before strict mode" permanent-gap case.

**The one gap — S5 `dogfood-orientation.test.js` — resolved (option b, deliberately declined):** S5's two orientation ACs describe a **one-time historical event** (relocating Signal's specific 64 KB body; evicting its 6 promoted entries), not a standing invariant. A live-`.planning/`-coupled standing test was **considered and declined** because it false-fails on normal growth (a stamped-but-not-yet-drained terminal entry; a growing In-flight section) and re-introduces the fixture-coupling correctly avoided across the rest of the suite. The **underlying mechanisms are fixture-tested**: `evictTerminalToLedger` (`drain.test.js`), frontmatter-preserving writes (`state-schema.test.js` / `state.test.js` skeleton), coverage gate (`evict.test.js`). The dogfood's own numbers are recorded in `M5.E1-PROGRESS.md` S5 + reproduced in this report. **Conscious decision, documented — not a silent skip.**

## Part D — Build verification
- `npm run validate` → **"Plugin validation passed."** (the effective quality gate).
- No new runtime warnings; no new dependencies; no manifest change.
- `npm run lint` → **broken (BUGS.md B5)** — ESLint v9 pins no flat config; **pre-existing** (predates M5.E1), tracked, not a blocker. The validator + vitest suite are the effective gate.

## Part E — Known limits & carry-forwards (into REVIEW / M5.E2 / M5.E3)
1. **FR2b `evictEpicNarrative` is fixture-proven, armed but never live-fired.** Wired into `ship.md §5.5`; first real firing is a *future* Epic's SHIP with a `## <EpicId>` body section. It **no-ops at M5.E1's own SHIP** (skeleton body has no `## M5.E1` heading). **REVIEW must scrutinize the wired path.**
2. **S5 `dogfood-orientation.test.js` deliberately not written** — see Part C (verified-once historical event; live-coupled standing test declined by design).
3. **M5.E3 finding:** relocating an *already-migrated* inlined body (vector-2-existing) has **no shipped mechanism** (FR2a is legacy-only; FR2b is Epic-scoped). S5 used a one-off hand script (`scratchpad/s5-relocate.mjs`). E3/FR6 builds this; must not assume it exists. (`doc-runtime-model.md` §3 corrected.)
4. **Doc refresh deferred to SHIP** (normal close-time step): `INDEX.md` (add STATE-HISTORY.md + ledger rows, refresh corpus counts) + `CONTEXT.md` narrative (M5.E1 EXECUTE progress).

## Verdict
**PASS.** All plan ACs verified with evidence; full suite green; Nyquist compliance satisfied (one mapped test consciously declined + documented; strict proof-of-fail met via failure-discriminating negative tests, with the audit-granularity caveat stated honestly); build clean. Three known limits carried forward — none blocking. Ready for REVIEW.

## Last Updated
2026-07-16 — VERIFY complete. PASS with 4 documented carry-forwards.
