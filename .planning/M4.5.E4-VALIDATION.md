# M4.5.E4 — Plan Validation (PLAN phase)

**Phase:** PLAN (output of `/sig:plan` Steps 4–5, 2026-06-02). **Tier:** FULL — all 8 dimensions + strict Nyquist mapping.
**Validates:** `M4.5.E4-PLAN.md` against `M4.5.E4-REQUIREMENTS.md`.

## 8-Dimension validation

| # | Dimension | Verdict | Notes |
|---|---|---|---|
| 1 | **Goal alignment** | ✅ PASS | All 10 tasks serve "runnable + current + self-explaining example, prose comparison page, both linked." No task is orphaned from the goal. |
| 2 | **Completeness** | ✅ PASS | Every FR + AC maps to a task (see coverage table below). No requirement unaddressed. |
| 3 | **Dependency correctness** | ✅ PASS | Slice 1 internal chain (t1→t2→t3→t4/t5) is real and minimal; Slices 1 & 2 are independent; Slice 3 correctly waits on both. No false or missing edges. |
| 4 | **Testability** | ✅ PASS (with honest caveat) | AC2/AC4/AC9 are automated-test-backed. AC1/AC3(counts)/AC5/AC6/AC7/AC8 are VERIFY-phase inspection checks — legitimate for a docs/example Epic; forcing automated tests on prose (e.g., a markdown link-checker) would be gold-plating unless cheap (t10 may script it). |
| 5 | **Scope discipline** | ✅ PASS | One example (D-E4-5), ≤80-line vs.md, storage swap is the minimal R1 fix, validator change is one line. No gold-plating; the SQLite-flavor rabbit hole was explicitly cut (decide-plumbing-by-default). |
| 6 | **Context feasibility** | ✅ PASS | Every task is S or M, ≤~5 files. t3 (M) and t6 (M) are the largest but each fits one focused session. |
| 7 | **Risk coverage** | ✅ PASS | R1 resolved (RESEARCH §1); the 4 residual risks each have a mitigation in PLAN § Risks; the guard test (t5) is itself a risk-mitigation against future rot. |
| 8 | **Vertical slicing** | ✅ PASS | Three slices each ship a complete user-visible thing (example / comparison page / wired-up README). Tasks within a slice are steps of one slice, not horizontal layers. |

**Overall: PASS across all 8 dimensions.** No dimension requires a re-plan.

## Requirement → task coverage

| FR / AC | Task(s) |
|---|---|
| FR1 (examples/ structure) | t1, t2, t3 |
| FR2 (annotated README) | t4 |
| FR3 (current conventions) | t3 |
| FR4 (currency guard) | t5 |
| FR5 (vs.md prose guide) | t6 |
| FR6 (README links) | t8 |
| FR7 (CHANGELOG) | t9 |
| FR8 (validator wiring) | t7 |
| AC1 (example exists, no node_modules) | t1 |
| AC2 (example `npm test` passes) | t2 |
| AC3 (schema_version:1 + counts + drift note) | t3 |
| AC4 (root guard, suite green) | t5 |
| AC5 (vs.md ≤80 lines, content) | t6 |
| AC6 (README links both) | t8 |
| AC7 (no dead links) | t10 |
| AC8 (CHANGELOG entry) | t9 |
| AC9 (validator green, no new deps) | t2 (removes dep), t7 |

Every requirement and acceptance criterion is covered. No coverage gaps.

## Nyquist test-coverage mapping (strict)

Strict Nyquist = every acceptance criterion has a verification path; proof-of-fail-before-pass is enforced in VERIFY. Honest mapping for a docs/example Epic:

| AC | Verification type | Concrete mechanism | Proof-of-fail (VERIFY) |
|---|---|---|---|
| AC2 | **Automated (unit/integration)** | example's own vitest suite (~39 tests) via `npm test` in `examples/url-shortener/` | The stale tree already fails 22/39 (ABI drift) → post-swap green is the red→green evidence |
| AC4 | **Automated (guard)** | `tests/example-currency.test.js`: `readProfile` resolves + `readState._schema === 1` | t5 verify step: revert STATE.md to legacy → guard fails → restore → passes (explicit red→green) |
| AC9 | **Automated (validator)** | `tests/validate-plugin.test.js` subprocess asserts exit 0 after `docs/vs.md` added to REQUIRED_FILES; `package.json` shows no `better-sqlite3` | Adding `docs/vs.md` to REQUIRED_FILES before the file exists → validator fails → file present → passes |
| AC1 | Inspection | VERIFY: `examples/url-shortener/` tree present; `find` shows no `node_modules`/nested `.git` | n/a (structural) |
| AC3 (counts) | Inspection | VERIFY: read reconciled counts across REQUIREMENTS/1-SHIP/1-VERIFICATION; confirm drift-audit note present | n/a (content) |
| AC5 | Inspection | VERIFY: `wc -l docs/vs.md` ≤ 80; read against the 5 accuracy traps | n/a (content) |
| AC6 | Inspection | VERIFY: README contains both links, resolving | n/a (content) |
| AC7 | Inspection / light script | t10 link sweep across new/changed docs | n/a (content) |
| AC8 | Inspection | VERIFY: CHANGELOG E4 entry present | n/a (content) |

**Nyquist verdict:** every AC has a verification path; the three behavior-bearing ACs (AC2, AC4, AC9) carry automated tests with explicit proof-of-fail evidence. The content/structure ACs are VERIFY-phase inspection — appropriate and not under-covered for a docs/example Epic.

## Gate readiness

- [x] PLAN.md exists with vertical slices + acceptance criteria
- [x] RESEARCH.md captures findings (incl. R1 resolution + env check)
- [x] VALIDATION.md maps tests to requirements
- [x] Plan passes 8-dimension validation
- [ ] User explicitly approves the plan
