# v0.1.6 — Plan Validation (8-dimension) + Nyquist Mapping

Plan: `v0.1.6-PLAN.md`. Spec: `v0.1.6-REQUIREMENTS.md`. `plan_validation_dims: all` (FULL), `nyquist_enforcement: strict`.

## 8-Dimension Validation

| # | Dimension | Verdict | Note |
|---|---|---|---|
| 1 | Goal alignment | **PASS** | All 5 slices serve "prevent new doc-integrity pathology + converge two papercuts": FR1 blocks frontmatter prose, FR2 flags bloat, FR3 converges the drain, FR4 fixes derived titles, FR5 is bug hygiene. |
| 2 | Completeness | **PASS** | Every FR (5) and AC (FR1 1.1–1.7, FR2 2.1–2.4, FR3 3.1–3.3, FR4 4.1–4.3 + edge cases, FR5 5.1–5.3) maps to a slice. |
| 3 | Dependency correctness | **PASS** | Only real dep: S4→{S2,S3} (so B3/B4 land as `fixed`). S1/S2/S3/S5 independent — minimal + accurate. |
| 4 | Testability | **PASS** | Every AC has a test type below except AC3.3 (verification-only; strict-Nyquist exception, documented — a live-file candidate-count check, no stable unit fixture). |
| 5 | Scope discipline | **PASS** | No gold-plating. Explicitly rejected: delta-only FR1 (b), write-time size-warn, drain option (b), STATE eviction (→M5), a 5th footer-drift fix (already shipped). |
| 6 | Context feasibility | **PASS** | Each slice ≤ ~3 files; S5's 4 lib/command files are one mechanical banner feature mirroring an existing template. All fit one agent context. |
| 7 | Risk coverage | **PASS** | Every P1/P2 in RESEARCH maps to an AD + AC (see PLAN risk table). The three P1s (blocker false-positive, fail-open-on-target, re-trip lockout) each have a dedicated AC (1.6, 1.5, 1.7). |
| 8 | Vertical slicing | **PASS** | Each slice is a full vertical (logic + wiring + tests), independently shippable and committable. |

**Overall: 8/8 PASS.**

## Nyquist Test-Coverage Mapping (AC → test)

| AC | Test type | Location / harness |
|---|---|---|
| AC1.1 completed_phases multi-line/>150 → block | unit (pure-fn + DI) | `hook-state-write.test.js`; cmmc frontmatter fixture |
| AC1.2 blockers.text multi-scalar/>500 → block | unit | `hook-state-write.test.js` |
| AC1.3 well-formed → allow (+58-char annotated, Signal's own) | unit | `hook-state-write.test.js` |
| AC1.4 retro-absence SHIP block unchanged | unit (regression) | existing 6 `checkProposedStateWrite` cases stay green |
| AC1.5 fail-open (bad input / parse-throw / non-STATE) | unit | `hook-state-write.test.js` |
| AC1.6 legit multi-line blocker object → allow | unit | orphan-fixture-shaped input, incl. semicolon `text` |
| AC1.7 clean-frontmatter write allowed regardless of prior state | unit | feed already-polluted "current" + clean proposed → allow |
| AC2.1 >150 KB → banner (resume/status/checkpoint) | unit | `state-size.test.js` (over-size fixture) + renderer tests |
| AC2.2 62 KB fixture → no banner | unit | controlled fixture (NOT live STATE.md) |
| AC2.3 tiny → no banner | unit | `state-size.test.js` |
| AC2.4 read-only, never blocks | unit | assert no mtime change; banner is advisory string |
| AC3.1 blockquote-only entry → excluded | unit (inline string) | `drain.test.js` |
| AC3.2 existing detection + 7/5 snapshot green; `Update` not matched | unit (regression) | `drain.test.js` |
| AC3.3 live-file candidate count drops (~42→~32) | **verification-only** | run detector on post-FR5 file; attested in VERIFICATION |
| AC4.1 cut at `—`/`.`/`:`/`,`+ws | unit (via buildFutureIdeasEntry) | `add.test.js` |
| AC4.2 no early boundary → fallback | unit | `add.test.js` |
| AC4.3 60-char cap + sentence-case retained | unit (regression) | existing cap test stays green |
| AC4 edge: URL / em-dash≠hyphen / char-2 floor / `?!` excluded | unit | `add.test.js` new cases |
| AC5.1 3 BUGS rows + counter | unit / doc-assert | BUGS.md content assertion |
| AC5.2 3 entries removed from FUTURE-IDEAS | unit | `parseEntries().length` −3; headings absent |
| AC5.3 footer intact, lint ok, drain parses | unit (dogfood) | `future-ideas-footer-lint.test.js` |

**Strict-Nyquist stance:** every AC gets a RED-first test in VERIFY **except AC3.3** (verification-only — documented attestation, mirrors E10's AC6.4 pattern). No permanent coverage gaps introduced.

## Last Updated
2026-07-13
