---
epic: M6.E8
phase: EXECUTE
created: 2026-09-14
---

# `M6.E8` — Progress

Plan: [`M6.E8-PLAN.md`](M6.E8-PLAN.md). Waves as planned: wave 1 = S1; wave 2 = S2 ∥ S3.

## Wave 1 — S1, measure first and tell the truth about the corpus

- [x] t1.1 — the live-measurement test and the three `*_MEASURED` constants — commit `8d2b4c0`
- [x] t1.2 — the retrospective read deleted; `ADVISOR_SOURCES` is four — commit `c36fa8d`
- [x] t1.3 — the milestone read kept, with its reason in the source — commit `392d261`
- [x] t1.4 — `consulted` derived from what `rankRows` was given; `backlogDischargeStatus` gains `sources` — commit `6c046e5`

**Checkpoint S1:** suite green (3395); the fixture artifact names four sources and three consulted;
the measurement test pins 2 / 3 / 4. One unplanned commit in the wave: `caae4b5`, STATE.md crossed
its 40 KB ceiling after the PLAN transition and was brought back to 14 KB by relocating two closed
blocks to the archive verbatim.

**Found while building t1.4, recorded in the code:** a corpus with no id-led row is honestly
*"`BACKLOG.md` only"* — input 3 opens its closure sources only when it has something to look up —
so the AC7.1 fixtures carry a unit-led row, and the *Read, not consulted* wording for a source input
3 did not open is neutral, because `sources` cannot tell "could not" from "had no reason to".

## Wave 2 — S2 (fold input) ∥ S3 (bug-discharge input, two corrections, docs)

- [ ] t2.1 — `declaresWorkMovedElsewhere` in `backlog.js`, `KEPT` first — pending
- [ ] t2.2 — input 7 in `rankRows`, with its reason; `AC4.3` pinned — pending
- [ ] t3.1 — `declaresBugDischarge` + the bug-discharge input; `AC1.3` pinned — pending
- [ ] t3.2 — `FR2`: `BLOCKED_RE` unchanged, the measurement recorded beside it; `AC2.1′` — pending
- [ ] t3.3 — `FR3`: the discharge reason names its source and evidence; three `AC3.2` fixtures — pending
- [ ] t3.4 — docs and the changelog — pending
