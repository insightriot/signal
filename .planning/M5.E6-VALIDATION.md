# M5.E6 — Plan Validation & Nyquist Map

Tier FULL/strict → `plan_validation_dims: all` (8) + `nyquist_enforcement: strict` (every AC maps to a test; proof-of-fail-before-pass enforced in VERIFY).

## 8-Dimension Plan Validation

| # | Dimension | Verdict | Notes |
|---|---|---|---|
| 1 | **Goal alignment** | ✅ | Every task serves "finish the doc-runtime's self-maintenance." Sweep (FR1/2) = the maintenance command; FR4/5/6 + B31 = the carry-over defects that would otherwise dangle into the re-audit; FR3 = the map-freshness hook; FR7 close-out = mark shipped work done. No task off-goal. |
| 2 | **Completeness** | ✅ | All 7 FRs' ACs are covered by ≥1 task (map below). Spec corrections (AC4.4 wording, FR7 re-scope) folded in. B31 added by decision. |
| 3 | **Dependency correctness** | ✅ | Only real dep chain: Slice 1→2→3 (checks→orchestrator→command). FR4/5/6/FR3/T21 genuinely independent (disjoint files). B31 sequenced last to isolate `/sig:add`. Shared-file serialization (BUGS.md, roster sites, rmw-lock.test.js) called out in PLAN. |
| 4 | **Testability** | ✅ | Every task has an AC + a RED-first test. The 3 "hard-to-test honestly" spots are handled explicitly: AC4.3 (residual-abort discriminator instead of an unbuildable inline-introduced fixture), AC2.7 (true-positive-retention fixture), AC7.5 (structural assertion that the prompt precedes lock acquisition). |
| 5 | **Scope discipline** | ✅ | Not-Doing list enforced (`--code`, inbox-curation, OBSERVATIONS.md, `--fix`). FR7 shrank to reality. B31 is the one scope addition — an explicit Brett-approved decision (D-M5E6-5), not creep. AD5/AD6 are minimal defaults, not gold-plating. |
| 6 | **Context feasibility** | ✅ | No task exceeds M (3–5 files). The two M-heavy areas (T1 doc-hygiene refactor, T23 add.js RMW) are single-subsystem. No XL. |
| 7 | **Risk coverage** | ✅ | 7 risks tabled with mitigations; the two High risks (guard-breakage, add.js destabilization) have byte-identical gates (existing suites must stay green). Gate-weakening (B27) covered by a dedicated tightness pair + discriminator. |
| 8 | **Vertical slicing** | ✅ | Each slice is a shippable unit (a check + its test; a bug + its RED-first fix + close-out). Not horizontal (no "all libs then all tests"). Sweep is 3 vertical slices (foundation→orchestrator→command), each leaving the suite green. |

**Anti-rationalization gate:** no unresolved gray areas block EXECUTE (all 4 DISCUSS gray areas + B31 decided); every task has acceptance criteria + a verification step; TDD/RED-first is explicit per task; slices are vertical; checkpoints exist after each wave.

## Nyquist Test-Coverage Map (AC → test type)

Legend: **U** unit · **I** integration (lib boundary / multi-fn) · **E2E** end-to-end (full `applyMigrate`/`runSweep`/`shipFR1Check` flow). All RED-first.

### FR1/FR2 — `/sig:sweep`
| AC | Test | Type |
|---|---|---|
| AC1.1 runs on invoking project | `runSweep(fixtureDir)` scans that dir, no Signal paths | E2E |
| AC1.2 `.planning/` in, `archive/` exempt | broken link in `.planning/` flagged; in `archive/` not | I |
| AC1.3 no plugin.json → Signal-only skipped+stated | stranger-repo fixture; roster/version/frontmatter absent+reported skipped | E2E |
| AC1.4 plugin.json → all checks | Signal-repo fixture, all checks run | E2E |
| AC1.5 read-only | before/after tree-hash unchanged | E2E |
| AC1.6 structural/advisory grouping | report groups by severity | U (renderSweepReport) |
| AC1.7 roster 17→18 reconciled | hygiene guard green post-bump | I (guard) |
| AC2.1 index-freshness pure | drift→structural, fresh→pass, **0 writes** | U + tree-hash |
| AC2.2 stale-inbox advisory | N>0 advisory, 0→none | U |
| AC2.3 CLAUDE.md bloat | over→advisory, under→none | U |
| AC2.4 command-frontmatter | missing/empty desc flagged; malformed YAML caught | U |
| AC2.5 portable on stranger repo | portable fire, Signal-only absent | E2E |
| AC2.6 determinism+offline | two runs byte-identical; source-grep no-network | I + meta |
| AC2.7 no false-positive flood | real dead link outside code flagged; `](x.md)` in backticks not | I (fixture) |

### FR3 — docs/map line
| AC3.1–3.3 | ship.md carries the two-screen line; "no change" valid; no mechanical enforcement | inspection (doc task) |

### FR4 — migrate gate
| AC | Test | Type |
|---|---|---|
| AC4.1 B27 flags not aborts | inverted `:284-335`: migrate succeeds + flag | E2E |
| AC4.2 B28 absolute dangle survives | e2e migrate succeeds, block byte-identical + unit no-edit for `](/x.md)` | E2E + U |
| AC4.3 gate not weakened | residual-flat-path abort (e2e) + partitionDangling tightness pair (U) | E2E + U |
| AC4.4 flag surfacing | distinct renderDryRun line + captured in apply warnings, consistent | U + E2E |
| AC4.5 BUGS B27/B28→fixed | inspection | — |

### FR5 — retro-gate (B30)
| AC | Test | Type |
|---|---|---|
| AC5.1 fresh-flow halts | `phase:REVIEW`, no-row, no-retro → NO_RETRO_FILE (RED on main) | U (shipFR1Check) |
| AC5.2 proceeds w/ retro + isEpicClose | retro present → halt:false AND isEpicClose:true | U |
| AC5.3 no false-fire | maintained-pending / SKETCH-short / mid-flow-EXECUTE skip; SPIKE `:339` stays skipped | U |
| AC5.4/5.5 recovery-clean + BUGS | nothing persisted on halt; B30→fixed | U + inspection |

### FR6 — `_afterRead` guard (B29)
| AC | Test | Type |
|---|---|---|
| AC6.1 pollution not invoked | `Object.prototype._afterRead` across 6 RMW_PATHS → not called (RED on main); `afterEach` cleanup | I |
| AC6.2 B25 seam unaffected | interleaving suite unchanged-green | I |
| AC6.3 prod byte-identical | default-undefined path unchanged | U |
| AC6.4 BUGS B29→fixed | inspection | — |

### FR7 — concurrency close-out + B31
| AC | Test | Type |
|---|---|---|
| AC7.1 BACKLOG done | inspection | — |
| AC7.2 B31 RED | add-write vs drain-disposition interleaving → lost update on main, none after fix (twin-discriminated) | I |
| AC7.3 B31 fix (writes under `.state.lock`) | add's inbox/OQ/BUGS write mutually exclusive with drain | I |
| AC7.4 no re-entrancy/deadlock | add completes under lock (would hang on naive self-lock) | I |
| AC7.5 prompt not under coarse lock | structural: scrub decision precedes `.state.lock` acquisition | U/inspection |
| AC7.6 existing suites green + byte-identical | add.test.js + rmw-lock.test.js green; B31→fixed | I |
| AC7.7 coverage symmetry | regeneratePlanningIndex in RMW_PATHS | I |

**Coverage verdict:** every AC has a mapped test; no AC is inspection-only except the pure doc/bookkeeping tasks (FR3, the BUGS/BACKLOG status flips), which have no runtime surface. Strict Nyquist satisfied for PLAN → VERIFY will enforce proof-of-fail-before-pass on each.
