---
epic: M6.E4
tier: FEATURE
phase: EXECUTE
created: 2026-08-23
---

# M6.E4 — Progress

## Wave 1
- [x] **S2 — a standing entry stops being an undrainable candidate** — see commit below

**The measurable outcome (`AC2.2c`): the inbox reports 0 live candidates and 1 standing.**
It reported 1 live before, and could never have reported 0.

**The never-run branch ran.** `commands/plan.md` Step 1b's *"No candidates"* path had never
executed on this repo, because the live count was structurally pinned at ≥ 1. It executed, printed
`(no inbox candidates to drain)`, and did not throw. Working was the likely outcome; running it
against a two-entry inbox rather than a stranger's clean one was the point.

**One defect avoided during the slice, not after it.** `listStandingEntries` was briefly exported
with **no caller but its own test** — `bugs-tally.js`'s exact shape, which derived the right number
for releases while `BUGS.md` went stale because nothing called it (`M6.E2` had to fix that). Wired
into `commands/plan.md` Step 1b, and pinned by an assertion that the command *calls* it rather than
merely mentions it (`tests/trigger-watchlist.test.js:109`'s precedent).

**⚠ A commit in this wave landed RED, and the record says so.** `95b9df0` was pushed with **two
failing tests**. Cause was process, not code: `npm test 2>&1 | tail -4 && git commit` **does not
gate** — the pipe to `tail` swallows npm's non-zero exit, so the failures scrolled past and the
commit ran anyway. Fixed forward in `1bd6cd8`; the habit is now `npm test && git commit`, which
stops on failure.

**What was red was worth knowing.** `.planning/ADHERENCE-LOG.md` publishes a **derived** directive
count, and S2's `plan.md` edit changed it: **523 → 527** directive lines, `plan.md` 49 → 53.
`node tools/adherence-ceiling.js` regenerates it, and the failing assertion **names that remedy in
its own message**. This is `M6.E2`'s machinery doing precisely its job — a published fact went stale
the instant its underlying artifact moved, and a derived-then-compared check caught it in minutes
rather than a reader noticing months later.

**One of the 4 added directive lines is trace-measurable** (97 → 98 naming a real `tools/lib`
export) because it names `listStandingEntries`. That is the property separating this wiring from the
**78.2%** the ceiling reports as having no observable trace.

**Tests:** 2929 → **2946**. 17 new in `tests/drain-standing.test.js` (2929 + 17 = 2946, derived not
asserted). **RED first** — 10 of 12 failed before implementation. Two were added after review, to
pin an entry carrying a marker but **no `**Status:**` line** — the header-region window is bounded by
the Status line, and hand-added entries (the ones most likely to be permanent) are exactly the shape
that lacks one. The implementation already behaved correctly; it was simply unasserted.

## Wave 2
- [x] **S1 — the plan reads its own numbers** — see commit below

**Reach: 10 of 13 corpus projects** — better than `M6.E2`'s checks, three of which reach 1 of 12.
432 task units, **46 flagged (10.6%)**. Signal's own tree: 129 units, **18 flagged (14.0%)**, 10 of
19 files flagging zero.

⚠ **These are the THIRD measurement.** The first (28.6%) used a `###`-only split the fixture
refuted; the second (40 / 9.3%; Signal 20.9%) was taken with two `QUANTITY_RE` defects live, both
found by the PR #200 reviewer. Fixing them moved the numbers **both ways** — corpus recall up as
percentages became visible, Signal's own rate down by a third as ISO-date false positives cleared.
The published *"~2.2× denser"* claim was an **artifact of the defect**.

**⚠ The first corpus measurement was wrong, and the fixture proved it.** The initial method split
plans on **`### ` only** and produced 28.6%. Then `AC1.1a`'s fixture turned out to be
**`#### S1.t11`** — an h4 task nested under an h3 slice, the shape Signal's *older* plans use. A
method that cannot see the one task the check exists to catch is the wrong method. Corrected to
**any heading at level 3 or deeper**, re-measured, and the superseded figure left visible in
RESEARCH §6: *measured* and *measured correctly* are not the same claim.

**No `##` fallback, refused on evidence rather than taste.** The 2 non-evaluable projects use `##`
exclusively, and their h2 headings are **8 of 12** and **10 of 12** *section* vocabulary (phase
goal, dependencies, scope…), not tasks. Parsing them would flag `## Phase goal` as a task — noise
dressed as coverage. `cannot-check` with a stated reason is the honest answer, and it is a real
state: **3 of 13** projects land in it.

**A guard caught a `B82` violation mid-slice.** The detector's first draft defined its own
`/\bAC\d/` pattern. `tests/requirement-ids.test.js` fails the suite if any second `tools/lib`
module defines an FR/NFR/AC pattern — `requirement-ids.js` is the single owner. Rewritten to call
`matchIds(whole, AC_ID_RE)`. **The second implementation was prevented, not filed** — which is what
that guard was built for.

**Dogfood run (scheduled at VERIFY, run early):** the detector flags **S1 and S2 of this Epic's own
plan**, and `M6.E3-PLAN.md` returns `clean`. The obligation was then discharged by name — S1's
figures are measurements rather than thresholds gating a criterion, and S2's `0 live + 1 standing`
matches its own `AC2.2c` exactly. **Both consistent.**

**Tests:** 2946 → **2961** (15 new in `tests/plan-consistency.test.js`). RED first — the module did
not exist, so collection itself failed.

⚠ **`AC1.2c` ships open by design:** nothing fails if the plan-checker ignores the worklist. `B75`'s
shape at a smaller radius, disclosed in `plan-checker.md` at the point of use and pinned by a test
that the disclosure — and its `B75` cross-reference — is present.

## Wave 3
- [x] **S3 — the research reaches the builder** — see commit below

All three leaks closed, each pinned by a **cross-document** test (`M5.E17`'s shape) — the only kind
that can catch a prompt-layer omission, because there is no code path to assert against.

1. **`executor.md` `## Inputs` now declares `{phase}-RESEARCH.md`.** PLAN pays a codebase researcher
   to find *"existing patterns, reusable code, integration points"*, writes them to RESEARCH.md, and
   the executor's own input list did not mention the file. The work was paid for and thrown away.
2. **`plan.md`'s required-contents list now carries `Files likely touched`.** The defect was never
   that both ends lacked it — the planning **skill** has had the field all along while the
   authoritative **command** list omitted it. The test asserts **both ends**, so the *disagreement*
   is the failure, not the absence.
3. **A per-task `Out of scope` field now exists.** The executor's *"every changed line traces to the
   acceptance criteria"* was a discipline rule with no named boundary to point at.

**A reference, not the document** (`AC3.1c`): the exemplar pointer is a per-task field in the PLAN,
so it reaches the executor through the task it already receives. No dispatch change, and no bulk
injection of RESEARCH.md into every task's context — which would fight the constraint CLAUDE.md
names as this project's highest risk.

**Advisory, not fatal** (`AC3.2d`, `D-M6E4-7`): every corpus plan predates these fields and was
correct when written. `plan-checker.md` reports their absence under **completeness** — a `core`
dimension, so it runs at FEATURE — and the dimension count stays **8**.

**The adherence ceiling was regenerated BEFORE committing this time**, not after a red suite. Wave
1's lesson applied rather than restated.

**Tests:** 2961 → **2970** (9 new in `tests/task-handoff.test.js`). RED first — 7 of 9 failing.
