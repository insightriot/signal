---
tier: FEATURE
schema_version: 1

calibration:
  scope: feature
  stakes: minor
  novelty: familiar
  reversibility: trivial
  horizon: months

phases_skipped: []

rigor_overrides:
  tdd_required: true
  security_audit: basic
  performance_pass: true
  simplification_pass: true
  nyquist_enforcement: strict
  plan_validation_dims: core
  research_parallelism: 2
  gate_strictness: light
  context_rot_reread: true
  review_depth: quality-only

metadata:
  created_at: 2026-08-23T00:00:00.000Z
  created_by: sig:calibrate (per-Epic override, D-BR0823-2)
  escalation_history: []
---

# M6.E4 — Epic Profile

**FEATURE, not the project's FULL.** Chosen 2026-08-23 (`D-BR0823-1`). FULL is a ceiling, not a
floor, and this Epic does not meet FULL's stated triggers in `references/tier-definitions.md` — no
auth, no payments, no data integrity, no public API, no one-way architectural decision. It is new
work on an existing system, applying a known pattern, revertible with the commit.

**This is also the first real use of the per-unit dial `B90` was filed about** — measured
2026-08-08 across 12 local projects, 7 ran FULL and exactly 1 had ever written a per-unit profile.
The tier advisory fired at `/sig:resume` on 2026-08-23 and this is the response to it.

**What FEATURE changes here:** core plan validation (3 dimensions, not 8), 2 research agents
instead of 4, light gate strictness (confirm once per phase), `quality-only` REVIEW depth. All
seven phases still run — nothing is skipped.

**`attention` is deliberately unset**, so it derives from `gate_strictness: light` — which yields
`checkpointed`, not `attended`. Verified by running `attentionFor`, not by reading: the first draft
of this file asserted `attended` and was wrong. `checkpointed` is the intent anyway — confirm at
slice boundaries, not at every step.
