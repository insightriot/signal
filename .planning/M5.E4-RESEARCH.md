# M5.E4 — Research synthesis (4 parallel codebase agents)

**Method:** 4 parallel `codebase-researcher` agents (FULL/strict `research_parallelism: 4`), each pinning exact fix sites, proven patterns, and RED-first test strategy for one FR cluster. Domain/prior-art angles skipped — this is internal bug/hygiene work. All findings are file:line-grounded and, where noted, empirically re-run.

## Headline: the ledger is stale — 3 bugs are already (partly) shipped

M5.E3 REVIEW landed fixes that were never flipped in `BUGS.md` (same lag that hid B9). This **shrinks the code surface** but surfaced one **false-green** that matters:

| Bug | Real status | M5.E4 work |
|---|---|---|
| **B18** | **Already fixed** — commit `8f1dda8` (slice→`relative()` at `roster.js:49`, `planning-index.js:64`, `:332`) + tests | 1 residual `regeneratePlanningIndex('.')` round-trip test (AC names it specifically; only `enumeratePlanningDocs('.')` is covered) + flip `confirmed→fixed` |
| **B19** | **Mechanism shipped (`497267a`) but BROKEN** — predicate hole + missing dry-run half + defanged RED test | Real fix (see FR1) — the P2 *is* currently unprotected on the default path |
| **B8** | **Both halves shipped** (`WHOLE_FILE_NOTE` + vector-1 de-prose); regression exists but one-sided | ~2-line command-level `block:true→false` flip assertion + flip `confirmed→fixed` |

## FR1 — B19 (the live P2): the shipped guard has a predicate hole

- **Apply-side:** `migrate-memory.js:2359-2377` — the `if (needsV3)` block; regen at `:2375` guarded by the skip predicate at `:2370`; warning set `:2371-2373`, surfaced at `:2461`.
- **The hole (proven empirically):** the predicate `existingIndex.trim().length>0 && !hasAnnotations && ann.legend === null` fails on the **real** old INDEX, because the old curated format **carries the exact `**Tier legend:**` heading** (it's the ancestor of the new format) → `ann.legend !== null` → skip does **not** fire → `regeneratePlanningIndex` clobbers. Verified against `git show 62bfa5b~1:.planning/INDEX.md` (`byPath=0, byEpic=0, legend!==null, predicate-fires=false`).
- **Correct discriminator:** the **new-format PREAMBLE marker** — `renderPlanningIndex` always emits `> Auto-generated by /sig:index` (`planning-index.js:34-37`, `:259`). Rule: **`regenerate iff (empty OR hasMarker OR hasAnnotations); else skip+flag`** (`|| hasAnnotations` is belt-and-suspenders). Extract a shared **`isForeignIndexFormat(existingIndex)`** helper co-located with `PREAMBLE` in `planning-index.js`, called by **both** the apply block and the dry-run block (kills the string-coupling → AC1.2 "both places" is cheap).
- **Missing dry-run half:** `renderDryRun` (`migrate-memory.js:1703`, unconditional "regenerated" at `:1857-1859`) must apply the shared predicate and emit "INDEX.md → LEFT INTACT (foreign/pre-v3 format) — reconcile via `/sig:index`" when foreign.
- **AC1.1 caveat (advisor):** `applyArchiveTree` (`:2337`) runs **before** the skip point (`:2359`) and can legitimately rewrite INDEX as a *referrer* if the curated INDEX links to a moved scaffold. So AC1.1 is **"skip fired + curated notes preserved,"** *not* strict byte-identical — verify the exact delta against the chosen fixture (Signal's own old INDEX has root-resident links, so likely moot, but confirm).
- **Test (mandate RED against `main`):** the false-green shipped because the fixture lacked the legend block. The new RED test **must use the real `62bfa5b~1` INDEX** (legend + tables + `Gotcha:`) and prove RED against current `main` before the fix. Add a `renderDryRun` foreign-path test. Keep an AC1.3 new-format regression (marker present → regen still fires). File: `tests/migrate-index-guard.test.js` (current `FOREIGN_INDEX` at `:27` is the defanged fixture).

## FR2 — path/scan hardening (B14, B18, B15, B22)

- **B14 (security):** proven helpers `realpathNearestExisting` + `assertRealInsidePlanning` (`migrate-memory.js:204-242`), currently **unexported, duplicated** in migrate-memory.js + archive-tree.js. → **Extract to new `tools/lib/path-confine.js`**, import in the 3 new sites only; **leave the 2 existing copies** (dedup = scope-creep + collides with B15/B21/FR5 edits landing in migrate-memory.js this Epic). Invariant: anchor on **`dirname(destAbs)`, never the leaf** (`atomicWrite` renames over the leaf, never follows a leaf symlink).
  - **Site A — `evict.js:349-351`** (`evictEpicNarrative` archive write): highest value, exact mkdir→write→readback shape; append `assertRealInsidePlanning(...)` after the lexical guard, before mkdir `:353`. Rides existing `withStateLock`.
  - **Site B — `add.js:866-884`** (`assertSafeFilePath`, `--file` gate): **DOCUMENTED-RATIONALE REVERSAL** — the docstring (`:853-857`, `:837`) declares `--file` symlinks *out of scope* + "pure path-math, NO I/O." B14 reverses that (adds sync realpath I/O). **The docstring must be rewritten to match** — a conscious reversal, ratified in the PLAN approval ask, not a silent add.
  - **Site C — `resume.js:52-78`** (`resolveArtifactPath`, read-side): breaks the injectable `existsFn` test seam (`realpathSync` bypasses injection). → harden via an **injectable `realpathFn` param** (default `realpathSync`) so the test seam survives; lower severity (read, not write) — its own careful sub-task.
- **B18:** already fixed (`8f1dda8`); only residual = a `regeneratePlanningIndex('.')` write round-trip test + ledger flip.
- **B15:** `FILE_SCAN_CEILING = 1MB` (`migrate-memory.js:1438`) truncates 4 scanners. **`scanDanglingLinks` (`:1472`, trunc `:1482`) is the blocking gate** (feeds `computeDanglingDelta:1500` → abort) → **must scan full-file** (precedent: `:2534`/`:2895` "give it the FULL file"). The 3 advisory scanners → **flag-on-truncation** (push a `scan-truncated` finding; never silent). **B15 has a second half in doc-hygiene.js:102** — inside `checkInternalLinks`, same region as B22 → one task.
- **B22:** `doc-hygiene.js checkInternalLinks:93-123` — escape at `:110` (`resolve(dirname(f),pathPart)`) → `:111` existsSync / `:361` readFile outside root. ~3-LOC bound-to-root guard after `:110` (trailing-`sep` form; add `sep` to the `node:path` import). Bundled with B15-doc-hygiene.

## FR3 — migrate polish (B16, B21)

- **B16:** pre-apply tag created `:2262-2270` (git mode); design-intent (`:2257-2261`) keeps it on rollback as a last-resort pointer. Fix: **delete the tag AFTER `await rollback()` succeeds** in the catch (`:2412-2418`), before rethrow — preserves the intent (rollback-*failure* keeps the tag), idempotent (`tag -d` best-effort). Successful applies untouched. Test: `migrate-apply-failure.test.js` forced-rollback fixture + `git tag -l` empty. (Shares the fixture with B23(c) "cached empty after abort.")
- **B21:** gate loop `:2854-2859` calls `resolveId` per evicted ID → `buildDecisionIdMap` walks the whole corpus each call. Safe to **build once before the loop** (eviction writes disk *before* the gate `:2834-2852` → disk stable). **Key on resolver identity** (`resolveId === defaultResolveDecisionId`) so injected test mocks still hit the per-call path. Needs an **extraction** in `planning-index.js`: split `resolveDecisionId` (`:366-407`) into `buildDecisionIdMap` (exists, `:318`) + new `resolveDecisionIdIn(baseDir, map, id)` (candidate/definer/tie-break body `:375-407`) — a naive inline range-check would drop the definer tie-break and regress the D-v016-2 case (`:386-392`).

## FR4 — B6 resume-trust banner "+1" false-positive

- **Root cause (grounded):** `markFresh` (`state.js:1322`) sets `last_updated_commit = HEAD-at-write-time` and writes STATE.md but does **not** commit (caller commits) → the STATE-write bookkeeping commit's parent == `last_updated_commit`, so `stored..HEAD` and `stored..origin/main` are always exactly 1 after a clean checkpoint+push.
- **Origin side (drop-in):** `isStaleVsOrigin:845` — change the rev-range from `${stored}..${tracking}` to **`HEAD..${tracking}`** at `:914` (+ move `:932` commits, `:957` touchedPlanning). The existing `aheadCount<=0→notStale` guard `:924` then gives AC4.1/AC4.2 with a correct count. **Reject the additive alternative** (off-by-one on genuine push). Existing `isStaleVsOrigin` tests need re-baselining.
- **Local side (needs its own task):** `isStateStale:758` — the literal `HEAD..origin>0` gate **cannot apply** (it's 0 in exactly the unpushed-local-work case where local-stale *must* fire). Chosen mechanism **(a): suppress when every commit in `lastCommit..HEAD` touches ONLY STATE-affecting paths** (a pure bookkeeping "+1"). Note: the walk is already `-- STATE_AFFECTING_PATHS`, so (a) needs an **extra unfiltered per-commit check** ("touches nothing outside those paths"). 3-case test: pure +1 → silent; real work-commit → fires; mixed → fires.

## FR5 — concurrency-lock (the isolated L slice)

- All 6 named RMW fns are **true command entries** → safe to self-lock. **Only `regeneratePlanningIndex` has an in-lock caller** (`migrate-memory.js:2375`, inside `applyMigrate`'s coarse `.state.lock` at `:2001`) → needs an **exported lock-free core**; the other 5 just wrap their bodies (exported names retained → **zero `commands/*.md` changes**).
- **§9 deadlock CONFIRMED** (`migrate-memory.js:82-87`): `file-lock.js` is **not reentrant** and **throws-on-contention** (`:36-42`) — a nested `withStateLock` under the coarse lock throws (aborts migrate) or, past the 5s TTL, stale-unlinks it (worse).
- **Reuse `withStateLock`/`.state.lock`** (`state.js:441-456`) — the *shared* lock is what gives mutual exclusion with migrate; a separate `.docs.lock` would leave migrate-vs-drain lost-updates unprotected.
- **Template = `relocateInlinedBody` (`:679`, re-reads UNDER the lock), NOT `applyDeproseVector1` (`:589`, reads OUTSIDE)** — the whole read→transform→atomicWrite body becomes the lock-free core; do **not** split at the read/write boundary (esp. `captureCheckpointContext`'s 3 read→write pairs `:305-324` must sit in one lock).
- **`applyDispositionToFile` dual-role gap (decided — close it):** it's both `promoteDrainEntry`'s inner helper AND a standalone command write (`commands/plan.md:79,81`). Split into lock-free `…Core` (used by `promoteDrainEntry`'s core) + self-locking wrapper (command path) so FR5 covers *all* RMW writes, not most.
- **Sub-ordering (one slice):** (a) `planning-index.js` core-export + `migrate-memory.js` import-swap (`:2360/:2375`, `:2936/:2937` → `regeneratePlanningIndexCore`) → full migrate suite green; then (b) wrap the other 5 + `applyDispositionToFile` split + AC5.2 tests.
- **AC5.2 test caveat:** the primitive is throw-on-contention, so the deterministic assertion is *"the second concurrent writer fails fast with a lock error"* (mirror `migrate-lock.test.js:46-54`). A throw-under-held-lock test passes even for a *wrong* write-only lock scope → the guard is the template instruction (§ above) + review.

## FR6 — test-infra & tooling (B17, B5/B20, B23)

- **B17:** `vitest.config.js` **already exists** (bug's "no config" claim wrong) → add one line `testTimeout: 15000` (global — 19 git-touching test files, not 4). No behavior change (only raises the ceiling; no test asserts the 5s value).
- **B5/B20:** `npm run lint` (`eslint tools/`) broken (ESLint 9.39.4, no flat config). `@eslint/js` **already installed** (transitive devDep). **Blast radius = 4 problems** (empirically run): 3 unused-vars (`backfill-retros.js:25`, `checkpoint.js:10`, `measure-phase-costs.js:37`) + 1 no-undef (`doctor.js:557` `AbortSignal`) — **0 auto-fixable, all trivial manual** → after 4 touches `eslint tools/` exits 0, **no ticket backlog**. Config: `@eslint/js` recommended + `sourceType:module` + inline node globals (incl. `AbortSignal`); the `languageOptions.globals` block is load-bearing (without it `no-undef` explodes into hundreds). Add `@eslint/js` to `devDependencies` explicitly (devDep → NFR1-clean). **B20 → dismissed (dup of B5**; its "no B5 entry exists" premise is false — B5 at `BUGS.md:24`).
- **B23:** (a) organic re-root-to-*missing*-target rollback (`rerootEvictPlan:2311`) — inject a real (non-synthetic) dangle; (b) >1MB `FILE_SCAN_CEILING` fixture (shared w/ B15; `doc-hygiene.js:102`, `migrate-memory.js:1482`); (c) rollback → `git diff --cached` empty (staging at `:2437` is post-try → naturally empty on abort; shares B16's fixture); (d) `promoteDrainEntry` (`drain.js:691`) batch re-run doesn't re-select `→ Promoted` terminal entries (selection in `commands/plan.md`). Nits: `backlogKey`→`blockKey` (`backlog.js:136`, used `:167`+`:208`); harden `promoteToBugs` dedupe-marker replace (`backlog.js:227-229`, fragile string-replace).

## FR7 — B8 close-on-verify

- Guard `checkStateFrontmatterShape` (`retrospective.js:468`), `WHOLE_FILE_NOTE:454`. Discoverability test exists (`hook-state-write.test.js:268`). Auto-remediation regression **exists but one-sided** (`migrate-vector1.test.js:90-94,149-156,243-245` — proves `block:false` *after*, no `block:true` *before*, and at the wrapper not the command). → add a **command-level `block:true→false` flip** in `migrate-all-vectors.test.js` (the full `applyMigrate` path, `:191-217` today only checks word-conservation), then flip B8 → `fixed`.

## Test conventions (whole plan)
- **Runner:** vitest `^3.1.0`; `vitest.config.js` `globals:true`, `include:['tools/**/*.test.js','tests/**/*.test.js']`; parallel `forks` pool (the B17 flake source).
- **Commands:** `npm test` (`vitest run`) · `npm run validate` (`node tools/validate-plugin.js`, NFR6) · `npm run build` (esbuild → node22 esm) · `npm run lint` (broken until B5/B20) · `npm run test:coverage`.
- **Location:** all 94 `*.test.js` in `tests/` (0 in `tools/`); fixtures `tests/fixtures/`, helpers `tests/helpers/`. New tests → `tests/{feature}.test.js`.
- **Count:** **1492** (v0.1.8). **NFR5: suite must grow, never shrink.**
- **Naming:** `describe('{Epic}.{Slice}.{task} {fn} (AC ref)', …)`; `it('AC… behavior', …)`.
- **RED-first / proof-of-fail** is the established convention (every recent retro; test comments cite it). Every fix lands with a failing-first test.

## Environment check (Step 6)
- Runtime: Node 22+ (build target `node22`). No native modules involved. **No new runtime deps** — `@eslint/js` is a transitive devDep already in `node_modules` (explicit `devDependencies` entry only). `vitest`/`esbuild`/`eslint` all installed. No registry fetch / native build needed; nothing to dry-run beyond confirming `node_modules` is present.

## Last Updated
2026-07-20 (PLAN research synthesis)
