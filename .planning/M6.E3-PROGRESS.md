# M6.E3 — Progress (EXECUTE)

**Branch:** `feat/m6.e3-jev-claims-audit`. **Plan:** [`M6.E3-PLAN.md`](M6.E3-PLAN.md).

## Wave 1
- [x] `t1.1` the receipt contract — `45f5075`
- [x] `t2.1` SHIP refuses on a receipt (fixtures) — `fda4d87`
- [x] `t1.2` `B125`: the audit scans shipped code and names every call — `fad7df0`
- [x] `t1.3` the Jev client — `230621d` · live smoke passed 2026-09-26 (para 23 → contradicts, 0.96, 309 ms); key read from `.env` (`D-M6E3-14`)
- [x] `t1.4` paragraphs with file lines, and the fact list — `f34ad8e`

## Wave 2
- [x] `t1.5` the Jev `STATE.md` check — `0f90222`

## Wave 3
- [x] `t1.6` `/sig:resume` line — `6f7c057`
- [x] `t2.2` Jev findings in the SHIP report — `17a41bf`
- [x] `t1.8` docs — `04c50c9`
- [~] `t1.7` measurement — run twice: **4 of 4** found, false alarms 1/26 then 0/26 (RESEARCH addendum). **Waiting on Brett to confirm six labels.**

## Wave 4 — held at the plan's checkpoint (show Brett before S3)
- [ ] `t3.1`, `t3.2` — `BUGS.md` still-open rows
- [ ] `t4.1`, `t4.2` — `OPEN-QUESTIONS.md` answered questions

## Deviations from the plan, recorded
- **`t2.1` scope:** `AC5.5` (fires in the fix lane) cannot be met from `ship.md` alone — the fix lane never runs `/sig:ship`. Added `tests/content-gate-live.test.js`, which runs the same gate on this repository in CI on every PR; its `ACCEPT_STALE` list is the CI-path override. `ship.md` §6.7 runs on every SHIP, not only Epic close.
- **`t1.8` README line landed early, in `t1.3`:** the README claims every call the audit finds is listed there; that claim became false the moment `jev.js` existed, so the line went in with the client.
- **`t1.5`:** `runDriftChecks` gained optional `{findings, coverage}` return support (additive) so "checked N of M" reaches the reader.
- **Dry run against the real endpoint with a deliberately invalid key** (before spending the real
  one): TypeSafe answered `401` → `unauthorized` on every paragraph, so the endpoint and auth shape are
  recognised. It also caught a **real bug**: Signal's plugin manifest is at
  `plugin/.claude-plugin/plugin.json`, which neither the fact list nor the script looked at (the fact
  list fell back to `package.json`, which happens to match today). Fixed, with a test.
- **Tests clear `TYPESAFE_API_KEY` before loading** (`tests/helpers/no-network-keys.js` via
  `vitest.config.js` `setupFiles`), so `AC8.7` holds on a machine with the key set — verified by
  running the suite with a fake key in the environment.
- **`AC8.2` reworded to match the PLAN decision** (`/sig:resume` is silent with no key; SHIP and
  docs-sweep say "did not run").
- **TDD note:** `t1.3` tests and code were written in one step, so its RED run was not observed separately (the module-missing failure is what it would have shown). Every other task's RED run was observed.
