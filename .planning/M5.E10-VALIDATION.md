# M5.E10 — VALIDATION

**Tier:** FULL → `plan_validation_dims: all` (8 dimensions) · `nyquist_enforcement: strict`

> **This file is a fixture for its own Epic.** FR2 checks that a VALIDATION artifact's dimension-2
> assignments agree with its Nyquist-map rows — so a disagreement *here* would be the Epic
> specifying a check it violates in the act of specifying it. The two halves below were written to
> agree and then re-read against each other; §2's slice column and §9's slice column are the same
> data, deliberately, so the check has something to compare.

## 1. Goal alignment

Phase goal: *make every completeness claim either derived, checked, or labeled unverified — and make
the half deliberately left unbuilt impossible to mistake for built.*

Every slice serves it. S1 makes the enumeration trustworthy; S2 derives coverage; S3 forces the
unverified label; S4 checks two specific claims (retro index, STATE narrative); S5 stops a corrected
claim reading as live and stops uninspected restatement; S6 keeps the unbuilt half legible and states
where the checks are blind. **No task serves anything else.** ✅

## 2. Completeness — every requirement has an owning slice

| Requirement | Owning task(s) | Notes |
|---|---|---|
| AC0.1 | S6.t2 | The deferred half stays visible |
| FR1 / AC1.1 | S2.t1 | |
| FR1 / AC1.2 | S2.t1 | Missing IDs named, not counted |
| FR1 / AC1.3 | S2.t1 | Three outcomes |
| FR1 / AC1.4 | S2.t3 | `verify.md` names REQUIREMENTS.md |
| FR1 / AC1.5 | S2.t1 + S1.t3 | Field fixture supplies the case |
| FR2 / AC2.1–2.3 | S2.t2 | |
| FR3 / AC3.1–3.2 | S3.t2 | Gate, incl. present-but-empty |
| FR3 / AC3.3–3.4 | S3.t1 | Template text + pairing statement |
| FR4 / AC4.1–4.4 | S5.t1 | Line-level; blocks at FULL |
| FR5 / AC5.1–5.2 | S5.t2 | `B38` reclassification |
| FR5 / AC5.3 | S5.t3 | Provenance rule |
| FR6 / AC6.1 | S2.t3 | Discipline into command text |
| FR6 / AC6.2 | S6.t1 | Agents reachable or documented |
| FR7 / AC7.1–7.2 | S4.t1 | |
| FR8 / AC8.1–8.3 | S4.t2 | |
| NFR1 (RED against `main`) | **all slices** | Cross-cutting; enforced per task |
| NFR2 (offline, deterministic) | S2, S4, S5.t1 | Every shipped check |
| NFR3 (fail-open at read surfaces) | S4.t2, S2.t1 | Feeds `/sig:resume`, `/sig:status` |
| NFR4 (three outcomes) | S2.t1, S2.t2, S4.t1, S4.t2 | |
| NFR5 (measured on real corpora) | **S6.t3** | **Added during this pass — it had no owner** |

**The gap this dimension caught.** NFR5 was declared in REQUIREMENTS and mapped to **nothing**. It
would have shipped as an intention — the Epic's own defect class, surfaced by the Epic's own
completeness check, which is the strongest available evidence that dimension 2 earns its keep. ✅

## 3. Dependency correctness

`S1 → {S2, S3}` · `S1 → S4` · `S2.t3 → S6.t1` · S5 depends on nothing.

Minimal and real: S2/S3/S4 consume S1's extractor; S6.t1 describes command text S2.t3 lands. S5 is
deliberately unblocked and could move earlier — sequenced into wave 3 for review size, and **that is
a preference, not a dependency**, stated so nobody later mistakes it for one. ✅

## 4. Testability

Every AC is executable except two, and both are named rather than hidden:

- **AC5.2** (`B38` classification table) — reviewed, not unit-tested. A table asserting each entry's
  *class* is a judgement; a test could only check the table exists, which AC3.2's lesson says is
  worth little.
- **AC6.2's "documented"** branch — a test can assert an agent is either dispatched or named in a
  doc; it cannot assert the documentation is *good*.

Everything else has a fixture and an assertion. ✅

## 5. Scope discipline

Three named exclusions carried from REQUIREMENTS into the PLAN (adversarial agent, obligation
tracker, agent dispatch). No task exceeds its requirement. **S1.t1 is the one refactor**, and it is
justified by RK2 rather than taste: two implementations of "what is a requirement ID" is `B82`'s
shape, which shipped a half-archived unit. It carries AC S1.3 (existing suite unchanged) precisely so
it stays a move, not an improvement. ✅

## 6. Context feasibility

Largest task is S2.t1 (coverage diff + three outcomes + field fixture). It reads one library module,
two artifacts, one fixture — comfortably one agent context. S5.t2 is the widest *read* (every
anti-rationalization table) but is mechanical per entry. ✅

## 7. Risk coverage

| Risk | Mitigated by |
|---|---|
| RK1 NFR-blind extractor | S1.t2 + AC S1.2, **RED first** |
| RK2 two ID implementations | S1.t1 + AC S1.1 |
| RK3 empty-but-present section | S3.t2 + AC S3.2 |
| RK4 FR8 duplicates existing readings | S4.t2 + AC S4.2 |
| RK5 AC6.2 quietly dropped | Own task S6.t1, own AC |
| RK6 deferred half reads as done | S6.t2 + AC S6.1, checked by test |

All six carry a task and an acceptance criterion. ✅

## 8. Vertical slicing

Each slice ends in something that runs. S1 ends with a proven extractor; S2 with a working coverage
check wired into VERIFY; S3 with an enforced template; S4 with two live checks; S5 with a SHIP gate
plus published rules; S6 with the blind spots and the missing half stated. **No slice is a layer** —
none delivers "the parser" or "the tests" alone. ✅

## 9. Nyquist test-coverage map

| AC | Slice | Test type | Fixture |
|---|---|---|---|
| S1.1 | S1.t1 | unit | — |
| S1.2 | S1.t2 | unit, **RED first** | `NFR1` literal |
| S1.3 | S1.t1 | regression (existing suite) | existing evict fixtures |
| S1.4 | S1.t3 | unit (README assertions) | frozen field pair |
| AC1.1–1.3, 1.5 | S2.t1 | unit + acceptance | frozen field pair |
| AC1.4, 6.1 | S2.t3 | docs-integrity | `commands/verify.md` |
| AC2.1–2.3 | S2.t2 | unit | sandbox self-contradicting VALIDATION |
| S2.3 (cannot-evaluate) | S2.t1 | unit | project with no REQUIREMENTS |
| S2.4 (malformed input) | S2.t1 | unit | malformed markdown |
| AC3.1–3.2 | S3.t2 | unit | artifacts per failure mode |
| AC3.3–3.4 | S3.t1 | docs-integrity | template text |
| AC4.1–4.4 | S5.t1 | unit | correction-below vs. inline-retraction |
| AC5.1–5.2 | S5.t2 | **review only** (see §4) | — |
| AC5.3 | S5.t3 | docs-integrity | anti-rationalization table |
| AC6.2 | S6.t1 | docs-integrity | `agents/verifiers/` |
| AC7.1–7.2 | S4.t1 | unit | stale vs. fresh retro index |
| AC8.1–8.2 | S4.t2 | unit | **five states from git history** |
| AC8.3 | S4.t2 | differential | same five, run through the existing two checks |
| AC S6.1 | S6.t2 | docs-integrity | shipped docs |
| AC S6.3 | S6.t3 | measurement | ~12 local corpora, read-only |
| NFR1 | all | each fix's test shown RED against `main` | — |

**Strict-Nyquist note.** `nyquist_enforcement: strict` requires proof-of-fail-before-pass, enforced at
VERIFY. Two rows cannot satisfy it and say so here rather than at VERIFY: **AC5.1–5.2** is review-only,
and **AC S6.3** is a measurement whose "failure" is an unmeasured corpus, not a red test. Both are
**declared deviations** under the 2026-08-06 rule — never counted as strict-Nyquist compliance.

## Verdict

**PASS — 8 of 8 dimensions.** One completeness gap found and closed by adding S6.t3 rather than by
narrowing the requirement.
