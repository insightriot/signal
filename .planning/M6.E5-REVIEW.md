---
epic: M6.E5
tier: FEATURE
phase: REVIEW
created: 2026-08-28
---

# M6.E5 — Review Report

**Scope: Step 1 only.** `review_depth: quality-only` at FEATURE is the master switch — Steps 2
(security), 3 (performance) and 4 (simplification) are skipped regardless of what `security_audit`
and the others say. ⚠ **So this is a code-quality pass, not a security audit**, and the deliverable
is a permission proposal. Stated because "reviewed" should not be read as "security-reviewed".

**Loop status:** `loopStatusFor(state, 'REVIEW')` → count 0, ceiling 3, this is attempt 1.

---

## Critical Issues

### C1 — A proposed `deny` rule was silently dropped when the user had already **allowed** it

**`plugin/tools/lib/permissions-report.js` (`buildProposal`) → `permissions-state.js`
(`proposalDelta`).**

Deny proposals were routed through `proposalDelta`, which suppresses a rule found in `state.granted`
— **the allow set**. So a user who had already allowed `curl` silently lost the proposal to deny it,
and the loss was counted as *"already granted"*.

**Reproduced before fixing:**

```
settings.json: { permissions: { allow: ["Bash(curl:*)"] } }
deny proposed: ["Bash(sudo:*)", "Bash(rm -rf:*)", "Bash(git push --force:*)", "Read(./.env)"]
Bash(curl:*) proposed as deny?  false
```

**Why Critical rather than Important.** The protective half shrank **precisely in the case it exists
for**, and said nothing while doing it. `D-M6E5-5`'s whole argument is that *"yes, and don't ask
again"* only ever teaches the system to say yes, so the deny half cannot fill itself — and this made
the one mechanism that fills it get *quieter* the more the allow list had grown.

**Fixed in-phase.** A new `denyDelta` compares proposed denies against the existing **deny** set. A
collision with an existing **allow** is not a suppression — it is a **conflict**, and it now renders
as its own line:

```
⚠ CONFLICT: Bash(curl:*) is proposed as a deny AND is already in your allow list.
    Deny wins outright, so installing this silently overrides that allow. Decide deliberately.
```

That is the most useful thing the report can say to that user: deny-first precedence means
installing it would override an allow they set on purpose.

**Coverage written for the defect (6 new tests, all green):** existing deny rules collect into their
own set; an allow does **not** suppress the matching deny; an existing deny **does**; the collision
is reported as a conflict; the conflict renders as a visible line; the deny proposal survives.

## Important Issues

None.

## Suggestions

- **S1 — `bareRuleWouldOverreach` scans the whole classification per candidate.** O(table) per
  scanned key. Irrelevant at this size (~90 entries × ~130 keys) and not worth an index yet. Noted
  so it is a decision rather than an oversight.
- **S2 — `lineOf` is O(n) per match**, so line attribution is O(n·m) across a file. Same reasoning:
  measured runtime is a few milliseconds; leave it.

## Checked and found clean

- **The tracked artifact leaks nothing.** Verified by rendering it against this repo:
  `renderArtifact` output contains **no home-directory path** and **none of the user's existing
  rules** (checked for `graphify`, a real entry in the live settings). This matters because
  `.planning/PERMISSIONS.md` is committed and this repository is public. The scope *paths* appear in
  the terminal report only, never in the artifact.
- **No write path to a settings file**, asserted on disk after a full render, and again by source
  inspection.
- **Naming** — `stackInfo` (what was detected) vs `stack` (what is proposed) was corrected during
  EXECUTE after the first draft used one key for both.

## Security findings

⚠ **Not run.** `review_depth: quality-only` skips Step 2. C1 above is security-adjacent and was
found by code review, not by a security pass. **A FEATURE-tier review of a permissions feature is a
defensible tier choice and a real limit at the same time**, and `M6.E5-PROFILE.md` names
`/sig:escalate` upward as the response if the deny list grows.

## Performance findings

⚠ **Not run** (Step 3 skipped). S1 and S2 above are the incidental observations.

## Simplification opportunities

⚠ **Not run** (Step 4 skipped).

---

## Verdict

**FAIL — by one line, and the honest reading is FAIL.**

Every PASS-WITH-FIXES condition is met **except the size cap**:

| Condition | Result |
|---|---|
| Critical discovered **and closed inside REVIEW** | ✅ |
| Tests still green | ✅ 3094 passing, 180 files |
| **New coverage written for the defect** | ✅ 6 new tests, none of which existed before |
| No design impact | ✅ one new exported function; no caller outside the report module |
| **≤ 50 LOC of non-test source** | ❌ **51** — 47 insertions + 4 deletions |

**Why this is recorded as FAIL rather than argued down.** The conditions are conjunctive and this
file says every one is load-bearing; it also says *"the diff was small"* is exactly how a Critical
gets under-fixed. Trimming the explanatory docblock to reach 50 would be optimising for the metric —
the anti-pattern the rule exists to prevent — so it was not done.

**Composition of the 51, stated so the decision can be made on facts:** 28 code insertions, 19
comment/docblock insertions, 4 deletions. **Under a code-only reading it is 32 and passes
comfortably.**

⚠ **An observation about the rule itself, not an argument for an exception.** The cap explicitly
excludes test lines so that *"a rule that penalises the reviewer who tests is a rule that trains
reviewers not to test."* It does **not** exclude comment lines — so it penalises the reviewer who
*explains*, by the same logic. Nineteen of my forty-seven insertions are the docblock recording what
the defect was and why the fix takes this shape. **This is filed as an observation for the backlog,
not used to reclassify this verdict.**

**Ceiling:** this was REVIEW attempt 1 of 3.

---

# REVIEW — attempt 2 (2026-08-28), after the loop back

**Loop status:** `loopStatusFor(state, 'REVIEW')` → count **1**, ceiling 3, attempt **2**. Derived
from `completed_phases`, not remembered (`B76`).

## What changed between attempts

**Nothing in the code.** `C1`'s fix was written and committed during attempt 1; the loop back
re-homed it as EXECUTE work, which is where a 51-LOC change belongs. Nothing was reverted and
nothing was re-implemented — the loop moved the *record*, not the diff.

**Measured against the EXECUTE baseline:** `git diff HEAD -- plugin/tools/lib/` → **0 insertions, 0
deletions**. Working tree clean.

## Re-review of the tree as it now stands

| Axis | Finding |
|---|---|
| Correctness | `C1` closed and covered. No new Critical, no Important. |
| Readability | `denyDelta` sits beside `proposalDelta` and its docblock states the defect it replaces. |
| Architecture | One new export, one caller. No ripple. |
| Security | ⚠ Step 2 **not run** — `review_depth: quality-only`. Unchanged from attempt 1. |
| Performance | `S1`/`S2` from attempt 1 stand as suggestions. Not run as a pass. |

## Critical Issues

None.

## Important Issues

None.

## Verdict — attempt 2

**PASS.**

0 Critical, 0 Important. Suite **3094 passing**, 180 files. The size cap is not in play: the diff
under review is zero, because the change it measured is now part of the EXECUTE baseline.

⚠ **What this second pass did NOT do, stated so the PASS is not read as broader than it is.** It did
not re-examine the code from scratch; it confirmed that the tree is the one attempt 1 reviewed, plus
nothing. The substantive review is attempt 1's, and its scope limit — quality-only, no security
pass, on a permissions feature — applies unchanged.

## Filed for the backlog rather than decided here

**The PASS-WITH-FIXES cap counts comment lines.** It explicitly excludes *test* lines so that *"a
rule that penalises the reviewer who tests is a rule that trains reviewers not to test."* By that
same logic it penalises the reviewer who **explains**: 19 of the 47 insertions that pushed this over
were the docblock recording what the defect was and why the fix takes its shape. Filed as an
observation against `review.md`; **not** used to reclassify either verdict.
