# M4.5.E2 — `/sig:add` — Execution Progress

**Phase:** EXECUTE. **Tier:** FULL — strict gate at each wave boundary.
**Started:** 2026-05-14.

---

## Wave 1 — Slice 1: Hardened hot path  [**shipped 2026-05-14**]

**Goal:** Ship `/sig:add "idea"` → atomic write to `.planning/FUTURE-IDEAS.md` with verbatim capture, sensitive-data scrub, lock, footer rewrite.

**Files written / modified:**

- ✓ `commands/add.md` (new) — meta-command shape (no tier-gating preamble; modeled on `commands/resume.md`)
- ✓ `tools/lib/add.js` (new, ~250 lines) — `parseInput`, `scrubSensitive`, `checkBodyLength`, `buildFutureIdeasEntry`, `rewriteFooter`, `insertAboveFooter`, `atomicWrite`, `acquireLock`, `releaseLock`, `captureToFutureIdeas`, `BODY_LENGTH_SOFT_CAP`
- ✓ `tests/add.test.js` (new, 40 tests) — pure-function unit tests + integration tests for atomic-write / lock / capture
- ✓ `tests/fixtures/add/future-ideas-minimal/.planning/FUTURE-IDEAS.md` (new, ~25 lines) — synthetic FUTURE-IDEAS for fixture tests
- ✓ `tools/validate-plugin.js` — `REQUIRED_COMMANDS` += `'commands/add.md'`
- ✓ `README.md` — added `/sig:add` callout (line ~101) + per-command bullet (line ~147)
- ✓ `CLAUDE.md` — "9 slash commands" → "13 slash commands" with full list

**Acceptance criteria (all met):**

| # | Criterion | Status |
|---|---|---|
| 1 | `/sig:add "test"` writes well-formed entry above footer; exit 0 | ✓ (test: `captureToFutureIdeas integration > writes an entry above the footer and rewrites footer date`) |
| 2 | No `.planning/` → exit non-zero with `/sig:init` suggestion | ✓ (test: `throws when .planning/ is missing`) |
| 3 | Concurrent runs: second invocation fails with lock error | ✓ (test: `refuses concurrent capture (lock held)`) |
| 4 | Sensitive-data hit prompts user; abort → no write; keep → write | ✓ (tests: `aborts cleanly when sensitive scrub prompt returns "abort"`, `writes verbatim when sensitive scrub prompt returns "keep"`) |
| 5 | Atomic write: rename fails → destination unchanged | ✓ (test: `leaves destination unchanged if rename throws`) |
| 6 | Footer rewrite: date matches today after every successful write | ✓ (test: `writes an entry above the footer and rewrites footer date` asserts footer rewritten + old date absent) |
| 7 | `npm run validate` passes | ✓ ("Plugin validation passed.") |
| 8 | `npm install --dry-run` clean (Node 22+) | ✓ (env check at PLAN time; no new deps added in S1) |
| 9 | Body > 4000 chars warns via prompt | ✓ (tests: `warns and consults bodyLengthPrompt`, `aborts cleanly when bodyLengthPrompt returns "abort"`) |

**Test summary:** **209 / 209 passing** (169 prior + 40 new `add.test.js`). `npm run validate` green.

**Defects found + fixed in-slice:**
- Stale-lock test failure on first run — `acquireLock` did not `unlink` the stale lock before the atomic-create, so `openSync(lockPath, 'wx')` failed with EEXIST. Fixed by adding `unlink` in the stale-detection branch. Re-test passed.

**Anti-rationalization honored:**
- ✓ No `appendFile` shortcut — `atomicWrite` uses write-to-temp + `fs.rename` with `EXDEV` fallback.
- ✓ Lock file kept despite solo-dev assumption.
- ✓ Sensitive-data scrub: prompts user; never auto-redacts.
- ✓ No preview before write.
- ✓ Verbatim capture; no LLM rewrite, no smart-quoting, no normalization.
- ✓ No numeric entry IDs.

**Out-of-scope items deferred to later slices (per plan):**
- `--question`, `--milestone`, `--milestone N`, `--file` flags → S2
- New `tools/lib/milestones.js` helper → S2
- Naked invocation interview + heuristic single-key overrides → S3
- First-run onboarding warning + brownfield-detection enrichment → S4
- `gate_strictness` honoring → S4
- Vocab-lint validator extension → S4
- `commands/plan.md` "review FUTURE-IDEAS" step → S5

**Commit status:** User approved strict-gate 2026-05-14; committed as `M4.5.E2.S1: hardened hot path for /sig:add`.

---

## Wave 2 — Slice 2: Force-route flags + destination generalization  [**shipped 2026-05-31**]

7 atomic commits (`2dd9cf0` → `4db70ca`). FR1–FR4 + FR8 met; R2 (path-escape) + R4 (multi-flag) hard gates proven.
- S2.t1 `2dd9cf0` — `captureToDestination` generalized spine (R7)
- S2.t2 `dea4613` — `tools/lib/milestones.js` (`currentMilestone`, `listMilestones`, decimal-aware)
- S2.t3 `b3e1f60` — `parseInput` flags + `resolveDestination` multi-flag guard (FR4/R4)
- S2.t4 `2816fc3` — `--question` → OPEN-QUESTIONS.md (FR1/R3)
- S2.t5 `e2d6050` — `--milestone [N]` → milestone holding section (FR2/R5)
- S2.t6 `845f3fc` — `--file` escape valve + path-escape **hard gate** (FR3/R2)
- S2.t7 `4db70ca` — docs: add.md + README + CHANGELOG (FR8); `--file` kept undocumented

## Wave 3 — Slice 3: Naked-invocation interview  [**shipped 2026-05-31**]

2 commits. Heuristics CUT per Decision 5; quoted capture stays instant per Decision 4.
- S3.t1 `785d896` — naked `/sig:add` → one "What's the idea?" → FUTURE-IDEAS; blank answer → exit 0, no write, no lock (FR5.1–5.3/R6)
- S3.t2 `1996012` — no-heuristics export-surface guard test + README note + stale-comment cleanup (FR5.4)

## Wave 4 — Slice 4: Stranger-safety hardening  [**shipped 2026-05-31**]

3 commits. Q1 LOCKED + user-reconfirmed 2026-05-31: NO destination confirm at any `gate_strictness`.
- S4.t1 `3b2164d` — first-run onboarding note (gate_strictness-modulated: strict→blocking once / light+absent→FYI / off→silent) + `.add-onboarded` flag + brownfield/greenfield error (FR6.1–6.3)
- S4.t2 `4511ce7` — vocabulary lint in validate-plugin.js (banned `tranche` over add.md/add.js) (FR6.4)
- S4.t3 `6e7688b` — README onboarding note + CHANGELOG (FR8)

## Wave 5 — Slice 5: Close the loop (`/sig:plan` drains FUTURE-IDEAS, advisory)  [**shipped 2026-05-31**]

The final slice; closes the Epic's user-visible surface. 4 tasks, linear, executed sequentially (shared `drain.js`/`plan.md` — not parallelized). Carries the **R1 hard gate** (preview-diff before any drain write) + the R5 delete/merge sub-gate. Drain is ADVISORY (skippable) per Decision 2. Surfaces ALL un-dispositioned FUTURE-IDEAS entries, no date window, per **Q2**.
- **S5.t1** `d55afaa` — `tools/lib/drain.js`: `parseEntries` (fence-aware top-level `## ` segmentation, tolerates orphaned mid-file footer) + `listDrainCandidates` (Q2: all un-dispositioned; dispositioned = SHIPPED/PROMOTED/DEFERRED/MERGED/DELETED heading marker OR Promoted|Deferred|Merged|Deleted Status verb). 13 table-driven tests over a real-shape fixture.
- **S5.t2** `e7598c4` — `applyDisposition(content, entryIndex, verb, reason, date)` byte-range edits (promote/defer stamp the Status line or insert one; delete/merge remove block+`---`), `applyDispositions` batch (descending-safe, powers "defer all"), `applyDispositionToFile` (confirm gate for delete/merge regardless of gate_strictness + full-file atomicWrite). 12 snapshot/invariant tests.
- **S5.t3** `87cf4ad` — `commands/plan.md` `### 1b.` drain step between Step 1 and Step 2 (no renumber); advisory + skippable + "defer all remaining" batch + per-entry strict-enum [promote, defer, merge, delete] + explicit skip + **preview-diff R1 HARD GATE** + empty-note (FR7.1–7.4). 7 content-contract tests.
- **S5.t4** `eaffff7` — anti-rationalization row in plan.md ("skip the drain" → captures rot) + README `/sig:plan` drain mention + CHANGELOG `[0.1.3]` Slice 5 block (FR7.5/FR8). 3 doc-contract tests.

**Test summary:** **759 / 759 passing** (724 prior + 35 new `drain.test.js`). `npm run validate` green throughout. No new runtime deps (drain.js is pure Node + reuses the atomic-write substrate).

**Resume protocol:** Epic user-visible surface complete. Transition EXECUTE → VERIFY to close M4.5.E2.

---

*Progress breadcrumb updated 2026-05-31: Slice 5 shipped (4 commits `d55afaa`→`eaffff7`), 724 → 759 tests; Epic EXECUTE complete, VERIFY next. HEAD `eaffff7`.*
