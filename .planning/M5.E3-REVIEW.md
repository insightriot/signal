# M5.E3 — REVIEW Report

**Tier:** FULL/strict · `review_depth: full` (all 4 axes) · `security_audit: full` · `gate_strictness: strict`. **Date:** 2026-07-19.
**Verdict: ✅ PASS-WITH-FIXES — 0 Critical; 4 Important fixed in-phase (RED-first); suite 1481 → 1492 green. Ready for SHIP.**

Adversarial 3-specialist panel (code-quality, security, test-adequacy) over the M5.E3 diff (`cccaa7d..HEAD`), matching the prior Epics' pattern. The panel was rigorous — the test reviewer checked out a worktree at `51284c6` to *empirically* confirm the disclosed stash-verify RED was genuine (7/11 failed on behavior), and confirmed no arming-ripple assertion was weakened to hide a break.

## Critical Issues
**None.** The v3 compose chain, the archive-first/relocate-never-delete + surgical-rollback discipline, the fail-closed anchor gate, the TOCTOU hash-bind, and the `git add` staging filter were all reviewed and found sound.

## Important Issues — ALL FIXED IN-PHASE (RED-first, atomic commits)
| # | Issue | Fix | Commit |
|---|---|---|---|
| **B19** | **Dry-run/apply parity violation** — the dry-run (the surface the user approves before a release-gating migrate) OMITTED the tail INDEX-regen step apply performs, AND that regen silently clobbered a foreign/pre-v3-format curated INDEX (now the default path for every stamp-null external project post-nullfix). | (a) dry-run now enumerates the INDEX step; (b) **guard**: skip the regen + emit a warning when the existing INDEX is non-empty AND parses to zero annotations AND has no `**Tier legend:**` block (the 3-clause predicate — `legend===null` distinguishes a genuinely-foreign INDEX from a legit Signal INDEX with only `_(note pending)_` notes; ratified deviation from the literal 2-clause spec). | `497267a` |
| **B18** | `full.slice(baseDir.length+1)` corrupts every INDEX/roster key when `baseDir='.'` (`join('.','.planning')` normalizes → `slice(2)` chops `.p`) — reachable via `/sig:index` + `/sig:ship` passing a relative baseDir; also breaks the INDEX self-skip → INDEX lists itself → idempotency lost. 3 sites. | `relative(baseDir, full)` at all 3 sites (planning-index.js ×2, roster.js). Behaviorally verified: `enumeratePlanningDocs('.')` → 0 mangled keys. | `8f1dda8` |
| **IMPORTANT-1** (sec) | The planning-root realpath check fires ONLY through a relocate/archive vector; a **minimal `needsV3` run** (de-prose STATE + create BACKLOG, no vector) reaches the first write with no realpath check → a symlinked `.planning/` root escaping the repo is missed — the exact stranger-adoption symlink case, made live for every external project by the stamp-null fix. | One unconditional `assertRealInsidePlanning` on the planning root at the top of `applyMigrate`'s mechanical phase, before the first write (covers fs-backup mode too). RED: a symlinked-out `.planning` → refuses, target untouched. | `1ed60d5` |
| **Test-Important** | `isV3Conformant`'s `senseVector3` (closed-Epic evict) fold (migrate-memory.js:1211) had **zero coverage**, AND the two `decideLayoutBanner` unit tests were literal duplicates → a regression passed at both levels. | Added the senseVector3-pending conformance test (mutation-RED: neutralizing :1211 fails both) + differentiated the duplicate banner tests. | `5875cf6` |

**Why fix-in-phase (PASS-WITH-FIXES):** each is small, localized, tested, no design ripple (~40 LOC total code); tests stay green. Fixing B19 in-phase **resolves its release-gating question by removing it** — the parity fix + the clobber guard moot the data-loss severity, which is the "complete solution" outcome rather than shipping a documented limit.

## Security Findings (OWASP + ASVS L2)
- **No Critical.** OWASP relevant checks PASS: A01 path confinement (see B14), A03 git command/option injection (all `execFileSync` + hardcoded args + `--` + non-user-derived tag names), A04/A08 integrity (archive-first, byte-verify, TOCTOU bind), A05 (fs-backup snapshot self-gitignored). A02/A06/A07/A09/A10 N/A (no crypto/deps/auth/network — SSRF disproven for doc-hygiene, no-network independently verified).
- **B14 verdict: PASS for the M5.E3 NEW write paths** — they route through the E2-hardened symlink-aware confinement (`assertRealInsidePlanning` / `assertInsidePlanning`) or use fixed constant paths with regex-constrained variable components. The 3 lexical gates (evict.js/add.js/resume.js) are pre-existing B14 debt, NOT M5.E3 regressions.
- IMPORTANT-1 fixed (above). Deferred: B22 (doc-hygiene reads outside repo root — read-only/dev-only), SUGGESTION-1 hardened in-phase (marker-anchored archive dedupe, `4b04f44`).

## Performance Findings
No N+1/unbounded-fetch-class issues (CLI file tool, no service/DB/frontend). One flagged: the D-ID map is rebuilt per evicted ID in the anchor gate (O(N×rebuild)) — fine at Signal's scale, **deferred as B21** (~5 LOC, build-map-once) as it only bites projects with thousands of decisions.

## Simplification
Clean. No dead code, unused imports, or redundant abstractions in the 5 new/changed files. The three walk helpers (`walkMarkdown`/`walkDocs`/`collect`) are near-duplicates but justifiably separate (different predicates/ignore-sets/return shapes). Two prose nits fixed in-phase (`183bef8`).

## Deferred (cataloged, non-blocking)
- **B21** — D-ID map per-ID rebuild (perf).
- **B22** — doc-hygiene link-check unbounded disk read (dev-only).
- **B23** — test-hardening bundle: organic re-root-to-missing-target path, >1 MB scan-ceiling branch, a `git diff --cached`-empty rollback assertion, `promoteDrainEntry` terminal-entry re-selection check, + 2 naming nits.
- **B20** — lint tooling (ESLint 9 flat-config), pre-existing.
- **Accepted minor imprecision:** the dry-run line says "INDEX.md → regenerate" even though apply may skip+flag a foreign INDEX; the apply-time warning surfaces the skip. Primary parity defect (silent omission) is fixed.

## Verdict
- [ ] PASS
- [x] **PASS-WITH-FIXES** — 4 Important fixed in-phase (B18, B19, IMPORTANT-1, Test-Important), RED-first, suite 1481→1492 green, validate green. 0 Critical. Deferred items cataloged (B20–B23).
- [ ] FAIL

**Ready for `/sig:ship`.** The B19 release-gating concern is resolved (fixed, not deferred). B14 clean for the new write paths. RED-first discipline empirically confirmed genuine; no weakened assertions.

## Last Updated
2026-07-19 — REVIEW complete. 3-specialist panel; PASS-WITH-FIXES (4 Important fixed: `8f1dda8`, `497267a`, `1ed60d5`, `5875cf6`, `4b04f44`, `183bef8`). B19 resolved in-phase. Deferred → B20–B23.
