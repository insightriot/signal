# M6.E2 — PROGRESS

Branch `epic/m6-e2-published-facts`. Tier FULL / strict, `tdd_required: true` — every task RED
before GREEN.

## Wave 1

- [x] **S1.t1** — `published-facts.js`: the tally check, three-valued applicability — `044a767`
- [x] **S1.t3** — reach rendered from a single `REACH` entry via `describeReach` — landed with t1
- [x] **S1.t2** — `ALL_DRIFT_CHECKS` composed into `/sig:sweep` + `/sig:resume` — `03f720b`
- [x] **S2.t1–t5** — `/sig:add --bug` write path — `22d2e68`
- [x] *(unplanned)* `STATE.md` narrative corrected — found by this Epic's own wiring

## Wave 2

- [ ] S3 — the remaining four checks
- [ ] S4 — reach disclosure + report shaping
- [ ] S5 — fold in the evidence (`B102`, single-home `[Unreleased]`)

## What the wave-1 slice actually found

**The first `/sig:sweep` after wiring the new check returned `findings` on a check that already
existed.** `body-omits-current-epic` fired: `STATE.md`'s frontmatter said `current_epic: M6.E2` while
its body still read *"Milestone 6 is not opened."* Signal's own state narrative was an instance of
the class this Epic is named after, one hour into the Epic.

It was not found by the new check. It was found because **wiring the new check meant running the
composed registry against this repository for the first time** — which is the wave-1 rationale
stated in the PLAN, paying out on the same day.

Drift checks now: **9**, all clean on this tree.

## Wave 1 verified end to end, against the real drifted file

`.planning/BUGS.md` is itself the migration case — a capture had already been stranded below the
tally. Run against a **copy** of it (production files are not test beds):

```
BEFORE  tally ok: true   tally at line 184 of 202
repaired: true
AFTER   tally ok: true   tally at line 209 of 210
stranded entry preserved: true
author narrative preserved: true
```

The tally moved from mid-file to the last content line, the stranded entry survived, the author's
narrative was untouched, and the counts agree with the file in both directions.

## Deviations from the plan

- **S1.t3 landed inside S1.t1** rather than as a separate commit. The reach rendering is three
  functions and splitting it would have produced a commit that could not be tested alone.
- **Research ran single-context**, not as 4 parallel agents (`RESEARCH` method note). Consequence
  carried forward: no independent reading before REVIEW.
