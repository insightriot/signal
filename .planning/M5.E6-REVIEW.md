# M5.E6 — Review Report

**Tier:** FULL / strict · `review_depth: full` · `security_audit: full` (OWASP + ASVS L2) · **Verdict: ✅ PASS**
Reviewed `927318d..eea284f` (all 25 task commits) in `tools/` + `commands/`. Method: a **3-specialist adversarial panel** (code-quality, security, test-integrity) run in parallel, each empirically verifying via source-trace + mutation, synthesized and independently re-verified by the orchestrator.

## Panel & method
| Reviewer | Scope | Headline |
|---|---|---|
| code-quality | 5 axes + perf + simplification; ran the suite 17× (default / shuffle / no-isolate) | No correctness defect blocks SHIP |
| security | OWASP Top 10 + ASVS L2; attacked untrusted-repo reads, gate weakening, lock, pollution | No Critical; one B29-parity Suggestion |
| test-integrity | false-green hunt (8 mutations) where the orchestrator's VERIFY pass didn't reach | 0 false-greens; holds the M5.E5 bar |

The panel's mutations **complement** the VERIFY-phase pass: between them, **both** B27 conjuncts, **all three** B30 conjuncts, the B29 guard, the sweep check internals, AC7.7's standalone lock, and the NFR1 directory audit are each proven to bite under mutation. B31's permanent broken-twin is a stronger anti-tautology than a one-off.

## Critical Issues (must fix before SHIP)
**None.**

## Important Issues
1. **ship-fr1 test flake — RESOLVED as environmental (not a code defect).** code-quality saw two pure-logic B30/B26 tests (`AC2.3 SKETCH-short`, `AC5.3 SKETCH-mid-flow`) fail once on its first full-suite run under heavy concurrent load (3 review agents running suites at once), non-reproducible across 16 subsequent runs. **Independently reconfirmed on a quiet box:** ship-fr1 22✓ × 3, full suite 1623✓. The failing production path (`shipFR1Check`) is pure/deterministic (in-memory synth, per-test `mkdtemp`, no shared global); all 6 `_afterRead` sites are guarded; the only reproducible cross-file leak is **pre-existing** and only under non-default `--no-isolate` (`migrate-layout-banner.test.js`, outside this diff). **Most consistent with** the repo's already-documented **B17 parallel-fork-timeout-under-load** — but a *working hypothesis, not established*: the failure text was never captured, and B17 is a timeout while these were assertion-shaped. Does not implicate the M5.E6 diff (watch-item: gate SHIP on a clean quiet-box run; capture the text if it recurs). → left as an ops note, not a blocker.
2. **AC2.7 real-corpus regression test missing — DEFERRED with justification.** No committed test runs the dead-link check over Signal's *real, moving* `.planning/` corpus; the AC is covered by a synthetic `stripCode` fixture + the anchor unit test + the manual VERIFY dogfood (which ran clean — only INDEX-stale + 39-inbox true-positives). This is a *missing regression test*, not a hidden defect, and it's hard to automate on a corpus that changes every phase. Constituent paths are unit-pinned. **Deferred** — a brittle moving-corpus test would cost more than it protects.

## Suggestions
- **[cataloged → B34] `renameFn` B29-parity prototype-pollution gap.** `drain.js` bare-destructures `renameFn` (3 cores) → `atomicWrite` invokes under lock — the same class B29 hardened for `_afterRead`, left on the literal sibling. **Unreachable** (0 prod callers, no in-process function-pollution primitive; YAML sink proven safe) — same tier B29 was before it was fixed. Fix mirrors B29 (`hasOwn` guard at the 3 destructures). **Decision surfaced to maintainer:** complete-the-hardening now vs. schedule with a broader injectable-seam pass.
- **[cataloged → B35] `sweep.js checkIndexFreshness` unwrapped compose/diff** can crash the read-only sweep on a Signal-managed pathological `.planning/` (stranger repos early-return, so the stranger contract holds). Fix = try/catch→advisory, RED-first via mocked-throw.
- **[fixed in-phase] `add.js:985` comment** mis-attributed the 30s TTL to `.state.lock` (it's `.add.lock`'s; `.state.lock` is 5s). Corrected — comment-only, no behavior change.

## Security Findings (OWASP + ASVS L2)
**No Critical. SHIP not blocked.** Surfaces attacked and found **clean**, with evidence: YAML frontmatter parsing (`__proto__` becomes own-prop, not pollution; alias-bomb throws→caught), path traversal via link targets (B22 repo-root containment) and inbox resolution (hardcoded constants), ReDoS (all regexes linear), command injection (only `execFileSync` argv-form, no shell), the B27/B28 gate weakening (flag does strictly *less* than abort — nothing to weaponize; migrate-introduced dangles still abort), B31 lock DoS (`.state.lock` held only across the ms-scale non-interactive write; acyclic order), scrub-before-write ordering. Findings: **B34** (renameFn, Suggestion, above); symlinked-`.md`-outside-root walk (Suggestion, pre-existing, negligible read-only exfil — optional `realpath`-contain); whole-file read before the 1 MB cap-slice (Suggestion, self-limiting).

## Performance Findings
No anti-patterns (no N+1, no unbounded loops over findings). Minor: `runSweep` walks `.planning/` twice (`checkInternalLinks` + `checkIndexFreshness`) — doc trees are small; not worth restructuring. Noted.

## Simplification Opportunities (Nits — accepted, not fixed)
- `checkCommandFrontmatter`'s internal `.sort` is re-sorted by `runSweep` (harmless redundancy).
- `renderSweepReport` filters strictly on `structural|advisory` — a future check emitting an unmapped severity would silently vanish (latent; safe today since all natives map).
- `checkCommandFrontmatter` labels valid-YAML-but-non-mapping frontmatter "invalid YAML" (deliberate stable-message tradeoff for determinism).

## Verified genuinely clean (all three reviewers)
B27 exemption tight (AND; dry-run↔apply consistent via `scaffoldMoveMap`) · B28 absolute-skip correct and necessary (resolve vs posix.join divergence) · B30 synth sound (`rowStatus===null` gate; fails open-to-skip) · B31 lock acyclic + re-entrancy-tested + `addDocWriteCore` orphan-free · B29 complete across all 6 `_afterRead` seams · doc-hygiene parameterization genuinely additive (defaults byte-identical) · stranger-repo robustness holds (portable checks degrade to advisory; Signal-only auto-skip is *stated*).

## Verdict
- [x] **PASS** — 0 Critical; both Important items resolved (flake = environmental non-defect) or deferred-with-justification (AC2.7 moving-corpus test); Suggestions cataloged (B34/B35) or fixed in-phase (comment). Ready for SHIP.

**Exit criteria:** Critical resolved ✅ · Important resolved/deferred ✅ · security checklist complete ✅ · perf anti-patterns addressed (none) ✅ · simplification pass complete ✅ · report written ✅ · **maintainer approval pending** → next `/sig:ship`.

## Carry-ins for SHIP
1. **B30 self-gates SHIP** — the retro-gate this Epic shipped will hard-block SHIP until `M5.E6-RETROSPECTIVE.md` exists. Write the retro at ship.
2. **B34/B35** await your fix-now-vs-defer call (build-complete option offered).
3. Transient-red per-task commits (Slices 6 & 7) → optional squash at clean-history.
4. `INDEX.md` regen (auto `/sig:index`) + AC7.7 → DECISIONS entry, at ship.
