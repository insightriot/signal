# M5.E1 — Ship Record (SHIP phase)

**Shipped:** 2026-07-16 — Doc-runtime & memory hygiene (model + eviction mechanics). **Effective tier:** FULL / `gate_strictness: strict`. **Shipping mode:** direct-to-main (Signal-on-Signal; no PR). **Release:** **intentionally unreleased — batched with M5.E2/E3** (DECISIONS 2026-07-16); `plugin.json` stays `0.1.7`, CHANGELOG entry is `[Unreleased]`.

## FR1 retro gate (§0.5)
`shipFR1Check` → `{halt: false, isEpicClose: true, retroPath: .planning/M5.E1-RETROSPECTIVE.md}`. Retro `validateRetroContent` → valid (all 8 FULL sections). The E9 chicken-egg resolved as designed: adding the Epic-status table to `MILESTONE-5.md` (E1 = *shipped*) flipped `isEpicCloseShip` to true, at which point the gate enforced the (already-written) retro.

## Pre-ship checklist
- [x] No secrets in code or history (security audit clean; no network/secret surface)
- [x] `.env.example` — N/A (no env vars)
- [x] README — unchanged (new mechanics are internal / wired into existing commands; a README/user-doc pass rides the eventual release)
- [x] CHANGELOG updated — `[Unreleased]` M5.E1 section (Added / Changed / Fixed)
- [x] All tests pass — **1070/1070** (52 files)
- [x] Build/validator — `node tools/validate-plugin.js` green
- [x] Review report issues resolved — REVIEW PASS-WITH-FIXES; 4 Important fixed in-phase + verified, 3 Suggestions taken, rest deferred with justification

## Evict-on-close (§5.5) — dogfood
`evictEpicNarrative(baseDir, "M5.E1")` ran and returned `{evicted: false, reason: "no-section"}` — the wired FR2b path **correctly no-ops** on the live-above-the-fold skeleton body (no `## M5.E1` section). This is the expected close-time behavior; FR2b remains **fixture-proven, never live-fired**.

## Commit span (this session, EXECUTE→SHIP)
- Release-hygiene: `f54c0cc` (B7 — plugin.json 0.1.6→0.1.7, clean main was red).
- Slices: `a8f5f9f` (S1), `bdb35d0`+`d7beb0e` (S2), `3a4970a`+`4675772` (S3), `7fd68d5`+`ba30267` (S4), `1f4ba14` (S5 dogfood).
- Phase closes: VERIFY `9a3db48`, REVIEW `6ae2dcd`, SHIP `eefb6d5`, batch-decision `4e486f3`.
- All pushed; origin synced. Rebased once past `db37944` (MPS bot STATUS.md regen).

## State
`phase: SHIP`; `completed_phases: [DISCUSS, PLAN, EXECUTE]`; `current_epic: M5.E1` (rolls when the next Epic opens). STATE.md body is the lean skeleton (~1 KB; the 64 KB legacy body lives in `STATE-HISTORY.md`). `RETROSPECTIVES.md` regenerated.

## Final anti-rationalization
"Shipping because it's ready, not because I'm tired." Ready: 1070 tests, validator green, two independent specialist REVIEW agents that found (and fixed) a **silent data-loss bug** + a **gate-defeat bug** a green suite sailed past, strict-Nyquist VERIFY, and every carry-forward named rather than glossed. Landed unreleased by deliberate choice, not fatigue.

## Carry-forward (into M5.E2 / M5.E3)
- **FR2b `evictEpicNarrative` fixture-proven, never live-fired** — REVIEW hardened its path (4 Important) before its first real firing at a future Epic's close.
- **FR4/FR5 → M5.E2** (all-docs hygiene runtime + living `BACKLOG.md`; resolves the derived-vs-hand-curated INDEX conflict). **FR6/FR7 → M5.E3** (auto-sensing migrate command + doc-layout stamp; includes relocating already-migrated inlined bodies — no shipped mechanism yet).
- **S5 `dogfood-orientation.test.js` declined by design** (verified-once historical event).
- **markFresh 1-commit staleness floor** (B6) — resume may show a benign "1 commit behind / origin ahead" banner; false-positive, documented.

## Last Updated
2026-07-16 — SHIP complete. Landed on main, unreleased (batched with E2/E3).
