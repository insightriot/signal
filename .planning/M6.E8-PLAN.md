---
epic: M6.E8
tier: FEATURE
phase: PLAN
created: 2026-09-14
---

# `M6.E8` — Plan

**Goal:** `/sig:advise` ranks on every source it claims to consult, drops the read nothing
consults, and its artifact names exactly the sources the ranking used — with every vocabulary
measured on this repository's own file before it is chosen.

**Tier FEATURE** ([`M6.E8-PROFILE.md`](M6.E8-PROFILE.md), written at PLAN entry — `D-BR0914-1`) →
`plan_validation_dims: core`, `research_parallelism: 2` (run), `nyquist_enforcement: strict`,
`attention: checkpointed`.

**Inbox drain:** 1 candidate — *the tier a decision states vs. the tier the phase commands read* —
**promoted** to `BACKLOG.md` as hygiene work (`M6.E8` drain, not absorbed: it is a profile/decision
check, not an advisor input). Trigger watchlist: 11 rows, **11 decided, 0 unevaluated**, 1 dated
(`2026-08-23`, already **expired-clean**). 1 standing entry not counted. Two terminal entries
evicted to the ledger.

---

## What research changed about the plan

Research is [`M6.E8-RESEARCH.md`](M6.E8-RESEARCH.md); the plan changed in four places.

1. **`FR2` does not survive measurement.** *"Entry price for"* hits one live row — the gate row
   itself — and the four rows DISCUSS called *gated on `B75`* cite `B75` as a measurement. Twelve
   sibling phrases: zero correct hits. `BLOCKED_RE` is **not** widened; the zero is recorded beside
   the pattern; `AC2.1` is amended (`D-M6E8-7`). ⚠ **This removes the Epic's stated live effect** —
   see *Open question* at the end.
2. **`FR3` is already input 3.** `backlogDischargeStatus` resolves unit ids through
   `resolveClosures` and `rankRows` drops the result. Building it again is `NFR2`'s forbidden
   second definition. `FR3` becomes a reason-labelling task (`AC3.1`) plus fixtures (`AC3.2`, now
   three), and the depth-3 gap points at the filed bug (`D-M6E8-8`).
3. **`FR7`'s line was wrong the other way.** Input 3 already reads `BUGS.md` and STATE/closure, so
   *"`BACKLOG.md` only"* under-claimed. *Consulted* gets a definition and is derived from what
   `rankRows` received (`D-M6E8-9`).
4. **`re-homed` is in the fold vocabulary**, or `AC4.3`'s *"exactly 2 preserved"* is 1.

## Contract first — the interfaces, before any code

```js
// plugin/tools/lib/backlog.js — two new heading-only predicates beside declaresNotLiveWork
declaresWorkMovedElsewhere(headingText)
  → { moved: boolean, kind: 'folded-into'|'absorbed-into'|'re-homed'|null,
      declaration: string|null, kept: boolean }        // kept:true ⇒ moved:false, always
declaresBugDischarge(headingText)
  → { id: string|null, declaration: string|null }      // verb ADJACENT to the id, or null
export const FOLD_MEASURED, KEPT_MEASURED, BUG_DISCHARGE_MEASURED   // { on, liveRows, hits }

// plugin/tools/lib/advise.js
rankRows(rows, { today, stale = [], discharge = null, confirmedBugs = null })
  → { recommended, declined, consulted: string[] }     // consulted ⊆ ADVISOR_SOURCES, in its order
//   discharge:     the `backlogDischargeStatus` result (or null); its additive `sources`
//                  field says which of the two closure sources input 3 could read
//   confirmedBugs: Set<string> of `confirmed` ids from corpus.sources.bugs (or null)
export const BLOCKED_MEASURED, TRIGGER_MET_MEASURED, NOT_LIVE_MEASURED

// plugin/tools/lib/advise-corpus.js
ADVISOR_SOURCES = ['BACKLOG.md', 'BUGS.md', 'STATE/closure', 'milestone rows']   // four
readCorpus(...).sources = { backlog, bugs, closure, milestones }                  // no retros
```

**Ranking inputs after this Epic, in order** (the doc string in `rankRows` and step 2 of
`commands/advise.md` both carry this list): 1 blocked-by · 2 trigger-met · **3 bug-discharge
(promotes)** · 4 discharge (drops; reason names the source) · 5 age · 6 self-declared not-live
(drops) · **7 fold (drops; `KEPT` overrides)**. Sort: `blocked` asc, `triggerMet` desc,
`dischargesBug` desc, `ageDays` desc, `line` asc.

**Consulted line grammar:** `**Consulted by the ranking:** \`BACKLOG.md\` · \`BUGS.md\` ·
\`STATE/closure\`. **Read, not consulted:** \`milestone rows\` — kept because it is cheap and is the
natural home for a future "already sequenced into an open Epic" input; no ranking input reads it.`
Rendered from `ranked.consulted`, never a literal.

**Every measured constant** has the shape `Object.freeze({ on: 'YYYY-MM-DD', liveRows: N, hits: N
})`, sits beside its pattern, and is asserted by `tests/advise-live-measurement.test.js` against
this repository's file. Its failure message names the remedy: *re-measure, read the new hits by
hand, update the constant.*

---

## What this Epic does for the first time — and it is wave 1

- **`corpus.sources.bugs` is consumed by a ranking input.** `readCorpus` has read `BUGS.md` on
  every run since 2026-09-06 and nothing has ever used the result. Input 3 reads bugs through its
  own path; this is the first consumer of the corpus reader's copy.
- **The counts a vocabulary records are checked by a test.** `declaresNotLiveWork`'s docblock says
  *"50 live rows: parked ×2, not sprint material ×1, reconciliation ×2"* and nothing has ever
  re-run it; the file is at 48 now. `NFR6` asks for counts *in the source*; the source has carried
  unverified counts for a week.
- **The Outcome section's procedure is executed.** *"Diff the next advisory against
  `BACKLOG-REVIEW-2026-09-13.md`"* is a written procedure no run has followed; `M6.E7` VERIFY
  failed to exercise the equivalent on its own numbers. It runs at VERIFY, against a harness built
  in wave 1.

**Therefore S1 is wave 1**: the measurement test lands before any input it will measure, so every
input in S2 and S3 is built against a checked number rather than a remembered one.

---

## Slices

Three, each independently verifiable and shippable. Wave 1 = S1. Wave 2 = S2 and S3 in parallel
(both depend on S1's `consulted` plumbing and measurement test; neither depends on the other).

---

### S1 — Measure first, and tell the truth about the corpus (`FR5`, `FR6`, `FR7`, `NFR6`) — **M**

#### t1.1 — The live-measurement test, and the constants it checks

**Description.** A test that runs this repository's `BACKLOG.md` through `readCorpus` and the
existing predicates and asserts the counts recorded in exported constants beside each pattern.
Ships with the three existing inputs pinned; S2/S3 add theirs.

**Acceptance criteria**
- [ ] `BLOCKED_MEASURED`, `TRIGGER_MET_MEASURED` (advise.js) and `NOT_LIVE_MEASURED` (backlog.js)
      exist, frozen, `{on, liveRows, hits}`, beside their patterns, with today's measured values
      (2 / 3 / 4 on 48 live rows).
- [ ] `tests/advise-live-measurement.test.js` reads the repo's own `.planning/BACKLOG.md` through
      `readCorpus` (depth 4) and asserts each predicate's hit count equals its constant, with a
      failure message that names the remedy.
- [ ] `declaresNotLiveWork`'s docblock counts are re-stated as *"see `NOT_LIVE_MEASURED`"* rather
      than a second copy of the number.

**Test strategy.** Integration (reads the live file). Written first; red until the constants exist.
**Files:** `plugin/tools/lib/advise.js`, `plugin/tools/lib/backlog.js`, `tests/advise-live-measurement.test.js` (new).
**Out of scope:** any change to what the predicates match. Pinning only.
**Exemplar:** RESEARCH § *Exemplars* — `declaresNotLiveWork`'s docblock (the shape) and `drain-standing` `AC2.2c` (the repo-root read, and `B120` as the failure mode to design against).
**Dependencies:** none.

#### t1.2 — The retrospective read is deleted (`FR5`)

**Acceptance criteria**
- [ ] `ADVISOR_SOURCES` has four entries; `sources` has no `retros` slot; the `retro-index` and
      `retrospective` imports are gone from `advise-corpus.js` (`AC5.1`, `AC5.2`).
- [ ] `AC5.2` is asserted by absence: a test spies the module's use of `enumerateRetros` /
      confirms no call — rendered artifact shape is **not** the evidence.
- [ ] The artifact's *Corpus read* section never names retrospectives (`AC5.3`); *"all N sources"*
      renders 4.
- [ ] `t2.3` is removed; `t2.6`'s literal pins four; the *absent / empty / unreadable `.planning/`*
      cases keep their **meaning** on `milestone rows` (absent → cannot-check; empty-but-existing →
      checked with zero files; unreadable → cannot-check with the reason).
- [ ] `commands/advise.md` step 1 and `README.md`'s `/sig:advise` bullet name four sources.

**Test strategy.** Unit + integration; existing suites edited, one new absence assertion.
**Files:** `advise-corpus.js`, `tests/advise-corpus.test.js`, `plugin/commands/advise.md`, `README.md`.
**Out of scope:** `enumerateRetros` and `retrospective.js` themselves (four other callers); `CHANGELOG.md` history.
**Dependencies:** none.

#### t1.3 — The milestone read stays, and the source says why (`FR6`)

**Acceptance criteria**
- [ ] Branch 5 of `readCorpus` carries a header comment stating both halves: cheap (one `readdir`,
      two parses) **and** the future *"already sequenced into an open Epic"* input it is the home
      for, and that no ranking input reads it today.
- [ ] `AC6.1`: a test reads `advise-corpus.js` and asserts the reason sentence is present next to
      the milestone branch (the `M6.E4` *"mechanism is REACHED"* tests are the precedent for a
      source-reading assertion).

**Test strategy.** Unit (source-text assertion). **Files:** `advise-corpus.js`, `tests/advise-corpus.test.js`.
**Out of scope:** using the milestone rows. **Dependencies:** none.

#### t1.4 — `consulted` is derived, and the line renders from it (`FR7`, `D-M6E8-9`)

**Acceptance criteria**
- [ ] `rankRows` accepts `discharge` and `confirmedBugs`, returns `consulted`: `BACKLOG.md` always;
      `STATE/closure` when `discharge.sources.units` is true and `BUGS.md` when
      `discharge.sources.bugs` is true; `BUGS.md` also when `confirmedBugs` is a Set; in
      `ADVISOR_SOURCES` order, deduplicated; never `milestone rows`. ⚠ **Not keyed off
      `discharge.outcome`**: `backlogDischargeStatus` returns `clean` when `BUGS.md` was unreadable
      but no live row led with a bug id, so the outcome would over-claim — `D-M6E8-9`'s own shape.
- [ ] `backlogDischargeStatus` gains an **additive** `sources: { units: boolean, bugs: boolean }`
      field (`readClosureSources` already knows: each map is `null` when unreadable). Additive only;
      the sweep's outcome and `stale` are byte-identical (`NFR3`'s spirit).
- [ ] `runAdvise` passes the full `backlogDischargeStatus` result (not only `.stale`) and a
      confirmed-id Set built from `corpus.sources.bugs` (or `null` when that source was
      cannot-check).
- [ ] `renderArtifact` renders the *Consulted* and *Read, not consulted* sentences from
      `ranked.consulted` (grammar above). No literal source list remains in the renderer.
- [ ] `AC7.1`: a test compares the rendered line against `ranked.consulted` for three runs — clean
      corpus (three consulted), `BUGS.md` unreadable (`BACKLOG.md` + `STATE/closure` only, even
      when the discharge outcome reads `clean`), and `STATE.md` unreadable (`BACKLOG.md` +
      `BUGS.md`) — and never against a literal. The `advise.test.js` case pinning *"no current ranking input reads them"* is rewritten.

**Test strategy.** Unit on `rankRows`; integration through `runAdvise` on the fixture project.
**Files:** `advise.js`, `backlog.js` (additive field), `tests/advise.test.js`, `tests/backlog-discharge.test.js`. **Out of scope:** any new input (S2/S3 extend `consulted`, they do not redefine it). **Dependencies:** none within S1.

**Checkpoint S1:** suite green; the fixture run's artifact names four sources and three consulted;
the measurement test pins 2 / 3 / 4.

---

### S2 — The fold input, with `KEPT` as an override evaluated first (`FR4`) — **S/M**

#### t2.1 — `declaresWorkMovedElsewhere` in `backlog.js`

**Acceptance criteria**
- [ ] Heading-only. Vocabulary `FOLDED INTO` / `absorbed into` / `re-homed`, case-insensitive;
      `KEPT` (case-insensitive) tested **first** and returning `{moved:false, kept:true}` before the
      fold vocabulary runs.
- [ ] Docblock records the measurement (5 / 2 on 48 rows; the one body-only mention heading-only
      avoids) and `FOLD_MEASURED` / `KEPT_MEASURED` sit beside the patterns.
- [ ] Unit fixtures are the five real headings verbatim: three `moved`, two `kept` (`AC4.1`,
      `AC4.2`); plus a lowercase-`kept` prose heading (preserved — the safe direction) and a
      body-only fold (not moved).
- [ ] `HELD_OPEN_RE` untouched (`NFR3`) — asserted by the existing `backlog.test.js` coverage still passing unchanged.

**Files:** `backlog.js`, `tests/backlog.test.js`. **Out of scope:** `backlogDischargeStatus`, `declaresNotLiveWork`. **Dependencies:** t1.1 (constant shape + measurement test).

#### t2.2 — Input 7 in `rankRows`, with its reason

**Acceptance criteria**
- [ ] A `moved` row lands in `dropped` with `declineReason` *"Dropped by the **fold** input — the
      row's own heading says `absorbed into M5.E12`, so its work lives elsewhere."* A `kept` row
      stays live and its recommend/decline reason is unchanged.
- [ ] `AC4.3` on this repo via the measurement test: exactly 3 dropped by fold, exactly 2 preserved
      by `KEPT` (named by heading in the assertion message).
- [ ] `commands/advise.md` step 2 lists input 7 and the override.

**Files:** `advise.js`, `tests/advise.test.js`, `tests/advise-live-measurement.test.js`, `plugin/commands/advise.md`.
**Out of scope:** the artifact's section structure. **Dependencies:** t2.1, t1.4.

---

### S3 — The bug-discharge input, and the two corrected requirements (`FR1`, `FR2`, `FR3`) — **M**

#### t3.1 — `declaresBugDischarge` in `backlog.js`, and input 3 in `rankRows` (`FR1`)

**Acceptance criteria**
- [ ] Heading-only; the discharge verb is **adjacent** to the id (`fixes B75`, `closes B75`,
      `resolves B75`, `discharges B75`, `B75 — fixed/closed/resolved`); a bare done-word elsewhere in
      the heading (*"open/closed work"*) does not match. Docblock + `BUG_DISCHARGE_MEASURED` (0 on
      48 rows; 2 headings name only `fixed` ids; 9 bodies name a confirmed id — the body population
      deliberately not read).
- [ ] `rankRows` promotes a row whose declared id is in `confirmedBugs` (sort key after
      `triggerMet`); `recommendReason` says *"its heading says it discharges `B7`, which `BUGS.md`
      still records as confirmed"*.
- [ ] `AC1.1`: two fixture rows identical except the heading verb; the discharging one ranks above.
- [ ] `AC1.2`: a fixture row whose **body** cites `B75` verbatim from the real file is **not**
      promoted.
- [ ] `AC1.3`: measurement test pins 0 on this repo.
- [ ] `consulted` includes `BUGS.md` when the Set was supplied (already t1.4's rule — asserted here for the input path).

**Files:** `backlog.js`, `advise.js`, `tests/backlog.test.js`, `tests/advise.test.js`, `tests/advise-live-measurement.test.js`.
**Out of scope:** body reading of any kind. **Dependencies:** t1.1, t1.4.

#### t3.2 — `FR2`: the widening that measurement refused (`D-M6E8-7`)

**Acceptance criteria**
- [ ] `BLOCKED_RE` is byte-identical to today. Beside it, a comment records the 2026-09-14
      measurement: *"entry price for"* → 1 hit, the gate row; twelve sibling phrases → 0 correct
      hits; and why the body-bug heuristic is not the answer.
- [ ] `AC2.1′` (amended): the measurement test asserts `BLOCKED_MEASURED.hits` on this repo and
      that the gate row (named by heading) is **not** blocked.
- [ ] `AC2.2` holds by construction (nothing changed); asserted by the same pin.

**Files:** `advise.js`, `tests/advise-live-measurement.test.js`. **Out of scope:** any new blocked vocabulary — that is the `D-M6E8-6` row's work if it is ever pulled in. **Dependencies:** t1.1. **⚠ Gated on the Open question below.**

#### t3.3 — `FR3`: the discharge reason names its source (`D-M6E8-8`)

**Acceptance criteria**
- [ ] `declineReason` for a discharged row reads *"Dropped by the **discharge** input — `M5.E6`
      reads closed in **unit closure** (<evidence>)"* / *"… in **`BUGS.md`** (<evidence>)"*, the
      family decided the way input 3 decides it (`/^B\d+$/`), the evidence taken from the `stale[]`
      entry (`AC3.1`).
- [ ] `AC3.2`: three fixtures with the closed unit present in the fixture's closure — the `M5.E6`
      PARTIALLY-SHIPPED heading, the `M5.E16` renumbered-from heading, and this Epic's own row
      naming `M6.E7` — none dropped.
- [ ] `AC3.3`: `stale.length === 0` on this repo via the measurement test.
- [ ] `commands/advise.md` step 2 names the source in input 4's description; the depth-3 gap is
      cross-referenced to the filed bug by heading, not fixed here.

**Files:** `advise.js`, `tests/advise.test.js`, `tests/advise-live-measurement.test.js`, `plugin/commands/advise.md`.
**Out of scope:** `backlogDischargeStatus`'s parse depth (filed). **Dependencies:** t1.4.

#### t3.4 — Docs and the changelog

- [ ] `commands/advise.md` steps 1–2 reflect four sources and seven inputs; `README.md` bullet
      updated; `CHANGELOG.md` *Unreleased* entry states what fires on this repo (fold 3/2) and what
      ships inert with the count (bug-discharge 0), and names the two DISCUSS corrections.

**Files:** `plugin/commands/advise.md`, `README.md`, `CHANGELOG.md`. **Dependencies:** all above.

**Checkpoint S2+S3 (end of EXECUTE):** suite green; the fixture-project `runAdvise` writes one
file and touches nothing else (`AC8.1` — existing `AC1.4/AC1.5` tests); the citation gate and the
stale-read guard still fail their trip-wire runs (`AC8.2` — existing t3.5 and stale-read tests);
measurement test pins every constant.

---

## Plan validation (core — 3 dimensions)

1. **Goal alignment.** Every task either makes a source count (t2.x, t3.1), deletes a read nothing
   consults (t1.2), or makes the artifact's claim about sources true (t1.3, t1.4, t3.3). t3.2 is the
   one task that *removes* work, and it serves the goal's last clause — measured before chosen.
2. **Completeness.** `FR1`→t3.1 · `FR2`→t3.2 (amended) · `FR3`→t3.3 (amended) · `FR4`→t2.1+t2.2 ·
   `FR5`→t1.2 · `FR6`→t1.3 · `FR7`→t1.4 · `NFR1`/`NFR5`→checkpoint (existing tests) · `NFR2`→t3.3's
   design (no second closure) · `NFR3`→t2.1 · `NFR4`→no profile field anywhere · `NFR6`→t1.1 and
   every `*_MEASURED`. `AC`→test map is [`M6.E8-VALIDATION.md`](M6.E8-VALIDATION.md).
3. **Testability.** Every AC names a test; the three repo-file pins name their failure mode and
   remedy; the two amended ACs are restated so VERIFY reads the column (`B116`) and not the number.

## Risks

| Risk | Mitigation |
|---|---|
| Repo-file pins are `B120`'s shape | Exported constants + remedy in the failure text; a change is the moment to re-check precision, not a nuisance — stated in RESEARCH and in each constant's comment. |
| `KEPT` overlooked and a kept row drops | Override returns **before** the fold vocabulary is consulted; both real headings are fixtures. |
| `consulted` and `cannotCheck` disagree | By design; derived from `rankRows`' inputs. One test covers the disagreement case. |
| Renderer / stale-read guard regress on new scored fields | Additive fields; existing t3.5, stale-read and contrast-row tests are the net. |
| The outcome does not read as "the ranking moved" | It will not, and the plan says so below rather than at VERIFY. |

## Environment

Node v22.13.0 (`engines: >=22`); dependencies resolve; no native modules. Assumed and tested on
Node 22.x.

## Open question — for the PLAN gate, not for EXECUTE

✅ **Answered 2026-09-14 — "go": proceed with the restated outcome** (`D-M6E8-7`). The age-ordering
row stays out of scope.

**The Epic's Outcome, as DISCUSS wrote it, will not happen, and measurement is why.** Item 2
(*four `B75`-gated rows demote*) rests on a gate those rows do not state; item 4 (*the recommended
five change composition*) followed from item 2, since the five fold rows already sit in the declined
pool. What this Epic *will* change on the next advisory: three declined rows carry the correct
reason and two are explicitly preserved; the artifact names four sources and tells the truth about
which three the ranking consulted; two correct-and-inert inputs ship with their zero recorded; and
two DISCUSS claims are corrected in the record. **Recommended: proceed with that restated outcome.**
The alternative that would make the ranking move is pulling the *"ranks on age alone for 44 of 46
rows"* row into scope — a measured blocked vocabulary — which `D-M6E8-6` explicitly left to Brett
and is a scope change, not a plan detail.
