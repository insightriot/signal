# M4.5.E11 — Progress board

Wave graph: `S1 → {S2 ∥ S3} → S4`. Per-task atomic commits `M4.5.E11.S{N}.t{K}`.

## S1 — write-half + mode detection + back-compat net — ✅ COMPLETE (2026-07-15)

| Task | What | Commit | Status |
|---|---|---|---|
| t1 | `EPIC_ID_STRICT_RE` canonical shared regex (kills the schism) | `d7fc9eb` | ✅ |
| t2 | `setCurrentEpic` — current_epic write-half (validate + roll-reset) | `2442bea` | ✅ |
| t3 | `detectMode` — sole mode signal, fail-open to linear | `d9c8705` | ✅ |
| t7 | `deriveNextEpicId` + `--epic` wiring in discuss/new-project (B1) | `77dcc5e` | ✅ |
| t4 | Harden retro-gate hook — fail-open + warn-not-block (R2, D-E11-5) | `92c0c8d` | ✅ |
| t5 | `isEpicDone` + done-Epic guard | `4b10bd0` | ✅ |
| t6 | Linear golden fixture + byte-identity anchor | `8d36e38` | ✅ |

**S1 result:** 894 → **948 suite** (+54, all RED-first), validator green, zero edits to existing fixtures (linear back-compat proven). New surface: `state.js` (`EPIC_ID_STRICT_RE`, `setCurrentEpic`, `detectMode`), `milestones.js` (`deriveNextEpicId`), `retrospective.js` (`isEpicDone`, shared regex), `hooks/check-state-write.js` (warn+fail-open), `commands/{discuss,new-project}.md` (`--epic`), `tests/fixtures/epic-native/linear/`.

## S2 — Epic-scoped artifact naming (FR2) — ✅ COMPLETE (2026-07-15)

| Task | What | Commit | Status |
|---|---|---|---|
| t1 | `artifactName()` — FR2 write-half, symmetric to `resolveArtifactPath` | `35efecd` | ✅ |
| t3 | Epic-mode golden fixture (`M4.5.E99-*`) + resolve anchor | `1f43d85` | ✅ |
| t2 | Thread `artifactName` into the 6 phase commands' write points | `4678361` | ✅ |

## S3 — Per-Epic calibration (FR3) — ✅ COMPLETE (2026-07-15)

| Task | What | Commit | Status |
|---|---|---|---|
| t1 | `readEffectiveProfile()` — whole-file Epic PROFILE shadow, fail-open | `82eabf3` | ✅ |
| t3 | `formatTierLine()` — override never silent (+ resume/status wiring) | `07e9ac9`, `e1d3644` | ✅ |
| t4 | Retrofit the 6 phase commands' gate-read to `readEffectiveProfile` | `cbff2fd` | ✅ |
| t2 | calibrate/escalate/open-flow target the Epic PROFILE when active | `c29f388` | ✅ |

**S2+S3 result:** 948 → **997 suite** (+49, all RED-first for the helpers), validator green, linear golden fixture untouched (byte-identity holds). **C2 honored** — one owner drove all 6 command-file edits; gate-read (§0) and write-points (Workflow) touch different sections, no clobber. New surface: `resume.js` (`artifactName`), `profile.js` (`readEffectiveProfile`, `readProfileFromPath`), `status.js` (`formatTierLine`), `renderResumeBriefing` (`projectTier` param); 6 phase commands + calibrate/escalate/resume/status prose; `tests/fixtures/epic-native/epic/`.

## S4 — Bootstrap dogfood + docs — ✅ COMPLETE (2026-07-15)

| Task | What | Commit | Status |
|---|---|---|---|
| t2 | End-to-end Epic-native chain test (against disk) | `116b85a` | ✅ |
| t3 | `references/epic-native-flow.md` + `MILESTONE-4.5.md` § E11 | `e5becf9` | ✅ |
| t1 | Bootstrap-as-evidence attestation (below) | this commit | ✅ |
| t4 | Validator + full suite + `markFresh` | — | ⏳ close |

### S4.t1 — Bootstrap-as-evidence attestation

E11's own DISCUSS did the STATE transition + Epic-ID assignment **by hand** (the machinery didn't exist yet); the REQUIREMENTS treat that manual bootstrap as the canonical worked example. Each by-hand step E11 now automates, with evidence:

| By-hand at E11 bootstrap | Now automated by | Evidence |
|---|---|---|
| `current_epic: M4.5.E11` hand-typed into STATE | `setCurrentEpic` (FR1, S1.t2) | S4.t2 opens `M4.5.E77` via `setCurrentEpic` — **zero hand-edited STATE** — and reads it back (`116b85a`) |
| Epic ID chosen by hand | `deriveNextEpicId` + `--epic` (S1.t7) | unit: `M4.5.E11` latest → `M4.5.E12` (`77dcc5e`) |
| Artifacts hand-named `M4.5.E11-*.md` | `artifactName` (FR2, S2.t1) | S4.t2 write→read round-trip for all 7 phase artifacts against disk (`116b85a`) |
| Per-Epic tier judged by hand (E11 ran at project FULL) | `readEffectiveProfile` (FR3, S3.t1) | S4.t2: Epic SKETCH PROFILE shadows project FULL (`116b85a`) |

**Caveat (honest):** E11 itself could not be a zero-hand-edit dogfood — its Epic was opened before the write-half existed. The mechanical proof that a *new* Epic auto-manages end-to-end is the S4.t2 fixture, not a re-run of E11. This attestation is the EXECUTE-phase record; VERIFY formalizes it in `M4.5.E11-VERIFICATION.md` per the strict-Nyquist per-AC attestation pattern.

## Last Updated

2026-07-15 — S1–S4 all shipped; EXECUTE complete for E11 (all 4 slices). Next: VERIFY.
