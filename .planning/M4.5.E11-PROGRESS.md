# M4.5.E11 — Progress board

Wave graph: `S1 → {S2 ∥ S3} → S4`. Per-task atomic commits `M4.5.E11.S{N}.t{K}`.

## S1 — write-half + mode detection + back-compat net — ✅ COMPLETE (2026-07-15)

| Task | What | Commit | Status |
|---|---|---|---|
| t1 | `EPIC_ID_STRICT_RE` canonical shared regex (kills the schism) | `d7fc9eb` | ✅ |
| t2 | `setCurrentEpic` — current_epic write-half (validate + roll-reset) | `2442bea` | ✅ |
| t3 | `detectMode` — sole mode signal, fail-open to linear | `d9c8705` | ✅ |
| t7 | `deriveNextEpicId` + `--epic` wiring in discuss/new-project (B1) | `77dcc5e` | ✅ |
| t4 | Harden retro-gate hook — fail-open + warn-not-block (R2, D-E11-5) | `92c0c8d` | ✅ |
| t5 | `isEpicDone` + done-Epic guard | `4b10bd0` | ✅ |
| t6 | Linear golden fixture + byte-identity anchor | `8d36e38` | ✅ |

**S1 result:** 894 → **948 suite** (+54, all RED-first), validator green, zero edits to existing fixtures (linear back-compat proven). New surface: `state.js` (`EPIC_ID_STRICT_RE`, `setCurrentEpic`, `detectMode`), `milestones.js` (`deriveNextEpicId`), `retrospective.js` (`isEpicDone`, shared regex), `hooks/check-state-write.js` (warn+fail-open), `commands/{discuss,new-project}.md` (`--epic`), `tests/fixtures/epic-native/linear/`.

## S2 — Epic-scoped artifact naming (FR2) — ⏳ next

`artifactName()` helper + thread into 6 phase commands + Epic golden fixture. Deps: S1 ✅.

## S3 — Per-Epic calibration (FR3) — ⏳ (parallel with S2 after S1)

`readEffectiveProfile` + `--epic` writes `{EpicID}-PROFILE.md` + status/resume tier provenance + 6-command gate-read retrofit. **Coordination (C2):** S2.t2 and S3.t4 both edit the 6 command `.md` files — serialize those two.

## S4 — Bootstrap dogfood + docs — ⏳ last

Run E11's own EXECUTE→SHIP with auto-managed current_epic + Epic-named artifacts; end-to-end fixture; docs (Epic mode + two-tier retro); `MILESTONE-4.5.md` § E11. Deps: S2 + S3.

## Last Updated

2026-07-15 — S1 shipped (7/7 tasks). Next: S2 + S3.
