# M5.E2 — REVIEW Report

**Phase:** REVIEW. **Tier:** FULL — `review_depth: full`, `security_audit: full`, `performance_pass: true`, `simplification_pass: true`, `gate_strictness: strict`. **Date:** 2026-07-18. **Scope:** `git diff f616525..HEAD` — the `/sig:migrate-memory` command (~2,724 production lines; `tools/lib/migrate-memory.js` 2,018 + `archive-tree.js` + `hooks/warn-layout-drift.js` + resume/status/state wiring).

## Method
Adversarial **3-specialist parallel panel** (independent, read-only): **code-reviewer** (5-axis quality + perf + simplification), **security-auditor** (OWASP Top 10 + ASVS L2 on the fs/git/stranger-repo surface), **test-engineer** (test-theater + coverage). The code-reviewer and security-auditor **independently found and empirically reproduced the same Critical** (rollback gap) — high-confidence convergence. All findings were then fixed in-phase across **5 surgical batches**, each RED-first where behavioral, with the full suite green throughout.

## Verdict: **PASS-WITH-FIXES**
REVIEW found **1 Critical (SHIP-blocker) + 6 Important + assorted suggestions**. Every Critical and Important was **fixed in-phase** (surgical per-finding + test additions; no architectural re-plan needed), RED-first-proven, suite green. Deferred items are genuine conscious-decisions / bounded fast-follow tickets (B14, B15), none SHIP-blocking. Final: **1300 tests / 81 files, build + validator green.** (Fix volume exceeds the 50-LOC PASS-WITH-FIXES rule-of-thumb, but no finding required re-planning or loop-back-to-EXECUTE; this is the workflow's "REVIEW findings fixed in-phase" pattern, consistent with prior Epics.)

## Critical Issues (must fix before SHIP) — RESOLVED
- **C1. Rollback not wired around the mechanical move/rewrite phase → unrecoverable partial write.** `migrate-memory.js:1890` (STATE write) + `:1919` (`applyArchiveTree`) sat *outside* the try/catch calling `rollback()`. A routine I/O throw mid-apply (ENOSPC/EACCES/ENOTDIR/byte-identity assert) left STATE de-prosed+stamped, some scaffolds moved+deleted, referrers half-rewritten — and in `fs-backup`/`--force`-dirty modes the durable snapshot persisted only *after* the throw → **zero recovery**. *(Both reviewers reproduced it via `.planning/archive` as a regular file → ENOTDIR.)* **→ FIXED `50ad065`:** mechanical phase wrapped in `try{…}catch(e){ await rollback(); throw e }`; durable snapshot + pre-apply tag hoisted *before* the phase; RED-proven (STATE left mutated before → byte-identical rollback after).

## Important Issues — RESOLVED
- **I-a. Fence-less STATE.md false-success** (`50ad065`) — a legacy no-frontmatter STATE.md returned `applied:true` + a fresh tag while changing nothing (re-taggable forever). Now a **clean refuse** (`applied:false`, no tag, clear message); `changed` made truthful (N3).
- **I-b. Directory-symlink path escape (security MEDIUM)** (`ab2242d`) — lexical `startsWith(planningRoot+sep)` let a checked-in directory symlink in `.planning/` write outside the tree + delete the in-tree source. Now `realpathNearestExisting` + `assertRealInsidePlanning` on both write gateways; RED-proven (out-of-tree write + source deletion → refused). Leaf-file-symlink carve-out preserved.
- **I-c. No real-parser round-trip assertion (test C1)** (`bc8d0b5`) — the migrate's regex "conformant" was never checked against the real `state.js` `yaml` consumer that resume/status/doctor use. Added a round-trip test across all 5 shapes. **Result: PASSED for every shape — no bug** (safe by construction; the gap was "unasserted," now closed).
- **I-d. `readLayoutBanner` hot-path perf / DoS** (`d655252`) — called `senseProject` (full-corpus walk + uncapped STATE read) unconditionally on every `/sig:status` + `/sig:resume`. Now short-circuits on a cheap 64 KB stamp read (RED-proven: `senseProject` 1×→0× on a stamped project); helpers promoted to dependency-light `tools/lib/layout-stamp.js` (hook keeps fail-open-at-import).
- **I-e. Confounded conservation test (I1)** (`bc8d0b5`) — `migrate-conservation.test.js` claimed to pin multiset counting but its fixture dropped a word entirely (green for the wrong reason, on a load-bearing gate). Replaced with a true discriminator (drops one `the`, keeps `and`).
- **I-f. Missing blocker-label + CRLF-core tests (I2/I3)** (`bc8d0b5`) — B12's `deriveNonStandardLabel` was wired to `blockers[].text` with no test; the migrate core had no CRLF fixture. Both added (+42 tests total). CRLF confirmed **N2** (mixed-EOL in the relocated *body* region) is **cosmetic-only** — frontmatter stays clean CRLF, conservation holds, idempotent, round-trips.

## Security Findings (OWASP Top 10 + ASVS L2)
- **Fixed:** rollback/partial-write integrity (C1, ASVS V1.11 / A04); directory-symlink escape (I-b, A01 / ASVS V12.3); uncapped-read DoS on status/resume (I-d, A05); `git add --` end-of-options defense-in-depth (`50ad065`, ASVS V5.3.8 — was unreachable, hardened).
- **Deferred (ticketed):** codebase-wide lexical confinement in `evict.js`/`add.js`/`resume.js` (**B14**); lock-TTL steal window on a >5 s whole-corpus apply (accepted — mitigated by the "no parallel sessions on one repo" norm).
- **Confirmed SAFE (with the guard named):** path traversal via `current_epic`/Epic-ID/filename (`EPIC_ID_STRICT_RE` + trailing-`sep`); git command/option injection (all `execFileSync`, args arrays, no shell); `--force` destructive safety (surgical per-file snapshot, **never** `git reset --hard`); TOCTOU hash-binding (under the lock, pre-write); coarse-lock non-reentrancy (never calls self-locking wrappers); regex backtracking (≤ O(n²), ceiling-bounded); atomicWrite leaf-symlink overwrite (same-dir tmp + rename); secrets/info-leak (none; hook fails open to a fixed banner). **No unresolved security defect blocks SHIP.**

## Performance Findings
- `readLayoutBanner` short-circuit (I-d) — the one real hot-path issue, fixed. Redundant re-senses inside `applyMigrate`/`renderDryRun` are acceptable for a one-shot command (noted, not changed).

## Simplification
- **B13** NUL byte → `\0` (`50ad065`) — file greppable again (was the session-long "grep glitch"). **S2** command-doc footgun (`dd77ef1`) — the 3 self-locking standalone wrappers (`relocateInlinedBody`/`setDocsLayoutVersion`/`applyDeproseVector1`) moved to a ⚠ "STANDALONE / testing-only" blockquote (they bypass the composed-under-one-lock harness). Architecture: the 2,018-line `migrate-memory.js` is large but the pure-planner / disk-sense / apply-orchestrator split is coherent and individually testable — **no split demanded** (size didn't hide C1; an omitted try/catch hides in any file).

## Deferred — conscious decisions & fast-follow tickets (none SHIP-blocking)
- **B14** (P3, ticket): codebase-wide symlink-blind confinement (`evict.js`/`add.js`/`resume.js`) — same fix pattern as I-b; deferred to avoid scope-creeping E2.
- **B15** (P3, ticket): `FILE_SCAN_CEILING` 1 MB truncation can silently defeat the dangling gate on a >1 MB single doc (extreme — real corpora <1 MB).
- **S3** (decision): `stampOnConformance` is V3/archive-blind — a project with un-evictable closed narrative (no retro → no-fabricate flag) still gets stamped and thereafter the banner goes silent. *Accepted:* nothing more the tool can safely do; the human dry-run sees the flagged narrative. Worth a doc note in a later pass.
- **S4** (decision): V2's 8 KB threshold can relocate a large *live* STATE body to `STATE-HISTORY.md` behind a "history" pointer (no carry-over lifting). *Accepted:* human-gated by the dry-run + model-intended thin body; the pointer's "history" framing is the only wart.
- **S5** (defense-in-depth, ticket): V1 WORD conservation checks the whole body, not specifically the new relocation section — safe today (verbatim-by-construction) but weaker than its docstring. Deferred rather than re-scope a load-bearing gate pre-ship.
- **N2** (cosmetic): mixed-EOL in the relocated body region on CRLF files — cosmetic-only (proven); fix locus noted (`buildRelocationSection` join with `nl`).

## Exit criteria
- [x] All Critical issues resolved (C1 fixed `50ad065`, RED-proven).
- [x] All Important issues resolved (I-a…I-f fixed/added across `50ad065`/`ab2242d`/`d655252`/`bc8d0b5`).
- [x] Security checklist completed (OWASP + ASVS L2; 1 HIGH + 1 MEDIUM fixed, LOWs fixed/ticketed, SAFE items enumerated).
- [x] Performance anti-patterns addressed (readLayoutBanner short-circuit).
- [x] Simplification pass completed (B13, S2).
- [x] Review report written.
- [ ] **User approves review results** — the REVIEW gate.

## Last Updated
2026-07-18 — REVIEW **PASS-WITH-FIXES**. 3-specialist adversarial panel; 1 Critical (rollback gap, 2× independently reproduced) + 6 Important, ALL fixed in-phase across 5 RED-first batches (`50ad065`→`dd77ef1`); B11/B13 fixed; B14/B15 ticketed; S3/S4/S5/N2 documented decisions. 1281→**1300 tests**, build + validator green. Next: REVIEW gate approval → SHIP.
