# M4.5.E11 — Ship Record (SHIP phase)

**Released:** v0.1.7 — Epic-native flow — 2026-07-15. **Effective tier:** FULL / `gate_strictness: strict`. **Shipping mode:** direct-to-main (Signal-on-Signal; no PR — the release tag + GitHub Release are the artifact).

## FR1 retro gate (§0.5)
`shipFR1Check` → `{halt: false, isEpicClose: true, retroPath: .planning/M4.5.E11-RETROSPECTIVE.md}`. Retro `validateRetroContent` → valid (all 8 FULL sections, above threshold). The E9 chicken-egg resolved as designed: annotating `MILESTONE-4.5.md` (§ E11 status + status-table row) as *shipped* is what flipped `isEpicCloseShip` to true, at which point the gate enforced the (already-written) retro.

## Pre-ship checklist
- [x] No secrets in code or history (security audit clean; no env/secret surface added)
- [x] `.env.example` — N/A (no env vars)
- [x] README updated — Epic-mode note added (`--epic`, opt-in, → `references/epic-native-flow.md`)
- [x] CHANGELOG updated — `[0.1.7]` section (Added / Fixed / Notes)
- [x] All tests pass — 999/999 (49 files)
- [x] Build/validator — `node tools/validate-plugin.js` green
- [x] Review report issues resolved — REVIEW PASS-WITH-FIXES; I-1 fixed in-phase, S-1 rejected-with-reason, N-1 deferred

## Release mechanics
- **Content/release commit:** `8e05e56` — version bump, CHANGELOG, retro, docs, index, STATE→SHIP.
- **Pin commit (tag carrier):** `0eb8ca8` — `marketplace.json` `ref: v0.1.7` + `sha: 8e05e56`, `markFresh`.
- **Tag:** `v0.1.7` on `0eb8ca8`. **GitHub Release:** https://github.com/insightriot/signal/releases/tag/v0.1.7
- **Commit span (this session, S2→SHIP):** `35efecd..0eb8ca8`, all pushed; origin synced.

## State
`phase: SHIP`; `completed_phases: [DISCUSS, PLAN, EXECUTE, VERIFY, REVIEW]`; `current_epic: M4.5.E11` (rolls when the next Epic opens). `RETROSPECTIVES.md` index regenerated (11 retros).

## Final anti-rationalization
"Shipping because it's ready, not because I'm tired." Ready: 999 tests, validator green, two independent specialist REVIEW agents (0 Critical), strict-Nyquist VERIFY with no unmapped AC, a runtime FR3 check a green suite couldn't provide, and a rejected specialist suggestion that would have broken back-compat. The feature is additive and byte-identical in linear mode — the blast radius on existing users is zero.

## Carry-forward
- **Two-tier retro gate (B2):** kept `/sig:ship` §0.5 hard per the SHIP-time decision (2026-07-15).
- **markFresh 1-commit staleness floor** — flagged in the retro as a FUTURE-IDEAS candidate (structural, benign).
- **Milestone 4.5 external-validation criterion** (≥3 non-Signal testers) still open — unchanged by this Epic.

## Last Updated
2026-07-15 — SHIP complete. v0.1.7 released.
