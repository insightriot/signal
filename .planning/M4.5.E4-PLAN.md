# M4.5.E4 — Plan (PLAN phase)

**Phase:** PLAN (output of `/sig:plan`, 2026-06-02). **Tier:** FULL — `gate_strictness: strict`, TDD required, all 8 validation dims, strict Nyquist.
**Inputs:** `M4.5.E4-REQUIREMENTS.md`, `M4.5.E4-RESEARCH.md`, DECISIONS 2026-06-01.

## Phase goal

Ship one runnable, current, self-explaining worked example and a short prose comparison page — both linked from the README — so a newcomer can see what Signal produces and learn when to reach for it.

## Architecture decisions (locked in DISCUSS + RESEARCH)

- Worked example = full `calibrate→ship` run, reused from `.dogfood/url-shortener-fulltier/`, promoted to a **git-tracked** `examples/url-shortener/` (D-E4-1/2/3).
- Storage swapped `better-sqlite3` → Node built-in `node:sqlite`; `engines.node >= 22.5`; example `test` script runs `node --experimental-sqlite` (RESEARCH §1).
- Only **STATE.md** needs convention refresh (`schema_version: 1`, `phase: SHIP`, `current_epic: null`); the AC-count contradiction is **reconciled** (RESEARCH §2).
- Currency guard asserts **`state._schema === 1`** + `readProfile` succeeds (RESEARCH §3).
- `docs/vs.md` = prose decision guide, ≤80 lines, 5 comparators, heritage tone, calibration router as the wedge (D-E4-4/6).
- `docs/vs.md` added to validator `REQUIRED_FILES` (RESEARCH §3).

## Task list — 3 vertical slices

### Slice 1 — Worked example (`examples/url-shortener/` + currency guard)

Independently shippable: a stranger can clone, `npm test`, and read what each phase produced.

- [ ] **t1 — Promote the dogfood into `examples/url-shortener/`.**
  - Copy `src/`, `tests/`, `.planning/`, `README.md`, `CHANGELOG.md`, `package.json`, `.env.example`, `.gitignore` from `.dogfood/url-shortener-fulltier/`. **Exclude** `node_modules/` and the nested `.git/` (J4).
  - Acceptance: `examples/url-shortener/` exists git-tracked; no `node_modules/`, no nested `.git/`; tree matches the dogfood source minus those two.
  - Verify: `git status` shows the new files staged-able; `find examples/url-shortener -name node_modules -o -name .git` returns nothing.
  - Files: new `examples/url-shortener/**`. Scope: **S**.
- [ ] **t2 — Swap storage to `node:sqlite` + verify the example's own suite.**
  - Edit `examples/url-shortener/src/storage.js`: `better-sqlite3` → `node:sqlite` `DatabaseSync`; move the 3 pragmas into `db.exec('PRAGMA …')`. Trim `package.json` (remove `better-sqlite3`; `engines.node >= 22.5`; `test` script → `node --experimental-sqlite ./node_modules/.bin/vitest run` or equivalent). Note Node 24+ is flag-free in the example README (covered in t4).
  - Acceptance (AC2): `npm test` inside `examples/url-shortener/` passes all ~39 tests on the dev machine (Node 22.13.0); no native module remains (`better-sqlite3` gone from deps).
  - Verify: run the example suite; re-confirm `tests/shutdown.test.js` directory-path case still asserts a thrown error under `DatabaseSync`.
  - Files: `examples/url-shortener/src/storage.js`, `examples/url-shortener/package.json`, possibly `tests/shutdown.test.js`. Scope: **S**.
- [ ] **t3 — Refresh artifacts to current conventions + reconcile AC count.**
  - Hand-author `examples/url-shortener/.planning/STATE.md` with `schema_version: 1` frontmatter, `phase: SHIP`, `current_epic: null`, `current_wave: null`, `current_tasks: []`, `completed_phases` (the 7 entries, array shape), `blockers: []`, plausible 2026-04-26-era timestamps; preserve any human narrative below `---`.
  - Reconcile the AC-count mismatch (16 vs 24) across REQUIREMENTS.md / 1-SHIP.md / 1-VERIFICATION.md to the true count + correct labels (J3); skim 1-RESEARCH/VALIDATION/PROGRESS/VERIFICATION/REVIEW for the same issue + any `DONE`-style enum refs (J5).
  - Acceptance (AC3): example STATE.md carries `schema_version: 1` + valid `phase`; PROFILE.md still validates; AC counts consistent across artifacts; a short drift-audit note records what changed (and that "tranche/Milestone vocab" was a no-op).
  - Verify: `node -e "import('./tools/lib/state.js').then(m=>m.readState('examples/url-shortener').then(s=>console.log(s._schema)))"` prints `1`; manual read of the reconciled counts.
  - Files: `examples/url-shortener/.planning/STATE.md` + the AC-count artifacts + a drift-audit note (in the example README or `.planning/REFRESH-NOTE.md`). Scope: **M**.
- [ ] **t4 — Write the example's annotated-tour README.**
  - Top-level `examples/url-shortener/README.md`: what the project is; that it was calibrated **FULL** and *why* (which calibration answers fired which rule); a short walk of what each phase artifact shows; the run command + Node note (24+ flag-free, 22.5+ needs `--experimental-sqlite`).
  - Acceptance (FR2): a newcomer can read it and understand the calibration→rigor link and how to run the app.
  - Verify: manual read-through; all internal links resolve.
  - Files: `examples/url-shortener/README.md`. Scope: **M**.
- [ ] **t5 — Add the currency guard test.**
  - `tests/example-currency.test.js`: assert `readProfile(EXAMPLE_DIR)` resolves; assert `readState(EXAMPLE_DIR)` returns `_schema === 1`. Read-only, no fixtures (RESEARCH §3 path pattern).
  - Acceptance (AC4): the guard passes against the refreshed example; root suite stays green.
  - Verify: `npm test` (root) green; temporarily revert t3's STATE.md to legacy → guard fails (proves it bites) → restore.
  - Files: `tests/example-currency.test.js`. Scope: **S**.

**Checkpoint (Slice 1):** example clones + `npm test` passes; guard green; root suite green; artifacts current.

### Slice 2 — Comparison page (`docs/vs.md`)

Independent of Slice 1. Source material in RESEARCH §4.

- [ ] **t6 — Write `docs/vs.md`.**
  - Prose decision guide, the 8-section structure (RESEARCH §4), ≤80 lines, lead with "reach for Signal when…" (AC5), heritage-respectful, calibration router as the wedge, link to `analysis/REPO-ANALYSIS.md`. Honor all 5 accuracy traps (no upstream counts; v2-planned honesty for superpowers/compound-engineering; benchmark attribution).
  - Acceptance (AC5): file exists, ≤80 lines, covers the 5 comparators + Signal, leads with Signal, names the calibration router, heritage tone, no accuracy-trap violations.
  - Verify: line count `wc -l docs/vs.md` ≤ 80; manual read against the 5 traps; all links resolve.
  - Files: `docs/vs.md`. Scope: **M**.
- [ ] **t7 — Add `docs/vs.md` to validator `REQUIRED_FILES`.**
  - Acceptance (FR8): `tools/validate-plugin.js` lists `docs/vs.md`; validator still exits 0.
  - Verify: `node tools/validate-plugin.js` exit 0; `validate-plugin.test.js` subprocess test green.
  - Files: `tools/validate-plugin.js`. Scope: **S**.

**Checkpoint (Slice 2):** vs.md published + under budget + validator green.

### Slice 3 — Wire-up (README links + CHANGELOG + link integrity)

Depends on Slices 1 + 2 existing.

- [ ] **t8 — Link both artifacts from the README.**
  - Add a "see a full run" pointer to `examples/url-shortener/` (near `## Your first project`) and a "how Signal compares" pointer to `docs/vs.md`. Keep consistent with the existing `## Open Source Origins` characterizations.
  - Acceptance (AC6/FR6): README links both; characterizations don't contradict vs.md.
  - Verify: links resolve; manual consistency read.
  - Files: `README.md`. Scope: **S**.
- [ ] **t9 — CHANGELOG entry.**
  - Add an E4 entry under the current Unreleased/next block (per E7/E3/E9/E8/E2 convention).
  - Acceptance (AC8): CHANGELOG names the worked example + comparison page.
  - Files: `CHANGELOG.md`. Scope: **S**.
- [ ] **t10 — Link-integrity sweep.**
  - Check every link in `docs/vs.md`, `examples/url-shortener/README.md`, and the README additions resolves (no dead links).
  - Acceptance (AC7): no dead links.
  - Verify: grep links + resolve each; or a lightweight check in VERIFY.
  - Files: none (verification task). Scope: **S**.

**Checkpoint (Slice 3 / Epic):** all 9 ACs met; root suite + validator green; both artifacts linked.

## Dependencies

- Slice 1: t1 → t2 → t3 → (t4, t5 parallel after t3).
- Slice 2: t6 → t7. Independent of Slice 1 (parallelizable).
- Slice 3: after Slices 1 + 2 (t8 needs both to exist). t8 → t9 → t10.

## Risks & mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| `node:sqlite` experimental warning on Node 22.x muddies "clean run" | Low | `engines.node >= 22.5` + flag in test script + README note; EXECUTE may bump to `>=24` if available for a flag-free run |
| `shutdown.test.js` directory-path case behaves differently under `DatabaseSync` | Low | t2 re-verifies it; adjust the assertion to the new error shape if needed (keep it asserting a real failure) |
| Reconciling AC counts edits "historical" artifacts | Low | Treat as curated showcase; correct to the *true* count only (accuracy, not fabrication); record in drift-audit note |
| vs.md drifts into competitive/inaccurate claims | Med | RESEARCH §4 trap-list is the checklist; REVIEW re-reads against it |

## Environment check (PLAN Step 6)

Done in RESEARCH §1: Node 22.13.0 / npm 10.9.2 / macOS arm64 / Xcode CLT present. `node:sqlite` available (flag-free w/ warning on 22.13). The swap *removes* the native dep, so no build-toolchain budget needed in EXECUTE. No external services.

## Open questions

None blocking. All forks resolved in DISCUSS + RESEARCH; plumbing defaults locked per the gate-at-product-altitude norm.
