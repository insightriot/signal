---
tier: FEATURE
schema_version: 1

calibration:
  scope: feature
  stakes: minor
  novelty: familiar
  reversibility: moderate
  horizon: months

phases_skipped: []

rigor_overrides:
  tdd_required: true
  security_audit: basic
  performance_pass: true
  simplification_pass: true
  nyquist_enforcement: strict
  plan_validation_dims: core
  research_parallelism: 2
  gate_strictness: light
  context_rot_reread: true
  review_depth: quality-only

metadata:
  created_at: 2026-09-03T00:00:00.000Z
  created_by: sig:discuss (per-Epic override, D-M6E6-1)
  escalation_history: []
---

# `M6.E6` — per-unit calibration

**FEATURE, inside a FULL project.** The third use of the per-unit dial, after `M6.E4` and `M6.E5`.

**Why FEATURE.** The modules exist and are tested (`queueDecision`, `readQueue`,
`formatQueuedDecision`, all shipped `v0.1.31`); the work is wiring them to a caller and deciding a
routing rule that a prior analysis already specified in full. `novelty: familiar` is honest for
that. `stakes: minor` — nothing here changes what reaches `main`, and the floors are untouched.

**`reversibility: moderate`, not `trivial`** — and the distinction is the Epic's own subject. FR2
narrows what DISCUSS currently auto-adopts at `unattended`, which changes behaviour for anyone
running that mode. Reversible by revert; not free.

**The trigger for escalating back to FULL:** if `D3` (per-decision reversibility tagging) turns out
to need a mechanism rather than a convention, that is new design surface with no prior art here, and
it earns the fuller treatment. Sized at DISCUSS as a convention; revisit at PLAN if research says
otherwise.
