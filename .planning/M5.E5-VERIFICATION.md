# M5.E5 — Verification Report

**Verdict: ✅ PASS.** All acceptance criteria satisfied with evidence (AC2.4 struck by ratified descope). Full suite green + deterministic; strict Nyquist compliance met; build + lint clean.

**Method:** independent of the executors' self-reports — the suite was re-run fresh, and every AC-mapped test was physically confirmed to exist (named + line-located) in the committed test files. Tier FULL/strict → all steps run; `nyquist_enforcement: strict`.

## Suite / build / lint
- `npm test`: **1561 passed** (96 files, 0 fail) — independent VERIFY run, consistent with the EXECUTE-close deterministic double-run (1529 baseline → 1561, +32).
- `npm run validate`: Plugin validation passed.
- `npm run lint`: 0 errors.

## Acceptance-criteria verification (test → status → evidence)

### FR1 / B24 — `501ad9d` (`tests/migrate-dangling-baseline.test.js`)
| AC | Test (line) | Status |
|---|---|---|
| AC1.1 migrate succeeds on pre-existing closed-block dangle | `AC1.1 — the migrate SUCCEEDS…` (L227) | ✅ RED-first (inverted the old abort assertion) |
| AC1.2 dry-run/apply parity | `AC1.2 — dry-run/apply parity…` (L259) | ✅ RED-first |
| AC1.3 introduced dangle still aborts (non-injected) | `a NEW dangle the migrate introduces → abort + rollback` (L126) | ✅ gate-strength guard (disclosed green-both; discriminates vs a catastrophic "delta always empty" fix) |
| AC1.4 multiset — 2nd dangle to same missing target | `COUNT/MULTISET semantics… (AC1.4)` (L91) | ✅ RED-first; **verified to fail against a `Set` swap** |
| AC1.5 `computeDanglingDelta` unit updated | `…keyed on resolved abs-target` (L61) + re-key test (L76) | ✅ |
| AC1.6 named limitation documented | in-code `migrate-memory.js:1519-1524` | ✅ |

### FR2 / B26 — `d62e5f2` (`ship-fr1` / `retrospective` / `hook-state-write` tests)
| AC | Test (line) | Status |
|---|---|---|
| AC2.1 fires on self-hosted no-row Epic-close | `AC2.1: fires…halts NO_RETRO_FILE` (ship-fr1 L256) + passes-with-retro (L269) | ✅ RED-first |
| AC2.2 no false-fire on maintained pending row | `AC2.2: no false-fire…` (ship-fr1 L285) | ✅ green-both (row-absence guard) |
| AC2.3 tier-aware | ship-fr1 L298/L323/L339 + `isEpicCloseByState` units (retrospective L483-545) | ✅ RED-first (FULL returned false unfixed) |
| AC2.4 (Layer 3) | — | **STRUCK — descoped (ratified).** No-op: Layer 3 at EXECUTE, fallback at SHIP. See CONTEXT D2. |
| AC2.5 Layer 2 re-key | `AC2.5:…B26 re-key` (hook-state-write L90/L108/L122) | ✅ RED-first (spawn fixture stopped warning) |
| AC2.6 existing ship-fr1 cases pass | per-slice skip (L148), tier anchors (L174), etc. | ✅ |
| AC2.7 `:600-603` trigger undisturbed | `detectDirtyExecute` + `isEpicCloseShip` defs byte-unchanged (guard verified at `:697`); `does not fall back when phase not SHIP` (ship-fr1 L361) | ✅ verified by non-modification + passing existing tests |

### FR3 / B25 — `8cbb8b7` (`tests/rmw-lock.test.js`)
| AC | Test (line) | Status |
|---|---|---|
| AC3.1 discriminates (fail-on-broken / pass-on-real) | real read-enclosed (L321) + broken-twin `LOST UPDATE` (L339) | ✅ RED proven via throwaway scratch spec (deleted); committed as a permanent anti-tautology guard (twin asserts `lostUpdate===true`) |
| AC3.2 behavioral (on-disk lost-update) | L321/L339 assert final content | ✅ |
| AC3.3 throw-lock semantics (arriving writer fails fast) | L321 asserts B threw `/lock/i`, never committed | ✅ |
| AC3.4 seam inert in prod | 0 `commands/*.md` callers; seam defaults undefined | ✅ |
| AC3.5 no re-entrancy regression | `promoteDrainEntryCore` routes lock-free Core | ✅ |
| AC3.6 full close (all 6) | `it.each(RMW_PATHS)` (L353), `RMW_PATHS` = 6 (L71) | ✅ |

### FR4 / B6 — `35f6a04` (`tests/is-state-stale.test.js`)
| AC | Test (line) | Status |
|---|---|---|
| AC4.1 STATE-only "+1" → not stale | `AC4.1: a pure bookkeeping +1…` (L231) | ✅ |
| AC4.2 single PLAN-only commit → stale | `AC4.2: a single *-PLAN.md-only commit…IS stale` (L288) | ✅ **RED-first** (`expected false to be true` on unfixed) |
| AC4.3 two PROGRESS-only → stale (count 2) | `AC4.3:…commitCount 2` (L304) | ✅ RED-first |
| AC4.4 split STATE refresh → not stale | `AC4.4:…split refresh…NOT stale` (L323) | ✅ green-both (count-independence guard vs the rejected `commits.length===1`) |
| AC4.5 Case 2 + Case 3 still stale | L246 / L262 | ✅ |
| AC4.6 `:136` pathspec green | pathspec test unchanged | ✅ (`STATE_AFFECTING_PATHS` byte-unchanged) |
| AC4.7 CONTEXT.md-only → not stale | `AC4.7: a CONTEXT.md-only commit is NOT stale` (L346) | ✅ green-both (bookkeeping classification) |
| AC4.8 fail-open + short-circuit unchanged | L171 / L187 / L198 | ✅ |

## Nyquist strict compliance
Every FIX-verifying test is RED-first — form (b) attestation (test written before the implementation, in the same atomic per-task commit, the executor witnessing the failure): **AC1.1/1.2/1.4** (501ad9d), **AC2.1/2.3/2.5** (d62e5f2), **AC3.1** (8cbb8b7 — scratch-RED + committed anti-tautology guard), **AC4.2/4.3** (35f6a04). Honestly disclosed as **green-both-ways guards** (regression/invariant protection, not fix-repros, so outside the "failed-first" requirement): AC1.3, AC2.2, AC4.4, AC4.7. No false-greens: the two load-bearing discriminators (AC1.4 vs a `Set`; AC3.1 vs the read-outside-lock twin) were each verified to genuinely fail on the wrong implementation.

## Known / descoped items (carried to REVIEW + SHIP)
- **AC2.4 struck** — Layer 3 descope, ratified (CONTEXT D2). B26 harm fully closed by Layers 1+2.
- **`planning-index.js` (7th RMW wrapper) unseamed** — out of `RMW_PATHS`, covered indirectly by the FR5a `applyMigrate` test; justified plan/impl divergence.
- **B27** (`needs-triage`) — archive-doc inline-link migrate-abort; out of scope, awaits a design call.

## Anti-rationalization at exit (strict)
- "Tests pass so it works" → checked the tests *physically exist and map to ACs*, not just the pass count; confirmed the two discriminators fail on wrong impls.
- "Edge case won't happen" → every AC has an owning test, including the tier matrix (SKETCH/SPIKE/FEATURE/FULL) and the multiset corner.
- "Close enough" → the one AC that couldn't be honestly met (AC2.4) was struck with a ratified rationale, not fudged green.

## Last Updated
2026-07-21 (VERIFY — PASS)
