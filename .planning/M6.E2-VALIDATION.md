# M6.E2 — VALIDATION

**8-dimension plan validation + Nyquist test-coverage mapping.** `plan_validation_dims: all`,
`nyquist_enforcement: strict` (strictness — proof-of-fail-before-pass — is enforced at VERIFY, not
here). Coverage re-derived with `diffRequirementCoverage`, not asserted: **26 of 26 criteria
attributed to a slice, 0 missing, 0 unattributable groups.**

## 8 dimensions

| # | Dimension | Verdict | Basis |
|---|---|---|---|
| 1 | Goal alignment | **pass** | Every slice serves "derived-and-compared, or excluded with a reason." S4 (reach disclosure) is the one that could read as decoration — it is not: `D-M6E2-1` makes disclosure the obligation the scope decision created. |
| 2 | Completeness | **pass, derived** | 26/26 criteria mapped, by tool. The first draft failed this at 19/22 and was corrected. |
| 3 | Dependency correctness | **pass** | S1→S3→{S4,S5}; S2 parallel. S2 touches `add.js` + `bugs-tally.js` read paths; S1 touches a new file + two call sites. No shared file. |
| 4 | Testability | **pass** | Every AC maps to a named test type below. |
| 5 | Scope discipline | **pass, with one flag** | The derived corpus sweep is excluded on the record (`D-M6E2-2`). ⚠ **S3 is the gold-plating risk**: four checks, three of which reach one project. That is a deliberate, recorded call (`D-M6E2-1`, Brett), not drift — but it is the part a reviewer should challenge. |
| 6 | Context feasibility | **pass** | Largest slice is S3; it is four independent checks against a pattern S1 proves, splittable per check. |
| 7 | Risk coverage | **partial — stated** | Covered: harness generality (S1 wave 1), FR1's wrong premise (found at PLAN), false-positive risk in `AC3.2` (ships as suspicion, heal category 3), briefing noise (`AC5.3`). **Not covered:** whether `AC3.2`'s heuristic is precise enough — untested, scheduled behind one real instance (`B102`), which is a sample of one. |
| 8 | Vertical slicing | **pass** | S1 ships a working check end to end; S2 ships a working write path. Neither is a layer. S4 is the thinnest — it is a shaping pass over S1+S3's output and cannot ship alone; recorded rather than pretended otherwise. |

## Nyquist mapping — every acceptance criterion to a test

| AC | Test type | Note |
|---|---|---|
| AC1.1 | unit | insert above the tally line, anchored on `readPublishedTally().lineNumber` |
| AC1.2 | unit + round-trip | after capture, `compareBugTally(after).ok === true` — the write path asserted *through the check* |
| AC1.3 | unit | fixture with no tally keeps `insertAtEnd` |
| AC1.4 | unit | stranded-entries announce; fixture is Signal's own current `BUGS.md` shape |
| AC1.5 | cross-document | reads `commands/add.md` against the code's behaviour |
| AC2.1 | unit | registry is exported from `published-facts.js`, frozen, and not present in `STATE_DRIFT_CHECKS` |
| AC2.2 | unit | check is built by the imported `defineCheck`; a definition missing a heal category throws |
| AC2.3 | integration | `runDriftChecks` with the composed registry; plus a call-site test that both commands compose it |
| AC2.4 | integration | a non-test caller of `compareBugTally` exists — asserted by grep-shaped test, since "has a caller" is the property |
| AC3.1 | unit + proof-of-fail | historical fixtures from `bugs-tally.test.js` must report findings |
| AC3.2 | unit + proof-of-fail | `B102`'s real row is the fixture; must report a **suspicion**, and must NOT assert a verdict |
| AC3.3 | unit | fixture from a corpus shape, not Signal's CHANGELOG (the only check with reach) |
| AC3.4 | unit | milestone row vs `STATE.md`; `M6.E1`'s stale row is the historical fixture |
| AC3.5 | unit | `facts.md` figure vs artifact |
| AC4.1 | unit | each check's `describe` carries a reach figure |
| AC4.2 | unit | the three Signal-only checks are identifiable in output |
| AC4.3 | unit | reach is derived from the measurement tool's shape; a hardcoded literal fails the test |
| AC5.1 | unit | all four `STATUS` values reachable per check |
| AC5.2 | integration | on a fixture project with no `BUGS.md`, the report says *could not check*, not nothing |
| AC5.3 | integration | not-applicable checks render summarised, not one line each |
| AC6.1 | manual, recorded | the check finds `B102`; the run is recorded, then the row is corrected |
| AC6.2 | cross-document | the `[Unreleased]` defect appears in one home; the other cross-references |
| NFR1 | integration | network-call audit (existing `tools/audit-network-calls.js`) |
| NFR2 | unit | a check that throws degrades to `cannot-evaluate` and the briefing still renders (`B70`) |
| NFR3 | unit | `package.json` dependency count unchanged |
| NFR4 | unit | no prompt is added to the capture path |

## What this validation could not establish

- **That the tests will be written RED-first.** Nyquist strictness is a VERIFY-time property; this
  document maps criteria to test *types*, and mapping is not proof of discipline.
- **`AC6.1` is a manual step.** It is recorded as manual rather than dressed as automated — the
  check finding one real instance is evidence it fires, not evidence it is precise.
- **No independent reading.** Research and validation both ran single-context (`RESEARCH` method
  note). REVIEW is the only place a second perspective enters.
