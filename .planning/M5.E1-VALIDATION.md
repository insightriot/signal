# M5.E1 — Doc-runtime & memory hygiene — Validation

**Phase:** PLAN gate. **Tier:** FULL (`plan_validation_dims: all`, `nyquist_enforcement: strict`).
**Validates:** `M5.E1-PLAN.md` against `M5.E1-REQUIREMENTS.md`.

## Part A — 8-dimension plan validation

| # | Dimension | Verdict | Note |
|---|---|---|---|
| 1 | **Goal alignment** | PASS | Every slice serves "ship the eviction model + mechanics + dogfood": S1 foundation, S2/S3/S4 mechanics, S5 proof. No orphan tasks. |
| 2 | **Completeness** | PASS-with-note | All E1 FRs covered (FR1, FR2a/b/c/d, FR3). FR4/FR5→E2, FR6/FR7→E3, and the **acute vector-1 (frontmatter-prose) remediation→E3** are deferrals recorded in-plan, not gaps. The 3-vector table makes the boundary explicit. |
| 3 | **Dependency correctness** | PASS | DAG S1→{S2,S3,S4}→S5 is accurate and minimal; S2/S3/S4 are genuinely independent after S1 (different files: state.js migration/size vs body-helper vs drain.js). |
| 4 | **Testability** | PASS | Every AC maps to a concrete test (Part B). The faithfulness gate (S3.t3) is testable via lossy-vs-golden fixtures — the risk that most needed a test has one. |
| 5 | **Scope discipline** | PASS | Anti-scope section fences E2/E3 + the v0.1.6 no-rebuild list. Go-big is bounded to these 5 slices; no gold-plating (tiered-index *build*, graph, OKF all deferred). |
| 6 | **Context feasibility** | PASS-with-note | S1/S2/S4/S5 fit one agent each. **S3 is the watch item** — 5 tasks spanning distill (judgment) + verify + mechanical move. If an executor can't hold it, split S3 into S3a (helper+distill+gate) / S3b (move+wiring) at EXECUTE. Flagged, not blocking. |
| 7 | **Risk coverage** | PASS | R1→S3 ordered gate + coverage backstop; R2→3-vector routing (acute→E3); R3→scope-to-`current_epic` + carry-over lift; R4→S4 ledger-first atomicity + terminal-only predicate + dangling-fence exclude; R5→S5 discrete/snapshot/no-mid-transition; R6→noted, FR7 deferred to E3 with fail-open read. |
| 8 | **Vertical slicing** | PASS-with-note | S2/S3/S4/S5 are vertical (each a full mechanism + its tests + its wiring). **S1 is an enabling/foundation slice** (model doc + skeleton) — horizontal by nature but legitimate: it's the canonical decision the others reference, and it ships a real artifact (`references/doc-runtime-model.md`). Accepted per FR1-is-critical-path. |

**Gate verdict: PASS** (3 notes, none blocking). The two watch items for EXECUTE: possible S3 split; S1's foundation nature means S2–S4 shouldn't start until S1's destination rule + gate spec are locked.

## Part B — Nyquist test-coverage mapping (every AC → test type + home)

| Slice | AC | Test type | Test home |
|---|---|---|---|
| S1 | initState body = skeleton headings | Unit | `tests/state-schema.test.js` (initState block) |
| S1 | model doc covers axes/destination/gate/3-vector | Doc-lint (section presence) | new `tests/doc-runtime-model.test.js` or validator check |
| S1 | INDEX hand-curation unchanged | Assertion (no INDEX write in E1) | manual + validator (no regression) |
| S2 | legacy migration → pointer + STATE-HISTORY, frontmatter byte-identical | Unit | `tests/state-schema.test.js` (`upgradeStateFile`) |
| S2 | re-run migration = no-op (idempotency) | Unit | `tests/state-schema.test.js` |
| S2 | tier→threshold applied; flat fallback | Unit | `tests/state-size.test.js` |
| S2 | stale message replaced | Unit (string assertion) | `tests/state-size.test.js` |
| S3 | closed Epic narrative → pointer + archived card | Integration | new `tests/evict-on-close.test.js` |
| S3 | in-flight Epic block byte-identical | Integration (negative) | `tests/evict-on-close.test.js` |
| S3 | lossy card FAILS coverage gate; golden PASSES | Unit | `tests/evict-on-close.test.js` (gate fn) |
| S3 | carry-over lifted to live section | Integration | `tests/evict-on-close.test.js` |
| S3 | original byte-identical in archive (zero-loss) | Unit (word-accounting) | `tests/evict-on-close.test.js` |
| S4 | shipped/promoted entry leaves inbox → ledger; no re-surface | Integration | `tests/drain.test.js` |
| S4 | Deferred entry survives inbox | Unit (predicate) | `tests/drain.test.js` |
| S4 | dangling-fence → scoped no-op + warning, no corruption | Unit | `tests/drain.test.js` |
| S4 | crash between ledger+inbox → no dupe/loss on re-run | Unit (renameFn stub) | `tests/drain.test.js` |
| S4 | non-target blocks byte-identical | Snapshot | `tests/drain.test.js` |
| S5 | Signal STATE body shrinks; STATE-HISTORY holds body | Integration (dogfood assertion) | `tests/dogfood-orientation.test.js` + manual |
| S5 | `/sig:resume`+`/sig:status` orientation unchanged pre/post | Integration | `tests/dogfood-orientation.test.js` |
| S5 | link-health green | Integration | link-check step |

**Nyquist coverage: complete.** Every AC has ≥1 mapped test; no acceptance criterion is unmapped. Strict enforcement (proof-of-fail-before-pass) is applied in VERIFY, per the tier contract.

**Highest-signal negative tests** (the ones that catch the real failure modes): S3 lossy-card-fails-gate (R1), S3 in-flight-block-untouched (R3), S4 crash-injection-no-dupe (R4), S4 Deferred-survives (R4 wrong-predicate), S2 frontmatter-byte-identical (R2 round-trip).

## Last Updated
2026-07-16 — 8-dim PASS (3 non-blocking notes); Nyquist mapping complete.
