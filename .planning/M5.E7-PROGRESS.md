# M5.E7 — Execution Progress

**Epic:** M5.E7 — the v2 direction audit (BR-8) · **Tier:** FULL (Epic-scoped, ceremony off)
**Plan:** `M5.E7-PLAN.md` · 11 tasks / 4 slices

**Dispatch policy (dogfooding demand item #29, "executor cost calibration"):** delegate the
large / isolated / independence-requiring tasks; the orchestrator does mechanical and
judgment-synthesis tasks directly to avoid round-trip latency. Recorded because the corpus
explicitly asks for this tradeoff to be *stated* rather than assumed.

---

## Wave 1 — demand ∥ supply *(parallel is an integrity requirement, AC2.4)*

| Task | Mode | Status | Commit |
|---|---|---|---|
| **S1.t1** Harvest retro corpus | direct | ✅ **done** — 39/39 items, matched estimate exactly | `67fb032` |
| **S1.t2** Harvest work ledgers | delegated | ✅ **done** — 8% inbox cap, recorded; 0 true intra-file dupes | `8a79f4b` + `1d057d7` |
| **S2.t4** superpowers deep verify | delegated | ✅ **done** — `<HARD-GATE>` has no mechanism; breaker is the port | `3c5d8d3` |
| **S2.t6a** compound-engineering re-map | delegated | ✅ **done** — architecture re-mapped | `136e598` |

**Wave 1 complete (4/4).** All artifacts verified on disk + in git independently of agent
self-reports; each commit touches exactly one file; working tree clean; no work lost.

## Wave 2

| Task | Mode | Status | Commit |
|---|---|---|---|
| **S2.t5** pm-skills, then gstack | delegated | ✅ **done** — overlap verdict is 3-part; Signal found *ahead* on performance | `fe5f6ef` `5662216` (+2 refinements) |
| **S2.t6b** compound: 3 new capabilities | delegated | ✅ **done** — cross-model review deflated; no measured benefit | `4e135d3` `8236b09` |
| **S1.t3** Dedupe · cluster · rank | direct | ✅ **done** — 19 net-new signals / 11 clusters; retro corpus is 67% net-new | `3289e49` |
| **S4.t10** Correct the record | direct | ✅ **done** — 10 dated markers, 6 IDs, at source in `analysis/` | `e2ba186` |

**🟢 SLICE 2 (SUPPLY) COMPLETE — all four repos verified at depth.**
Sequential dispatch of t5 → t6b produced **no `--amend` race**, confirming D-M5E7-9b's fix works.

**🟢 SLICE 1 (DEMAND) COMPLETE — `M5.E7-DEMAND-REGISTER.md`.**
**🟢 WAVE 2 COMPLETE (4/4). 8 of 11 tasks done.** Suite 1623/1623, validator green.

## Wave 3

| Task | Mode | Status |
|---|---|---|
| **S3.t7** Counterfactual pass | delegated *(independence IS the mitigation)* | ✅ **done** — `M5.E7-COUNTERFACTUAL.md`; **both flattering theses falsified** | `c8a52b3` `9fcc74b` `b7befa8` |
| **S3.t8** Disposition every candidate | direct | ✅ **done** — `M5.E7-DISPOSITIONS.md`; 41 rows / 41 verbs; **9 build · 20 continue · 15 abandon** | `7cab863` |

**🟢 SLICE 3 COMPLETE. 10 of 11 tasks.** Only S4.t9 (the roadmap) + S4.t11 (land it) remain.

**20. The dispositions, in one line each.** All four **COMPOUND** items **abandoned** — S3.t7
falsified the memory thesis, so the group the reframe most wanted to keep is the group it cut.
gstack's `/cso` replacement **abandoned** on threat-model fit (Phase 8 carved out as a `continue`).
`<HARD-GATE>` **abandoned** — nothing to port (C3); the real five-round breaker carved out as
**15b**. pm-skills **GTM abandoned** on fit — the cut the seed most needed and never made. **Group A
(upstream) deliberately NOT cut:** the counterfactual question is structurally blind to upstream
capability, so every "no" there carries near-zero information — parked on a 3-condition trigger and
routed to Brett as a **positioning** call.

**21. 7 of the 9 `build`s are Signal-native, and none is a straight port.** AC3.4's column was
predicted to be *"productive, not a remainder bucket"* — it is now the largest `build` column in the
table. **This is also exactly the shape a confirmation-biased audit would produce** (D7's High,
structural risk). `M5.E7-DISPOSITIONS.md` §6 carries an explicit self-attack with a **stated
falsifier**: *if N2 lands, measurement shows Signal's enforcement is as good as claimed, and the
ports still never happen — the reframe was decorative.* S4.t9 must carry that falsifier forward, not
drop it.

**22. Two mechanical checks were wrong on first pass and only re-derivation caught them** — the row
count (38 → **41**, hand-count error) and AC2.3, whose grep found *"factually stale"* sitting in item
24's narrative. Both corrected in place and recorded. **Third and fourth instances this Epic of a
number being wrong until re-derived** (after the register's two). That is now four data points for
**N10** (SHIP-time ledger-reconcile) and for retro #4's claimed-facts ledger — the pattern is no
longer anecdotal.

**Verified per D-M5E7-9c, not taken on self-report** — and the check earned its keep twice. The
agent **went idle without reporting back** (the PLAN-phase failure mode recurring, 3rd instance);
the artifact was nonetheless on disk at 35 KB with three commits landed. Content then verified by
reading: AC7.1 four verdicts ✅, AC7.2 both theses attacked ✅, AC7.3 caveat present ✅, and the
information barrier **held** — the register was read only at §6, after §§1–5 were written.

**⚠ It corrected the orchestrator, not the other way round.** The dispatch brief asserted the
v0.1.9 `evict.js` false-green "shipped." **It did not** — M5.E4's own REVIEW panel caught it
in-phase. Only **`B19`** shipped (green and inert for a full release). The counterfactual verified
this at source, refused to inherit the brief's framing, and flagged the disagreement rather than
silently reconciling it. **The error had propagated into the committed demand register** (cluster B's
headline evidence + the rank-2 row); corrected 2026-07-26 in both, and in finding #10 above.

## Wave 4

| Task | Mode | Status |
|---|---|---|
| **S4.t9** Write `analysis/SIGNAL-V2-ROADMAP.md` | direct | ✅ **done** — 5 Epics (M5.E8–E12), leads with the measurement finding | `c6aa91c` |
| **S4.t11** Supersede · reconcile · land in BACKLOG | direct | ✅ **done** — 12 work items landed with triggers + first slices | `0e7486b` |

**🟢 ALL 11 TASKS COMPLETE. EXECUTE closed.** Suite 1623/1623, validator green, **zero dead links**
across `analysis/` and `.planning/` (the latter checked by hand — the hygiene guard deliberately
skips that directory, which is **B40**). INDEX regenerated, 174 docs.

> **Two carried constraints on S4.t9, ratified D-M5E7-10 (2026-07-26).** Bugs `B32`–`B40` defer to a
> bug-squash Epic; M5.E7 does not fix them. In exchange: **(1)** that Epic must be a **named item in
> the roadmap's sequence with a real ID and trigger** — verified 2026-07-26 that **no bug-squash
> sprint exists in `BACKLOG.md`**, so nine open bugs currently have no home; deferring into an
> unnamed container repeats the silence pattern this Epic catalogued. **(2)** the roadmap must
> **state the `B39` caveat wherever it proposes a trigger** — P4 requires triggers, and `B39`
> establishes the trigger substrate has never run. Carry `B39` as a `build` candidate in its own
> right: it is the item that makes every other trigger real.

---

## Findings so far — carry these into S1.t3 / S3.t7 / S3.t8

**1. Every capability that sounded like a must-port deflated on close inspection. Three for three.**
- `<HARD-GATE>` — prose in **one** skill; 4 grep hits repo-wide, **zero** in `hooks/`, zero in
  `tests/`, no parser, no validator. Its own author calls the mechanism unsettled. The real
  machinery is `subagent-driven-development`'s five-round breaker (1,063 lines, shipped 2026-07-23,
  sized **L**). **Signal has been planning to port a syntax instead of a mechanism.**
- pm-skills' review depth — *"548 lines of prompt with zero enforcement machinery. Nothing
  validates that the model actually ran the self-refute stage… The discipline is instruction, not
  mechanism."*
- compound's cross-model adversarial review — **adds no new detection path.** The peer is
  adversarial-only, gets the *same* persona brief, and is *mutually exclusive* with the in-process
  adversarial reviewer: it **replaces a lens, it does not add one**. Buys exactly two mechanical
  things (a one-notch confidence promotion on agreement; permission to skip a validator).
  **No measured cross-model-vs-same-model comparison exists** — the repo's one rigorous eval (150
  runs, blind judge) varies models *within* the peer slot and states independence is "not a
  variable here." The repo records the correlated-blind-spot caveat against itself.

→ Strengthens **D-M5E7-7**. Points most ports toward **`continue` (spike first)** rather than
`build`: the sources' advertised capabilities don't survive contact, so committing build capacity on
reputation would repeat the 0-for-6.

**2. HYPOTHESIS, NOT CONCLUSION — the sources have method depth; Signal has enforcement machinery.**
Signal's is real (exit-2 write guards, hooks, suite-enforced gates, a retro gate that hard-blocked
its own SHIP). If it holds, the roadmap's shape changes: **port methods into Signal's existing
machinery rather than porting whole commands.** ⚠ This flatters Signal, which is precisely the
profile **AC7.2** says to attack rather than confirm — same treatment as the memory-loop thesis.
S3.t7 must try to falsify it.

**3. First place Signal is AHEAD of a source.** On performance, the stance is *inverted*: Signal's
`performance-optimization` is measurement-first and refuses to optimize without evidence;
pm-skills' `/performance-audit-static` **never measures**. On security it reverses — pm-skills is
deeper in method, Signal better integrated into a phase. A coverage scorecard structurally cannot
see this column (it counts only what's absent) — direct evidence for D-M5E7-1's reframe.

**4. `/derive-tests` is an axis available from NEITHER alternative.** gstack has no
test-derivation; `/cso` is read-only and yields findings, not coverage maps.

**5. gstack's `sections/manifest.json` lazy-loading is worth evaluating independently of `/cso`'s
content.** Signal's stated #1 risk is skill token cost ("never load all 21 at once") and gstack
independently solved exactly that for its largest skill.

**6. Theme F verified genuinely net-new** — narrow pattern (`delegat|executor cost`) returns
**0 hits** across BACKLOG, BUGS, and the inbox. The nearest-looking BACKLOG item
(`Concurrency-lock the doc-runtime RMW paths`) concerns **file locking** and is already **DONE** —
a different layer. So the gap stands on four legs: raised in three consecutive Epics, absent from
all 38 tracked BACKLOG subjects, **reproduced live during this Epic**, and with a named upstream
supply (`using-git-worktrees`, one of four superpowers skills with no Signal analog).

**7. Corrections now number FIVE** (C1, C2, C6, C7 mine; C3, C4, C5, C8 in `analysis/`) — see
`M5.E7-RESEARCH.md` §0. **C8 is new and the widest-reaching:** the prohibition/anti-rationalization
table form is measurably counterproductive for output-*shaping* failures, while Signal adopted it
universally per April's *"every phase gate uses this format."* Filed as **B38**.

**8. Out-of-band, closed during this Epic:** the corpus's only dated trigger (synthesizer
validator-side check, 2026-08-23) was checked at Brett's direction — **zero regressions, and the
zero is informative** (a dedicated `tests/synthesizer-regression.test.js` guard has run green
throughout). Closed **by decision**, not left to lapse. Two more bugs catalogued: **B37** (D16
dual-write violates the doc model's single-home rule), **B38** (above).

---

## Notes

- **S1.t1 result:** 39 items / ~1,510 words, exactly the PLAN estimate. Zero sourced from the 4
  stub retros (AC1.5). Standout preliminary gap: **Theme F** (EXECUTE dispatch guidance) — three
  consecutive Epics, zero ledger coverage.
- **Auto-state-protocol verified live:** `setCurrentTask` → `clearCurrentTask({status:'done'})`
  round-tripped correctly at FULL/strict on t1.
- **Open process risk:** during PLAN, all four research agents completed and went idle **without
  delivering**; each needed an explicit re-request. Applying the mitigation here — every delegated
  task is confirmed landed (file on disk + commit sha) before its row is marked done. Never mark a
  task done on an agent's self-report alone (demand item #17). **It paid off immediately:** t4
  reported complete while its file was still untracked.

- **⚠ CONCURRENT-DISPATCH RACE REPRODUCED LIVE — this Epic hit the exact hazard it harvested.**
  Demand item **#32** (M5.E5 retro) says: *"EXECUTE guidance should distinguish 'logically
  parallel' from 'safe to dispatch concurrently' … every executor runs the full suite + commits
  against a shared index, so concurrent dispatch races."* Three agents were dispatched
  concurrently against one worktree. **Two of them silently absorbed a sibling's file** via
  `git commit --amend`, which targets whatever HEAD is at that instant rather than the agent's own
  commit; a `git reset` was needed to unpick it. Reflog trail: `545e685` → amended twice → `6b5db56`
  → reset to `136e598` → `1d057d7` → amended into a sibling's commit → reset → `3c5d8d3`.
  **Verified independently: no work lost**, each final commit holds exactly one agent's file.
  **Two mitigations, both cheap and known:** (1) `isolation: "worktree"` per dispatched agent;
  (2) an executor rule — `git add <path> && git commit`, **never** `git commit --amend`, on a
  shared worktree. *Second-order significance: item #32 is no longer merely un-tracked, it is
  actively costing — which strengthens Theme F's case in S1.t3 beyond "raised three times."*

---

---

## Findings added by Wave 2's second half

**9. The demand asymmetry is the Slice-1 result.** The inbox subtracts **85%** against `BACKLOG.md`
(33 of 39 already tracked, shipped, closed, or excluded) — AC1.2a's premise confirmed by
measurement. The retro corpus subtracts only **33%**, and **zero of the 20 M5-era retro items has a
BACKLOG home**. Signal's newest recorded demand is its least-tracked. D-M5E7-3's bet on the retro
corpus as primary evidence base is now measured, not assumed.

**10. Two clusters are backed by something that already went wrong; nine are not.** **F** (EXECUTE
dispatch guidance — a live incident *this Epic*, 0-hit across all four ledgers) and **B**
(false-green/test-strength — ~~two guards shipped as false-greens~~ **corrected 2026-07-26 by
S3.t7: only `B19` shipped** — green and inert for a full release, found by M5.E4 PLAN research;
`B14` was caught in-phase by M5.E4's own REVIEW panel. The honest framing is an **asymmetry** — the
panel catches its own Epic's in-flight work and missed the one guard that reached users). Everything
from rank 3 down rests on "four documents asked for it," which is weaker evidence and is labelled as
weaker. **Ranking deliberately avoids frequency** — AC1.3 establishes it is recorded nowhere; the
corpus's only two recorded recurrences are `B13` and `B6`.

**11. The plan's one worked example of an intra-file duplicate was not a duplicate.**
`#13 ≡ #27` is a `/sig:audit` **name collision** that BR-1 deliberately **split into two commands**.
AC1.2a's third subtraction therefore ran with an **empty input**. Surfaced as a PLAN deviation, not
skipped — the pattern retro item #9 asks for.

**12. The subtraction authority was itself stale.** `BACKLOG.md` (stamp 2026-07-19) carries **four
unmarked closures** shipped by M5.E6 on 2026-07-25 — `/sig:sweep` (its row still reads *"Confirmed
not yet built"*), CLAUDE.md de-bloat + command-frontmatter freshness (both live in `sweep.js`), and
`docs/map` Stage 1 (`ship.md:65`). Live-open is **33 of 38**, not 38. Routed to S4.t11.

**13. Corrections resolved to SIX marked IDs, and the PROGRESS count of "FIVE" was ambiguous.**
`analysis/`-side corrections are C3, C4, C5, C6, C8 (five, as finding 7 said) — the parenthetical
grouping in that finding mislabeled C6 as `.planning/`-side. **C7 was also marked** at the site
carrying it, giving 10 markers over 6 IDs. Two further record errors found while placing them:
**C8 had never been written into `M5.E7-RESEARCH.md` §0** despite `B38` citing it there — **fixed
here** (C8 is now a §0b row), because t10's own markers pointed at that address; and **`B38`
attributes the C8 claim to `REPO-ANALYSIS.md` when it lives in
`SIGNAL-INTEGRATION-RUNDOWN.md:186`** — routed to S4.t11.

**15. "Tracked in BACKLOG" is two different answers, and S3.t8 needs them separated.** Of the 23
tracked inbox entries, **18 sit on a sprint item** and **5 sit only on a parked trigger-gated
bullet**. Three of those parked gates cannot fire for the demand landing on them: the
version-decision rubric is bundled behind *"a platform tester volunteers"* (it needs no tester); the
map drift-guard sits behind *"the deeper refresh protocol"* while **`B32` is open precisely because
the guard doesn't exist**; and GitHub Issues sits behind a trigger that **already fired 2026-07-15**
and promoted nothing. **Treat these three as untracked.** This is P4's warning surfacing on the
coverage side — a trigger nobody evaluates reads as tracked while behaving as shelved.

**16. Root cause of #15 found, and it is mechanical — `B39` (`confirmed`, P2).** The trigger
watchlist has **never been walked, because `/sig:plan` was never taught to walk it.**
`ISSUES-INBOX.md:1407` instructs *"at each `/sig:plan` drain, walk the conditions below"*;
`grep -ril "watchlist" commands/ tools/` returns **nothing**, and `commands/plan.md` mentions `drain`
11 times and the watchlist zero. A mechanism ratified 2026-07-04 (BR-6) to fix *"10+ parked entries
carry promote-back conditions that nothing evaluated"* has itself evaluated nothing. **This directly
threatens S4.t9:** every roadmap `build` item carries a trigger per P4 — adding them to a watchlist
nothing walks reproduces the shelf-ware risk AC5.6 exists to close. **`B40`** also cataloged
(citation integrity in `analysis/` unenforceable by construction — finding #13's near miss, with the
mechanism gap separated from the content fix).

**14. The register miscounted itself on the first pass, and says so.** Initial totals (20 tracked
inbox entries, 21 net-new retro items) were wrong; recounting from the enumerated citation lists
gave 23 and 26. Recorded in `§4` rather than quietly fixed — it is a live instance of the
claimed-facts-ledger discipline retro item #4 asks for, in the one document that could least afford
to demonstrate the opposite.

**17. S3.t7 falsified BOTH flattering theses. This is the Epic's most important result.**

**Verdicts (AC7.1):** gstack **no** · pm-skills **yes, narrowly** (`intended-vs-implemented` →
B37/B39/B40 — a class the register does not carry) · superpowers **yes, narrowly** (M4.5 exit
criterion; M5.E4's ~36 min/task) · compound-engineering **yes on technique 5** of the adversarial
persona brief (cites **B19**), **no on cross-model** — structurally, because the peer gets the
*same* persona brief, is mutually exclusive with the in-process reviewer, and buys only a
confidence promotion plus permission to skip a validator. **It replaces a lens; it does not add one.**

**Thesis 1 — "fix the memory loop before porting anything" (AC7.2): broad form FALSIFIED.** The
three carry-over chains, read *with their dates and Epic IDs attached*, say the opposite of what
they are cited for — in every case **the knowledge was in-context at the moment of the miss**
(B27 surfaced *while building B24's own fixture*; B34 found by the same REVIEW panel as B29's fix;
B30 found dogfooding B26 on M5.E5's own SHIP). **Nobody forgot anything.** A cross-session store
with confidence decay contributes nothing to a same-session failure. The actual gap is
**class-completeness at fix time** — a review-scope rule Signal already wrote down
(`M5.E6-RETROSPECTIVE.md:32`). Survives only as: *B39's watchlist walk is specified-but-unbuilt and
cheap* — **a Signal bug fix, not a port.**

**Thesis 2 — "port methods into Signal's machinery": DOES NOT SURVIVE INTACT.** Two blows.
(a) The machinery closest to *"gate a phase on a judgment call"* — the shape every ported method
needs — is the FR1 retro gate, which has **the worst reliability record in the repo** (B26 → B30 →
B36, three residuals across three Epics, B36 open today), plus B39, ratified and never implemented.
(b) **The sharpest blow is measurement, not opinion:** `ce-retune`'s A/A noise floor — *"12 runs
across two identical builds gave workflow adherence 7 of 12 and output tokens from 21,872 to
155,682, a **7.12× spread on identical code**."* If run-to-run variance on a byte-identical prompt
corpus is that large, **"port the method into Signal's machinery" is an unmeasurable intervention
for Signal today.** Signal's 1,623 tests exercise `tools/`; **Signal has no test anywhere that
asserts a prompt instruction was obeyed** (verified by grep). Both gstack and superpowers ship
exactly that harness. *What survives:* for genuinely deterministic, file-shaped candidates the move
is right — `intended-vs-implemented` bound to a doc-hygiene-style check, and gstack's
`maxSkeletonBytes` assertion, are in that class.

**Independent convergence worth more than agreement:** the counterfactual reached the register's
rank-2 cluster (**B, false-green/test-strength**) from the *supply* side via technique 5, with a
deliberate information barrier between the two passes. Two different inputs, same target.

---

## Last Updated

**23. The Epic's own enforcement worked, and it is worth naming.** AC5.6 existed because Signal's
record on unenforced docs is damning — `MILESTONE-5.md`'s **25 port checkboxes have never had one
checked**; `M4.5.E5-LAUNCH-KIT.md` §3 sits entirely unchecked *while v0.1.4 demonstrably shipped*.
S4.t11 therefore landed **12 work items into `BACKLOG.md`** rather than leaving them in `analysis/`.
**Those 25 checkboxes were deliberately left unchecked and unedited** — they are the audit's own
evidence, and tidying them would erase it. **Residual risk, stated:** `BACKLOG.md` is live and
hygiene-touched, but **nothing forces the roadmap itself to be consulted**, and **`B39` means every
trigger landed today is a note rather than an enforcement until M5.E9 fixes it.** That caveat is
stamped at the top of the BACKLOG section, not buried here.

**24. Fifth miscount, and the pattern is now the finding.** The `build` total was written as 14;
it is **11 distinct** — item 28's worktree half **is** N5, and N13a and N3 are one build. Five
counts wrong-until-re-derived in a single Epic (register ×2, dispositions ×2, this one). **Every
one was caught by re-deriving, never by reading.** That is the strongest evidence the audit produced
for its own **N10** (SHIP-time ledger reconcile) and for retro item #4's claimed-facts ledger — and
it was produced *by the audit failing at exactly the thing it was recommending*.

---

## Last Updated

2026-07-26 — **M5.E7 EXECUTE COMPLETE — all 11 tasks, all 4 slices.**
Suite 1623/1623, validator green, working tree clean, INDEX regenerated (174 docs).

**Deliverable:** `analysis/SIGNAL-V2-ROADMAP.md` — 45 candidates verbed (**11 build · 16 continue ·
19 abandon**), sequenced **M5.E8–M5.E12**, landed in `BACKLOG.md` with triggers and first slices.
**No code shipped, by design.**

**NEXT: VERIFY** — `nyquist_enforcement: off`, so every AC is checked by reading or by a mechanical
check per `M5.E7-VALIDATION.md` §2 (30 ACs + P1–P8; 22 mechanically checkable). Then **REVIEW**
(`review_depth: quality-only`, repurposed per **AC7.4**: the panel attacks the **reasoning**, not
the prose — this Epic ships no code, so a code-quality review would verify nothing). **AC7.4 is
where this Epic fails if it fails** — `M5.E7-VALIDATION.md` D4 says so plainly: the mechanical
checks can confirm every candidate *got* a verb; **nothing in the plan can confirm the verb was
right.** Point REVIEW at §6 of the roadmap and §6 of the dispositions — the self-attack and its
falsifier are the parts most worth trying to break.

<!-- superseded stamp below retained for provenance -->

2026-07-26 — **Waves 1–3 complete (10 of 11 tasks). Slices 1, 2, 3 all closed.**
Suite 1623/1623, validator green, working tree clean.

**NEXT: S4.t9 — write `analysis/SIGNAL-V2-ROADMAP.md` (direct).** Everything it needs now exists:
demand register, supply menu, counterfactual, disposition table, corrections. **Its binding
constraints, consolidated:**
- **Lead with the measurement finding** (D-M5E7-12) — it is the spine, not a rank.
- **P1** ✅ already satisfied (15 abandons) · **P2** every claim cites a path or is labelled
  `(recollection — no artifact)` · **P3** every `build` names a first slice + stranger-verifiable
  done-when · **P4** every item carries a **trigger**, not a position · **P6** state the real
  evidence size (**12 retro files / ~1,510 words**, not the advertised 16) · **P7** a *"what this
  roadmap gets wrong"* section · **P8** the sequenced queue fits **one page**.
- **AC4.3** — real Epic IDs from **M5.E8** onward; the pre-override `M5.E1–E6` port labels are dead.
- **AC7.3** — the ideation-silence caveat appears **verbatim** in the body.
- **D-M5E7-10** — the bug-squash Epic is a **named item with an ID and trigger** (N8), and the
  **B39 caveat** appears wherever the roadmap proposes a trigger.
- **Carry §6's self-attack and its falsifier forward.** A roadmap whose builds are all
  Signal-native must say why that is not just the authors preferring their own work.

Then **S4.t11**: supersede the seed (partial — §1's frame dies, §2/§3 carry), point `MILESTONE-5.md`
at the roadmap, mark `SIGNAL-INTEGRATION-RUNDOWN.md` historical, land every `build` in `BACKLOG.md`
with its trigger (**AC5.6**), reconcile the four unmarked BACKLOG closures + `CLAUDE.md`'s stale
B27/B28 + B38's wrong file attribution, regenerate `/sig:index`, and run validate + suite.

**Carry into S3.t8:** P1 needs ≥1 `abandon` and the counterfactual supplies the evidence for it
(gstack: plain no; **compound-engineering's cross-model implementation**: structurally no). AC2.3
still holds — **no `abandon` may cite dormancy or staleness**; argue fit or overlap.

**Two Brett corrections at the Wave-3 checkpoint — `D-M5E7-11` and `D-M5E7-12`. Both bind S3.t8.**

**18. A capability was never dispositioned, and the reasoning that buried it has a hole.** Both S2.t6b
and S3.t7 answered *"should Signal port compound-engineering's cross-model review?"* (**no** — it
substitutes a reviewer, correct, stands). **Neither answered *"should Signal have a genuine
independent second opinion?"*** That fell between "port theirs" and "build our own" and got no verb.
**It goes to t8 as its own AC3.4 Signal-native row.** The hole: §2.4 justified *"adds nothing"* by
citing **B14** — the case the panel **won**, in-phase, on its own Epic's work — and skipped **B19**,
the case it **lost** (shipped green and inert a full release; found by PLAN research, not REVIEW).
Worse, §2.4's answer to B19 is **technique 5**, three lines of unenforced prompt, while §3.2 of the
same document establishes prompt interventions run at **7-of-12 adherence** and §5 concedes it is
unknowable whether it would have fired. **It answers the one failure that reached users with the one
class of mechanism it just proved unverifiable.**

**19. The measurement gap is the spine, not a rank — `S4.t9` leads with it.** Six separately-ranked
findings are one finding: **Signal cannot detect whether its own interventions work, in any
dimension.** No test asserts a prompt instruction was obeyed · 7-of-12 adherence / **7.12× output
spread on byte-identical code** · false-greens are the largest cluster (9 items) · **B39 ratified
2026-07-04, never built, unnoticed for three weeks** · the claimed-facts-ledger gap · **this
register miscounted itself twice.** It is the condition under which every other item can be judged
at all. **The telemetry bolt-on** is captured verbatim in `ISSUES-INBOX.md` (2026-07-26) — **single
home, not restated here** — and carries a hard ordering constraint: **local behavioral measurement
before cross-install aggregation**; backwards collects noise at scale. Mass-market palatability is
**explicitly waived** by Brett, so consent is a design parameter, not a blocker. **Named as scope
growth** (the D5 discipline); **M5.E7 ships no code** — candidate only.
