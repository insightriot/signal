# M5.E7 — Execution Progress

**Epic:** M5.E7 — the v2 direction audit (BR-8) · **Tier:** FULL (Epic-scoped, ceremony off)
**Plan:** `M5.E7-PLAN.md` · 11 tasks / 4 slices

**Dispatch policy (dogfooding demand item #29, "executor cost calibration"):** delegate the
large / isolated / independence-requiring tasks; the orchestrator does mechanical and
judgment-synthesis tasks directly to avoid round-trip latency. Recorded because the corpus
explicitly asks for this tradeoff to be *stated* rather than assumed.

---

## Wave 1 — demand ∥ supply *(parallel is an integrity requirement, AC2.4)*

| Task | Mode | Status | Commit |
|---|---|---|---|
| **S1.t1** Harvest retro corpus | direct | ✅ **done** — 39/39 items, matched estimate exactly | `67fb032` |
| **S1.t2** Harvest work ledgers | delegated | ✅ **done** — 8% inbox cap, recorded; 0 true intra-file dupes | `8a79f4b` + `1d057d7` |
| **S2.t4** superpowers deep verify | delegated | ✅ **done** — `<HARD-GATE>` has no mechanism; breaker is the port | `3c5d8d3` |
| **S2.t6a** compound-engineering re-map | delegated | ✅ **done** — architecture re-mapped | `136e598` |

**Wave 1 complete (4/4).** All artifacts verified on disk + in git independently of agent
self-reports; each commit touches exactly one file; working tree clean; no work lost.

## Wave 2

| Task | Mode | Status | Commit |
|---|---|---|---|
| **S2.t5** pm-skills, then gstack | delegated | ✅ **done** — overlap verdict is 3-part; Signal found *ahead* on performance | `fe5f6ef` `5662216` (+2 refinements) |
| **S2.t6b** compound: 3 new capabilities | delegated | ✅ **done** — cross-model review deflated; no measured benefit | `4e135d3` `8236b09` |
| **S1.t3** Dedupe · cluster · rank | direct | ✅ **done** — 19 net-new signals / 11 clusters; retro corpus is 67% net-new | `3289e49` |
| **S4.t10** Correct the record | direct | ✅ **done** — 10 dated markers, 6 IDs, at source in `analysis/` | `e2ba186` |

**🟢 SLICE 2 (SUPPLY) COMPLETE — all four repos verified at depth.**
Sequential dispatch of t5 → t6b produced **no `--amend` race**, confirming D-M5E7-9b's fix works.

**🟢 SLICE 1 (DEMAND) COMPLETE — `M5.E7-DEMAND-REGISTER.md`.**
**🟢 WAVE 2 COMPLETE (4/4). 8 of 11 tasks done.** Suite 1623/1623, validator green.

## Wave 3

| Task | Mode | Status |
|---|---|---|
| **S3.t7** Counterfactual pass | delegated *(independence IS the mitigation)* | 🟢 **unblocked — NEXT** (S1 ✅ + S2 ✅) |
| **S3.t8** Disposition every candidate | direct | ⏸ blocked on t7 |

## Wave 4

| Task | Mode | Status |
|---|---|---|
| **S4.t9** Write `analysis/SIGNAL-V2-ROADMAP.md` | direct | ⏸ blocked on t8 |
| **S4.t11** Supersede · reconcile · land in BACKLOG | direct | ⏸ blocked on t9 + t10 |

---

## Findings so far — carry these into S1.t3 / S3.t7 / S3.t8

**1. Every capability that sounded like a must-port deflated on close inspection. Three for three.**
- `<HARD-GATE>` — prose in **one** skill; 4 grep hits repo-wide, **zero** in `hooks/`, zero in
  `tests/`, no parser, no validator. Its own author calls the mechanism unsettled. The real
  machinery is `subagent-driven-development`'s five-round breaker (1,063 lines, shipped 2026-07-23,
  sized **L**). **Signal has been planning to port a syntax instead of a mechanism.**
- pm-skills' review depth — *"548 lines of prompt with zero enforcement machinery. Nothing
  validates that the model actually ran the self-refute stage… The discipline is instruction, not
  mechanism."*
- compound's cross-model adversarial review — **adds no new detection path.** The peer is
  adversarial-only, gets the *same* persona brief, and is *mutually exclusive* with the in-process
  adversarial reviewer: it **replaces a lens, it does not add one**. Buys exactly two mechanical
  things (a one-notch confidence promotion on agreement; permission to skip a validator).
  **No measured cross-model-vs-same-model comparison exists** — the repo's one rigorous eval (150
  runs, blind judge) varies models *within* the peer slot and states independence is "not a
  variable here." The repo records the correlated-blind-spot caveat against itself.

→ Strengthens **D-M5E7-7**. Points most ports toward **`continue` (spike first)** rather than
`build`: the sources' advertised capabilities don't survive contact, so committing build capacity on
reputation would repeat the 0-for-6.

**2. HYPOTHESIS, NOT CONCLUSION — the sources have method depth; Signal has enforcement machinery.**
Signal's is real (exit-2 write guards, hooks, suite-enforced gates, a retro gate that hard-blocked
its own SHIP). If it holds, the roadmap's shape changes: **port methods into Signal's existing
machinery rather than porting whole commands.** ⚠ This flatters Signal, which is precisely the
profile **AC7.2** says to attack rather than confirm — same treatment as the memory-loop thesis.
S3.t7 must try to falsify it.

**3. First place Signal is AHEAD of a source.** On performance, the stance is *inverted*: Signal's
`performance-optimization` is measurement-first and refuses to optimize without evidence;
pm-skills' `/performance-audit-static` **never measures**. On security it reverses — pm-skills is
deeper in method, Signal better integrated into a phase. A coverage scorecard structurally cannot
see this column (it counts only what's absent) — direct evidence for D-M5E7-1's reframe.

**4. `/derive-tests` is an axis available from NEITHER alternative.** gstack has no
test-derivation; `/cso` is read-only and yields findings, not coverage maps.

**5. gstack's `sections/manifest.json` lazy-loading is worth evaluating independently of `/cso`'s
content.** Signal's stated #1 risk is skill token cost ("never load all 21 at once") and gstack
independently solved exactly that for its largest skill.

**6. Theme F verified genuinely net-new** — narrow pattern (`delegat|executor cost`) returns
**0 hits** across BACKLOG, BUGS, and the inbox. The nearest-looking BACKLOG item
(`Concurrency-lock the doc-runtime RMW paths`) concerns **file locking** and is already **DONE** —
a different layer. So the gap stands on four legs: raised in three consecutive Epics, absent from
all 38 tracked BACKLOG subjects, **reproduced live during this Epic**, and with a named upstream
supply (`using-git-worktrees`, one of four superpowers skills with no Signal analog).

**7. Corrections now number FIVE** (C1, C2, C6, C7 mine; C3, C4, C5, C8 in `analysis/`) — see
`M5.E7-RESEARCH.md` §0. **C8 is new and the widest-reaching:** the prohibition/anti-rationalization
table form is measurably counterproductive for output-*shaping* failures, while Signal adopted it
universally per April's *"every phase gate uses this format."* Filed as **B38**.

**8. Out-of-band, closed during this Epic:** the corpus's only dated trigger (synthesizer
validator-side check, 2026-08-23) was checked at Brett's direction — **zero regressions, and the
zero is informative** (a dedicated `tests/synthesizer-regression.test.js` guard has run green
throughout). Closed **by decision**, not left to lapse. Two more bugs catalogued: **B37** (D16
dual-write violates the doc model's single-home rule), **B38** (above).

---

## Notes

- **S1.t1 result:** 39 items / ~1,510 words, exactly the PLAN estimate. Zero sourced from the 4
  stub retros (AC1.5). Standout preliminary gap: **Theme F** (EXECUTE dispatch guidance) — three
  consecutive Epics, zero ledger coverage.
- **Auto-state-protocol verified live:** `setCurrentTask` → `clearCurrentTask({status:'done'})`
  round-tripped correctly at FULL/strict on t1.
- **Open process risk:** during PLAN, all four research agents completed and went idle **without
  delivering**; each needed an explicit re-request. Applying the mitigation here — every delegated
  task is confirmed landed (file on disk + commit sha) before its row is marked done. Never mark a
  task done on an agent's self-report alone (demand item #17). **It paid off immediately:** t4
  reported complete while its file was still untracked.

- **⚠ CONCURRENT-DISPATCH RACE REPRODUCED LIVE — this Epic hit the exact hazard it harvested.**
  Demand item **#32** (M5.E5 retro) says: *"EXECUTE guidance should distinguish 'logically
  parallel' from 'safe to dispatch concurrently' … every executor runs the full suite + commits
  against a shared index, so concurrent dispatch races."* Three agents were dispatched
  concurrently against one worktree. **Two of them silently absorbed a sibling's file** via
  `git commit --amend`, which targets whatever HEAD is at that instant rather than the agent's own
  commit; a `git reset` was needed to unpick it. Reflog trail: `545e685` → amended twice → `6b5db56`
  → reset to `136e598` → `1d057d7` → amended into a sibling's commit → reset → `3c5d8d3`.
  **Verified independently: no work lost**, each final commit holds exactly one agent's file.
  **Two mitigations, both cheap and known:** (1) `isolation: "worktree"` per dispatched agent;
  (2) an executor rule — `git add <path> && git commit`, **never** `git commit --amend`, on a
  shared worktree. *Second-order significance: item #32 is no longer merely un-tracked, it is
  actively costing — which strengthens Theme F's case in S1.t3 beyond "raised three times."*

---

---

## Findings added by Wave 2's second half

**9. The demand asymmetry is the Slice-1 result.** The inbox subtracts **85%** against `BACKLOG.md`
(33 of 39 already tracked, shipped, closed, or excluded) — AC1.2a's premise confirmed by
measurement. The retro corpus subtracts only **33%**, and **zero of the 20 M5-era retro items has a
BACKLOG home**. Signal's newest recorded demand is its least-tracked. D-M5E7-3's bet on the retro
corpus as primary evidence base is now measured, not assumed.

**10. Two clusters are backed by something that already went wrong; nine are not.** **F** (EXECUTE
dispatch guidance — a live incident *this Epic*, 0-hit across all four ledgers) and **B**
(false-green/test-strength — **two guards shipped as false-greens**, `B14` and `B19`). Everything
from rank 3 down rests on "four documents asked for it," which is weaker evidence and is labelled as
weaker. **Ranking deliberately avoids frequency** — AC1.3 establishes it is recorded nowhere; the
corpus's only two recorded recurrences are `B13` and `B6`.

**11. The plan's one worked example of an intra-file duplicate was not a duplicate.**
`#13 ≡ #27` is a `/sig:audit` **name collision** that BR-1 deliberately **split into two commands**.
AC1.2a's third subtraction therefore ran with an **empty input**. Surfaced as a PLAN deviation, not
skipped — the pattern retro item #9 asks for.

**12. The subtraction authority was itself stale.** `BACKLOG.md` (stamp 2026-07-19) carries **four
unmarked closures** shipped by M5.E6 on 2026-07-25 — `/sig:sweep` (its row still reads *"Confirmed
not yet built"*), CLAUDE.md de-bloat + command-frontmatter freshness (both live in `sweep.js`), and
`docs/map` Stage 1 (`ship.md:65`). Live-open is **33 of 38**, not 38. Routed to S4.t11.

**13. Corrections resolved to SIX marked IDs, and the PROGRESS count of "FIVE" was ambiguous.**
`analysis/`-side corrections are C3, C4, C5, C6, C8 (five, as finding 7 said) — the parenthetical
grouping in that finding mislabeled C6 as `.planning/`-side. **C7 was also marked** at the site
carrying it, giving 10 markers over 6 IDs. Two further record errors found while placing them:
**C8 was never actually written into `M5.E7-RESEARCH.md` §0** despite `B38` citing it there, and
**`B38` attributes the C8 claim to `REPO-ANALYSIS.md` when it lives in
`SIGNAL-INTEGRATION-RUNDOWN.md:186`**. Both routed to S4.t11.

**14. The register miscounted itself on the first pass, and says so.** Initial totals (20 tracked
inbox entries, 21 net-new retro items) were wrong; recounting from the enumerated citation lists
gave 23 and 26. Recorded in `§4` rather than quietly fixed — it is a live instance of the
claimed-facts-ledger discipline retro item #4 asks for, in the one document that could least afford
to demonstrate the opposite.

---

## Last Updated

2026-07-26 — **Waves 1+2 complete (8 of 11 tasks). Slices 1 and 2 both closed.**
Suite 1623/1623, validator green, working tree clean.
**NEXT: Wave 3 — S3.t7 counterfactual (delegated; independence is the mitigation) → S3.t8
disposition.** Then Wave 4: S4.t9 roadmap → S4.t11 land in BACKLOG.
