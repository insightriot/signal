# M5.E7 S1.t2 — Demand register fragment: the work ledgers

**Task:** S1.t2 (`M5.E7-PLAN.md` § Slice 1) · **Spec:** `M5.E7-REQUIREMENTS.md` FR1 (AC1.2, AC1.2a, AC1.3)
**Sibling fragment:** S1.t1 harvests the retro corpus. **This file harvests the four work ledgers only.**
**Date:** 2026-07-25

**What this file is NOT.** It is **not deduped**. AC1.2a fixes a mandatory dedupe order (subtract
`BACKLOG.md` first, then shipped commands, then intra-file duplicates) and that order is **S1.t3's
job**, not this task's. Nothing below is subtracted, collapsed against BACKLOG, or ranked. Where a
correspondence between two ledgers is visible it is recorded as a **raw pointer**, never as the
conclusion "therefore not net-new."

Citations are inline code (`` `file:line` ``), not markdown links. **Precise reason, since it
matters for S4.t11:** the standing hygiene guard walks `README.md` + `CLAUDE.md` + `docs/` +
`analysis/` and **deliberately not `.planning/`** — `tools/lib/doc-hygiene.js:91-93` states the
exclusion exists because `.planning/` research and plan docs *"legitimately quote link syntax like
`](path.md)` that a standing HARD guard would false-fail."* So this fragment is outside the guard's
scope, and the inline-code convention is belt-and-braces (it also survives `/sig:sweep`'s widened
`.planning/` walk, which passes `stripCode: true`). **The guard does bind `analysis/`** — which is
where `SIGNAL-V2-ROADMAP.md` lands, so a link typo *there* is a real suite failure at S4.t11.
Verified 2026-07-25: this file trips none of the four checks (`checkInternalLinks`,
`checkRosterCounts`, `checkVersionConsistency`, `checkFillInStubs`); no `## Last Updated` section is
enforced by any of them.
**Line numbers are as of this commit** (2026-07-25); headings and dates are the durable key.

---

## §0 — Coverage ledger: what was read, what was capped, what was never opened

P5 makes the input budget numeric and enforceable, and requires the skip be **recorded** — a silent
cap reads as full coverage, which is a documented Signal failure mode.

| File | Lines | Coverage | Method |
|---|---|---|---|
| `BUGS.md` | 91 | **FULL** — every row, every word | Read in full |
| `BACKLOG.md` | 191 | **FULL** | Read in full |
| `OPEN-QUESTIONS.md` | 32 | **FULL** | Read in full |
| `ISSUES-INBOX.md` | 1,507 | **CAPPED** — see below | Headings + `Status` + `Update` lines, then 6 targeted bodies |

### The `ISSUES-INBOX.md` cap, stated exactly

**Read for all 39 entries:** the `## ` heading, the `**Status:** Logged YYYY-MM-DD` line, and any
`> **Update 2026-07-04 …**` second-touch line. That is 3 lines per entry — roughly **120 of 1,507
lines (~8%)**.

**Entry bodies read in full — 6 of 39** (the cap allows ≤ 8). Each is named with why the theme
turned on the body:

| Entry heading | Line | Why the body was needed |
|---|---|---|
| `"Spec-internal consistency" as a PLAN-validation axis` | `:128` | Oldest un-promoted entry with no BACKLOG counterpart; heading did not reveal whether it proposes a mechanism or an observation |
| `Dry-run gate as a standard PLAN pattern` | `:146` | Same — needed to know if it is a codified pattern proposal or a note |
| `Map drift-guard (v0.1.6 doc-integrity candidate)` | `:1449` | Determines whether the `docs/map` subject is one theme or several across four homes |
| `Scan project integrations into a tooling catalog for agents` | `:1467` | Recent, no update marker, heading gives no mechanism |
| `Config-drift hazard check in VERIFY (+ SHIP reminder)` | `:1475` | Same — and it opens a stated general question (universal hazard checks) the heading hides |
| `Resume-time retro nudge when parked at SHIP with no retro (B26 Layer-3 follow-up)` | `:1489` | The one cross-file edge from the open `BUGS.md` B26→B30→B36 chain into the inbox |

**Bodies deliberately NOT read — 33 of 39.** Their headings + `Status` + `Update` lines were
sufficient to place them in a theme and cite them. **Consequence, stated plainly:** for those 33,
this fragment records *that the entry exists, when it was logged, and what subject it names* — it
does **not** record their internal proposals, sub-questions, or cross-references. If S1.t3 or S3.t8
needs a mechanism-level read of a specific entry, that entry's body is still unread.

### Never opened at all (scope boundary, not a skip)

- **No `*-RETROSPECTIVE.md` files** — that is S1.t1's corpus. This fragment carries zero retro signal.
- **No `.planning/archive/**`** — including `archive/BACKLOG-REVIEW-2026-07-04.md`, the frozen
  snapshot `BACKLOG.md` succeeded. `BACKLOG.md:5` states the snapshot's items are already folded in.
- **No `DECISIONS.md`, `MILESTONE-5.md`, `CONTEXT.md`, `STATE.md`, or `analysis/`.**

**This fragment is four files. It must not be read as "everything Signal recorded."**

---

## §1 — `BUGS.md`: chains, standalone open items, and non-chains

`BUGS.md` holds **36 entries** (B1–B36): 5 `needs-triage` · 0 `confirmed` · 2 `dismissed` ·
29 `fixed` (`BUGS.md:91`). FR1 asks for **open items *and* patterns across closed ones** — both
halves are below.

**Label discipline (AC1.2).** **DOCUMENTED** = Signal's own rows cross-reference each other by
B-ID or by an unambiguous named artifact. **INFERRED** = the edge is this reading's judgment.

**One documented edge sits outside all five chains:** `B20 → B5` (`BUGS.md:46`, `:76`) — a
same-defect **re-catalogue**, not a chain. Recorded in §5 proxy #2, where it is a datum about
ledger integrity rather than about demand.

### §1a — Canonical signals table (each chain is exactly ONE signal)

| Signal | Chain | Edges | Status now | Theme |
|---|---|---|---|---|
| **S-BUG-1** | `B24 → B27 → B28` | **DOCUMENTED** | **CLOSED** | The migrate dangling-link gate **over-aborts** — it mis-attributes a pre-existing dangle to the migrate |
| **S-BUG-2** | `B15 → B23(b)` | **DOCUMENTED** | **CLOSED** | The *same* gate **under-detects** — a 1 MB scan ceiling silently lets a real dangle through |
| **S-BUG-3** | `B29 → B34` (+ `B14` inferred) | **DOCUMENTED** (B29→B34); **INFERRED** (B14) | **OPEN at B34** (P3) | A defensive fix lands only at its discovery site while the identical sibling seam goes unswept |
| **S-BUG-4** | `B26 → B30 → B36` | **DOCUMENTED** | **OPEN at B36** (**P2 — the only open P2 in the ledger**) | The FR1 retro gate silently no-ops; each fix leaves a new residual |
| **S-BUG-5** | `B18 → B19` | **INFERRED (strong)** | **CLOSED** | Auto-regeneration of `INDEX.md` destroys or mangles what it regenerates over |

**S-BUG-1 and S-BUG-2 are the same gate failing in opposite directions.** Recorded as two signals
because the failure modes, fixes, and risk profiles are opposite (fail-safe over-abort vs. silent
under-detect), but S1.t3 should note that **five of the 36 bugs are one gate** — the single densest
defect cluster in the ledger.

### §1a-detail — chain verification (the refinement, not a restatement)

**S-BUG-1 · `B24 → B27 → B28` — DOCUMENTED, CLOSED.**
- `B24` (`BUGS.md:51`, P2): a pre-existing broken `](*.md)` link inside a strictly-closed-milestone
  DECISIONS section blocks `applyMigrate` — abort + byte-identical rollback. Fail-**safe** but
  over-eager. Discovered 2026-07-20 (M5.E4.T4.1, `8fec7bf`). **Fixed M5.E5 (`501ad9d`)** — delta
  re-keyed on the resolved absolute target with multiset semantics.
- `B27` (`BUGS.md:54`, P3): archive-doc *inline* link to an FR6-renamed target aborts the migrate.
  Edge is explicit — *"Surfaced 2026-07-21 during **M5.E5.T1** (B24) while building the AC1.3
  gate-strength fixture. B24's resolved-target delta correctly treats…"* **Fixed M5.E6 (T11/T12,
  `5ec8a81`+`61f4b67`)** — flags instead of aborting.
- `B28` (`BUGS.md:55`, P3): the absolute-path `](/abs/foo.md)` sibling. Edge is explicit twice —
  *"the B27-sibling for absolute links"* and *"not a regression (it false-aborted pre-B24 too; B24
  fixed the relative-link case)."* **Fixed M5.E6 (T10, `cf352f9`)**.

> **Status correction for downstream tasks.** `CLAUDE.md`'s Current State paragraph and the S1.t2
> brief both describe `B27`/`B28` as open (`needs-triage`) and the gate as still over-aborting.
> **`BUGS.md` is the live record and says otherwise:** all three are `fixed`, closed out at
> `BUGS.md:54-55`. This chain is a **pattern across closed bugs**, not an open want.

**S-BUG-2 · `B15 → B23(b)` — DOCUMENTED, CLOSED.** `B15` (`BUGS.md:39`, P3): `FILE_SCAN_CEILING`
(1 MB) truncation can silently defeat the *blocking* dangling gate on a >1 MB doc. `B23(b)`
(`BUGS.md:50`) names the same defect as a coverage gap and says so by ID — *"also the B15 class."*
Both closed in M5.E4 by the same commit family (`cb4cbf9` — full-file scan + a >1 MB fixture,
`BUGS.md:71`, `:79`).

**S-BUG-3 · `B29 → B34`, with `B14` as an inferred third instance — OPEN at `B34`.**
- `B29` (`BUGS.md:56`, P3): `_afterRead` destructured from `opts` at 6 RMW cores — an unreachable
  prototype-pollution gadget. **Fixed M5.E6 (T19, `78c433e`)**.
- `B34` (`BUGS.md:62`, P3, **`needs-triage`**): *"`renameFn` injectable seam is the B29-parity
  prototype-pollution gap … the SAME shape B29 hardened for `_afterRead`, left unguarded on the
  literal sibling."* Explicit ID edge. Surfaced 2026-07-24 by the M5.E6 REVIEW security panel.
- **`B14` (`BUGS.md:37`) is a third instance of the identical theme, INFERRED.** Its own text:
  *"M5.E2 REVIEW fixed this in `migrate-memory.js` + `archive-tree.js` … but the same pattern
  remains at `evict.js`, `add.js`, `resume.js`."* A defensive fix at the discovery site, siblings
  unswept — the same shape, one Epic earlier, in a different vulnerability class (symlink
  confinement, not prototype pollution). **The edge is textually anchored but not by ID:** `B34`
  cites *"the 'one-seam-over' the **evict.js false-green** primed for,"* and the evict.js
  false-green is recorded in `B14`'s close-out (`BUGS.md:70`). Labeled INFERRED because no row
  names both IDs.

**S-BUG-4 · `B26 → B30 → B36` — DOCUMENTED, OPEN at `B36`, P2.**
- `B26` (`BUGS.md:53`): `shipFR1Check` returns `{skipped:true}` for a fully-completed Epic on the
  self-hosted flow. **Fixed M5.E5 (`d62e5f2`)** — STATE-based Epic-close fallback, **Layers 1+2
  only; Layer 3 descoped**.
- `B30` (`BUGS.md:57`, P2): the pre-check runs before the SHIP transition, so B26's fallback can't
  fire. Explicit — *"Residual of B26."* **Fixed M5.E6 (T15, `0b1ef86`)**.
- `B36` (`BUGS.md:64`, **P2, `needs-triage`**): the gate skips on a **stale/non-shipped milestone
  row**. Explicit — *"**Third milestone-row residual after B26 (no row) + B30 (phase timing)**"* and
  *"Ironic residual on the Epic that fixed B30."* Confirmed 2026-07-25 dogfooding M5.E6's own SHIP.
- **Cross-file edge (raw pointer, no conclusion drawn):** B26's descoped **Layer 3** has a second
  home in the inbox — `ISSUES-INBOX.md:1489` *"Resume-time retro nudge when parked at SHIP with no
  retro (B26 Layer-3 follow-up)"*, logged 2026-07-21. Same subject, two ledgers.

**S-BUG-5 · `B18 → B19` — INFERRED (strong), CLOSED. `B32` deliberately NOT joined to it.**
- `B18` (`BUGS.md:43`): `regeneratePlanningIndex('.')` mangles doc keys and writes garbage over
  `INDEX.md`. `B19` (`BUGS.md:44`, P2): the v3 migrate's tail `regeneratePlanningIndex` silently
  clobbers a hand-curated `INDEX.md`. **Neither row names the other's ID.** The edge is: same
  function, same surface, both confirmed 2026-07-19, both surfaced in the same M5.E3 dogfood
  session (`B19` names `exec-s6b-curate`; `B18` says it *"bit the curation pass once"*). Strong,
  but INFERRED.
- **`B32` (`BUGS.md:60`, `needs-triage`) should NOT be folded into this chain.** It is the
  `docs/map` `COMMANDS` JS array missing two commands. Zero cross-reference to `B18`/`B19`, a
  different file (`docs/map/index.html`), a different mechanism (a hand-maintained array outside a
  roster guard, not an auto-regenerator). The only connection is theme-level — *a derived artifact
  is only as trustworthy as the guard covering it*. **Recorded here as an explicit non-join** so a
  later reader does not re-make the merge and inflate the chain to three.

### §1b — Standalone open items (no chain; would vanish from a chains-only view)

Two of the five `needs-triage` bugs belong to no chain:

| ID | Pri | Subject | Cite |
|---|---|---|---|
| `B33` | P3 | Stale `LOCK_TTL_MS` comment in `add.js` — the comment describes an arrangement that was already inaccurate before M5.E6 and is doubly so after B31/T23. Comment-only, no behavior change. | `BUGS.md:61` |
| `B35` | P3 | `sweep.js checkIndexFreshness` unwrapped compose/diff can crash the read-only sweep — every *other* sweep check is crash-safe. Low reachability; the stranger-repo never-crash contract holds. | `BUGS.md:63` |

Full open set for completeness: `B32` (P3, docs/map array), `B33` (P3), `B34` (P3, in S-BUG-3),
`B35` (P3), `B36` (**P2**, in S-BUG-4).

### §1c — Patterns across closed bugs (INFERRED; recorded, not actioned)

Per NFR3 these are recorded as observations with citations. **No work item is proposed here.**

**Pattern P-A — false-greens and test-strength gaps.** A test that passes for the wrong reason has
bitten Signal repeatedly, and in at least two cases the *guard shipped to prevent a bug was itself
a false-green*:
- `B14` (`BUGS.md:70`) — *"REVIEW caught an incomplete fix at the `evict.js` site … a **false-green
  test** planted the symlink one level too high."*
- `B19` (`BUGS.md:75`) — *"marker-based foreign-INDEX guard (**the shipped legend-based one was a
  false-green**)."*
- `B25` (`BUGS.md:52`) — a **test-strength gap only**: the lock tests *"pass even for a wrapper that
  reads OUTSIDE its lock."*
- `B23` (`BUGS.md:50`) — a four-part coverage bundle (organic re-root path, >1 MB branch, staged-diff
  assertion, batch re-run).
- `B34` (`BUGS.md:62`) — *"the 'one-seam-over' the evict.js false-green primed for."*

**This pattern is load-bearing for S3.t7.** The plan names cross-model adversarial review vs. the
v0.1.9 `evict.js` false-green as the counterfactual's live lead (`M5.E7-PLAN.md:128`, `:146`). The
citations above are the ledger-side evidence for that lead, pre-assembled.

**Pattern P-B — dogfooding Signal on Signal is a primary discovery channel.** A large share of the
ledger was found by *running* Signal, not by its tests:
- `B10`, `B11`, `B12` (`BUGS.md:32-35`) — the M5.E2 S4.t1/t2 dry-run and full-migrate on Signal's
  own `.planning/` and on the `nextpass` copy. `B12` is explicitly labeled *"a 'battle-tested' find."*
- `B19` (`BUGS.md:44`) — the M5.E3 Signal dogfood (89-line curated INDEX → 165-line auto-gen).
- `B26`, `B30`, `B36` — **each found dogfooding Signal's own SHIP**, on three consecutive Epics
  (M5.E4, M5.E5, M5.E6). `B26` was *"dogfooded on its own SHIP."*
- `B31` (`BUGS.md:59`) — found 2026-07-23 during M5.E6 PLAN's own lock-status verification.
- `B8` (`BUGS.md:28`) — *"Confirmed live 2026-07-16 on `nextpass/.planning/STATE.md`"*, a real
  external repo.

### §1d — Co-occurring clusters that are NOT chains (recorded, not collapsed)

The brief requires these be recorded and **not** presented as chains. Verified: **zero
cross-reference edges** between the members of either group.

| Cluster | Members | Why it is not a chain |
|---|---|---|
| The `/sig:add` capture-pipeline cluster | `B2` (footer drift), `B3` (derived title cut mid-clause), `B4` (drain missed blockquote promotions) — `BUGS.md:20-22` | All three touch the capture/drain pipeline and all closed in the v0.1.5–v0.1.6 window, but **no row references another**. Distinct root causes: footer placement, string slicing, regex coverage. Co-occurrence is a release window, not a causal chain. |
| The Epic-open / release-hygiene cluster | `B6` (resume origin-drift false positive), `B7` (`plugin.json` version stuck at 0.1.6), `B9` (`setCurrentEpic` doesn't reset phase state) — `BUGS.md:25`, `:26`, `:30` | All three surfaced during the M5.E1/M5.E2 opens (2026-07-15/16) and all three are "the bookkeeping around an Epic boundary is wrong," but the mechanisms are unrelated (banner predicate / release version bump / state-mutation reset) and **no row references another**. |

---

## §2 — `ISSUES-INBOX.md`: all 39 entries, clustered by theme

**Key = heading + logged date.** Line numbers are a convenience as of this commit. Ordinals are
deliberately avoided in the citation key — `#32` and `B32` are different things and would collide.

`U` = carries a `> **Update 2026-07-04 (backlog review)**` second-touch marker (10 of 39; see §5).

> **HIGH-LEVERAGE NOTE FOR S1.t3 — the `#13 ≡ #27` shorthand in `M5.E7-PLAN.md:75` and
> `M5.E7-REQUIREMENTS.md:52` is NOT a duplicate pair.** The two entries
> (`/sig:audit` — engineering-readiness audit, `:567`, logged 2026-05-09) and
> (`New command /sig:audit --docs / --code`, `:1309`, logged 2026-06-04) were a **name collision**
> on `/sig:audit`, and **BR-1 resolved it on 2026-07-04 by splitting them into two distinct
> commands** — `/sig:audit` keeps the readiness scorecard, the hygiene sweep is renamed
> `/sig:sweep`. Both live separately in `BACKLOG.md` today (`BACKLOG.md:108` Sprint 5 `/sig:audit`;
> `BACKLOG.md:50` Sprint 3 `/sig:sweep`). **Collapsing them on the plan's shorthand would destroy a
> deliberate, ratified distinction.** Recorded, not resolved — the disposition is S1.t3's.

### T1 — Compounding, memory, and the doc-runtime (6)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| Compound-engineering audit before `/sig:compound` design | 2026-05-27 | | `:11` |
| FUTURE-IDEAS drain process — disposition protocol folds into M4.5.E2.S5 | 2026-05-24 | | `:981` |
| Memory & Documentation Management as Signal-managed Runtime (wiki/index pattern + retro enforcement + migration tooling) | 2026-05-25 | U | `:1118` |
| Passive Stop-hook → continuous in-the-moment observation | 2026-06-04 | | `:1301` |
| Archive-migration dogfood → `/sig:migrate-memory` lessons | 2026-06-05 | | `:1319` |
| STATE.md append-without-evict — closed-work narrative must leave the live file | 2026-07-04 | | `:1427` |

### T2 — v2 direction and landscape re-aim (5)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| Roadmap refresh — post-M4.5 reality check on v2 vision | 2026-05-27 | | `:49` |
| Vocabulary attribution sweep on E9 retro mechanism | 2026-05-27 | | `:89` |
| M5 opening move — feature-parity audit | 2026-06-01 | U | `:955` |
| Codebase knowledge-graph as a Signal-managed artifact (graphify, graph-only) | 2026-05-24 | U | `:1037` |
| Codebase-traversal + memory borrows from the "5 open-source CC tools" review | 2026-06-05 | U | `:1342` |

*Raw pointer:* this cluster is the subject of **this Epic** (BR-8) and of `BACKLOG.md:20` Sprint 2.

### T3 — New command surface / cockpit (8)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| Pre-scoped DISCUSS agenda — surface gray areas as a checklist before drilling in | 2026-05-02 | | `:403` |
| `/sig:report` — narrative project report (separate from `/sig:status`) | 2026-05-03 | | `:444` |
| `/sig:orient` — plain-language project overview (no Signal jargon) | 2026-05-24 | | `:490` |
| `/sig:audit` — engineering-readiness audit (codebase scorecard + refactor plan) | 2026-05-09 | U | `:567` |
| `/sig:add` — capture-and-route new work to the right altitude in `.planning/` | 2026-05-13 | | `:716` |
| `/sig:goal` — phase-aware wrapper around Claude Code's `/goal` for intra-phase autonomous loops | 2026-05-14 | | `:781` |
| I wonder aloud at having a … *(truncated heading; the Update line identifies it as the STATE-frontmatter statusline breadcrumb)* | 2026-05-18 | U | `:873` |
| New command /sig:audit --docs / --code *(renamed `/sig:sweep` by BR-1)* | 2026-06-04 | U | `:1309` |

*Raw pointer, no conclusion:* the `/sig:add` entry names a command that **shipped in v0.1.3**.
AC1.2a lists shipped commands as the second subtraction — recorded for S1.t3, not applied here.

### T4 — Calibration depth (3)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| Calibration granularity — making PROFILE.md more expressive | 2026-04-22 | | `:198` |
| Multi-feature project lifecycle | 2026-04-23 | U | `:277` |
| Signal should acclimate it's language to … *(truncated heading; the Update line identifies it as the audience-technicality dial)* | 2026-06-03 | U | `:971` |

`Calibration granularity` at 2026-04-22 is the **oldest entry in the inbox**.

### T5 — Phase architecture and plan discipline (3)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| PREPARE phase — splitting PLAN's tail from EXECUTE's head | 2026-04-25 | U | `:307` |
| "Spec-internal consistency" as a PLAN-validation axis | 2026-05-26 | | `:128` |
| Dry-run gate as a standard PLAN pattern | 2026-05-26 | | `:146` |

*Body-read detail (these two were among the 6 opened):* the **spec-internal-consistency** entry
proposes a concrete **9th VALIDATION dimension** — for each PLAN-stated formula/threshold, walk the
matching AC and verify the formula's output satisfies the AC's input; the trigger was a real
M4.5.E9.S1.t11 contradiction the existing 8 dimensions structurally could not catch. The
**dry-run gate** entry proposes a 4-step codified PLAN pattern for any Epic writing to existing user
state, on evidence of **two of two** recent Epics where the gate caught real issues (M4.5.E6 D15;
M4.5.E9 S1.t10, two bugs that would have propagated to five committed files). Both carry a
`**Resolve by**` that has passed. Both cite retro sections — meaning **S1.t1 may surface the same
two subjects independently from the retro corpus**; flagged for S1.t3 as a possible cross-fragment
overlap, not resolved here.

### T6 — Doc and roster drift guards (5)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| `/sig:docs-update` — doc-vs-codebase drift verification (port from GSD) | 2026-05-12 | | `:643` |
| `docs/map/index.html` — refresh protocol (auto-generated vs. manual sync) | 2026-05-24 | | `:884` |
| Path-scoped skills — a second scoping axis (phase × path) | 2026-06-07 | | `:1391` |
| CLAUDE.md "de-bloat" test + command-frontmatter freshness discipline | 2026-06-07 | | `:1399` |
| Map drift-guard (v0.1.6 doc-integrity candidate) | 2026-07-13 | | `:1449` |

*Body-read detail:* `Map drift-guard` proposes a **roster/version guard, explicitly not a
regenerator and explicitly not a hook** — a `tools/` check comparing the map's listed
commands/agents/skills + version stamp against the real repo files, wired into the test suite and a
`/sig:ship` checklist line, with all prose left hand-curated. **This is the same subject as `B32`**
(`BUGS.md:60`, the `COMMANDS` array drifting *because* it is not roster-guard-checked). Recorded as
a raw cross-ledger pointer; see §5 for why this subject is the corpus's strongest recurrence signal.

*Note:* `BACKLOG.md` files `/sig:docs-update` under Sprint 7 (framework ports, `BACKLOG.md:152`),
not under doc hygiene. The clustering here is by subject; the BACKLOG placement is recorded so
S1.t3 can reconcile rather than re-derive.

### T7 — Verification hardening (3)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| Synthesizer-output validator-side sanity-check (deferred from M4.5.E7) | 2026-05-23 | | `:170` |
| Config-drift hazard check in VERIFY (+ SHIP reminder) | 2026-07-21 | | `:1475` |
| Resume-time retro nudge when parked at SHIP with no retro (B26 Layer-3 follow-up) | 2026-07-21 | | `:1489` |

*Body-read detail:* `Config-drift hazard check` proposes a deterministic offline VERIFY pass
reconciling config keys referenced in code against keys declared in `.env.example`/templates
(referenced-but-undeclared, orphans, alias pairs), tier-gated advisory→enforced. Origin is a real
incident in a Vercel app. It opens a **stated general question** the heading hides: *"should Signal
carry a small set of stack-aware universal pre-ship hazard checks?"* — a direction question, not
just a feature. Recorded verbatim in substance because it is the kind of item AC3.4's
Signal-native column exists for.

### T8 — Process, tooling, and meta (6)

| Entry heading | Logged | U | Line |
|---|---|---|---|
| E3 contribution scaffolding — deferred (CONTRIBUTING.md + issue templates + docs/compatibility.md sub-doc) | 2026-05-24 | | `:1089` |
| `/sig:doctor` helper-script split (M4.5.E8 S2 deviation; logged 2026-05-29) | 2026-05-29 | | `:1284` |
| GitHub Issues for work-item tracking — deferred until live users | 2026-06-06 | | `:1366` |
| Trigger watchlist — standing entry (check conditions at every drain) | 2026-07-04 | | `:1407` |
| **Dedicated test-sandbox project for Signal QA.** A commi… *(truncated heading)* | 2026-07-17 | | `:1457` |
| Scan project integrations into a tooling catalog for agents | 2026-07-21 | | `:1467` |

*Body-read detail:* `Scan project integrations into a tooling catalog` asks for a scan of the
project's available integrations (MCPs, CLIs) written into documentation so agents know what tooling
exists rather than guessing, refusing, or asking for redundant installs — plus a periodic rescan.
It is **verbatim raw capture** (unedited `/sig:add` prose, including the `//` aside) and has **no
`BACKLOG.md` counterpart visible**. Recorded; the net-new determination is S1.t3's.

*Structural note:* the `Trigger watchlist` entry is marked **"Standing entry — never promote, merge,
or delete."** It is a mechanism, not a want, and should not be counted as demand.

### §2 — count reconciliation

6 + 5 + 8 + 3 + 3 + 5 + 3 + 6 = **39**. Every top-level `## ` heading in `ISSUES-INBOX.md` is
placed in exactly one theme. No entry is dropped, merged, or deduped.

---

## §3 — `BACKLOG.md`: the deduped authority (30 sprint items + 8 parked)

**This is not a peer input.** AC1.2a makes `BACKLOG.md` the **first** subtraction in S1.t3's
mandatory dedupe order. The reason is recorded in the file itself: the 2026-07-04 review sharpened
inbox entries into BACKLOG sprints **without removing them from the inbox** (`BACKLOG.md:5` — the
snapshot's *"added items (A1–A5), sharpened items, and sprint clusters are folded in below"*).
**BACKLOG is the deduped view; the inbox is the raw log.** Any other order re-derives Sprints 2–7
and presents them as findings.

**Inventory: 30 `### ` items across 6 sprints, plus 8 parked bullets = 38 tracked subjects.**
Subtracting only the 30 would re-derive the parked items as findings.

| Sprint | Line | Items | Framing |
|---|---|---|---|
| Sprint 2 — Re-aim the map | `:20` | 5 | Research; gates the v2-port arc |
| Sprint 3 (residual) — Memory & doc-runtime | `:46` | 5 | The maintenance-command half |
| Sprint 4 — Compounding replay | `:74` | 4 | Closes the "ship and forget" gap |
| Sprint 5 — Cockpit & interaction surface | `:96` | 6 | The new command surface |
| Sprint 6 — Calibration depth | `:126` | 4 | Data-gated |
| Sprint 7 — Framework ports | `:148` | 6 | M5's remaining ports |
| Parked — trigger watchlist | `:178` | 8 bullets | Trigger-gated, "not sprint material" |

**One of the 30 is already struck through and closed:** `~~Concurrency-lock the doc-runtime RMW
paths~~ — **DONE** (M5.E4 FR5 + M5.E5 B25; closed-out M5.E6 FR7)` (`BACKLOG.md:66`), with the
original entry preserved beneath it for provenance. So **29 open + 1 closed-with-provenance**.

### The two entries the brief singles out

**`BACKLOG.md:82` — `Retro *replay* into the next Epic's DISCUSS/PLAN`** (Sprint 4, roadmap tag).
Its own text: *"E9 built retro **capture** only; the gap (**named in the very first inbox entry**)
is surfacing captured learnings into the next Epic's DISCUSS/PLAN context."* BACKLOG explicitly
names its own inbox provenance.

**`BACKLOG.md:86` — `Cross-Epic pattern detection`** (Sprint 4, roadmap tag): *"Detect recurring
patterns across `RETROSPECTIVES.md` over time."*

**Both are already tracked.** `M5.E7-PLAN.md:82` is explicit that neither may be presented as a
discovery — *"**BACKLOG already knew** — reconcile against it, don't re-derive it."* Recorded here
so the instruction travels with the data.

### Parked watchlist — 8 bullets, with raw inbox pointers

Recorded as pointers only. **No "therefore not net-new" conclusion is drawn** — that is S1.t3's.

| Parked bullet (`BACKLOG.md:182-189`) | Trigger | Inbox entry naming the same subject |
|---|---|---|
| E1 Slices 3–5 — Linux/WSL install matrix + versioning policy | a platform tester volunteers | *(none found in the 39 headings)* |
| E3 contribution scaffolding | a/b/c in its entry | `E3 contribution scaffolding — deferred…` (2026-05-24) |
| Synthesizer validator-side check | **2+ regressions by 2026-08-23** | `Synthesizer-output validator-side sanity-check` (2026-05-23) |
| `/sig:doctor` helper-script split | deferred refactor | `/sig:doctor` helper-script split (2026-05-29) |
| `docs/map` Stages 2/3 | the deeper refresh protocol | `docs/map/index.html` — refresh protocol (2026-05-24) |
| GitHub Issues full setup | first live external tester — **already fired 2026-07-15** | `GitHub Issues for work-item tracking` (2026-06-06) |
| PREPARE-phase early-promotion triggers | 3 conditions | `PREPARE phase — splitting PLAN's tail…` (2026-04-25) |
| STATE auto-update Options B/C | Option A discipline demonstrably fails | *(none found in the 39 headings)* |

**Six of eight have an inbox twin; two do not.** The `Synthesizer validator-side check` trigger is
the corpus's **only dated expiry (2026-08-23)** — `BACKLOG.md:184` notes it *"expires unobserved
without the watchlist."* As of this harvest it is **29 days out**.

---

## §4 — `OPEN-QUESTIONS.md`

**CONFIRMED — zero unique signal.** Both live items are already in `BACKLOG.md`:
`Tier count: validate 4 tiers` (`OPEN-QUESTIONS.md:9`) → `BACKLOG.md:142` Sprint 6
*"Tier-count validation … (OPEN-QUESTIONS watch-item)"*; `Slash-command testing strategy`
(`OPEN-QUESTIONS.md:22`) → `BACKLOG.md:100` Sprint 5 *"Slash-command testing harness (A5) —
**Promoted from OPEN-QUESTIONS**"*. Not re-derived, per the brief.

---

## §5 — Metadata reality per file (AC1.3)

**RECORDED and INFERRED are kept in physically separate columns and are never presented as
equivalent. Frequency is recorded nowhere in any of the four files.**

### Per-file recency

| File | Date field? | RECORDED recency | Range | Gaps |
|---|---|---|---|---|
| `ISSUES-INBOX.md` | **Prose, not a field** — `**Status:** Logged YYYY-MM-DD` | **39/39 entries carry a logged date** | 2026-04-22 → 2026-07-21 | 38 carry it on a `**Status:**` line; **1 carries it in the heading only** (`/sig:doctor` helper-script split … logged 2026-05-29, `:1284`, no `**Status:**` line). Format is not uniform — e.g. `Logged X`, `Logged X via /sig:add`, `Deferred from M4.5.E7 … Logged X`, `Added X`, `Leaning Option C for v2. Logged X` — so it is **not machine-queryable** without tolerant parsing. |
| `BUGS.md` | **No date column** — the table is `ID / Status / Pri / Summary` | Most entries carry a discovery or confirmation date **inside the Summary prose**, usually Epic-stamped | ~2026-06-06 (B1 triage) → 2026-07-25 (B36) | Dates are embedded in narrative, so **no date is a queryable field**. File-level stamp at `:91`. Row order is not strictly ID-ordered — **`B16` precedes `B15`** (`:38`, `:39`). |
| `BACKLOG.md` | **NONE — no per-entry date field at all** | **One file-level stamp:** `*Last updated: 2026-07-19*` (`:191`) | — | Exactly **5 incidental prose dates** appear inside the 38 tracked subjects (`:64` scope widened 2026-07-21; `:66` deferred from the 2026-07-19 memory-layer review; `:76` substrate locked 2026-07-15; `:184` the 2026-08-23 trigger expiry; `:187` trigger fired 2026-07-15). Everything else is undated. **Sequence is encoded as sprint position, not time.** |
| `OPEN-QUESTIONS.md` | **NONE** | One file-level triage stamp, `2026-04-26` (`:5`) | — | Individual questions are undated. |

### Frequency — recorded NOWHERE; only these four proxies exist

| # | Proxy | What it measures | Measured value | What it does NOT mean |
|---|---|---|---|---|
| 1 | `> **Update 2026-07-04 (backlog review)**` markers | An entry was **touched a second time** | **10 of 39** — `:279`, `:309`, `:569`, `:875`, `:957`, `:973`, `:1039`, `:1120`, `:1311`, `:1344` | All 10 formal markers are from **one review session on one day** — a single reviewer's selection, not 10 independent recurrences. It cannot distinguish "came up repeatedly" from "was picked up once." **The formal markers are also not the whole picture:** at least one entry carries an *in-prose* second touch on a different date — `` `docs/map/index.html` — refresh protocol `` has **`**Updated 2026-06-03:**`** mid-paragraph (`:886`), invisible to a `^> **Update` scan. It is on the same subject flagged as the strongest recurrence in proxy #3. AC1.3 pins this proxy at the 10 formal markers, so the count stands as reported — but "one session, one day" is true of the markers, not of every second touch in the file. |
| 2 | Duplicate / colliding entries | Same subject captured twice | **Inbox: zero true duplicates** among the 39 headings. The one pair is the `/sig:audit` **name collision** (`:567` + `:1309`), split by BR-1 into two distinct commands. **`BUGS.md`: one real, explicitly-dismissed duplicate — `B20 ≡ B5`.** `B20` (`BUGS.md:46`) re-catalogues the ESLint-9 flat-config defect on the stated premise *"no B5 entry actually exists in this file (lost in an earlier edit)"*; the M5.E4 close-out (`BUGS.md:76`) dismisses it — *"duplicate of B5 (its 'no B5 entry exists' premise was **false**)."* Same defect logged twice, **2026-07-13 (v0.1.6 VERIFY) and 2026-07-19 (M5.E3 VERIFY)**, six days apart, because the first entry was wrongly believed lost. | The plan's `#13 ≡ #27` shorthand overstates the inbox case; see the note in §2. **`B20`/`B5` is a different thing from the B13/B6 recurrences below:** those are one defect *occurring* twice; this is one defect *recorded* twice. It measures **ledger integrity**, not demand frequency, and must not be counted as a repeat want. |
| 3 | Cross-file recurrence | One subject with homes in ≥2 ledgers | **Strongest: `docs/map` — 4 homes.** `ISSUES-INBOX.md:884` (refresh protocol, 2026-05-24) + `:1449` (drift-guard, 2026-07-13) + `BACKLOG.md:62` (Sprint 3) + `BACKLOG.md:186` (parked Stages 2/3) + `BUGS.md:60` (`B32`, open). **Also:** FR1 retro gate — `BUGS.md` B26/B30/B36 + `ISSUES-INBOX.md:1489`. Compound/replay — `ISSUES-INBOX.md:11` + `BACKLOG.md:28` + `BACKLOG.md:78` + `:82`. `/sig:sweep` — `ISSUES-INBOX.md:1309` + `BACKLOG.md:50`. | A second home may be a **promotion** (the same idea moved) rather than a second occurrence. Under AC1.2a most of these are expected to be exactly that. |
| 4 | `BUGS.md` cross-reference edges | A defect explicitly cites another | **9 ID-level edges.** 8 across the 5 chains — `B27→B24`, `B28→B27`, `B28→B24`, `B23b→B15`, `B34→B29`, `B30→B26`, `B36→B26`, `B36→B30` — the densest being the migrate dangling gate at **5 bugs** (B15, B23b, B24, B27, B28). Plus **1 outside every chain: `B20→B5`** (proxy #2). | Edge count reflects **how carefully the ledger was written**, not how often the problem occurred. `B20→B5` in particular is an edge created by a *bookkeeping error*, not by a causal relationship. |

### The only two recorded recurrences in the entire corpus

These are the strongest genuine frequency data available, because in both cases Signal's own record
states that the *same defect happened twice*:

- **`B13` (`BUGS.md:36`) — a literal NUL byte, twice.** Fixed in `tools/lib/migrate-memory.js` on
  **2026-07-18**; then *"**Recurrence in `.planning/BUGS.md` itself, fixed 2026-07-25**"* — the bug
  writeup pasted the offending source snippet verbatim, NUL included, so **`BUGS.md` became
  grep-blind** from `5062526` until the fix. Failure mode: `grep -c needs-triage .planning/BUGS.md`
  returned nothing, i.e. **a silent "no open bugs" read** on the file agents grep to check bug
  status. Two occurrences, 7 days apart, of one root cause, in the fix's own documentation.
- **`B6` (`BUGS.md:25`) — an explicit re-observation.** *"(Re-observed 2026-07-16 during the M5.E1
  `/sig:resume` — both the local-stale and origin-drift banners fired … same root cause.)"*

Everything else about frequency in this corpus is **inference from a proxy**, not a count.

### Two capture-discipline observations (flagged, not actioned)

Per the "observations need action" norm, each carries an explicit disposition. Per NFR3
(*"if the audit starts generating new work items rather than dispositioning existing ones, it has
slipped scope"*), **neither becomes a work item here.**

1. **The inbox went quiet while the bug ledger stayed live.** Last inbox capture: **2026-07-21**.
   `BUGS.md` received `B34`/`B35` on 2026-07-24 and `B36` on 2026-07-25. Across the whole of M5.E6,
   findings routed to `BUGS.md` and none to `ISSUES-INBOX.md`. **Disposition:** awareness-only input
   to S1.t3's recency weighting — the plan already weights recent retros over M4.5-era items
   (`M5.E7-PLAN.md:80`), and this says the *inbox's* recency curve flattens after 2026-07-21.
   It is not evidence that nothing was captured; it is evidence about *where* capture went.

2. **`ISSUES-INBOX.md` still carries its pre-rename header.** Line 1 reads `# Future Ideas`, and
   the intro at `:3` describes the file as *"distinct from `MILESTONE-4.md`, which is specifically
   the 'rundown v2' integrations"* — both survived the M5.E3 v3-layout rename
   (`FUTURE-IDEAS.md` → `ISSUES-INBOX.md`). `BUGS.md:5` and `:14` likewise still route readers to
   `FUTURE-IDEAS.md`. **Disposition:** this is `.planning/`, so it is **not FR6 scope** (FR6 governs
   `analysis/`), and it is **not a new backlog item**. Routed to this Epic's
   `## What to feed back into Signal` at close — it is the same drift class FR6 addresses, showing
   up inside the directory the doc-runtime *does* govern.

---

## §6 — Handoff to S1.t3

Available for the mandatory AC1.2a order, in the order t3 must apply it:

1. **Subtract `BACKLOG.md` first — all 38 tracked subjects** (30 sprint items + 8 parked), not just
   the 30. Sprint map and parked-bullet pointers are in §3.
2. **Then shipped commands.** `/sig:add` (`ISSUES-INBOX.md:716`) shipped v0.1.3; `/sig:doctor` is
   shipped with a residual split entry parked (`:1284`). Recorded, not applied.
3. **Then intra-file duplicates.** **Zero true duplicates found among the 39 headings.** The
   `#13 ≡ #27` pair named in the plan is a resolved name collision, not a duplicate — see §2.

Carried forward unresolved: the T5 note that `"Spec-internal consistency"` and `Dry-run gate` both
cite retro sections, so S1.t1's fragment may surface the same two subjects independently.

**Not deduped, not ranked, not clustered against BACKLOG. By design.**

---

*S1.t2 · written 2026-07-25 · `ISSUES-INBOX.md` read at 8% of lines under the P5 cap (§0); the
other three ledgers read in full.*
