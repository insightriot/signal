# Signal — Bugs & Findings

Lightweight catalog of bugs, defects, and verified findings for Signal itself.
Markdown-native by design — GitHub Issues are deferred until Signal has live
users (see `FUTURE-IDEAS.md` § "GitHub Issues for work-item tracking — deferred").

**Discipline** (per the "always catalog bugs" norm): catalog a finding the moment
it surfaces → **triage** (read the code / confirm the root cause) → mark
**confirmed** or **dismissed**. Never graduate a finding to a confirmed bug
unverified.

- **Status:** `needs-triage` · `confirmed` · `dismissed` · `fixed`
- **Priority:** `P1` ship-blocker · `P2` should-fix · `P3` nice-to-have
- **Scope:** defects + verified findings only. Architectural ideas → `FUTURE-IDEAS.md`;
  design questions → `OPEN-QUESTIONS.md`; decisions → `DECISIONS.md`.

| ID | Status | Pri | Summary |
|---|---|---|---|
| B1 | `dismissed` | — | **`/sig:checkpoint` "Epic-ID-as-task-ID" + "only 2 of 5 commits".** Non-bug (triaged 2026-06-06). `isStateStale` deliberately filters the commit walk to `STATE_AFFECTING_PATHS` (`state.js:583`, D6 — editing future-ideas / decisions / milestone files is metadata curation, excluded by design), so 2-of-5 counting is correct. `TASK_ID_RE` (`checkpoint.js:25`) intentionally matches every hierarchy level, so `taskIdsInCommits` can include Epic/milestone IDs — harmless: it only filters `current_tasks`, which holds full task IDs. Surfaced from unverified surface behavior during a `markFresh` detour; reading the source dismissed it. |
| B2 | `fixed` | P2 | **FUTURE-IDEAS footer drift — new entries could land below the `*Last updated:*` footer.** Once any content sat below a stranded mid-file footer, `insertAboveFooter` buried every subsequent `/sig:add` above it. **Fixed in v0.1.5 (M4.5.E10.S3):** `insertFutureIdeasEntry` footer-repair (relocate footer to EOF + absorb stranded content, announce) + `lintFutureIdeasFooter` dogfood lint. Migrated from FUTURE-IDEAS 2026-07-13 (v0.1.6.S4). |
| B3 | `fixed` | P3 | **`/sig:add` derived-title cut mid-clause.** `deriveHeading` (`add.js`) sliced the first ~6 words / 60 chars, landing mid-clause (the "STATE.md append-without-evict" capture headed "closed-work narrative must"). **Fixed in v0.1.6 (FR4, `16155b3`):** prefer the first em-dash/`.`/`:`/`,` boundary (followed by whitespace; em-dash not hyphen; ≥20-char floor) before the length cap, word-slice fallback. Shared helper → all 3 capture destinations. |
| B4 | `fixed` | P2 | **Drain disposition-detector missed blockquote promotions.** The 2026-07-04 backlog review stamped promotions as `> **Promoted … → …**` blockquotes, which neither `HEADING_DISPOSED_RE` nor `STATUS_DISPOSED_RE` recognized — so promoted entries resurfaced on every `/sig:plan` drain (~42 candidates, incl. 6 already promoted). **Fixed in v0.1.6 (FR3, `df4fdb4`):** `parseEntries` scans the entry's header region for a `^`-anchored, fence-aware leading blockquote stamp. Live drain candidates 43→37 (FR3 alone; 35 after FR5 removed 3 entries). |

| B5 | `fixed` | P3 | **`npm run lint` is broken — ESLint v9 with no flat config.** `package.json` pins `eslint ^9.0.0` (installed 9.39.4), which requires an `eslint.config.js`; none has ever been committed, so `npm run lint` exits 2 (`ESLint couldn't find an eslint.config.(js\|mjs\|cjs)`). **Pre-existing** (predates v0.1.6 — no lint config in git history; v0.1.6 touched none of it). Not a blocker: `npm run validate` (the plugin validator + 889-test vitest suite) is the effective quality gate. Surfaced during v0.1.6 VERIFY (2026-07-13). Fix = add a flat `eslint.config.js` (separate chore). |
| B6 | `fixed` | P3 | **`/sig:resume` + `/sig:status` origin-drift banner false-positives on your own just-pushed checkpoint commit.** `isStaleVsOrigin` compares `origin/main` to STATE's `last_updated_commit`, **not** to local `HEAD`. A STATE-writing commit leaves `last_updated_commit` one behind `HEAD` (it records the last *work* commit; the bookkeeping commit is the "+1"), so right after a clean checkpoint+push, resume shows `⚠ origin is 1 commit ahead … git pull` even though local `HEAD` == `origin/main` and there is nothing to pull. Confirmed 2026-07-15 (M4.5.E11.S1 close): `{stale:true, aheadCount:1, touchedPlanning:true}` while `git rev-list HEAD..origin/main` = 0. Advisory + fail-open, so harmless — but the "git pull" advice is misleading at the exact resume-trust moment E10 was built to protect. **Fix:** gate the banner on `git rev-list HEAD..origin/main > 0` (a genuine remote push — local actually *behind* origin), not merely on the STATE baseline lagging. Adjacent to M4.5.E10/E11 resume-trust work. *(Re-observed 2026-07-16 during the M5.E1 `/sig:resume` — both the local-stale and origin-drift banners fired on the `markFresh after PLAN close` commit while local HEAD == origin/main; same root cause. The local-stale side is the same "+1 bookkeeping commit" lag.)* |
| B7 | `fixed` | P2 | **`plugin.json` version stuck at `0.1.6` through the entire v0.1.7 release — clean main fails `install-contract.test.js`.** At `HEAD` **and at the `v0.1.7` tag itself**, `.claude-plugin/plugin.json` reports `"version": "0.1.6"` while `CHANGELOG.md` (`[0.1.7] — 2026-07-15`), the `v0.1.7` git tag, and `marketplace.json` `source.ref` all say v0.1.7. `install-contract.test.js:66` (`expect(marketplace…source.ref).toBe(\`v${plugin.version}\`)`) fails on a clean checkout: ref `v0.1.7` ≠ `v0.1.6`. **User-visible:** a `/plugin install` of v0.1.7 self-reports version 0.1.6 in the plugin list. **Root cause:** the M4.5.E11 SHIP (v0.1.7) bumped CHANGELOG + tag + marketplace ref but missed `.claude-plugin/plugin.json` (last bumped at `7302c21 Signal v0.1.6`). Confirmed 2026-07-16 (M5.E1 EXECUTE baseline run: 998 pass / 1 fail). **Secondary drift (needs a look, not the test cause):** `marketplace.json` pins `sha: 8e05e56…` but `v0.1.7` resolves to commit `0eb8ca8…` — the pinned sha ≠ the tag commit. **Fix:** bump `plugin.json` → `0.1.7` (and reconcile the marketplace `sha` if the drift confirms) on a standalone release-hygiene commit; not M5.E1 scope. |

| B8 | `fixed` | P2 | **v0.1.6 write-guard wedges pre-existing prose files, and its reason message hides the only escape.** The FR1 guard evaluates *whole proposed content*, not the delta (`v0.1.6-PLAN.md:9` R1.2 option (a)) — justified at PLAN by "**FR1 is self-unblocking** — a cleanup edit to clean frontmatter always passes (AC1.7)" (`v0.1.6-PLAN.md:11`). **That claim is true but load-bearing on knowledge the user never gets.** Because the block is whole-file, a file with N prose entries only unblocks via **one atomic write that de-proses ALL N at once**; fixing them one at a time — the natural instinct, and what an Edit-driven agent does by default — fails on *every* intermediate attempt, so the file presents as permanently bricked. The reason string ("move the narrative into the STATE.md body below the frontmatter, then re-write") never says the block is whole-file or that incremental cleanup cannot work. **Confirmed live 2026-07-16** on `eval-project-A/.planning/STATE.md` (529 KB; 12 `completed_phases` entries, **10 prose**, one 55 lines; `completed_phases` = 93 of 107 frontmatter lines): Brett reports STATE writes blocked in that repo — every Signal command that writes STATE is stopped. Escape hatch verified against the real file: one edit replacing the whole entry list with 12 single-line ≤150-char scalars flips the guard `block:true → block:false`, frontmatter 107→27 lines, **body byte-identical**. Note remediation is **two Epics out** — vector-1 de-prose is routed to E3/FR6 (`references/doc-runtime-model.md` §3), so v0.1.6 shipped a hard block whose only interim path is undocumented manual surgery. **Fix (cheap, no design change):** extend the reason string to state the block is whole-file and that all offending entries must be fixed in a single write; consider naming the count/first offending entry. Guard semantics stay as-is — the self-unblocking design is sound, only its discoverability fails. **→ Mitigation landed 2026-07-16 (this commit):** all four guard reason strings now append a whole-file note ("this check reads the whole file, so if more than one entry is affected they must all be fixed in a single save — correcting them one at a time will fail on every attempt") via a shared `WHOLE_FILE_NOTE` const in `retrospective.js`, with a B8 regression test (`hook-state-write.test.js`). The *discoverability* half is closed; the *auto-remediation* half (a one-shot de-prose of a wedged file) remains the migrate command's job — **pulled forward to M5.E2** (was E3/FR6) per the 2026-07-16 sequencing pivot. B8 stays `confirmed` until the migrate command lands. |

| B9 | `fixed` | P2 | **`setCurrentEpic` doesn't reset `phase`/`completed_phases`/`last_completed_task` on an Epic roll — every 2nd+ Epic opens with stale phase state.** `setCurrentEpic` (`state.js:577`) resets only `current_wave`→null and `current_tasks`→[] on a roll (`state.js:595-596`); it leaves `phase`, `completed_phases`, and `last_completed_task` at the *previous* Epic's values. The E11 `--epic` open path (`commands/discuss.md` §"Epic mode") calls `setCurrentEpic` and documents only the wave/tasks reset, so opening a new Epic into DISCUSS leaves e.g. `phase: SHIP` + the prior Epic's `completed_phases`. The first phase-gate (`transitionPhase`) then *appends* to that stale list — polluting the new Epic's `completed_phases` with the prior Epic's phases + `SHIP`. **Confirmed 2026-07-16** (M5.E2 open): after `setCurrentEpic(base,"M5.E2")` the roll left `phase: SHIP` + `completed_phases: [DISCUSS,PLAN,EXECUTE]` from M5.E1 (had to hand-fix to `phase: DISCUSS` + `[]`). Triaged how M5.E1 itself came out clean: its first post-open commit (`0fff9e5`) already shows `completed_phases: [DISCUSS]` + `last_completed_task: null`, i.e. it too was reset out-of-band (manual) since no tool does it — so the gap is real, not a one-off. **Fix:** `setCurrentEpic`, on a roll (not the idempotent same-Epic case), should also reset `phase`→null (or the caller sets DISCUSS), `completed_phases`→[], and `last_completed_task`→null atomically under the same lock; update `commands/discuss.md` + `commands/new-project.md` Epic-open docs to match. Candidate to fold into M5.E2 (touches the same state-mutation core) or a standalone E11 follow-up. **→ Fixed 2026-07-18 (`22abe65`, M5.E2.S1.t0):** on a roll (not the idempotent same-Epic case) `setCurrentEpic` now resets `phase`→null, `completed_phases`→[], `last_completed_task`→null atomically under the state lock (`blockers` preserved — a blocker can span Epics); the caller sets the real phase immediately after via `transitionPhase`, which records a clean per-Epic `completed_phases`. `commands/discuss.md` + `new-project.md` Epic-open docs updated to match. Verified live 2026-07-18 opening M5.E3: clean `phase: null`→DISCUSS, zero stale M5.E2 leak. *(Ledger correction: the fix shipped inside M5.E2 but this entry wasn't flipped at the time — caught opening M5.E3.)* |

| B10 | `fixed` | P2 | **`/sig:migrate-memory` doesn't archive `SHIP` (or `LAUNCH-KIT`) scaffolds — closing docs orphaned in root while their Epic's siblings move to `archive/`.** `SCAFFOLD_SUFFIXES` (`tools/lib/archive-tree.js:50`) = `[REQUIREMENTS, RESEARCH, PLAN, PROGRESS, VERIFICATION, VALIDATION, REVIEW]` — no `SHIP`. Inherited verbatim from the `archive-migrate.mjs` prototype's `SCAFFOLD` literal (written before Signal had `-SHIP.md` docs). **Confirmed 2026-07-17** via the S4.t1 dogfood dry-run on Signal's own `.planning/`: 28 moves proposed for M4.5.E5 / M4.5.E10 / M4.5.E11 / M5.E1, but `M4.5.E5-SHIP.md`, `M4.5.E11-SHIP.md`, `M5.E1-SHIP.md`, and `M4.5.E5-LAUNCH-KIT.md` are left in root — an incomplete/inconsistent archive (NOT data-loss; relocate-never-delete holds, nothing deleted). **Fix:** add `SHIP` to `SCAFFOLD_SUFFIXES` (+ a non-M4.5 fixture test); decide `LAUNCH-KIT` (one-off — archive-with-Epic or keep-in-root is a judgment call). Should fix BEFORE the S4.t1 apply so the dogfood produces a complete archive. **→ Fixed 2026-07-17 (`9064340`, S2.t6):** added `'SHIP'` to `SCAFFOLD_SUFFIXES` (RED-first on a non-M4.5 `M6.E1` fixture); re-dry-run on Signal's `.planning/` now = 31 moves (the 3 `-SHIP.md` docs archive with their Epics); `RETROSPECTIVE` + `LAUNCH-KIT` stay in root. 1258→1260 tests. |
| B11 | `fixed` | P3 | **`vector-2-defer` flag over-fires — one spurious flag per historical closed Epic on any already-evicted corpus.** `classifyClosedEpicBody(stateBody, epicId)` returns `vector-2-reclassify` whenever a closed Epic has no section heading in the *current* STATE body — but on a healthy/mature project the STATE body holds only current state, so **every** past closed Epic (whose narrative long ago moved to its retro + archive) trips it, emitting `FLAG: vector-2-defer — <Epic> … handled by the vector-2 whole-body relocate, not V3` with **no corresponding move** (`vectors:[]`, `v3.evicts:0`). **Confirmed 2026-07-17** via S4.t1 dry-run on Signal's `.planning/`: 12 vector-2-defer flags, zero backing relocations. Advisory noise, no data impact — but it makes the dry-run's flag section useless-to-misleading on any real project. **Fix (REVIEW):** only emit `vector-2-defer` when there is actual un-sectioned closed-Epic narrative *present in the body* to relocate, not for every historical Epic absent from it. **→ Fixed 2026-07-18 (`dd77ef1`):** `planVector3` case 4 now gates the flag on `senseInlinedBody(stateText).candidate` (the same predicate `applyMigrate` uses for the whole-body relocate) — fires iff the relocate it defers to will actually run. Skeleton/already-relocated → 0 flags (eval-project-A real dry-run 12→0); genuine inline narrative → still fires. Advisory-only; noop/idempotency/vectors unchanged. |

| B12 | `fixed` | P2 | **Vector-1 de-prose leaves a generic `[relocated…]` placeholder (not a meaningful label) for a `completed_phases` entry that lacks a leading `PHASE (date)` token — and sweeps an *active* marker into history.** The de-prose keeps a short scalar by grabbing the leading `PHASE (date)` prefix; an entry without one falls back to `"[relocated to STATE body — migrate-memory]"`, erasing which entry it was (the prose is preserved verbatim in the body — **no data loss** — but the frontmatter label is useless). **Confirmed 2026-07-18** via the S4.t2 eval-project-A full-migrate on a throwaway copy (546 KB → 1.3 KB STATE.md): the entry `"**▶ Active: Slice SEC1 — Supabase Security-Advisor Hardening: DISCUSS ✅ → …"` (a free-form active-work marker parked in `completed_phases`) placeholdered, and its prose relocated to `STATE-HISTORY.md` — i.e. an **active** item filed into **history** (exactly the "don't bury a live open item" case the S1.t2 carry-forward flagged for the S4 eyeball). **Fix (two parts):** (1) derive a meaningful truncated label from any entry, never a generic placeholder; (2) DECIDE (product) whether an entry that reads active (`▶ Active:`/similar) should be flagged/skipped rather than swept to history, or whether `completed_phases` is always fair game (→ data-hygiene guidance instead). Surfaced by the real dogfood — a "battle-tested" find. Fix before the release. **→ Fixed 2026-07-18 (`e42afed`):** new `deriveNonStandardLabel` (mirrors `/sig:add`'s clause-boundary `deriveHeading`: first clause ≤60 chars, 6-word fallback) for any `completed_phases`/`blockers` entry lacking a `PHASE (date)` prefix — never the generic placeholder; idempotent; conservation unchanged. `renderDryRun` now emits `⚠ Non-standard completed_phases entries relocated (N) … verify these were actually complete` (product call: flag-in-dry-run, still relocate). Verified on the real eval-project-A file: SEC1 entry → label `"▶ Active: Slice SEC1"` + warning fires; 0 placeholders. 1264→1269 tests. |
| B13 | `fixed` | P3 | **`tools/lib/migrate-memory.js` has a literal NUL byte (~offset 57833) that makes `grep` treat the whole file as binary (returns nothing).** In `computeDanglingDelta` a map key uses a raw NUL delimiter `` `${d.file}<NUL>${d.link}` `` written as an actual NUL in source, not the `\0` escape. Functional (the key works), but plain `grep`/`grep -n` on the file silently returns nothing — every agent working M5.E2 hit this and had to fall back to `rg -a`/node (it's the "grep glitchy in this sandbox" note that recurred). Dev-experience regression, not a runtime defect. **Fix:** replace the raw NUL with the `\0` escape sequence (byte-identical behavior, restores greppability). Surfaced during M5.E2 VERIFY (B12 work), 2026-07-18. **→ Fixed 2026-07-18 (`50ad065`):** raw NUL → `\0` escape at the `computeDanglingDelta` key; byte-identical behavior, `grep` restored (38 exports now visible, was 0). **→ Recurrence in `.planning/BUGS.md` itself, fixed 2026-07-25 (found at `/sig:resume`):** this entry re-introduced the byte — the writeup above pasted the offending source snippet verbatim, NUL included, so **BUGS.md became grep-blind** from `5062526` until this fix (`grep -c needs-triage .planning/BUGS.md` → no output, exit 1; `grep -a` → 8). Sharper edge than the source case: BUGS.md is the file agents grep to check bug status, so the failure mode is a silent "no open bugs" read. Fixed by rendering the delimiter as the placeholder `<NUL>`; Node/`fs` reads were never affected. **Lesson:** never paste a raw control byte into a bug writeup — describe it. |
| B14 | `fixed` | P3 | **Lexical (symlink-blind) path confinement in `evict.js` / `add.js` / `resume.js` — the codebase-wide sibling of the M5.E2 symlink escape.** The `resolve(dest).startsWith(planningRoot + sep)` lexical guard (no `realpathSync`) lets a checked-in **directory symlink** inside `.planning/` escape the tree on write/move. **M5.E2 REVIEW fixed this in `migrate-memory.js` + `archive-tree.js`** (commit `ab2242d`: `realpathNearestExisting` + `assertRealInsidePlanning`), but the same pattern remains at `tools/lib/evict.js:350-351` (`evictEpicNarrative` archive path — closest sibling, same mkdir/write/move shape), `tools/lib/add.js:830` (capture path), and `tools/lib/resume.js:74` (read-side filter, lower severity). Confirmed 2026-07-18 by the M5.E2 security audit. **Fix:** apply the same `realpath` re-assertion to those write/move gateways (read-side is lower priority). Separate ticket — deferred to avoid scope-creeping the E2 REVIEW. **Why safe to defer for E2:** `/sig:migrate-memory` never routes a write through `evict.js`'s vulnerable path — §9 forbids calling self-locking `evictEpicNarrative` (the engine uses the lock-free `relocateFaithful` spine, which E2 REVIEW hardened), so this codebase-wide gap is genuinely NOT in this command's attack surface. |
| B16 | `fixed` | P3 | **A failed/rolled-back `--apply` in git mode now leaves a lingering `pre-migrate-memory-<stamp>` tag.** Introduced by the C1 rollback fix (`50ad065`): the pre-apply tag is hoisted to run *before* the mechanical phase so non-git modes have a recovery pointer. On a successful apply the tag is the intended revert pointer (correct, printed in `revertLine`). But on a **rolled-back** apply the tag now persists (pointing at the restored pre-apply state) — harmless, but it accumulates across retries in a stranger repo. **Fix:** clean up the tag in the rollback catch (or make it idempotent per-run). P3 — note in SHIP notes. Surfaced 2026-07-18 (M5.E2 REVIEW, Batch 1 handoff). |
| B15 | `fixed` | P3 | **`FILE_SCAN_CEILING` (1 MB) truncation can silently defeat the blocking dangling-link gate on a >1 MB single doc.** `scanDanglingLinks`/`scanResidualFlatPaths`/`scanAtRiskAnchors`/`scanUnhandledLinkForms` (`migrate-memory.js` ~:1350-1495) `slice(0, 1MB)` before matching. A migrate-introduced dangle sitting **past the 1 MB mark** in a single `.planning` doc is never seen → the apply commits a dangling link the gate was supposed to catch. **Bounded/extreme:** the real corpora are well under 1 MB (eval-project-A 546 KB), so no shipping repo hits it today; needs a >1 MB single file. Confirmed 2026-07-18 (M5.E2 REVIEW test-adequacy audit — the truncation branch is also untested). **Fix:** for dangling detection specifically, scan the whole file (or a much higher ceiling), or flag when truncation occurs so a silent pass is impossible. Separate ticket. |

| B17 | `fixed` | P3 | **4 git-heavy migrate tests flake on vitest's 5 s default timeout under full-suite parallel load.** `migrate-symlink-confinement.test.js` + `migrate-apply*.test.js` (and 1–2 siblings) do real `git init` / `execFileSync` per test; when all 81 files run in parallel (`npm test`) a few intermittently hit `Test timed out in 5000ms`. They pass clean in isolation and on a second full run — **not** a product defect, the tests are correct. **Confirmed 2026-07-18** during the M5.E2 SHIP pre-ship checklist: run 1 = 4 failed / 1296 passed (all 4 were 5000 ms timeouts in migrate suites); run 2 = 1300 passed. There is no `vitest.config.*` (default 5 s applies). Dev-experience / CI-flake only. **Fix:** give the git/fs-heavy migrate suites an explicit `testTimeout` (~15 s) — a `vitest.config.js` with `test.testTimeout`, or the per-`it` third arg on the offending tests. Deferred out of the SHIP commit (introducing test-infra config isn't in a migrate-command SHIP's surgical scope). |

| B18 | `fixed` | P3 | **`regeneratePlanningIndex('.')` / `enumeratePlanningDocs('.')` corrupt INDEX.md when `baseDir` is `'.'`.** The doc-key derivation does `full.slice(baseDir.length + 1)` assuming `baseDir + '/'` prefixes every walked path. With `baseDir='.'`, `path.join('.', '.planning')` normalizes to `.planning` (no `./`), so `baseDir.length` is 1 and `slice(2)` chops the first two chars → mangled keys (`lanning/DECISIONS.md`), a `{written:true}` diff, and garbage written over INDEX.md. **Latent, not a live break:** every shipped caller (`migrate-memory.js` tail regen, `ship.md`, `commands/index.md`) passes an absolute / `process.cwd()` baseDir, so the canonical path is unaffected; only a `'.'` (or other non-prefix-normalized) baseDir triggers it. Confirmed root cause 2026-07-19 (M5.E3 S6b dogfood curation — bit the curation pass once; recovered from the staged copy). **Fix:** `resolve(baseDir)` at entry (or compute the rel key via `path.relative(baseDir, full)` instead of `slice`). Separate ticket. |
| B19 | `fixed` | P2 | **The v3 migrate's tail `regeneratePlanningIndex` silently clobbers a pre-v3 hand-curated INDEX.md.** `applyMigrate`'s composed flow ends with an unconditional `regeneratePlanningIndex(baseDir)` (the FR3 SHIP/migrate reconcile). `parseExistingAnnotations` preserves curated notes in the NEW list format, but **cannot read the OLD table format** — so a project migrating from a richly hand-curated INDEX (Signal's own: a tier legend + per-file gotchas + per-Epic one-liners) has every note wiped to `_(note pending)_` on first migrate, with no warning and no diff surfaced in the dry-run (a verbatim-move preview shows no INDEX prose change). Confirmed 2026-07-19 (Signal dogfood — 89-line curated INDEX → 165-line auto-gen, 1 Gotcha → 0, 143 note-pending; repaired manually by porting the still-valid notes, `exec-s6b-curate`). **General-project risk:** any stranger repo with an old-format curated INDEX loses it silently. **Blast radius WIDENED by the stamp-null fix (`6dc7fa2`):** before it, only stamp-2 repos armed the migrate; now EVERY existing external project (all stamp-null at the combined release) routes through v3 on first `/sig:migrate-memory`, so this silent-INDEX-clobber is the **default path for all of them**. Real-world exposure is still small (few external repos hand-curate INDEX), but REVIEW should decide whether it **gates the release** vs. ships as a documented limitation. **Fix candidates:** (a) detect an unportable-format INDEX and SKIP the auto-regen (leave the curated file, flag for manual `/sig:index`); (b) surface the INDEX rewrite in the dry-run diff so it's human-approved; (c) a one-time old→new annotation-format migrator. REVIEW item (M5.E3). |

| B20 | `fixed` | P3 | **`npm run lint` is non-functional under ESLint 9 — no flat `eslint.config.js`.** `eslint tools/` exits **2** with "ESLint couldn't find an eslint.config.(js\|mjs\|cjs) file" (ESLint 9.39.4 dropped `.eslintrc.*` support; the repo has neither a flat config nor a legacy one). So the VERIFY "linter passes" criterion cannot be satisfied — code quality is instead covered by the clean `npm run build`, the 1481-test suite, and the FR4 doc-hygiene guard. **Pre-existing, not M5.E3:** `CONTEXT.md` references this as "`BUGS.md` B5 (lint tooling)" but no B5 entry actually exists in this file (lost in an earlier edit) — re-cataloged here. Confirmed 2026-07-19 (M5.E3 VERIFY). **Fix:** add a flat `eslint.config.js` (ESLint 9 format) importing the intended rule set; a separate ticket (introducing lint config isn't in M5.E3's surgical scope). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **this row was mis-statused as `dismissed`.** It is not a non-bug — it is a duplicate of **`B5`** (the entry it says is missing does exist, at the top of this table), and the bug it describes is **fixed**: `eslint.config.js` is present at the repo root and `npm run lint` exits clean on `tools/` (verified 2026-08-02). Kept rather than deleted so the ID does not dangle; treat `B5` as the canonical entry. |

| B21 | `fixed` | P3 | **`resolveDecisionId` rebuilds the whole D-ID map from disk on every call; the FR5 anchor gate calls it once per evicted ID.** `migrate-memory.js:2803-2804` loops evicted IDs and calls `resolveDecisionId` (→ `buildDecisionIdMap` walks `archive/` + reads every DECISIONS.md + regexes the corpus) per ID → O(N_ids × full-corpus-rebuild). Fine at Signal's scale (73 IDs × ~183 KB, one-time migrate) but drags on a project with thousands of decisions. **Fix (~5 LOC):** build the map once before the gate loop, resolve in-memory — safe *here* because eviction has already written to disk before the gate runs (map is stable across the loop); leave the mechanical-fresh `resolveDecisionId` for the standalone/self-heal callers. Deferred (Suggestion, M5.E3 REVIEW rev-code). |
| B22 | `fixed` | P3 | **`doc-hygiene.js` internal-link check touches disk outside the walk root.** `checkInternalLinks` resolves `](target)` to `resolve(dirname(f), pathPart)` (`:110`) then `existsSync` + (anchors) `readFile` (`:361`) — a `](../../../x.md#a)` target reads outside the repo. Read-only, offline (AC4.3 no-network independently verified), runs only in Signal's OWN test suite → no exfil, but an unbounded disk touch. **Fix (~2-3 LOC):** bound the resolved target to repo root before existsSync/readFile. Deferred (Suggestion, M5.E3 REVIEW rev-sec). |
| B23 | `fixed` | P3 | **M5.E3 REVIEW test-hardening follow-up (bundle).** Non-blocking coverage gaps the panel flagged: (a) the **organic re-root-to-missing-target** path — CHECK-ITEM 2 re-roots to a planted *existing* sibling; the dangling/rollback tests INJECT synthetic dangles, so "re-root produces a real dangling link → non-injected `enforceNoDangling` catches → abort+rollback" is never exercised (inert on Signal — 0 inline `](*.md)` in DECISIONS — but live for external repos). (b) the **>1 MB `FILE_SCAN_CEILING` truncation** branch (`doc-hygiene.js:102`, `migrate-memory.js:1482`) has no fixture crossing the 1 MB cap (largest is 200 KB) — also the B15 class. (c) **no rollback test asserts `git diff --cached` is empty** after a mid-phase abort — correctness holds structurally (staging is post-gate) but the "do NOT reorder" ordering (`migrate-memory.js:2263`) is comment-protected, not test-protected. (d) **`promoteDrainEntry` batch re-run:** verify the `/sig:plan` drain does NOT re-select already-`→ Promoted` terminal entries (once stamped, the block's sha1 no longer matches its dedupe marker, so a re-promote would duplicate; the selection lives in `commands/plan.md`, outside the E3 code diff). Plus 2 cosmetic nits: `backlogKey` → `blockKey` (used for both backlog + bugs markers); `promoteToBugs` dedupe-marker string-replace on rendered output is fragile if `buildBugsEntry`'s template changes. Confirmed 2026-07-19 (M5.E3 REVIEW panel). |
| B24 | `fixed` | P2 | **A pre-existing broken `](*.md)` link inside a strictly-closed-milestone DECISIONS section blocks `applyMigrate` — it aborts + rolls back byte-identical, violating the FR6.3 "a pre-existing dangle must NOT be attributed to the migrate" contract.** The append-log evict relocates the closed block to `archive/M{n}/` AND re-roots the still-broken link, so `computeDanglingDelta`'s `file\0link` key can't subtract it against the pre-apply baseline (both the file path and the link text changed) → it's flagged as a NEW dangle. **Fail-SAFE** (never a partial write) but **over-eager**: any external repo whose closed DECISIONS history carries ONE broken `](*.md)` link is blocked from migrating. Cross-check: `renderDryRun` lists this same dangle under "pre-existing dangling" (not the migrate's fault), yet apply aborts on it — a dry-run/apply divergence. **Discovered + reproduced 2026-07-20** (M5.E4.T4.1, `8fec7bf` — asserts current behavior in `migrate-dangling-baseline.test.js`, does NOT endorse it). Inert on Signal (0 inline `](*.md)` in DECISIONS.md). **Fix (deferred — out of M5.E4's known-bug scope):** make baseline-subtraction survive the block-move+reroot (key the delta on the decision ID / original link, not `file\0link`), OR exempt links inside evicted-and-rerooted closed blocks from the gate. Candidate for a v0.1.10 fast-follow. |
| B25 | `fixed` | P3 | **FR5 AC5.2 has no behavioral read-enclosure test — `rmw-lock.test.js`'s throw-under-held-lock tests pass even for a wrapper that reads OUTSIDE its lock** (a silent lost-update — the exact bug FR5 exists to kill). Read-enclosure IS correct (confirmed by inspection ×3 during REVIEW: all 6 wrappers are `withStateLock(baseDir, () => XxxCore(…))` with the read inside `Core`) and disclosed in `M5.E4-VERIFICATION.md`, so the *behavior* is right — this is a **test-strength** gap only. A true proof needs an interleaving test (writer A pauses after its read → B commits → A resumes → assert A fails-fast or incorporates B), which requires an injectable pause-hook in each Core (none today). Surfaced by the M5.E4 REVIEW test-integrity panel (2026-07-21). Deferred to v0.1.10. |
| B26 | `fixed` | P3 | **`shipFR1Check` returns `{skipped:true, 'not an Epic-close'}` for a FULLY-completed Epic on the Signal-on-Signal self-hosted flow — the flagship FR1 retro gate silently skips.** At M5.E4's SHIP (all 6 phases done, Epic complete), `shipFR1Check({state, profile, milestoneContent, baseDir})` returned `{halt:false, skipped:true, reason:'not an Epic-close SHIP…'}`, so the retro enforcement did NOT fire. The M5.E4 retro was written manually (correct), but the hard-block designed to prevent a missing retro didn't engage — the Epic-close detection likely relies on a `MILESTONE-{n}.md` close-signal the hand-managed flow (Epic-prefixed artifacts, hand-set `current_epic`) doesn't populate. **General risk:** a hand-managed / self-hosted project could close an Epic with no retro and no gate firing. Surfaced 2026-07-21 (M5.E4 SHIP). Needs triage: inspect the Epic-close predicate in `shipFR1Check` + how it reads the milestone file. |
| B27 | `fixed` | P3 | **An archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts `applyMigrate` — fail-safe but blocks a stranger repo's migrate; in tension with R7.** Surfaced 2026-07-21 during **M5.E5.T1** (B24) while building the AC1.3 gate-strength fixture. B24's resolved-target delta correctly treats a link that *resolved* before the migrate and *dangles* after (its target renamed by FR6) as migrate-introduced → abort + rollback. But R7 exempts archive **prose** flat-paths from the residual scan as "historical, not links to repair" — it does **not** exempt archive **inline** `](*.md)` links from the dangling gate. So an external repo whose archive doc inline-links a file FR6 later renames (e.g. `](../../FUTURE-IDEAS.md)` → `ISSUES-INBOX.md`) is blocked from migrating. Inert on Signal (0 such links). **Triage question:** should archive-doc inline links to renamed targets be a *flag* (matching R7's prose exemption) rather than an abort? Needs a design call — not M5.E5 scope. **→ Fixed in M5.E6 (T11/T12, `5ec8a81`+`61f4b67`):** migrate dangling-gate now FLAGS (not aborts) archive-inline links whose resolved target ∈ renameFroms — a tight AND (`isUnderArchive && archiveExemptFroms.has(abs)`), mirroring R7's archive-prose exemption; surfaced via a distinct dry-run line + apply warnings. Migrate-INTRODUCED dangles still abort+rollback (a tightness pair + a residual-abort discriminator prove the gate still bites). |
| B28 | `fixed` | P3 | **An absolute-path `](/abs/foo.md)` `.md` link that is a pre-existing dangle inside an evicted closed block false-aborts `applyMigrate` — the B27-sibling for absolute links.** `scanDanglingLinks` resolves the target with `resolve()` (absolute *wins* → `/abs/foo.md`, abs key `../abs/foo.md`) while `computeLinkEdits` reroots via `posix.join()` (absolute *concatenates* → the link is rewritten `/abs/foo.md` → `../../abs/foo.md`, abs key `.planning/abs/foo.md`). The before/after abs keys diverge → the pre-existing dangle is mis-attributed as migrate-introduced → abort + rollback. **Confirmed empirically 2026-07-21** (M5.E5 REVIEW code-quality panel H1 + a path-math probe). **Fail-SAFE** (blocks the migrate, never a silent escape) and **not a regression** (it false-aborted pre-B24 too; B24 fixed the *relative*-link case). Near-zero occurrence — Signal never authors absolute-path `.md` links; needs a hand-authored one as a pre-existing dangle in a closed DECISIONS block in a stranger repo. Deeper cause: `computeLinkEdits` mangles absolute links (rewriting `/abs/foo.md` to point at `.planning/abs/foo.md`) — a pre-existing reroot quirk, not B24. **Fix (deferred — arch ripple into the reroot machinery, out of M5.E5's surgical scope):** make `scanDanglingLinks`/`computeLinkEdits` agree on absolute targets (leave absolute links unrewritten — they resolve identically regardless of file location), OR exempt absolute-path links from the gate. Same disposition as B27. **→ Fixed in M5.E6 (T10, `cf352f9`):** `computeLinkEdits` now skips absolute-path (`/…`) targets (fix-divergence) — a pre-existing absolute-path dangle inside an evicted closed block migrates byte-identical instead of aborting. Distinct fix from B27 (both R7-consistent). |
| B29 | `fixed` | P3 | **The `_afterRead` FR5 test seam destructures `opts._afterRead` (resolves inherited props) — an unreachable prototype-pollution gadget; add an own-property/`typeof` guard (defense-in-depth).** The 6 RMW Cores (`drain.js:476,615,755`, `checkpoint.js:316`, `retro-index.js:235,372`) read `opts._afterRead` and, if truthy, `await` it inside the coarse state lock. **Currently unreachable** (M5.E5 REVIEW security audit, OWASP A08): 0 prod callers pass it (none in `commands/`/`hooks/`), and JSON/CLI input cannot carry a function — only a pre-existing `Object.prototype._afterRead` *function* pollution (which requires code-exec already) could trigger it; worst case is a `TypeError` inside a lock whose promise rejects (fail-safe, lock released) — not RCE, not lock-DoS. **Fix (deferred per the auditor — non-blocking hardening):** gate the seam on `Object.hasOwn(opts, '_afterRead') && typeof opts._afterRead === 'function'`, or key it on a `Symbol`. Surfaced 2026-07-21 (M5.E5 REVIEW). **→ Fixed in M5.E6 (T19, `78c433e`):** the `_afterRead` test seam is gated on `Object.hasOwn(opts,'_afterRead') && typeof …==='function'` at all 6 RMW sites (own-property, not a destructured local) — a prototype-polluted `_afterRead` no longer fires. RED-proven across all 6 paths; the B25 interleaving suite stays unchanged-green. |
| B30 | `fixed` | P2 | **The FR1 retro pre-check (`/sig:ship` Step 0.5) runs BEFORE the SHIP transition (Step 5), so on a fresh REVIEW→SHIP flow it sees `phase=REVIEW` and skips — B26's STATE-based Epic-close fallback (which requires `phase===SHIP` + REVIEW in `completed_phases`) only fires once phase is already SHIP.** Surfaced 2026-07-21 **dogfooding the B26 fix on M5.E5's own SHIP:** `shipFR1Check` returned `{skipped:true}` at `phase=REVIEW` (Step-0.5 pre-check), then `{halt:true, NO_RETRO_FILE}` after `transitionPhase('SHIP')` set `phase=SHIP` + appended REVIEW to `completed_phases`. So B26's fix is *correct* but its real-world firing depends on phase already being SHIP at the gate. A forgetful maintainer on a fresh flow gets no enforcement at the pre-check, and the programmatic `transitionPhase` at Step 5 bypasses the Layer-2 `PreToolUse` hook. **Fix candidates:** (a) `/sig:ship` transitions to SHIP *before* its own FR1 pre-check; (b) `isEpicCloseByState` also treats "current phase = the last non-skipped pre-SHIP phase + all earlier phases complete" as about-to-close; (c) re-run the pre-check post-transition. Residual of B26 — the fix works once phase=SHIP. **→ Fixed in M5.E6 (T15, `0b1ef86`):** `shipFR1Check` fires the retro gate on a fresh REVIEW→SHIP flow via an in-memory synthesized post-SHIP state (approach (c), persist-nothing) — an Epic can no longer ship with no retrospective. Tight 3-conjunct `aboutToClose` AND; no false-fire on SPIKE / maintained-pending / SKETCH-short / mid-flow-EXECUTE (each mutation-witnessed). (c)-recovery: nothing is persisted on halt, so `/sig:resume` re-orients cleanly. |

| B31 | `fixed` | P3 | **`/sig:add` and drain RMW the same `ISSUES-INBOX.md` under DIFFERENT locks (`.add.lock` vs `.state.lock`) → concurrent lost-update.** Found 2026-07-23 during **M5.E6 PLAN** (FR7 lock-status verification). `add.js:46` takes `.planning/.add.lock` (30s TTL, sized for the interactive capture prompt); drain's `promoteDrainEntry`/`evictTerminalToLedger`/`applyDispositionToFile` take `.planning/.state.lock`. Different mutexes = **no mutual exclusion**, so a concurrent `/sig:add` + `/sig:plan`(defer/delete) or `/sig:ship`(evict) can lost-update the inbox (data-loss of a captured idea; `atomicWrite` prevents torn writes, not this — recoverable via git, low probability, needs exact concurrent same-repo timing → P3). **NOT covered by M5.E4 FR5's read-enclosure locking** (that put a *single* lock per RMW path; this is a *two-lock mismatch* on one shared file). Sits **inside D-M5E6-3's exact threat model** (Brett runs parallel sessions). The separate `.add.lock` is deliberate — sharing `.state.lock` would let a slow interactive `/sig:add` block STATE writes for 30s — so the fix needs design (candidates: a dedicated short `.inbox.lock` both paths take for the *write step only*; or add's write-phase re-acquires `.state.lock` non-interactively after the prompt resolves). **Disposition = M5.E6 scope decision (see M5.E6 PLAN gate).** **→ Fixed in M5.E6 (T23, `bef3ba3`):** `/sig:add`'s doc-write now runs under `.state.lock` (re-read-inside-lock RMW Core) — mutually exclusive with drain/ship writes to the same files. Pure insert/build helpers stay lock-free (backlog/checkpoint call them already-inside `.state.lock` → no re-entrancy/deadlock); the scrub prompt stays outside under `.add.lock`. `add.test.js` byte-identical. |
| B32 | `fixed` | P3 | **docs/map `COMMANDS` JS array incomplete.** The `docs/map/index.html` `COMMANDS` array (~line 710) is missing `/sig:index` + `/sig:migrate-memory` — 16 enumerated vs the 18-command header after M5.E6.T8. The array is NOT roster-guard-checked (only the header count + CLAUDE.md `#` count are), which is why the gap slipped through since v0.1.8 (was 15-vs-17). Predates M5.E6; surfaced during T8's roster reconcile. Fix: add the missing commands to the array, or bring the array under the roster guard so it can't drift again. Low-risk (visual map only). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live, and the gap has widened.** The `COMMANDS` array in `docs/map/index.html` now enumerates **17** commands against a **19**-command header and 19 files in `commands/`. The two missing are still exactly `/sig:index` and `/sig:migrate-memory` — the count moved 16-vs-18 → 17-vs-19 only because `/sig:update` was added to both. The array is still not roster-guard-checked, which is the actual defect. **→ FIXED 2026-08-03** (fix lane, no Epic). `/sig:index` + `/sig:migrate-memory` added to the array, and — the part that closes the defect rather than the symptom — **`tests/map-roster-reconcile.test.js` now reconciles all three arrays (`COMMANDS`/`AGENTS`/`SKILLS`) against `roster.js` in BOTH directions**, naming the offending entries. RED-first: it failed on the real 17-vs-19 gap, naming both missing commands, before either was added. Marker comments (`// map-roster:begin/end X`) anchor extraction, and a test asserts the markers are present so deleting one fails loudly instead of silently passing on zero blocks (`B39`/`B54`'s shape). The three headings now **render their count from the arrays** (`renderRosterCount`), so the number is no longer an independent claim that can drift — and the three `docs/map` entries were consequently **removed from `ROSTER_SITES`**, mandatory because their regexes stop matching once the HTML holds no digits and `checkRosterCounts` *skips a site whose pattern is absent*, which would have left three guards that look alive and check nothing. Net: one number-check traded for a full both-directions name-set check. Also folded in: the header stamp (`v0.1.9`, and the word *"generated"* — nothing generates that file) is now pinned to `plugin.json` via `VERSION_SOURCES`, with a companion test asserting the stamp **exists**, since a null reader would disable the check rather than fail it. |
| B33 | `confirmed` | P3 | **Stale `LOCK_TTL_MS` comment in `add.js`.** The comment (~lines 42-45) says `.add.lock` "holds the lock across a sensitive-data prompt" — inaccurate even before M5.E6 (the scrub prompt runs BEFORE `.add.lock` acquisition), and now doubly so: B31 (T23) additionally put the doc-write under `.state.lock`. Pre-existing; surfaced during Slice 8. Fix: one-line comment refresh describing the current arrangement (scrub → `.add.lock` interactive TTL → `.state.lock` write). Comment-only, no behavior change. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** The comment above `LOCK_TTL_MS` in `tools/lib/add.js` still reads *"`/sig:add` holds the lock across a sensitive-data prompt"*, unchanged. Comment-only, no behaviour change needed. |
| B34 | `confirmed` | P3 | **`renameFn` injectable seam is the B29-parity prototype-pollution gap.** `drain.js` bare-destructures `renameFn` from `opts` at 3 cores (`applyDispositionToFileCore` :473, `evictTerminalToLedgerCore` :607, `promoteDrainEntryCore` :740) and forwards it to `atomicWrite` (`atomic-write.js:21/27`), which invokes it under the lock — the SAME shape B29 hardened for `_afterRead`, left unguarded on the literal sibling. **Unreachable** (0 prod callers pass `renameFn`; no in-process primitive can pollute `Object.prototype.renameFn` with a *function* — the YAML sink is proven pollution-safe), so same tier B29 was. Surfaced 2026-07-24 by the M5.E6 REVIEW security panel (the "one-seam-over" the evict.js false-green primed for). Fix: mirror B29 — `Object.hasOwn(opts,'renameFn') && typeof opts.renameFn==='function'` at the 3 destructures (root fix; guarding `atomic-write.js` alone fails — drain makes it an OWN prop first), RED-first with a pollution test mirroring B29's. Completes the pollution-class hardening. Not a SHIP blocker. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `applyDispositionToFileCore` still bare-destructures `renameFn` from `opts` (`tools/lib/drain.js:473`) and forwards it to `atomicWrite`, which invokes it under the lock (`tools/lib/atomic-write.js:21,27`) — the shape `B29` hardened for `_afterRead` and left unguarded on the literal sibling. Reachability is unchanged (still zero production callers). |
| B35 | `confirmed` | P3 | **`sweep.js checkIndexFreshness` unwrapped compose/diff can crash the read-only sweep.** Every other sweep check is crash-safe (try-catch→advisory), but the managed-index path (`enumeratePlanningDocs`/`renderPlanningIndex`, sweep.js:86-88) is unwrapped — a throw mid-walk crashes the whole sweep. **Low reachability** (only a Signal-managed, non-empty, non-foreign INDEX.md reaches it — stranger repos early-return at the foreign/absent gate, so the stranger-repo "never crash" contract HOLDS; the gap is a Signal repo with a pathological `.planning/`). Surfaced 2026-07-24 (M5.E6 REVIEW code-quality). Fix: wrap the compose-and-diff in try/catch → advisory (RED-first via a mocked-throw on `enumeratePlanningDocs`), making the never-crash guarantee total. Not a SHIP blocker. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `checkIndexFreshness` remains unwrapped on both ends: its `enumeratePlanningDocs` / `renderPlanningIndex` compose-and-diff has no try/catch (`tools/lib/sweep.js:93-95`), and `runSweep` calls it bare at `:404`. Every neighbouring check is crash-safe; this one is the exception. |
| B36 | `fixed` | P2 | **FR1 retro-gate (`/sig:ship` Step 0.5) silently skips on a stale/non-shipped milestone row — Layer-1 enforcement is structurally inert for a row-maintained project at ship-start.** `shipFR1Check` (retrospective.js:805-808) detects Epic-close via (A) `isEpicCloseShip` (row contains "shipped") OR (B/C) the B26/B30 STATE fallbacks — but both fallbacks are **row-absence-gated** (D-E9-5, "a maintained row wins"). At Step 0.5 the row is never yet "shipped" (shipping hasn't run), so (A) is always false for a row-maintained project; if the row EXISTS but is stale/in-progress (not absent), (B/C) don't fire either → skip. **Confirmed dogfooding M5.E6's own SHIP (2026-07-25):** the E6 row read `🔨 DISCUSS` (never advanced from Epic open), `shipFR1Check` returned `{skipped}` on a real FULL Epic-close with no retro — the Epic could have shipped retro-less if not caught by hand; the gate fired only after the row was marked `shipped`. **Third milestone-row residual after B26 (no row) + B30 (phase timing).** Fix candidates: (a) SHIP marks the row close/"shipping" BEFORE Step 0.5; (b) narrow the row-absence gate so a *non-terminal* row (pending/in-flight/pre-SHIP-phase status) still lets `isEpicCloseByState` win when STATE unambiguously shows close. Layer-2 (`PreToolUse`) + Layer-3 (resume) hooks don't read the row so are unaffected, but Layer 1 is the only cross-runtime layer (D-E9-8). Ironic residual on the Epic that fixed B30. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (S4.t1, FR3.1)** and never flipped. The stale-row skip is now **loud**: `shipFR1Check` returns `{skipped:true, unevaluated:true, cause:"stale-milestone-row"}` with a reason the command must emit verbatim (`tools/lib/retrospective.js:850-876`, `commands/ship.md:38`). Asserted at `tests/ship-fr1.test.js:591`. |
| B37 | `confirmed` | P2 | **`/sig:checkpoint --context` (and `resume.md`, `discuss.md`) instruct a single-home violation — D16's dual-write was never reconciled to the doc-runtime.** `commands/checkpoint.md:12` says *"dual-write decisions to `CONTEXT.md` § Locked Decisions AND `DECISIONS.md` (per D16)"*, and `:139` makes it a gate item ("D16 dual-write hit both"). `commands/resume.md` and `commands/discuss.md` carry the same `CONTEXT.md` § Locked Decisions instruction. **This directly contradicts the canonical doc model that M5.E1 established:** `references/doc-runtime-model.md:56` — *"Eviction destination — one rule: unit-homed single-home"* — and `:59`, *"no piece is copied into two places to keep in sync (a single-home violation)."* D16 predates the doc-runtime (M5.E1 FR1) and was never reconciled to it, so three commands instruct an agent to commit precisely the violation the doc-runtime exists to prevent. **Also collides with the Signal-on-Signal convention** that `CONTEXT.md` is the orientation doc and decisions live in `DECISIONS.md` — Signal's own `CONTEXT.md` has no § Locked Decisions section, so the instruction is not merely wrong-in-principle but unexecutable here without inventing one. **Confirmed 2026-07-25** during an M5.E7 `/sig:checkpoint --context`, which is why the checkpoint wrote decisions to `DECISIONS.md` only. **Second field sighting 2026-08-14, in `discuss.md` rather than `checkpoint.md`:** `M6.E1`'s DISCUSS wrote its decisions to `DECISIONS.md` per the repo convention and left `CONTEXT.md` alone — while that command's own Exit Criteria still reads *"CONTEXT.md captures all locked decisions."* The phase passed its gate by **disobeying** the checklist item, which is the clearest statement of this bug available: the instruction is not merely wrong in principle, it is wrong every time the phase runs correctly. **Fix:** amend all three commands to a single home (`DECISIONS.md`; `CONTEXT.md` gets a pointer at most), drop the `:139` gate line, and record a superseding decision against D16. Standalone commit — unrelated to M5.E7's audit scope. |
| B38 | `fixed` | P2 | **⟨STATUS CORRECTED 2026-08-14 — this row read `confirmed` for one day after shipping.⟩ SHIPPED in `v0.1.25` (`M5.E10`, 2026-08-13):** 109 entries classified (93 discipline / 16 shaping), `references/anti-rationalization-forms.md` generated from the corpus and pinned both ways, and the claim-provenance rule added as a shaping entry. **The stale status was found by the 2026-08-14 unreached-mechanism count** — i.e. by measuring this file, not by any mechanism that reconciles it; `dischargeObligation` exists and is called by nothing (`v0.1.24`). Original entry follows, unaltered. **Signal applies the prohibition/anti-rationalization-table form universally, but its own upstream source measured that form as counterproductive for output-*shaping* failures.** Verified at source 2026-07-25 (superpowers v6.2.0, `skills/writing-skills/SKILL.md` § "Match the Form to the Failure", ~:459-474). The source classifies four baseline failure types and pairs each with a right and a wrong form. **Prohibition + rationalization table is correct for discipline failures** (*"skips/violates a rule under pressure — knows better, does it anyway"*) — which most of Signal's tables are, so they are fine. **It is the WRONG form for shaping failures** (*"complies, but output has the wrong shape — bloated, buried verdict, restated spec"*), where the right form is a positive recipe/contract stating what the output IS. The source's evidence is empirical, not stylistic: *"in head-to-head wording tests … the prohibition arm produced clearly more of the unwanted content than the recipe arm (fully separated distributions), and trended worse than even the no-guidance control."* **Why this is Signal's problem:** `analysis/SIGNAL-INTEGRATION-RUNDOWN.md:186` dispositioned the table as *"Adopt as Signal convention — every phase gate uses this format"* *(source corrected 2026-07-26 during M5.E7 S4.t10 — this row originally cited `REPO-ANALYSIS.md`; the claim is not there. Grepped, not assumed.)*, and Signal took the generalization — every `commands/*.md` anti-rationalization table plus `references/anti-rationalization.md`. At least one entry is a shaping rule wearing the prohibition form: `commands/resume.md`'s *"Render the full PROJECT.md / CONTEXT.md verbatim" → "Summarize"*. **Two further source rules Signal has nowhere:** (a) *no nuance clauses* — "don't X unless it matters" reopens the negotiation, and appending one degraded a winning recipe from consistent to noisy; (b) *exemption clauses don't scope* — "this limit doesn't apply to code blocks" still suppresses code blocks. **Fix (not a removal):** classify every table entry across `commands/` + `references/` as discipline vs. shaping; keep the discipline ones, convert the shaping ones to positive recipes. Surfaced by M5.E7 S2.t4 supply verification; the falsified April claim is also carried as correction **C8** in `M5.E7-RESEARCH.md` §0b *(added there 2026-07-26 — it was cited before it was written)* and is a roadmap `build` candidate with this as its first slice. |

| B39 | `fixed` | **P2** | **The trigger watchlist is never walked — `/sig:plan` was never taught the drain step the standing entry instructs.** `ISSUES-INBOX.md:1407` is a **standing entry** (*"never promote, merge, or delete"*), ratified BR-6 2026-07-04, whose whole text is an instruction to a command: *"At each `/sig:plan` drain, walk the conditions below and act on any that have fired."* **Verified 2026-07-26 by reading the code, not inferred:** `grep -ril "watchlist" commands/ tools/` returns **nothing**. `commands/plan.md` mentions `drain` 11 times and the watchlist zero times; `tools/lib/drain.js` has no concept of it. **The mechanism was specified and never implemented.** Consequences already visible in the ledger: (a) the watchlist's own `Fired?` column reads `—` on **all 11 rows**, including *GitHub Issues adoption*, whose trigger `BACKLOG.md:187` separately records as **fired 2026-07-15** — two homes for one fact, disagreeing, which is also a single-home violation of the same class as **B37**; (b) at least two `/sig:plan` drains have run since that trigger fired (M5.E6, M5.E7) and the item is still parked with **no record of a decision either way**; (c) the entry's own stated rationale was *"10+ parked entries carry promote-back conditions that **nothing evaluated**"* — the fix for that problem has itself never evaluated anything. **Root cause of the M5.E7 finding** that three parked BACKLOG bullets sit behind gates that cannot fire for the demand landing on them (`M5.E7-DEMAND-REGISTER.md` §1). **The dated synthesizer trigger did not lapse only because Brett called it by hand during M5.E7.** Fix: implement the walk as a real drain step in `commands/plan.md` + `tools/lib/drain.js`, and make it **record-producing** — a checked-and-declined trigger must be distinguishable from an unchecked one, or the next audit re-finds this. Surfaced during M5.E7 S1.t3/S4.t10 review. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (FR2.1)** and never flipped. `/sig:plan` now carries `#### Walk the trigger watchlist` (`commands/plan.md:99`) backed by `parseTriggerWatchlist` (`tools/lib/drain.js:791`); the second half — a *checked-and-declined* row staying visibly distinct from an unchecked one — is asserted at `tests/trigger-watchlist.test.js:86`, and `:109` asserts the walk is actually wired into the command rather than merely documented. |
| B40 | `confirmed` | P3 | **Citation integrity in `analysis/` is unenforceable by construction — the authoring convention routes around the one guard that could check it.** The standing hygiene guard walks `analysis/` and runs `checkInternalLinks`, but Signal's docs deliberately write cross-directory references as **inline code** (`` `.planning/M5.E7-RESEARCH.md` ``) rather than markdown links, precisely so they cannot trip it — a convention S1.t2 states outright (`M5.E7-DEMAND-LEDGERS.md` §0) and S4.t10 repeated. **Live instance, caught by review and not by the suite (2026-07-26):** M5.E7's C8 correction markers in `analysis/` cited *"Evidence: `.planning/M5.E7-RESEARCH.md` §0"* while **C8 had never been written into that section**; `BUGS.md:66` (B38) carried the same false citation. Fixed at the content level (C8 is now a §0b row) — **the mechanism gap is what this entry tracks.** Note the guard could not have caught it either way: `checkInternalLinks` tests **file existence**, and this failure was *file exists, cited section does not contain the claim* — a **claim-level** integrity problem, not a link-level one. **Therefore do not "fix" this by converting refs to markdown links** — that buys a file-existence check against a section-content failure and re-exposes every ref to false-fails from quoted link syntax (the documented reason for the `.planning/` exclusion at `tools/lib/doc-hygiene.js:91-93`). Fold instead into the `analysis/` staleness recommendation (`M5.E7-CORRECTIONS.md` §2): the `verified-against` stamp check should also assert that a cited `{file} §{section}` actually contains the ID it claims. **Interim discipline, in force now:** grep the cited section for the ID before writing any marker that cites it. |
| B41 | `fixed` | **P2** | **Four of the seven phase commands never advance `phase`, so `completed_phases` is a ledger of phases that were never recorded — and `markFresh` stamps it fresh, suppressing the one banner that would show it.** `transitionPhase` is called by **exactly three** commands — `calibrate.md`, `discuss.md`, `ship.md` (grep over `commands/*.md`, 2026-07-26). `plan.md`, `execute.md`, `verify.md`, `review.md` **never call it, and instruct no other `phase` write.** *(Precision, because the scope matters when this is fixed: `execute.md` is not state-blind — its auto-state-protocol wraps every dispatch in `dispatchTaskWithState` → `setCurrentTask` / `clearCurrentTask`, so `current_tasks` and `last_completed_task` do move. **It is `phase` and `completed_phases` specifically that no middle command touches**, and `execute.md` calls neither `markFresh` nor `transitionPhase`.)* Consequence for a **command-driven** project: `/sig:discuss` sets `phase: DISCUSS`; nothing changes it again until `/sig:ship` calls `transitionPhase('SHIP')`, which appends **whatever `state.phase` still says** — so the run ends with `completed_phases: [DISCUSS]`, `phase: SHIP`, and **PLAN / EXECUTE / VERIFY / REVIEW never recorded at all**, while `/sig:status` and `/sig:resume` report `DISCUSS` for the entire build. **The symptom is already written into the code as an unsatisfiable precondition:** `plan.md:50` says *"`STATE.md` — verify current phase is PLAN"*, and **no command can ever make that true.** **The aggravator is the real sting:** `plan.md:149`, `verify.md:72`, `review.md` Step 5b all call `markFresh`, whose stated purpose is *"so the staleness banner in `/sig:resume` reads as fresh after {PHASE} closes"* — so each one **stamps a fresh timestamp over a stale position**, converting *stale-and-flagged* into *stale-and-silent*. **Live instance (2026-07-26):** after M5.E7's REVIEW `markFresh`, STATE read `phase: VERIFY` with a fresh stamp and no banner would have fired; caught by hand at REVIEW exit, not by anything. **Masked on Signal-on-Signal** because this repo's `phase`/`completed_phases` are maintained by hand — which is why it survived 11 releases. *Note what class this is: a **deterministic, file-shaped** instance of the Epic's own headline finding — a state ledger nothing checks. It needs no measurement layer to fix, so it is M5.E9 work, not M5.E8.* **Fix shape:** a `transitionPhase(baseDir, '{PHASE}')` at each phase command's entry (not exit — entry is what makes `plan.md:50`'s precondition meaningful), or one shared preamble step. Pairs with **B36** (the other structurally-inert enforcement at ship-start). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (FR6)** and never flipped. All four middle phase commands now carry a `## Phase entry — record the phase` section (`commands/plan.md:42`, `execute.md:42`, `verify.md:36`, `review.md:47`), and `tests/phase-recording.test.js` asserts each one. M5.E13 (`B48`) later made the instruction conditional on the phase having an artifact; that refines the fix, it does not undo it. |

| B42 | `fixed` | **P1** | **A linear-mode project cannot run `/sig:ship` at all — the FR1 retro gate hard-halts on `current_epic: null`, and there is no bypass.** `shipFR1Check` (`retrospective.js:751-762`) returns `{halt:true, code:'NO_CURRENT_EPIC'}` whenever `!state.current_epic`, *before* any Epic-close/per-slice reasoning. `ship.md:27` runs that check **"before any other Workflow step, regardless of `gate_strictness`"** and `ship.md:41` states **"No bypass"** — no flag, no env var, no extra-args trick. `ship.md:34` compounds it: Step 0.5's milestone-file derivation parses `state.current_epic` and halts if the derived `MILESTONE-{n}.md` is missing, which is also unsatisfiable with no Epic. **Linear mode is first-class everywhere else** — `new-project.md:35` (*"Without `--epic`, the project starts in linear mode"*), and the identical Epic-mode/linear-mode profile+artifact-naming preamble in `discuss.md:13`, `plan.md:13`, `execute.md:13`, `verify.md:13`, `review.md:13`, **`ship.md:13` itself**, plus `status.md:115` / `resume.md:143`. So six phase commands support a mode the seventh refuses to run. **Not an untested gap — the halt is asserted:** `tests/ship-fr1.test.js:125` (*"halts with NO_CURRENT_EPIC when state.current_epic is empty (AC1-extended)"*) and `:138`; `M4.5.E9-VERIFICATION.md` records AC1-extended as PASS. Fixing this means **rescoping an AC**, not just editing a branch: the M4.5.E9 author read empty `current_epic` as *"state is broken"*, not *"this project has no Epics."* **Landed `3d41159` (2026-05-26, M4.5.E9.S1.t6), shipped in v0.1.3 (2026-05-31) — live across nine releases (v0.1.3 → v0.1.11), invisible because Signal-on-Signal has run Epic-mode since M4.5.E11.** **Reported live 2026-07-26** from `eval-project-A`, which has run linear (`current_epic` always null) its whole life: FR1 halted `/sig:ship` on step zero; the close was completed by hand. **Fix shape — and it is a rescope, not a three-liner. Three separate gates each assume an Epic, and fixing only the first leaves ship still blocked:** (1) `shipFR1Check`'s early halt (`retrospective.js:755`) → return `{halt:false, skipped:true, reason:'linear mode'}`; (2) **`ship.md:34`, which runs *before* it** — Step 0.5 derives the milestone file by parsing `state.current_epic` and halts on its own when the derived `MILESTONE-{n}.md` is missing, so a linear project halts one step earlier regardless; (3) `isEpicCloseByState` (`retrospective.js:412`) returns `false` on `!state.current_epic`, so the entire B26/B30 Epic-close machinery — `aboutToClose`, the synthesized post-SHIP state, the row-absence fallback — is **dead code in linear mode**. Decide what the close unit even *is* for a linear project before touching any of them (`resume.js:129` already says *"in linear mode the retro is milestone-scoped"* — the intent existed and no gate implemented it). **Sharpest evidence the halt was never a deliberate reading:** the layers disagree on the same condition. Layer 1 fails **closed** (hard halt, no bypass); **Layer 2 fails open** — `checkProposedStateWrite` returns `{block:false}` on a null/absent `current_epic` (`retrospective.js:500-502`). One missing Epic, a hard block in one layer and a silent pass in the other. **P1 rather than P2:** this is not degraded behavior, it is a documented mode blocked end-to-end with the escape hatch explicitly designed out. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (D-M5E9-1)** and never flipped — all three gates, which is what the fix shape above demanded. (1) `shipFR1Check` returns `{halt:false, skipped:true, reason:"linear mode …"}` on `detectMode(state) === "linear"` (`tools/lib/retrospective.js:764-790`). (2) `ship.md:34` Step 0.5 is now **Epic mode only** and passes `milestoneContent: null` in linear mode, naming this entry as its reason. (3) `isEpicCloseByState` returning `false` in linear mode is now correct by scope, not dead code — the gate is Epic-only by ratified decision. Asserted end-to-end at `tests/linear-mode-e2e.test.js:93`. |

| B43 | `fixed` | **P2** | **`transitionPhase` records the phase you are *leaving*, so a SHIP date can never be written by it — SHIP is terminal, and `ship.md:96` documents the opposite.** `state.js:390-391` appends **`state.phase`** (the phase being left) with today's date, then sets `phase` to the argument. `ship.md:96` claims `transitionPhase(baseDir,'SHIP')` *"appends `SHIP (YYYY-MM-DD)` to `completed_phases`"* — it appends `REVIEW (date)`. **The consequence is structural, not cosmetic** (sharpened by Brett 2026-07-26, and the reason this is P2 not P3): `SHIP` enters `completed_phases` only on a transition *away from* SHIP, and there is none — so **no correct SHIP date can ever be recorded by this function**, in any mode, Epic or linear. This repo's own STATE is the proof: `completed_phases: [… REVIEW]`, `phase: SHIP`. **A second failure mode rides on it:** the appended entry is stamped with **today's** date but names the phase being left, which may have closed days earlier — so the date is wrong for the phase it labels. Live instance: `transitionPhase(base,'SHIP')` called at `phase: PLAN` stamped `PLAN (2026-07-26)` and recorded no SHIP at all — *"even without the collapse (B44), the call would have silently failed to record the thing it was called to record."* **Not covered by B41**, which is about the middle commands never calling `transitionPhase` at all — though B41 is what let `phase` still read `PLAN` at ship time. **The codebase already knows this and says so — in the other direction from the docs:** `retrospective.js:493` comments the Epic-close detector as keying on pre-SHIP coverage, *"**NOT** a `- SHIP` entry (Signal never writes one)."* So the gate was built around the fact that SHIP is unrecordable while `ship.md:96` tells the reader the opposite. **Fix:** append `nextPhase` (or both, `"left X / entered Y"`), and carry each phase's real close date rather than the write date. Then correct `ship.md:96` — **and re-check `isEpicCloseByState`/`checkProposedStateWrite`, which are written against today's never-writes-SHIP behavior** and would need the `coversPreShip` test revisited if SHIP starts appearing in the list. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (FR2)** and never flipped. `completePhase` (`tools/lib/state.js:655`) records a phase complete *without* transitioning away from it, which is what a terminal SHIP needs; `ship.md:98` was corrected in the same Epic and now describes `transitionPhase` as appending the phase being **left**. Asserted at `tests/linear-mode-e2e.test.js:178`. |

| B44 | `fixed` | **P2** | **`transitionPhase`'s dedupe assumes a ≤7-entry per-Epic ledger — nothing enforces that invariant, so the first programmatic call on a long linear-mode history silently collapses it.** `state.js:388-395` keys `completed_phases` by phase name in a `Map` (most-recent wins) — **specified**, not accidental: the docstring says *"dedupes by phase name … so recovery re-transitions don't accumulate duplicates"* and `references/state-schema.md:128` documents *"Dedupe by phase name on re-transition."* The dedupe is correct **for the scope it was designed against** — an Epic, where the list holds at most one entry per phase and B9's `setCurrentEpic` reset clears it on every roll. **Linear mode has no such reset** (B9's fix fires only on an Epic roll), so `completed_phases` is the project's whole multi-slice history — and the collapse is unbounded, silent, and unwarned. **Live instance 2026-07-26 (`eval-project-A`):** a hand-maintained 53-entry history was rewritten to 8 by one `transitionPhase` call; caught before commit, by hand. **The two preconditions are both Signal's:** the ≤7 invariant is undocumented and unasserted, and the history could only have been hand-built in the first place **because of B41** (the middle commands never call `transitionPhase`, so a linear project maintains the field manually). Fix B41 alone and this gets *worse*, not better — every newly-wired phase command becomes another collapse site. **The precise framing (Brett, 2026-07-26): set semantics applied to log data.** The `Map` is keyed on the first whitespace token and JS `Map` is last-write-wins per key, so the operation is only coherent if `completed_phases` means *"which phases has this project ever reached."* Used as an append-only chronological log — which is what a multi-slice project's STATE is, and what `STATE-HISTORY.md` treats it as — **every prior slice's history is destroyed on the first call.** *Scope correction: this is not linear-mode-only. **Any** project past its first unit hits it; Epic mode is spared only because `setCurrentEpic` (`state.js:614-616`, the B9 fix) deliberately zeroes `completed_phases` on every roll, so the list never gets long enough to notice. Linear mode has no roll and therefore no reset.* **What made it dangerous rather than merely wrong is the silence:** `writeStateFrontmatter` (`state.js:478-483`) re-serializes the whole frontmatter atomically — no diff, no warning, no dropped-entry count. Caught in `eval-project-A` only because a `git diff` was run before committing a file expected to carry two edits. **Fix shape (decide, don't assume):** if it's a log, **drop the `Map` entirely** — a one-line fix; if it is genuinely a set, then **entries must not carry dates**, because a date on a last-write-wins entry looks like *"when this happened"* and means *"most recent write,"* which is exactly how it misled here. Either way **write the invariant down in `references/state-schema.md:128` and assert it** — that line currently specifies the dedupe without ever stating the ≤7 scope it depends on. Sequence **before or with B41** (same state-mutation core, M5.E9): fixing B41 first turns four more commands into collapse sites. **Writer audit (done 2026-07-26, answering "is this class waiting in the other frontmatter writers?" — no):** every other mutator spreads the parsed state via `stripStateMeta` and **patches named fields** — `markFresh` two scalars (`state.js:1384-1389`), `setCurrentTask` a pure append (`:516`), `clearCurrentTask` a filter on an explicit id, `addBlocker`/`clearBlocker`/`touchDecisionTimestamp` likewise. `transitionPhase` is the **only** writer that rebuilds an array by derivation. Two residual notes, neither a truncation: `setCurrentEpic` wipes `completed_phases` by design (B9) with no archival step, and because every write round-trips the frontmatter through the YAML serializer, **comments and hand formatting inside the frontmatter block are dropped by any writer** (data keys survive). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (D-M5E9-5)** and never flipped. The set-vs-log question was decided in favour of **log**: `transitionPhase` is now append-only with no dedupe (`tools/lib/state.js:425`), so a long linear-mode history can no longer be collapsed by a single call. `references/state-schema.md` was updated with it. Asserted in `tests/state.test.js`, `tests/phase-recording.test.js` and `tests/adherence-append-only.test.js`. |

| B45 | `fixed` | **P2** | **`transitionPhase` validates its argument but never validates existing entries, so any junk line in `completed_phases` becomes a permanent phantom phase.** `state.js:377-381` rejects a `nextPhase` outside `PHASES` — the only validation in the function. Existing entries go straight into `phaseNameOf = (entry) => entry.split(' ')[0]` (`:388`) with no `PHASES` check, so **whatever the first whitespace token happens to be becomes a `Map` key**, indistinguishable from a real phase. Live instance (`eval-project-A`, 2026-07-26): a pre-existing prose line `**▶ Active: Slice SEC1 …` keyed on `**▶`, survived the dedupe as an eighth "phase," and **will survive every future dedupe** — the collapse that destroys real history preserves the junk, because a phantom key has no competitor to overwrite it. **Independent of B44's set-vs-log question:** fix the dedupe either way and an unvalidated entry still round-trips forever; the two just interact badly (B44 makes the list lossy, B45 makes the loss selectively favor garbage). **Why it can happen at all:** `completed_phases` is hand-maintained in exactly the projects this bites (B41 — no command writes it mid-flow), and the v0.1.6 write-guard (B8) polices *prose in frontmatter* at the hook layer, which `state.js`'s own `node:fs` writers bypass by design (`v0.1.6-RESEARCH.md` R1.5). So the one guard that would have caught this shape cannot see this writer. **Fix:** validate entries against `PHASES` on read and **quarantine** malformed lines rather than keying on them — surface them (count + first offender) instead of silently promoting them to phases. Pairs with B44; same function, same commit. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9** and never flipped. `partitionCompletedPhases` (`tools/lib/state.js:385`) splits `completed_phases` into well-formed and malformed entries and the malformed ones are **quarantined and returned rather than keyed on** (`:366`, `:446-447`) — so a junk line can no longer become a phantom phase. `/sig:sweep` names this entry when it reports the repairable case (`tools/lib/sweep.js:319`). |

| B46 | `dismissed` | P2 | **⟨RE-TRIAGED AND DISMISSED 2026-08-10 — cause disproved, symptom discharged.⟩** The re-triage this row was held open for is now made, and both halves resolve independently. **The cause is wrong** (below, 2026-08-09): the disposition table's population is the v2 feature-port list, not the inbox, so the fix as written would write verbs from one population onto a different one — data corruption, not correction. **The symptom is gone**: the 2026-08-10 inbox drain stamped 50 of 52 entries across PRs #136/#137/#138, so nothing resurfaces identically at the next drain; the 2 that remain are standing entries that forbid their own disposition. **Dismissed rather than fixed, and the row is kept** — a dismissal that deletes its own reasoning teaches nothing, and this row's value is the record of a plausible cause that measurement disproved. **What the drain learned that this row could not:** the resurfacing was never one defect. 26 entries had been promoted to `BACKLOG.md` and never stamped, 12 described shipped work, and only 12 were genuinely un-homed — so *"47 candidates resurface"* was one number covering three different situations, which is why no single fix ever fit it. **⟨PRIOR: STALENESS RE-CHECK 2026-08-09 — PREMISE FALSIFIED. Needed re-triage, NOT a fix.⟩** M5.E13 measured **0 of 48** inbox candidates corresponding to any disposition row: the disposition table's population is the **v2 feature-port list**, not the inbox, so the stated cause is wrong and stamping would corrupt rather than correct. **This is recorded in this file's own footer and was never carried into the row** — which is why it is being written here, at the granularity a `grep` reads. Independently re-verified 2026-08-09: **0 disposition stamps, 52 inbox entries** — so the *symptom* is real and worse, while the *cause* named below is not it. `B89`'s fix (drain now required, with an unconditional defer-all escape) addresses the resurfacing half. **Status deliberately left `confirmed` rather than flipped** — re-triage is a decision someone must make, and moving the label without making it would just relocate the problem. Original: **M5.E7 dispositioned 45 inbox candidates into a side file and never stamped the inbox, so every one of them resurfaces on every future `/sig:plan` drain — including the ones verbed `abandon`.** `M5.E7-DISPOSITIONS.md` carries the verbs (**76 table rows**, 41 KB); `ISSUES-INBOX.md` contains **zero** `→ Promoted` / `→ Deferred` / `→ Shipped` stamps (grep, 2026-07-27). `listDrainCandidatesWithRecovery` keys *"already dispositioned"* off those inline stamps, so it returns **47 candidates** — a wall that will re-present itself identically at every future PLAN, forever, with no memory that 45 of them were already decided. **Surfaced live at M5.E9 PLAN Step 1b**, which is what a drain step is *for*: the step worked; the prior Epic's write-back did not. **`plan.md`'s drain contract is not at fault** — it specifies destination-first-then-stamp via `promoteDrainEntry` / `applyDispositionToFile` and describes exactly that crash-safe ordering. **M5.E7 never ran it**, because the audit's disposition table was authored as an *analysis artifact* rather than driven through the drain, and nothing checks that the two agree. **Note the class — this Epic's own headline finding, one level up:** a decision recorded where nothing reads it back. Same shape as **B39** (a watchlist written down and never walked) and **B41** (a phase ledger nothing checks). **Why it was not fixed in-flight:** stamping *"Deferred (M5.E9 drain)"* across 47 entries would write the **wrong** verb onto entries already dispositioned `build` or `abandon` — satisfying a checklist by corrupting the idea database. **Fix shape:** a reconciliation pass that reads `M5.E7-DISPOSITIONS.md` and stamps each entry with **its own recorded verb** (never a blanket defer), then evicts the terminal ones to the ledger — plus a hygiene assertion that a dispositions artifact and the inbox cannot disagree. **Homed in M5.E9 as a later slice; explicitly NOT v0.1.12.** |

| B47 | `confirmed` | P3 | **`/sig:resume` reads `0/7 phases done` immediately after a linear-mode ship.** M5.E9's FR5 trim clears `completed_phases` at ship (relocating the run to `STATE-HISTORY.md`), so the first thing a user sees after shipping is `Phase: SHIP (0/7 phases done)` — at exactly the moment they want confirmation something *completed*. Defensible on a literal reading (the **next** run has zero done) and it is cosmetic, not data loss; the archived run is intact and verified zero-loss. **Cataloged at M5.E9 REVIEW (2026-07-27) as I1, deliberately not fixed in-phase:** the honest fix is for the resume banner to render the archived run's count, which means teaching `resume.js` to read `STATE-HISTORY.md` — **a new capability, not a defect fix**, and Slice 2 was already the Epic's largest unit. Recorded rather than absorbed so the next reader does not have to rediscover it. **Fix shape:** `renderResumeBriefing` reads the most recent `phase-log:archived` section when the live list is empty and `phase: SHIP`, and renders e.g. `SHIP (last run: 7/7 done, archived)`. |

| B48 | `fixed` | P2 | **`execute.md`'s phase-entry instruction is unconditional, and an agent refused it on correctness grounds — writing a false record is the alternative.** M5.E9's FR6 tells the agent to call `transitionPhase(baseDir, 'EXECUTE')` **"before any Workflow step"**, with no precondition clause. Observed live during M5.E8's first adherence run (2026-07-28): against a fixture with no PLAN artifact, `/sig:execute` halted at its preconditions and the agent **explicitly declined** the phase-entry write, reasoning that calling it *"would have written `phase: EXECUTE` for a project with nothing to execute, and appended `PLAN` to `completed_phases` when PLAN produced no artifact — a false record in the exact log that `resume.js` and `isEpicCloseByState` read as ground truth."* **The agent is right, and the instruction as written is wrong.** `recordPhase` (`state.js:429`) appends unconditionally with no evidence check, so obeying the instruction literally on a halted phase corrupts the ledger M5.E9 had just finished making honest — while disobeying it reproduces `B41`, the very defect FR6 was written to fix. **The instruction and the phase-log integrity rules are in direct conflict, and nothing in the corpus resolves it.** **Not found by the harness's verdict** — that run scored `ABSENT`, which is a fact about the fixture. It was found by reading the transcript, which is why transcript retention is now unconditional. **Fix shape:** state the precondition explicitly — transition at entry *once the phase's preconditions hold*, and if the command halts before doing any work, do not record the phase as left. Applies to all four commands M5.E9 changed (`plan`, `execute`, `verify`, `review`), not just `execute.md`. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (FR1.2)** and never flipped — in the text *and* the code beneath it. The phase-entry instruction is now conditional in all four commands (see the `**Why the condition (M5.E13, `B48`).**` paragraph at `commands/execute.md:48` and its three siblings), and `transitionPhase` refuses to record an artifact-less phase. The originating EXECUTE case is asserted by name at `tests/phase-recording.test.js:201`. |

| B49 | `fixed` (v0.1.13) | P3 | **`package.json` says `0.1.11`; `.claude-plugin/plugin.json` says `0.1.12`.** The plugin manifest is the version of record for what a user installs, and it is correct; `package.json` was not bumped at the v0.1.12 release cut. Low impact today — nothing in the runtime reads `package.json.version`, and the marketplace install path uses the manifest — but the two files are the project's two public statements of its own version, and they disagree. **Cataloged from M5.E8 VERIFY (2026-07-28), deliberately not fixed there:** a version bump inside a measurement Epic is scope creep, and the release that fixes it should be the one that sets it. **FIXED in v0.1.13 (2026-07-28)** — bumped at the release cut, exactly as the fix shape specified. **And the fix shape's second half was wrong in an informative way:** `checkVersionConsistency` **already exists and already works** — it fired immediately on `.claude-plugin/marketplace.json` during this very release, catching a **third** version file the bump had missed. What it does not cover is `package.json`, which is precisely why `B49` survived undetected while the suite stayed green. **Remaining open work, re-scoped:** extend `checkVersionConsistency` to include `package.json`. The guard is not missing; its **scope** is. |
| B50 | `confirmed` | **P2** | **Completeness claims are written from the shape of the work rather than from the artifact — and Signal contains zero mechanisms that check any claim against its source.** The **class**, given a ledger identity 2026-07-28 so future retros count sightings instead of rediscovering it (`ISSUES-INBOX.md` § "Claim integrity"). The author composes a coverage/status/completeness statement ("0 gaps", "all verified", "checklist complete") from their mental model of what just happened instead of re-deriving it from the thing it describes; **the false statement is always plausible, which is what makes it dangerous.** Distinct from the class M5.E8 named (*a guard written, shipped, never called* — `B39`/`B46`/`I2`), and **the two compose badly: one writes the false claim, the other ensures the check that would catch it never runs.** **Structural causes, verified by reading the code:** `commands/verify.md` names `REQUIREMENTS.md` **zero times** (its coverage loop enumerates PLAN tasks, so a requirement dropped at PLAN is invisible to VERIFY forever); the verifier agents that *do* have the enumerate-with-a-denominator shape (`agents/verifiers/verifier.md`, `nyquist-auditor.md`) are dispatched by **no command**; `review.md` is designated the faithfulness backstop in two places (`ship.md` §5.5, `evict.js` header) and implements it nowhere; every deterministic validator checks structure, not claims — **exactly two claim-vs-reality checks exist in all of Signal** (`checkRosterCounts`, `adherence-ceiling.js --check`). **The M5.E8 harness cannot see this class by construction** — its traces are "a named function was called" / "a named file was written", never what the file *says*, so a VERIFY report omitting a requirement still scores as a PASS trace. **Instances: 7 prior in Signal's corpus + 5 in one eval-project-C phase, plus two found while cataloguing it** — M5.E8's own retro proposed a mechanism (`--check`-has-a-caller) that covers **1 of its 3** motivating instances (`--check` appears in exactly one file in `tools/`), and this entry's own inbox capture was filed with a status line reading "Logged" and **no body**. Full evidence, causes, and eight ranked fixes: [`../analysis/CLAIM-INTEGRITY-ANALYSIS.md`](../analysis/CLAIM-INTEGRITY-ANALYSIS.md) (single home). **Homed at M5.E10** (§6 items 1–3, 5–8); the retro-index freshness check (item 8) taken by **M5.E13** per its "whichever ships first" clause. |
| B51 | `fixed` | **P2** | **`discuss.md` still instructs the DISCUSS close to set `phase: PLAN`, which makes `/sig:plan`'s at-entry transition record PLAN as complete before PLAN has run.** `commands/discuss.md:158-161` ends with *"Update `STATE.md`: `## Current Phase` / `PLAN`"* — the pre-M5.E9 convention, where the **outgoing** command advanced the phase. M5.E9's `B41` fix moved that responsibility to the **incoming** command: `commands/plan.md:44` now says *"Before any Workflow step, call `await transitionPhase(baseDir, 'PLAN')` … It appends the phase being **left** to `completed_phases`."* **Both instructions survive, and obeying both writes a false entry.** If DISCUSS sets `phase: PLAN`, then at `/sig:plan` entry `leaving` resolves to **PLAN**, so `recordPhase` appends `PLAN (date)` — recording PLAN complete **before it did any work**. `recordPhase` is **deliberately append-only with no dedupe** (`state.js:425-427`, D-M5E9-5 / `B44` — a genuine re-transition did happen twice), so the false entry is **permanent** and later sits beside the real `PLAN (date)` appended when EXECUTE transitions. **Same family as `B48`:** an instruction whose literal execution corrupts the ledger M5.E9 built to make honest, while ignoring it is the only correct move. **Found by dogfooding — M5.E13's own DISCUSS, 2026-07-28.** The instruction was not obeyed; `phase` was left at `DISCUSS` so `/sig:plan` records the transition itself, which is the post-`B41` design. **Scope check: `discuss.md` is the sibling case of the four commands `B48` covers** — M5.E9 updated the four *incoming* commands and left the *outgoing* instruction in the fifth. `calibrate.md` and `ship.md` also called `transitionPhase` pre-M5.E9 and need the same read. **Folded into M5.E13 FR1** rather than opened as separate work: one instruction family, one fix. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13** and never flipped. `commands/discuss.md:158` now reads **"Do NOT set `phase: PLAN` here"** — the incoming command advances the phase, not the outgoing one. Pinned by `tests/commands-wording.test.js:61`, which asserts no command sets a phase the next command also sets. |
| B52 | `fixed` (v0.1.20) | **P1** | **A Claude Code session binds to ONE plugin-cache version for its whole life, so Signal-on-Signal can dogfood a release two versions behind the repo — and it silently destroyed M5.E8's phase ledger.** Observed live 2026-07-28 at M5.E13 PLAN entry. `.claude-plugin/plugin.json`, `package.json` and `marketplace.json` all read **0.1.13** (pinned to `8d20193`), a **0.1.13 cache exists and is correct** (`cache/signal/sig/0.1.13/commands/plan.md` has both `transitionPhase` refs), **yet the session loaded every `/sig:` command and every `tools/lib` module from `cache/signal/sig/0.1.11/`** — two releases behind. **The damage:** `setCurrentEpic` rolling `M5.E8 → M5.E13` ran **0.1.11's `state.js`**, which has **zero** references to `archivePhaseLog`/`partitionCompletedPhases` (repo has 6) because it predates M5.E9. The Epic-roll therefore reset `completed_phases` **without the archive-before-reset step M5.E9 added specifically to prevent this** — M5.E8's six-phase DISCUSS→SHIP run was **discarded with no error, no warning, and no record**. Proven by the sibling case sitting beside it: `.planning/archive/M5/E9/STATE-NARRATIVE.md` **exists** (written by the real tool on 2026-07-27, when M5.E8 opened under v0.1.12 code), while `.planning/archive/M5/E8/` **did not exist at all**. **Recovered** verbatim from `git show aff4098:.planning/STATE.md` into `archive/M5/E8/STATE-NARRATIVE.md`, zero entries lost — recoverable **only** because `.planning/` is tracked in git. **Why this outranks the bug it reproduced:** `B44` was *"a silent write that drops data"* and M5.E9 fixed it; this is that same loss re-entering through the **install layer**, where no amount of correct repo code prevents it. **It also corrupts dogfooding evidence in both directions** — a fixed bug can appear live (the stale cache still has it) and a live bug can appear fixed. Every `/sig:` command run in this session, including the `/sig:discuss` that opened M5.E13, was **v0.1.11 text**. **Fix shape (two halves):** (1) a session-start version check comparing the resolved plugin-cache path against `.claude-plugin/plugin.json`, warning loudly on mismatch — cheap, deterministic, and the half that matters; (2) decide whether `setCurrentEpic` should refuse to reset a non-empty `completed_phases` it did not archive, which would have made this loud instead of silent regardless of version. **Note the shape:** the guard that would have caught it existed and was correct — it just wasn't the code that ran. That is the `B39`/`B46`/`I2` class arriving by a new route, which is why it belongs in **M5.E13**. **ROOT CAUSE CONFIRMED 2026-07-28 — a mid-session auto-update race, not a broken install.** `installed_plugins.json` is **correct** (`installPath: …/sig/0.1.13`, `version: 0.1.13`) and `known_marketplaces.json` has **`autoUpdate: true`**. The plugin's `lastUpdated` is **`2026-07-28T13:38:41Z`**; this session had already started (its SessionStart hook reported STATE as of `13:37:23`). **Claude Code resolves the plugin path once at session start and holds it for the session's life**, so a session that began ~78 seconds before the auto-update kept **0.1.11** while every config file on disk correctly read 0.1.13. **Reproduced by Brett in a second, unrelated repo the same day** — it is **per-session, not per-project**: any session alive across an auto-update keeps the version it started with. **Not Signal-specific in mechanism** (it affects any Claude Code plugin) **but severe for Signal specifically**, because Signal's tools mutate project state, so a stale binding does not merely behave oddly — it writes with retired code paths. **Immediate remedy: restart the CLI process. A context clear (`/clear`) is very likely NOT sufficient — and the distinction matters, because "restart the session" is ambiguous between exactly those two things.** *Reasoned, not verified:* plugin resolution builds the command/skill registry, which is **process-level** state; `/clear` resets conversation context without restarting the process, so the resolved paths should survive it. A marketplace refresh that re-fetches without rebuilding the in-process registry should likewise not help. **Decisive 10-second test, because every `/sig:` command prints its own version in its text** (this session's `/sig:resume` opened by citing `…/sig/0.1.11/tools/lib/profile.js`): run `/clear`, then `/sig:status`, and read the cited path — `0.1.13` means a clear re-resolves, `0.1.11` means only a full process restart does. **Nothing in config needs repair either way.** **→ SECOND LIVE SIGHTING 2026-08-02, and this one delivered a retired instruction rather than destroying data.** Caught at the top of `/sig:discuss --epic M5.E18`: the skill text the session loaded cites `…/cache/signal/sig/**0.1.13**/skills/define/…`, while `installed_plugins.json` records `installPath: …/sig/**0.1.15**`, `version: 0.1.15`, `lastUpdated: 2026-08-01T18:13:18Z`. **Config on disk is correct and the session is still two releases behind it.** **CORRECTION, same day, before anyone acted on it:** this row first said the timing *"differs from the originating case"* and that the mechanism was *"not yet explained."* **Both were wrong, and wrong the same way — I read the session transcript's creation time as the process's start time.** `ps` says otherwise: the CLI process serving this session (**pid 86260**, cwd `…/dev-biz/signal`) started **Tue Jul 28 17:29**, when v0.1.13 *was* the current release. It has been alive **five days**. The session id was minted Aug 2 12:50 by a `/clear` **inside that same process**. So this is the originating mechanism exactly — *a session alive across an auto-update keeps the version it started with* — with a **four-day** window instead of 78 seconds. **And it converts this row's own open hypothesis into a measurement.** The row said, marked *reasoned, not verified*: *"A context clear is very likely NOT sufficient … `/clear` resets conversation context without restarting the process, so the resolved paths should survive it."* **Verified now:** a `/clear` ran at 12:50, the process kept its 0.1.13 binding, and the very next `/sig:` command cited `…/sig/0.1.13/`. Delete the *reasoned-not-verified* caveat — restart is required, clear is not enough, observed. **The generalisation that matters more than either:** a long-lived process is the common case, not the edge one. `ps` at the same moment found **five** live CLI processes — `elicitation-engine` (Jul 25), `eval-project-J` (Jul 25), `eval-project-C` (Jul 28), `signal` (Jul 28), `eval-project-A` (Aug 2) — so **four of five were running machinery 5 to 8 days old**, against real projects, with nothing anywhere saying so. The originating incident read as a freak 78-second race; it is not. It is the default state of any editor left open over a weekend. **The damage this time:** v0.1.13's `discuss.md` still carries the exact block `B51` deleted — *"Update `STATE.md`: `## Current Phase` / `PLAN`"* — so an agent following the text it was handed would have written the permanent false `PLAN (date)` entry `B51` exists to prevent (`completed_phases` is append-only with no dedupe, `D-M5E9-5`, so it cannot be undone by a later correct run). Diffed live: cached 174 lines vs repo 170; the four extra lines *are* the retired instruction. **So the row's prediction — "a fixed bug can appear live" — is now observed, and it is worse than stated: the stale cache does not merely fail to deliver a fix, it actively re-issues the instruction the fix removed.** **Also measured:** there is **no `0.1.16` cache at all** (`0.1.5, 0.1.6, 0.1.8–0.1.11, 0.1.13, 0.1.15`), so even a correctly-bound session runs one release behind `main` until the marketplace re-fetches — expected, given `source: "."` means users track `main` and the cache refreshes on update, but it means **Signal-on-Signal always dogfoods older machinery than it just merged.** **Consequence for the Epic:** `M5.E18` was NOT opened in this session — no `setCurrentEpic`, no `transitionPhase`, no artifacts written. Running a Signal-on-Signal Epic on retired command text corrupts exactly the dogfooding evidence the Epic exists to gather. **This strengthens fix half (1)** — a session-start check comparing the resolved cache path against the install record — from *cheap and deterministic* to *the only thing that would have caught either sighting*; both were found by a human noticing a version string in passing. **→ THIRD LIVE SIGHTING 2026-08-03, at a `/sig:resume` that had just been restarted for the second one.** The command text loaded cites `…/cache/signal/sig/**0.1.16**/tools/lib/…` while `installed_plugins.json` records `installPath: …/sig/**0.1.17**`, `version: 0.1.17`. **No damage:** Signal's own `phase: DISCUSS` is canonical, so 0.1.16's pre-`B70` `nextActionForPhase` returned `/sig:plan` rather than throwing — verified by executing it, not assumed. **But the near-miss is the point:** v0.1.17 *is* the `B70` fix, so the identical stale binding on any of the **5 of 12** projects with a non-canonical `phase` takes out the whole briefing. **And it corrects a measurement in this row.** The second-sighting text asserts *"there is **no** `0.1.16` cache at all"* and concludes that Signal-on-Signal therefore *"always dogfoods older machinery than it just merged."* The first half has expired — the cache directory now holds `0.1.5, 0.1.8–0.1.11, 0.1.13, 0.1.15, **0.1.16**, **0.1.17**`, so the marketplace re-fetch does land. The second half survives for a different reason: **the fetch lands on disk and the running process still does not see it.** That is a sharper claim than the one it replaces, and it removes the only explanation that made this look self-limiting. **Sighting cadence is now the strongest argument for fix half (1):** three sightings in six days (07-28, 08-02, 08-03), each found by a human reading a version string in passing, and the interval is shrinking while the version gap narrows — a smaller gap is *harder* to notice, not easier. **→ FIXED in v0.1.20, both halves, in the fix lane.** Half 1 is `tools/lib/plugin-binding.js`: it compares the plugin copy **the process actually resolved** — derived from the module's own path via `import.meta.url`, never `CLAUDE_PLUGIN_ROOT`, because the env var states where the plugin is *supposed* to be and this bug is exactly that disagreement — against what `installed_plugins.json` records, and renders a banner naming both versions, both paths, and the one remedy. **Wired at two surfaces, and the second is not optional:** the SessionStart hook (`hooks/warn-stale-plugin-binding.js`) **structurally cannot see the originating sighting** — the binding is resolved before the hook runs, so a mid-session auto-update is invisible to it, and the 78-second race would still be silent. `/sig:status` and `/sig:resume` re-read both files **at the moment of use**, which is the only moment that can observe an update that landed after binding. A hook-only fix would have covered the case that mostly is not the bug. **False-positive gate:** a bound root outside `~/.claude/plugins/cache/` is a local checkout and stays silent, or Signal-on-Signal would banner itself from the moment `cut-release.js` bumps a branch. **Inherent limit, stated so nobody tests it wrong:** the check ships inside the cache copy it inspects, so it cannot fire until a session binds to a version that has it. **Half 2 is the `setCurrentEpic` guard, and building it found the bug was wider than this row said.** The row framed the ledger loss as a stale-cache consequence; it is not — the reset is unconditional, so *"archive first"* was a step that could be skipped rather than an invariant that held, and **three** branches reached the zeroing write with an unarchived log while only one was covered. Two are reachable with no stale cache at all: a **linear** project opening its first Epic never entered the archive branch, and `epicArchiveDirFor` returned null for any non-strict `current_epic` — `PHASE11`, measured live in eval-project-C by `B53` — whereupon the caller read that null as *"skip the archive"* and reset anyway. Now: linear history archives to `.planning/STATE-HISTORY.md` (the destination `completePhase`'s trim already uses), non-strict-but-safe units get M5.E18's flat per-unit directory, and a log with **no** safe destination **throws before any write** — pinned by a test asserting STATE.md is byte-identical after the refusal, not merely that it threw. 2233 → **2269 tests**.

  **⚠ Related hazard, unobserved — flagged as a risk, not a finding.** `~/.claude/plugins/.last_inuse_sweep` (last run `2026-07-28T00:41:11Z`) indicates a periodic prune of cached plugin versions, and the cache currently holds **seven** (`0.1.5`, `0.1.6`, `0.1.8`, `0.1.9`, `0.1.10`, `0.1.11`, `0.1.13`). If that sweep removes a version it considers unused **while a session is still bound to it**, that session loses its command surface mid-flight — and a Signal session losing `tools/lib` mid-Epic is worse than one running stale code, because partial writes become possible. `0.1.11` is intact today (18 commands present), so **this has not happened and is not claimed to have happened.** It is the same shape as the binding bug itself: **a live reference that nothing else knows is live.** Worth confirming the sweep's in-use detection accounts for open sessions before treating the multi-version cache as harmless. **Separate observation, uncertain significance:** that same record carries `gitCommitSha: a73d550`, which is *"M4.t19: marketplace install layout fix + plugin slug signal→sig (v0.1.0)"* — a **v0.1.0-era commit**, unchanged since `installedAt: 2026-05-14` while `lastUpdated` has moved across thirteen releases. Whether that field is meant to track the release or the original install is **not established**; flagged rather than claimed, since a third disagreeing version statement is exactly `B49`'s shape and should not be asserted from a guess. |
| B53 | `fixed` | **P2** | **A non-strict `current_epic` splits artifact WRITE naming from READ resolution, so a phase command can write `1-PLAN.md` while the next command reads a stale `PHASE11-PLAN.md` — and `references/epic-native-flow.md` states the opposite as a guarantee.** Two seams read the same field with **different regexes**: `artifactName` (`resume.js`) uses the **strict** shape and fails open to *linear* naming (documented at `epic-native-flow.md:44`), while `resolveArtifactPath` (`resume.js:30`) uses the **lenient** `EPIC_ID_RE = /^[A-Za-z0-9._-]+$/` and places the Epic-prefixed candidate **first** in its candidate list — so an existing `{value}-{ARTIFACT}.md` **shadows** a freshly written `1-{ARTIFACT}.md`, which is checked only afterward. **`epic-native-flow.md:31` claims they cannot diverge:** *"They are symmetric — whatever `artifactName` emits, `resolveArtifactPath` with the same opts resolves back."* **True for a strict Epic ID; false for every non-strict value.** **Verified by execution** against the installed v0.1.13 with `eval-project-C`'s real STATE value: `artifactName('PLAN', {currentEpic:'PHASE11'})` → **`1-PLAN.md`**; `resolveArtifactPath(…, 'PLAN', {currentEpic:'PHASE11'})` → **`PHASE11-PLAN.md`**. With `M5.E9` both agree (`M5.E9-PLAN.md`). **Live field case:** `eval-project-C` carries `current_epic: PHASE11` — `detectMode` → **linear**, `setCurrentEpic` would have **rejected** it (it re-validates the strict shape), so it entered by a direct STATE write, the one path nothing validates. Its artifacts are all named `PHASE11-*.md`, so **reads work today only because the lenient regex matches**; the first `/sig:` command to *write* an artifact forks the naming and the write is then invisible to the next read. **The divergence is unobservable:** `detectMode` is named in exactly **one** command file (`ship.md`) — `/sig:status` and `/sig:resume` never print the detected mode, so a project reads as Epic to a human and linear to Signal with nothing saying so. **Secondary hazard, inferred from the instruction text and NOT measured** (it is agent-executed prose — M5.E8's exact subject): `ship.md` §0.5 derives `MILESTONE-{n}.md` from `current_epic`; `eval-project-C` has **no milestone file of any name** (verified), so an agent that eyeballs `PHASE11` as "Epic mode" rather than computing `detectMode` halts one step *before* `shipFR1Check` runs. **Origin — and it is a `B50` sighting.** Found while checking a `eval-project-C` session's claim that *"`shipFR1Check` halts with `NO_CURRENT_EPIC` on this project, which has never used Epics."* **That claim is false, on both the installed version and the one before it:** `PHASE11` is **truthy**, so the pre-`B42` `!state.current_epic` branch never fired either — executed, v0.1.13 → `{halt:false, skipped:true, reason:'linear mode …'}` and v0.1.11 → `{halt:false, skipped:true, reason:'not an Epic-close SHIP …'}`. The claim was composed from the *shape* of the situation ("no Epics → `NO_CURRENT_EPIC`") rather than from the artifact — **`B50`'s class, sighted in a downstream project rather than in Signal's own docs.** **Fix shape, three parts:** (1) a **`/sig:sweep` check for a set-but-non-strict `current_epic`**, naming what is therefore inactive — **shippable where M5.E9's removed phase-log check was not, and the distinction is the whole argument: the evidence here is *in the file* and mechanically testable against `EPIC_ID_STRICT_RE`, so it cannot false-positive on a healthy project**, whereas a past history-collapse was undetectable because the bug destroyed its own evidence; (2) **surface the detected mode** in `/sig:status` and `/sig:resume`; (3) **decide whether the two regexes should agree** — a design call, not a typo: the lenient one is load-bearing for existing non-strict projects' *reads*, so tightening it silently breaks them. **P2, not P1:** no data loss observed and reads currently resolve — but this is a silent stale-read path, and the same *"two layers, one field, opposite verdicts"* shape `B42` was closed on. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): the defect is fixed in **M5.E13 (S1.t1)** and was never flipped. The write/read split is closed **by construction**: `resolveArtifactPath`'s first candidate *is* `artifactName(artifact, {currentEpic})` (Pattern W, `tools/lib/resume.js:64`), so neither seam guesses at the other's Epic-ID regex — `references/epic-native-flow.md:33` was rewritten to claim symmetry by construction rather than by convention, and `tests/resolve-artifact.test.js:166` pins it. Fix-shape part (1) shipped separately as M5.E16's sweep check `(h)` `epic-id-not-strict` (`tools/lib/state-drift.js:617`). **Residual, deliberately left open:** fix-shape part (2) — *surface the detected mode in `/sig:status` and `/sig:resume`* — is **not** implemented; `detectMode` is still called by no renderer (only referenced in a comment at `tools/lib/resume.js:146`), so a non-strict project still reads as Epic mode to a human and linear to Signal with nothing saying so. Tracked as `B68`. |
| B54 | `fixed` | **P2** | **`checkGateArtifacts` is the never-called-guard class's FOURTH instance — and the only one that is not merely inert but *wrong if wired up*.** `state.js:616` exports a phase-gate artifact-existence checker. **Nothing calls it.** An exhaustive grep across `*.md`/`*.js`/`*.json` (minus `node_modules/`, `.git/`) returns exactly five hits: the definition; four test references (`tests/state.test.js:6,169,172,187`); and **three prose hits that describe it as part of the flow** — `CONTEXT.md:170` lists it in `state.js`'s API, and `STATE-HISTORY.md:222` + `archive/milestones/MILESTONE-3.md:79` both record a **fix once applied to it** (*"`checkGateArtifacts('.', 'PLAN')` now correctly finds PROJECT.md"*) — a repair made to a guard nothing runs. No command file, no hook, no tool invokes it. **The part that makes it worse than `B39`/`B46`/`I2`:** those three are absent, so wiring them up is neutral; this one **fails on Signal itself**. Executed at M5.E13 PLAN against this repo: `checkGateArtifacts(signal, 'PLAN')` → **`{ready:false, missing:['REQUIREMENTS.md']}`**. Its `PLAN` list is the hardcoded, unprefixed `['PROJECT.md','CONTEXT.md','REQUIREMENTS.md']`, while Epic mode writes `M5.E13-REQUIREMENTS.md` and no bare `REQUIREMENTS.md` exists — **so the obvious remediation (switch it on) would block PLAN for every Epic-mode project.** This is the same mode-dependent-name false seam `B53` proves and FR1.2's revision warns about. **Additionally, four of its five gates are vacuous:** `EXECUTE`/`VERIFY`/`REVIEW`/`SHIP` are empty arrays behind the comment `// Dynamically checked based on phase number` — dynamics never implemented — so all four return `{ready:true}` unconditionally. **Root cause of the invisibility, and the reason this is worth an ID rather than a footnote: being uncalled is what protected its bug from discovery.** A dormant guard is not neutral — it rots, and its documentation rots with it, so each dead guard is potentially two defects rather than one. **Found at M5.E13 PLAN** while reading `state.js` for FR1.2, which proposed building an artifact-existence refusal *in the same file where this unwired one already sat*. **Disposition (ratified 2026-07-29, Brett): REPLACE** — delete `checkGateArtifacts` and build the refusal fresh in `transitionPhase`, following M5.E9's precedent of removing a wrong check rather than re-tuning it (the `/sig:sweep` phase-log check). Repair was rejected as near-equal work plus rename risk; leave-in-place was rejected as shipping a new guard beside a dead one in the Epic named for dead guards. **Corrects D-M5E13-3's locked count of three instances to four** — which is itself the decision's own lesson (it was written to correct M5.E8's retro for under-counting the class from a single instance's shape). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13** by **deletion** and never flipped. `checkGateArtifacts` is gone from `tools/` — asserted, not assumed: `tests/guard-callers.test.js:164` fails if the name reappears in live code (historical records under `archive/` legitimately still name it). |
| B55 | `fixed` | **P2** | **The adherence canary's control arm is not isolated across files, so `deleteSection` never removed the instruction from the agent's reach — it only removed it from one file.** `adherence-run.js` calls `createPluginCopy(ROOT)` (whole plugin) and mutates **exactly one** command file (`adherence-run.js:374` — `join(plugin.root, 'commands', ${canary.command}.md)`). But `transitionPhase` is named **4×** in `plan.md`, **4×** in `verify.md`, **4×** in `review.md`, plus `calibrate.md`, `discuss.md`, `ship.md` and `references/state-schema.md`. An agent running `/sig:execute` in the control arm that opens any sibling command file finds the same instruction. **The `$deletionNote` in `adherence-canaries.json` records that the control was already fixed once for this exact class** — the first version deleted one line and left the section restating the instruction three more times, a near-miss `INERT`. The fix was widened from line to **section**. It should have been widened to **corpus**; it stopped one scope short. **Found 2026-07-29 by the first real use of the harness (M5.E13 S5.t1 / FR1.3).** Re-running `B41-phase-entry` against the reworded instruction returned **treatment 3/3, control 1/3 → `INDETERMINATE`**, replacing M5.E8's `OBEYED` (3/3 vs 0/3). The control run that leaked is the whole finding. **M5.E13 S2.t4 amplified it, by design and unavoidably:** FR1.1 required a **single shared wording** across all four middle commands precisely so they cannot drift, which means the control-arm agent now finds a **byte-identical** copy of the deleted instruction in `plan.md`. The requirement and the measurement are in direct tension, and nothing said so. **What this does and does not say about M5.E8's verdict:** the earlier `OBEYED` is **not falsified** — 0/3 means no leak was observed in those three runs — but it was **unisolated**, so it could never have distinguished *"the instruction works"* from *"the agent found it elsewhere."* The flagship verdict rested on a control arm that was clean by luck rather than by construction. **This is a fifth instance of M5.E8's own retro sentence** — *every defect this Epic found was in the measuring instrument, and every one produced a plausible-looking result rather than an error.* `3/3 vs 0/3 → OBEYED` is exactly such a result. **Fix shape (not attempted here):** delete the instruction corpus-wide across the copied plugin, not per-file — which requires the canary to declare a corpus-level pattern rather than a single `deleteSection` anchor, and requires deciding what a verdict means when an instruction legitimately appears in several commands. **Do NOT re-run for a cleaner number until the arm is fixed** — a second run is a coin-flip, and taking the better of two is the tuning M5.E8's impostor table exists to forbid. **✅ FIXED in v0.1.19 (M5.E15), 2026-08-06.** The canary now declares all five directive sites (`execute`/`plan`/`verify`/`review` by section, `ship.md` by line) and the control arm deletes every one; an **independent leak walk** (`tools/lib/adherence-leak.js`) then greps the whole copied tree and never reads `canary.deletions`, so it cannot confirm the mutation against itself the way the old check did. Unrecognised file naming the token → directive → **refuse before any agent runs**. Re-measured live: `B41-phase-entry` returned **`OBEYED`** (treatment 3/3, control 0/3, seam PASS, zero directive residue) — the same numbers M5.E8 recorded, now isolated. |
| B56 | `confirmed` | P3 | **⟨STALENESS RE-CHECK 2026-08-09 — numbers stale, structural gap fully live.⟩** The headline figure is three corrections old (`facts.md` now reads **2389**, set by `cut-release.js` from the gating vitest run). The defect is untouched: `cross-file-consistency.test.js` still compares docs to `facts.md` and **never to the suite**, and `cut-release.js` is referenced only from `tests/` — never from CI, never from a command — so *"nothing requires the script to be used"* remains exactly true. **`B77`'s fix is the pattern to copy here:** derive the number and pin it with a test that fails on disagreement; that is now shipped for `BUGS.md`'s tally and is the same shape this row needs for the test count. Original: **`references/facts.md` publishes a test count that is wrong by 912, and the cross-file guard over that exact file cannot see it — it pins two documents to each other, never to the thing they describe.** `references/facts.md:22` reads `**Test count:** 894`; the suite is at **1806**. `tests/cross-file-consistency.test.js` is the guard: it extracts `TEST_COUNT` via `extractFact('Test count')`, asserts `expect(TEST_COUNT).toBeTruthy()` (:46), and asserts that any doc mentioning "N tests" **matches facts.md** (:65 — `` `${doc} mentions "${hit} tests" but facts.md says ${TEST_COUNT}` ``). **It never compares facts.md's number to the actual test count.** So facts.md and README.md agree with each other, drift together from reality, and the suite stays green — a published, currently-wrong number with a passing guard over it. Its own file header calls facts.md the "source-of-truth," which is the assumption that makes the gap invisible: a source of truth is only true if something checks it against the source. **Found 2026-07-31 at M5.E17 PLAN**, incidentally, while establishing the Epic's test baseline — *not* by any mechanism, which is the M5.E13 retro sentence again. **Same class as `B39`/`B48`** (a guard that does not guard) with a data shape rather than a document shape, and it is a **fourth** location for it. **Not folded into M5.E17** — it traces to none of FR1–FR4 and absorbing it would be the scope creep the Epic's "Not doing" section forbids. **Fix shape:** have the test derive the real count (collect the vitest totals, or count `it(`/`test(` across `tests/`) and assert facts.md matches, so the guard compares the document to the world rather than to its own echo. → homed at **M5.E16** (the data-shaped detector); a one-line correction of the 894 → 1806 string is *not* the fix and would leave the guard exactly as blind. **UPDATE 2026-08-03 — the prediction in that last sentence has now been confirmed twice, by two more one-line corrections.** v0.1.16 corrected the number to **1938**; v0.1.17 corrected it again to **1954** (it was found stale by 16 while cutting that release). **M5.E16 shipped without taking this on**, so each release has repeated the same one-line fix and left the guard untouched — the number is right at each tag and wrong again by the next. This is no longer a predicted recurrence; it is a measured one, three instances deep. **Sharpening the fix shape, because half of the original suggestion does not work:** counting `it(`/`test(` across `tests/` **undercounts**, since several suites generate cases in a loop — `tests/status.test.js` builds its `nextActionForPhase` cases with `for (const [phase, skipped, expected] of cases) { it(...) }`, and `tests/noncanonical-phase.test.js` iterates `PHASES`. Static counting cannot see runtime-generated tests. **The workable half is the other one:** have the suite emit its own total (a vitest reporter, or a post-`npm test` step writing the count to a generated file) and pin `facts.md` to that artifact. Small tooling, not a one-line assertion — which is the honest reason this keeps getting deferred, and worth stating so the next person does not re-derive it. **Second, independent weakness in the same test, unfixed:** AC3 is vacuous by construction — *"only enforce if a doc actually mentions a count"* (`cross-file-consistency.test.js:59-60`) — so removing the count from README makes the assertion pass trivially rather than fail. Any fix should close both. **Still P3**: the published number is wrong between releases, which misleads a reader but breaks nothing. **UPDATE 2026-08-03 (second pass, same day) — the reason three corrections have not stuck is that the field has no defined semantic, and that must be decided before any guard can be written.** Nobody has ever said whether `facts.md`'s count means *"tests in the last shipped release"* or *"tests at HEAD."* The two readings disagree constantly and give opposite verdicts **right now**: the file reads **1954**, which is exactly what v0.1.17 shipped (correct under the release reading), while HEAD carries **1965** after the `B32` work (wrong under the HEAD reading). Its own header sentence — *"Updated at the v0.1.6 release"* — implies the release reading; this row's original framing (*"the suite is at 1806"*) assumes the HEAD reading. **Under the HEAD reading the number is unmaintainable by hand** — every test-adding PR invalidates it, which is precisely the churn that produced three throwaway corrections. **Under the release reading it is maintainable and is already part of the release checklist.** Recommendation: **adopt the release reading**, then the guard is not *"count the suite"* at all — it is *"`facts.md`'s count matches the count recorded at the current `plugin.json` version,"* which is a comparison between two artifacts that both change only at a tag. That is a far smaller mechanism than the vitest-reporter route sketched above, and it is why **`facts.md` was deliberately left at 1954 in this pass** rather than corrected a fourth time. **UPDATE 2026-08-03 (third pass) — the release reading is now implemented, and this bug stays `confirmed` anyway.** `tools/cut-release.js` sets the count from **the same `vitest --reporter=json` run that gates the release**, so a tagged release can no longer publish a stale number, and the hand-correction loop is broken. **Two reasons that is not a fix.** (1) **Nothing requires the script to be used** — a release cut by hand skips it entirely, which is exactly `B71`'s shape one file over: a mechanism that works, reachable only through a path nobody is obliged to take. (2) **The guard is unchanged.** `cross-file-consistency.test.js` still compares `facts.md` to `README`/`SECURITY.md` and never to the suite, so a hand-edit between releases is still invisible, and AC3 is still vacuous when no doc mentions a count. **What would actually close it:** record the suite count at tag time in a committed artifact the script writes, then assert `facts.md` matches *that* — a comparison between two artifacts that both change only at a tag, which is the mechanism sketched in the previous update. Until then this is a **process** where a **guard** is wanted. **Claim corrected the same day:** `CHANGELOG.md` and the `cut-release.js` commit message both originally said the script *"closes `B56`"*. It does not. That sentence was written from the shape of the work rather than from the artifact — the class [`../analysis/CLAIM-INTEGRITY-ANALYSIS.md`](../analysis/CLAIM-INTEGRITY-ANALYSIS.md) names and this row's own subject. Caught by checking the status column against the claim before reporting completion. |
| B57 | `fixed` | P2 | **`/sig:sweep` walks the migrate snapshot backup and reports a frozen directory as broken live docs — 92% noise on a real project.** `/sig:migrate-memory` writes a pre-reorg snapshot to `.planning/.migrate/snapshot/` before relocating anything; its links point at **pre-migration paths by design**. `WALK_IGNORE` (`tools/lib/doc-hygiene.js:40`) already exempts `archive/` for exactly this reason (AC1.2) and simply never listed `.migrate`. **Found 2026-08-01 by the first run of `/sig:sweep` against real non-Signal projects** (eval-project-C, eval-project-A, eval-project-F, eval-project-D) — the FR1-first-use discipline shipped in v0.1.15, applied the day after it shipped. eval-project-A reported **12 structural findings; 11 were snapshot links**. **This is the precision failure M5.E16's FR2 exists to prevent, present in the tool today:** a report that is 92% noise gets muted within a week, and a muted checker looks like coverage while providing none — the same reasoning that deleted M5.E9's REVIEW Critical rather than re-tuning it. **Fixed 2026-08-01 (`.migrate` added to `WALK_IGNORE`, RED-first):** eval-project-A 12 structural → **2, both real** (a stale INDEX and a genuine link escaping the repo root into `~/.codex/`). Control test asserts a dead link in *live* `.planning/` is still flagged, so the exemption is scoped, not a mute. |
| B58 | `fixed` | **P1** | **`marketplace.json`'s pinned `sha` sat two releases behind its `ref`, so every `/plugin` install since v0.1.14 silently delivered v0.1.13.** The entry read `ref: "v0.1.15"` with `sha: "8d20193…"` — and `8d20193` is **v0.1.13's release commit**. Claude Code resolves the **sha**, so the `ref` was decorative. **Found 2026-08-01 by the user**, who ran `/plugin` → update and was told *"sig is already at the latest version (0.1.13)"* — two days after v0.1.14 shipped and twenty minutes after v0.1.15 did. **The report was truthful; the manifest was lying to it.** **Impact: every fix in v0.1.14 (M5.E13, guards that don't guard) and v0.1.15 (M5.E17) was unreachable.** The user was running v0.1.13 machinery against live projects while reporting that Signal 'constantly' needed teeny manual fixes — an unknown share of that experience is bugs already fixed in releases that could not reach him. **Why the guard missed it, and it is this session's own theme:** `install-contract.test.js` asserted the sha's **shape** (`/^[a-f0-9]{40}$/` — a stale sha passes) and separately asserted `ref === v{plugin.version}` (correct, and **not the field being resolved**). **Neither compared the two fields to each other**, so `ref` advanced every release while `sha` sat still and the suite stayed green — the same class as `B56` (a guard checking the property adjacent to the one that matters). **`B7` recorded this exact drift at v0.1.7** (*"marketplace.json pins sha 8e05e56… but v0.1.7 resolves to 0eb8ca8… — needs a look, not the test cause"*) and it was never closed. **The note was the guard**, which is `B39`'s shape. **Fixed 2026-08-01:** sha → `8acd1d2` (v0.1.15), plus a test that resolves `source.ref` via `git rev-list -n1` and asserts the sha equals it. **Proof-of-fail recorded:** the new test fails against the exact manifest that shipped for two releases. It **throws rather than skips** when tags are unresolvable — a guard protecting release delivery must never pass by being unable to run. **→ Superseded same day by DELETION (the better fix).** The `source` block is now the relative `.` form — the plugin **is** this repo — so `url`/`ref`/`sha` are gone and **there is no second place to record which commit ships**. Signal was the only marketplace on the machine using a pinned remote source; `prose`, `cloudflare`, `openai-codex` and `anthropics/claude-code` all use a relative path. The v0.1.1 fix that introduced `url` was choosing against the **`github` shorthand** (which resolved to SSH and broke stranger installs) — the relative form was never on the table. The resolve-the-tag test was removed with it: it guarded a shape that no longer exists. What replaced it guards the **shape, not the value** — `source` must be the string `.`, and no 40-char sha or `ref` key may reappear anywhere in the file, so the fragile form cannot come back. **Trade accepted knowingly:** users now track `main` rather than a pinned tag, which is safe because CI gates every push and Epic work moves to branches (`D-M5E17-4`). |
| B59 | `fixed` | P2 | **`M5.E16-PROFILE.md` carried two values outside their enums, so the Epic that declared FEATURE ran DISCUSS at the project's FULL — and nothing anywhere validated it.** `calibration.stakes: moderate` (valid only for `reversibility`) and `calibration.reversibility: easy` (valid nowhere) both fail `readProfileFromPath`. `readEffectiveProfile(baseDir, {currentEpic: 'M5.E16'})` therefore **throws**, and `resume.md` §4 fail-opens to the project PROFILE — so `/sig:resume` reported tier **FULL** for an Epic whose file says FEATURE, and the Epic's whole point of overriding `research_parallelism: 0` / `plan_validation_dims: core` / `gate_strictness: light` was inert. **Found 2026-08-01 at the `/sig:plan` tier-gating preamble**, the first time any code read the file. Written by hand in `18741a8` — the *same commit* as M5.E16's DISCUSS — and unread for the entire phase. **Both values are plain-English words a person writes without opening the schema**, which is why prose review would not have caught it and a parse does instantly. **Validation short-circuits on the first bad field**, so fixing what the error message named would have fixed one of two and left the file still unloadable. **Why nothing caught it:** Epic-scoped profiles are `created_by: hand`, no test walked `.planning/*PROFILE.md`, and no sweep or doc-hygiene check reads PROFILE at all (`grep PROFILE tools/lib/sweep.js tools/lib/doc-hygiene.js` → 0 hits). **Fixed 2026-08-01 (fix lane):** `stakes: minor`, `reversibility: trivial` — matching the file's own prose (*"no security boundary… a `git revert` away from undone"*) and its closest sibling `M5.E17-PROFILE.md` (`stakes: minor`). **Pinned by `tests/own-profiles-parse.test.js`:** every `.planning/*PROFILE.md` must load, *and* each Epic profile must be demonstrably **reached** rather than silently falling back to the project profile — because a profile that parses but is never selected is the same invisible failure one step over. **Proof-of-fail recorded:** the test goes red against the exact file that shipped through DISCUSS. |
| B60 | `confirmed` | P2 | **All six phase commands handle only the "PROFILE.md not found" error and have no branch for a malformed one — the four meta commands do.** `discuss.md`, `plan.md`, `execute.md`, `verify.md`, `review.md`, `ship.md` each carry one identical line: *"If neither PROFILE.md is present: `readEffectiveProfile` throws the same not-found error — halt with …"*. `readEffectiveProfile` also throws `ProfileSchemaError` for a **malformed** profile (bad tier, bad `schema_version`, any calibration or rigor-override violation), with a schema message, not a not-found one — and those six files give no handler, so an agent following them literally hits an unhandled throw at the preamble. `status.md` (§"any other `ProfileSchemaError`"), `init.md`, `add.md` and `resume.md` all branch on it explicitly. **6 silent / 4 explicit — and the split runs exactly the wrong way:** the commands that handle it are the read-only meta commands; the ones that don't are the six that consume the tier to set rigor. **Surfaced 2026-08-01 by `B59`**, which is the malformed case arriving for real. **This is `D-M5E17-2`'s shape one layer over** — that audit found `markFresh` ordering stated in 2 command files, silent in 2, wrong in 1, and fixed all five together. **Fix:** decide the correct behavior once (halt with the schema message? fail open to the project PROFILE as `resume.md` does? the two are *not* interchangeable — a briefing may degrade, a phase that sets rigor arguably may not), then state it in all ten command files rather than the four that thought of it. Pin with a test comparing the files to each other, as M5.E17 did. Not fixed with `B59`: `B59` was one file's data, this is ten files' instructions and needs a decision, not an edit. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `ProfileSchemaError` is named in exactly four command files — `add.md`, `status.md`, `init.md`, `resume.md` — and in **none** of the seven phase commands. The asymmetry the entry describes is unchanged. |
| B61 | `confirmed` | P3 | **A hand-edited `last_updated_commit` whose short sha looks numeric is silently type-coerced by the YAML reader.** `0012345` parses to the integer `12345` (leading zeros stripped), `1e23456` to `Infinity`, `0e12345` to `0` — so the stored baseline becomes a *different value* than the one written, and every consumer comparing it to git history (`isStateStale`, `checkBaselineCommitOffHistory`) is comparing against a sha that was never a commit. Roughly **2–3% of short shas** match a YAML number form (all-digits is ~3.7% on its own, plus the `<digits>e<digits>` float form, since `e` is a valid hex character). **Signal's own writers are SAFE** — the `yaml` package quotes numeric-looking strings on `stringify`, verified: `stringify({last_updated_commit: '0012345'})` emits `"0012345"`. **The exposure is a hand-edited or externally-generated STATE.md**, which is not hypothetical: Signal's own `.planning/STATE.md` has been hand-maintained for months, and `/sig:migrate-memory` exists precisely because real projects arrive with hand-written state. **Found 2026-08-02** in the M5.E16 REVIEW loop, as a ~1-in-10 flake in `state-drift-checks-general.test.js` — the test fixture hand-wrote the sha unquoted, reproducing the exposure exactly. **The fixture was fixed** (quote the value); the product gap is filed here rather than fixed, because the honest fix is a *reader-side* coercion guard (`readState` should reject or re-stringify a non-string `last_updated_commit`) and that touches the core state reader every command depends on. **Current behaviour is fail-safe, not silent-wrong:** a coerced value fails `checkBaselineCommitOffHistory`'s `/^[0-9a-f]{7,40}$/` applicability test and reports **not-applicable with a reason** rather than a false clean — which is the M5.E16 contract working. `isStateStale`'s behaviour on a coerced value is **not yet characterised** and is the reason this is `confirmed` rather than `dismissed`. |
| B62 | `confirmed` | P2 | **⟨STALENESS RE-CHECK 2026-08-09 — the headline has LAPSED; a residue is live.⟩** The claim is squash-merge specific, and the **Epic lane moved to `--merge` at v0.1.19** (`CLAUDE.md`) precisely so branch commits survive. Verified: STATE's `last_updated_commit` **is** an ancestor of `HEAD`. *"Every release closes with an orphaned sha"* is no longer true for Epic-lane releases. **Still live:** the fix lane still squashes, and `ship.md` §9 still gives no post-merge instruction. **Re-scope or close — do not fix as written.** Original: **`ship.md` §9's `markFresh` records a sha that the squash merge then destroys — so every release closes with `last_updated_commit` pointing at a commit no branch reaches.** Two instructions, each correct alone, that contradict when combined. **§9** (M5.E17 `FR3`) says *create the SHIP commit, **then** `markFresh`* — precisely so the recorded sha reflects the release rather than predating it. **`D-M5E17-4`/`D-M5E17-5`** (written **later**, 2026-08-01) moved Epic work onto a branch merged via **`gh pr merge --squash`**. A squash merge **discards the branch commits** and writes a new one, so the sha §9 just recorded is orphaned the moment the PR merges. **Confirmed live on v0.1.16's own ship, 2026-08-02:** `markFresh` stamped `e7416c9`, the squash produced `8d94b76`, and `git merge-base --is-ancestor e7416c9 HEAD` failed — `isStateStale` reported the release commit as unreflected work, and **M5.E16's own new check `(d)` fired on the release that shipped it**. **Not data loss and it self-heals** — the next STATE write re-stamps a reachable sha (exercised in production immediately after the merge: `findings` → `clean`), which is exactly the category-1 contract `(d)` declares. **But every release will trip its own detector until the ordering is stated.** **Fix direction:** §9 cannot stamp a sha that survives its own merge, so the freshness stamp belongs in a **follow-up write against the merged HEAD** — which is what the repo has been doing by hand (`chore: markFresh STATE after … merge`) without any instruction saying to. **`ship.md` should say it**, or `markFresh` should refuse a commit that is not reachable from the default branch. **This is the M5.E17 class recurring in the file M5.E17 fixed**, caused by a decision made *after* that fix and never re-checked against it. |
| B63 | `fixed` (v0.1.18) | P2 | **FIXED — flipped 2026-08-04**, verified in source not documents: `tools/lib/migrate-memory.js:1945` opens the fix block naming the bug, and `explainArchiveOutcome` is appended unconditionally at `:1976`. `CHANGELOG.md` `[0.1.18]` § Fixed listed it while this row still read `confirmed` — **the third instance in one release of the fixed-and-never-flipped pattern this file's own footer said was "about to happen again inside the Epic meant to stop it."** Original entry follows. | **`/sig:migrate-memory`'s dry-run prints `0` for its two Epic-only vectors on a linear project, so "could not apply" is byte-identical to "already clean."** `renderDryRun` pushes `vector-3 (closed-Epic evicts): ${v3.evicts.length}` and `archive-tree moves: ${archive.moves.length}` unconditionally (`migrate-memory.js:1931-1932`). On a project that does not use Epic IDs both are **structurally** zero, not incidentally: `planArchiveMoves` filters its input through `EPIC_ID_STRICT_RE` and only matches `.planning/{epicId}-{suffix}.md` (`archive-tree.js:96-110`), and `extractEpicSection` rejects any non-strict ID (`evict.js:175`). If nothing else fires, `noop` goes true (`:1907`) and the whole run reports as a no-op — **a linear project is told the migration is complete when half of it never applied in principle.** **This is `C1`'s exact class in the command next door**, live in the same week M5.E16 shipped the four-status model that fixes it for `/sig:sweep`: a `0` that means *could not look* rendered the same as a `0` that means *checked and clean*. **Found 2026-08-02** reviewing a eval-project-A session report; confirmed by reading the renderer, not by running it. **Fix shape:** apply M5.E16's applicability distinction here — a linear project should read *"not applicable: no Epic-ID'd units in this project"*, never `0`. The missing **capability** (an archive path for linear units) is a separate, Epic-sized item and is filed in `BACKLOG.md`, not here; this entry is only the false-clean report. |
| B64 | `fixed` | P2 | **`senseArchiveTree` treats a retrospective file's existence as proof that an Epic is closed, and discards the `isStub` flag it already computed.** `closedEpicIds = retros.map((r) => r.epicId)` (`archive-tree.js:356-358`) — unfiltered. `enumerateRetros` returns a record for **any readable file matching `*-RETROSPECTIVE.md`** (`retro-index.js:46-82`) and sets `isStub` on it (`:74`, true when the content carries a `[FILL IN]` marker). That flag is consumed by the retro-index renderer (`:197`), by `doc-hygiene.js:410`, and by `/sig:resume`'s summary (`commands/resume.md:89`) — **never by the archiver.** So a placeholder retro is sufficient to archive a live Epic, and `B36` records retros being written *before* the gate ran, so an early retro is not hypothetical. **Bounded, not data-loss:** relocate-never-delete holds and a pre-apply git tag exists, so the failure is a wrong move, not a destroyed file. **Independently reported from eval-project-A 2026-08-02**, whose own archiver has this gap one layer over and whose report names it exactly — *"that's a label, not a guard — the planner still can't tell closed from live."* Theirs keys on slice-filename shape; Signal keys on a **different filename's** shape. A better proxy, still a proxy. **Fix shape:** drop `isStub` retros from `closedEpicIds` and state the exclusion in the dry-run — this uses a signal the code already produces and adds no new inference. **✅ FIXED 2026-08-04 (M5.E18 S5.t1).** `senseArchiveTree` now filters `isStub` out of `closedEpicIds`. Measured on Signal’s own tree: 23 retros, **4 stubs** — `M4.5.E1`, `M4.5.E3`, `M4.5.E6`, `M4.5.E7` — and all four were in `closedEpicIds` before the fix (RED-first, `AC5.2`); none are now. A companion test asserts `M5.E13` (complete retro) **is** still proposed, so the filter is not a blanket off-switch. **The sibling sweep found this was 1 of FIVE sites, not 2** — `detectDirtyExecute`, `senseVector3` and `checkEpicWithoutRetro` had the same defect and the plan named none of them. Root cause is one value, not five bugs: `isStubRetro` has existed since M4.5.E9 and `enumerateRetros` reports `isStub` on every record, but it was consumed in **exactly one place** — rendering `*stub*` in `INDEX.md`. Every caller that used a retro to make a *decision* threw it away. Fixed as one: `RETRO_STATUS` + `retroStatusFromContent` in `retro-index.js`, five consumers. See `M5.E18-PLAN.md` § S5 correction. |
| B65 | `confirmed` | P3 | **The migrate's fs-backup snapshot is never expired, aged, or reported — and `B57`'s ignore is what made it invisible.** `applyMigrate` writes `.planning/.migrate/snapshot/` plus a `*` gitignore beside it in `fs-backup` mode or `dirty && force` (`migrate-memory.js:2406-2412`). **`B57` (fixed 2026-08-01) then added `.migrate` to `WALK_IGNORE`** (`doc-hygiene.js:40-50`) — correct for noise, and measured: eval-project-A went 12 structural findings → 2, both real. **The consequence is that nothing looks at the snapshot at all.** Nothing checks its age, its size, or whether it can still roll back; the `*` gitignore means git cannot surface it either. eval-project-A carried **34 pre-migration doc copies** from the 2026-07-20 v2→v3 migration with roughly a dozen shipped slices on top of them — a recovery pointer that could no longer recover, invisible **by construction** one day after the ignore shipped. **This is the precision/coverage trade M5.E16's FR2 exists to manage, resolved in one direction without the other half being built.** **Fix shape:** an **advisory** sweep finding naming `.migrate/snapshot/`, its file count and its age. Deliberately advisory — a fresh snapshot is correct and must not read as a defect. |
| B66 | `confirmed` | P3 | **`checkStaleInbox` fires at `n > 0` with no magnitude, so a 110-entry inbox produces the same one-line advisory as a 1-entry inbox.** `sweep.js:188-192` emits `inbox has ${n} undrained entries — consider draining` for any non-zero count, with no threshold, no tiering, and no escalation. **The concrete cost, observed 2026-08-02:** eval-project-A removed a contradictory second index and deliberately filed a **tripwire** into `ISSUES-INBOX.md` — recording what was removed and the symptoms that would point back — specifically so it would surface at the next `/sig:plan` drain. It became **entry #110** in a pile that Signal describes with the sentence it used at entry 1. **Their mechanism was sound; Signal is what muted it.** Not a eval-project-A problem: Signal's own inbox sits at 48 candidates with `B46`'s drain non-convergence still open and its premise falsified, so the pile grows on both sides. **Fix shape:** tier the advisory by count, or state the count against a stated threshold, so "draining is overdue" is distinguishable from "one thing came in." |
| B67 | `confirmed` | P3 | **Nothing detects a second, rival index — the freshness check only ever looks at the one file it manages.** `checkIndexFreshness` reads exactly `.planning/INDEX.md` (`sweep.js:68-70`); `isForeignIndexFormat` (`planning-index.js:131-138`) judges **that one file's** format and downgrades a hand-written one to advisory. Neither notices that a *different* index-shaped artifact exists alongside it. **Found by hand at eval-project-A 2026-08-02:** `.planning/llms.txt` at **1182 lines**, still carrying four dead rows, against a **482-line** `INDEX.md` — two indexes disagreeing about the same corpus, with no way to know which an agent would read first (they had to grep across `mjs/ts/tsx/json/yml/md` to establish that nothing did). **The shape was anticipated and never checked for:** `references/doc-runtime-model.md:178` names `llms.txt` explicitly, as out-of-scope for discovery. Out-of-scope for generation is not the same as absent from the tree. **Fix shape:** flag a second index-shaped artifact in `.planning/` as a finding, since the failure is contradiction rather than staleness and the freshness check cannot express it. |
| B68 | `confirmed` | P3 | **The detected Epic/linear mode is never printed, so a project reads as Epic mode to a human and linear to Signal with nothing saying so.** The unimplemented half of `B53`'s fix shape (part 2 of 3), split out 2026-08-02 when that entry was reconciled to `fixed` — parts (1) and (3) shipped, this one did not, and folding it into a closed row would have hidden it. `detectMode` (`tools/lib/state.js`) is called by **no renderer**: `tools/lib/status.js` never imports it, and `tools/lib/resume.js` names it only in a comment (`:146`). It is named in exactly one command file, `ship.md`. So `/sig:status` and `/sig:resume` both print a briefing whose artifact resolution, retro gate and archive eligibility all depend on a mode neither one shows. **Live instances today:** `eval-project-C` (`current_epic: "PHASE12"`) and `eval-project-I` (`current_epic: "M1"`) — both non-strict, both therefore linear to every resolver, both reading as Epic mode to anyone who opens STATE.md. M5.E16's sweep check `(h)` (`epic-id-not-strict`) reports the contradiction, but only when someone runs `/sig:sweep`; the two commands people actually run at orientation stay silent. **Fix shape:** call `detectMode(state)` in `renderResumeBriefing` and the `/sig:status` renderer and print the mode on the `Phase:` line. **Sequencing:** natural fit for **M5.E18**, which is re-measuring the linear/Epic split across the corpus anyway. |
| B69 | `confirmed` | P3 | **The SHIP retro write-guard cannot evaluate a project whose `current_epic` is non-strict, and says nothing — the third behaviour on a field everyone assumed had two.** `checkProposedStateWrite` reaches `expectedRetroPath({current_epic})` → `deriveRetroPath`, which **throws** on any value not matching `M{N}[.{N}]*.E{N}` (`tools/lib/retrospective.js:149`). The `PreToolUse` hook wraps that call in a bare `catch` and exits 0 (`hooks/check-state-write.js:116-122`). **The fail-open is deliberate and correct** — the comment says so, and a `PreToolUse` hook must never crash a normal edit. **What is wrong is the silence:** the guard did not pass, it *could not run*, and nothing distinguishes those. **`B42` enumerated two behaviours for this field** — Layer 1 hard-halts on a missing Epic, *"Layer 2 fails open — `checkProposedStateWrite` returns `{block:false}` on a null/absent `current_epic`"*. A **truthy but non-strict** value is neither: it throws. **Verified by execution 2026-08-02** against `eval-project-I` (`current_epic: M1`, `phase: SHIP`, all six pre-SHIP phases present in `completed_phases` — every precondition met): `checkProposedStateWrite` threw `deriveRetroPath: malformed epicId "M1"`. `eval-project-C` does **not** trip it, and only by accident — its `phase` is a prose blob rather than the literal `SHIP`, so the function returns at the first gate. **Exactly `B63`'s shape one layer over** — *could not apply* rendered identically to *already clean* — which is why it belongs with **M5.E18**'s port of M5.E16's four-status model rather than as a standalone fix. **Fix shape:** have `checkProposedStateWrite` catch the malformed-ID case itself and return a distinct `{block:false, unevaluated:true, cause:"non-strict-current-epic"}`, and have the hook surface it as a warning instead of swallowing it — the same *needs a person vs. clears itself* split `/sig:sweep` already ships. |
| B70 | `fixed` | **P1** | **FIXED 2026-08-03 (fix lane, ahead of M5.E18 — `D-M5E18-5`).** Both commands now route through **`describeNextAction`** (never throws) + **`formatNextActionCopy`** (renders the copy); `nextActionForPhase` keeps its strict contract, so callers wanting the guard still get it. The briefing renders in full and **names the problem** — phase unrecognized, a one-line elided excerpt of what STATE.md holds, and the seven valid values. Rendering nothing was rejected as `B39`'s shape. 16 tests in `tests/noncanonical-phase.test.js`, RED-first, keyed to `eval-project-C`'s real five-paragraph `phase` value; verified end-to-end against that project, the one of three locally-reachable projects that reproduced the throw. **Original report:** **`/sig:status` and `/sig:resume` throw outright on a project whose `phase` is not one of the seven canonical names — 5 of 12 real projects, measured.** `nextActionForPhase` throws `unknown currentPhase "…"` on anything outside `PHASES` (`tools/lib/status.js:46-50`). **Neither command carries a fail-open instruction for it, and every neighbouring optional read does** — verified by reading both files: `isStaleVsOrigin`, `readLayoutBanner`, `readStateSizeForTier` and `readEffectiveProfile` are each marked *fail-open* or *advisory* in `resume.md` and `status.md`; `nextActionForPhase` is the one call nobody marked safe. In `resume.md:128` it sits **inside the argument list of `renderResumeBriefing`**, so a literal execution throws before the render is entered; `status.md:175` computes it at §2.6. **Scope of the evidence, stated rather than blurred:** the throw is **measured**; the user-facing consequence is **read, not run** — these are agent-executed prose files, and what a given agent does when an instruction throws is not determined by the file (M5.E8's exact subject). So the defensible claim is *the orientation commands lose their next-action step, and nothing in either file tells the agent to continue without it* — not *the user is guaranteed to see nothing*. **Nothing upstream stops the bad value getting there:** `readState` returns `phase` verbatim without validating it against `PHASES`, and the `PreToolUse` frontmatter-shape guard only ever inspects `completed_phases` — `checkStateFrontmatterShape` calls `sectionLines('completed_phases')` and nothing else, and `sectionLines` returns `null` for any **inlined** scalar (`tools/lib/retrospective.js`), so a multi-line prose `phase:` is invisible to it by construction. That is `B8`'s vector-1 class on a field the de-prose work never covered. **Measured 2026-08-02, not cited** — every `/Users/macstudio/dev-biz/*/.planning/STATE.md` read through `readState`, then `nextActionForPhase(state.phase, [])`: **7 ok · 5 crash · 1 `readState` throws** (13 total, one of them Signal). Crashing: `eval-project-C`, `operator-os`, `eval-project-K`, `eval-project-J`, `eval-project-E` — every one of them a hand-maintained linear project that parked narrative in the `phase` scalar. `eval-project-B` never gets that far (`readState` throws). So **6 of 13 real projects cannot run the two orientation commands**, and M5.E16's corpus table already recorded the input (*"Canonical `phase`: 7 of 12"*, `M5.E16-RESEARCH.md:54`) without anyone executing the consequence. **`P1`, matching `B42`'s precedent:** not degraded behaviour — two documented commands refusing to run at all against the majority mode. **Fix shape (decide, do not assume):** the cheap half is for both commands to tolerate a non-canonical `phase` — render the raw value and say *"not a recognised phase, so there is no next action"* rather than throw; the real question is whether `readState` should quarantine an off-enum `phase` the way `partitionCompletedPhases` (the `B45` fix) already quarantines an off-enum ledger entry, which would give one answer for both fields. **Sequencing: M5.E18.** It is the same population — the 8-of-12 linear projects the archive half does not reach — and fixing the archive for projects whose `/sig:resume` throws would be shipping the second half of a door. |

| B78 | `fixed` | P3 | **`commands/review.md` states the PASS-WITH-FIXES rule four times and no two agree — and the test pinning it is a fifth voice with a different count.** Reported from `eval-project-A` 2026-08-03, then re-verified against Signal's own files (the eval-project-A numbers are **not** used here — they arrived mojibake'd and are unverifiable from this repo). Four statements: `:121` checklist bullet; `:125` guidance paragraph — *"conjunctive, and all four are load-bearing"*, **`≤ 50 LOC`**, *"no **design** impact"*; `:132` PASS-WITH-FIXES table row — **`< 50 LOC`**, *"no **architectural** impact"*; `:133` FAIL row — *"any fix **`> 50 LOC`**"*. **Three distinct disagreements.** (1) **Four conditions or six:** `:125` declares four, then names *"ripple beyond a single file"* and *"require new tests"* as further disqualifiers — neither is among the four, and the table shows four. (2) **The operator splits three ways** (`≤ 50` / `< 50` / `> 50`), so a fix of **exactly 50 lines** is PASS-WITH-FIXES per the paragraph and **falls through both table rows**. (3) **The cap never states its denominator** — 50 LOC of what? Source only, or source plus tests? Insertions, or insertions plus deletions? Under any reading tests inflate the count, so the reviewer who writes the regression test lands nearer FAIL than the one who does not. **Live drift, from Signal's own corpus rather than the reporter's:** `M5.E16-REVIEW.md:60-67` is a real REVIEW that says *"the four conditions are conjunctive"* and then builds **six rows** — `:66` treats *"requires new tests"* as load-bearing, and `:67` adds *"requires re-validation"*, **a condition that appears nowhere in `review.md`. The reviewer invented one.** That is the tell: once the enumerated set is visibly not the real set, readers stop trusting the enumeration and supply their own. **No wrong verdict is on record** — that REVIEW returned FAIL on *"no design impact"*, a genuine condition, so the outcome was correct regardless. What is demonstrated is drift in how the rule is **read**, not a bad call. **Third miscalibration of this same paragraph:** `D-M5E17-1` exists because the table read *"FAIL \| Any Critical"* while practice went the other way for two releases; one release later the replacement text contradicts itself. **Single home confirmed** — `skills/` holds no copy (grepped), so the fix is a small diff. **Traps for whoever fixes it:** (a) `tests/commands-wording.test.js:240` asserts the literal `'50 loc'` — rewording to *"50 non-test source lines"* turns it **red**; keep the token and qualify around it. (b) That same test block is a **fifth voice**: its comment at `:195-196` enumerates **three** conditions and its case at `:235` is named *"the three conditions stay CONJUNCTIVE"* while the file says four — shipping the file fix without moving the test reproduces the exact defect class being fixed. (c) The LOC denominator refines `D-M5E17-1`, and `PROJECT.md:30` makes IDs permanent addresses — precedent is a **new dated entry that supersedes**, not a rewrite in place; the test at `:245-251` passes under either shape, so CI will not catch a wrong choice. **⚠ OPEN PRODUCT QUESTION, Brett's call, blocks only the first fix step:** is *"requires new tests"* wanted as a **real** disqualifier — *a fix needing new coverage is by definition too big to close in-phase* — or was it always shorthand for the LOC cap? Recommendation is to cut it (the cap already bounds risk; tests reduce it), but that is a call about how strict REVIEW is on itself. **Deliberately NOT bundled with `B74`/`B76`**, the other two `review.md` verdict-path findings: those need new capability, this is doc correctness. **Natural deadline: fix before M5.E18 reaches its own REVIEW**, or the same paragraph misfires a third time on the Epic that is auditing it. **✅ FIXED 2026-08-04, fix lane, ahead of the deadline** (`D-M5E18-6`). **Product call (Brett): "requires new tests" flips from disqualifier to obligation** — the fix carries new coverage, in-phase, and it must be green. Rationale recorded in the decision: the old wording penalised the reviewer who wrote a regression test, *and* `"all tests still pass"` only ever constrained tests that **already existed**, so a fix could close in-phase with zero new coverage and satisfy every condition — the rule read strict while leaving the test-debt door open. The flip closes it and removes the perverse incentive in one move. **Cap now `≤ 50 LOC of non-test source` (insertions + deletions), with the required coverage excluded from it** — it has to be, or the new obligation fights the cap. Operator unified `≤`/`≤`/`>` so exactly-50 has a verdict; the row's *"no architectural impact"* becomes *"no design impact"*; *"touches architecture"* and *"ripples beyond a single file"* **demoted to illustrations of design impact, not cut** — a fix that ripples still fails, on the condition it was always failing. **No count is written anywhere now**: the set is enumerated, and a test fails on any spelled condition-count in `review.md`, including the correct one. All three traps handled — the `'50 loc'` token preserved and qualified around it; the test block's own stale count removed (it was the fifth voice); a new dated decision rather than a rewrite of `D-M5E17-1`. New `B78` describe block pins the statements **against each other**, RED-first verified (7 of 7 failed pre-fix). 2054 → **2062 tests**. `M5.E16-REVIEW.md:60-67` deliberately left as written — a record of a REVIEW that happened, not a statement of the rule. |
| B79 | `confirmed` | P3 | **`evictEpicNarrative` has never been able to fire for Signal's own `.planning/STATE.md`, and reports that as a clean no-op.** Found live at M5.E18's SHIP (2026-08-04): §5.5 returned `{evicted:false, reason:'no-section'}`. `extractEpicSection` locates an Epic's narrative by a `## {EpicID}` **heading**, and Signal's STATE.md carries its per-Epic narrative as **bold-prefixed paragraphs under one `## Resume pointer`** — `grep -cE '^## M[0-9.]+\.E[0-9]+' .planning/STATE.md` returns **0**. So the evict-on-close feature (M5.E1 FR2b) has been shipped and gated on every Epic-close SHIP since, and has never once had a section to move in this repo. **`no-section` is documented as "a safe no-op"** — correct for a project that genuinely has no narrative, and indistinguishable from one whose narrative is in a shape the extractor cannot see, which is `C1`/`B63`'s class exactly: *could not look* rendering as *nothing to do*. Consequence: STATE.md grows without bound in the one repo that runs this flow most, and the growth it was built to stop is invisible. **Not a regression and nothing is lost** — relocate-never-delete means a no-op simply leaves the narrative live. **Two candidate fixes, neither taken here:** teach `extractEpicSection` the bold-paragraph shape, or have `no-section` distinguish *no narrative* from *narrative present in an unrecognised shape* and say so. The second is the smaller and matches M5.E16's model. Filed at SHIP rather than fixed — the Epic was already past REVIEW. |
| B80 | `fixed` | P3 | **`ADHERENCE-LOG.md` carries three kinds of annotation and `NOTICE_KINDS` knows two, so the third was hand-written into an append-only log.** `tools/lib/adherence-log.js:187-191` freezes `NOTICE_KINDS` to `INVALIDATED` and `QUALIFIED`, and `appendNotice` **throws** on any other kind. But the log's `INDETERMINATE` record carries a `> ### ⚠ DIAGNOSED` block (`.planning/ADHERENCE-LOG.md:205`), and `grep -rn DIAGNOSED tools/ tests/` returns **nothing** — so that block cannot have come from `appendNotice`. It was written by hand into the one file whose integrity guarantee is *"structural, not procedural"* (`appendRunRecord`'s header: there is no code path that reads an existing record, so there is none that can rewrite one). The guarantee holds for **records**; annotations have no such protection, and the only supported way to add one silently mislabels a diagnosis as a qualification. **Found 2026-08-04 at M5.E15 DISCUSS**, while establishing how M5.E8's unisolated `OBEYED` should be amended (`D-M5E15-7`) — i.e. by the next person who needed the mechanism, which is the same way `B55` was found. **In scope for M5.E15 (AC7.3):** whichever kind FR7 uses must exist in code, or this Epic's own amendment is a second hand-edit. Same class as `B39` and `B54` — a convention that lives in a document while the code knows a smaller version of it. **✅ FIXED in v0.1.19 (M5.E15), 2026-08-06.** `DIAGNOSED` is a real kind in `NOTICE_KINDS` — a finding about the *instrument*, where the number stands and what it could ever have shown is what changed. An unknown kind still throws. M5.E8's `OBEYED` record now carries its unisolated stamp appended **from code**: the first annotation in that log not written by hand, and `appendNotice`'s first production caller. |
| B81 | `confirmed` | P2 | **`guard-callers.test.js` governs 1 of ~25 CLI flags in `tools/`, and its "population is not empty" assertion cannot tell that apart from governing all of them.** `findCliGuards` detects flags with a single regex, `argv.includes('--flag')` (`tests/guard-callers.test.js:76`). `tools/` contains **three** argv idioms: that one (`adherence-ceiling.js`, 1 flag), a `for`-loop `a === '--flag'` comparison (`adherence-run.js`, **12 flags**), and `args.includes` over a sliced argv (`cut-release.js:41`). Measured by re-running the test's own `findCliGuards` + `hasCaller` logic with all three patterns: population **1 → 25 flags across 8 files**, surfacing **10 flags with no caller**, every one in `adherence-run.js`. **The first assertion — *"finds at least one CLI guard to govern (the population is not empty)"* — passes on a population of one.** It was written to prove the check is not vacuous and it cannot distinguish *found everything* from *found one thing*: `C1`'s class inside the file built to fix that class. **The fix is NOT to widen the regex.** The 10 orphans are mostly *options* (`--runs`, `--keep`, `--transcripts`, `--arm`), not *guards*; the test's stated scope is "CLI **guards**" and it has no definition separating a flag that gates behavior from one that carries a value. So the stated population and the detected population are **both** wrong, in opposite directions, and reconciling them needs that definition written down first. **Found 2026-08-04 at M5.E15 PLAN**, by checking whether `D-M5E15-5` (register the new leak check here) actually lands — it does not: `adherence-run.js`, the file M5.E15 modifies, contributes **zero** flags to the governed population despite having twelve. M5.E15 therefore uses a bespoke named assertion (`B54`'s shape) and does not absorb this; widening would drag 10 flags and a missing definition into an unrelated Epic. |
| B82 | `fixed` | **P2** | **`planArchiveMoves` does not use `deriveUnits`, so it archives half a unit — the exact outcome M5.E18's own test comment says it prevents.** `archive-tree.js` rebuilds every candidate as `` `${PLANNING_DIR}/${unit}-${suffix}.md` `` and never consults `work-units.js`; `grep -rn deriveUnits tools/` returns `closure.js:260` and `state-drift.js:496` and **nothing in `archive-tree.js`**. `deriveUnits` performs a **conservative fold** that the template cannot express, so the two disagree about which files belong to a unit. **Measured 2026-08-04 against eval-project-A's live `.planning/`:** `deriveUnits` resolves `SLICE-SSO` to **5 files** (folding `PLAN-SLICE-SSO-RESEARCH.md` + `PLAN-SLICE-SSO-VALIDATION.md` in alongside the three `SLICE-SSO-*` files); `planArchiveMoves(['SLICE-SSO'], …)` plans **3**. Two files the system already knows belong to the unit are left live. **M5.E18 knew.** `tests/work-units.test.js:86-90` carries a `eval-project-A_SPLIT_PAIRS` fixture built from these real filenames and a comment stating the split *"puts the VERIFICATION on one side and the PLAN on the other, **so FR2 would archive half a slice**"* — the Epic wrote the sentence describing the defect it shipped, fixed it in the derivation, and left the mover on the old template. **This is the two-implementations-of-one-rule shape** that `M5.E18-REQUIREMENTS.md` open question 1 and M5.E13 REVIEW both warn about, realised across two modules. **Impact:** a unit split across `.planning/` and `.planning/archive/` — `INDEX.md`, cross-doc links and the next reader all disagree about where it lives. The external tool this replaces at least moved all-or-nothing. **Found 2026-08-04** while writing the manual-archiving runbooks for eval-project-A + eval-project-D, by running both functions against the real corpus instead of reading either one. **Fix shape (not attempted):** `planArchiveMoves` takes the file list `deriveUnits` already returns, rather than re-deriving names. **Separate and NOT this bug:** a bare `PLAN-SLICE-SSO.md` (no recognised suffix) stays `ungrouped` — accepted by design under `D-M5E18-2`, which requires the ungrouped set be **reported, never dropped**. |
| B83 | `fixed` | P2 | **`guard-callers.test.js`'s scope-limit assertion was vacuous: every string it pinned appeared inside the assertion that pinned it.** `AC2.4 — this file states its own scope limit` read the WHOLE file into `self` and asserted `self` contained `SCOPE LIMIT`, `2 of the 4 known instances`, `B39`, `B46`, `NOT a check for the`. Each of those literals also occurs in the `expect(...)` line asserting it, so the test matched its own source and could never fail. **Proven, not reasoned: deleting the entire header comment block — all five pinned strings — left the test green.** Shipped M5.E13 (2026-07-29) and unguarded for three releases. **This is the exact failure the file was written to catch, and the file says so forty lines above**, in `hasCaller`: it explicitly skips `guard-callers.test.js` when looking for callers because *"a checker that satisfies itself is the failure it looks for"* — the authors saw the trap for the mechanism and not for the label. The scope limit is `D-M5E13-3`'s deliverable, the thing that stops the guard-caller mechanism over-claiming its coverage; it was the one part of the file nothing protected. **Found 2026-08-05 at M5.E15 VERIFY**, by mutation-testing the M5.E15 assertions rather than trusting them green — the new `AC5.2` note assertion was written the same way and was equally vacuous, so the Epic came within one commit of shipping a second instance. **Fixed in the same pass:** both assertions now read a `headerBlock()` helper scoped to everything above the first `import`, so a pinned string must appear where a reader actually meets it. A further assertion pins the non-vacuity itself (the header must be strictly shorter than the file and must not contain `expect(header)`), and both mutations — deleting the header, and removing only the `B81` banner — now correctly go red. **Second-order finding, same fix:** the `auto-discovered count is NOT the coverage count` assertion only ever passed because the phrase wraps across a comment line in the header and *did not* wrap in the assertion. Scoping to the header made it fail honestly, and it now matches the wrapped form. |
| B84 | `fixed` | P2 | **`cut-release.js`'s "you forgot to write the release notes" guard cannot fire in this repo, and what it does instead is rewrite history.** `foldChangelog` refuses when `/^## \[Unreleased\]/m` finds nothing — a guard whose stated purpose is that *"a release with no notes is the failure this is meant to prevent, not a case to paper over."* But `CHANGELOG.md:239` carries a **permanent, historical** `## [Unreleased]` section: M5.E7's v2 direction audit, which deliberately shipped no code and so was never versioned. That section satisfies the guard **unconditionally and forever**, so the refusal branch is unreachable. Worse than merely dead: the replace is `/m` without `/g`, so it takes the **first** match — and when no new `[Unreleased]` exists at the top, the first match is the historical one. **Observed, not reasoned:** cutting v0.1.20 without pre-written notes rewrote `## [Unreleased] — 2026-07-26 — The v2 direction audit (M5.E7)` into `## [0.1.20] — 2026-08-06 — …`, relabelling a closed record as this release and destroying the only heading that marks M5.E7 as no-code-shipped. Caught by reading the diff; nothing in the tool or the suite objected. **This is the guards-that-don't-guard class (v0.1.14 / M5.E13's entire theme) inside the release tool itself,** and it compounds a known interaction: the same historical heading is why `changelogBetween` skips M5.E7's section in every `/sig:update` delta (already filed as a heading-capture). **Fix shape:** anchor the fold to an `[Unreleased]` heading that appears **above the newest released `## [x.y.z]` heading**, so a historical one lower in the file can neither satisfy the guard nor be the replace target; and give M5.E7's section a non-`[Unreleased]` heading so one file has one meaning for the token. Filed 2026-08-06 from the v0.1.20 cut; the release was completed by writing the notes first, which is the documented-but-unenforced order. **→ FIXED same day.** The anchor is now **positional**: the pending section is the one *above* the newest released `## [x.y.z]` heading, so anything below it is history and can neither satisfy the guard nor be the replace target. **Verified against the real tool, not a fixture** — `node tools/cut-release.js 0.1.21` now refuses by name and leaves M5.E7's heading byte-identical, where the same invocation before the fix relabelled it. **The old refusal test passed the entire time**, because its fixture omitted the `[Unreleased]` heading the real file always has — a guard proven on a corpus that could not exhibit the bug, which is why the new tests assert against the shipped `CHANGELOG.md` itself. **And the fix exposed a second, quieter instance of the same dependency:** three version-site tests were green *because* of the bug — they read the real CHANGELOG and relied on the historical heading to make `releaseEdits` succeed. They now seed their pending section explicitly. M5.E7's heading was deliberately **left as-is**: renaming it would silently change the status of the separately-filed `changelogBetween` heading-capture, and that is a different bug's call to make. 2269 → **2273 tests**. |
| B85 | `fixed` (v0.1.23) | P2 | **`/sig:update`'s only mutating step prescribes a command the CLI rejects.** `commands/update.md:49` reads *"Run `claude plugin update sig`"*. Measured 2026-08-06 during a live `/sig:update` run at v0.1.19: that command prints `✘ Failed to update plugin "sig": Plugin "sig" not found` and exits **1**. The marketplace-qualified form works — `claude plugin update sig@signal` → `✔ Plugin "sig" updated from 0.1.19 to 0.1.20`, and `installed_plugins.json` then correctly reads `0.1.20` at `.../cache/signal/sig/0.1.20`. **Likely cause, stated as inference:** `installed_plugins.json` keys every plugin as `name@marketplace` (`sig@signal`, `prose@prose`, `supabase@claude-plugins-official`), and `claude plugin update --help` takes a single `<plugin>` argument that appears to resolve against those keys, so a bare name matches nothing. Not confirmed against the CLI's source — only against its behavior on this machine. **Impact:** steps 1–4 (read installed, read available, render the CHANGELOG delta) work correctly; step 5, the one action that changes anything, cannot succeed as written. **It fails loudly** — non-zero exit plus a `✘` line — so it does not produce a false *"updated, go restart"*; the harm is a dead final step and a runner forced to improvise the fix mid-flight, which is how this was found. **Nothing in the suite executes the CLI string in `update.md`,** so a prescription that has never worked shipped in v0.1.16 and survived four releases. Same shape as the prescriptions-vs-code drift M5.E17 fixed for `ship.md` — a command file asserting a behavior no test compares against reality. **Fix shape (fix lane):** change `commands/update.md:49` to `claude plugin update sig@signal`, and add a test pinning that the file prescribes the marketplace-qualified form — the test can pin the string in the file; it cannot verify the CLI offline, and should not pretend to. |
| B86 | `dismissed` | — | **`claude plugin update` exits 0 on failure.** Non-bug (triaged 2026-08-06, in the same session that surfaced it). Reported to the user as a second finding alongside `B85`, on the strength of an `EXIT=0` printed after **both** the failing and the succeeding invocation. That number was produced by `echo "EXIT=${PIPESTATUS[1]:-$?}"` following a pipe into `tail`: `PIPESTATUS` is bash's spelling and is unset in zsh, so the `:-` fallback read **`tail`'s** exit status, not `claude`'s. Re-measured without a pipe — `out=$(claude plugin update sig 2>&1); rc=$?` — gives **`rc=1`** on the failing form. The CLI's exit code is correct, and `B85` therefore cannot cause a silent false-success. **Recorded rather than dropped** for two reasons: the claim reached the user before it was verified, and the trap is reusable — an exit code read through a pipe in zsh is not the exit code of the command you care about, so any future finding of this shape must be re-measured unpiped before it is filed. Same discipline as `B1`: surfaced from unverified surface behavior, dismissed by measuring properly. |
| B77 | `fixed` | P2 | **FIXED 2026-08-09 — the count is now DERIVED and pinned.** `tools/lib/bugs-tally.js` counts **both** formats (table rows *and* `**Status:**` heading-captures), normalises the status cell before counting so `` `fixed` (v0.1.13) `` is not invisible — the fix this row's own footer note asked for in those words — and `tests/bugs-tally.test.js` fails when the published tally disagrees with the file. **Proof-of-fail run, not claimed:** the live guard was shown red against the real 2026-08-08 error (`28 confirmed` / `26`), naming the cell and printing the corrected segment, then green after restore; two historical fixtures pin both real failures, including the trap that **the total was correct because the two errors offset** — which is why a spot-check of one cell is not a re-derivation. A missing tally returns `no-tally` and **fails** rather than passing, per `B39`. **The other half is deliberately NOT closed:** `captureToBugs` still inserts with `insertAtEnd`, so a captured bug still lands *below* the tally — see the capture at this file's tail. The mechanism: `FUTURE_IDEAS_FOOTER_RE` anchors on `*Last updated:…*`, and **this file's footer is a tally line that does not match that shape**, so even `promoteToBugs` — which correctly calls `insertAboveFooter`/`rewriteFooter` — cannot see it. The guard does not prevent that insertion; it makes it **loud**, which is the difference between a guard and a preference. **Original entry:** **`BUGS.md`'s tally cannot see the capture format Signal itself writes, so a captured bug is uncounted and unreferenceable for as long as nobody triages it — and nothing anywhere says one exists.** Found 2026-08-03 reconciling a parallel session's work. `promoteToBugs` (`tools/lib/backlog.js:206`) and `captureToBugs` append a **heading-style entry** at EOF — its docblock states the design verbatim: *"a SIMPLE entry (heading + `**Status:** needs-triage` + verbatim body + `---`) … **no B-ID, no table row (triage is a later human step)**"*. **The format is intentional and correct; the gap is in the counting.** The tally at the foot of this file derives from `\| B{n} \| \`status\` \|` **table rows**, so on 2026-08-03 it read **"0 needs-triage"** while **four** `**Status:** needs-triage` entries sat ~30 lines below it (the `LE-1`…`LE-4` captures, now triaged to `B73`–`B76`). **This is `B56`'s class one layer over, and strictly worse:** `B56` was a hand-maintained count that drifted, which a re-derivation fixes. This is a **derived** count whose derivation is structurally blind to the format the capture tool produces — so re-deriving it *correctly* still yields the wrong answer about the file. The tally's own text says *"Counts re-derived by grepping the status column, not incremented"*, which is true and still wrong. **Confirmed by execution:** the grep returns `0 needs-triage` against a file containing four. **Consequence:** a capture can sit indefinitely — uncounted, un-numbered, uncitable — and the one number a reader checks says the triage bucket is empty. **Fix shape:** count both shapes, or report un-triaged captures as their own line so *"0 needs-triage"* can never mean *"0 captured, 4 waiting"*. The second is preferable — it keeps the two states distinguishable, which is the same distinction M5.E16 shipped for `/sig:sweep`. |
| B76 | `fixed` | P2 | **`review.md`'s FAIL path is a bare "return to EXECUTE" with no user ask and no loop ceiling — an unbounded loop for any unattended mode.** **FIXED 2026-08-21, and the fix is a counter rather than a sentence.** The prediction in this row came true exactly: `/sig:drive` shipped in `v0.1.31` and inherited the unbounded loop on day one — `FLOORS` had five entries and none at REVIEW, and `drive.js` carried no iteration cap at all. ⚠ **Mirroring `verify.md`'s wording would have closed this on paper only**: that ceiling (*"you've looped <3 times"*) is **prose enforced by nothing**, counted by a reader from memory — writing the rule more carefully is the remedy `UNREACHED-MECHANISM-ANALYSIS.md` names as the failure. So the count is **derived from state**: `completed_phases` is append-only and records the phase being *left* (`B44`), so the number of `REVIEW` entries **is** the number of completed passes, and it is per-Epic for free because `setCurrentEpic` resets the log on a roll (`B9`). New `tools/lib/loop-ceiling.js` (`loopStatusFor`, `countPhaseCompletions`, `formatLoopCeilingHalt`) reuses `partitionCompletedPhases` rather than reimplementing the parse (`B82`'s lesson), and `canProceedUnattended` **refuses** on it — above the attention branches, because `unattended` buys freedom from being *asked*, not an unbounded loop. **It also refuses when it cannot read the count** (`loop-unknown`, kept distinct from `loop-ceiling` so the halt never implies evidence it lacks): an actor that cannot tell should stop. The arithmetic is `verify.md`'s existing rule unchanged — two re-entries run, the third stops. `review.md` gains the 3-options ask it never had. **Proof-of-fail:** 4 of the 21 new tests fail against the pre-fix `drive.js` and pass after. ⚠ **Scope: the driver's decision, not the model's obedience.** This bounds what `/sig:drive` will do; a human or agent ignoring `review.md`'s ask is the adherence problem, unmeasured as ever.** Was `LE-4`, captured 2026-08-03 by the loop-engineering gate audit, triaged to a numbered row 2026-08-03. Contrast `verify.md:106-125`, which asks via the 3-options pattern and carries a **3-loop ceiling**; `references/phase-gates.md:24-30` documents the VERIFY→EXECUTE loop only — **no REVIEW→EXECUTE loop is documented anywhere**. **M5.E16 hit a live REVIEW→EXECUTE loop-back in the field**, so this is not hypothetical. Tolerable while a human is attending; an infinite-loop hazard the moment anything runs unattended. **Needs new capability, so Epic-homed rather than fix-lane.** Detail: `analysis/LOOP-ENGINEERING-ANALYSIS.md` §3.3. **PARTIALLY ADDRESSED 2026-08-21 — and deliberately NOT closed.** The *documentable* half is done: `phase-gates.md` now records that **EXECUTE has no approval checkbox by design** (its exits are machine-checkable), because "a reader cannot tell design from omission" is `B39`'s shape and an undocumented absence in a gate reference reads as a missing gate. **The load-bearing half is untouched:** the five `"User approves…"` checkboxes (`discuss.md`, `plan.md`, `verify.md`, `review.md`, `ship.md`) are still unconditional while the dial above them is conditional. ⚠ **What changed is the premise, not the size.** This row's triage said it "needs new capability"; `attention` (`v0.1.31`) **is** that capability, so the remaining work is wiring rather than design — but it is still five command files plus the phase-gate rendering, and this row homes it to an Epic. Left `confirmed` on purpose: marking it `fixed` for the half that was cheap is exactly the claim-integrity defect this repository is named after. **CLOSED 2026-08-21.** Four of the five boxes now read *"when `gates.confirm_{phase}` is set"* and name their flag; **the fifth is deliberately still unconditional** because `ship.md`'s *"User approves PR for merge"* is a **floor** (`ship-pr`), not a tier-gated ask — merge is delivery (`D-M5E17-5`), and the box now says so at the box so nobody tidies the asymmetry away. Pinned by a test that asserts the four name their flag **and** that SHIP does not. ⚠ **The lane call recorded in this row was revised, not quietly ignored:** this row and `phase-gates.md` both said *"Epic-homed, not fix-lane"* on the strength of the original *"needs new capability"* triage — `attention` (`v0.1.31`) **is** that capability, leaving wiring with no design surface, which is the fix lane by this repo's two-lane rule. The `phase-gates.md` sentence was corrected **in the same commit that did the work**, because leaving it standing while doing the opposite would be `M5.E17`'s class committed inside the file just fixed for `B73`. |
| B75 | `confirmed` | P2 | **`gate_strictness: light` and `strict` expand to identical gate config in code except a single boolean — every other difference between them is prose.** Was `LE-3`; triaged 2026-08-03 and **re-verified by reading the switch**, not the summary: `applyRigorOverrides` (`tools/lib/profile.js:392-423`) sets `auto_advance` and all six `confirm_*` flags **identically** for both, differing only in `gates.anti_rationalization`. *"Confirm at every step"* — the documented meaning of strict — is enforced **nowhere programmatic**. Related and measured: `execute.md:25-27`'s wave-boundary confirm exists in that one preamble row only — not in the §2 workflow body, not in `tools/lib/execute.js` (`dispatchTaskWithState` is per-task, and reads `gate_strictness` only to decide state-write failure handling), and not in `agents/executors/executor.md` (zero user-facing asks). **Live instance:** M5.E18 is executing at `strict` and its wave-boundary gates are being honoured **because the command text says so**, not because anything enforces it. **The tier system's most-cited rigor dial is therefore thinner than every document implies** — which matters most for the attention-axis proposal, since that design assumes the dial is real. Detail: `analysis/LOOP-ENGINEERING-ANALYSIS.md` §3.3. **RE-TRIAGED 2026-08-21 after the attention split (`v0.1.31`) — the headline claim is now FALSE, and the finding still stands.** Measured, not reasoned: `applyRigorOverrides` on two otherwise-identical profiles now differs in **two** behavioural flags, not one — `gates.anti_rationalization` **and** `gates.confirm_in_phase` (plus the derived `workflow.attention` label and the echoed input). So *"identical except a single boolean"* no longer describes the code. ⚠ **But the practical finding is unchanged, because the new boolean has ZERO consumers.** `confirm_in_phase` is written at `profile.js:455` and read by **one test and nothing else** — no phase command consults it. *"Confirm at every step"*, the documented meaning of `strict`, is still enforced nowhere at the point of use. The dial now produces a different **value**; it does not yet produce different **behaviour**. That is `UNREACHED-MECHANISM-ANALYSIS.md`'s exact shape, and `bugs-tally.js`'s exact history — correct since `B77`, only caller a test, until `M6.E2` reached it. **Stays `confirmed` on purpose:** flipping it to `fixed` because the config diff grew would be claiming a behaviour change no user can observe. **Its remainder is the same work as `B74`'s** — the five unconditional *"User approves…"* checkboxes ARE the in-phase asks, so wiring `confirm_in_phase` into the phase commands and making those checkboxes conditional is **one slice, not two**. **STILL OPEN after the 2026-08-21 wiring slice, and the reason is the point.** That slice (`B74`/`B108`) made six command files name `gates.confirm_in_phase` where the in-phase ceremony lives, so the flag finally has consumers — **but they are prose consumers.** Ask what would *fail* if a command simply ignored the flag: **nothing.** No test breaks, no gate refuses, no run halts. The asks are now **conditional in prose** rather than **unconditional in prose**, which is real progress and is not enforcement. ⚠ **Closing `B75` on the strength of that would be this repository's named defect** — and it would be committed in the same week as `analysis/PHASE-C-BUILD-VS-ADOPT.md`, whose central finding is that Signal puts load-bearing checks in JS *because* the prompt layer is 77.6% unmeasurable (`M5.E8`). A document cannot be both the argument and the exception. *What would actually close it:* something that fails when an in-phase ask is skipped — the adherence harness's canary shape (`adherence-run --canary`) applied to `confirm_in_phase` is the existing mechanism, and `B55` is the precedent for how easily such a canary measures the wrong thing. Until then this row records a dial that is **documented** end to end and **enforced** nowhere. **UPDATE 2026-08-22 — the OBSERVABILITY half shipped; the row stays `confirmed` ON PURPOSE, because the enforcement half was DECLINED, not missed.** `confirm_in_phase` now has a consumer, so the *"read by one test and nothing else"* sentence above is no longer true: `hooks/record-ask.js` (`PostToolUse` on `AskUserQuestion`) appends each observed question and its running phase to `.signal/asks.jsonl`, and `hooks/check-state-write.js` reports at phase close — detected as one new `completed_phases` entry, which is append-only and records the phase being *left* (`B44`). **Hookability was settled by running it**, not by reading: two readings of the same doc page disagreed and the *"not hookable"* one was a summarising model's inference (`OPEN-QUESTIONS.md`). The test carried a **control arm** on a second, deliberately-triggerable tool, because a silent log cannot separate *"did not fire"* from *"setup broken"* — `B55` exactly. ⚠ **It WARNS and never refuses** — asked as a direct either/or on 2026-08-22, the answer was report-don't-block, keeping `check-state-write.js`'s two-tier posture (integrity blocks, process warns) intact. **So this closes as CHECKED AND REPORTED, not ENFORCED, and `phase-gates.md` now says so in those words.** Nothing fails when a command ignores the dial, which is why the row above still describes reality and the status is unchanged. ⚠ **It also counts nothing, and that is not a gap to tighten later:** measured across the six preambles, `confirm_in_phase` means a *countable* quantity in only two — `execute.md` (every wave boundary) and `ship.md` (every checklist step) — while `discuss.md` is one per gray area found as you go and `plan.md`/`verify.md`/`review.md` say "every step" without defining a step. A count would be fabricated for four of six. So the check answers one question — *was anything asked at all* — and a box-ticking question satisfies it. **An absent record reads `cannot-check`, never clean** (`B39`). *What would still close this row:* something that FAILS on a skipped ask — the `adherence-run --canary` shape named above remains the candidate. |
| B74 | `fixed` | P3 | **Every phase's Exit Criteria carries an unconditional "User approves…" checkbox while the same file's §0 preamble makes that approval tier-conditional.** Was `LE-2`; triaged 2026-08-03. Sites: `discuss.md:156`, `plan.md:217`, `verify.md:104`, `review.md:162`, `ship.md:191`. Same gate, two statements in one file, and the checkbox ignores the profile — **M5.E17's defect class exactly** (instructions contradicting instructions), in an Epic that shipped. Also noted: **EXECUTE has no approval box at all**, which may be intentional (its exits are machine-checkable) but is undocumented, so a reader cannot tell design from omission. **Needs new capability** — the checkbox has to become conditional, which means the phase-gate rendering learns about `gate_strictness` — so Epic-homed, not fix-lane. Detail: `analysis/LOOP-ENGINEERING-ANALYSIS.md` §3.3. |
| B73 | `fixed` | P3 | **FIXED 2026-08-21.** The opening line now states the real rule — approval is set by the `attention` dial (`attended` / `checkpointed` / `unattended`) — and names the two things the dial **cannot** turn off: the `FLOORS` roster (referenced, not re-listed, so there is one roster rather than two) and the `B76` loop ceiling. The old absolute is kept as a boxed correction rather than deleted, because it is the clearest example of the class: the reference whose job is to state the rule was the file contradicting the code. Original: **`references/phase-gates.md:3` says "Every phase transition requires explicit approval. No exceptions." — and `gate_strictness: off` is exactly that exception, shipped as a SKETCH default.** Was `LE-1`; triaged 2026-08-03. `off` means auto-advance (`references/profile-schema.md:150`) and appears in every phase command's preamble table; `references/tier-definitions.md:141` ships SKETCH with it. **Nothing pins the two documents together**, though the pinning pattern already exists for tier precedence (`tests/tier-precedence-consistency.test.js`) — so this is a guard-shaped gap, not merely a wording slip. **M5.E17's defect class** (instructions contradicting instructions) in a fifth location, found by a different audit. Detail: `analysis/LOOP-ENGINEERING-ANALYSIS.md` §3.3. |
| B72 | `fixed` | **P2** | **`/sig:discuss`'s done-Epic guard — the one that stops a writing command clobbering a finished unit's artifacts — returns `false` for *every* linear-mode project, so it has never been able to fire on 8 of 12 real projects.** Found 2026-08-03 at M5.E18 PLAN research, by reading `isEpicDone`'s body rather than its docblock. `isEpicDone` (`tools/lib/retrospective.js:168`) opens with `if (typeof epicId !== 'string' \|\| !EPIC_ID_STRICT_RE.test(epicId)) return false;` — and `false` is the *"not done, proceed"* answer. So on `eval-project-C` (`current_epic: PHASE12`) or `eval-project-I` (`M1`), the guard evaluates *not done* no matter what is on disk, and `/sig:discuss` without `--epic` proceeds into a completed unit. Its own docblock states the contract it is failing: *"a writing command against a done Epic with no `--epic` must halt rather than clobber that Epic's artifacts"*, and `discuss.md:54` — **the only caller** — spells out the damage: *"it would clobber `{EpicID}-REQUIREMENTS.md`"*. **The strict gate is doing double duty as both a shape check and a verdict**, which is the exact conflation M5.E18 exists to end: `EPIC_ID_STRICT_RE` failing means *"I cannot evaluate this"*, and the code returns *"I evaluated it and the answer is no."* **`B42`'s shape** — a documented mode a whole guard silently does not serve — and **`B53`'s seam** (a non-strict `current_epic` splitting one behaviour in two), now in a third location. Note the asymmetry that hid it: in Epic mode the guard works, and Signal-on-Signal has been Epic-mode since M4.5.E11, so no dogfood run could ever have surfaced it. **Not a regression** — `isEpicDone` has been strict-gated since M4.5.E11.S1.t1, when `EPIC_ID_STRICT_RE` was made the single source of truth; the linear-mode consequence was never derived. **Absorbed into M5.E18 as part of FR5's slice (Brett, 2026-08-03)** rather than filed and deferred: FR5 already rewrites `isEpicDone`'s closure condition for stub retros, so the shape fix and the stub fix land in the same function, in one edit, with one pair of tests. Fixing only the stub half would leave the function still blind to the projects the Epic is being written for. **✅ FIXED 2026-08-04 (M5.E18 S5.t3).** `isEpicDone` now returns `{status: 'done'|'not-done'|'cannot-evaluate', reason}` — a non-strict unit id is `cannot-evaluate`, never the proceed answer. Its only caller is prose (`commands/discuss.md:54`, re-verified), so the polarity is pinned in the document too: **proceed only on a clean `not-done`; halt on `done` or `cannot-evaluate` unless `--epic` was passed.** `AC5.4` asserts the halt and `AC5.6` the escape hatch — the same sentence read both ways, because a newly-unconditional guard that makes a documented mode unusable is `B42`, which M5.E13's REVIEW shipped once already. **`tests/epic-native.test.js:285` was this bug in test form**: it asserted `false` for a non-strict id, pinning the exact collapse this fixes. Rewritten against the three-value contract. |
| B71 | `confirmed` | **P2** | **⟨STALENESS RE-CHECK 2026-08-09 — the live consequence is gone; the defect is not.⟩** `M5.E17-RETROSPECTIVE.md` was backfilled 2026-08-03, so `isEpicDone('M5.E17')` is now true and `REQUIREMENTS.md` is no longer clobberable — the *symptom* this row was filed from has resolved itself. **The detector gap is untouched:** `retrospective.js` still carries only its two detectors and no world-check, so the next compressed Epic — the kind most likely to skip a retro — remains invisible to the gate. **Do not close on the symptom.** Original: **The retro gate can only fire on an Epic that ran the full six-phase sequence — so its coverage is inversely correlated with the risk it exists to cover, and the Epic most likely to skip a retrospective is the one it cannot see.** Verified by reconstructing `M5.E17`'s own release. At the v0.1.15 ship commit (`8acd1d2`) `STATE.md` read **`phase: PLAN`**, **`completed_phases: [DISCUSS (2026-07-31)]`**, `current_epic: M5.E17` — and `MILESTONE-5.md` contained **zero** `E17` rows. The release commit touched five files (`plugin.json`, `package.json`, `marketplace.json`, `CHANGELOG.md`, `STATE.md`): **no retrospective, no SHIP artifact, no phase advance.** The whole Epic was three commits — DISCUSS, one combined work commit, the release. **`/sig:ship` was never invoked, and the retro gate lives inside `/sig:ship`** (`ship.md` §0.5 → `shipFR1Check`), so nothing ran and nothing said so. **The gate would not have fired even if it had been invoked**, and this is the actual defect: both Epic-close detectors return `false` for this shape. `isEpicCloseShip` (`retrospective.js:352`) returns `false` when `findEpicStatusRow(...) === null` — **there was no row**. `isEpicCloseByState` (`:410`), the `B26`/`M5.E5.T2` fallback that exists precisely for row-absence, requires `phase === 'SHIP'` **and** `completed_phases` covering every tier-enabled pre-SHIP phase — it had **one of five**. **An Epic that runs all six phases is the one least likely to skip its retro; an Epic compressed into two commits is the one most likely — and is exactly the shape both detectors are blind to.** **This is the class M5.E13 and M5.E16 both named, in a third shape:** the gate verifies one *document's claim* (a milestone row, a STATE phase ledger) against another, and never against the world — *did a release actually get cut?* `B36` was the near-miss on this same gate (a **stale** row silently skipping it, made loud in v0.1.11); this is a **missing** row plus a phase ledger that never advanced, and `B36`'s loud-skip path cannot reach it because that path only runs once the gate has already decided this is an Epic close. **Not caught by the gate — caught after the fact by `/sig:sweep`'s `epic-without-retro` check** (M5.E16, v0.1.16), on **2026-08-03 at the `/sig:resume` that opened M5.E18**: its first fire in normal use, one day after shipping, working exactly as designed. **Detection exists; prevention does not.** **Noted, not blamed:** `M5.E17` is the Epic that removed `ship.md`'s direct-to-main self-exemption, and its own release was cut directly to `main` without running `ship.md`. **The branch-and-PR ruleset (`D-M5E17-5`) does not close this** — a PR is not a ship, and no rule requires `/sig:ship` to run before a tag. **Live consequence, low severity today:** `isEpicDone` is `existsSync(retroPath)` (`retrospective.js:168`), so `isEpicDone('M5.E17')` is **`false`** — `/sig:discuss`'s done-Epic guard would not halt on it, leaving `M5.E17-REQUIREMENTS.md` clobberable by anyone who sets `current_epic` back to it. **Fix shape (not attempted here):** the gate needs a third detector keyed on something outside `.planning/` — a tag, a `plugin.json` version bump, a release commit — so *"a release happened for this Epic"* is answerable without the phase ledger's cooperation. That is a **world-check**, and it is `M5.E18`'s FR2 question (*what proves a unit is closed*) pointed at Signal's own repo rather than at a user project. **Homed at M5.E18** — same mechanism, same Epic; see `D-M5E18-3` and the FR5/AC5.3 open question, which this makes answerable: **`isEpicDone`'s tightening should be in scope, not deferred.** Filed 2026-08-03. |

**M5.E4 close-out (v0.1.9, 2026-07-20) — the confirmed-bug backlog cleared.** All 12 in-scope confirmed bugs fixed (or dismissed), RED-first, in Epic M5.E4:
- **B5** → `fixed` (`b5f3631`): flat `eslint.config.js` — `npm run lint` runs clean (0 errors).
- **B6** → `fixed` (`8fbdecf`): resume/status banners gate on genuine drift (`HEAD..origin`), not the bookkeeping "+1".
- **B8** → `fixed`: both halves shipped v0.1.8 (`WHOLE_FILE_NOTE` + vector-1 de-prose); M5.E4 added the command-level `block:true→false` flip assertion in `migrate-all-vectors.test.js`.
- **B14** → `fixed` (`b5ed3f0` + REVIEW completion): shared `path-confine.js` realpath guard at evict/add/resume (add.js reversal ratified, D-M5E4-5). **REVIEW caught an incomplete fix at the `evict.js` site** — it passed the *directory* to the guard, so the guard resolved only the archive parent and a directory symlink at the Epic *leaf* still escaped (a false-green test planted the symlink one level too high). Completed in REVIEW: anchor the guard on the *file* path (matching `archive-tree.js`) + a RED-first leaf-symlink test.
- **B15** → `fixed` (`cb4cbf9`): the blocking dangling scanner reads the full file (no 1 MB truncation).
- **B16** → `fixed` (`9d36aa4`): the pre-apply tag is deleted after a successful rollback.
- **B17** → `fixed` (`0c284b9`): `vitest.config.js testTimeout: 15000` for git-heavy suites.
- **B18** → `fixed`: already `8f1dda8` (M5.E3); M5.E4 `f810acb` added the `regeneratePlanningIndex('.')` round-trip guard.
- **B19** → `fixed` (`dd5c1c5`): marker-based foreign-INDEX guard (the shipped legend-based one was a false-green) + dry-run parity; RED-proven against `main`.
- **B20** → `dismissed`: duplicate of B5 (its "no B5 entry exists" premise was false).
- **B21** → `fixed` (`0505cab`): the D-ID map is built once for the anchor-gate loop.
- **B22** → `fixed` (`cd7504d`): `checkInternalLinks` bounds the target to repo root before any disk touch.
- **B23** → `fixed`: coverage bundle — (a) `8fec7bf` (organic case proven unreachable + real-gate coverage), (b) `cb4cbf9` (>1 MB fixture), (c/d) `d554861`, nits `f67196f`.

Newly discovered / surfaced during the Epic, all deferred to v0.1.10: **B24** (pre-existing dangle blocks migrate), **B25** (FR5 AC5.2 test-strength — read-enclosure correct-by-inspection), **B26** (`shipFR1Check` Epic-close detection skips on the self-hosted flow).

**M5.E5 close-out (v0.1.10, 2026-07-21) — the M5.E4 carry-overs cleared.** All 3 confirmed carry-overs + the B6 local-stale refinement fixed RED-first in Epic M5.E5 (FULL/strict, sequential dispatch, suite 1529 → **1561**):
- **B24** → `fixed` (`501ad9d`): `computeDanglingDelta` re-keyed on the **resolved absolute target** with multiset/count semantics — a pre-existing closed-block dangle survives the append-log move+reroot; dry-run/apply agree; the gate still catches migrate-introduced dangles (the multiset test was verified to fail against a `Set` swap). Named limitation documented in-code.
- **B25** → `fixed` (`8cbb8b7`): behavioral read-enclosure **interleaving test** + `_afterRead` seam across all 6 RMW wrappers; the new test genuinely fails on a read-outside-lock twin (a permanent anti-tautology guard: twin asserts `lostUpdate===true`, real wrapper `false`).
- **B26** → `fixed` (`d62e5f2`): STATE-based Epic-close fallback (tier-aware via `PHASES − phases_skipped`, **row-absence-gated**) in Layer 1 (`shipFR1Check`) + Layer 2 (`checkProposedStateWrite`). **Layer 3 descoped (ratified)** — `detectDirtyExecute` runs at EXECUTE, the fallback exists at SHIP (empty intersection = no-op); the harm is fully closed by Layers 1+2; the resume-nudge enhancement → `ISSUES-INBOX.md`.
- **B6 refinement** → `fixed` (`35f6a04`): `isStateStale` local-stale tightened by **file identity** (new `BOOKKEEPING_PATHS` = STATE + CONTEXT drives Walk 2's exclude) — a committed-but-unrolled PLAN/PROGRESS now nudges; count-independent (the reviewer's `commits.length===1` candidate rejected). `STATE_AFFECTING_PATHS` byte-unchanged.

Surfaced during the Epic, deferred: **B27** (needs-triage) — archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts migrate (R7 tension; a design call).
| B87 | `fixed` | **P2** | **`B41`'s shape survives in human-driven runs: a phase that RAN is absent from `completed_phases`.** Caught live during M5.E19's own VERIFY, while it was happening rather than reconstructed after. The Epic's ledger reads `DISCUSS, PLAN, VERIFY` — **EXECUTE is missing, and EXECUTE ran**: `commands/archive.md`, `tools/lib/archive-command.js` and `tests/archive-command.test.js` were all written and committed (`cafab4a`), and `M5.E19-PROGRESS.md` records all five slices. `transitionPhase` records the phase being **left**, so a run that goes PLAN → VERIFY without ever calling `transitionPhase('EXECUTE')` appends `PLAN` and EXECUTE never enters the log at all. **Why `B41`'s fix cannot catch this:** M5.E9 closed `B41` by putting the `transitionPhase` call **inside each phase command** — which does nothing for a run that never invokes the commands. **Signal-on-Signal is exactly that run**, and so is any Epic a maintainer drives by hand, which is how most of this repo's own Epics have been executed. So the ledger's accuracy silently depends on the operator remembering a call, for the one project that produces all of Signal's evidence. **Not repaired at discovery, deliberately:** the phase log is append-only with no dedupe (`D-M5E9-5`) and hand-inserting an entry to tidy the record is the falsification the append-only rule exists to prevent — `B48` settled that writing a phase entry the flow did not produce is wrong even when an instruction demands it. **Candidate fixes, neither chosen:** (a) a drift check comparing `completed_phases` against artifacts on disk — a `{Epic}-PROGRESS.md` exists ⇒ EXECUTE ran; note `phase-behind-artifacts` already compares `phase` to artifacts but is **blind to gaps in the log**, so this is a new check rather than a widening; (b) make the phase-entry call reachable outside the command path. **Found 2026-08-07** at M5.E19 VERIFY. |
| B88 | `fixed` | **P1** | **Signal is branch-blind: nothing creates a branch, nothing checks one exists, and `/sig:ship`'s "create a pull request" is unreachable once every commit has landed on the default branch.** Reported from a **`eval-project-A`** slice (VOICE1/T7) where five commits of real code reached `main` with no branch and therefore no PR — CI ran *after* each push instead of gating it, and `git push` printed *"Bypassed rule violations"* and succeeded because `enforce_admins=false`. **Verified against Signal's own files, not accepted on report** — and the truth is broader than the report claimed: `grep -rln 'git branch --show-current|rev-parse --abbrev-ref' commands/ tools/lib/` returns **nothing**. Not one of 20 commands and not one library module reads the current branch. Three confirmed sites: (a) **`commands/execute.md` contains zero occurrences of the word "branch"** — step 2 says "create an atomic git commit" and commits to whatever branch you happen to be on; (b) **`ship.md` §3 (`:94`) is a bare instruction**, *"Create a pull request with:"*, with no precondition that a PR is still possible — if HEAD is the default branch there is nothing to open one from, and the step fails silently; (c) **the Exit Criteria (`:198`) reads `- [ ] PR created with description, test plan`** — a checkbox satisfied from the felt sense of having shipped, never from a PR URL. That is exactly the `CLAIM-INTEGRITY-ANALYSIS.md` class. **The sharpest part is the location:** `ship.md:116` is the paragraph that *removed* the direct-to-main exemption (`D-M5E17-5`) and states in prose that a change *"does need a branch, a PR, and a green suite"* — while the same file provides no mechanism to make that true and no check that it happened. **The file that states the rule still supplies no enforcement**, which is the precise shape `D-M5E17-5` was filed to end (thirteen releases, exactly one PR). **Fourth instance of the family**, three of which are verified in this repo: `B41` (four commands never called `transitionPhase`, phases unrecorded for eleven releases), `B43` (`ship.md` claimed a SHIP date `transitionPhase` structurally cannot write — SHIP is terminal), `D-M5E17-5` (the self-granted exemption). **Remediation, three candidate sites, not yet chosen:** (1) **`/sig:execute` phase entry** — it already calls `transitionPhase(baseDir,'EXECUTE')` at a guarded point (`execute.md:46`), so asserting a non-default branch there is the earliest moment that matters and makes a slice unable to *begin* on the default branch; (2) **`/sig:ship` §3 as a precondition, not an instruction** — halt when `HEAD == default`, the same remedy `B48` applied to `transitionPhase`, which *"now also refuses to record a phase that produced no artifact, so the text and the code agree rather than one relying on the other"*; (3) **the Exit Criteria read from a PR URL**, not a checkbox. **(2) is the one that also catches the human half** — a halt forces the conversation the reporter chose to have with a summary file instead. **Live in this repo today, and only masked by habit:** every branch in this session was created because `CLAUDE.md` says to and the operator remembered — the same *depends-on-remembering* failure mode as `B87`, filed hours earlier. **Found 2026-08-08**, reported by the `eval-project-A` maintainer, confirmed here by reading the command corpus. **FIXED 2026-08-08** — `tools/lib/branch-guard.js`, wired at all three candidate sites rather than one, since they catch different failures: `/sig:execute` refuses to *begin* on the default branch, `/sig:ship` refuses to ship from it, and the Exit Criteria is filled by `formatPrEvidenceLine` from a real PR URL instead of being ticked. **Hard stop with `--allow-default-branch` as the deliberate way past**, and **tier-gated to FEATURE + FULL** — derived from `references/tier-definitions.md`, not from taste: SKETCH has *"Gates off"* and SPIKE's output is *"A findings document… Not a PR"*, so halting a spike for lacking a branch would have been a fresh `B89` (one instruction contradicting another) shipped inside the fix for it. **The guard does NOT reuse `state.js`'s default-branch resolution**, which falls back to the literal `main`: correct for an advisory banner, wrong for a halt, because the same fallback produces a false halt (no remote) and a false clear (a `master`-default repo) from one line — pinned by a `master`-repo regression test. Four outcomes stay distinguishable (`state-drift.js`'s contract): `cannot-determine` never renders as clean. **Two defects found in this fix's own work, both by the M5.E15 harness:** the new precondition restated the phase-entry rule, putting a directive **outside the `B41-phase-entry` canary's deletion scope** — the `B55` shape, in the release that cites `B55`; and the module header's historical citations tripped the leak walk, fixed by rewording rather than by allowlisting the file, since the allowlist has no per-line grain and an entry would have blinded the walk to any real directive the module acquires later. 2330 → **2361 tests**. |
| B89 | `fixed` | **P2** | **`/sig:plan` grants permission to skip the drain and forbids skipping the drain, in the same file.** `commands/plan.md:66` (Step 1b): *"This step is **advisory and fully skippable** — if you'd rather not triage now, skip the whole step and planning proceeds unchanged."* `commands/plan.md:210` (the Anti-Rationalization table): *"The plan's decided — skip the FUTURE-IDEAS drain"* → *"**No.** … the PLAN drain (Step 1b) **IS** the promotion step. Skip it and captures rot in a write-only file."* **The table names skipping as a rationalization to refuse; the step grants it as an option.** An agent that skips is simultaneously obeying Step 1b and committing the exact rationalization the same file forbids, and neither instruction is wrong on its own terms. **This is `M5.E17`'s class** (*instructions that contradict other instructions*) — but **inside one file**, where the cross-document tests M5.E17 shipped do not look. **Live, and I am the evidence:** during `M5.E19` PLAN on 2026-08-07 the drain was skipped, citing Step 1b's permission, with **52 candidates** in the inbox (the oldest dating to 2026-05) — the precise outcome the table exists to prevent. The skip was declared out loud, which is the only reason it is visible at all. **Consequence beyond the contradiction:** the drain is Signal's *only* culling mechanism for `/sig:add` captures, and it runs **only at PLAN**, so a permission to skip it is a permission for the inbox to grow without bound. `evictTerminalToLedger` makes the inbox *converge* once entries are dispositioned — nothing makes them get dispositioned. **The fix is a product call, not a wording tidy:** either (a) the step is genuinely optional and the anti-rationalization row is deleted, or (b) the drain is mandatory and Step 1b's escape becomes a bounded one — e.g. *defer-all* (which stamps every entry and is one action) rather than *skip*, so the inbox always advances even when the operator does not want to triage. **(b) is the one that matches the table's stated intent**, and `applyDispositions` already supports the batch. **Found 2026-08-08** by reading Step 1b against the table in the same file, prompted by a question about how the roadmap is culled. **FIXED 2026-08-08 — resolved toward (b), required-with-a-bounded-escape**, on Brett's call and for the asymmetry the entry itself names: the drain is the only culling mechanism and runs only at PLAN, so a permission to skip is a permission for the inbox to grow without bound. Step 1b is now **required**; *"defer all remaining"* is the escape and is offered **unconditionally** rather than *"on a large first run"* — an escape that appears only above an unstated size threshold is not one a required step can rely on. `applyDispositions` already supported the batch, so this is wiring and wording, not new machinery. The anti-rationalization row survives, now naming the permitted move (a refusal with no alternative is what made the contradiction survivable) and no longer naming the retired `FUTURE-IDEAS` filename. **`tests/intra-file-consistency.test.js` guards the class**: M5.E17 shipped cross-document tests, so nothing ever compared a file against **itself** — an anti-rationalization table is by construction a list of what the file forbids, and its own steps a list of what it permits, and those two lists had never been read against each other in any command. **Two things the fix found.** The general check was first written as *"strip blockquotes"* — wrong, and measured so: `add.md` and `migrate-memory.md` both carry **live directives** inside `>` blocks, so that shortcut would have blinded the check to real instructions. It keys on an explicit `[RETIRED]` marker instead, which is the BACKLOG corollary *"retract at the granularity people search at"* applied to a retired instruction rather than a false claim — the old wording is **kept, quoted, and marked**, because deleting it would pass every check while losing the only in-file record of why the rule changed. 2361 → **2366 tests**. |
| B90 | `fixed` | **P1** | **Signal can lower rigor per unit of work, and every surface that introduces the feature says it only raises it — so users pay FULL-tier ceremony on two-hour slices and conclude the tool has no opinion about types of work.** Reported from **`eval-project-A`** 2026-08-08: *"every slice — including a two-hour UI fix — runs the full seven-phase FULL-tier machine… the ceremony is flat-cost regardless of slice size, so small user-facing work is where it hurts most, which is exactly backwards."* **The capability is NOT missing** — verified: `/sig:escalate` implements **de-escalation** (`escalate.md:87`, *"Case C — de-escalation downward (less rigor)"*, with `backfill_warnings: []` because nothing needs back-filling going down); an Epic-scoped `{EpicID}-PROFILE.md` shadows the project profile and `escalate.md:94` **targets it** when an Epic is active; `tier-definitions.md:28` names **FEATURE** *"the default tier — when in doubt, answer FEATURE"*; and `phases_skipped` already drops REVIEW at SKETCH and REVIEW+SHIP at SPIKE. **What is missing is every path to discovering any of it.** (a) The command is **named `escalate`**; `CLAUDE.md` describes it twice as *"promotes tier mid-flight"* and *"upgrades tier mid-flight **if scope grows**"* — a user reading either learns the dial turns one way. (b) Per-Epic tiering is an **optional prose offer** inside `discuss.md`'s Epic-mode section; nothing prompts it, and a linear-mode project never sees it at all. (c) **Nothing notices the mismatch** — no check says *this slice touched two files and ran seven phases at FULL; consider a lower tier*, though the ingredients exist (`readEffectiveProfile` knows the tier; the diff size is a `git` call away). (d) At FULL, `phases_skipped: []`, so **the default path once calibrated FULL is to pay everything, forever, unless somebody actively intervenes.** **Same shape as `B87`/`B88`, filed the same day:** the mechanism exists, nothing reaches for it, and correctness depends on the operator already knowing. Here the cost is not a broken record — it is **a week of a real user's time**, and the conclusion that Signal *"is a complete idiot when it comes to the importance of types of work."* **Candidate fixes, not chosen:** rename or alias the command so the down direction is nameable (`/sig:retier`, or `/sig:escalate --down` surfaced in `CLAUDE.md`); make per-unit tier a **prompt** at Epic open rather than an offer; add a cheap tier-vs-diff-size advisory to `/sig:status`/`/sig:resume`; and state in `tier-definitions.md` that **project tier is a ceiling, not a floor** — the per-unit tier is the one that should move. **Found 2026-08-08** from a `eval-project-A` retro; capability verified present in Signal before filing, so this is a discoverability defect, not a feature request. |
| B91 | `fixed` | **P2** | **FIXED 2026-08-09 (`D-BR0809-1`, Wave 1 of the backlog review) — the unbuilt work is renumbered `M5.E20`; `M5.E16` now means only the shipped drift-detection Epic.** The old ID was **not reused and not silently swapped**: the `BACKLOG.md` entry carries a visible note recording that it held a dead ID for nine days, because the value of ID-is-identity is that a collision stays legible. **The second half — the missing allocator reconciliation — is deliberately NOT closed here** and is the candidate fix below: nothing compares the two places an Epic ID gets assigned, so this can recur tomorrow. Filed as the durable follow-up rather than claimed as done. **Original entry:** **`M5.E16` names two different Epics — one shipped, one still open — so a live roadmap item is unreachable by its own ID.** `BACKLOG.md:550` carries `### M5.E16 — The other two shapes of "shipped but never run"`, **promoted on evidence 2026-07-30 (Brett), no closure marker, still open.** The Epic that shipped as `M5.E16` (v0.1.16, 2026-08-02) is different work: `M5.E16-REQUIREMENTS.md:1` reads *"M5.E16 — STATE-vs-world drift detection"*, **opened 2026-08-01** — one day after the backlog entry was promoted. Verified by reading both: the requirements file mentions *"document-shaped"* / *"data-shaped"* **once**, the retrospective **zero** times; the shipped Epic's stated reason is *"20 of the 56 entries in `BUGS.md` are STATE.md-related"*, which is not the promoted entry's subject. **This breaks ID-is-identity**, locked in `PROJECT.md` § Vocabulary, and it breaks it in the most expensive direction: anyone asking *"did `M5.E16` ship?"* gets **yes**, for work that is not the item, so the open entry is invisible to every future check that keys on the ID — including the trigger walk, which reads `BACKLOG.md`. **How it happened is the useful part, and it is not carelessness:** the ID was assigned by promotion in one file (`BACKLOG.md`) and by Epic-open in another (`.planning/{EpicID}-*.md`), one day apart, and **nothing reconciles the two allocators.** `B73`'s class (two documents disagreeing with no test pinning them) applied to identity rather than to a rule. **Found 2026-08-09** by a backlog-review pass reading `BACKLOG.md` against the on-disk artifact set — the same method that found `B59` and `B63`, and the third time document-against-document reading has caught something no test could. **The fix is a product call, not a rename:** (a) give the unbuilt work a **fresh ID** and strike the `M5.E16` entry with a pointer — never silently renumber, since the whole value of the rule is that the wrong ID stays wrong out loud; and (b) decide whether ID allocation gets a mechanism — the cheap version is a hygiene test asserting every `M5.E{n}` heading in `BACKLOG.md` either has no `{EpicID}-REQUIREMENTS.md` on disk or matches its title, which would have gone red on 2026-08-01. Related: `M5.E8`'s heading at `BACKLOG.md:418` carries **no closure marker** and reads `Trigger: NONE — unconditional next`, while `:1108` of the same file says *"M5.E8 landed as v0.1.13"* — same file, two answers, and it should be struck in the same pass. |
| B102 | `fixed` | **P1** | **Signal's own migration instructions uninstall the plugin and then tell you they didn't.** Shipped in `v0.1.26` (`M6.E1`), found 2026-08-18 when Brett asked whether `/sig:update` could perform the migration and the sequence was **run** for the first time. `claude plugin marketplace remove signal` **uninstalls the plugin** — measured in an isolated config, `claude plugin list` reports *"No plugins installed"* immediately after. `marketplace add <url>` does **not** restore it; a third command, `plugin install sig@signal`, is required. **All three surfaces prescribe only two commands** — `plugin/commands/doctor.md`'s `P6` block, `docs/map/install/index.html`'s existing-user section, and `README.md`'s migration note — so a user who follows them lands with **Signal uninstalled**. **Two of the three additionally assert *"Your installed plugin and its settings are untouched by this"*, which is false in its first half**: settings survive (`enabledPlugins` came through intact), the plugin does not. **This is the `CLAIM-INTEGRITY-ANALYSIS.md` class, committed in the release whose own Epic filed `B100` and `B101` about the same thing** — the sentence was written from the *shape* of the operation rather than from running it, by the author of `AC0.1`'s warning, three days after writing it. **`P6` is info-only and the old path keeps working, so nobody is broken by inaction — the damage is only to users who *follow the advice*.** *Fix:* all three surfaces prescribe the **three**-command sequence and drop the false clause, replacing it with what is actually true (*your settings and projects are untouched; the plugin is removed and reinstalled*). Pinned by a test asserting that any surface prescribing `marketplace remove` also prescribes `plugin install` — derived, so a fourth surface cannot drift. **Not automatable in `/sig:update`, decided the same day:** step 1 deletes the plugin that is running the command, so a failure at step 2 or 3 strands the user with no Signal and no `/sig:` command to recover with. |
| B103 | `fixed` | **P1** | **`/sig:doctor --fix` generated `rm -rf` for cache copies that *running* sessions had resolved.** `detectP2OrphanCacheEntry` treats "not the manifest's current version" as sufficient grounds for deletion, and `buildFixScript` never looked at process state — so the generated script offered to delete a directory a live session was executing out of. Claude Code resolves a plugin path once at session start and holds it for the life of the process; that is **`B52`**, and `v0.1.20` shipped a *detector* for it that this path never consulted. `UNREACHED-MECHANISM-ANALYSIS.md`'s class: the capability existed and nothing reached for it. The per-step `[y/N]` is not mitigation — it reads as a formality, so the reflexive answer is the destructive one. **Found by running `--fix` on this machine 2026-08-18, not by reading code:** three `claude` processes were live and two of them (pids 29938, 78473) had resolved `sig/0.1.25`, which the script listed for deletion; the step was declined by hand, and nothing in Signal would have declined it. **Fixed in `v0.1.28`:** `parseClaudeProcesses` + `detectLiveBindings` (pure) infer each session's copy as the newest cache dir that existed when the process started, `readLiveBindings` does the I/O and never throws, and both script builders emit a guard that **re-checks at script-run time** — not at generation time, because a session can start in the gap and `~/.claude/sig-doctor.sh` survives to be re-run days later. The guard calls back into the same module rather than reimplementing the inference in bash (`B82`'s shape). Held directories are skipped outright, never prompted. ⚠ **The inference is a heuristic and is labeled one:** there is no API that reports another session's resolved path, so a downgrade or a hand-moved cache dir can mis-attribute — it errs toward holding, since a false hold costs disk and a false delete costs someone's session. `ps` unavailable / an unreadable dir / no `node` are `cannotDetermine`, and those steps render `[UNVERIFIED]` rather than prompting as though the check had run (`B39`). Verified end-to-end: answering `y` to every prompt now deletes nothing while a session holds the copy. |
| B104 | `fixed` | **P1** | **`/sig:init` spawned four agents holding `Bash` across a codebase, and nothing anywhere told them the content was data.** ⚠ **CORRECTED 2026-08-18, same day, after Brett asked how scanners write to a codebase — they do not.** The first version of this row and of `v0.1.30`'s release note said *"an arbitrary repository"* / *"a stranger's repository"* and implied a write risk. `/sig:init` runs on **your own** existing codebase, and every scanner is constrained to `.planning/scan/{name}.md` with `npm install` and `git push` forbidden. **The real exposure is command execution (they hold `Bash`) and downstream propagation (their output becomes `LANDSCAPE.md` → `PROJECT.md` → every later phase), not file writes.** It remains a real gap because your own codebase carries text you did not write — vendored deps, contributor commits, forked files, fixtures — and because people do point `/sig:init` at a repo they cloned to evaluate or at a client's. The fix is unchanged; the severity framing was written from the shape of the work rather than from the artifact, which is `B50`'s class committed in a bug report about security. Found 2026-08-18 while reviewing `vercel-labs/eve-software-factory-template` (MIT), which ships a prompt-injection eval Signal had no equivalent of. **The diagnosis was a grep, not a judgement:** `plugin/agents/scanners/*.md` and `commands/init.md` contained **zero** instructions treating scanned content as data — not a weak instruction, none — while all four scanners declare `tools: Read, Bash, Grep, Glob`. Signal is publicly installable with four non-Signal users, so a `README` in any scanned repo saying *"ignore the tier gate"* or *"run this setup command first"* met no instruction standing against it. **Fixed in `v0.1.30`:** a Constraints clause in every scanner naming the content as data, naming the destination for such a finding (`suspicious embedded directive at {path}` under *Detection Failures*), and naming `Bash` as what makes the constraint load-bearing; plus the same instruction in `init.md` at the spawn site. **Pinned by a whole-population assertion** — the test reads the scanner directory from disk rather than a hardcoded list, so a fifth scanner fails the suite until it carries the clause. That shape is itself borrowed from the same repo, and is what `B81` needs. ⚠ **Scope, stated rather than implied: this covers the scan surface only.** Two of the three surfaces named at filing are untouched — `/sig:add` captures verbatim **by design**, and every phase command reads `.planning/` documents that anyone with commit access can write. Whether those need the same treatment is undecided; a clause in a scanner does not generalise to a document Signal itself authored. ⚠ **An instruction is not a sandbox.** This makes the expected behaviour explicit and testable; it does not make injection impossible, and no eval exercises it — Signal has no run-event stream to assert against (see the inbox entry on behavioral evals). |
| B105 | `fixed` | P3 | **A guard on `CHANGELOG.md` asserted a property that is only true while no release is in progress, so WRITING THE RELEASE NOTES failed the suite.** `tests/cut-release.test.js` asserted ``expect(real).not.toMatch(/^##\s*\[Unreleased\]/m)`` against the shipped `CHANGELOG.md` — *"this repository contains no `[Unreleased]` heading at all"*. That holds only while nothing is pending. But `foldChangelog` **refuses a cut without a pending section** (correctly — a release with no notes is the failure it exists to prevent), so writing the notes is the required first step of every release, and doing it turned the suite red. **The guard blocked the workflow it exists to protect.** Found 2026-08-20 by writing the pending notes for the 2026-08-20 loop work, not by reading the file. **The assertion's own comment predicted this and was ignored:** its recorded `v1` was *"the real CHANGELOG refuses a cut"*, annotated *"true when written, FALSE ten minutes later once notes existed for the next release. It tested a transient property of the file rather than the invariant."* `v3` repeated that error **inverted**, with the paragraph explaining `v1`'s failure sitting directly above it — the same mistake twice in one file, the second time under its own postmortem. **`B84`'s hazard is positional, and so is `foldChangelog`'s anchor:** a pending section sits **above** the newest released heading; anything below is history and must never satisfy the guard. **Fixed 2026-08-20:** the assertion now slices from the newest released heading and tests only the history below it, which is what the comment said it wanted. **Proof-of-fail, not just green:** injecting a historical `[Unreleased]` below the newest release fails the repaired test (1 failed / 19 passed); reverting passes 20/20. The synthetic fixture above it, which owns its input, still pins the fold behaviour independently. ⚠ **Class, stated rather than implied: this is `M5.E13`'s "guards that don't guard", and the specific sub-shape is a test pinned to a transient state of the repository rather than to an invariant.** No sweep of the suite for sibling instances was done, so how many other assertions read a live artifact this way is **unknown, not zero**. |
| B106 | `fixed` | P3 | **`cut-release.js` updated the published test count and left the sentence naming the release that produced it — so every cut published a fresh figure attributed to the PREVIOUS version.** `plugin/references/facts.md` carries `- **Test count:** N` followed by *"Set at each release … most recently **vX.Y.Z (date)**"*. `setFactsTestCount` rewrote the number; **nothing rewrote the attribution**, which is a release-time fact for exactly the same reason the count is (`B56`) — only knowable at the cut, and unre-derivable between cuts. **Found 2026-08-21 by cutting `v0.1.31`**, not by reading the file: `facts.md` correctly read `2786` while the line beneath it still said *most recently v0.1.30 (2026-08-18)*. ⚠ **The detector worked and the tool did not.** `M6.E2`'s `facts-attribution` check exists for precisely this condition and **fired on the cut** — so this is not a gap in detection, it is `UNREACHED-MECHANISM-ANALYSIS.md`'s class one layer over: the check names the situation, and the tool that *creates* the situation every single release was never taught to fix it. Left alone, it re-fires at every future cut and trains the operator to clear it by hand. **Fixed 2026-08-21:** new `setFactsAttribution(source, version, date)` mirroring `setFactsTestCount`, composed into the same `releaseEdits` entry so the count and its attribution are written **by one edit** and cannot diverge. It **throws rather than silently no-opping** when the attribution is absent — `rewriteBugTally`'s lesson (a replace matching nothing returns the input unchanged and the caller reports success on a file it never fixed). **Pinned by a cross-file assertion, not a literal:** a test reads `FACTS_REL` **out of `published-facts.js` source** and parses this function's output with it, so if the writer and the reader drift the suite fails — pinning the literal string in the test would have recreated the bug inside the test (`B85`'s lesson). ⚠ **What this does not do:** it asserts the figures were re-derived *at that release*, not that they are *correct* — the check's own stated limit, unchanged. |
| B107 | `fixed` | P3 | **The private-name guard walked `git ls-files` only — tracked files — so a brand-new file was invisible to it until staged, and a local `npm test` returned **clean** on exactly the files most likely to introduce a new name.** Found 2026-08-21 while fixing `B76`: `plugin/tools/lib/loop-ceiling.js` and `tests/loop-ceiling.test.js` passed the full local suite and then **failed in CI on identical content**, because by then they were tracked. The collision itself was real and is the reason this matters — an ordinary-looking camelCase identifier introduced as a field name **is** one of the thirteen denied project names, which is precisely the residual hole `B97` recorded (*"three names are ordinary English words, caught only in project shape"*). Renamed; no private name ever reached a commit. ⚠ **The guard was not wrong about what it read — it was wrong about what it had read**, reporting a clean result for a population it never opened. That is `B39`'s shape, and it is the same failure `B97` itself was: that guard passed while 8 of 13 names sat in the repository. A guard whose local signal is clean and whose CI signal is red trains the operator to treat the local run as authoritative when it is structurally blind. **Fixed 2026-08-21:** the walk is now tracked **plus** `ls-files --others --exclude-standard`, i.e. the set a commit would actually carry; `.gitignore` still excludes scratch and build output, so nothing noisy comes in. **Proved in both directions:** an untracked file carrying the collision fails the guard, and deleting it passes — run before and after in the same step. ⚠ **CI was never blind and is not the thing that changed** — this closes the gap between what a developer sees before committing and what the gate sees after. |
| B108 | `fixed` | P2 | **`v0.1.31` moved confirm cadence from `gate_strictness` to `attention` and left SIX command files describing the old dial.** A regression the split introduced, live on `main`, in the files an operator reads to learn how the gates behave. Sites: `discuss.md` (table + the Step-4 paragraph), `plan.md`, `execute.md`, `verify.md`, `review.md`, `ship.md`, plus four prose claims in `checkpoint.md` — every one mapping `off`/`light`/`strict` to auto-advance and confirm timing that `gate_strictness` **no longer controls**. `applyRigorOverrides` gives it exactly one gate (`anti_rationalization`); `attention` drives `auto_advance`, the six `confirm_*` flags and `confirm_in_phase`. **This is `M5.E17`'s class — instructions contradicting the code — created by the very release that fixed the dial.** ⚠ **Found by a check, not by the sweep that preceded it:** a manual grep found three files, and the test written to pin the fix immediately failed on **three more** (`verify.md`, `review.md`, `ship.md`). The sweep was wrong and the assertion was right, which is the argument for writing the assertion. **Fixed 2026-08-21:** every table now keys on `attention`, with a `gate_strictness: strict` row kept that states its one remaining job. **Pinned by a whole-population assertion over the command directory read from disk** (`B104`'s shape), so a new command reintroducing the mapping fails the suite — plus a derived assertion that `gate_strictness` really does set only `anti_rationalization`, which fails if a future change gives it another gate and makes the docs wrong on purpose. ⚠ **One false positive was designed out rather than suppressed:** anti-rationalization tables quote the temptation in the first cell (*"`strict` means I should confirm the destination"* — `add.md:208`), so the check skips quoted-temptation rows; matching them would have flagged a **denial** of the mapping as the bug. ⚠ **`add.md`'s `gate_strictness` use is real and untouched** — `resolveOnboardingMode` reads it in code for the first-run onboarding note, a genuine second job. |
| B109 | `fixed` | P3 | **FIXED 2026-08-25.** `setChangelogTestCount` reconciles the pending section's trailer from the same gating run that supplies `facts.md`'s count, composed into the same `releaseEdits` entry as the fold so the two cannot diverge. ⚠ **The design stated when this row was filed — *throw on absence* — was WRONG, and measuring is what said so:** 14 of 33 released sections carry a trailer and **19 do not**, so throwing would have failed the cut for the more common shape (`B42`'s defect). Absence returns unchanged **and reports**, because a silent no-op is `rewriteBugTally`'s bug. The baseline is checked against `facts.md` and deliberately not rewritten. `pendingSectionBounds` is now shared with `foldChangelog` (`B82`) — they must agree or the trailer could rewrite history while the fold refuses (`B84`). **Proof-of-fail in three directions** (fold-before-trailer → 4 red; throw-on-absence → 7 red; first-match → 2 red) **and replayed against the real pre-cut `CHANGELOG.md` from git**: corrects `2927 → 2979`, history byte-identical. ⚠ **The first implementation shipped this bug's own shape and CI review caught it:** the regex took the **first** match in the section, and the notes for the fix quote a trailer as a prose example above the real one — so it rewrote the quoted example, left the trailer stale, and reported *"corrected"*. A **false green inside the function written to prevent one**, found by running the tool against the real file rather than by the suite (`B82`, third instance). Now anchored on the **last** match, with multi-candidate sections disclosed in the note. **Original entry:** **`cut-release.js` does not reconcile the CHANGELOG section's own test-count trailer, so every release publishes whatever number was true mid-Epic.** Found 2026-08-24 by CI review on the `v0.1.33` cut (PR #203), not by reading the file: `CHANGELOG.md`'s pending section closed with `2841 → **2927 tests**` while `facts.md`, `CLAUDE.md` and `.planning/CONTEXT.md` — all written in the same PR — said **2979**. A 52-test gap for one release, in the document that *is* the release notes. ⚠ **This is `B106` one layer over, and `B106`'s own fix is why it is visible.** `B106` taught `releaseEdits` to write `facts.md`'s count **and** its attribution by one edit so they cannot diverge; the CHANGELOG trailer is the same class of release-time fact — only knowable at the cut, unre-derivable between cuts — and no edit touches it. It is hand-typed into the `[Unreleased]` section while the Epic is still adding tests, so it is stale **by construction** at every cut, not occasionally. **The detector gap is the sharper half:** `M6.E2`'s `facts-attribution` check reads `facts.md` and nothing reads this trailer, so the condition is invisible to the suite and was caught only because a reviewer compared two files by hand. **Fix shape:** set the trailer in `releaseEdits` from the same `vitest` run that supplies `facts.md`'s count, and **throw rather than no-op** when the pattern is absent (`rewriteBugTally`'s lesson). ⚠ **The trailer's shape is not guaranteed** — it is prose an author writes freehand, so a strict pattern that throws will fail a cut whose author phrased it differently. That is the design question, and it is why this was filed rather than fixed inside a release commit. |
| B110 | `fixed` | P3 | **FIXED 2026-08-24, same day, by removing the number rather than correcting it.** The heading now reads *"Promoted from the inbox drain"* and carries a boxed note recording why. Correcting *"Twelve"* to *"Thirteen"* was rejected: it would be true until the next row lands, which is the property that produced the bug. **`.planning/BACKLOG.md`'s heading reads *"Twelve promoted from the inbox drain"* and thirteen `####` rows sit under it.** Found 2026-08-24 while answering *"what is recommended next"* — the count in the heading was used to derive an answer, and the derivation was wrong. A count written into a heading at capture time (2026-08-10) and never re-derived as rows were added: **`M6.E2`'s exact class, in the file that holds the queue.** ⚠ **The consequence is not cosmetic.** `D-M5E18-1` makes `BACKLOG.md` the queue of record — *"the queue is `.planning/BACKLOG.md`, not `CLAUDE.md`"* — so a reader sizing remaining work counts from this heading. It propagated once already: a draft of `CLAUDE.md`'s v0.1.33 block published *"nine of the twelve rows remain unbatched"*, which was 12−3 arithmetic off the wrong denominator, corrected before merge. **No check reads it.** `M6.E2` shipped five published-fact checks and all five key on `STATE.md`, `BUGS.md`, `CHANGELOG.md` and `facts.md`; a heading that states a count over its own child rows is a shape none of them look for, in a file all of them skip. **Fix shape:** either drop the number from the heading (cheapest, and the queue is a live document whose length is not a fact worth pinning), or add a check deriving the `####` count under any heading that states one. ⚠ **Prefer the first.** A check here reaches one project — this one — which is the reach problem `M6.E2` published rather than solved, and a heading that simply does not claim a number cannot go stale. |
| B113 | `fixed` | P2 | **FIXED 2026-09-01, same day as filing.** `drive.md` §3 now reads `loopStatusFor(state, state.phase)` and passes `{ loopStatus }` as the third argument; the reference list names `loop-ceiling.js` and its three symbols; the halt renders via `formatLoopCeilingHalt` rather than a sentence written twice. **Pinned by `tests/drive-doc-contract.test.js`, shown RED on the pre-fix file before green** — 3 of 6 failed on the old content. ⚠ **The generalisation in that file does not catch this bug's own shape:** it resolves every symbol the reference list *names* against real exports, so it catches a **wrong** entry and not a **missing** one, and it passed on the pre-fix file. Catching a missing entry needs knowing what ought to be listed, which nothing derives. ⚠ **Not driven end-to-end through a real Epic** — the fix is verified at the documented call site and against the module, not by a live `/sig:drive` run. **Original entry:** **`commands/drive.md` documents the two-argument call to `canProceedUnattended`, so the loop ceiling it is supposed to enforce can only ever refuse.** `canProceedUnattended(phase, profile, { hasFloor, loopStatus })` fails closed on not knowing: for `LOOP_BOUNDED_PHASES` (`VERIFY`, `REVIEW` — `plugin/tools/lib/loop-ceiling.js:60`) a caller supplying no `loopStatus` gets `{proceed:false, reason:'loop-unknown'}`, deliberately and with the reason stated in the source (*"an actor that cannot tell should stop"*). `plugin/commands/drive.md` §3 documents the call as `canProceedUnattended(state.phase, profile)` — two arguments — and the file mentions `loopStatus`, `loopStatusFor` and the ceiling **zero** times; its authoritative-references list (`:19`) names `canProceedUnattended, FLOORS, floorsFor, queueDecision, readQueue` and omits `loopStatusFor`. **Reproduced against the live module**, not inferred: the documented call returns `loop-unknown` at both `VERIFY` and `REVIEW`, above the attention branches, so no `attention` value changes it. Followed literally, `/sig:drive` halts at every VERIFY and every REVIEW. The ceiling shipped in `v0.1.32` as the stated *entry price for autonomy work* (`B76`) is therefore invoked by the only command that drives the loop in a form that cannot pass. `M5.E17`'s class — a document contradicting the code it names — and no test covers it because the call site is prose. Found 2026-09-01 by the goal-direction analysis ([`../analysis/LOOP-GOAL-DIRECTION.md`](../analysis/LOOP-GOAL-DIRECTION.md) §1.1); **not yet driven end-to-end through a real Epic**, which is what would verify the fix. |
| B101 | `confirmed` | P2 | **`diffRequirementCoverage` clears a requirement group from `unattributableGroups` when the report merely *names* the group — including in a sentence saying the group was not checked.** Found 2026-08-17 while running VERIFY on `M6.E1`, by watching the value change after an edit that verified nothing. Against the draft report the check returned `unattributableGroups: ["NFR-1","NFR-2","NFR-3","NFR-4"]`. A bullet was then added *disclosing* those four groups, and the value became **`[]`** — no requirement was verified in between; the only change was that the four strings now appear in the document. **Reproduced deliberately, and the reproduction is the severity:** a verification text reading `"NFR-1, NFR-2, NFR-3 and NFR-4 were not checked at all"` also returns `[]`. **A sentence declaring the groups unchecked satisfies the check whose job is to surface unchecked groups.** This is the exact limit `AC0.1` / `D-M5E10-1` states in the abstract — *everything shipped compares **tokens*** — reproduced on a live artifact rather than argued, and it is worse than the abstract statement suggests, because the satisfying sentence can assert the opposite of coverage. **Sibling to `B100`**, found in the same run on the same tool: `B100` is a denominator that silently omits ids, this is an exclusion list that empties itself on mention. Both make a **reassuring** word (`covered`, `[]`) out of an absence. *Fix shape (not attempted):* attribution should require evidence adjacency — a group counts as attributed only when its ids or an evidence marker appear, not when the group label appears anywhere in the file. The deeper fix is `AC0.1`'s semantic half, which is deliberately unbuilt and has a live `BACKLOG.md` row. **Do not fix by widening the token match** — that recreates the defect one layer out, which is the lesson `B85`'s test recorded. |
| B100 | `confirmed` | P2 | **`diffRequirementCoverage` cannot see a letter-suffixed acceptance-criterion id, and reports `covered` while blind to it.** Found 2026-08-14 on its **first use in a new Epic**, against `M6.E1-VALIDATION.md`. The Epic's `REQUIREMENTS.md` carries **24** ACs by grep (`AC1.1 … AC6.3`, including `AC1.2a` and `AC1.2b`); `diffRequirementCoverage` returns a denominator of **22** and an outcome of `covered` with `missing: 0`. **The two it drops are the two added during DISCUSS** to close gaps a review had found — i.e. exactly the late-added criteria most likely to be forgotten, which is the population the check exists to protect. **This is `M5.E10`'s own shape, one form over.** That Epic's VERIFICATION failed its own coverage check because six ids were written only inside an en-dash range; this is the same defect in the *suffix* dimension rather than the *range* dimension, and it was found the same way — by running the check instead of trusting the artifact. **A clean `covered` from a check with an incomplete denominator is a false green**, and it is worse than a failure because the outcome word is reassuring. ⚠ **The human claim was ALSO wrong, and differently:** the first draft of that VALIDATION asserted *"23 of 23"* — matching neither the grep's 24 nor the tool's 22 — while its own sub-counts summed to 24. Both the tool and the author were wrong about the same document, in different directions. The refutation is kept in `M6.E1-VALIDATION.md` rather than tidied away. *Fix shape (not attempted):* widen the id pattern to accept a trailing letter, and — the more important half — have the check **state its denominator's basis** so a reader can tell `covered` from `covered, as far as I could parse`. `AC0.1`'s sentence applies to the deterministic checks themselves, not only to the semantic gap they leave. |
| B99 | `fixed` | **P2** | **A plugin install carries Signal's private working notes and its test suite: ~11 MB of tracked files, of which `.planning/` is 4.7 MB (43%) and `tests/` 2.1 MB — where ~1.9 MB is what running `/sig:*` actually needs.** Raised by the maintainer 2026-08-13 after being told the install ships the whole repository. `marketplace.json` declares `"source": "."`, so an install carries every tracked path. **Comparison, measured across the other plugins installed on this machine: not one ships `tests/` or `node_modules/`, and the largest is 2.5 MB.** Signal is the outlier, and the cause is that its repo is a full development environment rather than a plugin payload. ⚠ **A CORRECTION TO THIS ROW'S FIRST VERSION, which asserted "57 MB delivered to users".** That number came from the copy in this machine's plugin cache, which is **57 MB including 47 MB of `node_modules`** — and `node_modules` is **gitignored and untracked**, so it is there because a local-path install copied the working directory, not because a user would receive it. A remote install carries **tracked files only: ~11 MB**. The original claim was measured on one install and asserted about all of them, which is `CLAIM-INTEGRITY-ANALYSIS.md`'s class committed while filing a bug during the Epic about that class. Corrected in place rather than rewritten, per `AC4.3`. **Checked at the same time and clean:** no `.env`, no `.claude/settings.local.json`, no `coverage/`, no `dist/` in the cache — the local copy carried no secret. **Two costs remain, and the second is why this is filed:** (1) size — 11 MB where 1.9 MB is needed; (2) **content** — `.planning/` is Signal's working memory: measurements taken against the maintainer's real client projects, bug diagnoses, business decisions and roadmap reasoning. Publishing it **in the repository** is a deliberate choice and defensible (`.planning/` must travel with the project, and it is a live demonstration of the methodology). **Copying it into every user's plugin cache is a different act that was never separately decided**; it followed from `source: "."`. It is also what makes `B97` a publishing event rather than repo hygiene. ~~*Fix — needs research, and no mechanism is asserted:*~~ **RESEARCH DONE 2026-08-14.** Every claim below was verified two ways — measured on this machine, and read in the current plugin reference (`https://code.claude.com/docs/en/plugins-reference.md`, 1316 lines, fetched) — because restating an upstream summary about a third artifact without opening it is the provenance rule `v0.1.25` shipped as a shaping entry. **FIXED (packaging half) 2026-08-17 in `M6.E1`, shipped as `v0.1.26`.** Measured on a real isolated install: **985 files / 12.4 MB → 382 / 3.2 MB**, and the 47 MB dependency install no longer fires (the plugin root carries no `package.json` or lockfile, which is the documented trigger). `.planning/` **270 files → 0**; `tests/` **218 → 0**. The marketplace's 19 MB clone is removed by the published URL catalog. ⚠ **The half this row calls load-bearing is NOT closed:** `.planning/` is no longer *copied to users*, and is still *public in the repository*. That remains a separate deferred decision, and this row is marked `fixed` for the packaging question only — stated here so the status is not read as answering the positioning one. Detail: `M6.E1-RETROSPECTIVE.md`, `D-M6E1-7`.

**1. There is no include / exclude / files / ignore field. This is a confident "no such field exists," not a failure to find one.** A case-insensitive grep for `"files"`, `"include"`, `"exclude"`, `"ignore"`, `.gitignore` and `.npmignore` across the full reference returns **zero hits**. `commands`/`agents`/`skills` scope what **loads**, not what **copies**. Claude Code *"ignores top-level fields it does not recognize"*, so an invented `"files": [...]` would validate as a warning and silently do nothing.

**2. A subdirectory plugin root is supported and is the only lever.** `"source": "./plugin"` is still the **relative string form**, so `B58`'s lesson survives — `install-contract.test.js:69` asserts `typeof source === 'string'` *and* `toBe('.')`; only the second line changes. Constraint that must be checked before moving anything: *"Copied plugins cannot reference files outside their directory. Paths that traverse outside the plugin root (such as `../shared-utils`) will not work after installation."* `${CLAUDE_PLUGIN_ROOT}` becomes the subdirectory, and it is referenced from `tools/`, `hooks/` and **7+ command files**.

**3. The biggest number in the install is not in this row, and the same move fixes it.** Measured: the cached copy is **58 MB, of which `node_modules` is 47 MB** — devDependencies (`vitest`, `eslint`, `esbuild`) on every user's machine. It is not copied; Claude Code **installs** it. Documented trigger: the install *"runs only when the plugin's root directory contains **both** a `package.json` and a supported lockfile"* — and *"You can't turn the automatic install off; no setting or environment variable disables it."* **A plugin root without those two files never triggers it.** Signal has exactly **one** runtime dependency (`yaml`, used by `tools/lib/profile.js` + `state.js`); everything else is dev-only.

**4. ⚠ The move does NOT fix the content half, and this is the finding that matters.** The marketplace and the plugin are **separate fetches**. Adding a git-hosted marketplace clones the marketplace repo **in full** — measured here at **19 MB at `~/.claude/plugins/marketplaces/signal`, `.planning/` included**. No `source` value changes that, because `source` describes the plugin, not the marketplace. **So every packaging fix addresses size and none of them addresses "users receive Signal's working notes."** Removing `.planning/` from what users receive requires either taking it out of the public repo, or decoupling the plugin fetch from the marketplace repo (`git-subdir` against a separate repo, `npm` — whose `files`/`.npmignore` are the only real allowlist anywhere in this system — or `archive` with a CI-built zip + `sha256`). **All three collide head-on with `D-M5E17-4`** (relative `.`, users track `main`, no `ref`/`sha` to drift) and reintroduce a publish step.

**Also measured, not one of the questions asked, and larger than the bug:** `~/.claude/plugins/cache/signal` holds **13 versions totalling 728 MB** — `0.1.5` is still present. Whatever the per-version size becomes, it is multiplied by that.

**Migration deliverable, required in the same release as the packaging change (Brett, 2026-08-14):** an existing-users section on `signal.insightriot.com/install` — *"installed before v0.1.2x? here is how to clear the old copy."* **It cannot be written before the packaging change ships**, because the instruction is *re-add from the new URL* and that URL does not exist yet. **Measured first, and the obvious version of this section would be wrong:** the plugin cache **mostly clears itself**. `~/.claude/plugins/cache/signal/sig` holds 13 versions / 728 MB, but `0.1.6`–`0.1.12` and `0.1.14` are **already gone**, and 11 of the 13 survivors are inside the documented **~14-day orphan grace period** (*"marks the previous version directory as orphaned and removes it in a background sweep roughly 14 days later"*). Telling users to `rm -rf` inside their Claude config for something that self-heals would be bad advice. **Two things are genuinely worth an instruction:** (1) the **marketplace clone** — 19 MB, not version-swept, and orphaned outright by a move to a URL marketplace, so it needs `claude plugin marketplace remove` rather than aging out; (2) `claude plugin prune` is **not** the tool for either — it removes auto-installed *dependencies*, never version directories. **One measured anomaly, stated rather than explained:** `0.1.5` (2026-07-14) is **31 days** old and still present, well past the grace period, cause unknown.

**Bottom line: the size half is small and the content half is a positioning decision.** `.planning/` staying in git is still not in question; only what an install copies — and the answer is that packaging alone cannot stop it while the marketplace source is this repo. The broader *"should `.planning/` be public at all"* question (deferred 2026-08-13) is therefore **load-bearing for this row**, not adjacent to it. |
| B97 | `fixed` | **P1** | **`tests/private-name-guard.test.js` covers 5 of the 13 eval-corpus projects, so a tracked file naming one of the other 8 passes the suite — and this Epic added one such line.** Found 2026-08-13 by the independent `/code-review` pass on `M5.E10`, then reproduced by hashing every corpus directory basename and scanning tracked files. **Two mechanism gaps and one coverage gap, all verified:** (1) **coverage** — `DENIED` holds **5** hashes; there are **13** corpus projects on disk, so 8 are unguarded. (2) **greedy tokenisation** — `deniedTokensIn` matches `/[a-z][a-z0-9]*(?:-[a-z0-9]+)*/g`, which takes the LONGEST hyphenated run, so a guarded name embedded in a longer token is invisible. (3) **content only, never the path** — the walk reads each file's bytes and never hashes tokens from its own path, so a file *named* after a corpus project passes. **The live evidence:** two source comments each anonymise one corpus project and name another **in the same sentence** — `tools/lib/state-drift.js` (**an added line in this Epic's own diff**, so it is a NEW mention, not historical) and `tools/lib/closure.js` (pre-existing on `main`). Measured spread of the two names: **19 tracked files** for one, **5** for the other in path-shaped use. **Severity is P1 because this repository is public and the corpus is private commercial work** — the exposure is the point, not the tidiness. **It is also two false completeness claims about the guard itself:** the test's own docstring asserts *"the inventory is EMPTY … any failure here is a genuinely NEW mention"* and `references/eval-corpus.md` asserts *"a file naming a corpus project fails the suite"*. Both are false today, and the second is a published rule. That is `M5.E10`'s own defect class, inside the guard built to enforce the rule it misstates. **Names are deliberately not reproduced in this row.** *Fix (needs a decision, not just code):* extend `DENIED` to all 13 hashes; decide which stable letter each unlabelled project takes (`references/eval-corpus.md` publishes A–L and only A–E are hash-pinned — see `B98`); make the tokeniser match a guarded name as a **substring of a longer token**, not only as a whole token; hash the path as well as the content. **The commit history cannot be scrubbed** — `ADHERENCE-LOG.md` pins shas and `main` is protected — which is stated in the guard already and is the reason the tree must not add more. **FIXED 2026-08-13 (`M5.E10`).** 113 replacements across 30 tracked files; all 13 names denied, in **two modes** — 10 matched anywhere including embedded in a longer hyphenated token, 3 matched only in project shape (`name/FILE`, backticked) because they are ordinary English words and one appears in 100+ files as prose. **The residual hole is stated, not claimed away:** a bare prose mention of those three is not caught and cannot be without false positives. Generated lockfiles are excluded — a base64 integrity digest produced a real false match. The false claims in the guard's own docstring and in `references/eval-corpus.md` are corrected in place. **A measurement error of mine is recorded too:** the first pass treated every `~/dev-biz` directory with a `.planning/` as private, which turned 17 files red over ordinary prose before the two nine-character names were re-bucketed. |
| B98 | `fixed` | **P2** | **`tools/measure-corpus.js` pins only labels `A`–`E` by hash; `F` onward are handed out by `spare.shift()` over a hash-sorted list, so adding or removing one corpus project renames every unpinned project.** Found 2026-08-13 by the independent `/code-review` pass on `M5.E10` and confirmed by reading `measure-corpus.js`. The comment immediately above the code says *"Labels must be STABLE across runs and consistent with the ones already used in Signal's documents"*; `references/eval-corpus.md` says *"`eval-project-A` is the same project in every document that mentions it, so evidence stays traceable across releases"*; and `CLAUDE.md` cites `eval-project-L` by name. **For A–E all three statements are true. For F–L none of them are.** The corpus is a live set of directories, so its membership does change — `M5.E10`'s own measurement moved `FR8` from 5 to 4 to 5 in two days for exactly that reason. Every recorded measurement naming a project past `E` is therefore **quietly incomparable across releases**, which is the failure the comment says positional labelling already caused once. *Fix:* derive the letter deterministically from the hash rather than from list position, or pin all 13. Pairs with `B97`, which needs the same decision about which letter each project takes. **FIXED 2026-08-13 (`M5.E10`)** — all 13 pinned by hash in `measure-corpus.js`; a new project takes the next free letter and keeps it, and a departing project does **not** free its letter (reuse would make two projects share a label across releases, which is the whole failure). **Brett's call, recorded:** pin today's mapping and publish the caveat rather than reconstruct `F`–`L` by hand — the original assignment was never recorded and reconstruction would be a guess. `references/eval-corpus.md` now says that references to `F`+ predating this date may not resolve to the same project. **Confirmed live while fixing it:** the mapping moved twice inside ten minutes, and one project shifted `M` → `L` between two runs. |
| B96 | `fixed` | **P2** | **`extractEpicSection` cannot see a heading inside a blockquote, so `evictEpicNarrative` reports `no-section` — *"a safe no-op"* — for a narrative that exists and is 5 KB.** Found 2026-08-13 while validating `M5.E10`'s SHIP preconditions, by dry-running the §5.5 coverage gate rather than waiting for ship to run it. `evict.js:189` builds `` `(?:^|\n)(#{1,6})[ \t]+…` ``, which requires the heading to start the line; Signal's live STATE.md carries its in-flight block as `> ## ▶ IN FLIGHT — M5.E10, opened 2026-08-11`, inside a blockquote. `extractEpicSection` returns `{found: false}`, `evictEpicNarrative` returns `{evicted: false, reason: 'no-section'}`, and `commands/ship.md` §5.5 documents that outcome as *"a safe no-op"*. **It is safe; it is not a no-op.** The consequence is that the closing Epic's narrative is never relocated to `archive/`, so live `STATE.md` accretes every Epic's block forever — precisely what `M5.E1` FR2b built eviction to prevent, and the `STATE.md`-size advisory then fires on growth nothing will drain. **This is `B39`'s shape:** *could not find it* renders identically to *there is nothing to find*. `M5.E19` evicted correctly (`archive/M5/E19/STATE-NARRATIVE.md` exists), so this is a regression in the **document**, not the code — which is exactly why the code should distinguish the two cases. *Fix (design call, not taken here):* either report a fourth outcome when the Epic id appears in the body but no heading matches, or teach the heading regex to skip a `> ` prefix. **Deliberately not fixed inside `M5.E10`** — it is an `M5.E1` module, found at REVIEW-close, and the safe half (the gate refuses rather than corrupting) already holds. *Repro:* `extractEpicSection(parseFrontmatter(STATE.md).body, 'M5.E10')` → `{found:false}` while the body contains the block. **FIXED 2026-08-13 (`M5.E10`).** The heading and boundary patterns accept an optional `> ` prefix. **Fixed in the code rather than by un-blockquoting the document, deliberately: the code should read the shape the maintainer writes, not require the maintainer to write the shape the code reads.** Confirmed end-to-end — the section now resolves at 4562 bytes, and `verifyCardCoverage` then **refused** it, naming six decision ids and one date the retrospective did not carry. That is the R1 safety working: the retro was extended to cover them and the gate now passes, so eviction will actually run at ship. |
| B95 | `fixed` | **P2** | **`RETROSPECTIVES.md` orders its rows by file mtime, which git does not preserve — so the retro-index freshness check reports `structural` drift on a fresh clone.** Found 2026-08-13 by `M5.E10`'s own VERIFY, running the Epic's own `checkRetroIndexFreshness` (FR7) against this repository: it reported **stale**, and diffing expected-vs-on-disk showed the difference is **ordering only** — every row present, same text, different sequence. `tools/lib/retro-index.js:227` sorts `(a, b) => b.lastModified.getTime() - a.lastModified.getTime()` where `lastModified` is `stat().mtime` (line 111). **Git stores no mtime**, so a clone, a checkout, a rebase or a stray `touch` rewrites the order while the retros themselves are unchanged. **Why it matters beyond noise:** the check is `structural` severity, so `/sig:sweep` reports a hard finding that carries no information, and the SHIP-time regen (`ship.md` §6) will happily rewrite the file to whatever today's filesystem happens to say — a diff in every release that means nothing. It is also this Epic's own class: a check reporting drift that is not drift, and a document whose order asserts a chronology it did not derive. **Not fixed here** — it is a pre-existing `M4.5.E9` defect surfaced by VERIFY, not `M5.E10` work, and the fix is a design call (sort by the Epic id's natural order, or by a date parsed from the retro's own `**Closed:**` line, which is the data actually claiming chronology). *Repro:* `node -e` diff of `renderIndex(await enumerateRetros(cwd), parseExistingHooks(onDisk))` against the on-disk file. **FIXED 2026-08-13 (`M5.E10`).** Sorted by **descending Epic ID** — a key every retro has by construction and no filesystem operation can change. **The suggested fix was measured out first:** sorting by each retro's own `**Closed:**` date is the field actually claiming chronology, and **3 of 28 retros carry a parseable one**, so 25 would have had no key. The index is therefore deliberately **not** a timeline (`M5.E19` shipped before `M5.E10`) and does not claim to be — an index owes the reader the same answer twice, not a chronology it cannot derive. `retroIndexFreshness` now reports `fresh`. |
| B94 | `fixed` | **P1** | **`BACKLOG.md` is append-only: nothing in Signal ever marks a backlog row done when the work ships, so the queue asserts false status indefinitely.** Verified 2026-08-13 in Signal's own tree — four rows describing work completed that same day (requirement-coverage diff, VERIFICATION template, `B38` reclassification, correction protocol) still read as pending. **Mechanism, checked in the code rather than inferred:** (1) `commands/ship.md` reconciles FIVE document surfaces at Epic close — retro index §6, evict-on-close §5.5, STATE §5, CHANGELOG §1, and a **light inbox sweep §6.5** — and touches `BACKLOG.md` in none of them. (2) `tools/lib/backlog.js` has exactly two write paths, `createIfMissing` and promote-append; **no discharge function was ever written**. (3) `BACKLOG.md` and SHIP's inbox sweep were built by the SAME Epic (M5.E3 FR2), which gave the inbox both ends of the pipeline and the backlog only the intake end. (4) Signal's lifecycle machinery is **Epic-scoped** (`transitionPhase`, PROGRESS, retro gate, evict-on-close, archive tree); a backlog row is pre-Epic, so on promotion it becomes orphaned data with no owner and **no structured link back** — `M5.E10-REQUIREMENTS.md` and `-PLAN.md` each mention `BACKLOG` once, in prose. Even a diligent SHIP has nothing to follow. **Field evidence (eval-project-A, 2026-08-13):** `BUGS.md` accurate (26/28 closed, closure is commit-tied); `ISSUES-INBOX.md` accurate as a historical capture log (115 entries routed 2026-07-28, routing recorded — the PLAN drain works); `BACKLOG.md` **two weeks stale with ~15 shipped slices reading as pending**. The one document users treat as the queue is the one document with no closing mechanism. **Why it stayed invisible:** Signal's own backlog is hand-groomed by the maintainer in conversation, so drift presented as *"Brett re-grooms occasionally"* rather than *"the tool never does this"* — `B82`'s dogfood blindness exactly. **Severity is P1 and the reason is claim integrity, not convenience:** a stale queue is not missing coverage, it is a document actively asserting false completeness — `M5.E10`'s own defect class, in the file a user trusts most. **None of the six checks `M5.E10` shipped detect it**, and the NFR5 corpus measurement did not look for it. *Fix:* `dischargeBacklogRows()` in `backlog.js` reusing the `discharged`/`dischargedBy` shape `obligations.js` already defines; a SHIP §6.6 step symmetrical with the inbox sweep, requiring the Epic to name the rows it discharges; a `/sig:sweep` check flagging rows whose work looks shipped. **FIXED 2026-08-13 (`M5.E10` S7, `92ad0df`)** — `dischargeBacklogRows` + `parseBacklogRows` in `backlog.js`, `ship.md` §6.6 (Epic-close, rows named by the Epic), and a `/sig:sweep` `backlog-discharge` check. The discharge rewrites the **heading** rather than dropping an HTML marker: a comment does not render, so the document a reader opens would still assert the same false status. **The detector is 1 of 12 on the corpus and cannot evaluate `eval-project-A`, the project this bug was filed from** — its rows name no unit ids. The *closing mechanism* reaches it; the detector half is Signal-shaped and the measurement says so rather than implying coverage. |
| B93 | `confirmed` | **P2** | **`commands/discuss.md` reads the tier BEFORE the Epic roll, so a `--epic` run gates the whole phase on the PREVIOUS Epic's profile.** §0 is headed *"Tier-gating preamble (run before anything else)"* (`discuss.md:11`) and Epic mode is §*"run before Step 1"* (`:42`) — so an agent following the file literally calls `readEffectiveProfile` while `current_epic` still names the **closing** Epic, and an Epic-scoped `{PrevEpic}-PROFILE.md` is still shadowing. **Measured at `M5.E10`'s open 2026-08-11, not reasoned:** pre-roll the effective profile read **FEATURE / `gate_strictness: light`** (`M5.E19-PROFILE.md`); post-roll it read the project's **FULL / strict**. Those differ in behaviour the phase actually branches on — `light` batch-confirms once at the end, `strict` confirms every gray area individually. **This is `B59`'s shape one layer up.** `B59` was a malformed Epic PROFILE that threw, so `M5.E16` ran its whole DISCUSS at the project's FULL; here the file is well-formed and the *ordering* supplies the wrong one. `B59` was found the first time any code read the file; this was found the first time anyone read the tier twice. **Not hit at `M5.E10`** — the tier was deliberately re-read after `setCurrentEpic` and the requirements record why (`D-M5E10-5`), so the exposure is the instruction, not this run. **Fix:** in `--epic` runs the Epic roll must precede the tier-gate read; the preamble's *"before anything else"* needs an explicit `--epic` carve-out, since it is correct for every non-Epic invocation. **Check the five sibling phase commands for the same ordering before fixing just this one** — `plan.md`, `execute.md`, `verify.md`, `review.md`, `ship.md` all open with the same preamble, and fixing one file is how `M5.E17`'s class recurs. |
| B92 | `confirmed` | **P2** | **A drain disposition stamp is only recognised when the reason's LAST word is `drain`.** `STATUS_DISPOSED_RE` (`drain.js:65`) is `\(\[^)\\n]*\\bdrain\\b\)` — the word must sit immediately before the closing paren, not merely somewhere inside it. So `(inbox drain — shipped in M4.5.E2)` reads **live** to the code while reading **disposed** to a human, and the entry resurfaces at every future drain; `(delivered by M4.5.E2 — inbox drain)` disposes. Measured both ways 2026-08-10 during the 52-entry inbox drain, on an in-memory copy before any write. **This is `B4`'s shape** (a stamp format the detector could not see, so promoted entries resurfaced) and the exact hazard `drain.js:60-64` warns about for the verb list — stated there for *which verbs* are recognised, and silent about *where in the reason* the keyword must fall. Nothing in the reason's own contract says it: `applyDisposition`'s JSDoc calls `reason` a free-text "stamp context, e.g. 'M4.5.E2 drain'", and the example happens to comply. **Caught only because the run asserted the candidate count dropped by the batch size**; a visual read of the file would have passed. **Fix candidates:** relax the regex to `\\bdrain\\b[^)\\n]*` so the keyword may fall anywhere in the reason, or validate the reason at write time in `transformBlock` and throw — silently writing an unrecognised stamp is the failure mode. **Second, smaller defect in the same pass:** `transformBlock` appends to the first physical `**Status:**` line, so on a hard-wrapped Status paragraph the stamp lands mid-sentence (hit 1 of 12 entries; repaired by hand). |


## M5.E7's changelog section is invisible to `/sig:update` · **RESOLVED 2026-08-18**

**Status:** resolved-not-a-defect

The heading was relabelled from `## [Unreleased] — 2026-07-26 …` to `## 2026-07-26 — The v2 direction audit (M5.E7) · analysis only, no version cut`, which was the actual defect: a section that said *unreleased* about finished work.

**The invisibility itself turns out to be correct, not a bug.** The entry's own first line says *"No code shipped, so no version was cut."* Nobody updating between two versions ever received this, because there was nothing to receive — so `changelogBetween` skipping it is the right behaviour, and giving it a version number would have invented a release that never happened. That would have been a false published fact, in the file this week's work exists to keep honest.

Closes the matching entry in `OPEN-QUESTIONS.md`.

---

## Whole-population deny assertion — the shape B81 needs

**Status:** needs-triage

**Approach for `B81`, borrowed from `vercel-labs/eve-software-factory-template` (MIT).** Its eval harness enumerates the whole write-tool population once, in `evals/helpers.ts`, and every read-only eval asserts `notCalledTool` across **all of it** rather than naming the one tool a bad run might reach for. Its own comment states the property: *"a new write tool added to the extension is automatically forbidden in every read-only eval until someone allows it deliberately."*

That is deny-by-default over an enumerated population, and it is exactly the shape `B81` needs. `guard-callers.test.js` today governs **1 of ~25 CLI flags** in `tools/` and its "population is not empty" assertion cannot distinguish that from governing all of them — so the fix is not "detect more flags", it is "assert over the declared population and fail when a member is ungoverned."

Two details worth copying with it: the population list is **kept next to the thing it mirrors** with an explicit sync note naming the source file, and the *exceptions are named individually* with a reason (`read_factory_brain` and `read_artifact` are deliberately absent from the deny list, and the comment says why). An exception with a written reason is auditable; an omission is not.

Reference: `evals/helpers.ts` in that repo. Not a straight port — Signal has no tool-call event stream to assert against — but the population-assertion shape transfers to `tools/` flags directly.

---

## Archived phase logs are written but never staged

**Status:** needs-triage

Archived phase logs are written to disk but never staged, so an Epic's ledger survives the roll and then never reaches the repo. Found 2026-08-19 by `git status` at the M6.E3 PLAN entry, not by any check. Both Milestone 6 archives were untracked: `.planning/archive/M6/E1/STATE-NARRATIVE.md` (written 2026-08-18, orphaned since v0.1.26) and `.planning/archive/M6/E2/STATE-NARRATIVE.md` (written during the M6.E3 roll). Neither is gitignored, and every M4.5 and M5 archive IS tracked, so this is a staging gap, not a policy. `commands/ship.md` §5.5 says the evicted narrative is "staged into the SHIP commit" and §6/§6.5/§6.6/§8 say the same for their artifacts; for M6.E1 and M6.E2 that did not happen, twice in a row, and nothing noticed. THE SHARP EDGE: `.planning/INDEX.md` IS tracked and committed, and it links to both files — lines 292-293, `archive/M6/E1/STATE-NARRATIVE.md` and `archive/M6/E2/STATE-NARRATIVE.md`. So the repository publishes an index pointing at two files that do not exist for anyone who clones it, and `/sig:index` regenerated those rows from the working tree without ever asking whether the target was in the repo. WHY IT MATTERS BEYOND TIDINESS: this is `B52`'s harm reached by a different route. The `setCurrentEpic` guard correctly refuses to zero an unarchived phase log, the archive gets written, and then nothing carries it into version control — so the ledger exists on exactly one disk. The maintainer works on one machine, so an untracked archive is one disk failure from gone, and the guard that exists to prevent silent ledger loss reports success. FIX SHAPE, NOT YET DECIDED: either the eviction path stages what it writes, or a check asserts that every path `INDEX.md` links to is tracked — the second is the more general one and would catch any future write-then-forget, not just this one. The first is smaller. Note the second is a closed-list absence check (the list being INDEX.md's own links), which is exactly the shape `M6.E3` `D-M6E3-2` says absence is provable against.

---

## `attention` is undocumented in profile-schema.md

**Status:** needs-triage

`attention` is a live PROFILE.md field that `profile-schema.md` does not document. Found 2026-08-23 while writing `M6.E4-PROFILE.md`. `ATTENTION_LEVELS` (`attended`/`checkpointed`/`unattended`) is exported from `tools/lib/profile.js` and `attentionFor` derives it from `gate_strictness` — but `references/profile-schema.md`, which `tier-definitions.md` points to as "the full PROFILE.md schema", has ZERO occurrences of the word. It is documented only in `phase-gates.md` and `anti-rationalization-forms.md`. Same class as `B108` (v0.1.31 moved confirm cadence to `attention` and left six command files describing the old dial) — the authoritative schema doc was one of the surfaces that release did not sweep. Cost: anyone writing a PROFILE.md by hand from the authoritative doc cannot discover the field, and cannot learn that omitting it derives a value rather than leaving it unset. Concretely: this session's first draft asserted `light` derives `attended`; it derives `checkpointed`, and the schema doc offers no way to check that.

---

## dischargeBacklogRows is blind to rows nested below h3

**Status:** needs-triage

dischargeBacklogRows cannot see any row nested deeper than h3. Found 2026-08-24 at M6.E4 SHIP §6.6. parseBacklogRows matches /^(#{2,3})\s/ (backlog.js:325), so rows at #### are invisible — and #### is CORRECT markdown for a row nested under a ### section heading, which is how BACKLOG.md's "Twelve promoted from the inbox drain" section is built. All three rows M6.E4 closed returned not-found and had to be struck by hand. This is B94's own class recurring: the mechanism built so the backlog stops asserting false completeness cannot reach the rows that need discharging. Scope: 12+ rows in that section today, plus any future section that nests its rows. Fix candidates: (a) widen to #{2,4}, weighing the container-fold comment at backlog.js:331 which reasons explicitly about ## vs ### depth pairs and would need re-checking at depth 4; (b) flatten the section so rows sit at ###, which loses the grouping. Note the instruction in ship.md §6.6 says not-found means "check the wording" — it assumes a wording problem and has no branch for a structural one.

---

## ⚠ WITHDRAWN as a duplicate of `B100` — `AC_ID_RE` drops the sub-letter

**Status:** withdrawn (duplicate)

**This was filed at `M6.E5` DISCUSS on 2026-08-27 as `B111`. It is a duplicate of `B100`**
(`confirmed`, P2, filed **2026-08-14**), which already says the same thing and says it first:
*"`diffRequirementCoverage` cannot see a letter-suffixed acceptance-criterion id, and reports
`covered` while blind to it."* `B100` found it on `M6.E1-VALIDATION.md` — 24 ACs by grep, a
denominator of 22, outcome `covered`.

⚠ **It was never assigned the id `B111` in this file** — it was captured as a heading, not a table
row — **while five other documents in the same PR cited `B111` as though it were.** Caught by the
independent reviewer on PR #211, not by the filer.

**How it happened, because that is the useful part.** `BUGS.md` was not grepped before filing. The
defect was rediscovered by running the coverage extractor over a fresh Epic's REQUIREMENTS, which is
exactly how `B100` was found two weeks earlier — the same route, the same tool, the same conclusion,
by someone who did not know the row existed. **A capture command with no duplicate check makes this
the default outcome, not an unlucky one.**

**What this entry adds to `B100`, and it is small but real:** a second, independent measurement
(`M6.E5-REQUIREMENTS.md` — the shipped extractor returns **5** ids where **34** exist), and the
observation that the collapse propagates into `checkValidationConsistency`, which reads both halves
of its diff through the same extractor and can therefore only confirm agreement at **group**
granularity. Both are folded into `B100` rather than tracked here.

---

## `detectProjectKind` calls every non-git directory `greenfield`, so a real codebase is told to start fresh

**Status:** needs-triage

**Status: verified by execution WITH A CONTROL ARM, 2026-08-27.** `detectProjectKind`
returns `greenfield` for **any** directory without a `.git`, however much source it contains — so
`/sig:add`'s missing-`.planning/` error tells the operator of a full codebase to *"start fresh"*
when *"scan the existing code"* is the correct advice.

**The deciding line** (`tools/lib/add.js:192`):

```js
export function detectProjectKind(baseDir) {
  if (!existsSync(join(baseDir, '.git'))) return 'greenfield';   // <-- returns here
  ...
  const hasSource = entries.some(...)                             // <-- dead for non-git dirs
  return hasSource ? 'brownfield' : 'greenfield';
}
```

**Measured, with the control arm that makes it a finding rather than one odd row:**

| Directory | Returns | Message composed |
|---|---|---|
| this repo (`.git` + source) | `brownfield` | run `/sig:init` — correct |
| `examples/url-shortener` (real Node project, no own `.git`) | **`greenfield`** | run `/sig:new-project` — **wrong** |
| scratch: full codebase, **no `.git`** (`package.json` + `src/` + `README.md`) | **`greenfield`** | run `/sig:new-project` — **wrong** |
| scratch: **truly empty** dir, no `.git` | `greenfield` | run `/sig:new-project` — correct |

**Rows 3 and 4 are indistinguishable.** A full codebase and an empty directory produce the same
verdict and the same advice. The `hasSource` scan that exists precisely to tell them apart is
unreachable for either, because neither has a `.git`.

**The consequence, in the user's terms.** `buildMissingPlanningError` exists to route someone to the
right onboarding command. For a non-git codebase it routes them to `/sig:new-project` (*"start
fresh"*) instead of `/sig:init` (*"scan it"*). Following that advice onboards a real project as a
blank one.

**How common is a codebase with no `.git`?** More common than it sounds: an unpacked archive, a
vendored or downloaded project being evaluated, a subdirectory of a larger repo (which is what
`examples/url-shortener` is — the case that surfaced this), or someone who simply has not run
`git init` yet. The last is also the case where the advice matters most, because they have the most
existing code and the least scaffolding.

**Priority P3, not P2:** the failure is a wrong *message* on an error path, not data loss or a wrong
write. Nobody is broken by inaction — only by taking the advice, which is `B102`'s shape at much
lower stakes.

**Fix shape (candidate, not decided).** Do the `hasSource` scan **first** and let `.git` be one
signal among several rather than a gate. ⚠ **Not obviously free:** `.git` is currently doing double
duty as a cheap "is this a project root at all" guard, so removing it as a precondition means
deciding what an empty non-git directory should return, and `NON_SOURCE_ENTRIES` becomes
load-bearing where today it is unreachable for this case. Worth pairing with a test that asserts the
two scratch directories above return **different** verdicts — the assertion that currently cannot
pass.

**Found 2026-08-27** at `M6.E5` EXECUTE, by the wave-1 first-execution probe (`S1.t0`) that the
plan scheduled specifically to run existing machinery before building on it. The probe's stated
purpose was to check whether `detectProjectKind` was usable for `FR5.3`; the answer was no, and the
reason turned out to be a live defect on a different command's path.

---

## An Epic-lane `--merge` produced a SQUASH, and a published adherence verdict immediately pointed at a commit absent from `main`

**Status:** needs-triage

**Status: observed live on `M6.E5`'s merge, 2026-08-28, with the predicted consequence.**

`gh pr merge 211 --merge --delete-branch` produced a **single-parent commit** — GitHub's squash
shape (`d0ac0c5`, titled with the first commit's message plus `(#211)`) — and **none of the
Epic's 24 commits reached `main`.**

```
git rev-list --parents -n1 HEAD   ->  1 parent
gh api .../pulls/211              ->  commits: 24, merge_commit_sha: d0ac0c5
gh api .../commits/main           ->  parents: 1
```

**Nothing forced it.** The repository allows all three methods
(`allow_merge_commit: true`), and the only ruleset (`main-requires-pr-and-green-ci`) permits
`["merge","squash","rebase"]`. So `--merge` was passed, squash was performed, and **no error was
raised at any layer.**

**The predicted consequence happened immediately, and it is not hypothetical.**
`CLAUDE.md` § *How changes reach `main`* mandates `--merge` for the Epic lane on one stated
ground: `.planning/ADHERENCE-LOG.md` pins commit SHAs as its reproducibility anchor, and
`adherence-run.js` names the failure mode in its own words — AC4.3 breaks when *"the record would
name a state nobody can return to."*

After this merge, `ADHERENCE-LOG.md:56` read **`Commit: 0e88e03`** — a commit from this very
Epic, absent from `main`, surviving only on a deleted branch ref in one local clone. **A published
verdict pointing at a state nobody can return to, exactly as written.** Repaired by recomputing the
ceiling against `d0ac0c5`, which is on `main`.

**Why this is the repository's own named class.** The two-lane table is prose. Nothing reads it, and
nothing checks the merge method afterwards — so the rule held for as long as whoever merged happened
to remember it, and failed the first time the flag alone was trusted to carry it. That is
`UNREACHED-MECHANISM-ANALYSIS.md`'s class applied to delivery, and it is the same shape as
`B88` (Signal was branch-blind while `execute.md` said *"create an atomic git commit"*) one step
further down the pipeline.

⚠ **Root cause NOT established.** What is verified is the outcome and that no configuration required
it. Whether `gh` silently fell back, whether a repository default overrode the flag, or whether the
flag was mis-parsed is **unknown** — and guessing would be the completeness-claim class this
repository files bugs about. The next Epic merge is the experiment: pass `--merge`, then check
`git rev-list --parents -n1 HEAD` before doing anything else.

**Fix shape, cheapest first:**

1. **A post-merge check that reads the artifact rather than trusting the flag** — after an Epic-lane
   merge, assert `main`'s tip has two parents, or that a named Epic commit is an ancestor of
   `main`. Cheap, deterministic, and it fires where the situation is.
2. **A guard over `ADHERENCE-LOG.md` itself:** every `Commit:` it pins must be an ancestor of
   `main`. This is strictly better than (1) because it checks the property that actually matters —
   reproducibility — rather than the mechanism assumed to deliver it, and it would have caught this
   regardless of how the squash happened.
3. Set the repository's default merge method for good measure. ⚠ **On its own this is the weakest
   option**, because it is another unenforced setting: it would not tell anyone when it stopped
   working, which is precisely how this row came to exist.

**Found by checking the merge result instead of trusting the command's exit code** — the merge
reported success, and the tally that revealed the problem was a `git rev-list --parents` run purely
to confirm the Epic lane had been honoured.

---









*0 needs-triage · **8 captured-untriaged** · 24 confirmed · 3 dismissed · 84 fixed (**119 total**). Last updated: 2026-09-01 — **`B113` FILED AND FIXED (P2)**: `drive.md` documents a two-argument `canProceedUnattended` call, so the `v0.1.32` loop ceiling can only ever return `loop-unknown` at VERIFY and REVIEW — reproduced against the live module. Prior: 2026-08-25 — **`B109` FIXED (P3)**: the release notes' own test-count trailer is reconciled from the gating run. ⚠ The fix design stated at filing (*throw on absence*) was **wrong** — 19 of 33 released sections carry no trailer, so absence returns unchanged **and reports** instead. Prior: 2026-08-24 — **`B109` FILED (P3) + `B110` FILED AND FIXED (P3)**, from the `v0.1.33` cut. `B109`: `cut-release.js` reconciles `facts.md`'s count and attribution but not `CHANGELOG.md`'s own test-count trailer, which is hand-typed mid-Epic and therefore stale **by construction** at every cut — `B106` one layer over, caught by CI review comparing two files, invisible to the suite. `B110`: `BACKLOG.md`'s *"Twelve promoted"* heading sits over **thirteen** rows, and that count was used to derive a wrong answer before merge. Prior: 2026-08-21 — **`B108` FILED AND FIXED (P2) + `B74` CLOSED**: `v0.1.31` moved confirm cadence to `attention` and left **six** command files describing the old dial — `M5.E17`'s class created by the release that fixed the dial. **The manual sweep found three; the test written to pin the fix found three more.** Four phase-approval boxes are now conditional and name their flag; **SHIP's stays unconditional because it is a floor**, and says so at the box. ⚠ **`B75` deliberately NOT closed** — the dial is now documented end to end and enforced nowhere: nothing fails if a command ignores `confirm_in_phase`. Prior: 2026-08-21 — **`B107` FILED AND FIXED (P3)**: the private-name guard walked tracked files only, so a new file was invisible to it until staged — a local run reported clean on the very files most likely to carry a new name, and CI caught what the developer's own suite could not. Found because a `B76` identifier collided with a denied project name (`B97`'s recorded residual hole, in project shape). Prior: 2026-08-21 — **`B76` FIXED (P2) — the loop that did not stop.** `review.md`'s FAIL path returned to EXECUTE with no ask and no ceiling; `/sig:drive` shipped in `v0.1.31` and inherited it **exactly as this row predicted in August** (*"any driver built on top inherits it on day one"*). Fixed with a **counter derived from `completed_phases`**, not a sentence — mirroring `verify.md`'s prose ceiling would have closed it on paper while leaving the unattended loop unbounded. **`B73` FIXED** alongside it (`phase-gates.md` claimed approval was unconditional while the code had three attention levels). **`B74` deliberately NOT closed** — its documentable half is recorded, its five unconditional approval checkboxes are not, and marking it fixed for the cheap half would be this repository's own named defect. Prior: 2026-08-21 — **`B106` FILED AND FIXED (P3)** at the `v0.1.31` cut: `cut-release.js` rewrote the published test count and left the sentence attributing it to the previous release. **The detector worked and the tool did not** — `M6.E2`'s `facts-attribution` check fired on the cut, which is the unreached-mechanism class one layer over: the check names the condition, and the tool that creates it every release was never taught to fix it. Prior: 2026-08-20 — **`B105` FILED AND FIXED (P3)**: a guard on `CHANGELOG.md` asserted a property true only while no release is pending, so **writing the release notes failed the suite** — the guard blocked the workflow it protects. Found by doing the ordinary thing, and its own comment history had already recorded the identical error one revision earlier, inverted. Counts re-derived by `rewriteBugTally`, not incremented by hand. Prior: 2026-08-18 — **TWO CAPTURES FILED from a read of `vercel-labs/eve-software-factory-template` (MIT)**: a whole-population deny-assertion shape that is what `B81` actually needs, and the observation that Signal has **no prompt-injection resistance and no test for it** while `/sig:init`'s scanners, `/sig:add`, and every phase command read text Signal's operator did not write. Both filed by `captureToBugs` **above** this line with the counts re-derived — `v0.1.29`'s fix on its first real use, and it reported `repaired: true`, normalising a file that had had captures stranded below this tally since 2026-08-04. Prior: 2026-08-18 — **`B102` STATUS CORRECTED to `fixed`**, and it was corrected **by a check finding it** rather than by anyone remembering: `M6.E2`'s `bug-status-vs-changelog` flagged the row while `B102` had shipped in `v0.1.27`. `B38` carried the identical drift in August and was found by a hand count. Counts re-derived by `rewriteBugTally`, the function `M6.E2` S2 built — its first use on real data. Prior: 2026-08-18 — **`B103` FILED AND FIXED (P1)**: `/sig:doctor --fix` generates `rm -rf` for cache copies that *running* sessions resolved, behind a `[y/N]` that reads as a formality — `B52`'s hazard reached through the remediation path instead of the detector. Found by running `--fix` on this machine: two live sessions were bound to `sig/0.1.25`, which the script listed for deletion. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-18 — **`B102` FILED (P1)**: the `v0.1.26` migration instructions uninstall the plugin and assert they don't. Prior: 2026-08-17 — **`B99` FIXED (packaging half) in `M6.E1`/`v0.1.26`**; the public-repo half stays deferred and the row says so. Also **`B101` FILED (P2) at `M6.E1` VERIFY**: `diffRequirementCoverage` empties its own `unattributableGroups` when the report merely names a group — including in a sentence saying the group was not checked. Sibling to `B100`, same tool, same run. Prior: 2026-08-14 — **`B100` FILED (P2) at `M6.E1` PLAN**: `diffRequirementCoverage` reports `covered` while blind to letter-suffixed AC ids — found on its first use in a new Epic, against this Epic's own VALIDATION, where it read 22 of 24 criteria. Both the tool and the author were wrong about the same document in different directions; the refutation is kept in `M6.E1-VALIDATION.md`. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-14 — **`B38` STATUS CORRECTED to `fixed`**: it shipped in `v0.1.25` and read `confirmed` for one day. Found by the unreached-mechanism count (`analysis/UNREACHED-MECHANISM-ANALYSIS.md` § *The count, run*) — by measuring this file, not by anything that reconciles it. No bug was fixed or filed; one status moved. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B95` and `B96` FIXED (`M5.E10`)**: the retro index sorts by Epic ID (the `**Closed:**`-date fix was measured out — 3 of 28 retros carry one), and `extractEpicSection` reads a blockquoted heading, so Epic-close eviction will actually run at ship. **`B99` CORRECTED IN PLACE** — its first version asserted "57 MB delivered to users"; that was a local-path install including untracked `node_modules`. A remote install carries ~11 MB of tracked files, 4.7 MB of it `.planning/`. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B97` and `B98` FIXED (`M5.E10`)**: 113 name replacements across 30 files, all 13 corpus projects denied in two match modes, all 13 labels pinned by hash, and the two published claims that overstated the guard corrected in place. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B99` FILED (P2), raised by the maintainer**: the installed plugin is the whole repo — **57 MB where 1.9 MB is needed**, including 47 MB of dev dependencies and **4.5 MB of Signal's private working notes**. Publishing `.planning/` in the *repository* was chosen; shipping it into every user's *plugin cache* followed from `source: "."` and was never decided. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B97` FILED (P1) and `B98` (P2) by the independent `/code-review` pass on `M5.E10`, both reproduced before filing**: the private-name guard covers 5 of 13 corpus projects and this Epic added a plaintext mention of an unguarded one, in a PUBLIC repository; and `measure-corpus.js` pins only labels A–E, so every project past E is renamed whenever corpus membership changes. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B96` FILED (P2) while validating `M5.E10`'s SHIP preconditions**: `extractEpicSection` cannot see a blockquoted heading, so `evictEpicNarrative` reports `no-section` — *"a safe no-op"* — for a 5 KB narrative that exists. `B39`'s shape, and live STATE.md accretes as a result. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B95` FILED (P2) by `M5.E10`'s own VERIFY**: `RETROSPECTIVES.md` orders by file mtime, which git does not preserve, so the Epic's own FR7 freshness check reports structural drift on a fresh clone — every row present, ordering only. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B94` FIXED (`M5.E10` S7, `92ad0df`): the backlog gains a discharge path, a SHIP step that uses it, and a sweep check — and the check's own corpus measurement says it evaluates 1 of 12 projects and cannot see the one `B94` was filed from.** Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-13 — **`B94` FILED (P1): `BACKLOG.md` is append-only; nothing marks a row done when work ships. Folded into `M5.E10` ahead of VERIFY.** Prior: 2026-08-11 — **`B93` FILED (P2) at `M5.E10`'s own DISCUSS**: `commands/discuss.md` reads the tier **before** the Epic roll, so a `--epic` run gates the whole phase on the *previous* Epic's profile. Measured at this Epic's open — pre-roll **FEATURE/light**, post-roll **FULL/strict**, and the phase branches on that difference. `B59`'s shape one layer up: there a malformed Epic PROFILE threw, here a well-formed one is read at the wrong moment. **Not hit here** — the tier was deliberately re-read after the roll (`D-M5E10-5`) — so the exposure is the instruction, not the run. The row names the five sibling phase commands that open with the same preamble, because fixing one file is how `M5.E17`'s class recurs. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-10 — **`B46` RE-TRIAGED AND DISMISSED**, the decision it was deliberately held `confirmed` for. Its cause was disproved by measurement in August and its symptom was discharged by the inbox drain the same week — the entries are stamped, so nothing resurfaces. **Kept as a row, not deleted:** the useful part is a plausible cause that measurement killed. The drain also showed why no single fix ever fitted it — *"47 candidates resurface"* was one number covering three different situations. Counts re-derived by `tools/lib/bugs-tally.js`, not incremented. Prior: 2026-08-10 — **`B92` FILED (P2)** during the 52-entry inbox drain: a disposition stamp is only recognised when the reason's **last** word is `drain`, so a natural reason like `(inbox drain — shipped in M4.5.E2)` reads disposed to a human and live to the code. `B4`'s shape, found the same way `B4` was — by an entry that would have resurfaced. **Caught only because the run asserted the candidate count dropped by the batch size**, not by reading the file. Counts re-derived by `tools/lib/bugs-tally.js` (92 table rows + 2 heading-captures), not incremented. Prior: 2026-08-09 — **`B77` FIXED: this line is now DERIVED, and a test fails when it disagrees with the file.** `tools/lib/bugs-tally.js` counts both entry formats and normalises the status cell; `tests/bugs-tally.test.js` pins it, with the live guard **shown red** against the real 2026-08-08 error before being shown green. **It caught its own author on first use:** flipping `B77` to `fixed` moved two cells, and the check named both before this line was rewritten — which is the entire point, since the previous four re-derivations were done by hand and one of them still shipped wrong. **Half of `B77` remains open by decision**, not oversight: captures still insert *below* this line, because `FUTURE_IDEAS_FOOTER_RE` anchors on `*Last updated:…*` and this footer is a tally line that never matched that shape. The guard cannot stop that insertion; it makes it loud. Also this pass: **four stale `confirmed` rows annotated in place** (`B62` headline lapsed at v0.1.19's merge-strategy change, residue live; `B46` **premise falsified**, needs re-triage not a fix; `B56` numbers three corrections old, structural gap fully live; `B71` symptom resolved by the M5.E17 backfill, detector gap untouched) — each carrying its re-check on the claim's own line, per `M5.E10`'s correction-protocol corollary. Counts re-derived **by the module, not by grep** (91 table rows + 2 heading-captures). Prior same-day: **`B91` FIXED the same day it was filed**, in Wave 1 of the backlog review: the unbuilt Epic is renumbered **`M5.E20`** and the `BACKLOG.md` entry records that it carried a dead ID for nine days rather than swapping it silently. **Half the bug is deliberately left open and said so:** nothing reconciles the two places an Epic ID is allocated (promotion in `BACKLOG.md`, Epic-open in `{EpicID}-*.md`), so the collision can recur tomorrow; the hygiene test that would have caught it on 2026-08-01 is named in the row and not yet built. Counts re-derived by grepping the status column (91 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B91` FILED (P2)**: `M5.E16` names two different Epics — an open `BACKLOG.md:550` entry promoted 2026-07-30 and the shipped STATE-vs-world Epic opened 2026-08-01 — so a live roadmap item is unreachable by its own ID, breaking ID-is-identity. Found by a backlog-review pass reading `BACKLOG.md` against the on-disk artifacts. **Counts re-derived by grepping the status column (91 table rows) plus `**Status:**` heading-captures (2), not incremented — and the previous tally was wrong in two of its five cells:** it read `28 confirmed · 60 fixed` where the file held `26 · 62`. Nothing was miscounted at write time; `B87` and `B90` flipped to `fixed` on 2026-08-08 and **that same tally paragraph announces both flips by name** while the cells above it were left alone. That is `B56`'s class in the footer that exists to count, for the fourth recorded time, and the convention it violates is stated a dozen times in this very paragraph. The total (`92`) was **correct** both before and after, because the two errors offset — which is exactly why a spot-check of one cell is not a re-derivation. Prior: 2026-08-08 — **`B87` FIXED**: a new `phase-log-gap` drift check compares `completed_phases` against the artifacts that PROVE a phase ran, so a hand-driven Epic that skips the phase-entry call is caught by `/sig:sweep` and `/sig:resume` instead of by someone reading the ledger. **Detect-only by decision, not limitation** — the log is append-only (`D-M5E9-5`) and writing an entry the flow never produced is `B48`'s lesson. Fires today on Signal's own repo, naming the M5.E19 gap. Counts re-derived by grepping the status column (90 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B90` FIXED the same day it was filed**, and fixed as a **mechanism, not a paragraph**: `readTierAdvisory` fires at `/sig:status` and `/sig:resume` on a FULL project with no per-unit `PROFILE`, naming both ways down; `CLAUDE.md`, `escalate.md`'s own description and `tier-definitions.md` no longer describe the dial as one-way; and *project tier is a CEILING, not a floor* is now stated where tiers are defined. **Verified against the corpus, not asserted:** the advisory fires on exactly the 6 of 12 projects the scan flags, and is silent on the 1 that has used the dial. **The scan counter did NOT move and is not expected to** — it measures adoption, which changes when a project is re-tiered, not when Signal is fixed; saying so is the point, since chasing it would tempt weakening the check. Counts re-derived by grepping the status column (90 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B90` filed (P1)**: Signal **can** lower rigor per unit (`/sig:escalate` Case C, Epic-scoped PROFILE, `phases_skipped`) and every surface that introduces the feature says it only raises it — so a `eval-project-A` slice paid FULL-tier ceremony on a two-hour UI fix. A **discoverability** defect, not a missing feature: the capability was verified present before filing. Counts re-derived by grepping the status column (90 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B89` filed (P2)**: `commands/plan.md` grants permission to skip the inbox drain at `:66` and forbids skipping it at `:210`, in the same file — `M5.E17`'s class, but **within one file**, where the cross-document tests M5.E17 shipped do not look. Live: the drain was skipped at `M5.E19` PLAN citing that permission, with **52 candidates** queued. Counts re-derived by grepping the status column (89 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B88` filed (P1)**: Signal is **branch-blind** — nothing in 20 commands or any library module reads the current branch, so `/sig:execute` commits wherever you are and `/sig:ship`'s "create a pull request" is unreachable once everything has landed on the default branch. Reported from a `eval-project-A` slice that put five code commits on `main` with no PR; **verified against Signal's own files rather than accepted on report**, and the grep came back broader than the report claimed. Counts re-derived by grepping the status column (88 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B85` fixed (v0.1.23)**: `commands/update.md` now prescribes the marketplace-qualified `sig@signal`; the bare `sig` was rejected by the CLI (`Plugin "sig" not found`, reproduced on the live CLI before fixing), so the command's only mutating step had never succeeded as written through four releases. **The class is now guarded, not just the instance** — `tests/prescribed-cli.test.js` walks every `claude plugin <verb> <target>` string in `commands/` and compares the target against an identity DERIVED from `plugin.json` + `marketplace.json`, so it needs no CLI, no network, and keeps working if either name changes. Verified to go red against the pre-fix text. Counts re-derived by grepping the status column (87 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior: 2026-08-07 — **`B87` filed at M5.E19 VERIFY**: `B41`'s shape survives in human-driven runs — EXECUTE ran and never entered `completed_phases`, because `transitionPhase` lives inside commands this repo's own Epics frequently bypass. Caught while it was happening. Counts re-derived by grepping the status column (87 table rows) plus `**Status:**` heading-captures (2), not incremented. Prior same-day: **`B82` flipped to `fixed`** in the fix lane: `planArchiveMoves` now consumes `deriveUnits` instead of rebuilding `{unit}-{suffix}.md`, so a closed unit archives whole. Measured through `senseArchiveTree` (the live command path) rather than the library alone: `SLICE-SSO` planned 3 of 5 files in **both** `eval-project-A` and `eval-project-D`; across 12 local projects 3 split units / 6 stranded files → **0 / 0**. Signal's own tree shows 0 splits before and after — every unit here is a strict Epic ID, so the template and the derivation agree by construction and **dogfooding was structurally blind to it**. Counts re-derived by grepping the status column (86 table rows) plus `**Status:**` heading-captures (2), not incremented; the previous tally was correct in all five cells before this flip. Prior: 2026-08-06 — **`B85` filed and `B86` dismissed, both from one live `/sig:update` run** (v0.1.19 → v0.1.20, user-confirmed). `B85`: `commands/update.md:49` prescribes `claude plugin update sig`, which the CLI rejects — the marketplace-qualified `sig@signal` is what works, so the command's only mutating step has never succeeded as written, through four releases, because **nothing in the suite executes the CLI string a command file prescribes**. `B86` is the finding that filing `B85` corrected: *"the CLI exits 0 on failure"* was reported to the user **before it was measured**, from an `EXIT=0` that a zsh pipe had made meaningless (`PIPESTATUS` is bash's spelling; the fallback read `tail`'s status). Unpiped, it exits 1. **The dismissal is kept as a row rather than deleted** — the trap is reusable and the claim was already stated aloud. Counts re-derived by grepping the status column (86 table rows) plus `**Status:**` heading-captures (2), not incremented; the previous tally was **correct in all five cells**, the first time in this file's recent history that a re-derivation has changed nothing. Prior same-day: **`B84` filed AND fixed the same day**, in the fix lane: the anchor is now positional (pending = above the newest released heading), verified by running the real tool rather than a fixture. Counts re-derived by grepping the status column (84 table rows) plus `**Status:**` heading-captures (2), not incremented. Filed earlier that day: **`B84` from the v0.1.20 cut itself**: `cut-release.js`'s no-release-notes guard is unreachable, because a historical `## [Unreleased]` heading satisfies it forever — and the tool then relabelled that historical section as this release. Found by reading the diff the tool produced, not by reading the tool. Same day: **`B52` flipped to `fixed` (v0.1.20)**, both halves, in the fix lane; the row carries what building it found (the ledger loss has **two** causes with no stale cache involved, so the fix is wider than the row asked for). **Counts re-derived by grepping the status column (83 table rows) plus `**Status:**` heading-captures (2) — not incremented — and the previous tally was wrong in three of its five cells:** it read `30 confirmed · 51 fixed (84 total)` where the file held `28 · 54 · 83+2`. Nothing was miscounted at write time; the tally simply was not re-derived while `B64`/`B72`/`B63` and others were flipped around it, which is `B56`'s class (a published count nothing derives from the artifact) sitting in the footer that exists to count. Prior: 2026-08-04 — **`B63` flipped to `fixed`**, found by a cross-document contradiction sweep: this row read `confirmed` while `CHANGELOG.md` `[0.1.18]` listed it as Fixed **and** `migrate-memory.js:1945` carries the fix naming the bug. That is the **third** fixed-and-never-flipped instance in this one release — the pattern this footer said, the same day, was "about to happen again inside the Epic meant to stop it." It did. Counts re-derived, not incremented. Prior same-day: **`B82` filed from the curator-removal work**: `planArchiveMoves` never calls `deriveUnits`, so it plans **3 files where the derivation resolves 5** (measured on eval-project-A's `SLICE-SSO`) — archiving half a unit, which is the outcome M5.E18's own test comment says the fold prevents. Found by running both functions against a real corpus rather than reading either. Prior same-day: **`B81` filed at M5.E15 PLAN**: `guard-callers.test.js` detects one of three argv idioms in `tools/`, so it governs **1 of ~25 flags** — and its "population is not empty" assertion passes on a population of one, unable to distinguish *found everything* from *found one thing*. Found by checking whether `D-M5E15-5` actually lands (it does not — `adherence-run.js` contributes zero flags despite having twelve). **Not absorbed by M5.E15**: the naive widen is wrong, because the 10 orphans it surfaces are mostly options rather than guards and the test has no definition separating them. Prior same-day: **`B80` filed at M5.E15 DISCUSS**: `ADHERENCE-LOG.md` carries three kinds of annotation while `NOTICE_KINDS` knows two, so the `DIAGNOSED` block on the `INDETERMINATE` record was hand-written into an append-only log. Found by the next person who needed the mechanism — the same way `B55`, the bug this Epic exists to fix, was found. In scope for M5.E15 (AC7.3). **Counts re-derived by grepping the status column with the `B77`-safe pattern (80 table rows) plus `**Status:**` heading-captures (2) — not incremented.** Prior same-day: **two heading-captures filed via `/sig:add --bug`, and the second is the defect that filing the first exposed.** The first: M5.E7 shipped under an `## [Unreleased]` heading at `CHANGELOG.md:213`, and `changelogBetween` skips `[Unreleased]` by design — so that release's section can never appear in any `/sig:update` delta, for any user, at any version range. The second: `captureToBugs` inserts with `insertAtEnd` and `commands/add.md` asserts BUGS.md has *"no footer to rewrite"* — **this line is that footer**, so both captures landed physically below it and neither incremented it. **This tally was corrected by hand, which is precisely the bug**; the inbox path has had `insertAboveFooter` + `rewriteFooter` + a `repaired` announce all along. Counts re-derived from both formats — 79 table rows by status column, 2 heading-captures by `**Status:**` grep — not incremented. Prior same-day: **`B79` filed at M5.E18's SHIP** — `evictEpicNarrative` has never been able to fire for Signal's own STATE.md and reports it as a clean no-op. Prior same-day: **`B64` and `B72` fixed by M5.E18 wave 3** (S5.t1 and S5.t3), flipped in the same pass that shipped them. **Flagged because it nearly recurred:** both were cited in commit messages and left reading `confirmed` — *"fixed and never flipped"* is the exact pattern the 2026-08-02 reconciliation found **eleven** instances of, and it was about to happen again inside the Epic meant to stop it. **The status grep is also corrected here:** the pattern now anchors on `` | B{n} | `status` `` without requiring the cell to end there, so `B49`'s `` `fixed` (v0.1.13) `` is counted — the earlier pattern read 47 where the truth was 48, which is `B77`'s class in the tally itself. Prior same-day: **`B78` fixed in the fix lane** ahead of its own deadline (`D-M5E18-6`); `review.md` now states the PASS-WITH-FIXES rule once, and the product call flipped *"requires new tests"* from disqualifier to **obligation**. **The re-derivation caught `B77`'s class a second time, in this very tally:** a status-column grep returns **47** `fixed`, because one row (`B49`) reads `` `fixed` (v0.1.13) `` — a status cell with a parenthetical the pattern cannot see. The true count is **48**. A tally that greps for exact cell values is still a tally that cannot see one of its own file's formats; **`B77`'s open code fix should normalise the status cell before counting, not add a second pattern.** Prior: 2026-08-03 — **`B73`–`B78` filed in one reconciliation pass across two parallel sessions.** `B73`–`B76` are the loop-engineering audit's `LE-1`…`LE-4`, **triaged from heading-captures into numbered rows** so they are countable and citable; `B77` is why that mattered; `B78` is the `review.md` PASS-WITH-FIXES finding reported from `eval-project-A` and re-verified here. **The `captured-untriaged` column is new and is `B77`'s point:** `promoteToBugs` appends heading-style entries with no B-ID by design, and this tally greps table rows — so it read *"0 needs-triage"* on a file holding four. **A count that cannot see one of its own file's two formats is not a count.** Both are now derived, and `0 · 0` means *checked both, found none* rather than *looked at one*. **ID collision avoided in the same pass:** two independent sessions had each been told the next ID was `B73`; they are now `B73`–`B76` and `B78`, assigned once. Counts re-derived by grepping the status column, not incremented. Prior same-day: `B70` fixed (v0.1.17), `B71` filed, `B32` fixed. Counts re-derived from the table, not incremented by hand; they were stale by two before this pass, which is what a hand-maintained tally does. Prior note, 2026-08-02 (**status-column reconcile against the code**, fix lane, no Epic. STATE.md queued this as *"26 rows read `confirmed`; B42, B44, B45 and B53 are verifiably fixed."* Checking all 26 — plus every `needs-triage` and `dismissed` row, because the status column is the thing being reconciled and no one had said the others were right — found **17 wrong rows, not 4**. **Eleven were fixed and never flipped:** B39, B41, B42, B43, B44, B45, B48, B51, B53, B54 (all M5.E9 or M5.E13, each carrying its bug number in the code beside the fix) and **B36**, which was sitting at `needs-triage` despite being closed by M5.E13 S4.t1. STATE's framing that *"the linear-mode rows are exactly the stale ones"* is half right: B41–B45 are linear-mode, but B39/B48/B51/B53/B54 are M5.E13 rows and were equally stale. **B20 was mis-statused `dismissed`** — a real bug, duplicate of B5, and fixed (`eslint.config.js` exists, `npm run lint` is clean). **Five `needs-triage` rows triaged to `confirmed`** against the code — B32 (widened to 17-vs-19), B33, B34, B35, B60 — leaving the `needs-triage` bucket empty for the first time. **New: B68**, the unimplemented half of B53's fix shape, split out rather than buried in a row now marked `fixed`. Every flip carries a file:line or a test that fails if it regresses; nothing was flipped on STATE's say-so. Verified clean at 1938 tests. Prior: 2026-08-02 (**B63**–**B67** — five findings from a eval-project-A session report, reviewed against the code the same day and all `confirmed` by reading it. **B63**: `/sig:migrate-memory`'s dry-run prints `0` for its two Epic-only vectors on a linear project, so *could not apply* is byte-identical to *already clean* — `C1`'s class in the command next door, live the same week M5.E16 fixed it for `/sig:sweep`. **B64**: `senseArchiveTree` proves closure by a retro **file existing** and discards the `isStub` flag `enumerateRetros` already computed, so a `[FILL IN]` placeholder archives a live Epic. **B65**: `B57`'s `.migrate` ignore — correct for noise, 12 structural findings → 2 — means nothing now reports the snapshot's age or whether it can still roll back. **B66**: `checkStaleInbox` fires at `n > 0` with no magnitude, which is what turned a deliberately-filed eval-project-A tripwire into indistinguishable entry #110. **B67**: nothing detects a *second* index (`llms.txt` at 1182 lines contradicting a 482-line `INDEX.md`). **Count correction made in the same pass:** this tally read `5 · 18 · 2 · 30 (55 total)` against a table holding **62** rows — stale by seven since `B56` landed, and itself an instance of `B56`'s class (a published count nothing derives from the artifact). Re-derived by counting the rows, twice. Prior: 2026-07-29 (**B55** — the adherence canary's control arm was never isolated ACROSS FILES: only the target command is mutated, while `transitionPhase` is named 4× each in `plan.md`/`verify.md`/`review.md`. Found by the harness's first real use; `B41-phase-entry` re-ran **3/3 treatment vs 1/3 control → INDETERMINATE**, replacing M5.E8's `OBEYED`. **M5.E13 S2.t4 amplified it by design** — FR1.1's single-shared-wording requirement puts a byte-identical copy of the deleted instruction in the sibling files. M5.E8's verdict is not falsified but was **unisolated**: clean by luck, not construction. Prior: **B46 PREMISE FALSIFIED at M5.E13 EXECUTE — re-triage required, NOT fixed.** B46 says *"M5.E7's 45 dispositions were written to a side artifact and never stamped back into `ISSUES-INBOX.md`, so 47 candidates resurface identically at every future drain."* Measured against both files: `M5.E7-DISPOSITIONS.md` holds **66 data rows** (not 45) and the inbox holds **48** candidates (not 47) — but the decisive fact is that **0 of the 48 inbox candidates correspond to any disposition row**. The disposition table's population is the **v2 feature-port candidates** (§3 is explicitly *"`MILESTONE-5.md` § 'Candidate v2 feature-port scope' — 23 items"*, plus §4's seed-five and §5's Signal-native N-items). All five of its references to the inbox cite it as **evidence of demand** — *"captured verbatim in `ISSUES-INBOX.md`, its single home"* (:156), *"Register cluster I — `ISSUES-INBOX.md:1475`"* (:161) — never as a target to be stamped. **So FR2.2's stamp-back would write verbs from one population onto a different one: not a data correction but a data corruption,** and precisely the hazard M5.E9 named when it refused a blanket defer (*"would have written wrong verbs over already-decided entries"*). **Work stopped rather than forced; S3.t2 left open.** The residual complaint is real — 48 candidates do resurface at every drain — but its cause is not un-stamped dispositions, so the remedy is a scope call, not an implementation detail. *Incidentally confirmed that dispositions are actionable and simply were not walked:* row **N4** reads *"`B39` — implement the trigger-watchlist walk | **BUILD**"*, which M5.E13 S3.t1 built — reaching it via the bug ledger, not via the disposition table. Prior: **B54** cataloged at M5.E13 **PLAN** — `checkGateArtifacts` is the never-called-guard class's **fourth** instance and the only one that is **wrong if wired up**: nothing calls it, yet executed against Signal it returns `{ready:false, missing:['REQUIREMENTS.md']}` because it hardcodes the unprefixed name while Epic mode writes `M5.E13-REQUIREMENTS.md`, and four of its five gates are vacuous empty lists. Two docs describe it as part of the flow and a third records a **fix once applied to it**. **Being uncalled is what protected its bug from discovery.** Disposition ratified same day: **replace** (delete + rebuild the refusal in `transitionPhase`), per M5.E9's remove-don't-re-tune precedent. **Corrects D-M5E13-3's locked count of three to four.** Prior: 2026-07-28 (**B53** cataloged — a **non-strict `current_epic` splits artifact WRITE naming from READ resolution**: `artifactName` uses the strict shape and emits `1-PLAN.md`, `resolveArtifactPath` uses a lenient regex and resolves `PHASE11-PLAN.md` **first**, so a fresh write is shadowed by a stale file — while `references/epic-native-flow.md:31` guarantees the two "are symmetric." Verified by executing both against `eval-project-C`'s live `current_epic: PHASE11`. Its origin is a **`B50` sighting in a downstream project**: a session predicted `/sig:ship` would halt with `NO_CURRENT_EPIC`, and executing v0.1.13 **and** v0.1.11 shows neither halts — `PHASE11` is truthy, so the pre-`B42` branch never fired. *Counts corrected in the same pass: the tally still read `12 confirmed / 49 total` after **B50**–**B52** landed.* Prior: **B49 FIXED in v0.1.13** — `package.json` and the plugin manifest disagreed on the version; both now read `0.1.13`. The consistency *assertion* is still open. **B36 sighted live a third time** at M5.E8's SHIP: the FR1 retro gate skipped because `MILESTONE-5.md` still carried E8 as `▶ NEXT` — the retro existed only because it was written before the gate ran. Prior: **B48** cataloged at M5.E8 **EXECUTE** — `execute.md`'s phase-entry instruction is unconditional, and an agent **explicitly refused it** rather than write a false record into the phase log; the instruction and M5.E9's own ledger-integrity rules are in direct conflict, and obeying either one breaks the other. Found by reading an adherence-run transcript, **not** by the harness's verdict — that run scored `ABSENT`, which was a fact about the fixture. Prior: **B47** cataloged at M5.E9 **REVIEW** — `/sig:resume` reads `0/7 phases done` right after a linear ship, a cosmetic consequence of FR5's trim, deliberately deferred because the fix is a new capability. Prior: **B46** cataloged at M5.E9 **PLAN** Step 1b — M5.E7's 45 dispositions were written to a side artifact and **never stamped back** into `ISSUES-INBOX.md`, so 47 candidates resurface identically at every future drain, `abandon` verdicts included; `confirmed` by grepping both files. Homed in M5.E9 as a **later slice, not v0.1.12** — a blanket defer would have written wrong verbs over already-decided entries. Prior: 2026-07-26 — **four findings cataloged from a `eval-project-A` ship report**, all triaged to `confirmed` by reading the code the same day — **B42** (a project running in linear mode cannot run `/sig:ship` at all; the FR1 gate hard-halts on `current_epic: null` with no bypass, and the halt is *asserted* by `tests/ship-fr1.test.js` — **P1**, live since v0.1.3), plus **three distinct defects stacked in the same seven lines, `state.js:388-395`**: **B43** (it records the phase being *left*, so a SHIP date is unrecordable by construction — SHIP is terminal — **P2**), **B44** (set semantics applied to log data: the dedupe collapses a multi-unit `completed_phases`, silently, with no diff/warning/count — **P2**), **B45** (existing entries are never validated, so a junk line becomes a permanent phantom phase key — **P2**). B44/B45 are **not linear-mode-only**: Epic mode is spared only because `setCurrentEpic` zeroes the list on every roll. All three sequence **before or with B41**, which would otherwise turn four more commands into collapse sites. A writer audit run at the same time clears the other frontmatter mutators — `transitionPhase` is the only one that rebuilds an array rather than patching named fields (detail in B44). A fourth item in the same report — `npm run ship:archive` archiving future-slice briefs — is **not Signal's**: that script is `scripts/ship-archive.mjs`, built inside the reporting project's own DOC1 slice (see `examples/Example-cmmc-STATE.md:101`); Signal ships no archive-on-SHIP command. Prior: M5.E7 **REVIEW** — **B41** (four phase commands never call `transitionPhase`; `markFresh` then stamps the stale position fresh — **P2**, confirmed by grepping `commands/*.md` and reading `transitionPhase`) cataloged; homed in **M5.E9**. Prior: M5.E7 EXECUTE Wave 2 — **B39** (trigger watchlist never walked; `/sig:plan` has no such drain step — **P2**, root cause of the register's dead-trigger finding) and **B40** (citation integrity in `analysis/` unenforceable by construction) cataloged, both triaged to `confirmed` by reading the code. Prior: M5.E7 cataloged **B37**/**B38**; M5.E6 SHIP cataloged **B36**).*
