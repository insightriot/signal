# Signal — Bugs & Findings

Lightweight catalog of bugs, defects, and verified findings for Signal itself.
Markdown-native by design — GitHub Issues are deferred until Signal has live
users (see `FUTURE-IDEAS.md` § "GitHub Issues for work-item tracking — deferred").

**Discipline** (per the "always catalog bugs" norm): catalog a finding the moment
it surfaces → **triage** (read the code / confirm the root cause) → mark
**confirmed** or **dismissed**. Never graduate a finding to a confirmed bug
unverified.

- **Status:** `needs-triage` · `confirmed` · `dismissed` · `fixed`
- **Priority:** `P1` ship-blocker · `P2` should-fix · `P3` nice-to-have
- **Scope:** defects + verified findings only. Architectural ideas → `FUTURE-IDEAS.md`;
  design questions → `OPEN-QUESTIONS.md`; decisions → `DECISIONS.md`.

| ID | Status | Pri | Summary |
|---|---|---|---|
| B1 | `dismissed` | — | **`/sig:checkpoint` "Epic-ID-as-task-ID" + "only 2 of 5 commits".** Non-bug (triaged 2026-06-06). `isStateStale` deliberately filters the commit walk to `STATE_AFFECTING_PATHS` (`state.js:583`, D6 — editing future-ideas / decisions / milestone files is metadata curation, excluded by design), so 2-of-5 counting is correct. `TASK_ID_RE` (`checkpoint.js:25`) intentionally matches every hierarchy level, so `taskIdsInCommits` can include Epic/milestone IDs — harmless: it only filters `current_tasks`, which holds full task IDs. Surfaced from unverified surface behavior during a `markFresh` detour; reading the source dismissed it. |
| B2 | `fixed` | P2 | **FUTURE-IDEAS footer drift — new entries could land below the `*Last updated:*` footer.** Once any content sat below a stranded mid-file footer, `insertAboveFooter` buried every subsequent `/sig:add` above it. **Fixed in v0.1.5 (M4.5.E10.S3):** `insertFutureIdeasEntry` footer-repair (relocate footer to EOF + absorb stranded content, announce) + `lintFutureIdeasFooter` dogfood lint. Migrated from FUTURE-IDEAS 2026-07-13 (v0.1.6.S4). |
| B3 | `fixed` | P3 | **`/sig:add` derived-title cut mid-clause.** `deriveHeading` (`add.js`) sliced the first ~6 words / 60 chars, landing mid-clause (the "STATE.md append-without-evict" capture headed "closed-work narrative must"). **Fixed in v0.1.6 (FR4, `16155b3`):** prefer the first em-dash/`.`/`:`/`,` boundary (followed by whitespace; em-dash not hyphen; ≥20-char floor) before the length cap, word-slice fallback. Shared helper → all 3 capture destinations. |
| B4 | `fixed` | P2 | **Drain disposition-detector missed blockquote promotions.** The 2026-07-04 backlog review stamped promotions as `> **Promoted … → …**` blockquotes, which neither `HEADING_DISPOSED_RE` nor `STATUS_DISPOSED_RE` recognized — so promoted entries resurfaced on every `/sig:plan` drain (~42 candidates, incl. 6 already promoted). **Fixed in v0.1.6 (FR3, `df4fdb4`):** `parseEntries` scans the entry's header region for a `^`-anchored, fence-aware leading blockquote stamp. Live drain candidates 43→37 (FR3 alone; 35 after FR5 removed 3 entries). |

| B5 | `fixed` | P3 | **`npm run lint` is broken — ESLint v9 with no flat config.** `package.json` pins `eslint ^9.0.0` (installed 9.39.4), which requires an `eslint.config.js`; none has ever been committed, so `npm run lint` exits 2 (`ESLint couldn't find an eslint.config.(js\|mjs\|cjs)`). **Pre-existing** (predates v0.1.6 — no lint config in git history; v0.1.6 touched none of it). Not a blocker: `npm run validate` (the plugin validator + 889-test vitest suite) is the effective quality gate. Surfaced during v0.1.6 VERIFY (2026-07-13). Fix = add a flat `eslint.config.js` (separate chore). |
| B6 | `fixed` | P3 | **`/sig:resume` + `/sig:status` origin-drift banner false-positives on your own just-pushed checkpoint commit.** `isStaleVsOrigin` compares `origin/main` to STATE's `last_updated_commit`, **not** to local `HEAD`. A STATE-writing commit leaves `last_updated_commit` one behind `HEAD` (it records the last *work* commit; the bookkeeping commit is the "+1"), so right after a clean checkpoint+push, resume shows `⚠ origin is 1 commit ahead … git pull` even though local `HEAD` == `origin/main` and there is nothing to pull. Confirmed 2026-07-15 (M4.5.E11.S1 close): `{stale:true, aheadCount:1, touchedPlanning:true}` while `git rev-list HEAD..origin/main` = 0. Advisory + fail-open, so harmless — but the "git pull" advice is misleading at the exact resume-trust moment E10 was built to protect. **Fix:** gate the banner on `git rev-list HEAD..origin/main > 0` (a genuine remote push — local actually *behind* origin), not merely on the STATE baseline lagging. Adjacent to M4.5.E10/E11 resume-trust work. *(Re-observed 2026-07-16 during the M5.E1 `/sig:resume` — both the local-stale and origin-drift banners fired on the `markFresh after PLAN close` commit while local HEAD == origin/main; same root cause. The local-stale side is the same "+1 bookkeeping commit" lag.)* |
| B7 | `fixed` | P2 | **`plugin.json` version stuck at `0.1.6` through the entire v0.1.7 release — clean main fails `install-contract.test.js`.** At `HEAD` **and at the `v0.1.7` tag itself**, `.claude-plugin/plugin.json` reports `"version": "0.1.6"` while `CHANGELOG.md` (`[0.1.7] — 2026-07-15`), the `v0.1.7` git tag, and `marketplace.json` `source.ref` all say v0.1.7. `install-contract.test.js:66` (`expect(marketplace…source.ref).toBe(\`v${plugin.version}\`)`) fails on a clean checkout: ref `v0.1.7` ≠ `v0.1.6`. **User-visible:** a `/plugin install` of v0.1.7 self-reports version 0.1.6 in the plugin list. **Root cause:** the M4.5.E11 SHIP (v0.1.7) bumped CHANGELOG + tag + marketplace ref but missed `.claude-plugin/plugin.json` (last bumped at `7302c21 Signal v0.1.6`). Confirmed 2026-07-16 (M5.E1 EXECUTE baseline run: 998 pass / 1 fail). **Secondary drift (needs a look, not the test cause):** `marketplace.json` pins `sha: 8e05e56…` but `v0.1.7` resolves to commit `0eb8ca8…` — the pinned sha ≠ the tag commit. **Fix:** bump `plugin.json` → `0.1.7` (and reconcile the marketplace `sha` if the drift confirms) on a standalone release-hygiene commit; not M5.E1 scope. |

| B8 | `fixed` | P2 | **v0.1.6 write-guard wedges pre-existing prose files, and its reason message hides the only escape.** The FR1 guard evaluates *whole proposed content*, not the delta (`v0.1.6-PLAN.md:9` R1.2 option (a)) — justified at PLAN by "**FR1 is self-unblocking** — a cleanup edit to clean frontmatter always passes (AC1.7)" (`v0.1.6-PLAN.md:11`). **That claim is true but load-bearing on knowledge the user never gets.** Because the block is whole-file, a file with N prose entries only unblocks via **one atomic write that de-proses ALL N at once**; fixing them one at a time — the natural instinct, and what an Edit-driven agent does by default — fails on *every* intermediate attempt, so the file presents as permanently bricked. The reason string ("move the narrative into the STATE.md body below the frontmatter, then re-write") never says the block is whole-file or that incremental cleanup cannot work. **Confirmed live 2026-07-16** on `nextpass/.planning/STATE.md` (529 KB; 12 `completed_phases` entries, **10 prose**, one 55 lines; `completed_phases` = 93 of 107 frontmatter lines): Brett reports STATE writes blocked in that repo — every Signal command that writes STATE is stopped. Escape hatch verified against the real file: one edit replacing the whole entry list with 12 single-line ≤150-char scalars flips the guard `block:true → block:false`, frontmatter 107→27 lines, **body byte-identical**. Note remediation is **two Epics out** — vector-1 de-prose is routed to E3/FR6 (`references/doc-runtime-model.md` §3), so v0.1.6 shipped a hard block whose only interim path is undocumented manual surgery. **Fix (cheap, no design change):** extend the reason string to state the block is whole-file and that all offending entries must be fixed in a single write; consider naming the count/first offending entry. Guard semantics stay as-is — the self-unblocking design is sound, only its discoverability fails. **→ Mitigation landed 2026-07-16 (this commit):** all four guard reason strings now append a whole-file note ("this check reads the whole file, so if more than one entry is affected they must all be fixed in a single save — correcting them one at a time will fail on every attempt") via a shared `WHOLE_FILE_NOTE` const in `retrospective.js`, with a B8 regression test (`hook-state-write.test.js`). The *discoverability* half is closed; the *auto-remediation* half (a one-shot de-prose of a wedged file) remains the migrate command's job — **pulled forward to M5.E2** (was E3/FR6) per the 2026-07-16 sequencing pivot. B8 stays `confirmed` until the migrate command lands. |

| B9 | `fixed` | P2 | **`setCurrentEpic` doesn't reset `phase`/`completed_phases`/`last_completed_task` on an Epic roll — every 2nd+ Epic opens with stale phase state.** `setCurrentEpic` (`state.js:577`) resets only `current_wave`→null and `current_tasks`→[] on a roll (`state.js:595-596`); it leaves `phase`, `completed_phases`, and `last_completed_task` at the *previous* Epic's values. The E11 `--epic` open path (`commands/discuss.md` §"Epic mode") calls `setCurrentEpic` and documents only the wave/tasks reset, so opening a new Epic into DISCUSS leaves e.g. `phase: SHIP` + the prior Epic's `completed_phases`. The first phase-gate (`transitionPhase`) then *appends* to that stale list — polluting the new Epic's `completed_phases` with the prior Epic's phases + `SHIP`. **Confirmed 2026-07-16** (M5.E2 open): after `setCurrentEpic(base,"M5.E2")` the roll left `phase: SHIP` + `completed_phases: [DISCUSS,PLAN,EXECUTE]` from M5.E1 (had to hand-fix to `phase: DISCUSS` + `[]`). Triaged how M5.E1 itself came out clean: its first post-open commit (`0fff9e5`) already shows `completed_phases: [DISCUSS]` + `last_completed_task: null`, i.e. it too was reset out-of-band (manual) since no tool does it — so the gap is real, not a one-off. **Fix:** `setCurrentEpic`, on a roll (not the idempotent same-Epic case), should also reset `phase`→null (or the caller sets DISCUSS), `completed_phases`→[], and `last_completed_task`→null atomically under the same lock; update `commands/discuss.md` + `commands/new-project.md` Epic-open docs to match. Candidate to fold into M5.E2 (touches the same state-mutation core) or a standalone E11 follow-up. **→ Fixed 2026-07-18 (`22abe65`, M5.E2.S1.t0):** on a roll (not the idempotent same-Epic case) `setCurrentEpic` now resets `phase`→null, `completed_phases`→[], `last_completed_task`→null atomically under the state lock (`blockers` preserved — a blocker can span Epics); the caller sets the real phase immediately after via `transitionPhase`, which records a clean per-Epic `completed_phases`. `commands/discuss.md` + `new-project.md` Epic-open docs updated to match. Verified live 2026-07-18 opening M5.E3: clean `phase: null`→DISCUSS, zero stale M5.E2 leak. *(Ledger correction: the fix shipped inside M5.E2 but this entry wasn't flipped at the time — caught opening M5.E3.)* |

| B10 | `fixed` | P2 | **`/sig:migrate-memory` doesn't archive `SHIP` (or `LAUNCH-KIT`) scaffolds — closing docs orphaned in root while their Epic's siblings move to `archive/`.** `SCAFFOLD_SUFFIXES` (`tools/lib/archive-tree.js:50`) = `[REQUIREMENTS, RESEARCH, PLAN, PROGRESS, VERIFICATION, VALIDATION, REVIEW]` — no `SHIP`. Inherited verbatim from the `archive-migrate.mjs` prototype's `SCAFFOLD` literal (written before Signal had `-SHIP.md` docs). **Confirmed 2026-07-17** via the S4.t1 dogfood dry-run on Signal's own `.planning/`: 28 moves proposed for M4.5.E5 / M4.5.E10 / M4.5.E11 / M5.E1, but `M4.5.E5-SHIP.md`, `M4.5.E11-SHIP.md`, `M5.E1-SHIP.md`, and `M4.5.E5-LAUNCH-KIT.md` are left in root — an incomplete/inconsistent archive (NOT data-loss; relocate-never-delete holds, nothing deleted). **Fix:** add `SHIP` to `SCAFFOLD_SUFFIXES` (+ a non-M4.5 fixture test); decide `LAUNCH-KIT` (one-off — archive-with-Epic or keep-in-root is a judgment call). Should fix BEFORE the S4.t1 apply so the dogfood produces a complete archive. **→ Fixed 2026-07-17 (`9064340`, S2.t6):** added `'SHIP'` to `SCAFFOLD_SUFFIXES` (RED-first on a non-M4.5 `M6.E1` fixture); re-dry-run on Signal's `.planning/` now = 31 moves (the 3 `-SHIP.md` docs archive with their Epics); `RETROSPECTIVE` + `LAUNCH-KIT` stay in root. 1258→1260 tests. |
| B11 | `fixed` | P3 | **`vector-2-defer` flag over-fires — one spurious flag per historical closed Epic on any already-evicted corpus.** `classifyClosedEpicBody(stateBody, epicId)` returns `vector-2-reclassify` whenever a closed Epic has no section heading in the *current* STATE body — but on a healthy/mature project the STATE body holds only current state, so **every** past closed Epic (whose narrative long ago moved to its retro + archive) trips it, emitting `FLAG: vector-2-defer — <Epic> … handled by the vector-2 whole-body relocate, not V3` with **no corresponding move** (`vectors:[]`, `v3.evicts:0`). **Confirmed 2026-07-17** via S4.t1 dry-run on Signal's `.planning/`: 12 vector-2-defer flags, zero backing relocations. Advisory noise, no data impact — but it makes the dry-run's flag section useless-to-misleading on any real project. **Fix (REVIEW):** only emit `vector-2-defer` when there is actual un-sectioned closed-Epic narrative *present in the body* to relocate, not for every historical Epic absent from it. **→ Fixed 2026-07-18 (`dd77ef1`):** `planVector3` case 4 now gates the flag on `senseInlinedBody(stateText).candidate` (the same predicate `applyMigrate` uses for the whole-body relocate) — fires iff the relocate it defers to will actually run. Skeleton/already-relocated → 0 flags (nextpass real dry-run 12→0); genuine inline narrative → still fires. Advisory-only; noop/idempotency/vectors unchanged. |

| B12 | `fixed` | P2 | **Vector-1 de-prose leaves a generic `[relocated…]` placeholder (not a meaningful label) for a `completed_phases` entry that lacks a leading `PHASE (date)` token — and sweeps an *active* marker into history.** The de-prose keeps a short scalar by grabbing the leading `PHASE (date)` prefix; an entry without one falls back to `"[relocated to STATE body — migrate-memory]"`, erasing which entry it was (the prose is preserved verbatim in the body — **no data loss** — but the frontmatter label is useless). **Confirmed 2026-07-18** via the S4.t2 nextpass full-migrate on a throwaway copy (546 KB → 1.3 KB STATE.md): the entry `"**▶ Active: Slice SEC1 — Supabase Security-Advisor Hardening: DISCUSS ✅ → …"` (a free-form active-work marker parked in `completed_phases`) placeholdered, and its prose relocated to `STATE-HISTORY.md` — i.e. an **active** item filed into **history** (exactly the "don't bury a live open item" case the S1.t2 carry-forward flagged for the S4 eyeball). **Fix (two parts):** (1) derive a meaningful truncated label from any entry, never a generic placeholder; (2) DECIDE (product) whether an entry that reads active (`▶ Active:`/similar) should be flagged/skipped rather than swept to history, or whether `completed_phases` is always fair game (→ data-hygiene guidance instead). Surfaced by the real dogfood — a "battle-tested" find. Fix before the release. **→ Fixed 2026-07-18 (`e42afed`):** new `deriveNonStandardLabel` (mirrors `/sig:add`'s clause-boundary `deriveHeading`: first clause ≤60 chars, 6-word fallback) for any `completed_phases`/`blockers` entry lacking a `PHASE (date)` prefix — never the generic placeholder; idempotent; conservation unchanged. `renderDryRun` now emits `⚠ Non-standard completed_phases entries relocated (N) … verify these were actually complete` (product call: flag-in-dry-run, still relocate). Verified on the real nextpass file: SEC1 entry → label `"▶ Active: Slice SEC1"` + warning fires; 0 placeholders. 1264→1269 tests. |
| B13 | `fixed` | P3 | **`tools/lib/migrate-memory.js` has a literal NUL byte (~offset 57833) that makes `grep` treat the whole file as binary (returns nothing).** In `computeDanglingDelta` a map key uses a raw NUL delimiter `` `${d.file}<NUL>${d.link}` `` written as an actual NUL in source, not the `\0` escape. Functional (the key works), but plain `grep`/`grep -n` on the file silently returns nothing — every agent working M5.E2 hit this and had to fall back to `rg -a`/node (it's the "grep glitchy in this sandbox" note that recurred). Dev-experience regression, not a runtime defect. **Fix:** replace the raw NUL with the `\0` escape sequence (byte-identical behavior, restores greppability). Surfaced during M5.E2 VERIFY (B12 work), 2026-07-18. **→ Fixed 2026-07-18 (`50ad065`):** raw NUL → `\0` escape at the `computeDanglingDelta` key; byte-identical behavior, `grep` restored (38 exports now visible, was 0). **→ Recurrence in `.planning/BUGS.md` itself, fixed 2026-07-25 (found at `/sig:resume`):** this entry re-introduced the byte — the writeup above pasted the offending source snippet verbatim, NUL included, so **BUGS.md became grep-blind** from `5062526` until this fix (`grep -c needs-triage .planning/BUGS.md` → no output, exit 1; `grep -a` → 8). Sharper edge than the source case: BUGS.md is the file agents grep to check bug status, so the failure mode is a silent "no open bugs" read. Fixed by rendering the delimiter as the placeholder `<NUL>`; Node/`fs` reads were never affected. **Lesson:** never paste a raw control byte into a bug writeup — describe it. |
| B14 | `fixed` | P3 | **Lexical (symlink-blind) path confinement in `evict.js` / `add.js` / `resume.js` — the codebase-wide sibling of the M5.E2 symlink escape.** The `resolve(dest).startsWith(planningRoot + sep)` lexical guard (no `realpathSync`) lets a checked-in **directory symlink** inside `.planning/` escape the tree on write/move. **M5.E2 REVIEW fixed this in `migrate-memory.js` + `archive-tree.js`** (commit `ab2242d`: `realpathNearestExisting` + `assertRealInsidePlanning`), but the same pattern remains at `tools/lib/evict.js:350-351` (`evictEpicNarrative` archive path — closest sibling, same mkdir/write/move shape), `tools/lib/add.js:830` (capture path), and `tools/lib/resume.js:74` (read-side filter, lower severity). Confirmed 2026-07-18 by the M5.E2 security audit. **Fix:** apply the same `realpath` re-assertion to those write/move gateways (read-side is lower priority). Separate ticket — deferred to avoid scope-creeping the E2 REVIEW. **Why safe to defer for E2:** `/sig:migrate-memory` never routes a write through `evict.js`'s vulnerable path — §9 forbids calling self-locking `evictEpicNarrative` (the engine uses the lock-free `relocateFaithful` spine, which E2 REVIEW hardened), so this codebase-wide gap is genuinely NOT in this command's attack surface. |
| B16 | `fixed` | P3 | **A failed/rolled-back `--apply` in git mode now leaves a lingering `pre-migrate-memory-<stamp>` tag.** Introduced by the C1 rollback fix (`50ad065`): the pre-apply tag is hoisted to run *before* the mechanical phase so non-git modes have a recovery pointer. On a successful apply the tag is the intended revert pointer (correct, printed in `revertLine`). But on a **rolled-back** apply the tag now persists (pointing at the restored pre-apply state) — harmless, but it accumulates across retries in a stranger repo. **Fix:** clean up the tag in the rollback catch (or make it idempotent per-run). P3 — note in SHIP notes. Surfaced 2026-07-18 (M5.E2 REVIEW, Batch 1 handoff). |
| B15 | `fixed` | P3 | **`FILE_SCAN_CEILING` (1 MB) truncation can silently defeat the blocking dangling-link gate on a >1 MB single doc.** `scanDanglingLinks`/`scanResidualFlatPaths`/`scanAtRiskAnchors`/`scanUnhandledLinkForms` (`migrate-memory.js` ~:1350-1495) `slice(0, 1MB)` before matching. A migrate-introduced dangle sitting **past the 1 MB mark** in a single `.planning` doc is never seen → the apply commits a dangling link the gate was supposed to catch. **Bounded/extreme:** the real corpora are well under 1 MB (nextpass 546 KB), so no shipping repo hits it today; needs a >1 MB single file. Confirmed 2026-07-18 (M5.E2 REVIEW test-adequacy audit — the truncation branch is also untested). **Fix:** for dangling detection specifically, scan the whole file (or a much higher ceiling), or flag when truncation occurs so a silent pass is impossible. Separate ticket. |

| B17 | `fixed` | P3 | **4 git-heavy migrate tests flake on vitest's 5 s default timeout under full-suite parallel load.** `migrate-symlink-confinement.test.js` + `migrate-apply*.test.js` (and 1–2 siblings) do real `git init` / `execFileSync` per test; when all 81 files run in parallel (`npm test`) a few intermittently hit `Test timed out in 5000ms`. They pass clean in isolation and on a second full run — **not** a product defect, the tests are correct. **Confirmed 2026-07-18** during the M5.E2 SHIP pre-ship checklist: run 1 = 4 failed / 1296 passed (all 4 were 5000 ms timeouts in migrate suites); run 2 = 1300 passed. There is no `vitest.config.*` (default 5 s applies). Dev-experience / CI-flake only. **Fix:** give the git/fs-heavy migrate suites an explicit `testTimeout` (~15 s) — a `vitest.config.js` with `test.testTimeout`, or the per-`it` third arg on the offending tests. Deferred out of the SHIP commit (introducing test-infra config isn't in a migrate-command SHIP's surgical scope). |

| B18 | `fixed` | P3 | **`regeneratePlanningIndex('.')` / `enumeratePlanningDocs('.')` corrupt INDEX.md when `baseDir` is `'.'`.** The doc-key derivation does `full.slice(baseDir.length + 1)` assuming `baseDir + '/'` prefixes every walked path. With `baseDir='.'`, `path.join('.', '.planning')` normalizes to `.planning` (no `./`), so `baseDir.length` is 1 and `slice(2)` chops the first two chars → mangled keys (`lanning/DECISIONS.md`), a `{written:true}` diff, and garbage written over INDEX.md. **Latent, not a live break:** every shipped caller (`migrate-memory.js` tail regen, `ship.md`, `commands/index.md`) passes an absolute / `process.cwd()` baseDir, so the canonical path is unaffected; only a `'.'` (or other non-prefix-normalized) baseDir triggers it. Confirmed root cause 2026-07-19 (M5.E3 S6b dogfood curation — bit the curation pass once; recovered from the staged copy). **Fix:** `resolve(baseDir)` at entry (or compute the rel key via `path.relative(baseDir, full)` instead of `slice`). Separate ticket. |
| B19 | `fixed` | P2 | **The v3 migrate's tail `regeneratePlanningIndex` silently clobbers a pre-v3 hand-curated INDEX.md.** `applyMigrate`'s composed flow ends with an unconditional `regeneratePlanningIndex(baseDir)` (the FR3 SHIP/migrate reconcile). `parseExistingAnnotations` preserves curated notes in the NEW list format, but **cannot read the OLD table format** — so a project migrating from a richly hand-curated INDEX (Signal's own: a tier legend + per-file gotchas + per-Epic one-liners) has every note wiped to `_(note pending)_` on first migrate, with no warning and no diff surfaced in the dry-run (a verbatim-move preview shows no INDEX prose change). Confirmed 2026-07-19 (Signal dogfood — 89-line curated INDEX → 165-line auto-gen, 1 Gotcha → 0, 143 note-pending; repaired manually by porting the still-valid notes, `exec-s6b-curate`). **General-project risk:** any stranger repo with an old-format curated INDEX loses it silently. **Blast radius WIDENED by the stamp-null fix (`6dc7fa2`):** before it, only stamp-2 repos armed the migrate; now EVERY existing external project (all stamp-null at the combined release) routes through v3 on first `/sig:migrate-memory`, so this silent-INDEX-clobber is the **default path for all of them**. Real-world exposure is still small (few external repos hand-curate INDEX), but REVIEW should decide whether it **gates the release** vs. ships as a documented limitation. **Fix candidates:** (a) detect an unportable-format INDEX and SKIP the auto-regen (leave the curated file, flag for manual `/sig:index`); (b) surface the INDEX rewrite in the dry-run diff so it's human-approved; (c) a one-time old→new annotation-format migrator. REVIEW item (M5.E3). |

| B20 | `fixed` | P3 | **`npm run lint` is non-functional under ESLint 9 — no flat `eslint.config.js`.** `eslint tools/` exits **2** with "ESLint couldn't find an eslint.config.(js\|mjs\|cjs) file" (ESLint 9.39.4 dropped `.eslintrc.*` support; the repo has neither a flat config nor a legacy one). So the VERIFY "linter passes" criterion cannot be satisfied — code quality is instead covered by the clean `npm run build`, the 1481-test suite, and the FR4 doc-hygiene guard. **Pre-existing, not M5.E3:** `CONTEXT.md` references this as "`BUGS.md` B5 (lint tooling)" but no B5 entry actually exists in this file (lost in an earlier edit) — re-cataloged here. Confirmed 2026-07-19 (M5.E3 VERIFY). **Fix:** add a flat `eslint.config.js` (ESLint 9 format) importing the intended rule set; a separate ticket (introducing lint config isn't in M5.E3's surgical scope). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **this row was mis-statused as `dismissed`.** It is not a non-bug — it is a duplicate of **`B5`** (the entry it says is missing does exist, at the top of this table), and the bug it describes is **fixed**: `eslint.config.js` is present at the repo root and `npm run lint` exits clean on `tools/` (verified 2026-08-02). Kept rather than deleted so the ID does not dangle; treat `B5` as the canonical entry. |

| B21 | `fixed` | P3 | **`resolveDecisionId` rebuilds the whole D-ID map from disk on every call; the FR5 anchor gate calls it once per evicted ID.** `migrate-memory.js:2803-2804` loops evicted IDs and calls `resolveDecisionId` (→ `buildDecisionIdMap` walks `archive/` + reads every DECISIONS.md + regexes the corpus) per ID → O(N_ids × full-corpus-rebuild). Fine at Signal's scale (73 IDs × ~183 KB, one-time migrate) but drags on a project with thousands of decisions. **Fix (~5 LOC):** build the map once before the gate loop, resolve in-memory — safe *here* because eviction has already written to disk before the gate runs (map is stable across the loop); leave the mechanical-fresh `resolveDecisionId` for the standalone/self-heal callers. Deferred (Suggestion, M5.E3 REVIEW rev-code). |
| B22 | `fixed` | P3 | **`doc-hygiene.js` internal-link check touches disk outside the walk root.** `checkInternalLinks` resolves `](target)` to `resolve(dirname(f), pathPart)` (`:110`) then `existsSync` + (anchors) `readFile` (`:361`) — a `](../../../x.md#a)` target reads outside the repo. Read-only, offline (AC4.3 no-network independently verified), runs only in Signal's OWN test suite → no exfil, but an unbounded disk touch. **Fix (~2-3 LOC):** bound the resolved target to repo root before existsSync/readFile. Deferred (Suggestion, M5.E3 REVIEW rev-sec). |
| B23 | `fixed` | P3 | **M5.E3 REVIEW test-hardening follow-up (bundle).** Non-blocking coverage gaps the panel flagged: (a) the **organic re-root-to-missing-target** path — CHECK-ITEM 2 re-roots to a planted *existing* sibling; the dangling/rollback tests INJECT synthetic dangles, so "re-root produces a real dangling link → non-injected `enforceNoDangling` catches → abort+rollback" is never exercised (inert on Signal — 0 inline `](*.md)` in DECISIONS — but live for external repos). (b) the **>1 MB `FILE_SCAN_CEILING` truncation** branch (`doc-hygiene.js:102`, `migrate-memory.js:1482`) has no fixture crossing the 1 MB cap (largest is 200 KB) — also the B15 class. (c) **no rollback test asserts `git diff --cached` is empty** after a mid-phase abort — correctness holds structurally (staging is post-gate) but the "do NOT reorder" ordering (`migrate-memory.js:2263`) is comment-protected, not test-protected. (d) **`promoteDrainEntry` batch re-run:** verify the `/sig:plan` drain does NOT re-select already-`→ Promoted` terminal entries (once stamped, the block's sha1 no longer matches its dedupe marker, so a re-promote would duplicate; the selection lives in `commands/plan.md`, outside the E3 code diff). Plus 2 cosmetic nits: `backlogKey` → `blockKey` (used for both backlog + bugs markers); `promoteToBugs` dedupe-marker string-replace on rendered output is fragile if `buildBugsEntry`'s template changes. Confirmed 2026-07-19 (M5.E3 REVIEW panel). |
| B24 | `fixed` | P2 | **A pre-existing broken `](*.md)` link inside a strictly-closed-milestone DECISIONS section blocks `applyMigrate` — it aborts + rolls back byte-identical, violating the FR6.3 "a pre-existing dangle must NOT be attributed to the migrate" contract.** The append-log evict relocates the closed block to `archive/M{n}/` AND re-roots the still-broken link, so `computeDanglingDelta`'s `file\0link` key can't subtract it against the pre-apply baseline (both the file path and the link text changed) → it's flagged as a NEW dangle. **Fail-SAFE** (never a partial write) but **over-eager**: any external repo whose closed DECISIONS history carries ONE broken `](*.md)` link is blocked from migrating. Cross-check: `renderDryRun` lists this same dangle under "pre-existing dangling" (not the migrate's fault), yet apply aborts on it — a dry-run/apply divergence. **Discovered + reproduced 2026-07-20** (M5.E4.T4.1, `8fec7bf` — asserts current behavior in `migrate-dangling-baseline.test.js`, does NOT endorse it). Inert on Signal (0 inline `](*.md)` in DECISIONS.md). **Fix (deferred — out of M5.E4's known-bug scope):** make baseline-subtraction survive the block-move+reroot (key the delta on the decision ID / original link, not `file\0link`), OR exempt links inside evicted-and-rerooted closed blocks from the gate. Candidate for a v0.1.10 fast-follow. |
| B25 | `fixed` | P3 | **FR5 AC5.2 has no behavioral read-enclosure test — `rmw-lock.test.js`'s throw-under-held-lock tests pass even for a wrapper that reads OUTSIDE its lock** (a silent lost-update — the exact bug FR5 exists to kill). Read-enclosure IS correct (confirmed by inspection ×3 during REVIEW: all 6 wrappers are `withStateLock(baseDir, () => XxxCore(…))` with the read inside `Core`) and disclosed in `M5.E4-VERIFICATION.md`, so the *behavior* is right — this is a **test-strength** gap only. A true proof needs an interleaving test (writer A pauses after its read → B commits → A resumes → assert A fails-fast or incorporates B), which requires an injectable pause-hook in each Core (none today). Surfaced by the M5.E4 REVIEW test-integrity panel (2026-07-21). Deferred to v0.1.10. |
| B26 | `fixed` | P3 | **`shipFR1Check` returns `{skipped:true, 'not an Epic-close'}` for a FULLY-completed Epic on the Signal-on-Signal self-hosted flow — the flagship FR1 retro gate silently skips.** At M5.E4's SHIP (all 6 phases done, Epic complete), `shipFR1Check({state, profile, milestoneContent, baseDir})` returned `{halt:false, skipped:true, reason:'not an Epic-close SHIP…'}`, so the retro enforcement did NOT fire. The M5.E4 retro was written manually (correct), but the hard-block designed to prevent a missing retro didn't engage — the Epic-close detection likely relies on a `MILESTONE-{n}.md` close-signal the hand-managed flow (Epic-prefixed artifacts, hand-set `current_epic`) doesn't populate. **General risk:** a hand-managed / self-hosted project could close an Epic with no retro and no gate firing. Surfaced 2026-07-21 (M5.E4 SHIP). Needs triage: inspect the Epic-close predicate in `shipFR1Check` + how it reads the milestone file. |
| B27 | `fixed` | P3 | **An archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts `applyMigrate` — fail-safe but blocks a stranger repo's migrate; in tension with R7.** Surfaced 2026-07-21 during **M5.E5.T1** (B24) while building the AC1.3 gate-strength fixture. B24's resolved-target delta correctly treats a link that *resolved* before the migrate and *dangles* after (its target renamed by FR6) as migrate-introduced → abort + rollback. But R7 exempts archive **prose** flat-paths from the residual scan as "historical, not links to repair" — it does **not** exempt archive **inline** `](*.md)` links from the dangling gate. So an external repo whose archive doc inline-links a file FR6 later renames (e.g. `](../../FUTURE-IDEAS.md)` → `ISSUES-INBOX.md`) is blocked from migrating. Inert on Signal (0 such links). **Triage question:** should archive-doc inline links to renamed targets be a *flag* (matching R7's prose exemption) rather than an abort? Needs a design call — not M5.E5 scope. **→ Fixed in M5.E6 (T11/T12, `5ec8a81`+`61f4b67`):** migrate dangling-gate now FLAGS (not aborts) archive-inline links whose resolved target ∈ renameFroms — a tight AND (`isUnderArchive && archiveExemptFroms.has(abs)`), mirroring R7's archive-prose exemption; surfaced via a distinct dry-run line + apply warnings. Migrate-INTRODUCED dangles still abort+rollback (a tightness pair + a residual-abort discriminator prove the gate still bites). |
| B28 | `fixed` | P3 | **An absolute-path `](/abs/foo.md)` `.md` link that is a pre-existing dangle inside an evicted closed block false-aborts `applyMigrate` — the B27-sibling for absolute links.** `scanDanglingLinks` resolves the target with `resolve()` (absolute *wins* → `/abs/foo.md`, abs key `../abs/foo.md`) while `computeLinkEdits` reroots via `posix.join()` (absolute *concatenates* → the link is rewritten `/abs/foo.md` → `../../abs/foo.md`, abs key `.planning/abs/foo.md`). The before/after abs keys diverge → the pre-existing dangle is mis-attributed as migrate-introduced → abort + rollback. **Confirmed empirically 2026-07-21** (M5.E5 REVIEW code-quality panel H1 + a path-math probe). **Fail-SAFE** (blocks the migrate, never a silent escape) and **not a regression** (it false-aborted pre-B24 too; B24 fixed the *relative*-link case). Near-zero occurrence — Signal never authors absolute-path `.md` links; needs a hand-authored one as a pre-existing dangle in a closed DECISIONS block in a stranger repo. Deeper cause: `computeLinkEdits` mangles absolute links (rewriting `/abs/foo.md` to point at `.planning/abs/foo.md`) — a pre-existing reroot quirk, not B24. **Fix (deferred — arch ripple into the reroot machinery, out of M5.E5's surgical scope):** make `scanDanglingLinks`/`computeLinkEdits` agree on absolute targets (leave absolute links unrewritten — they resolve identically regardless of file location), OR exempt absolute-path links from the gate. Same disposition as B27. **→ Fixed in M5.E6 (T10, `cf352f9`):** `computeLinkEdits` now skips absolute-path (`/…`) targets (fix-divergence) — a pre-existing absolute-path dangle inside an evicted closed block migrates byte-identical instead of aborting. Distinct fix from B27 (both R7-consistent). |
| B29 | `fixed` | P3 | **The `_afterRead` FR5 test seam destructures `opts._afterRead` (resolves inherited props) — an unreachable prototype-pollution gadget; add an own-property/`typeof` guard (defense-in-depth).** The 6 RMW Cores (`drain.js:476,615,755`, `checkpoint.js:316`, `retro-index.js:235,372`) read `opts._afterRead` and, if truthy, `await` it inside the coarse state lock. **Currently unreachable** (M5.E5 REVIEW security audit, OWASP A08): 0 prod callers pass it (none in `commands/`/`hooks/`), and JSON/CLI input cannot carry a function — only a pre-existing `Object.prototype._afterRead` *function* pollution (which requires code-exec already) could trigger it; worst case is a `TypeError` inside a lock whose promise rejects (fail-safe, lock released) — not RCE, not lock-DoS. **Fix (deferred per the auditor — non-blocking hardening):** gate the seam on `Object.hasOwn(opts, '_afterRead') && typeof opts._afterRead === 'function'`, or key it on a `Symbol`. Surfaced 2026-07-21 (M5.E5 REVIEW). **→ Fixed in M5.E6 (T19, `78c433e`):** the `_afterRead` test seam is gated on `Object.hasOwn(opts,'_afterRead') && typeof …==='function'` at all 6 RMW sites (own-property, not a destructured local) — a prototype-polluted `_afterRead` no longer fires. RED-proven across all 6 paths; the B25 interleaving suite stays unchanged-green. |
| B30 | `fixed` | P2 | **The FR1 retro pre-check (`/sig:ship` Step 0.5) runs BEFORE the SHIP transition (Step 5), so on a fresh REVIEW→SHIP flow it sees `phase=REVIEW` and skips — B26's STATE-based Epic-close fallback (which requires `phase===SHIP` + REVIEW in `completed_phases`) only fires once phase is already SHIP.** Surfaced 2026-07-21 **dogfooding the B26 fix on M5.E5's own SHIP:** `shipFR1Check` returned `{skipped:true}` at `phase=REVIEW` (Step-0.5 pre-check), then `{halt:true, NO_RETRO_FILE}` after `transitionPhase('SHIP')` set `phase=SHIP` + appended REVIEW to `completed_phases`. So B26's fix is *correct* but its real-world firing depends on phase already being SHIP at the gate. A forgetful maintainer on a fresh flow gets no enforcement at the pre-check, and the programmatic `transitionPhase` at Step 5 bypasses the Layer-2 `PreToolUse` hook. **Fix candidates:** (a) `/sig:ship` transitions to SHIP *before* its own FR1 pre-check; (b) `isEpicCloseByState` also treats "current phase = the last non-skipped pre-SHIP phase + all earlier phases complete" as about-to-close; (c) re-run the pre-check post-transition. Residual of B26 — the fix works once phase=SHIP. **→ Fixed in M5.E6 (T15, `0b1ef86`):** `shipFR1Check` fires the retro gate on a fresh REVIEW→SHIP flow via an in-memory synthesized post-SHIP state (approach (c), persist-nothing) — an Epic can no longer ship with no retrospective. Tight 3-conjunct `aboutToClose` AND; no false-fire on SPIKE / maintained-pending / SKETCH-short / mid-flow-EXECUTE (each mutation-witnessed). (c)-recovery: nothing is persisted on halt, so `/sig:resume` re-orients cleanly. |

| B31 | `fixed` | P3 | **`/sig:add` and drain RMW the same `ISSUES-INBOX.md` under DIFFERENT locks (`.add.lock` vs `.state.lock`) → concurrent lost-update.** Found 2026-07-23 during **M5.E6 PLAN** (FR7 lock-status verification). `add.js:46` takes `.planning/.add.lock` (30s TTL, sized for the interactive capture prompt); drain's `promoteDrainEntry`/`evictTerminalToLedger`/`applyDispositionToFile` take `.planning/.state.lock`. Different mutexes = **no mutual exclusion**, so a concurrent `/sig:add` + `/sig:plan`(defer/delete) or `/sig:ship`(evict) can lost-update the inbox (data-loss of a captured idea; `atomicWrite` prevents torn writes, not this — recoverable via git, low probability, needs exact concurrent same-repo timing → P3). **NOT covered by M5.E4 FR5's read-enclosure locking** (that put a *single* lock per RMW path; this is a *two-lock mismatch* on one shared file). Sits **inside D-M5E6-3's exact threat model** (Brett runs parallel sessions). The separate `.add.lock` is deliberate — sharing `.state.lock` would let a slow interactive `/sig:add` block STATE writes for 30s — so the fix needs design (candidates: a dedicated short `.inbox.lock` both paths take for the *write step only*; or add's write-phase re-acquires `.state.lock` non-interactively after the prompt resolves). **Disposition = M5.E6 scope decision (see M5.E6 PLAN gate).** **→ Fixed in M5.E6 (T23, `bef3ba3`):** `/sig:add`'s doc-write now runs under `.state.lock` (re-read-inside-lock RMW Core) — mutually exclusive with drain/ship writes to the same files. Pure insert/build helpers stay lock-free (backlog/checkpoint call them already-inside `.state.lock` → no re-entrancy/deadlock); the scrub prompt stays outside under `.add.lock`. `add.test.js` byte-identical. |
| B32 | `confirmed` | P3 | **docs/map `COMMANDS` JS array incomplete.** The `docs/map/index.html` `COMMANDS` array (~line 710) is missing `/sig:index` + `/sig:migrate-memory` — 16 enumerated vs the 18-command header after M5.E6.T8. The array is NOT roster-guard-checked (only the header count + CLAUDE.md `#` count are), which is why the gap slipped through since v0.1.8 (was 15-vs-17). Predates M5.E6; surfaced during T8's roster reconcile. Fix: add the missing commands to the array, or bring the array under the roster guard so it can't drift again. Low-risk (visual map only). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live, and the gap has widened.** The `COMMANDS` array in `docs/map/index.html` now enumerates **17** commands against a **19**-command header and 19 files in `commands/`. The two missing are still exactly `/sig:index` and `/sig:migrate-memory` — the count moved 16-vs-18 → 17-vs-19 only because `/sig:update` was added to both. The array is still not roster-guard-checked, which is the actual defect. |
| B33 | `confirmed` | P3 | **Stale `LOCK_TTL_MS` comment in `add.js`.** The comment (~lines 42-45) says `.add.lock` "holds the lock across a sensitive-data prompt" — inaccurate even before M5.E6 (the scrub prompt runs BEFORE `.add.lock` acquisition), and now doubly so: B31 (T23) additionally put the doc-write under `.state.lock`. Pre-existing; surfaced during Slice 8. Fix: one-line comment refresh describing the current arrangement (scrub → `.add.lock` interactive TTL → `.state.lock` write). Comment-only, no behavior change. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** The comment above `LOCK_TTL_MS` in `tools/lib/add.js` still reads *"`/sig:add` holds the lock across a sensitive-data prompt"*, unchanged. Comment-only, no behaviour change needed. |
| B34 | `confirmed` | P3 | **`renameFn` injectable seam is the B29-parity prototype-pollution gap.** `drain.js` bare-destructures `renameFn` from `opts` at 3 cores (`applyDispositionToFileCore` :473, `evictTerminalToLedgerCore` :607, `promoteDrainEntryCore` :740) and forwards it to `atomicWrite` (`atomic-write.js:21/27`), which invokes it under the lock — the SAME shape B29 hardened for `_afterRead`, left unguarded on the literal sibling. **Unreachable** (0 prod callers pass `renameFn`; no in-process primitive can pollute `Object.prototype.renameFn` with a *function* — the YAML sink is proven pollution-safe), so same tier B29 was. Surfaced 2026-07-24 by the M5.E6 REVIEW security panel (the "one-seam-over" the evict.js false-green primed for). Fix: mirror B29 — `Object.hasOwn(opts,'renameFn') && typeof opts.renameFn==='function'` at the 3 destructures (root fix; guarding `atomic-write.js` alone fails — drain makes it an OWN prop first), RED-first with a pollution test mirroring B29's. Completes the pollution-class hardening. Not a SHIP blocker. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `applyDispositionToFileCore` still bare-destructures `renameFn` from `opts` (`tools/lib/drain.js:473`) and forwards it to `atomicWrite`, which invokes it under the lock (`tools/lib/atomic-write.js:21,27`) — the shape `B29` hardened for `_afterRead` and left unguarded on the literal sibling. Reachability is unchanged (still zero production callers). |
| B35 | `confirmed` | P3 | **`sweep.js checkIndexFreshness` unwrapped compose/diff can crash the read-only sweep.** Every other sweep check is crash-safe (try-catch→advisory), but the managed-index path (`enumeratePlanningDocs`/`renderPlanningIndex`, sweep.js:86-88) is unwrapped — a throw mid-walk crashes the whole sweep. **Low reachability** (only a Signal-managed, non-empty, non-foreign INDEX.md reaches it — stranger repos early-return at the foreign/absent gate, so the stranger-repo "never crash" contract HOLDS; the gap is a Signal repo with a pathological `.planning/`). Surfaced 2026-07-24 (M5.E6 REVIEW code-quality). Fix: wrap the compose-and-diff in try/catch → advisory (RED-first via a mocked-throw on `enumeratePlanningDocs`), making the never-crash guarantee total. Not a SHIP blocker. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `checkIndexFreshness` remains unwrapped on both ends: its `enumeratePlanningDocs` / `renderPlanningIndex` compose-and-diff has no try/catch (`tools/lib/sweep.js:93-95`), and `runSweep` calls it bare at `:404`. Every neighbouring check is crash-safe; this one is the exception. |
| B36 | `fixed` | P2 | **FR1 retro-gate (`/sig:ship` Step 0.5) silently skips on a stale/non-shipped milestone row — Layer-1 enforcement is structurally inert for a row-maintained project at ship-start.** `shipFR1Check` (retrospective.js:805-808) detects Epic-close via (A) `isEpicCloseShip` (row contains "shipped") OR (B/C) the B26/B30 STATE fallbacks — but both fallbacks are **row-absence-gated** (D-E9-5, "a maintained row wins"). At Step 0.5 the row is never yet "shipped" (shipping hasn't run), so (A) is always false for a row-maintained project; if the row EXISTS but is stale/in-progress (not absent), (B/C) don't fire either → skip. **Confirmed dogfooding M5.E6's own SHIP (2026-07-25):** the E6 row read `🔨 DISCUSS` (never advanced from Epic open), `shipFR1Check` returned `{skipped}` on a real FULL Epic-close with no retro — the Epic could have shipped retro-less if not caught by hand; the gate fired only after the row was marked `shipped`. **Third milestone-row residual after B26 (no row) + B30 (phase timing).** Fix candidates: (a) SHIP marks the row close/"shipping" BEFORE Step 0.5; (b) narrow the row-absence gate so a *non-terminal* row (pending/in-flight/pre-SHIP-phase status) still lets `isEpicCloseByState` win when STATE unambiguously shows close. Layer-2 (`PreToolUse`) + Layer-3 (resume) hooks don't read the row so are unaffected, but Layer 1 is the only cross-runtime layer (D-E9-8). Ironic residual on the Epic that fixed B30. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (S4.t1, FR3.1)** and never flipped. The stale-row skip is now **loud**: `shipFR1Check` returns `{skipped:true, unevaluated:true, cause:"stale-milestone-row"}` with a reason the command must emit verbatim (`tools/lib/retrospective.js:850-876`, `commands/ship.md:38`). Asserted at `tests/ship-fr1.test.js:591`. |
| B37 | `confirmed` | P2 | **`/sig:checkpoint --context` (and `resume.md`, `discuss.md`) instruct a single-home violation — D16's dual-write was never reconciled to the doc-runtime.** `commands/checkpoint.md:12` says *"dual-write decisions to `CONTEXT.md` § Locked Decisions AND `DECISIONS.md` (per D16)"*, and `:139` makes it a gate item ("D16 dual-write hit both"). `commands/resume.md` and `commands/discuss.md` carry the same `CONTEXT.md` § Locked Decisions instruction. **This directly contradicts the canonical doc model that M5.E1 established:** `references/doc-runtime-model.md:56` — *"Eviction destination — one rule: unit-homed single-home"* — and `:59`, *"no piece is copied into two places to keep in sync (a single-home violation)."* D16 predates the doc-runtime (M5.E1 FR1) and was never reconciled to it, so three commands instruct an agent to commit precisely the violation the doc-runtime exists to prevent. **Also collides with the Signal-on-Signal convention** that `CONTEXT.md` is the orientation doc and decisions live in `DECISIONS.md` — Signal's own `CONTEXT.md` has no § Locked Decisions section, so the instruction is not merely wrong-in-principle but unexecutable here without inventing one. **Confirmed 2026-07-25** during an M5.E7 `/sig:checkpoint --context`, which is why the checkpoint wrote decisions to `DECISIONS.md` only. **Fix:** amend all three commands to a single home (`DECISIONS.md`; `CONTEXT.md` gets a pointer at most), drop the `:139` gate line, and record a superseding decision against D16. Standalone commit — unrelated to M5.E7's audit scope. |
| B38 | `confirmed` | P2 | **Signal applies the prohibition/anti-rationalization-table form universally, but its own upstream source measured that form as counterproductive for output-*shaping* failures.** Verified at source 2026-07-25 (superpowers v6.2.0, `skills/writing-skills/SKILL.md` § "Match the Form to the Failure", ~:459-474). The source classifies four baseline failure types and pairs each with a right and a wrong form. **Prohibition + rationalization table is correct for discipline failures** (*"skips/violates a rule under pressure — knows better, does it anyway"*) — which most of Signal's tables are, so they are fine. **It is the WRONG form for shaping failures** (*"complies, but output has the wrong shape — bloated, buried verdict, restated spec"*), where the right form is a positive recipe/contract stating what the output IS. The source's evidence is empirical, not stylistic: *"in head-to-head wording tests … the prohibition arm produced clearly more of the unwanted content than the recipe arm (fully separated distributions), and trended worse than even the no-guidance control."* **Why this is Signal's problem:** `analysis/SIGNAL-INTEGRATION-RUNDOWN.md:186` dispositioned the table as *"Adopt as Signal convention — every phase gate uses this format"* *(source corrected 2026-07-26 during M5.E7 S4.t10 — this row originally cited `REPO-ANALYSIS.md`; the claim is not there. Grepped, not assumed.)*, and Signal took the generalization — every `commands/*.md` anti-rationalization table plus `references/anti-rationalization.md`. At least one entry is a shaping rule wearing the prohibition form: `commands/resume.md`'s *"Render the full PROJECT.md / CONTEXT.md verbatim" → "Summarize"*. **Two further source rules Signal has nowhere:** (a) *no nuance clauses* — "don't X unless it matters" reopens the negotiation, and appending one degraded a winning recipe from consistent to noisy; (b) *exemption clauses don't scope* — "this limit doesn't apply to code blocks" still suppresses code blocks. **Fix (not a removal):** classify every table entry across `commands/` + `references/` as discipline vs. shaping; keep the discipline ones, convert the shaping ones to positive recipes. Surfaced by M5.E7 S2.t4 supply verification; the falsified April claim is also carried as correction **C8** in `M5.E7-RESEARCH.md` §0b *(added there 2026-07-26 — it was cited before it was written)* and is a roadmap `build` candidate with this as its first slice. |

| B39 | `fixed` | **P2** | **The trigger watchlist is never walked — `/sig:plan` was never taught the drain step the standing entry instructs.** `ISSUES-INBOX.md:1407` is a **standing entry** (*"never promote, merge, or delete"*), ratified BR-6 2026-07-04, whose whole text is an instruction to a command: *"At each `/sig:plan` drain, walk the conditions below and act on any that have fired."* **Verified 2026-07-26 by reading the code, not inferred:** `grep -ril "watchlist" commands/ tools/` returns **nothing**. `commands/plan.md` mentions `drain` 11 times and the watchlist zero times; `tools/lib/drain.js` has no concept of it. **The mechanism was specified and never implemented.** Consequences already visible in the ledger: (a) the watchlist's own `Fired?` column reads `—` on **all 11 rows**, including *GitHub Issues adoption*, whose trigger `BACKLOG.md:187` separately records as **fired 2026-07-15** — two homes for one fact, disagreeing, which is also a single-home violation of the same class as **B37**; (b) at least two `/sig:plan` drains have run since that trigger fired (M5.E6, M5.E7) and the item is still parked with **no record of a decision either way**; (c) the entry's own stated rationale was *"10+ parked entries carry promote-back conditions that **nothing evaluated**"* — the fix for that problem has itself never evaluated anything. **Root cause of the M5.E7 finding** that three parked BACKLOG bullets sit behind gates that cannot fire for the demand landing on them (`M5.E7-DEMAND-REGISTER.md` §1). **The dated synthesizer trigger did not lapse only because Brett called it by hand during M5.E7.** Fix: implement the walk as a real drain step in `commands/plan.md` + `tools/lib/drain.js`, and make it **record-producing** — a checked-and-declined trigger must be distinguishable from an unchecked one, or the next audit re-finds this. Surfaced during M5.E7 S1.t3/S4.t10 review. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (FR2.1)** and never flipped. `/sig:plan` now carries `#### Walk the trigger watchlist` (`commands/plan.md:99`) backed by `parseTriggerWatchlist` (`tools/lib/drain.js:791`); the second half — a *checked-and-declined* row staying visibly distinct from an unchecked one — is asserted at `tests/trigger-watchlist.test.js:86`, and `:109` asserts the walk is actually wired into the command rather than merely documented. |
| B40 | `confirmed` | P3 | **Citation integrity in `analysis/` is unenforceable by construction — the authoring convention routes around the one guard that could check it.** The standing hygiene guard walks `analysis/` and runs `checkInternalLinks`, but Signal's docs deliberately write cross-directory references as **inline code** (`` `.planning/M5.E7-RESEARCH.md` ``) rather than markdown links, precisely so they cannot trip it — a convention S1.t2 states outright (`M5.E7-DEMAND-LEDGERS.md` §0) and S4.t10 repeated. **Live instance, caught by review and not by the suite (2026-07-26):** M5.E7's C8 correction markers in `analysis/` cited *"Evidence: `.planning/M5.E7-RESEARCH.md` §0"* while **C8 had never been written into that section**; `BUGS.md:66` (B38) carried the same false citation. Fixed at the content level (C8 is now a §0b row) — **the mechanism gap is what this entry tracks.** Note the guard could not have caught it either way: `checkInternalLinks` tests **file existence**, and this failure was *file exists, cited section does not contain the claim* — a **claim-level** integrity problem, not a link-level one. **Therefore do not "fix" this by converting refs to markdown links** — that buys a file-existence check against a section-content failure and re-exposes every ref to false-fails from quoted link syntax (the documented reason for the `.planning/` exclusion at `tools/lib/doc-hygiene.js:91-93`). Fold instead into the `analysis/` staleness recommendation (`M5.E7-CORRECTIONS.md` §2): the `verified-against` stamp check should also assert that a cited `{file} §{section}` actually contains the ID it claims. **Interim discipline, in force now:** grep the cited section for the ID before writing any marker that cites it. |
| B41 | `fixed` | **P2** | **Four of the seven phase commands never advance `phase`, so `completed_phases` is a ledger of phases that were never recorded — and `markFresh` stamps it fresh, suppressing the one banner that would show it.** `transitionPhase` is called by **exactly three** commands — `calibrate.md`, `discuss.md`, `ship.md` (grep over `commands/*.md`, 2026-07-26). `plan.md`, `execute.md`, `verify.md`, `review.md` **never call it, and instruct no other `phase` write.** *(Precision, because the scope matters when this is fixed: `execute.md` is not state-blind — its auto-state-protocol wraps every dispatch in `dispatchTaskWithState` → `setCurrentTask` / `clearCurrentTask`, so `current_tasks` and `last_completed_task` do move. **It is `phase` and `completed_phases` specifically that no middle command touches**, and `execute.md` calls neither `markFresh` nor `transitionPhase`.)* Consequence for a **command-driven** project: `/sig:discuss` sets `phase: DISCUSS`; nothing changes it again until `/sig:ship` calls `transitionPhase('SHIP')`, which appends **whatever `state.phase` still says** — so the run ends with `completed_phases: [DISCUSS]`, `phase: SHIP`, and **PLAN / EXECUTE / VERIFY / REVIEW never recorded at all**, while `/sig:status` and `/sig:resume` report `DISCUSS` for the entire build. **The symptom is already written into the code as an unsatisfiable precondition:** `plan.md:50` says *"`STATE.md` — verify current phase is PLAN"*, and **no command can ever make that true.** **The aggravator is the real sting:** `plan.md:149`, `verify.md:72`, `review.md` Step 5b all call `markFresh`, whose stated purpose is *"so the staleness banner in `/sig:resume` reads as fresh after {PHASE} closes"* — so each one **stamps a fresh timestamp over a stale position**, converting *stale-and-flagged* into *stale-and-silent*. **Live instance (2026-07-26):** after M5.E7's REVIEW `markFresh`, STATE read `phase: VERIFY` with a fresh stamp and no banner would have fired; caught by hand at REVIEW exit, not by anything. **Masked on Signal-on-Signal** because this repo's `phase`/`completed_phases` are maintained by hand — which is why it survived 11 releases. *Note what class this is: a **deterministic, file-shaped** instance of the Epic's own headline finding — a state ledger nothing checks. It needs no measurement layer to fix, so it is M5.E9 work, not M5.E8.* **Fix shape:** a `transitionPhase(baseDir, '{PHASE}')` at each phase command's entry (not exit — entry is what makes `plan.md:50`'s precondition meaningful), or one shared preamble step. Pairs with **B36** (the other structurally-inert enforcement at ship-start). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (FR6)** and never flipped. All four middle phase commands now carry a `## Phase entry — record the phase` section (`commands/plan.md:42`, `execute.md:42`, `verify.md:36`, `review.md:47`), and `tests/phase-recording.test.js` asserts each one. M5.E13 (`B48`) later made the instruction conditional on the phase having an artifact; that refines the fix, it does not undo it. |

| B42 | `fixed` | **P1** | **A linear-mode project cannot run `/sig:ship` at all — the FR1 retro gate hard-halts on `current_epic: null`, and there is no bypass.** `shipFR1Check` (`retrospective.js:751-762`) returns `{halt:true, code:'NO_CURRENT_EPIC'}` whenever `!state.current_epic`, *before* any Epic-close/per-slice reasoning. `ship.md:27` runs that check **"before any other Workflow step, regardless of `gate_strictness`"** and `ship.md:41` states **"No bypass"** — no flag, no env var, no extra-args trick. `ship.md:34` compounds it: Step 0.5's milestone-file derivation parses `state.current_epic` and halts if the derived `MILESTONE-{n}.md` is missing, which is also unsatisfiable with no Epic. **Linear mode is first-class everywhere else** — `new-project.md:35` (*"Without `--epic`, the project starts in linear mode"*), and the identical Epic-mode/linear-mode profile+artifact-naming preamble in `discuss.md:13`, `plan.md:13`, `execute.md:13`, `verify.md:13`, `review.md:13`, **`ship.md:13` itself**, plus `status.md:115` / `resume.md:143`. So six phase commands support a mode the seventh refuses to run. **Not an untested gap — the halt is asserted:** `tests/ship-fr1.test.js:125` (*"halts with NO_CURRENT_EPIC when state.current_epic is empty (AC1-extended)"*) and `:138`; `M4.5.E9-VERIFICATION.md` records AC1-extended as PASS. Fixing this means **rescoping an AC**, not just editing a branch: the M4.5.E9 author read empty `current_epic` as *"state is broken"*, not *"this project has no Epics."* **Landed `3d41159` (2026-05-26, M4.5.E9.S1.t6), shipped in v0.1.3 (2026-05-31) — live across nine releases (v0.1.3 → v0.1.11), invisible because Signal-on-Signal has run Epic-mode since M4.5.E11.** **Reported live 2026-07-26** from `nextpass`, which has run linear (`current_epic` always null) its whole life: FR1 halted `/sig:ship` on step zero; the close was completed by hand. **Fix shape — and it is a rescope, not a three-liner. Three separate gates each assume an Epic, and fixing only the first leaves ship still blocked:** (1) `shipFR1Check`'s early halt (`retrospective.js:755`) → return `{halt:false, skipped:true, reason:'linear mode'}`; (2) **`ship.md:34`, which runs *before* it** — Step 0.5 derives the milestone file by parsing `state.current_epic` and halts on its own when the derived `MILESTONE-{n}.md` is missing, so a linear project halts one step earlier regardless; (3) `isEpicCloseByState` (`retrospective.js:412`) returns `false` on `!state.current_epic`, so the entire B26/B30 Epic-close machinery — `aboutToClose`, the synthesized post-SHIP state, the row-absence fallback — is **dead code in linear mode**. Decide what the close unit even *is* for a linear project before touching any of them (`resume.js:129` already says *"in linear mode the retro is milestone-scoped"* — the intent existed and no gate implemented it). **Sharpest evidence the halt was never a deliberate reading:** the layers disagree on the same condition. Layer 1 fails **closed** (hard halt, no bypass); **Layer 2 fails open** — `checkProposedStateWrite` returns `{block:false}` on a null/absent `current_epic` (`retrospective.js:500-502`). One missing Epic, a hard block in one layer and a silent pass in the other. **P1 rather than P2:** this is not degraded behavior, it is a documented mode blocked end-to-end with the escape hatch explicitly designed out. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (D-M5E9-1)** and never flipped — all three gates, which is what the fix shape above demanded. (1) `shipFR1Check` returns `{halt:false, skipped:true, reason:"linear mode …"}` on `detectMode(state) === "linear"` (`tools/lib/retrospective.js:764-790`). (2) `ship.md:34` Step 0.5 is now **Epic mode only** and passes `milestoneContent: null` in linear mode, naming this entry as its reason. (3) `isEpicCloseByState` returning `false` in linear mode is now correct by scope, not dead code — the gate is Epic-only by ratified decision. Asserted end-to-end at `tests/linear-mode-e2e.test.js:93`. |

| B43 | `fixed` | **P2** | **`transitionPhase` records the phase you are *leaving*, so a SHIP date can never be written by it — SHIP is terminal, and `ship.md:96` documents the opposite.** `state.js:390-391` appends **`state.phase`** (the phase being left) with today's date, then sets `phase` to the argument. `ship.md:96` claims `transitionPhase(baseDir,'SHIP')` *"appends `SHIP (YYYY-MM-DD)` to `completed_phases`"* — it appends `REVIEW (date)`. **The consequence is structural, not cosmetic** (sharpened by Brett 2026-07-26, and the reason this is P2 not P3): `SHIP` enters `completed_phases` only on a transition *away from* SHIP, and there is none — so **no correct SHIP date can ever be recorded by this function**, in any mode, Epic or linear. This repo's own STATE is the proof: `completed_phases: [… REVIEW]`, `phase: SHIP`. **A second failure mode rides on it:** the appended entry is stamped with **today's** date but names the phase being left, which may have closed days earlier — so the date is wrong for the phase it labels. Live instance: `transitionPhase(base,'SHIP')` called at `phase: PLAN` stamped `PLAN (2026-07-26)` and recorded no SHIP at all — *"even without the collapse (B44), the call would have silently failed to record the thing it was called to record."* **Not covered by B41**, which is about the middle commands never calling `transitionPhase` at all — though B41 is what let `phase` still read `PLAN` at ship time. **The codebase already knows this and says so — in the other direction from the docs:** `retrospective.js:493` comments the Epic-close detector as keying on pre-SHIP coverage, *"**NOT** a `- SHIP` entry (Signal never writes one)."* So the gate was built around the fact that SHIP is unrecordable while `ship.md:96` tells the reader the opposite. **Fix:** append `nextPhase` (or both, `"left X / entered Y"`), and carry each phase's real close date rather than the write date. Then correct `ship.md:96` — **and re-check `isEpicCloseByState`/`checkProposedStateWrite`, which are written against today's never-writes-SHIP behavior** and would need the `coversPreShip` test revisited if SHIP starts appearing in the list. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (FR2)** and never flipped. `completePhase` (`tools/lib/state.js:655`) records a phase complete *without* transitioning away from it, which is what a terminal SHIP needs; `ship.md:98` was corrected in the same Epic and now describes `transitionPhase` as appending the phase being **left**. Asserted at `tests/linear-mode-e2e.test.js:178`. |

| B44 | `fixed` | **P2** | **`transitionPhase`'s dedupe assumes a ≤7-entry per-Epic ledger — nothing enforces that invariant, so the first programmatic call on a long linear-mode history silently collapses it.** `state.js:388-395` keys `completed_phases` by phase name in a `Map` (most-recent wins) — **specified**, not accidental: the docstring says *"dedupes by phase name … so recovery re-transitions don't accumulate duplicates"* and `references/state-schema.md:128` documents *"Dedupe by phase name on re-transition."* The dedupe is correct **for the scope it was designed against** — an Epic, where the list holds at most one entry per phase and B9's `setCurrentEpic` reset clears it on every roll. **Linear mode has no such reset** (B9's fix fires only on an Epic roll), so `completed_phases` is the project's whole multi-slice history — and the collapse is unbounded, silent, and unwarned. **Live instance 2026-07-26 (`nextpass`):** a hand-maintained 53-entry history was rewritten to 8 by one `transitionPhase` call; caught before commit, by hand. **The two preconditions are both Signal's:** the ≤7 invariant is undocumented and unasserted, and the history could only have been hand-built in the first place **because of B41** (the middle commands never call `transitionPhase`, so a linear project maintains the field manually). Fix B41 alone and this gets *worse*, not better — every newly-wired phase command becomes another collapse site. **The precise framing (Brett, 2026-07-26): set semantics applied to log data.** The `Map` is keyed on the first whitespace token and JS `Map` is last-write-wins per key, so the operation is only coherent if `completed_phases` means *"which phases has this project ever reached."* Used as an append-only chronological log — which is what a multi-slice project's STATE is, and what `STATE-HISTORY.md` treats it as — **every prior slice's history is destroyed on the first call.** *Scope correction: this is not linear-mode-only. **Any** project past its first unit hits it; Epic mode is spared only because `setCurrentEpic` (`state.js:614-616`, the B9 fix) deliberately zeroes `completed_phases` on every roll, so the list never gets long enough to notice. Linear mode has no roll and therefore no reset.* **What made it dangerous rather than merely wrong is the silence:** `writeStateFrontmatter` (`state.js:478-483`) re-serializes the whole frontmatter atomically — no diff, no warning, no dropped-entry count. Caught in `nextpass` only because a `git diff` was run before committing a file expected to carry two edits. **Fix shape (decide, don't assume):** if it's a log, **drop the `Map` entirely** — a one-line fix; if it is genuinely a set, then **entries must not carry dates**, because a date on a last-write-wins entry looks like *"when this happened"* and means *"most recent write,"* which is exactly how it misled here. Either way **write the invariant down in `references/state-schema.md:128` and assert it** — that line currently specifies the dedupe without ever stating the ≤7 scope it depends on. Sequence **before or with B41** (same state-mutation core, M5.E9): fixing B41 first turns four more commands into collapse sites. **Writer audit (done 2026-07-26, answering "is this class waiting in the other frontmatter writers?" — no):** every other mutator spreads the parsed state via `stripStateMeta` and **patches named fields** — `markFresh` two scalars (`state.js:1384-1389`), `setCurrentTask` a pure append (`:516`), `clearCurrentTask` a filter on an explicit id, `addBlocker`/`clearBlocker`/`touchDecisionTimestamp` likewise. `transitionPhase` is the **only** writer that rebuilds an array by derivation. Two residual notes, neither a truncation: `setCurrentEpic` wipes `completed_phases` by design (B9) with no archival step, and because every write round-trips the frontmatter through the YAML serializer, **comments and hand formatting inside the frontmatter block are dropped by any writer** (data keys survive). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9 (D-M5E9-5)** and never flipped. The set-vs-log question was decided in favour of **log**: `transitionPhase` is now append-only with no dedupe (`tools/lib/state.js:425`), so a long linear-mode history can no longer be collapsed by a single call. `references/state-schema.md` was updated with it. Asserted in `tests/state.test.js`, `tests/phase-recording.test.js` and `tests/adherence-append-only.test.js`. |

| B45 | `fixed` | **P2** | **`transitionPhase` validates its argument but never validates existing entries, so any junk line in `completed_phases` becomes a permanent phantom phase.** `state.js:377-381` rejects a `nextPhase` outside `PHASES` — the only validation in the function. Existing entries go straight into `phaseNameOf = (entry) => entry.split(' ')[0]` (`:388`) with no `PHASES` check, so **whatever the first whitespace token happens to be becomes a `Map` key**, indistinguishable from a real phase. Live instance (`nextpass`, 2026-07-26): a pre-existing prose line `**▶ Active: Slice SEC1 …` keyed on `**▶`, survived the dedupe as an eighth "phase," and **will survive every future dedupe** — the collapse that destroys real history preserves the junk, because a phantom key has no competitor to overwrite it. **Independent of B44's set-vs-log question:** fix the dedupe either way and an unvalidated entry still round-trips forever; the two just interact badly (B44 makes the list lossy, B45 makes the loss selectively favor garbage). **Why it can happen at all:** `completed_phases` is hand-maintained in exactly the projects this bites (B41 — no command writes it mid-flow), and the v0.1.6 write-guard (B8) polices *prose in frontmatter* at the hook layer, which `state.js`'s own `node:fs` writers bypass by design (`v0.1.6-RESEARCH.md` R1.5). So the one guard that would have caught this shape cannot see this writer. **Fix:** validate entries against `PHASES` on read and **quarantine** malformed lines rather than keying on them — surface them (count + first offender) instead of silently promoting them to phases. Pairs with B44; same function, same commit. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E9** and never flipped. `partitionCompletedPhases` (`tools/lib/state.js:385`) splits `completed_phases` into well-formed and malformed entries and the malformed ones are **quarantined and returned rather than keyed on** (`:366`, `:446-447`) — so a junk line can no longer become a phantom phase. `/sig:sweep` names this entry when it reports the repairable case (`tools/lib/sweep.js:319`). |

| B46 | `confirmed` | P2 | **M5.E7 dispositioned 45 inbox candidates into a side file and never stamped the inbox, so every one of them resurfaces on every future `/sig:plan` drain — including the ones verbed `abandon`.** `M5.E7-DISPOSITIONS.md` carries the verbs (**76 table rows**, 41 KB); `ISSUES-INBOX.md` contains **zero** `→ Promoted` / `→ Deferred` / `→ Shipped` stamps (grep, 2026-07-27). `listDrainCandidatesWithRecovery` keys *"already dispositioned"* off those inline stamps, so it returns **47 candidates** — a wall that will re-present itself identically at every future PLAN, forever, with no memory that 45 of them were already decided. **Surfaced live at M5.E9 PLAN Step 1b**, which is what a drain step is *for*: the step worked; the prior Epic's write-back did not. **`plan.md`'s drain contract is not at fault** — it specifies destination-first-then-stamp via `promoteDrainEntry` / `applyDispositionToFile` and describes exactly that crash-safe ordering. **M5.E7 never ran it**, because the audit's disposition table was authored as an *analysis artifact* rather than driven through the drain, and nothing checks that the two agree. **Note the class — this Epic's own headline finding, one level up:** a decision recorded where nothing reads it back. Same shape as **B39** (a watchlist written down and never walked) and **B41** (a phase ledger nothing checks). **Why it was not fixed in-flight:** stamping *"Deferred (M5.E9 drain)"* across 47 entries would write the **wrong** verb onto entries already dispositioned `build` or `abandon` — satisfying a checklist by corrupting the idea database. **Fix shape:** a reconciliation pass that reads `M5.E7-DISPOSITIONS.md` and stamps each entry with **its own recorded verb** (never a blanket defer), then evicts the terminal ones to the ledger — plus a hygiene assertion that a dispositions artifact and the inbox cannot disagree. **Homed in M5.E9 as a later slice; explicitly NOT v0.1.12.** |

| B47 | `confirmed` | P3 | **`/sig:resume` reads `0/7 phases done` immediately after a linear-mode ship.** M5.E9's FR5 trim clears `completed_phases` at ship (relocating the run to `STATE-HISTORY.md`), so the first thing a user sees after shipping is `Phase: SHIP (0/7 phases done)` — at exactly the moment they want confirmation something *completed*. Defensible on a literal reading (the **next** run has zero done) and it is cosmetic, not data loss; the archived run is intact and verified zero-loss. **Cataloged at M5.E9 REVIEW (2026-07-27) as I1, deliberately not fixed in-phase:** the honest fix is for the resume banner to render the archived run's count, which means teaching `resume.js` to read `STATE-HISTORY.md` — **a new capability, not a defect fix**, and Slice 2 was already the Epic's largest unit. Recorded rather than absorbed so the next reader does not have to rediscover it. **Fix shape:** `renderResumeBriefing` reads the most recent `phase-log:archived` section when the live list is empty and `phase: SHIP`, and renders e.g. `SHIP (last run: 7/7 done, archived)`. |

| B48 | `fixed` | P2 | **`execute.md`'s phase-entry instruction is unconditional, and an agent refused it on correctness grounds — writing a false record is the alternative.** M5.E9's FR6 tells the agent to call `transitionPhase(baseDir, 'EXECUTE')` **"before any Workflow step"**, with no precondition clause. Observed live during M5.E8's first adherence run (2026-07-28): against a fixture with no PLAN artifact, `/sig:execute` halted at its preconditions and the agent **explicitly declined** the phase-entry write, reasoning that calling it *"would have written `phase: EXECUTE` for a project with nothing to execute, and appended `PLAN` to `completed_phases` when PLAN produced no artifact — a false record in the exact log that `resume.js` and `isEpicCloseByState` read as ground truth."* **The agent is right, and the instruction as written is wrong.** `recordPhase` (`state.js:429`) appends unconditionally with no evidence check, so obeying the instruction literally on a halted phase corrupts the ledger M5.E9 had just finished making honest — while disobeying it reproduces `B41`, the very defect FR6 was written to fix. **The instruction and the phase-log integrity rules are in direct conflict, and nothing in the corpus resolves it.** **Not found by the harness's verdict** — that run scored `ABSENT`, which is a fact about the fixture. It was found by reading the transcript, which is why transcript retention is now unconditional. **Fix shape:** state the precondition explicitly — transition at entry *once the phase's preconditions hold*, and if the command halts before doing any work, do not record the phase as left. Applies to all four commands M5.E9 changed (`plan`, `execute`, `verify`, `review`), not just `execute.md`. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13 (FR1.2)** and never flipped — in the text *and* the code beneath it. The phase-entry instruction is now conditional in all four commands (see the `**Why the condition (M5.E13, `B48`).**` paragraph at `commands/execute.md:48` and its three siblings), and `transitionPhase` refuses to record an artifact-less phase. The originating EXECUTE case is asserted by name at `tests/phase-recording.test.js:201`. |

| B49 | `fixed` (v0.1.13) | P3 | **`package.json` says `0.1.11`; `.claude-plugin/plugin.json` says `0.1.12`.** The plugin manifest is the version of record for what a user installs, and it is correct; `package.json` was not bumped at the v0.1.12 release cut. Low impact today — nothing in the runtime reads `package.json.version`, and the marketplace install path uses the manifest — but the two files are the project's two public statements of its own version, and they disagree. **Cataloged from M5.E8 VERIFY (2026-07-28), deliberately not fixed there:** a version bump inside a measurement Epic is scope creep, and the release that fixes it should be the one that sets it. **FIXED in v0.1.13 (2026-07-28)** — bumped at the release cut, exactly as the fix shape specified. **And the fix shape's second half was wrong in an informative way:** `checkVersionConsistency` **already exists and already works** — it fired immediately on `.claude-plugin/marketplace.json` during this very release, catching a **third** version file the bump had missed. What it does not cover is `package.json`, which is precisely why `B49` survived undetected while the suite stayed green. **Remaining open work, re-scoped:** extend `checkVersionConsistency` to include `package.json`. The guard is not missing; its **scope** is. |
| B50 | `confirmed` | **P2** | **Completeness claims are written from the shape of the work rather than from the artifact — and Signal contains zero mechanisms that check any claim against its source.** The **class**, given a ledger identity 2026-07-28 so future retros count sightings instead of rediscovering it (`ISSUES-INBOX.md` § "Claim integrity"). The author composes a coverage/status/completeness statement ("0 gaps", "all verified", "checklist complete") from their mental model of what just happened instead of re-deriving it from the thing it describes; **the false statement is always plausible, which is what makes it dangerous.** Distinct from the class M5.E8 named (*a guard written, shipped, never called* — `B39`/`B46`/`I2`), and **the two compose badly: one writes the false claim, the other ensures the check that would catch it never runs.** **Structural causes, verified by reading the code:** `commands/verify.md` names `REQUIREMENTS.md` **zero times** (its coverage loop enumerates PLAN tasks, so a requirement dropped at PLAN is invisible to VERIFY forever); the verifier agents that *do* have the enumerate-with-a-denominator shape (`agents/verifiers/verifier.md`, `nyquist-auditor.md`) are dispatched by **no command**; `review.md` is designated the faithfulness backstop in two places (`ship.md` §5.5, `evict.js` header) and implements it nowhere; every deterministic validator checks structure, not claims — **exactly two claim-vs-reality checks exist in all of Signal** (`checkRosterCounts`, `adherence-ceiling.js --check`). **The M5.E8 harness cannot see this class by construction** — its traces are "a named function was called" / "a named file was written", never what the file *says*, so a VERIFY report omitting a requirement still scores as a PASS trace. **Instances: 7 prior in Signal's corpus + 5 in one traction-engine phase, plus two found while cataloguing it** — M5.E8's own retro proposed a mechanism (`--check`-has-a-caller) that covers **1 of its 3** motivating instances (`--check` appears in exactly one file in `tools/`), and this entry's own inbox capture was filed with a status line reading "Logged" and **no body**. Full evidence, causes, and eight ranked fixes: [`../analysis/CLAIM-INTEGRITY-ANALYSIS.md`](../analysis/CLAIM-INTEGRITY-ANALYSIS.md) (single home). **Homed at M5.E10** (§6 items 1–3, 5–8); the retro-index freshness check (item 8) taken by **M5.E13** per its "whichever ships first" clause. |
| B51 | `fixed` | **P2** | **`discuss.md` still instructs the DISCUSS close to set `phase: PLAN`, which makes `/sig:plan`'s at-entry transition record PLAN as complete before PLAN has run.** `commands/discuss.md:158-161` ends with *"Update `STATE.md`: `## Current Phase` / `PLAN`"* — the pre-M5.E9 convention, where the **outgoing** command advanced the phase. M5.E9's `B41` fix moved that responsibility to the **incoming** command: `commands/plan.md:44` now says *"Before any Workflow step, call `await transitionPhase(baseDir, 'PLAN')` … It appends the phase being **left** to `completed_phases`."* **Both instructions survive, and obeying both writes a false entry.** If DISCUSS sets `phase: PLAN`, then at `/sig:plan` entry `leaving` resolves to **PLAN**, so `recordPhase` appends `PLAN (date)` — recording PLAN complete **before it did any work**. `recordPhase` is **deliberately append-only with no dedupe** (`state.js:425-427`, D-M5E9-5 / `B44` — a genuine re-transition did happen twice), so the false entry is **permanent** and later sits beside the real `PLAN (date)` appended when EXECUTE transitions. **Same family as `B48`:** an instruction whose literal execution corrupts the ledger M5.E9 built to make honest, while ignoring it is the only correct move. **Found by dogfooding — M5.E13's own DISCUSS, 2026-07-28.** The instruction was not obeyed; `phase` was left at `DISCUSS` so `/sig:plan` records the transition itself, which is the post-`B41` design. **Scope check: `discuss.md` is the sibling case of the four commands `B48` covers** — M5.E9 updated the four *incoming* commands and left the *outgoing* instruction in the fifth. `calibrate.md` and `ship.md` also called `transitionPhase` pre-M5.E9 and need the same read. **Folded into M5.E13 FR1** rather than opened as separate work: one instruction family, one fix. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13** and never flipped. `commands/discuss.md:158` now reads **"Do NOT set `phase: PLAN` here"** — the incoming command advances the phase, not the outgoing one. Pinned by `tests/commands-wording.test.js:61`, which asserts no command sets a phase the next command also sets. |
| B52 | `confirmed` | **P1** | **A Claude Code session binds to ONE plugin-cache version for its whole life, so Signal-on-Signal can dogfood a release two versions behind the repo — and it silently destroyed M5.E8's phase ledger.** Observed live 2026-07-28 at M5.E13 PLAN entry. `.claude-plugin/plugin.json`, `package.json` and `marketplace.json` all read **0.1.13** (pinned to `8d20193`), a **0.1.13 cache exists and is correct** (`cache/signal/sig/0.1.13/commands/plan.md` has both `transitionPhase` refs), **yet the session loaded every `/sig:` command and every `tools/lib` module from `cache/signal/sig/0.1.11/`** — two releases behind. **The damage:** `setCurrentEpic` rolling `M5.E8 → M5.E13` ran **0.1.11's `state.js`**, which has **zero** references to `archivePhaseLog`/`partitionCompletedPhases` (repo has 6) because it predates M5.E9. The Epic-roll therefore reset `completed_phases` **without the archive-before-reset step M5.E9 added specifically to prevent this** — M5.E8's six-phase DISCUSS→SHIP run was **discarded with no error, no warning, and no record**. Proven by the sibling case sitting beside it: `.planning/archive/M5/E9/STATE-NARRATIVE.md` **exists** (written by the real tool on 2026-07-27, when M5.E8 opened under v0.1.12 code), while `.planning/archive/M5/E8/` **did not exist at all**. **Recovered** verbatim from `git show aff4098:.planning/STATE.md` into `archive/M5/E8/STATE-NARRATIVE.md`, zero entries lost — recoverable **only** because `.planning/` is tracked in git. **Why this outranks the bug it reproduced:** `B44` was *"a silent write that drops data"* and M5.E9 fixed it; this is that same loss re-entering through the **install layer**, where no amount of correct repo code prevents it. **It also corrupts dogfooding evidence in both directions** — a fixed bug can appear live (the stale cache still has it) and a live bug can appear fixed. Every `/sig:` command run in this session, including the `/sig:discuss` that opened M5.E13, was **v0.1.11 text**. **Fix shape (two halves):** (1) a session-start version check comparing the resolved plugin-cache path against `.claude-plugin/plugin.json`, warning loudly on mismatch — cheap, deterministic, and the half that matters; (2) decide whether `setCurrentEpic` should refuse to reset a non-empty `completed_phases` it did not archive, which would have made this loud instead of silent regardless of version. **Note the shape:** the guard that would have caught it existed and was correct — it just wasn't the code that ran. That is the `B39`/`B46`/`I2` class arriving by a new route, which is why it belongs in **M5.E13**. **ROOT CAUSE CONFIRMED 2026-07-28 — a mid-session auto-update race, not a broken install.** `installed_plugins.json` is **correct** (`installPath: …/sig/0.1.13`, `version: 0.1.13`) and `known_marketplaces.json` has **`autoUpdate: true`**. The plugin's `lastUpdated` is **`2026-07-28T13:38:41Z`**; this session had already started (its SessionStart hook reported STATE as of `13:37:23`). **Claude Code resolves the plugin path once at session start and holds it for the session's life**, so a session that began ~78 seconds before the auto-update kept **0.1.11** while every config file on disk correctly read 0.1.13. **Reproduced by Brett in a second, unrelated repo the same day** — it is **per-session, not per-project**: any session alive across an auto-update keeps the version it started with. **Not Signal-specific in mechanism** (it affects any Claude Code plugin) **but severe for Signal specifically**, because Signal's tools mutate project state, so a stale binding does not merely behave oddly — it writes with retired code paths. **Immediate remedy: restart the CLI process. A context clear (`/clear`) is very likely NOT sufficient — and the distinction matters, because "restart the session" is ambiguous between exactly those two things.** *Reasoned, not verified:* plugin resolution builds the command/skill registry, which is **process-level** state; `/clear` resets conversation context without restarting the process, so the resolved paths should survive it. A marketplace refresh that re-fetches without rebuilding the in-process registry should likewise not help. **Decisive 10-second test, because every `/sig:` command prints its own version in its text** (this session's `/sig:resume` opened by citing `…/sig/0.1.11/tools/lib/profile.js`): run `/clear`, then `/sig:status`, and read the cited path — `0.1.13` means a clear re-resolves, `0.1.11` means only a full process restart does. **Nothing in config needs repair either way.** **→ SECOND LIVE SIGHTING 2026-08-02, and this one delivered a retired instruction rather than destroying data.** Caught at the top of `/sig:discuss --epic M5.E18`: the skill text the session loaded cites `…/cache/signal/sig/**0.1.13**/skills/define/…`, while `installed_plugins.json` records `installPath: …/sig/**0.1.15**`, `version: 0.1.15`, `lastUpdated: 2026-08-01T18:13:18Z`. **Config on disk is correct and the session is still two releases behind it.** **CORRECTION, same day, before anyone acted on it:** this row first said the timing *"differs from the originating case"* and that the mechanism was *"not yet explained."* **Both were wrong, and wrong the same way — I read the session transcript's creation time as the process's start time.** `ps` says otherwise: the CLI process serving this session (**pid 86260**, cwd `…/dev-biz/signal`) started **Tue Jul 28 17:29**, when v0.1.13 *was* the current release. It has been alive **five days**. The session id was minted Aug 2 12:50 by a `/clear` **inside that same process**. So this is the originating mechanism exactly — *a session alive across an auto-update keeps the version it started with* — with a **four-day** window instead of 78 seconds. **And it converts this row's own open hypothesis into a measurement.** The row said, marked *reasoned, not verified*: *"A context clear is very likely NOT sufficient … `/clear` resets conversation context without restarting the process, so the resolved paths should survive it."* **Verified now:** a `/clear` ran at 12:50, the process kept its 0.1.13 binding, and the very next `/sig:` command cited `…/sig/0.1.13/`. Delete the *reasoned-not-verified* caveat — restart is required, clear is not enough, observed. **The generalisation that matters more than either:** a long-lived process is the common case, not the edge one. `ps` at the same moment found **five** live CLI processes — `elicitation-engine` (Jul 25), `mps` (Jul 25), `traction-engine` (Jul 28), `signal` (Jul 28), `nextpass` (Aug 2) — so **four of five were running machinery 5 to 8 days old**, against real projects, with nothing anywhere saying so. The originating incident read as a freak 78-second race; it is not. It is the default state of any editor left open over a weekend. **The damage this time:** v0.1.13's `discuss.md` still carries the exact block `B51` deleted — *"Update `STATE.md`: `## Current Phase` / `PLAN`"* — so an agent following the text it was handed would have written the permanent false `PLAN (date)` entry `B51` exists to prevent (`completed_phases` is append-only with no dedupe, `D-M5E9-5`, so it cannot be undone by a later correct run). Diffed live: cached 174 lines vs repo 170; the four extra lines *are* the retired instruction. **So the row's prediction — "a fixed bug can appear live" — is now observed, and it is worse than stated: the stale cache does not merely fail to deliver a fix, it actively re-issues the instruction the fix removed.** **Also measured:** there is **no `0.1.16` cache at all** (`0.1.5, 0.1.6, 0.1.8–0.1.11, 0.1.13, 0.1.15`), so even a correctly-bound session runs one release behind `main` until the marketplace re-fetches — expected, given `source: "."` means users track `main` and the cache refreshes on update, but it means **Signal-on-Signal always dogfoods older machinery than it just merged.** **Consequence for the Epic:** `M5.E18` was NOT opened in this session — no `setCurrentEpic`, no `transitionPhase`, no artifacts written. Running a Signal-on-Signal Epic on retired command text corrupts exactly the dogfooding evidence the Epic exists to gather. **This strengthens fix half (1)** — a session-start check comparing the resolved cache path against the install record — from *cheap and deterministic* to *the only thing that would have caught either sighting*; both were found by a human noticing a version string in passing.

  **⚠ Related hazard, unobserved — flagged as a risk, not a finding.** `~/.claude/plugins/.last_inuse_sweep` (last run `2026-07-28T00:41:11Z`) indicates a periodic prune of cached plugin versions, and the cache currently holds **seven** (`0.1.5`, `0.1.6`, `0.1.8`, `0.1.9`, `0.1.10`, `0.1.11`, `0.1.13`). If that sweep removes a version it considers unused **while a session is still bound to it**, that session loses its command surface mid-flight — and a Signal session losing `tools/lib` mid-Epic is worse than one running stale code, because partial writes become possible. `0.1.11` is intact today (18 commands present), so **this has not happened and is not claimed to have happened.** It is the same shape as the binding bug itself: **a live reference that nothing else knows is live.** Worth confirming the sweep's in-use detection accounts for open sessions before treating the multi-version cache as harmless. **Separate observation, uncertain significance:** that same record carries `gitCommitSha: a73d550`, which is *"M4.t19: marketplace install layout fix + plugin slug signal→sig (v0.1.0)"* — a **v0.1.0-era commit**, unchanged since `installedAt: 2026-05-14` while `lastUpdated` has moved across thirteen releases. Whether that field is meant to track the release or the original install is **not established**; flagged rather than claimed, since a third disagreeing version statement is exactly `B49`'s shape and should not be asserted from a guess. |
| B53 | `fixed` | **P2** | **A non-strict `current_epic` splits artifact WRITE naming from READ resolution, so a phase command can write `1-PLAN.md` while the next command reads a stale `PHASE11-PLAN.md` — and `references/epic-native-flow.md` states the opposite as a guarantee.** Two seams read the same field with **different regexes**: `artifactName` (`resume.js`) uses the **strict** shape and fails open to *linear* naming (documented at `epic-native-flow.md:44`), while `resolveArtifactPath` (`resume.js:30`) uses the **lenient** `EPIC_ID_RE = /^[A-Za-z0-9._-]+$/` and places the Epic-prefixed candidate **first** in its candidate list — so an existing `{value}-{ARTIFACT}.md` **shadows** a freshly written `1-{ARTIFACT}.md`, which is checked only afterward. **`epic-native-flow.md:31` claims they cannot diverge:** *"They are symmetric — whatever `artifactName` emits, `resolveArtifactPath` with the same opts resolves back."* **True for a strict Epic ID; false for every non-strict value.** **Verified by execution** against the installed v0.1.13 with `traction-engine`'s real STATE value: `artifactName('PLAN', {currentEpic:'PHASE11'})` → **`1-PLAN.md`**; `resolveArtifactPath(…, 'PLAN', {currentEpic:'PHASE11'})` → **`PHASE11-PLAN.md`**. With `M5.E9` both agree (`M5.E9-PLAN.md`). **Live field case:** `traction-engine` carries `current_epic: PHASE11` — `detectMode` → **linear**, `setCurrentEpic` would have **rejected** it (it re-validates the strict shape), so it entered by a direct STATE write, the one path nothing validates. Its artifacts are all named `PHASE11-*.md`, so **reads work today only because the lenient regex matches**; the first `/sig:` command to *write* an artifact forks the naming and the write is then invisible to the next read. **The divergence is unobservable:** `detectMode` is named in exactly **one** command file (`ship.md`) — `/sig:status` and `/sig:resume` never print the detected mode, so a project reads as Epic to a human and linear to Signal with nothing saying so. **Secondary hazard, inferred from the instruction text and NOT measured** (it is agent-executed prose — M5.E8's exact subject): `ship.md` §0.5 derives `MILESTONE-{n}.md` from `current_epic`; `traction-engine` has **no milestone file of any name** (verified), so an agent that eyeballs `PHASE11` as "Epic mode" rather than computing `detectMode` halts one step *before* `shipFR1Check` runs. **Origin — and it is a `B50` sighting.** Found while checking a `traction-engine` session's claim that *"`shipFR1Check` halts with `NO_CURRENT_EPIC` on this project, which has never used Epics."* **That claim is false, on both the installed version and the one before it:** `PHASE11` is **truthy**, so the pre-`B42` `!state.current_epic` branch never fired either — executed, v0.1.13 → `{halt:false, skipped:true, reason:'linear mode …'}` and v0.1.11 → `{halt:false, skipped:true, reason:'not an Epic-close SHIP …'}`. The claim was composed from the *shape* of the situation ("no Epics → `NO_CURRENT_EPIC`") rather than from the artifact — **`B50`'s class, sighted in a downstream project rather than in Signal's own docs.** **Fix shape, three parts:** (1) a **`/sig:sweep` check for a set-but-non-strict `current_epic`**, naming what is therefore inactive — **shippable where M5.E9's removed phase-log check was not, and the distinction is the whole argument: the evidence here is *in the file* and mechanically testable against `EPIC_ID_STRICT_RE`, so it cannot false-positive on a healthy project**, whereas a past history-collapse was undetectable because the bug destroyed its own evidence; (2) **surface the detected mode** in `/sig:status` and `/sig:resume`; (3) **decide whether the two regexes should agree** — a design call, not a typo: the lenient one is load-bearing for existing non-strict projects' *reads*, so tightening it silently breaks them. **P2, not P1:** no data loss observed and reads currently resolve — but this is a silent stale-read path, and the same *"two layers, one field, opposite verdicts"* shape `B42` was closed on. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): the defect is fixed in **M5.E13 (S1.t1)** and was never flipped. The write/read split is closed **by construction**: `resolveArtifactPath`'s first candidate *is* `artifactName(artifact, {currentEpic})` (Pattern W, `tools/lib/resume.js:64`), so neither seam guesses at the other's Epic-ID regex — `references/epic-native-flow.md:33` was rewritten to claim symmetry by construction rather than by convention, and `tests/resolve-artifact.test.js:166` pins it. Fix-shape part (1) shipped separately as M5.E16's sweep check `(h)` `epic-id-not-strict` (`tools/lib/state-drift.js:617`). **Residual, deliberately left open:** fix-shape part (2) — *surface the detected mode in `/sig:status` and `/sig:resume`* — is **not** implemented; `detectMode` is still called by no renderer (only referenced in a comment at `tools/lib/resume.js:146`), so a non-strict project still reads as Epic mode to a human and linear to Signal with nothing saying so. Tracked as `B68`. |
| B54 | `fixed` | **P2** | **`checkGateArtifacts` is the never-called-guard class's FOURTH instance — and the only one that is not merely inert but *wrong if wired up*.** `state.js:616` exports a phase-gate artifact-existence checker. **Nothing calls it.** An exhaustive grep across `*.md`/`*.js`/`*.json` (minus `node_modules/`, `.git/`) returns exactly five hits: the definition; four test references (`tests/state.test.js:6,169,172,187`); and **three prose hits that describe it as part of the flow** — `CONTEXT.md:170` lists it in `state.js`'s API, and `STATE-HISTORY.md:222` + `archive/milestones/MILESTONE-3.md:79` both record a **fix once applied to it** (*"`checkGateArtifacts('.', 'PLAN')` now correctly finds PROJECT.md"*) — a repair made to a guard nothing runs. No command file, no hook, no tool invokes it. **The part that makes it worse than `B39`/`B46`/`I2`:** those three are absent, so wiring them up is neutral; this one **fails on Signal itself**. Executed at M5.E13 PLAN against this repo: `checkGateArtifacts(signal, 'PLAN')` → **`{ready:false, missing:['REQUIREMENTS.md']}`**. Its `PLAN` list is the hardcoded, unprefixed `['PROJECT.md','CONTEXT.md','REQUIREMENTS.md']`, while Epic mode writes `M5.E13-REQUIREMENTS.md` and no bare `REQUIREMENTS.md` exists — **so the obvious remediation (switch it on) would block PLAN for every Epic-mode project.** This is the same mode-dependent-name false seam `B53` proves and FR1.2's revision warns about. **Additionally, four of its five gates are vacuous:** `EXECUTE`/`VERIFY`/`REVIEW`/`SHIP` are empty arrays behind the comment `// Dynamically checked based on phase number` — dynamics never implemented — so all four return `{ready:true}` unconditionally. **Root cause of the invisibility, and the reason this is worth an ID rather than a footnote: being uncalled is what protected its bug from discovery.** A dormant guard is not neutral — it rots, and its documentation rots with it, so each dead guard is potentially two defects rather than one. **Found at M5.E13 PLAN** while reading `state.js` for FR1.2, which proposed building an artifact-existence refusal *in the same file where this unwired one already sat*. **Disposition (ratified 2026-07-29, Brett): REPLACE** — delete `checkGateArtifacts` and build the refusal fresh in `transitionPhase`, following M5.E9's precedent of removing a wrong check rather than re-tuning it (the `/sig:sweep` phase-log check). Repair was rejected as near-equal work plus rename risk; leave-in-place was rejected as shipping a new guard beside a dead one in the Epic named for dead guards. **Corrects D-M5E13-3's locked count of three instances to four** — which is itself the decision's own lesson (it was written to correct M5.E8's retro for under-counting the class from a single instance's shape). **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): fixed in **M5.E13** by **deletion** and never flipped. `checkGateArtifacts` is gone from `tools/` — asserted, not assumed: `tests/guard-callers.test.js:164` fails if the name reappears in live code (historical records under `archive/` legitimately still name it). |
| B55 | `confirmed` | **P2** | **The adherence canary's control arm is not isolated across files, so `deleteSection` never removed the instruction from the agent's reach — it only removed it from one file.** `adherence-run.js` calls `createPluginCopy(ROOT)` (whole plugin) and mutates **exactly one** command file (`adherence-run.js:374` — `join(plugin.root, 'commands', ${canary.command}.md)`). But `transitionPhase` is named **4×** in `plan.md`, **4×** in `verify.md`, **4×** in `review.md`, plus `calibrate.md`, `discuss.md`, `ship.md` and `references/state-schema.md`. An agent running `/sig:execute` in the control arm that opens any sibling command file finds the same instruction. **The `$deletionNote` in `adherence-canaries.json` records that the control was already fixed once for this exact class** — the first version deleted one line and left the section restating the instruction three more times, a near-miss `INERT`. The fix was widened from line to **section**. It should have been widened to **corpus**; it stopped one scope short. **Found 2026-07-29 by the first real use of the harness (M5.E13 S5.t1 / FR1.3).** Re-running `B41-phase-entry` against the reworded instruction returned **treatment 3/3, control 1/3 → `INDETERMINATE`**, replacing M5.E8's `OBEYED` (3/3 vs 0/3). The control run that leaked is the whole finding. **M5.E13 S2.t4 amplified it, by design and unavoidably:** FR1.1 required a **single shared wording** across all four middle commands precisely so they cannot drift, which means the control-arm agent now finds a **byte-identical** copy of the deleted instruction in `plan.md`. The requirement and the measurement are in direct tension, and nothing said so. **What this does and does not say about M5.E8's verdict:** the earlier `OBEYED` is **not falsified** — 0/3 means no leak was observed in those three runs — but it was **unisolated**, so it could never have distinguished *"the instruction works"* from *"the agent found it elsewhere."* The flagship verdict rested on a control arm that was clean by luck rather than by construction. **This is a fifth instance of M5.E8's own retro sentence** — *every defect this Epic found was in the measuring instrument, and every one produced a plausible-looking result rather than an error.* `3/3 vs 0/3 → OBEYED` is exactly such a result. **Fix shape (not attempted here):** delete the instruction corpus-wide across the copied plugin, not per-file — which requires the canary to declare a corpus-level pattern rather than a single `deleteSection` anchor, and requires deciding what a verdict means when an instruction legitimately appears in several commands. **Do NOT re-run for a cleaner number until the arm is fixed** — a second run is a coin-flip, and taking the better of two is the tuning M5.E8's impostor table exists to forbid. |
| B56 | `confirmed` | P3 | **`references/facts.md` publishes a test count that is wrong by 912, and the cross-file guard over that exact file cannot see it — it pins two documents to each other, never to the thing they describe.** `references/facts.md:22` reads `**Test count:** 894`; the suite is at **1806**. `tests/cross-file-consistency.test.js` is the guard: it extracts `TEST_COUNT` via `extractFact('Test count')`, asserts `expect(TEST_COUNT).toBeTruthy()` (:46), and asserts that any doc mentioning "N tests" **matches facts.md** (:65 — `` `${doc} mentions "${hit} tests" but facts.md says ${TEST_COUNT}` ``). **It never compares facts.md's number to the actual test count.** So facts.md and README.md agree with each other, drift together from reality, and the suite stays green — a published, currently-wrong number with a passing guard over it. Its own file header calls facts.md the "source-of-truth," which is the assumption that makes the gap invisible: a source of truth is only true if something checks it against the source. **Found 2026-07-31 at M5.E17 PLAN**, incidentally, while establishing the Epic's test baseline — *not* by any mechanism, which is the M5.E13 retro sentence again. **Same class as `B39`/`B48`** (a guard that does not guard) with a data shape rather than a document shape, and it is a **fourth** location for it. **Not folded into M5.E17** — it traces to none of FR1–FR4 and absorbing it would be the scope creep the Epic's "Not doing" section forbids. **Fix shape:** have the test derive the real count (collect the vitest totals, or count `it(`/`test(` across `tests/`) and assert facts.md matches, so the guard compares the document to the world rather than to its own echo. → homed at **M5.E16** (the data-shaped detector); a one-line correction of the 894 → 1806 string is *not* the fix and would leave the guard exactly as blind. |
| B57 | `fixed` | P2 | **`/sig:sweep` walks the migrate snapshot backup and reports a frozen directory as broken live docs — 92% noise on a real project.** `/sig:migrate-memory` writes a pre-reorg snapshot to `.planning/.migrate/snapshot/` before relocating anything; its links point at **pre-migration paths by design**. `WALK_IGNORE` (`tools/lib/doc-hygiene.js:40`) already exempts `archive/` for exactly this reason (AC1.2) and simply never listed `.migrate`. **Found 2026-08-01 by the first run of `/sig:sweep` against real non-Signal projects** (traction-engine, nextpass, conversor, cm-mentor-coach) — the FR1-first-use discipline shipped in v0.1.15, applied the day after it shipped. nextpass reported **12 structural findings; 11 were snapshot links**. **This is the precision failure M5.E16's FR2 exists to prevent, present in the tool today:** a report that is 92% noise gets muted within a week, and a muted checker looks like coverage while providing none — the same reasoning that deleted M5.E9's REVIEW Critical rather than re-tuning it. **Fixed 2026-08-01 (`.migrate` added to `WALK_IGNORE`, RED-first):** nextpass 12 structural → **2, both real** (a stale INDEX and a genuine link escaping the repo root into `~/.codex/`). Control test asserts a dead link in *live* `.planning/` is still flagged, so the exemption is scoped, not a mute. |
| B58 | `fixed` | **P1** | **`marketplace.json`'s pinned `sha` sat two releases behind its `ref`, so every `/plugin` install since v0.1.14 silently delivered v0.1.13.** The entry read `ref: "v0.1.15"` with `sha: "8d20193…"` — and `8d20193` is **v0.1.13's release commit**. Claude Code resolves the **sha**, so the `ref` was decorative. **Found 2026-08-01 by the user**, who ran `/plugin` → update and was told *"sig is already at the latest version (0.1.13)"* — two days after v0.1.14 shipped and twenty minutes after v0.1.15 did. **The report was truthful; the manifest was lying to it.** **Impact: every fix in v0.1.14 (M5.E13, guards that don't guard) and v0.1.15 (M5.E17) was unreachable.** The user was running v0.1.13 machinery against live projects while reporting that Signal 'constantly' needed teeny manual fixes — an unknown share of that experience is bugs already fixed in releases that could not reach him. **Why the guard missed it, and it is this session's own theme:** `install-contract.test.js` asserted the sha's **shape** (`/^[a-f0-9]{40}$/` — a stale sha passes) and separately asserted `ref === v{plugin.version}` (correct, and **not the field being resolved**). **Neither compared the two fields to each other**, so `ref` advanced every release while `sha` sat still and the suite stayed green — the same class as `B56` (a guard checking the property adjacent to the one that matters). **`B7` recorded this exact drift at v0.1.7** (*"marketplace.json pins sha 8e05e56… but v0.1.7 resolves to 0eb8ca8… — needs a look, not the test cause"*) and it was never closed. **The note was the guard**, which is `B39`'s shape. **Fixed 2026-08-01:** sha → `8acd1d2` (v0.1.15), plus a test that resolves `source.ref` via `git rev-list -n1` and asserts the sha equals it. **Proof-of-fail recorded:** the new test fails against the exact manifest that shipped for two releases. It **throws rather than skips** when tags are unresolvable — a guard protecting release delivery must never pass by being unable to run. **→ Superseded same day by DELETION (the better fix).** The `source` block is now the relative `.` form — the plugin **is** this repo — so `url`/`ref`/`sha` are gone and **there is no second place to record which commit ships**. Signal was the only marketplace on the machine using a pinned remote source; `prose`, `cloudflare`, `openai-codex` and `anthropics/claude-code` all use a relative path. The v0.1.1 fix that introduced `url` was choosing against the **`github` shorthand** (which resolved to SSH and broke stranger installs) — the relative form was never on the table. The resolve-the-tag test was removed with it: it guarded a shape that no longer exists. What replaced it guards the **shape, not the value** — `source` must be the string `.`, and no 40-char sha or `ref` key may reappear anywhere in the file, so the fragile form cannot come back. **Trade accepted knowingly:** users now track `main` rather than a pinned tag, which is safe because CI gates every push and Epic work moves to branches (`D-M5E17-4`). |
| B59 | `fixed` | P2 | **`M5.E16-PROFILE.md` carried two values outside their enums, so the Epic that declared FEATURE ran DISCUSS at the project's FULL — and nothing anywhere validated it.** `calibration.stakes: moderate` (valid only for `reversibility`) and `calibration.reversibility: easy` (valid nowhere) both fail `readProfileFromPath`. `readEffectiveProfile(baseDir, {currentEpic: 'M5.E16'})` therefore **throws**, and `resume.md` §4 fail-opens to the project PROFILE — so `/sig:resume` reported tier **FULL** for an Epic whose file says FEATURE, and the Epic's whole point of overriding `research_parallelism: 0` / `plan_validation_dims: core` / `gate_strictness: light` was inert. **Found 2026-08-01 at the `/sig:plan` tier-gating preamble**, the first time any code read the file. Written by hand in `18741a8` — the *same commit* as M5.E16's DISCUSS — and unread for the entire phase. **Both values are plain-English words a person writes without opening the schema**, which is why prose review would not have caught it and a parse does instantly. **Validation short-circuits on the first bad field**, so fixing what the error message named would have fixed one of two and left the file still unloadable. **Why nothing caught it:** Epic-scoped profiles are `created_by: hand`, no test walked `.planning/*PROFILE.md`, and no sweep or doc-hygiene check reads PROFILE at all (`grep PROFILE tools/lib/sweep.js tools/lib/doc-hygiene.js` → 0 hits). **Fixed 2026-08-01 (fix lane):** `stakes: minor`, `reversibility: trivial` — matching the file's own prose (*"no security boundary… a `git revert` away from undone"*) and its closest sibling `M5.E17-PROFILE.md` (`stakes: minor`). **Pinned by `tests/own-profiles-parse.test.js`:** every `.planning/*PROFILE.md` must load, *and* each Epic profile must be demonstrably **reached** rather than silently falling back to the project profile — because a profile that parses but is never selected is the same invisible failure one step over. **Proof-of-fail recorded:** the test goes red against the exact file that shipped through DISCUSS. |
| B60 | `confirmed` | P2 | **All six phase commands handle only the "PROFILE.md not found" error and have no branch for a malformed one — the four meta commands do.** `discuss.md`, `plan.md`, `execute.md`, `verify.md`, `review.md`, `ship.md` each carry one identical line: *"If neither PROFILE.md is present: `readEffectiveProfile` throws the same not-found error — halt with …"*. `readEffectiveProfile` also throws `ProfileSchemaError` for a **malformed** profile (bad tier, bad `schema_version`, any calibration or rigor-override violation), with a schema message, not a not-found one — and those six files give no handler, so an agent following them literally hits an unhandled throw at the preamble. `status.md` (§"any other `ProfileSchemaError`"), `init.md`, `add.md` and `resume.md` all branch on it explicitly. **6 silent / 4 explicit — and the split runs exactly the wrong way:** the commands that handle it are the read-only meta commands; the ones that don't are the six that consume the tier to set rigor. **Surfaced 2026-08-01 by `B59`**, which is the malformed case arriving for real. **This is `D-M5E17-2`'s shape one layer over** — that audit found `markFresh` ordering stated in 2 command files, silent in 2, wrong in 1, and fixed all five together. **Fix:** decide the correct behavior once (halt with the schema message? fail open to the project PROFILE as `resume.md` does? the two are *not* interchangeable — a briefing may degrade, a phase that sets rigor arguably may not), then state it in all ten command files rather than the four that thought of it. Pin with a test comparing the files to each other, as M5.E17 did. Not fixed with `B59`: `B59` was one file's data, this is ten files' instructions and needs a decision, not an edit. **→ Status reconciled 2026-08-02** (fix lane, BUGS.md status-vs-code sweep): **triaged — still live.** `ProfileSchemaError` is named in exactly four command files — `add.md`, `status.md`, `init.md`, `resume.md` — and in **none** of the seven phase commands. The asymmetry the entry describes is unchanged. |
| B61 | `confirmed` | P3 | **A hand-edited `last_updated_commit` whose short sha looks numeric is silently type-coerced by the YAML reader.** `0012345` parses to the integer `12345` (leading zeros stripped), `1e23456` to `Infinity`, `0e12345` to `0` — so the stored baseline becomes a *different value* than the one written, and every consumer comparing it to git history (`isStateStale`, `checkBaselineCommitOffHistory`) is comparing against a sha that was never a commit. Roughly **2–3% of short shas** match a YAML number form (all-digits is ~3.7% on its own, plus the `<digits>e<digits>` float form, since `e` is a valid hex character). **Signal's own writers are SAFE** — the `yaml` package quotes numeric-looking strings on `stringify`, verified: `stringify({last_updated_commit: '0012345'})` emits `"0012345"`. **The exposure is a hand-edited or externally-generated STATE.md**, which is not hypothetical: Signal's own `.planning/STATE.md` has been hand-maintained for months, and `/sig:migrate-memory` exists precisely because real projects arrive with hand-written state. **Found 2026-08-02** in the M5.E16 REVIEW loop, as a ~1-in-10 flake in `state-drift-checks-general.test.js` — the test fixture hand-wrote the sha unquoted, reproducing the exposure exactly. **The fixture was fixed** (quote the value); the product gap is filed here rather than fixed, because the honest fix is a *reader-side* coercion guard (`readState` should reject or re-stringify a non-string `last_updated_commit`) and that touches the core state reader every command depends on. **Current behaviour is fail-safe, not silent-wrong:** a coerced value fails `checkBaselineCommitOffHistory`'s `/^[0-9a-f]{7,40}$/` applicability test and reports **not-applicable with a reason** rather than a false clean — which is the M5.E16 contract working. `isStateStale`'s behaviour on a coerced value is **not yet characterised** and is the reason this is `confirmed` rather than `dismissed`. |
| B62 | `confirmed` | P2 | **`ship.md` §9's `markFresh` records a sha that the squash merge then destroys — so every release closes with `last_updated_commit` pointing at a commit no branch reaches.** Two instructions, each correct alone, that contradict when combined. **§9** (M5.E17 `FR3`) says *create the SHIP commit, **then** `markFresh`* — precisely so the recorded sha reflects the release rather than predating it. **`D-M5E17-4`/`D-M5E17-5`** (written **later**, 2026-08-01) moved Epic work onto a branch merged via **`gh pr merge --squash`**. A squash merge **discards the branch commits** and writes a new one, so the sha §9 just recorded is orphaned the moment the PR merges. **Confirmed live on v0.1.16's own ship, 2026-08-02:** `markFresh` stamped `e7416c9`, the squash produced `8d94b76`, and `git merge-base --is-ancestor e7416c9 HEAD` failed — `isStateStale` reported the release commit as unreflected work, and **M5.E16's own new check `(d)` fired on the release that shipped it**. **Not data loss and it self-heals** — the next STATE write re-stamps a reachable sha (exercised in production immediately after the merge: `findings` → `clean`), which is exactly the category-1 contract `(d)` declares. **But every release will trip its own detector until the ordering is stated.** **Fix direction:** §9 cannot stamp a sha that survives its own merge, so the freshness stamp belongs in a **follow-up write against the merged HEAD** — which is what the repo has been doing by hand (`chore: markFresh STATE after … merge`) without any instruction saying to. **`ship.md` should say it**, or `markFresh` should refuse a commit that is not reachable from the default branch. **This is the M5.E17 class recurring in the file M5.E17 fixed**, caused by a decision made *after* that fix and never re-checked against it. |
| B63 | `confirmed` | P2 | **`/sig:migrate-memory`'s dry-run prints `0` for its two Epic-only vectors on a linear project, so "could not apply" is byte-identical to "already clean."** `renderDryRun` pushes `vector-3 (closed-Epic evicts): ${v3.evicts.length}` and `archive-tree moves: ${archive.moves.length}` unconditionally (`migrate-memory.js:1931-1932`). On a project that does not use Epic IDs both are **structurally** zero, not incidentally: `planArchiveMoves` filters its input through `EPIC_ID_STRICT_RE` and only matches `.planning/{epicId}-{suffix}.md` (`archive-tree.js:96-110`), and `extractEpicSection` rejects any non-strict ID (`evict.js:175`). If nothing else fires, `noop` goes true (`:1907`) and the whole run reports as a no-op — **a linear project is told the migration is complete when half of it never applied in principle.** **This is `C1`'s exact class in the command next door**, live in the same week M5.E16 shipped the four-status model that fixes it for `/sig:sweep`: a `0` that means *could not look* rendered the same as a `0` that means *checked and clean*. **Found 2026-08-02** reviewing a nextpass session report; confirmed by reading the renderer, not by running it. **Fix shape:** apply M5.E16's applicability distinction here — a linear project should read *"not applicable: no Epic-ID'd units in this project"*, never `0`. The missing **capability** (an archive path for linear units) is a separate, Epic-sized item and is filed in `BACKLOG.md`, not here; this entry is only the false-clean report. |
| B64 | `confirmed` | P2 | **`senseArchiveTree` treats a retrospective file's existence as proof that an Epic is closed, and discards the `isStub` flag it already computed.** `closedEpicIds = retros.map((r) => r.epicId)` (`archive-tree.js:356-358`) — unfiltered. `enumerateRetros` returns a record for **any readable file matching `*-RETROSPECTIVE.md`** (`retro-index.js:46-82`) and sets `isStub` on it (`:74`, true when the content carries a `[FILL IN]` marker). That flag is consumed by the retro-index renderer (`:197`), by `doc-hygiene.js:410`, and by `/sig:resume`'s summary (`commands/resume.md:89`) — **never by the archiver.** So a placeholder retro is sufficient to archive a live Epic, and `B36` records retros being written *before* the gate ran, so an early retro is not hypothetical. **Bounded, not data-loss:** relocate-never-delete holds and a pre-apply git tag exists, so the failure is a wrong move, not a destroyed file. **Independently reported from nextpass 2026-08-02**, whose own archiver has this gap one layer over and whose report names it exactly — *"that's a label, not a guard — the planner still can't tell closed from live."* Theirs keys on slice-filename shape; Signal keys on a **different filename's** shape. A better proxy, still a proxy. **Fix shape:** drop `isStub` retros from `closedEpicIds` and state the exclusion in the dry-run — this uses a signal the code already produces and adds no new inference. |
| B65 | `confirmed` | P3 | **The migrate's fs-backup snapshot is never expired, aged, or reported — and `B57`'s ignore is what made it invisible.** `applyMigrate` writes `.planning/.migrate/snapshot/` plus a `*` gitignore beside it in `fs-backup` mode or `dirty && force` (`migrate-memory.js:2406-2412`). **`B57` (fixed 2026-08-01) then added `.migrate` to `WALK_IGNORE`** (`doc-hygiene.js:40-50`) — correct for noise, and measured: nextpass went 12 structural findings → 2, both real. **The consequence is that nothing looks at the snapshot at all.** Nothing checks its age, its size, or whether it can still roll back; the `*` gitignore means git cannot surface it either. nextpass carried **34 pre-migration doc copies** from the 2026-07-20 v2→v3 migration with roughly a dozen shipped slices on top of them — a recovery pointer that could no longer recover, invisible **by construction** one day after the ignore shipped. **This is the precision/coverage trade M5.E16's FR2 exists to manage, resolved in one direction without the other half being built.** **Fix shape:** an **advisory** sweep finding naming `.migrate/snapshot/`, its file count and its age. Deliberately advisory — a fresh snapshot is correct and must not read as a defect. |
| B66 | `confirmed` | P3 | **`checkStaleInbox` fires at `n > 0` with no magnitude, so a 110-entry inbox produces the same one-line advisory as a 1-entry inbox.** `sweep.js:188-192` emits `inbox has ${n} undrained entries — consider draining` for any non-zero count, with no threshold, no tiering, and no escalation. **The concrete cost, observed 2026-08-02:** nextpass removed a contradictory second index and deliberately filed a **tripwire** into `ISSUES-INBOX.md` — recording what was removed and the symptoms that would point back — specifically so it would surface at the next `/sig:plan` drain. It became **entry #110** in a pile that Signal describes with the sentence it used at entry 1. **Their mechanism was sound; Signal is what muted it.** Not a nextpass problem: Signal's own inbox sits at 48 candidates with `B46`'s drain non-convergence still open and its premise falsified, so the pile grows on both sides. **Fix shape:** tier the advisory by count, or state the count against a stated threshold, so "draining is overdue" is distinguishable from "one thing came in." |
| B67 | `confirmed` | P3 | **Nothing detects a second, rival index — the freshness check only ever looks at the one file it manages.** `checkIndexFreshness` reads exactly `.planning/INDEX.md` (`sweep.js:68-70`); `isForeignIndexFormat` (`planning-index.js:131-138`) judges **that one file's** format and downgrades a hand-written one to advisory. Neither notices that a *different* index-shaped artifact exists alongside it. **Found by hand at nextpass 2026-08-02:** `.planning/llms.txt` at **1182 lines**, still carrying four dead rows, against a **482-line** `INDEX.md` — two indexes disagreeing about the same corpus, with no way to know which an agent would read first (they had to grep across `mjs/ts/tsx/json/yml/md` to establish that nothing did). **The shape was anticipated and never checked for:** `references/doc-runtime-model.md:178` names `llms.txt` explicitly, as out-of-scope for discovery. Out-of-scope for generation is not the same as absent from the tree. **Fix shape:** flag a second index-shaped artifact in `.planning/` as a finding, since the failure is contradiction rather than staleness and the freshness check cannot express it. |
| B68 | `confirmed` | P3 | **The detected Epic/linear mode is never printed, so a project reads as Epic mode to a human and linear to Signal with nothing saying so.** The unimplemented half of `B53`'s fix shape (part 2 of 3), split out 2026-08-02 when that entry was reconciled to `fixed` — parts (1) and (3) shipped, this one did not, and folding it into a closed row would have hidden it. `detectMode` (`tools/lib/state.js`) is called by **no renderer**: `tools/lib/status.js` never imports it, and `tools/lib/resume.js` names it only in a comment (`:146`). It is named in exactly one command file, `ship.md`. So `/sig:status` and `/sig:resume` both print a briefing whose artifact resolution, retro gate and archive eligibility all depend on a mode neither one shows. **Live instances today:** `traction-engine` (`current_epic: "PHASE12"`) and `agent-tools-sync` (`current_epic: "M1"`) — both non-strict, both therefore linear to every resolver, both reading as Epic mode to anyone who opens STATE.md. M5.E16's sweep check `(h)` (`epic-id-not-strict`) reports the contradiction, but only when someone runs `/sig:sweep`; the two commands people actually run at orientation stay silent. **Fix shape:** call `detectMode(state)` in `renderResumeBriefing` and the `/sig:status` renderer and print the mode on the `Phase:` line. **Sequencing:** natural fit for **M5.E18**, which is re-measuring the linear/Epic split across the corpus anyway. |
| B69 | `confirmed` | P3 | **The SHIP retro write-guard cannot evaluate a project whose `current_epic` is non-strict, and says nothing — the third behaviour on a field everyone assumed had two.** `checkProposedStateWrite` reaches `expectedRetroPath({current_epic})` → `deriveRetroPath`, which **throws** on any value not matching `M{N}[.{N}]*.E{N}` (`tools/lib/retrospective.js:149`). The `PreToolUse` hook wraps that call in a bare `catch` and exits 0 (`hooks/check-state-write.js:116-122`). **The fail-open is deliberate and correct** — the comment says so, and a `PreToolUse` hook must never crash a normal edit. **What is wrong is the silence:** the guard did not pass, it *could not run*, and nothing distinguishes those. **`B42` enumerated two behaviours for this field** — Layer 1 hard-halts on a missing Epic, *"Layer 2 fails open — `checkProposedStateWrite` returns `{block:false}` on a null/absent `current_epic`"*. A **truthy but non-strict** value is neither: it throws. **Verified by execution 2026-08-02** against `agent-tools-sync` (`current_epic: M1`, `phase: SHIP`, all six pre-SHIP phases present in `completed_phases` — every precondition met): `checkProposedStateWrite` threw `deriveRetroPath: malformed epicId "M1"`. `traction-engine` does **not** trip it, and only by accident — its `phase` is a prose blob rather than the literal `SHIP`, so the function returns at the first gate. **Exactly `B63`'s shape one layer over** — *could not apply* rendered identically to *already clean* — which is why it belongs with **M5.E18**'s port of M5.E16's four-status model rather than as a standalone fix. **Fix shape:** have `checkProposedStateWrite` catch the malformed-ID case itself and return a distinct `{block:false, unevaluated:true, cause:"non-strict-current-epic"}`, and have the hook surface it as a warning instead of swallowing it — the same *needs a person vs. clears itself* split `/sig:sweep` already ships. |
| B70 | `confirmed` | **P1** | **`/sig:status` and `/sig:resume` throw outright on a project whose `phase` is not one of the seven canonical names — 5 of 12 real projects, measured.** `nextActionForPhase` throws `unknown currentPhase "…"` on anything outside `PHASES` (`tools/lib/status.js:46-50`). **Neither command carries a fail-open instruction for it, and every neighbouring optional read does** — verified by reading both files: `isStaleVsOrigin`, `readLayoutBanner`, `readStateSizeForTier` and `readEffectiveProfile` are each marked *fail-open* or *advisory* in `resume.md` and `status.md`; `nextActionForPhase` is the one call nobody marked safe. In `resume.md:128` it sits **inside the argument list of `renderResumeBriefing`**, so a literal execution throws before the render is entered; `status.md:175` computes it at §2.6. **Scope of the evidence, stated rather than blurred:** the throw is **measured**; the user-facing consequence is **read, not run** — these are agent-executed prose files, and what a given agent does when an instruction throws is not determined by the file (M5.E8's exact subject). So the defensible claim is *the orientation commands lose their next-action step, and nothing in either file tells the agent to continue without it* — not *the user is guaranteed to see nothing*. **Nothing upstream stops the bad value getting there:** `readState` returns `phase` verbatim without validating it against `PHASES`, and the `PreToolUse` frontmatter-shape guard only ever inspects `completed_phases` — `checkStateFrontmatterShape` calls `sectionLines('completed_phases')` and nothing else, and `sectionLines` returns `null` for any **inlined** scalar (`tools/lib/retrospective.js`), so a multi-line prose `phase:` is invisible to it by construction. That is `B8`'s vector-1 class on a field the de-prose work never covered. **Measured 2026-08-02, not cited** — every `/Users/macstudio/dev-biz/*/.planning/STATE.md` read through `readState`, then `nextActionForPhase(state.phase, [])`: **7 ok · 5 crash · 1 `readState` throws** (13 total, one of them Signal). Crashing: `traction-engine`, `operator-os`, `operator-os-lean-loop`, `mps`, `agent-builder` — every one of them a hand-maintained linear project that parked narrative in the `phase` scalar. `affiliate-mojo` never gets that far (`readState` throws). So **6 of 13 real projects cannot run the two orientation commands**, and M5.E16's corpus table already recorded the input (*"Canonical `phase`: 7 of 12"*, `M5.E16-RESEARCH.md:54`) without anyone executing the consequence. **`P1`, matching `B42`'s precedent:** not degraded behaviour — two documented commands refusing to run at all against the majority mode. **Fix shape (decide, do not assume):** the cheap half is for both commands to tolerate a non-canonical `phase` — render the raw value and say *"not a recognised phase, so there is no next action"* rather than throw; the real question is whether `readState` should quarantine an off-enum `phase` the way `partitionCompletedPhases` (the `B45` fix) already quarantines an off-enum ledger entry, which would give one answer for both fields. **Sequencing: M5.E18.** It is the same population — the 8-of-12 linear projects the archive half does not reach — and fixing the archive for projects whose `/sig:resume` throws would be shipping the second half of a door. |

**M5.E4 close-out (v0.1.9, 2026-07-20) — the confirmed-bug backlog cleared.** All 12 in-scope confirmed bugs fixed (or dismissed), RED-first, in Epic M5.E4:
- **B5** → `fixed` (`b5f3631`): flat `eslint.config.js` — `npm run lint` runs clean (0 errors).
- **B6** → `fixed` (`8fbdecf`): resume/status banners gate on genuine drift (`HEAD..origin`), not the bookkeeping "+1".
- **B8** → `fixed`: both halves shipped v0.1.8 (`WHOLE_FILE_NOTE` + vector-1 de-prose); M5.E4 added the command-level `block:true→false` flip assertion in `migrate-all-vectors.test.js`.
- **B14** → `fixed` (`b5ed3f0` + REVIEW completion): shared `path-confine.js` realpath guard at evict/add/resume (add.js reversal ratified, D-M5E4-5). **REVIEW caught an incomplete fix at the `evict.js` site** — it passed the *directory* to the guard, so the guard resolved only the archive parent and a directory symlink at the Epic *leaf* still escaped (a false-green test planted the symlink one level too high). Completed in REVIEW: anchor the guard on the *file* path (matching `archive-tree.js`) + a RED-first leaf-symlink test.
- **B15** → `fixed` (`cb4cbf9`): the blocking dangling scanner reads the full file (no 1 MB truncation).
- **B16** → `fixed` (`9d36aa4`): the pre-apply tag is deleted after a successful rollback.
- **B17** → `fixed` (`0c284b9`): `vitest.config.js testTimeout: 15000` for git-heavy suites.
- **B18** → `fixed`: already `8f1dda8` (M5.E3); M5.E4 `f810acb` added the `regeneratePlanningIndex('.')` round-trip guard.
- **B19** → `fixed` (`dd5c1c5`): marker-based foreign-INDEX guard (the shipped legend-based one was a false-green) + dry-run parity; RED-proven against `main`.
- **B20** → `dismissed`: duplicate of B5 (its "no B5 entry exists" premise was false).
- **B21** → `fixed` (`0505cab`): the D-ID map is built once for the anchor-gate loop.
- **B22** → `fixed` (`cd7504d`): `checkInternalLinks` bounds the target to repo root before any disk touch.
- **B23** → `fixed`: coverage bundle — (a) `8fec7bf` (organic case proven unreachable + real-gate coverage), (b) `cb4cbf9` (>1 MB fixture), (c/d) `d554861`, nits `f67196f`.

Newly discovered / surfaced during the Epic, all deferred to v0.1.10: **B24** (pre-existing dangle blocks migrate), **B25** (FR5 AC5.2 test-strength — read-enclosure correct-by-inspection), **B26** (`shipFR1Check` Epic-close detection skips on the self-hosted flow).

**M5.E5 close-out (v0.1.10, 2026-07-21) — the M5.E4 carry-overs cleared.** All 3 confirmed carry-overs + the B6 local-stale refinement fixed RED-first in Epic M5.E5 (FULL/strict, sequential dispatch, suite 1529 → **1561**):
- **B24** → `fixed` (`501ad9d`): `computeDanglingDelta` re-keyed on the **resolved absolute target** with multiset/count semantics — a pre-existing closed-block dangle survives the append-log move+reroot; dry-run/apply agree; the gate still catches migrate-introduced dangles (the multiset test was verified to fail against a `Set` swap). Named limitation documented in-code.
- **B25** → `fixed` (`8cbb8b7`): behavioral read-enclosure **interleaving test** + `_afterRead` seam across all 6 RMW wrappers; the new test genuinely fails on a read-outside-lock twin (a permanent anti-tautology guard: twin asserts `lostUpdate===true`, real wrapper `false`).
- **B26** → `fixed` (`d62e5f2`): STATE-based Epic-close fallback (tier-aware via `PHASES − phases_skipped`, **row-absence-gated**) in Layer 1 (`shipFR1Check`) + Layer 2 (`checkProposedStateWrite`). **Layer 3 descoped (ratified)** — `detectDirtyExecute` runs at EXECUTE, the fallback exists at SHIP (empty intersection = no-op); the harm is fully closed by Layers 1+2; the resume-nudge enhancement → `ISSUES-INBOX.md`.
- **B6 refinement** → `fixed` (`35f6a04`): `isStateStale` local-stale tightened by **file identity** (new `BOOKKEEPING_PATHS` = STATE + CONTEXT drives Walk 2's exclude) — a committed-but-unrolled PLAN/PROGRESS now nudges; count-independent (the reviewer's `commits.length===1` candidate rejected). `STATE_AFFECTING_PATHS` byte-unchanged.

Surfaced during the Epic, deferred: **B27** (needs-triage) — archive-doc *inline* `](*.md)` link to an FR6-renamed target aborts migrate (R7 tension; a design call).

*0 needs-triage · 24 confirmed · 1 dismissed · 45 fixed (70 total). Last updated: 2026-08-02 (**status-column reconcile against the code**, fix lane, no Epic. STATE.md queued this as *"26 rows read `confirmed`; B42, B44, B45 and B53 are verifiably fixed."* Checking all 26 — plus every `needs-triage` and `dismissed` row, because the status column is the thing being reconciled and no one had said the others were right — found **17 wrong rows, not 4**. **Eleven were fixed and never flipped:** B39, B41, B42, B43, B44, B45, B48, B51, B53, B54 (all M5.E9 or M5.E13, each carrying its bug number in the code beside the fix) and **B36**, which was sitting at `needs-triage` despite being closed by M5.E13 S4.t1. STATE's framing that *"the linear-mode rows are exactly the stale ones"* is half right: B41–B45 are linear-mode, but B39/B48/B51/B53/B54 are M5.E13 rows and were equally stale. **B20 was mis-statused `dismissed`** — a real bug, duplicate of B5, and fixed (`eslint.config.js` exists, `npm run lint` is clean). **Five `needs-triage` rows triaged to `confirmed`** against the code — B32 (widened to 17-vs-19), B33, B34, B35, B60 — leaving the `needs-triage` bucket empty for the first time. **New: B68**, the unimplemented half of B53's fix shape, split out rather than buried in a row now marked `fixed`. Every flip carries a file:line or a test that fails if it regresses; nothing was flipped on STATE's say-so. Verified clean at 1938 tests. Prior: 2026-08-02 (**B63**–**B67** — five findings from a nextpass session report, reviewed against the code the same day and all `confirmed` by reading it. **B63**: `/sig:migrate-memory`'s dry-run prints `0` for its two Epic-only vectors on a linear project, so *could not apply* is byte-identical to *already clean* — `C1`'s class in the command next door, live the same week M5.E16 fixed it for `/sig:sweep`. **B64**: `senseArchiveTree` proves closure by a retro **file existing** and discards the `isStub` flag `enumerateRetros` already computed, so a `[FILL IN]` placeholder archives a live Epic. **B65**: `B57`'s `.migrate` ignore — correct for noise, 12 structural findings → 2 — means nothing now reports the snapshot's age or whether it can still roll back. **B66**: `checkStaleInbox` fires at `n > 0` with no magnitude, which is what turned a deliberately-filed nextpass tripwire into indistinguishable entry #110. **B67**: nothing detects a *second* index (`llms.txt` at 1182 lines contradicting a 482-line `INDEX.md`). **Count correction made in the same pass:** this tally read `5 · 18 · 2 · 30 (55 total)` against a table holding **62** rows — stale by seven since `B56` landed, and itself an instance of `B56`'s class (a published count nothing derives from the artifact). Re-derived by counting the rows, twice. Prior: 2026-07-29 (**B55** — the adherence canary's control arm was never isolated ACROSS FILES: only the target command is mutated, while `transitionPhase` is named 4× each in `plan.md`/`verify.md`/`review.md`. Found by the harness's first real use; `B41-phase-entry` re-ran **3/3 treatment vs 1/3 control → INDETERMINATE**, replacing M5.E8's `OBEYED`. **M5.E13 S2.t4 amplified it by design** — FR1.1's single-shared-wording requirement puts a byte-identical copy of the deleted instruction in the sibling files. M5.E8's verdict is not falsified but was **unisolated**: clean by luck, not construction. Prior: **B46 PREMISE FALSIFIED at M5.E13 EXECUTE — re-triage required, NOT fixed.** B46 says *"M5.E7's 45 dispositions were written to a side artifact and never stamped back into `ISSUES-INBOX.md`, so 47 candidates resurface identically at every future drain."* Measured against both files: `M5.E7-DISPOSITIONS.md` holds **66 data rows** (not 45) and the inbox holds **48** candidates (not 47) — but the decisive fact is that **0 of the 48 inbox candidates correspond to any disposition row**. The disposition table's population is the **v2 feature-port candidates** (§3 is explicitly *"`MILESTONE-5.md` § 'Candidate v2 feature-port scope' — 23 items"*, plus §4's seed-five and §5's Signal-native N-items). All five of its references to the inbox cite it as **evidence of demand** — *"captured verbatim in `ISSUES-INBOX.md`, its single home"* (:156), *"Register cluster I — `ISSUES-INBOX.md:1475`"* (:161) — never as a target to be stamped. **So FR2.2's stamp-back would write verbs from one population onto a different one: not a data correction but a data corruption,** and precisely the hazard M5.E9 named when it refused a blanket defer (*"would have written wrong verbs over already-decided entries"*). **Work stopped rather than forced; S3.t2 left open.** The residual complaint is real — 48 candidates do resurface at every drain — but its cause is not un-stamped dispositions, so the remedy is a scope call, not an implementation detail. *Incidentally confirmed that dispositions are actionable and simply were not walked:* row **N4** reads *"`B39` — implement the trigger-watchlist walk | **BUILD**"*, which M5.E13 S3.t1 built — reaching it via the bug ledger, not via the disposition table. Prior: **B54** cataloged at M5.E13 **PLAN** — `checkGateArtifacts` is the never-called-guard class's **fourth** instance and the only one that is **wrong if wired up**: nothing calls it, yet executed against Signal it returns `{ready:false, missing:['REQUIREMENTS.md']}` because it hardcodes the unprefixed name while Epic mode writes `M5.E13-REQUIREMENTS.md`, and four of its five gates are vacuous empty lists. Two docs describe it as part of the flow and a third records a **fix once applied to it**. **Being uncalled is what protected its bug from discovery.** Disposition ratified same day: **replace** (delete + rebuild the refusal in `transitionPhase`), per M5.E9's remove-don't-re-tune precedent. **Corrects D-M5E13-3's locked count of three to four.** Prior: 2026-07-28 (**B53** cataloged — a **non-strict `current_epic` splits artifact WRITE naming from READ resolution**: `artifactName` uses the strict shape and emits `1-PLAN.md`, `resolveArtifactPath` uses a lenient regex and resolves `PHASE11-PLAN.md` **first**, so a fresh write is shadowed by a stale file — while `references/epic-native-flow.md:31` guarantees the two "are symmetric." Verified by executing both against `traction-engine`'s live `current_epic: PHASE11`. Its origin is a **`B50` sighting in a downstream project**: a session predicted `/sig:ship` would halt with `NO_CURRENT_EPIC`, and executing v0.1.13 **and** v0.1.11 shows neither halts — `PHASE11` is truthy, so the pre-`B42` branch never fired. *Counts corrected in the same pass: the tally still read `12 confirmed / 49 total` after **B50**–**B52** landed.* Prior: **B49 FIXED in v0.1.13** — `package.json` and the plugin manifest disagreed on the version; both now read `0.1.13`. The consistency *assertion* is still open. **B36 sighted live a third time** at M5.E8's SHIP: the FR1 retro gate skipped because `MILESTONE-5.md` still carried E8 as `▶ NEXT` — the retro existed only because it was written before the gate ran. Prior: **B48** cataloged at M5.E8 **EXECUTE** — `execute.md`'s phase-entry instruction is unconditional, and an agent **explicitly refused it** rather than write a false record into the phase log; the instruction and M5.E9's own ledger-integrity rules are in direct conflict, and obeying either one breaks the other. Found by reading an adherence-run transcript, **not** by the harness's verdict — that run scored `ABSENT`, which was a fact about the fixture. Prior: **B47** cataloged at M5.E9 **REVIEW** — `/sig:resume` reads `0/7 phases done` right after a linear ship, a cosmetic consequence of FR5's trim, deliberately deferred because the fix is a new capability. Prior: **B46** cataloged at M5.E9 **PLAN** Step 1b — M5.E7's 45 dispositions were written to a side artifact and **never stamped back** into `ISSUES-INBOX.md`, so 47 candidates resurface identically at every future drain, `abandon` verdicts included; `confirmed` by grepping both files. Homed in M5.E9 as a **later slice, not v0.1.12** — a blanket defer would have written wrong verbs over already-decided entries. Prior: 2026-07-26 — **four findings cataloged from a `nextpass` ship report**, all triaged to `confirmed` by reading the code the same day — **B42** (a project running in linear mode cannot run `/sig:ship` at all; the FR1 gate hard-halts on `current_epic: null` with no bypass, and the halt is *asserted* by `tests/ship-fr1.test.js` — **P1**, live since v0.1.3), plus **three distinct defects stacked in the same seven lines, `state.js:388-395`**: **B43** (it records the phase being *left*, so a SHIP date is unrecordable by construction — SHIP is terminal — **P2**), **B44** (set semantics applied to log data: the dedupe collapses a multi-unit `completed_phases`, silently, with no diff/warning/count — **P2**), **B45** (existing entries are never validated, so a junk line becomes a permanent phantom phase key — **P2**). B44/B45 are **not linear-mode-only**: Epic mode is spared only because `setCurrentEpic` zeroes the list on every roll. All three sequence **before or with B41**, which would otherwise turn four more commands into collapse sites. A writer audit run at the same time clears the other frontmatter mutators — `transitionPhase` is the only one that rebuilds an array rather than patching named fields (detail in B44). A fourth item in the same report — `npm run ship:archive` archiving future-slice briefs — is **not Signal's**: that script is `scripts/ship-archive.mjs`, built inside the reporting project's own DOC1 slice (see `examples/Example-cmmc-STATE.md:101`); Signal ships no archive-on-SHIP command. Prior: M5.E7 **REVIEW** — **B41** (four phase commands never call `transitionPhase`; `markFresh` then stamps the stale position fresh — **P2**, confirmed by grepping `commands/*.md` and reading `transitionPhase`) cataloged; homed in **M5.E9**. Prior: M5.E7 EXECUTE Wave 2 — **B39** (trigger watchlist never walked; `/sig:plan` has no such drain step — **P2**, root cause of the register's dead-trigger finding) and **B40** (citation integrity in `analysis/` unenforceable by construction) cataloged, both triaged to `confirmed` by reading the code. Prior: M5.E7 cataloged **B37**/**B38**; M5.E6 SHIP cataloged **B36**).*
