# M5.E15 — Plan

**Epic:** M5.E15 — *the control arm, made real (`B55`)* · **Tier:** FULL · **Written:** 2026-08-04
**Requirements:** [`M5.E15-REQUIREMENTS.md`](M5.E15-REQUIREMENTS.md) · **Research:** [`M5.E15-RESEARCH.md`](M5.E15-RESEARCH.md)
**Decisions:** `D-M5E15-1 … D-M5E15-9`

## Phase goal

Make the adherence harness's control arm delete the instruction everywhere a command **orders** it,
prove that independently of what the canary declared, and re-run `B41-phase-entry` under the fixed arm
— publishing whatever verdict comes back.

---

## What this Epic does for the first time (M5.E17 FR1)

Two shipped code paths have **never run for their real purpose**, and both go in wave 1.

**1. `applyDeletion` — the line-level deleter — is dead code in production.**
`tools/adherence-run.js:378` is the false branch of `canary.deleteSection ? … : …`. The registry holds
one canary and it declares `deleteSection`, so the branch has never executed in a measurement. Its only
exercise is three unit tests. FR1's `ship.md` line anchor is its **first real use** — and it is being
handed the one file in the corpus whose structure defeated the section primitive.

**2. `appendNotice` has no production caller.** Only `tests/adherence-append-only.test.js:140` calls
it, yet `ADHERENCE-LOG.md` carries three annotation blocks — one of which (`DIAGNOSED`) it cannot
produce, because `NOTICE_KINDS` is frozen to two values and it throws on the rest (**`B80`**). FR7 is
the first time an annotation reaches that log **from code**.

**Therefore S1 ‖ S2 are wave 1** — ahead of the leak walk, which is the work that looks more
important. If the line-deletion path or the notice path is broken, it breaks against a unit fixture in
wave 1 rather than against the live 6-run measurement in wave 4.

*Neither item is the new code this Epic writes. A newly-authored thing has no history to be wrong
about; the risk lives in machinery that already exists, was built for a purpose, and has never been
run for it. That is the distinction — and it is the distinction that made `B55` itself a wave-6 find
in M5.E13 instead of a wave-1 one.*

---

## Waves

| Wave | Slices | Why together |
|---|---|---|
| **1** | S1 ‖ S2 | The two first-executions. Independent files, no shared edit. |
| **2** | S3 ‖ S4 | The walk and the copy-exclusion touch different modules. |
| **3** | S5 ‖ S6 | Wiring + the caller guarantee. Both depend on 1–2. |
| **4** | S7 | The live measurement. Depends on everything. |

---

## S1 — The registry declares directive sites; `applyDeletion` goes live · **M** · wave 1

Covers **FR1**. No dependencies.

| Task | What |
|---|---|
| **t1** | Registry schema: `isolation: "directive"` + `deletions: [{file, section} \| {file, line}]`. Loader validates and **throws** naming the canary id when neither key is present (AC1.1). |
| **t2** | `B41-phase-entry` declares its five sites — `execute/plan/verify/review` by **section** (one byte-identical heading, verified), `ship.md` by **line** (AC1.2). |
| **t3** | `applyDeletions(src, entries)` dispatcher: `section` → `applySectionDeletion`, `line` → `applyDeletion`. Absent anchor throws; a `line` matching more than once throws (AC1.4). **This is `applyDeletion`'s first live call.** |
| **t4** | Anchor-occurs-exactly-once assertion widened from the singular field to the list, read against the **live corpus** (AC1.3) — extends `tests/adherence-verdict.test.js:217-220`. |
| **t5** | A `section` entry whose heading is not its own discrete section **fails** (AC1.5). RED first: assert against a fixture shaped like `ship.md`'s `### 5. Update State`. |

**Test strategy** — unit, TDD. t5 and t3's throw-paths are RED-first. t4 is a corpus test: it fails if
anyone edits a phase-entry heading, which is the drift it exists to catch.

**Done when:** the registry declares five sites, all five anchors are asserted unique against the real
files, and the line path executes.

## S2 — `appendNotice` gets its third kind and its first real call · **S** · wave 1

Covers **FR7** + **`B80`**. No dependencies. *Deliberately not deferred to wave 4: the "unisolated"
stamp is true regardless of what the re-run produces.*

| Task | What |
|---|---|
| **t1** | `NOTICE_KINDS` gains the kind the log already uses in prose (`DIAGNOSED`), with its icon. RED first: assert `appendNotice` accepts it and rejects an unknown kind (AC7.3). |
| **t2** | Append the **unisolated** annotation to M5.E8's `OBEYED` record, from code (AC7.1). |
| **t3** | Assert the amended record is byte-identical beneath its annotation and that the text states it is **not retracted** — 0/3 means no leak was *observed*; unisolated is not falsified (AC7.2). |

**Test strategy** — unit + a byte-identity assertion on the log, reusing
`tests/adherence-append-only.test.js`'s harness.

**Done when:** the log's three annotation kinds all exist in code, and the `OBEYED` record carries its
scope on its face.

## S3 — The independent leak walk, fail-closed · **M** · wave 2

Covers **FR2**. Depends on S1 (needs the declared shape to contrast against — but must not read it).

| Task | What |
|---|---|
| **t1** | `walkResidue(copyRoot, token)` — every file under every `PLUGIN_COPY_DIRS` entry, returning `{file, line, text}` hits (AC2.5). |
| **t2** | The descriptive allowlist, pinned by a test enumerating exactly the seven entries (AC2.3), with `tools/lib/state.js` annotated in place as **the capability, not a leak** (AC2.4). |
| **t3** | Classification: allowlisted → descriptive; **everything else → directive**. RED first (AC2.2): a fixture with a new file naming the token **fails the check**. |
| **t4** | AC2.1: a mutated tree with one declared directive site left undeleted fails, naming file and line. |

**Test strategy** — unit against fixture trees; no live agent. **The walk must not consult
`canary.deletions`** — a test asserts the check still fires when the deletions list is empty, which is
the property that stops this becoming `B55` again.

**Done when:** an unknown site stops a run, and a stale allowlist breaks loudly rather than quietly.

## S4 — The canary registry leaves the plugin copy · **S** · wave 2

Covers **FR4**. No dependencies.

| Task | What |
|---|---|
| **t1** | `createPluginCopy` omits `references/adherence-canaries.json`; the rest of `references/` is unchanged (AC4.1). |
| **t2** | A run still resolves the registry, because `loadCanaryRegistry(ROOT)` reads the real root (AC4.2). |
| **t3** | The **shipped** plugin still contains the file — this is a copy-time exclusion, not a packaging change (AC4.3). |

**Test strategy** — unit on the copy contents; one assertion against the packaged file list.

## S5 — Wire the walk into the run; stop the caveat regression · **M** · wave 3

Covers **FR3**. Depends on S1 + S3.

| Task | What |
|---|---|
| **t1** | Replace `mutated.includes(residue)` (`adherence-run.js:383-384`) with the walk. Directive residue **throws before any agent is invoked** (AC3.1). |
| **t2** | **`buildCaveats:666` branches on `canary.deleteSection`** — the field S1 replaces. Left alone it silently stops rendering, and the record quietly loses the sentence explaining a whole section was removed. Re-point at `deletions[]`, per-entry. RED first. |
| **t3** | Isolation scope + descriptive residue onto the record via the existing `caveats[]` path (AC3.3). A record rendered without an isolation scope **fails a test** (AC3.4). |

**Test strategy** — unit. t2 is a regression test that fails against the S1 shape before the fix.

## S6 — The leak check has a caller, by a named assertion · **S** · wave 3

Covers **FR5**. Depends on S3.

| Task | What |
|---|---|
| **t1** | A **bespoke named assertion** in `tests/guard-callers.test.js` — `B54`'s shape, already the precedent in that file — asserting the leak check is invoked by something other than itself (AC5.1). |
| **t2** | A note in that file's scope-limit header recording that the leak check is governed **by name, not by the auto-discovered population**, and why (`B81`). |

**⚠ `D-M5E15-5` does not land as written, and this is the corrected task.** `findCliGuards` detects
one of three argv idioms; `adherence-run.js` contributes **zero** of its twelve flags to the governed
population. Registering there automatically would have appeared to work while checking nothing.
Measured and filed as **`B81`** (P2). **Do not widen the regex here** — the 10 orphans it surfaces are
mostly options, not guards, and the missing definition is a separate piece of work.

**Do not break** the header's pinned strings: the AC2.4 assertion at `:158-166` requires
`'2 of the 4 known instances'`, `SCOPE LIMIT`, `B39`, `B46` and `'NOT a check for the'` to remain
present. M5.E15 adds no new instance of that class, so the count stays correct; the note is additive.

## S7 — The live re-run, and the verdict is published · **L** · wave 4

Covers **FR6**. Depends on S1–S6.

| Task | What |
|---|---|
| **t0** | **Confirm the `claude` CLI is reachable before starting.** The only non-offline step in the Epic. |
| **t1** | Run ≥ 3 per arm under the directive-scoped arm, seam precondition passing (AC6.1). |
| **t2** | `appendRunRecord` with `isolation: directive` + the descriptive residue (AC6.2). |
| **t3** | Publish whatever the verdict is (AC6.3). |

**Pre-declared readings — fixed here, before the run:**

| Result | Reading |
|---|---|
| `OBEYED` | The rule works, isolated at directive scope. The first verdict this harness has produced that means what it says. |
| `INERT` | M5.E9's phase-entry instruction — four commands, FR1.1's shared wording — changes nothing. **Published** (`D-M5E15-6`). |
| `INDETERMINATE` again | The rule is **multi-homed beyond what deleting instructions can isolate** (`D-M5E15-9`). Recorded permanently with the residue list. **Does not auto-escalate to corpus scope** — that would silently overturn `D-M5E15-1`. |
| Run cannot complete | `AgentUnreachableError` → *could not measure*, never *measured zero*. **Blocks the close** (AC6.4). |

**Test strategy** — this slice is a measurement, not code. Its artifact is a log record. AC6.4 is the
testable part: a run that cannot complete must block rather than emit.

---

## Dependencies

```
S1 ─┬─> S3 ─┬─> S5
    │       └─> S6        ──> S7
S2 ─┘                     ──> S7
S4 ───────────────────────────> S7
```

## Environment

Node **v22.13.0** (manifest: 22+, no drift). No new dependencies; nothing native. Suite green at
**2168 / 129 files**. The one real environment risk is S7's live `claude` CLI — checked at t0, not
mid-run.

## Complexity

S1 **M** · S2 **S** · S3 **M** · S4 **S** · S5 **M** · S6 **S** · S7 **L** — 7 slices, 4 waves.
