{
  "$comment": [
    "Byte ceilings for ORIENTATION documents \u2014 the ones read at the start of a session, whose size",
    "is paid in context on every single run. Enforced by tests/doc-budgets.test.js.",
    "",
    "This is the 'promote a rule into a lint' step (see CLAUDE.md > Working in this repository >",
    "House rules and how they become checks). CLAUDE.md has been over the advisory 40 KB nudge for",
    "some time and the nudge changed nothing, which is the case for a ceiling that fails instead.",
    "",
    "TWO CATEGORIES, AND THE SPLIT IS THE WHOLE DESIGN.",
    "",
    "orientation \u2014 a ceiling plus a ratchet. These files are read to get oriented, so their cost is",
    "  recurring. A file at or under `target` is healthy. A file above it is `grandfathered` at its",
    "  size when the rule was adopted: it may SHRINK freely and may NOT grow. That is what makes the",
    "  rule adoptable today on a corpus that already violates it \u2014 the alternative, a flat limit,",
    "  fails on day one and therefore never gets adopted at all.",
    "",
    "exempt \u2014 append-only ledgers. A growth ceiling on these is simply WRONG: BUGS.md is supposed to",
    "  grow when a bug is found, and a check that fires on correct behaviour teaches people to ignore",
    "  checks. Their size is managed by ARCHIVING (/sig:docs-archive, /sig:docs-migrate eviction),",
    "  not by a limit. They are listed rather than merely absent so that the population test can tell",
    "  a deliberate exemption from a file nobody has considered.",
    "",
    "exemptPatterns \u2014 per-unit work artifacts (an Epic's PLAN/PROGRESS/SUPPLY docs, a milestone spec,",
    "  a dated review snapshot). These are outputs of one piece of work, not things read to orient, and",
    "  enumerating them would add manifest churn to every Epic for no reader. Matched on basename."
  ],
  "slackBytes": 4096,
  "populationThresholdBytes": 32768,
  "orientation": {
    "CLAUDE.md": {
      "target": 40960,
      "ceiling": 40960,
      "grandfathered": false,
      "reason": "De-bloated 2026-09-02: 51.4 KB -> 25.2 KB by removing per-release narrative duplicated from CHANGELOG.md and the retros. Now under target, so the grandfather flag is dropped and the ceiling is the target. The ratchet did its job \u2014 it failed the suite the moment the file shrank, which is how the improvement gets locked in rather than quietly re-spent."
    },
    ".planning/CONTEXT.md": {
      "target": 40960,
      "ceiling": 56600,
      "grandfathered": true,
      "reason": "Accumulates a dated 'where things stood' section per Epic and evicts none of them. The oldest are reachable from the retros. Ratcheted 64303 -> 56600 on 2026-09-04 at the M6.E6 close: the file was AT its ceiling, so recording a new Epic was impossible without evicting an old section \u2014 which is the policy this reason already described and nothing had ever performed. The 2026-08-19 section moved verbatim to archive/M6/E3/CONTEXT-2026-08-19.md (relocate-never-delete). Set with ~2 KB of headroom inside the 4096-byte slack window rather than at the exact byte size, per the STATE.md precedent where an exact ratchet failed on the very next ordinary edit."
    },
    ".planning/STATE.md": {
      "target": 40960,
      "ceiling": 47928,
      "grandfathered": true,
      "reason": "Carries inlined narrative alongside the authoritative frontmatter. Ratcheted 48659 -> 47928 on 2026-09-03 when M6.E5's closed-Epic narrative was evicted to RETROSPECTIVES.md at the M6.E6 open. The first ratchet set the ceiling to the EXACT byte size, and the very next frontmatter write -- recording two completed phases, about 40 bytes -- failed the suite. This manifest's own comment says ratcheting to exact size would fail on any ordinary edit; that applies to a grandfathered file too. Re-set with headroom inside the 4096-byte slack window, which is what that window is for. A later shrink still re-tightens it."
    },
    "README.md": {
      "target": 30720,
      "ceiling": 30720,
      "grandfathered": false,
      "reason": "Within budget. Listed so it stays that way."
    },
    ".planning/PROJECT.md": {
      "target": 40960,
      "ceiling": 40960,
      "grandfathered": false,
      "reason": "The v1 spec, named in CLAUDE.md as orientation reading. Within budget; listed so it stays that way."
    }
  },
  "exempt": {
    ".planning/BUGS.md": "Append-only ledger. Growth on a new bug is correct behaviour; size is managed by archiving.",
    ".planning/DECISIONS.md": "Append-only ledger \u2014 explicitly so; a reversal is a new entry, never an edit.",
    ".planning/BACKLOG.md": "Append-only queue; rows are struck rather than deleted so provenance survives.",
    "CHANGELOG.md": "Append-only release record.",
    ".planning/INDEX.md": "Generated by /sig:docs-index at every phase transition; its size is an output, not a choice.",
    ".planning/ADHERENCE-LOG.md": "Half generated by tools/adherence-ceiling.js.",
    ".planning/ISSUES-INBOX.md": "Capture inbox, drained at PLAN; a stale-inbox check already covers it.",
    ".planning/STATE-HISTORY.md": "Append-only history of STATE.md; its whole purpose is to accumulate."
  },
  "exemptPatterns": [
    {
      "pattern": "^[A-Z]\\d[A-Za-z0-9.]*-.+\\.md$",
      "reason": "Per-unit Epic artifact (PLAN / PROGRESS / VERIFICATION / REVIEW / SUPPLY / RETROSPECTIVE). An output of one piece of work, not orientation reading."
    },
    {
      "pattern": "^MILESTONE-.+\\.md$",
      "reason": "Milestone specification \u2014 read when working that milestone, not at every session start."
    },
    {
      "pattern": "^BACKLOG-REVIEW-\\d{4}-\\d{2}-\\d{2}\\.md$",
      "reason": "Dated review snapshot; frozen once written."
    }
  ]
}
