# Linear-mode plan (fixture)

A phase-named artifact (`1-PLAN.md`) as a linear-mode project writes them. In
Epic mode this would be `{EpicID}-PLAN.md`; the resolver must still find THIS
one when `current_epic` is null (M4.5.E11.S1.t6 byte-identity anchor).
