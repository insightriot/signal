# M4.5.E99 — Plan (fixture)

On-disk fixture for resolveArtifactPath's Epic-prefixed (pattern-0) resolution
test. An Epic-prefixed artifact name that the legacy numeric/no-prefix/phase
patterns would never find — proving the real-existsSync path, not just the
injected existsFn.
