# M1.E4 Verification

**Verdict: FAIL** — the expired-session path is not covered.

| Requirement | Evidence |
|---|---|
| FR1 | `login.test.js` — form submits and redirects |
| AC1.1 | `login.test.js` — valid pair accepted |

Two of the four declared requirements are absent from this table. A FAIL verdict
keeps the unit **open**, so this shape exercises the coverage diff without
touching the archive surface.
