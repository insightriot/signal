# M1.E4 — VALIDATION

The self-contradicting shape. The two sections below disagree about the same
data, and nothing outside this file has to be consulted to see it — which is
what makes it FR2's case rather than FR1's.

Two disagreements, both deliberate: `AC1.2` is assigned to S2 in dimension 2 and
to S1 in the Nyquist map, and `NFR1` has an owning slice but no test row.

Everything explaining the fixture is in this preamble on purpose. Prose inside a
compared section becomes indistinguishable from data — a sentence mentioning a
requirement would make it look covered.

## 2. Completeness — every requirement has an owning slice

| Requirement | Owning slice |
|---|---|
| FR1 | S1 |
| AC1.1 | S1 |
| AC1.2 | S2 |
| NFR1 | S2 |

## 9. Nyquist map — every criterion has a test

| Requirement | Slice | Test |
|---|---|---|
| FR1 | S1 | `login.test.js` |
| AC1.1 | S1 | `login.test.js` |
| AC1.2 | **S1** | `session.test.js` |
